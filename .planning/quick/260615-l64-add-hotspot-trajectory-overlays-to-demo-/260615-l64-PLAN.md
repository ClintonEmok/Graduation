---
id: 260615-l64
description: Add hotspot trajectory overlays to demo map and 3D views
status: ready
tasks:
  - id: 1
    description: Add map trajectory overlay
    files:
      - src/components/map/MapHotspotTrajectoryLayer.tsx
      - src/components/dashboard-demo/DemoMapVisualization.tsx
    action: Render hotspot centroid paths on the demo map using STKDE sliceResults so motion is visible without textual explanation.
    verify: |
      - overlay appears only when sliceResults contains 2+ slices
      - paths and nodes are drawn from tracked hotspot snapshots
  - id: 2
    description: Add 3D hotspot trajectory overlay
    files:
      - src/components/viz/HotspotTrajectoryOverlay.tsx
      - src/app/stkde-3d/components/Stkde3DScene.tsx
    action: Render tracked hotspot paths in the cube using projected scene coordinates and slice stack Y positions.
    verify: |
      - overlay is visible in the 3D demo view when multiple slices exist
      - path geometry updates when stkdeResponse changes
  - id: 3
    description: Keep summary card and reuse shared evolution data
    files:
      - src/lib/hotspot-evolution.ts
      - src/hooks/useHotspotEvolution.ts
    action: Reuse the existing evolution utility and hook without introducing a separate answer panel or explicit state labels.
    verify: |
      - no additional answer text is added beyond the existing summary card
      - overlay rendering derives from the same tracked hotspot snapshots
---
