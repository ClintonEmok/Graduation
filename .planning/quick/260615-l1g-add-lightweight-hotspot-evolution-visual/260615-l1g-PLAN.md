---
id: 260615-l1g
description: Add lightweight hotspot evolution visualization to demo dashboard
status: ready
tasks:
  - id: 1
    description: Create hotspot evolution utility function
    files:
      - src/lib/hotspot-evolution.ts
      - src/lib/hotspot-evolution.test.ts
    action: Write pure utility that takes sliceResults from STKDE response and produces tracked hotspot paths with centroid displacement, extent change, and stability (stable/transient/displacing)
    verify: |
      - function `buildHotspotEvolution(sliceResults)` returns `TrackedHotspot[]`
      - each tracked hotspot has: id, snapshots (centroid, support, radius per slice), displacementKm, stability status
  - id: 2
    description: Create useHotspotEvolution hook
    files:
      - src/hooks/useHotspotEvolution.ts
    action: Write hook that reads stkdeResponse from coordination store and computes hotspot evolution
    verify: |
      - hook returns { trackedHotspots, isAvailable, totalChange }
      - re-computes when stkdeResponse changes
  - id: 3
    description: Create HotspotEvolutionCard component
    files:
      - src/components/dashboard-demo/HotspotEvolutionCard.tsx
    action: Write a card component that shows tracked hotspot paths with displacement, support trend, extent change, and stability label
    verify: |
      - renders displacement in km
      - shows support trend per snapshot
      - shows stability status with color coding
  - id: 4
    description: Integrate into demo dashboard
    files:
      - src/components/dashboard-demo/DemoStkdePanel.tsx
    action: Add HotspotEvolutionCard below the existing hotspot list, gated on sliceResults being available and having 2+ slices
    verify: |
      - card appears when stkdeResponse has sliceResults with 2+ entries
      - card is hidden when fewer than 2 slices or no sliceResults
---
