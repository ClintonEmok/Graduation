---
phase: 66-full-integration-testing
plan: 02
type: execute
wave: 2
depends_on: [66-01]
requirements_addressed: [FLOW-04, FLOW-06, FLOW-01, FLOW-02, FLOW-03, FLOW-05]
files_modified:
  - .planning/phases/66-full-integration-testing/66-SIGNOFF.md
  - .planning/phases/66-full-integration-testing/66-VALIDATION.md
autonomous: false

must_haves:
  truths:
    - "A first-time analyst can complete blocker journeys in dashboard-v2 without route hopping"
    - "Two consecutive clean blocker-journey runs are recorded before sign-off"
    - "Performance and stability checks are explicitly documented with pass/fail evidence"
    - "Phase sign-off remains blocked if any blocker journey fails"
  artifacts:
    - path: ".planning/phases/66-full-integration-testing/66-SIGNOFF.md"
      provides: "Single source of truth for blocker journeys, run protocol, and evidence logging"
      contains: "two consecutive clean runs, FLOW-01..FLOW-06 mapping, blocking policy"
    - path: ".planning/phases/66-full-integration-testing/66-VALIDATION.md"
      provides: "Nyquist validation state for wave 2 checks"
      contains: "66-02-01, 66-02-02 status rows"
  key_links:
    - from: ".planning/phases/66-full-integration-testing/66-SIGNOFF.md"
      to: "FLOW-01..FLOW-06"
      via: "explicit mapping table"
      pattern: "FLOW-01|FLOW-02|FLOW-03|FLOW-04|FLOW-05|FLOW-06"
    - from: ".planning/phases/66-full-integration-testing/66-SIGNOFF.md"
      to: "dashboard-v2"
      via: "manual run instructions"
      pattern: "dashboard-v2"
---

<objective>
Establish and execute a strict sign-off gate with explicit evidence, including performance/stability checks and two consecutive clean blocker-journey runs.

Purpose: FLOW-04 and FLOW-06 are phase-level quality claims that require both automated checks and structured human verification.
Output: A completed sign-off artifact and updated validation statuses that either approve Phase 66 or clearly block it with reasons.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-full-integration-testing/66-CONTEXT.md
@.planning/phases/66-full-integration-testing/66-RESEARCH.md
@.planning/phases/66-full-integration-testing/66-QA-CHECKLIST-timeslicing-binning-quality.md
@.planning/phases/66-full-integration-testing/66-01-SUMMARY.md
@src/app/dashboard-v2/page.tsx
@src/app/timeslicing/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 66 sign-off protocol artifact</name>
  <read_first>
    - .planning/phases/66-full-integration-testing/66-CONTEXT.md
    - .planning/phases/66-full-integration-testing/66-QA-CHECKLIST-timeslicing-binning-quality.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>.planning/phases/66-full-integration-testing/66-SIGNOFF.md</files>
  <action>
Create `66-SIGNOFF.md` with these mandatory sections and concrete content:

1. `## Blocker Journeys` with at least 4 journeys:
   - Generate -> Review -> Apply baseline journey
   - Refine-after-apply journey
   - STKDE hotspot investigate journey
   - Empty/low-confidence/error-state journey

2. `## Requirement Mapping` table that maps each journey to FLOW-01..FLOW-06.

3. `## Two-Consecutive-Clean-Runs Rule` section stating sign-off is valid only when each blocker journey passes twice consecutively.

4. `## Blocking Policy` section with exact rule: if any blocker journey fails, phase sign-off is blocked.

5. `## Evidence Log` table template with columns:
   `run_id | journey | result | notes | evidence_ref | operator`
  </action>
  <acceptance_criteria>
    - 66-SIGNOFF.md exists
    - 66-SIGNOFF.md contains FLOW-01 through FLOW-06 strings
    - 66-SIGNOFF.md contains exact phrase `two consecutive clean runs`
    - 66-SIGNOFF.md contains exact phrase `sign-off is blocked`
  </acceptance_criteria>
  <verify>test -f .planning/phases/66-full-integration-testing/66-SIGNOFF.md</verify>
  <done>Phase 66 has a concrete sign-off protocol artifact with requirement mapping and blocking semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Execute automated performance/stability gate and update validation rows</name>
  <read_first>
    - .planning/phases/66-full-integration-testing/66-VALIDATION.md
    - src/app/dashboard-v2/page.stkde.test.ts
    - src/app/timeslicing/page.timeline-qa.test.ts
    - src/app/timeslicing/page.binning-mode.test.ts
  </read_first>
  <files>.planning/phases/66-full-integration-testing/66-VALIDATION.md</files>
  <action>
Run these commands in order and record result status in `66-VALIDATION.md` rows 66-02-01 and 66-02-02:

1. `pnpm -s tsc --noEmit`
2. `pnpm vitest src/app/timeslicing/page.timeline-qa.test.ts src/app/timeslicing/page.binning-mode.test.ts --run`
3. `pnpm vitest src/app/dashboard-v2/page.stkde.test.ts --run`

Set row status to `✅ green` only when command exits 0. If non-zero, keep non-green status and include a short failure note in that row.
  </action>
  <acceptance_criteria>
    - command 1 exits 0
    - command 2 exits 0
    - command 3 exits 0
    - 66-VALIDATION.md rows 66-02-01 and 66-02-02 are updated with current status
  </acceptance_criteria>
  <verify>pnpm -s tsc --noEmit && pnpm vitest src/app/timeslicing/page.timeline-qa.test.ts src/app/timeslicing/page.binning-mode.test.ts --run && pnpm vitest src/app/dashboard-v2/page.stkde.test.ts --run</verify>
  <done>Automated readiness checks for performance/stability baselines are explicitly captured in validation state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run two consecutive blocker-journey passes for sign-off</name>
  <read_first>
    - .planning/phases/66-full-integration-testing/66-SIGNOFF.md
    - .planning/phases/66-full-integration-testing/66-QA-CHECKLIST-timeslicing-binning-quality.md
  </read_first>
  <what-built>Automated hardening checks and a concrete sign-off protocol for Phase 66.</what-built>
  <how-to-verify>
    1. Start app (`pnpm dev`) and open `/dashboard-v2`.
    2. Execute all blocker journeys from `66-SIGNOFF.md` once; record outcomes in Evidence Log.
    3. Repeat the same journeys a second consecutive time; record outcomes.
    4. Confirm first-time analyst clarity for empty/low-confidence/error states.
    5. If any blocker journey fails in either run, mark phase sign-off blocked in `66-SIGNOFF.md`.
  </how-to-verify>
  <acceptance_criteria>
    - Evidence Log contains two consecutive entries per blocker journey
    - Every blocker journey result is pass for both runs OR phase is explicitly marked blocked
    - 66-SIGNOFF.md clearly states final sign-off status
  </acceptance_criteria>
  <verify>Human executes journey checklist and records results in 66-SIGNOFF.md</verify>
  <done>Phase 66 sign-off is either approved with evidence or explicitly blocked with failure reasons.</done>
  <resume-signal>Type "approved" after two clean runs, or share failures to continue hardening.</resume-signal>
</task>

</tasks>

<verification>
1. Sign-off protocol exists and maps all FLOW requirements.
2. Automated quality commands are green and logged in validation rows.
3. Human verification confirms two consecutive clean blocker journeys, or explicitly blocks sign-off.
</verification>

<success_criteria>
- FLOW-04 and FLOW-06 have explicit evidence, not implicit assumptions.
- Phase 66 exits with a deterministic sign-off decision (approved or blocked) backed by recorded runs.
- No route split or large feature expansion was introduced in this gate phase.
</success_criteria>

<output>
After completion, create `.planning/phases/66-full-integration-testing/66-02-SUMMARY.md`
</output>
