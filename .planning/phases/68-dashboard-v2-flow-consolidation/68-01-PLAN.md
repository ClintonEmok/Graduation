---
phase: 68-dashboard-v2-flow-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/dashboard-v2/page.tsx
  - src/components/binning/BinningControls.tsx
  - src/components/dashboard/DashboardHeader.tsx
  - src/store/useLayoutStore.ts
  - src/app/dashboard-v2/page.flow-consolidation.test.tsx
autonomous: true

must_haves:
  truths:
    - "Users can complete generate -> review -> apply -> refine -> analyze inside dashboard-v2 without route hopping."
    - "Only one primary CTA is visible for the active workflow step."
    - "Advanced tools and STKDE stay hidden until after apply/refine."
    - "The header stays informational and does not compete with the workflow rail."
  artifacts:
    - path: src/app/dashboard-v2/page.tsx
      provides: guided workflow shell and STKDE gating
      contains: Generate Draft Slices
    - path: src/components/binning/BinningControls.tsx
      provides: generate-step intent inputs and primary generation CTA
      contains: Generate Draft Slices
    - path: src/components/dashboard/DashboardHeader.tsx
      provides: status-only header with workflow and sync badges
      contains: slice summary
    - path: src/store/useLayoutStore.ts
      provides: default-hidden advanced panel state
      contains: refinement: false
    - path: src/app/dashboard-v2/page.flow-consolidation.test.tsx
      provides: regression coverage for route contract and visibility rules
      contains: dashboard-v2
  key_links:
    - from: src/app/dashboard-v2/page.tsx
      to: src/components/binning/BinningControls.tsx
      via: generate step render
      pattern: BinningControls
    - from: src/app/dashboard-v2/page.tsx
      to: src/components/stkde/DashboardStkdePanel.tsx
      via: workflow-phase gate after apply/refine
      pattern: panels\.stkde
    - from: src/components/dashboard/DashboardHeader.tsx
      to: route navigation
      via: removed header links
      pattern: next/link
---

<objective>
Consolidate `dashboard-v2` into a single guided workflow with one primary action per step and tightly gated advanced analysis.

Purpose: Make the v3.0 investigation flow obvious at a glance and reduce route hopping, competing actions, and premature analysis controls.
Output: A streamlined `dashboard-v2` shell, a surfaced generate step, a status-only header, hidden-by-default advanced panels, and flow-contract tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/68-dashboard-v2-flow-consolidation/68-CONTEXT.md
@.planning/phases/68-dashboard-v2-flow-consolidation/68-FLOWS.md
@src/app/dashboard-v2/page.tsx
@src/components/binning/BinningControls.tsx
@src/components/dashboard/DashboardHeader.tsx
@src/store/useLayoutStore.ts
@src/app/timeslicing/components/SuggestionToolbar.tsx
@src/components/stkde/DashboardStkdePanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild dashboard-v2 around the guided workflow rail</name>
  <files>src/app/dashboard-v2/page.tsx, src/components/binning/BinningControls.tsx, src/store/useLayoutStore.ts</files>
  <read_first>
    - `src/app/dashboard-v2/page.tsx`
    - `src/components/binning/BinningControls.tsx`
    - `src/store/useLayoutStore.ts`
    - `.planning/phases/68-dashboard-v2-flow-consolidation/68-FLOWS.md`
    - `.planning/phases/68-dashboard-v2-flow-consolidation/68-CONTEXT.md`
  </read_first>
  <action>
    Turn the page into a single guided route flow: surface the existing `BinningControls` as the generate step, keep `SuggestionToolbar` as the review/apply surface, and add a top workflow rail above the main canvas that clearly stages `generate -> review -> apply -> refine -> analyze`.

    Use `Generate Draft Slices` as the dominant generation CTA, remove the duplicate inline apply/clear row from `page.tsx`, and preserve only one primary action per step. Hide advanced panels by default by setting `panels.refinement`, `panels.layers`, and `panels.stkde` to `false` in `useLayoutStore.ts`, while keeping core views available.

    Gate `DashboardStkdePanel` so it stays fully hidden until the workflow reaches `applied` or `refine`, and keep the page free of route hopping or competing top-level CTAs. Do not introduce a new navigation surface; the workflow rail is the primary guidance.
  </action>
  <acceptance_criteria>
    - `src/components/binning/BinningControls.tsx` contains the exact CTA text `Generate Draft Slices`.
    - `src/store/useLayoutStore.ts` initializes `refinement: false`, `layers: false`, and `stkde: false`.
    - `src/app/dashboard-v2/page.tsx` renders `BinningControls` and no longer shows the old equal-weight toggle strip as the primary control cluster.
    - `src/app/dashboard-v2/page.tsx` does not render `DashboardStkdePanel` while the workflow is in generate/review.
    - The duplicate page-level draft apply/clear row is removed so the review/apply CTA lives in one place only.
  </acceptance_criteria>
  <verify>grep -n "Generate Draft Slices\|DashboardStkdePanel\|refinement: false\|layers: false\|stkde: false" src/app/dashboard-v2/page.tsx src/components/binning/BinningControls.tsx src/store/useLayoutStore.ts && pnpm vitest --run src/app/dashboard-v2/page.flow-consolidation.test.tsx</verify>
  <done>dashboard-v2 has a single obvious path from generation to analysis, with advanced analysis hidden until it is contextually appropriate.</done>
</task>

<task type="auto">
  <name>Task 2: Strip dashboard header navigation and keep it informational</name>
  <files>src/components/dashboard/DashboardHeader.tsx</files>
  <read_first>
    - `src/components/dashboard/DashboardHeader.tsx`
    - `src/app/dashboard-v2/page.tsx`
    - `.planning/phases/68-dashboard-v2-flow-consolidation/68-FLOWS.md`
  </read_first>
  <action>
    Remove the route links from the header entirely and keep the component focused on status: workflow phase, sync status, strategy/granularity, and slice counts.

    Keep the wording secondary to the workflow rail, avoid any action buttons or route-switching language, and make sure the header can’t compete with the generate/review/apply flow. Preserve the existing badge semantics, but simplify the layout so it reads as context rather than navigation.
  </action>
  <acceptance_criteria>
    - `src/components/dashboard/DashboardHeader.tsx` no longer imports `next/link`.
    - No `href="/dashboard-v2"`, `href="/timeslicing"`, or `href="/stkde"` remains in the header.
    - The header still renders workflow/sync badges and slice summary text.
    - The header copy no longer implies route hopping or alternate surfaces.
  </acceptance_criteria>
  <verify>grep -n "next/link\|href=\"/dashboard-v2\"\|href=\"/timeslicing\"\|href=\"/stkde\"" src/components/dashboard/DashboardHeader.tsx && pnpm vitest --run src/components/dashboard/DashboardHeader.flow-consolidation.test.tsx</verify>
  <done>The header is purely informational and no longer presents competing navigation inside dashboard-v2.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the phase 68 flow contract with regression tests</name>
  <files>src/app/dashboard-v2/page.flow-consolidation.test.tsx, src/components/dashboard/DashboardHeader.flow-consolidation.test.tsx</files>
  <read_first>
    - `src/app/dashboard-v2/page.tsx`
    - `src/components/dashboard/DashboardHeader.tsx`
    - `src/components/binning/BinningControls.tsx`
    - `src/store/useLayoutStore.ts`
    - `.planning/phases/68-dashboard-v2-flow-consolidation/68-FLOWS.md`
  </read_first>
  <action>
    Add source-level regression coverage that fails if the flow consolidation regresses. Cover the generate CTA label, the absence of header route links, the hidden-by-default advanced panel contract, and the STKDE gating rule in the dashboard shell.

    Keep the tests narrowly focused on contract strings and state defaults so they are cheap to maintain and easy to run in CI. Do not duplicate broader integration coverage; this is a phase-68 intent lock, not a full workflow suite.
  </action>
  <acceptance_criteria>
    - A dedicated dashboard-v2 flow-contract test file exists.
    - A dedicated header contract test file exists.
    - The tests assert `Generate Draft Slices`, hidden advanced-panel defaults, and removed header links.
    - The tests pass with a targeted Vitest run.
  </acceptance_criteria>
  <verify>pnpm vitest --run src/app/dashboard-v2/page.flow-consolidation.test.tsx src/components/dashboard/DashboardHeader.flow-consolidation.test.tsx</verify>
  <done>Phase 68 has repeatable tests that protect the one-flow dashboard contract from accidental reintroduction of clutter or route hopping.</done>
</task>

</tasks>

<verification>
1. `dashboard-v2` presents one primary workflow with no competing route links in the header.
2. `Generate Draft Slices` is the visible generation CTA and advanced analysis stays hidden until apply/refine.
3. Phase-specific regression tests pass and lock the flow contract.
</verification>

<success_criteria>
1. The page reads as a single guided investigation flow rather than six equal panels.
2. STKDE is not visible before apply/refine.
3. The header remains informational only.
4. Regression tests make the phase 68 contract explicit and repeatable.
</success_criteria>

<output>
After completion, create `.planning/phases/XX-name/{phase}-{plan}-SUMMARY.md`
</output>
