---
phase: 73-inspection-speed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard-demo/DemoInspectPanel.tsx
  - src/app/dashboard-demo/page.shell.test.tsx
autonomous: true
must_haves:
  truths:
    - "Opening Inspect shows the current applied slice immediately"
    - "The panel exposes comparison actions without leaving Inspect"
    - "Inspect starts paused and in the most useful view for the active slice"
  artifacts:
    - path: "src/components/dashboard-demo/DemoInspectPanel.tsx"
      provides: "Active slice summary, immediate controls, and comparison strip"
    - path: "src/app/dashboard-demo/page.shell.test.tsx"
      provides: "Source-level regression coverage for Inspect affordances"
  key_links:
    - from: "src/components/dashboard-demo/DemoInspectPanel.tsx"
      to: "src/store/useDashboardDemoCoordinationStore.ts"
      via: "active slice, playback, view mode, and comparison state"
      pattern: "activeRailTab|inspectIsPlaying|viewMode|comparisonSliceIds"
    - from: "src/components/dashboard-demo/DemoInspectPanel.tsx"
      to: "src/app/stkde-3d/components/SliceInspector.tsx"
      via: "active slice handoff into detail inspection"
      pattern: "SliceInspector"
---

<objective>
Make /dashboard-demo Inspect feel instant and comparison-ready: the current applied slice should be obvious at a glance, the panel should land in the best inspection posture on entry, and two-slice comparison should be available without switching away.

Purpose: Phase 73 reduces time-to-understand for an applied slice and removes hunting for comparison tools.
Output: A denser Inspect panel, automatic focus-on-entry behavior, inline comparison controls, and regression coverage.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/codebase/CONTROLS.md
@.planning/codebase/SLICES.md
@.planning/codebase/SLICE_MANAGER_UI.md
@.planning/phases/72-workflow-clarity/72-02-SUMMARY.md
@src/components/dashboard-demo/DemoInspectPanel.tsx
@src/components/dashboard-demo/DashboardDemoRailTabs.tsx
@src/store/useDashboardDemoCoordinationStore.ts
@src/app/stkde-3d/components/SliceInspector.tsx
@src/app/stkde-3d/components/SliceScrubber.tsx
@src/app/dashboard-demo/page.shell.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make Inspect open in the fastest inspection posture</name>
  <files>src/components/dashboard-demo/DemoInspectPanel.tsx</files>
  <action>
    Rework the top of the Inspect rail into a persistent summary card that always shows the active applied slice label, crime count, date range, view mode, playback state, and opacity readout above the fold.

    Add an effect that, when the Inspect rail is the active tab and applied slices exist, pauses playback and switches the panel into focus mode so the current slice is immediately readable.

    Keep the current controls in the same top block: Play/Pause, Stack/Single toggle, playback-speed select, and opacity slider. Do not bury the active slice context behind the detail inspector or scrubber.
  </action>
  <verify>
    Run `./node_modules/.bin/eslint src/components/dashboard-demo/DemoInspectPanel.tsx`.
    Confirm the file contains the new summary labels and the inspect-entry focus effect.
  </verify>
  <done>
    Inspect opens with the active slice summary visible immediately and no scroll is required to understand what is being inspected.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add inline comparison controls to Inspect</name>
  <files>src/components/dashboard-demo/DemoInspectPanel.tsx</files>
  <action>
    Add a visible Comparison section directly under the active slice summary.

    The section must read the existing comparison pair from `comparisonSliceIds`, show the left and right slot labels (or `Empty`), and provide four concrete actions: `Pin left`, `Pin right`, `Swap`, and `Clear`.

    `Pin left` and `Pin right` should place the current active slice into the chosen slot using the existing comparison store actions. `Swap` and `Clear` must call the existing store actions, and the comparison UI must stay on the same Inspect surface instead of hiding behind another tab or dialog.
  </action>
  <verify>
    Run `./node_modules/.bin/eslint src/components/dashboard-demo/DemoInspectPanel.tsx`.
    Confirm the file contains `comparisonSliceIds`, `pushComparisonSlice`, `swapComparisonSlices`, `clearComparisonSlices`, `Pin left`, `Pin right`, `Swap`, and `Clear`.
  </verify>
  <done>
    Inspect exposes comparison controls at first glance and users can stage a two-slice comparison without leaving the panel.
  </done>
</task>

<task type="auto">
  <name>Task 3: Lock the new Inspect affordances with source-level tests</name>
  <files>src/app/dashboard-demo/page.shell.test.tsx</files>
  <action>
    Extend the dashboard-demo shell source test so it explicitly guards the new Inspect behavior.

    Add expectations for the active-slice summary copy, the comparison section labels, the focus-on-entry behavior, and the comparison store action usage. Keep the test source-based like the existing shell coverage; do not convert it into a render test.
  </action>
  <verify>
    Run `./node_modules/.bin/vitest run src/app/dashboard-demo/page.shell.test.tsx`.
    Confirm the test asserts the new Inspect labels and store/action wiring.
  </verify>
  <done>
    The dashboard-demo shell test fails if the Inspect panel loses its immediate context or comparison affordances.
  </done>
</task>

</tasks>

<verification>
1. `./node_modules/.bin/vitest run src/app/dashboard-demo/page.shell.test.tsx` passes.
2. `./node_modules/.bin/eslint src/components/dashboard-demo/DemoInspectPanel.tsx src/app/dashboard-demo/page.shell.test.tsx` passes.
3. The Inspect rail source includes active-slice summary, focus-on-entry, and comparison controls above the fold.
</verification>

<success_criteria>
1. Inspect shows the current applied slice context immediately on entry.
2. Inspect opens in the fastest useful posture for reading the active slice.
3. Comparison controls are visible and actionable without switching away from Inspect.
4. Source-level tests protect the new Inspect behavior from regression.
</success_criteria>

<output>
After completion, create `.planning/phases/73-inspection-speed/73-01-SUMMARY.md`
</output>
