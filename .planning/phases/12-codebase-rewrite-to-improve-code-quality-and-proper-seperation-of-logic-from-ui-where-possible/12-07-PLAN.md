---
phase: 12-codebase-rewrite-to-improve-code-quality-and-proper-seperation-of-logic-from-ui-where-possible
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/timeline/hooks/useDualTimelineViewModel.ts
  - src/components/timeline/DualTimelineSurface.tsx
  - src/components/timeline/DualTimeline.tsx
  - src/components/timeline/DualTimeline.tick-rollout.test.ts
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "DualTimeline becomes a thin wrapper around a shared timeline surface and view-model"
    - "The shared timeline layer actually calls useDualTimelineScales and formatDateByResolution"
    - "DualTimeline is materially smaller after the extraction"
  artifacts:
    - path: "src/components/timeline/hooks/useDualTimelineViewModel.ts"
      provides: "Shared timeline model and label formatting logic"
    - path: "src/components/timeline/DualTimelineSurface.tsx"
      provides: "Reusable overview/detail timeline chrome"
    - path: "src/components/timeline/DualTimeline.tsx"
      provides: "Thin store adapter for the refactored timeline"
    - path: "src/components/timeline/DualTimeline.tick-rollout.test.ts"
      provides: "Source-inspection coverage for the refactor"
  key_links:
    - from: "src/components/timeline/DualTimeline.tsx"
      to: "src/components/timeline/DualTimelineSurface.tsx"
      via: "props + composed model"
      pattern: "DualTimelineSurface"
    - from: "src/components/timeline/hooks/useDualTimelineViewModel.ts"
      to: "src/hooks/useDualTimelineScales.ts"
      via: "hook composition"
      pattern: "useDualTimelineScales\\("
    - from: "src/components/timeline/hooks/useDualTimelineViewModel.ts"
      to: "src/lib/date-formatting.ts"
      via: "resolution adapter"
      pattern: "formatDateByResolution\\("
---

<objective>
Split the P0 DualTimeline monolith into a reusable timeline surface plus a thin wrapper that only owns store wiring.

Purpose: make the refactor real by extracting the shared render/model logic and wiring the already-created utilities.
Output: a smaller DualTimeline.tsx, a shared timeline surface, and a test that guards the new shape.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-codebase-rewrite-to-improve-code-quality-and-proper-seperation-of-logic-from-ui-where-possible/12-04-SUMMARY.md
@.planning/phases/12-codebase-rewrite-to-improve-code-quality-and-proper-seperation-of-logic-from-ui-where-possible/12-05-SUMMARY.md
@.planning/phases/12-codebase-rewrite-to-improve-code-quality-and-proper-seperation-of-logic-from-ui-where-possible/12-VERIFICATION.md
@src/components/timeline/DualTimeline.tsx
@src/components/timeline/DualTimeline.tick-rollout.test.ts
@src/hooks/useDualTimelineScales.ts
@src/lib/date-formatting.ts
@src/components/timeline/lib/tick-ux.ts
@src/components/timeline/lib/interaction-guards.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the shared timeline view-model and surface</name>
  <files>src/components/timeline/hooks/useDualTimelineViewModel.ts, src/components/timeline/DualTimelineSurface.tsx</files>
  <read_first>src/components/timeline/DualTimeline.tsx, src/components/timeline/DemoDualTimeline.tsx, src/hooks/useDualTimelineScales.ts, src/lib/date-formatting.ts, src/components/timeline/lib/tick-ux.ts, src/components/timeline/lib/interaction-guards.ts</read_first>
  <action>Create a shared hook that computes the normalized timeline model for the overview/detail views, including the resolution-to-DateResolution adapter used by formatDateByResolution and the shared useDualTimelineScales composition. Then create a reusable DualTimelineSurface component that renders the common header, overview/detail chrome, and tick labels from that model instead of keeping those blocks inside the monolith.</action>
  <acceptance_criteria>useDualTimelineViewModel.ts imports useDualTimelineScales and formatDateByResolution; DualTimelineSurface.tsx exports a reusable component; the shared files contain the overview/detail chrome that used to live inline in DualTimeline.tsx.</acceptance_criteria>
  <verify>grep -n "useDualTimelineScales" src/components/timeline/hooks/useDualTimelineViewModel.ts returns a match; grep -n "formatDateByResolution" src/components/timeline/hooks/useDualTimelineViewModel.ts returns a match; grep -n "DualTimelineSurface" src/components/timeline/DualTimelineSurface.tsx returns a match.</verify>
  <done>The shared timeline layer exists and owns the logic that previously made DualTimeline a god file.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor DualTimeline into a thin adapter</name>
  <files>src/components/timeline/DualTimeline.tsx</files>
  <read_first>src/components/timeline/DualTimeline.tsx, src/components/timeline/DualTimelineSurface.tsx, src/components/timeline/hooks/useDualTimelineViewModel.ts</read_first>
  <action>Remove the inline render/math blocks from DualTimeline.tsx and replace them with a thin adapter that builds the shared view-model and passes it into DualTimelineSurface. Keep the store selectors, selection handlers, and existing interaction semantics intact, but delete the dead imports once the shared surface owns them.</action>
  <acceptance_criteria>DualTimeline.tsx imports DualTimelineSurface and useDualTimelineViewModel; grep -n "formatDateByResolution" src/components/timeline/DualTimeline.tsx returns no matches; grep -n "useDualTimelineScales" src/components/timeline/DualTimeline.tsx returns no matches; wc -l src/components/timeline/DualTimeline.tsx reports a materially smaller file than the pre-refactor 1307-line baseline.</acceptance_criteria>
  <verify>wc -l shows DualTimeline.tsx below 1150 lines and grep checks confirm the dead imports are gone.</verify>
  <done>DualTimeline is an orchestration wrapper, not the place where timeline rendering logic lives.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the DualTimeline refactor with source inspection</name>
  <files>src/components/timeline/DualTimeline.tick-rollout.test.ts</files>
  <read_first>src/components/timeline/DualTimeline.tsx, src/components/timeline/DualTimeline.tick-rollout.test.ts</read_first>
  <action>Update the source-inspection test so it checks the wrapper shape instead of the old monolith shape. Assert the wrapper imports the shared surface/view-model, the dead utility imports are gone, and the file length threshold proves the refactor was substantial.</action>
  <acceptance_criteria>The test file checks for DualTimelineSurface and useDualTimelineViewModel imports, no longer checks for dead local helper bodies, and includes a line-count assertion against the wrapper file.</acceptance_criteria>
  <verify>pnpm vitest src/components/timeline/DualTimeline.tick-rollout.test.ts passes.</verify>
  <done>The refactor is protected by a source-inspection test that will fail if the monolith shape returns.</done>
</task>

</tasks>

<verification>
Run the DualTimeline source-inspection test and confirm the wrapper file no longer contains dead utility imports or the inline render blocks.
</verification>

<success_criteria>
DualTimeline is a thin wrapper, the shared timeline surface/view-model exist, the extracted utilities are actually used, and the wrapper file is materially smaller than before.
</success_criteria>

<output>
After completion, create `.planning/phases/12-codebase-rewrite-to-improve-code-quality-and-proper-seperation-of-logic-from-ui-where-possible/12-07-SUMMARY.md`
</output>
