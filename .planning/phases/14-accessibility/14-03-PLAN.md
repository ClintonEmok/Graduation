---
phase: 14-accessibility
plan: 03
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/components/map/MapBase.tsx
autonomous: true
must_haves:
  truths:
    - "Map style changes between Light and Dark based on theme"
  artifacts:
    - path: "src/components/map/MapBase.tsx"
      provides: "Themed map tiles"
  key_links:
    - from: "src/components/map/MapBase.tsx"
      to: "src/store/useThemeStore.ts"
      via: "useThemeStore"
---

<objective>
Connect the Map component to the theme store to swap the base map style.

Purpose: Ensure the map legibility matches the overall theme (Light map for Light theme, Dark for Dark).
Output: Dynamic map styling.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/components/map/MapBase.tsx
@src/lib/palettes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dynamic Map Style</name>
  <files>src/components/map/MapBase.tsx</files>
  <action>
    1. Import `useThemeStore` and `PALETTES`.
    2. Get `theme` from store.
    3. Determine style URL: `const mapStyle = PALETTES[theme].mapStyle`.
    4. Pass this dynamic `mapStyle` to the `<Map>` component.
    5. Ensure the component re-renders when the style changes.
  </action>
  <verify>
    grep "mapStyle=" src/components/map/MapBase.tsx
  </verify>
  <done>
    Map toggles between Dark Matter and Positron (or similar) based on theme.
  </done>
</task>

</tasks>

<verification>
- Switch to Light mode -> Map becomes light.
- Switch to Dark mode -> Map becomes dark.
</verification>

<success_criteria>
- Map style URL updates with theme.
</success_criteria>
