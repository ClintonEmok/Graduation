---
phase: 01-foundation-store-sync-slice-planes
plan: 01
type: execute
wave: 1
depends_on: []
requirements_addressed: [SLICE-01, SLICE-02]
files_modified:
  - src/store/useTimeslicingModeStore.ts
  - src/components/binning/BinningControls.tsx
  - src/lib/binning/rules.ts
  - src/lib/binning/engine.ts
  - src/lib/binning/monthly-contract.phase1.test.ts
autonomous: true
must_haves:
  truths:
    - "Monthly is available everywhere the binning UI exposes granularity."
    - "The shared granularity/type contract no longer disagrees about monthly."
    - "Monthly bin generation follows the same user path as other fixed intervals."
  artifacts:
    - path: src/store/useTimeslicingModeStore.ts
      provides: "Shared generation granularity contract"
      contains: "monthly"
    - path: src/components/binning/BinningControls.tsx
      provides: "Granularity selector options"
      contains: "Monthly"
    - path: src/lib/binning/engine.ts
      provides: "Monthly bin generation path"
      exports: ["generateBins"]
  key_links:
    - from: src/store/useTimeslicingModeStore.ts
      to: src/components/binning/BinningControls.tsx
      via: "TimeslicingGranularity union"
      pattern: "monthly"
    - from: src/lib/binning/rules.ts
      to: src/lib/binning/engine.ts
      via: "strategy switch"
      pattern: "case 'monthly'"
---

<objective>
Unify the shared timeslicing/binning contract so monthly is a first-class option end-to-end.

Purpose: The demo and legacy binning surfaces must agree on the same granularity vocabulary before the cube can rely on monthly-aware selection and generation.
Output: Updated granularity types, monthly binning behavior, and a regression test that fails if the monthly contract regresses.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/research/FEATURES.md
@src/store/useTimeslicingModeStore.ts
@src/components/binning/BinningControls.tsx
@src/lib/binning/rules.ts
@src/lib/binning/engine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add monthly to the shared binning contract</name>
  <files>src/store/useTimeslicingModeStore.ts, src/components/binning/BinningControls.tsx, src/lib/binning/rules.ts, src/lib/binning/engine.ts</files>
  <action>
    Extend the shared `TimeslicingGranularity` union in `src/store/useTimeslicingModeStore.ts` to include `monthly` and add a Monthly option to the generation granularity UI in `src/components/binning/BinningControls.tsx`.

    In `src/lib/binning/rules.ts`, add `monthly` to `BinningStrategy` and define a monthly preset that keeps the existing fixed-interval semantics intact.

    In `src/lib/binning/engine.ts`, add a monthly generation branch that partitions by calendar month boundaries (not a fake weekly/daily alias), keeps the bins sorted, and preserves the current behavior of all existing strategies.
  </action>
  <verify>grep -n "monthly" src/store/useTimeslicingModeStore.ts src/components/binning/BinningControls.tsx src/lib/binning/rules.ts src/lib/binning/engine.ts</verify>
  <done>Monthly is selectable and the engine has a dedicated monthly path instead of silently falling back.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the monthly contract with a source test</name>
  <files>src/lib/binning/monthly-contract.phase1.test.ts</files>
  <action>
    Add a compact source-inspection test that reads `useTimeslicingModeStore.ts`, `BinningControls.tsx`, `rules.ts`, and `engine.ts` and asserts that:
    - `monthly` appears in the shared granularity union
    - the UI exposes a Monthly option
    - `BinningStrategy` includes `monthly`
    - `generateBins` has an explicit `case 'monthly'`

    Keep the test in the existing source-inspection style used elsewhere in the repo.
  </action>
  <verify>pnpm vitest src/lib/binning/monthly-contract.phase1.test.ts</verify>
  <done>The monthly contract fails fast if any one of the touched files drops support again.</done>
</task>

</tasks>

<verification>
Run the monthly contract test, then inspect the touched source files for the exact monthly strings and switch branch.
</verification>

<success_criteria>
- `monthly` is accepted by the shared granularity type and appears in the binning UI.
- `generateBins` can build monthly bins through a dedicated code path.
- The monthly contract test passes and protects the shared vocabulary.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-store-sync-slice-planes/01-01-SUMMARY.md`
</output>
