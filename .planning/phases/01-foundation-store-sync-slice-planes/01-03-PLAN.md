---
phase: 01-foundation-store-sync-slice-planes
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
requirements_addressed: [PLANE-02, PLANE-03]
files_modified:
  - src/components/viz/TimeSlices.tsx
  - src/components/viz/SlicePlane.tsx
  - src/components/viz/time-slices-polish.phase1.test.ts
autonomous: true
must_haves:
  truths:
    - "Slice planes are readable without stealing attention from the cube."
    - "Dragging, resizing, and double-click creation still behave the same after the visual pass."
    - "The slice plane labels and helpers feel like part of the cube, not a separate tool window."
  artifacts:
    - path: src/components/viz/TimeSlices.tsx
      provides: "Scene-mounted slice plane group"
      contains: "SlicePlane"
    - path: src/components/viz/SlicePlane.tsx
      provides: "Interactive plane visuals"
      contains: "Html"
    - path: src/components/viz/time-slices-polish.phase1.test.ts
      provides: "Slice-plane regression coverage"
      contains: "TimeSlices"
  key_links:
    - from: src/components/viz/MainScene.tsx
      to: src/components/viz/TimeSlices.tsx
      via: "scene render"
      pattern: "TimeSlices"
    - from: src/components/viz/TimeSlices.tsx
      to: src/components/viz/SlicePlane.tsx
      via: "slice mapping"
      pattern: "slice => (<SlicePlane"
    - from: src/components/viz/SlicePlane.tsx
      to: user interaction
      via: "drag and double-click handlers"
      pattern: "onPointerDown|onDoubleClick"
---

<objective>
Polish the mounted slice planes so they read cleanly inside the cube and keep their interactions obvious.

Purpose: After the cube is wired correctly, the slice surfaces need visual hierarchy and legible labels so analysts can actually use them.
Output: Cleaner slice-plane rendering, clearer labels/helpers, and a regression test that protects the visual contract.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/components/viz/TimeSlices.tsx
@src/components/viz/SlicePlane.tsx
@src/app/timeline-test-3d/components/TimeSlices3D.tsx
@src/components/viz/MainScene.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten slice-plane visuals without changing behavior</name>
  <files>src/components/viz/TimeSlices.tsx, src/components/viz/SlicePlane.tsx</files>
  <action>
    Keep the current create/drag/resize semantics, but update the visual treatment so the cube reads more cleanly:

    - make labels easier to scan at a glance
    - reduce helper/grid visual noise
    - keep locked vs editable states visually distinct
    - keep range planes and point planes visually aligned with the demo cube palette

    Use the 3D timeline-test implementation as a style reference only; do not copy its heavier interaction model into this phase.
  </action>
  <verify>grep -n "Html\|onPointerDown\|onDoubleClick\|gridHelper\|SlicePlane" src/components/viz/TimeSlices.tsx src/components/viz/SlicePlane.tsx</verify>
  <done>Slice planes look intentional, not default, while the interaction semantics remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add a visual-contract regression test</name>
  <files>src/components/viz/time-slices-polish.phase1.test.ts</files>
  <action>
    Add a source-inspection test that reads `TimeSlices.tsx`, `SlicePlane.tsx`, and `MainScene.tsx`.

    Assert the scene still renders `TimeSlices`, the plane component still exposes the drag/double-click tokens, and the label/helper strings that define the cube's visual hierarchy remain present.
  </action>
  <verify>pnpm vitest src/components/viz/time-slices-polish.phase1.test.ts</verify>
  <done>The slice-plane polish cannot regress silently without the test failing.</done>
</task>

</tasks>

<verification>
Run the slice-plane contract test and confirm the cube still shows visible, legible planes with the same interactions.
</verification>

<success_criteria>
- Slice planes are legible in the cube.
- The drag / resize / double-click flows still exist.
- The regression test protects the visual and interaction contract.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-store-sync-slice-planes/01-03-SUMMARY.md`
</output>
