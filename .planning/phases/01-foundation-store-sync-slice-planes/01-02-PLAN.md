---
phase: 01-foundation-store-sync-slice-planes
plan: 02
type: execute
wave: 1
depends_on: []
requirements_addressed: [LINK-01, LINK-02, PLANE-01]
files_modified:
  - src/components/dashboard-demo/DashboardDemoShell.tsx
  - src/components/viz/CubeVisualization.tsx
  - src/components/viz/MainScene.tsx
  - src/components/viz/SimpleCrimePoints.tsx
  - src/components/viz/SelectedWarpSliceOverlay.tsx
  - src/components/viz/TimeSlices.tsx
  - src/components/viz/cube-store-overrides.phase1.test.ts
autonomous: true
must_haves:
  truths:
    - "The dashboard-demo cube reads the demo stores, not the legacy global stores."
    - "TimeSlices is actually rendered inside the 3D scene tree."
    - "Selection, warp, and filter state stay synchronized across the cube view."
  artifacts:
    - path: src/components/dashboard-demo/DashboardDemoShell.tsx
      provides: "Demo cube entrypoint"
      contains: "CubeVisualization"
    - path: src/components/viz/MainScene.tsx
      provides: "3D scene composition"
      contains: "TimeSlices"
    - path: src/components/viz/SimpleCrimePoints.tsx
      provides: "Cube crime rendering"
      contains: "useStore"
  key_links:
    - from: src/components/dashboard-demo/DashboardDemoShell.tsx
      to: src/components/viz/CubeVisualization.tsx
      via: "demo store override props"
      pattern: "filterStoreOverride|coordinationStoreOverride|adaptiveStoreOverride"
    - from: src/components/viz/CubeVisualization.tsx
      to: src/components/viz/MainScene.tsx
      via: "override prop forwarding"
      pattern: "MainScene"
    - from: src/components/viz/MainScene.tsx
      to: src/components/viz/TimeSlices.tsx
      via: "scene tree render"
      pattern: "TimeSlices"
---

<objective>
Wire the cube view to the dashboard-demo stores and place the slice planes into the live 3D scene.

Purpose: The cube must reflect the same analyst state as the rest of the demo, and the slice planes need to exist in the actual scene tree before visual polish can land.
Output: Demo-store-aware cube plumbing, TimeSlices mounted in MainScene, and a regression test that locks the wiring.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@src/components/dashboard-demo/DashboardDemoShell.tsx
@src/components/map/MapVisualization.tsx
@src/components/viz/CubeVisualization.tsx
@src/components/viz/MainScene.tsx
@src/components/viz/SimpleCrimePoints.tsx
@src/components/viz/SelectedWarpSliceOverlay.tsx
@src/components/viz/TimeSlices.tsx
@src/store/useDashboardDemoFilterStore.ts
@src/store/useDashboardDemoCoordinationStore.ts
@src/store/useDashboardDemoAdaptiveStore.ts
@src/store/useDashboardDemoTimeStore.ts
@src/store/useDashboardDemoSliceStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Forward the dashboard-demo stores into the cube</name>
  <files>src/components/dashboard-demo/DashboardDemoShell.tsx, src/components/viz/CubeVisualization.tsx, src/components/viz/SimpleCrimePoints.tsx, src/components/viz/SelectedWarpSliceOverlay.tsx</files>
  <action>
    Mirror the `MapVisualization` override pattern in the cube stack.

    Add explicit override props for the demo filter, coordination, adaptive, time, and slice stores on `CubeVisualization` and pass the dashboard-demo store instances from `DashboardDemoShell`.

    Update `SimpleCrimePoints` and `SelectedWarpSliceOverlay` to resolve the provided stores through `useStore` fallbacks instead of hard-wiring the legacy globals, so the cube keeps the same selection and warp state as the rest of the demo.
  </action>
  <verify>grep -n "filterStoreOverride\|coordinationStoreOverride\|adaptiveStoreOverride\|timeStoreOverride\|sliceStoreOverride" src/components/dashboard-demo/DashboardDemoShell.tsx src/components/viz/CubeVisualization.tsx src/components/viz/SimpleCrimePoints.tsx src/components/viz/SelectedWarpSliceOverlay.tsx</verify>
  <done>The cube can be mounted with demo stores and still render the same selection state as the rest of the dashboard-demo shell.</done>
</task>

<task type="auto">
  <name>Task 2: Mount TimeSlices inside the live scene</name>
  <files>src/components/viz/MainScene.tsx, src/components/viz/TimeSlices.tsx</files>
  <action>
    Import `TimeSlices` into `MainScene` and render it inside the scene tree alongside `SimpleCrimePoints`, `SpatialConstraintOverlay`, and `SelectedWarpSliceOverlay`.

    Keep the existing transparent canvas, camera controls, and pointer-event behavior intact. The slice planes should be part of the actual cube scene, not a separate overlay or stub.
  </action>
  <verify>grep -n "TimeSlices" src/components/viz/MainScene.tsx src/components/viz/TimeSlices.tsx</verify>
  <done>TimeSlices appears in the scene tree and can respond to the same store state as the cube points.</done>
</task>

<task type="auto">
  <name>Task 3: Add a regression test for cube-store wiring</name>
  <files>src/components/viz/cube-store-overrides.phase1.test.ts</files>
  <action>
    Add a source-inspection test that reads `DashboardDemoShell.tsx`, `CubeVisualization.tsx`, `MainScene.tsx`, `SimpleCrimePoints.tsx`, `SelectedWarpSliceOverlay.tsx`, and `TimeSlices.tsx`.

    Assert the exact override prop names, the `useStore` fallback pattern, and the `TimeSlices` render token so the cube cannot silently drift back to legacy globals.
  </action>
  <verify>pnpm vitest src/components/viz/cube-store-overrides.phase1.test.ts</verify>
  <done>The wiring contract is locked to the demo-store path and the scene tree path.</done>
</task>

</tasks>

<verification>
Run the cube wiring test, then confirm the shell passes the demo store props all the way into the live scene tree.
</verification>

<success_criteria>
- The dashboard-demo cube resolves state from demo stores.
- TimeSlices is mounted inside MainScene.
- Cube selection/warp/filter behavior stays synchronized with the demo shell.
- The wiring regression test passes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-store-sync-slice-planes/01-02-SUMMARY.md`
</output>
