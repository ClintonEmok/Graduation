---
phase: 46-guardrails-and-baselines
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useCrimeData.test.ts
  - src/app/api/crimes/range/route.test.ts
  - src/app/api/crimes/range/route.ts
autonomous: true

must_haves:
  truths:
    - "Buffering behavior between useCrimeData and /api/crimes/range is covered by regression tests"
    - "Coordinate-scale parity expectations are enforced for range endpoint responses"
    - "Contract breakage in range query params or meta payload fails tests before refactors ship"
  artifacts:
    - path: "src/hooks/useCrimeData.test.ts"
      provides: "Hook-level buffering/contract tests"
      min_lines: 80
    - path: "src/app/api/crimes/range/route.test.ts"
      provides: "API contract tests for range endpoint"
      min_lines: 120
  key_links:
    - from: "src/hooks/useCrimeData.test.ts"
      to: "src/hooks/useCrimeData.ts"
      via: "asserts buffered epoch query behavior"
      pattern: "bufferDays"
    - from: "src/app/api/crimes/range/route.test.ts"
      to: "src/app/api/crimes/range/route.ts"
      via: "GET request contract assertions"
      pattern: "meta.buffer"
---

<objective>
Add regression contracts for the hook/API pipeline that has known fragility around buffering and coordinate behavior.

Purpose: These tests lock current expected behavior before API/stores/query decomposition changes in later phases.
Output: Dedicated test suites for `useCrimeData` and `/api/crimes/range` validating buffering semantics, response meta shape, and coordinate parity assumptions.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REFACTORING-PLAN.md
@src/hooks/useCrimeData.ts
@src/app/api/crimes/range/route.ts
@src/types/crime.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add useCrimeData buffering regression tests</name>
  <files>src/hooks/useCrimeData.test.ts</files>
  <action>Create unit tests for `useCrimeData` covering buffered epoch calculations, query-key stability, and pass-through of API `meta` fields. Mock `fetch` and run hook tests through React Query provider wrappers so tests assert real query behavior, not isolated helper stubs. Include edge cases for non-default `bufferDays`, optional filters, and API error propagation.</action>
  <verify>Run `npx vitest run src/hooks/useCrimeData.test.ts` and ensure all new buffering/contract tests pass.</verify>
  <done>Hook tests fail on buffer contract drift (including accidental double-buffering) and verify returned `bufferedRange` and `meta` alignment.</done>
</task>

<task type="auto">
  <name>Task 2: Add /api/crimes/range contract tests for buffer and coordinate parity</name>
  <files>src/app/api/crimes/range/route.test.ts, src/app/api/crimes/range/route.ts</files>
  <action>Add API route tests that call `GET` with `Request` objects and mocked query/data dependencies. Cover required-param validation, buffered range calculations in `meta.buffer`, filter parsing, and output coordinate assertions (`x`/`z` numeric and consistent with returned `lat`/`lon` bounds in mock mode). If route internals are not currently test-friendly, perform minimal extraction in `route.ts` (pure parsing helpers only) to enable deterministic tests without changing runtime behavior.</action>
  <verify>Run `npx vitest run src/app/api/crimes/range/route.test.ts` and verify all contract tests pass.</verify>
  <done>Range endpoint behavior is pinned by tests for validation errors, meta buffer semantics, and coordinate-scale parity expectations.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/hooks/useCrimeData.test.ts src/app/api/crimes/range/route.test.ts` passes.
2. Tests include both success and invalid-input cases for endpoint contract.
3. No production behavior change beyond testability refactors.
</verification>

<success_criteria>
- REFACTOR-01: Regression tests lock buffering semantics between hook and range endpoint.
- REFACTOR-02: Coordinate parity and contract shape are covered before API stabilization work starts.
</success_criteria>

<output>
After completion, create `.planning/phases/46-guardrails-and-baselines/46-02-SUMMARY.md`
</output>
