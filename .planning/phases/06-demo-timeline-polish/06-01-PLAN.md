---
phase: 06-demo-timeline-polish
plan: 01
type: execute
wave: 1
depends_on:
  - 05-03
requirements_addressed:
  - TPL-01
  - TPL-02
  - TPL-03
  - TPL-04
  - TPL-05
files_modified:
  - src/components/dashboard-demo/DemoTimelinePanel.tsx
  - src/components/timeline/DemoDualTimeline.tsx
  - src/components/dashboard-demo/DemoSlicePanel.tsx
  - src/app/dashboard-demo/page.shell.test.tsx
autonomous: true
must_haves:
  truths:
    - "The demo timeline reads as the primary analysis surface at a glance."
    - "The focused timeline is visually stronger than the raw baseline."
    - "Slice, warp, playback, and review controls stay visible without crowding the view."
    - "The polished timeline remains demo-local and does not disturb the stable routes."
    - "The timeline is ready to support contextual layers in the next phase."
  artifacts:
    - path: "src/components/dashboard-demo/DemoTimelinePanel.tsx"
      provides: "Demo timeline card, control hierarchy, and wrapper copy"
    - path: "src/components/timeline/DemoDualTimeline.tsx"
      provides: "Demo-specific dual timeline wrapper with focus/raw presentation"
    - path: "src/components/dashboard-demo/DemoSlicePanel.tsx"
      provides: "Secondary slice companion controls and summaries"
    - path: "src/app/dashboard-demo/page.shell.test.tsx"
      provides: "Source-inspection coverage for the demo shell contract"
  key_links:
    - from: "src/components/dashboard-demo/DemoTimelinePanel.tsx"
      to: "src/components/timeline/DemoDualTimeline.tsx"
      via: "render the polished dual timeline wrapper inside the demo timeline card"
      pattern: "DemoDualTimeline"
    - from: "src/components/dashboard-demo/DemoSlicePanel.tsx"
      to: "src/app/dashboard-demo/page.shell.test.tsx"
      via: "lock the secondary-rail wording and stable route exclusions"
      pattern: "Slice Companion|DashboardDemoShell|DemoTimelinePanel|DemoDualTimeline"
---

<objective>
Polish the `/dashboard-demo` timeline so it becomes a calm, readable temporal surface with a strong focused track, a subtle raw baseline, and compact controls.

Purpose: tighten the demo timeline UI hierarchy before adding broader contextual data.
Output: a demo-local timeline shell and regression lock that keep editing, review, and warp controls available without overwhelming the view.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-stats-stkde-interaction/05-CONTEXT.md
@.planning/phases/02-dashboard-demo-ui-ux/02-CONTEXT.md
@src/components/dashboard-demo/DemoTimelinePanel.tsx
@src/components/timeline/DemoDualTimeline.tsx
@src/components/dashboard-demo/DemoSlicePanel.tsx
@src/components/dashboard-demo/DashboardDemoShell.tsx
@src/app/dashboard-demo/page.shell.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and tighten the demo timeline hierarchy</name>
  <files>src/components/dashboard-demo/DemoTimelinePanel.tsx, src/components/timeline/DemoDualTimeline.tsx</files>
  <read_first>
    - src/components/dashboard-demo/DemoTimelinePanel.tsx
    - src/components/timeline/DemoDualTimeline.tsx
    - src/components/dashboard-demo/DemoSlicePanel.tsx
    - src/components/timeline/DualTimeline.tsx
  </read_first>
  <action>
    Rework the demo timeline card so the top focused track is the visual anchor and the raw timeline reads as a subtle baseline beneath it. In `DemoTimelinePanel.tsx`, keep the existing time-store and warp-store wiring, but reduce the helper copy to a short two-line caption, group playback/scrub controls into one compact row, keep the temporal-resolution slider in the same card, and leave the warp factor toggle visible only when adaptive mode is active. In `DemoDualTimeline.tsx`, keep the demo-local store overrides and `disableAutoBurstSlices={true}` default, but add the focused/raw presentation language, use the existing warp overlay bands to emphasize slice regions, and prefer curved connector rendering where the underlying `DualTimeline` API allows it. Do not touch stable dashboard or stats/stkde routes.
  </action>
  <acceptance_criteria>
    - `DemoTimelinePanel.tsx` still renders `DemoDualTimeline` and the temporal-resolution slider.
    - `DemoTimelinePanel.tsx` contains `Warp factor` and `Time Scale` only in the adaptive controls area.
    - `DemoDualTimeline.tsx` still contains `buildDemoSliceAuthoredWarpMap`, `warpOverlayBandsOverride`, and `adaptiveWarpMapOverride`.
    - The stable `/dashboard` shell is untouched by this task.
  </acceptance_criteria>
  <verify>pnpm exec vitest run src/app/dashboard-demo/page.shell.test.tsx --reporter=dot</verify>
  <done>The demo timeline reads as a calm primary surface with a clear focused/raw hierarchy and retains all key temporal controls.</done>
</task>

<task type="auto">
  <name>Task 2: Align the slice companion with the polished timeline</name>
  <files>src/components/dashboard-demo/DemoSlicePanel.tsx, src/app/dashboard-demo/page.shell.test.tsx</files>
  <read_first>
    - src/components/dashboard-demo/DemoSlicePanel.tsx
    - src/app/dashboard-demo/page.shell.test.tsx
    - src/components/dashboard-demo/DashboardDemoShell.tsx
  </read_first>
  <action>
    Reduce the slice companion's visual weight so it reads as a secondary review rail. Tighten the header copy, keep the slice counts and mode badges compact, preserve the add/edit/lock/visibility controls, and keep the demo-local store reads intact (`useDashboardDemoSliceStore`, `useDashboardDemoTimeslicingModeStore`, `useDashboardDemoTimeStore`). Then update the source-inspection test so it explicitly locks the polished timeline copy and the stable route exclusions (`DashboardDemoShell`, `DemoTimelinePanel`, `DemoDualTimeline`, `DemoSlicePanel`, and the absence of the stable route regressions).
  </action>
  <acceptance_criteria>
    - `DemoSlicePanel.tsx` still contains `Slice Companion`, `datetime-local`, and the demo-local slice stores.
    - `page.shell.test.tsx` still asserts `DashboardDemoShell`, `DemoTimelinePanel`, `DemoDualTimeline`, and `DemoSlicePanel`.
    - `page.shell.test.tsx` still rejects the stable route regressions for `/dashboard`, `/timeslicing`, and the old workflow-shell wording.
  </acceptance_criteria>
  <verify>pnpm exec vitest run src/app/dashboard-demo/page.shell.test.tsx --reporter=dot</verify>
  <done>The demo shell contract locks the calmer timeline hierarchy and keeps the slice companion secondary.</done>
</task>

</tasks>

<verification>
Open `/dashboard-demo` and confirm the focused track is primary, the raw baseline stays subtle, the playback/resolution controls remain readable, and the slice companion stays secondary.
</verification>

<success_criteria>
The `/dashboard-demo` timeline feels readable, calm, and analysis-first, and the next phase can add contextual layers without reworking the timeline hierarchy again.
</success_criteria>

<output>
After completion, create `.planning/phases/06-demo-timeline-polish/06-01-SUMMARY.md`
</output>
