---
phase: 41-full-auto-optimization-ranking
plan: '02'
type: execute
wave: 2
depends_on:
  - '41-01'
files_modified:
  - src/app/timeslicing/components/AutoProposalSetCard.tsx
  - src/app/timeslicing/components/SuggestionPanel.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "AutoProposalSetCard displays 'Why recommended' text for recommended packages"
    - "Score breakdown shows 4 dimensions without contextFit"
    - "SuggestionPanel renders ranked list in vertical layout"
  artifacts:
    - path: "src/app/timeslicing/components/AutoProposalSetCard.tsx"
      provides: "Package card with score display"
      contains: "Why recommended, score breakdown"
    - path: "src/app/timeslicing/components/SuggestionPanel.tsx"
      provides: "Ranked list display"
      contains: "full-auto packages section"
---

<objective>
Update UI components to display "Why recommended" text and ensure proper ranked list rendering with 4-dimension score breakdown.
</objective>

<context>
@src/app/timeslicing/components/AutoProposalSetCard.tsx
@src/app/timeslicing/components/SuggestionPanel.tsx

# From CONTEXT.md (LOCKED):
- Vertical list layout matching SuggestionPanel pattern
- Percentages format (e.g., "87% relevance")
- Always show recommended badge
- Score display: Simple total score, expandable for breakdown
- Rationale: Include "why recommended" indicator (e.g., "Best: relevance + continuity")

# From Phase 40 Summary:
- SuggestionPanel exists with full-auto packages section
- AutoProposalSetCard exists with expandable score breakdown
- ProfileManager section in panel
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AutoProposalSetCard for whyRecommended display</name>
  <files>src/app/timeslicing/components/AutoProposalSetCard.tsx</files>
  <action>
    Update AutoProposalSetCard to display "Why recommended" text:
    
    1. Add prop: whyRecommended?: string (optional, for recommended packages only)
    
    2. In the card render, after the lowConfidenceReason section, add:
    ```tsx
    {isRecommended && proposalSet.reasonMetadata?.whyRecommended && (
      <div className="mt-2 rounded border border-emerald-500/40 bg-emerald-500/10 px-2 py-1 text-xs text-emerald-200">
        <div className="flex items-center gap-1 font-medium text-emerald-300">
          <Sparkles className="size-3" />
          Why recommended
        </div>
        <p className="mt-0.5 text-emerald-200/90">
          {proposalSet.reasonMetadata.whyRecommended}
        </p>
      </div>
    )}
    ```
    
    3. Update the Score Breakdown section to show 4 dimensions only (remove "Context fit"):
    - Coverage, Relevance, Overlap, Continuity
    - Display as percentages (e.g., "87%")
    
    4. Pass the whyRecommended prop when rendering AutoProposalSetCard in SuggestionPanel.
  </action>
  <verify>Recommended packages show "Why recommended" with top contributing dimensions</verify>
  <done>Card displays whyRecommended text for recommended packages</done>
</task>

<task type="auto">
  <name>Task 2: Verify SuggestionPanel integration</name>
  <files>src/app/timeslicing/components/SuggestionPanel.tsx</files>
  <action>
    Verify SuggestionPanel properly passes whyRecommended to AutoProposalSetCard:
    
    1. Check that full-auto packages section passes all required props to AutoProposalSetCard
    2. Ensure startEpoch and endEpoch are passed for date range display
    3. Verify ranked list renders in vertical layout (existing pattern)
    4. Confirm recommended badge logic works with new scoring
    
    The existing SuggestionPanel already has the full-auto packages section. Just ensure:
    - isRecommended prop correctly reflects highest-scoring package
    - All props are passed through to AutoProposalSetCard
  </action>
  <verify>SuggestionPanel displays ranked packages with recommended badge</verify>
  <done>SuggestionPanel integration complete</done>
</task>

<task type="auto">
  <name>Task 3: Remove contextFit from UI display</name>
  <files>src/app/timeslicing/components/AutoProposalSetCard.tsx</files>
  <action>
    In the Score Breakdown section (inside isExpanded), remove the "Context fit" row since contextFit is no longer part of the scoring:
    
    Before:
    ```
    <span>Context fit</span>
    <span className="text-right">{formatScore(proposalSet.score.contextFit)}</span>
    ```
    
    After: Remove this row entirely.
    
    Keep only: Coverage, Relevance, Overlap, Continuity.
  </action>
  <verify>Score breakdown shows exactly 4 dimensions</verify>
  <done>contextFit removed from UI score breakdown</done>
</task>

</tasks>

<verification>
- [ ] AutoProposalSetCard shows "Why recommended" for recommended packages
- [ ] whyRecommended text shows "Best: dimension1 + dimension2" format
- [ ] Score breakdown displays 4 dimensions: Coverage, Relevance, Overlap, Continuity
- [ ] contextFit removed from display
- [ ] Percentages format used (e.g., "87%")
- [ ] Recommended badge always shows on highest-scoring package
- [ ] SuggestionPanel renders vertical list of ranked packages
</verification>

<success_criteria>
- "Why recommended" displays for recommended packages with top 2 contributing dimensions
- Score breakdown shows exactly 4 dimensions in percentage format
- Recommended badge on highest-scoring package
- Vertical list layout matching SuggestionPanel pattern
</success_criteria>

<output>
After completion, create `.planning/phases/41-full-auto-optimization-ranking/41-02-SUMMARY.md`
</output>
