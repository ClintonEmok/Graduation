---
phase: 48-api-stabilization
plan: 03
type: execute
wave: 1
depends_on: [48-01, 48-02]
files_modified:
  - src/lib/coordinate-normalization.ts
  - src/app/api/crimes/range/route.ts
  - src/app/api/crimes/range/route.test.ts
  - src/lib/queries.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "`/api/crimes/range` mock responses derive x/z from the shared coordinate adapter"
    - "`src/lib/queries.ts` no longer owns separate Chicago bounds for mock or SQL normalization"
    - "Range and stream consumers keep the existing -50..50 Chicago coordinate contract after unification"
  artifacts:
    - path: "src/lib/coordinate-normalization.ts"
      provides: "Canonical Chicago bounds plus reusable normalization helpers for JS and SQL consumers"
      exports: ["CHICAGO_BOUNDS", "lonLatToNormalized", "normalizedToLonLat"]
    - path: "src/app/api/crimes/range/route.ts"
      provides: "Range route mock response path"
      updates: "Uses shared normalization helper instead of route-local MIN/MAX ownership"
    - path: "src/lib/queries.ts"
      provides: "Mock/query normalization for range consumers"
      updates: "Uses shared bounds/helpers for mock generation, SQL projection, and spatial bin math"
  key_links:
    - from: "src/app/api/crimes/range/route.ts"
      to: "src/lib/coordinate-normalization.ts"
      via: "import and use `lonLatToNormalized` plus shared bounds"
      pattern: "lonLatToNormalized|CHICAGO_BOUNDS"
    - from: "src/lib/queries.ts"
      to: "src/lib/coordinate-normalization.ts"
      via: "import shared bounds and normalization helper(s) instead of local constants"
      pattern: "coordinate-normalization"
---

<objective>
Close the remaining Phase 48 verification gap by making `src/lib/coordinate-normalization.ts` the only Chicago normalization authority for the range route and query layer.

Purpose: The phase goal is only fully true when the shared adapter is actually wired through every relevant range consumer, not just the stream route.
Output: Shared normalization ownership across the range route and `src/lib/queries.ts`, with regression checks still proving coordinate contract parity.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/48-api-stabilization/48-api-stabilization-VERIFICATION.md
@.planning/phases/48-api-stabilization/48-01-SUMMARY.md
@.planning/phases/48-api-stabilization/48-02-SUMMARY.md
@src/lib/coordinate-normalization.ts
@src/app/api/crimes/range/route.ts
@src/app/api/crimes/range/route.test.ts
@src/lib/queries.ts

Verification gap to close:
- `src/app/api/crimes/range/route.ts` still owns `MIN_LON`/`MAX_LON`/`MIN_LAT`/`MAX_LAT` and computes mock `x`/`z` inline.
- `src/lib/queries.ts` still owns duplicate Chicago bounds and inline normalization formulas in mock generation and SQL projection.
- Buffering is already correct from `48-02`; do not reintroduce client-side or duplicate buffering changes while fixing normalization ownership.
</context>

<tasks>

<task type="auto">
  <name>Unify range route mock normalization with the shared adapter</name>
  <files>src/lib/coordinate-normalization.ts, src/app/api/crimes/range/route.ts, src/app/api/crimes/range/route.test.ts</files>
  <action>Extend `src/lib/coordinate-normalization.ts` only as needed to support all range consumers from one source of truth (for example, reusable spans or a SQL-safe projection helper), without changing the existing -50..50 contract. In `src/app/api/crimes/range/route.ts`, remove route-local Chicago bounds ownership, generate mock lon/lat from `CHICAGO_BOUNDS`, and compute mock `x`/`z` through `lonLatToNormalized` instead of inline formulas. Update `src/app/api/crimes/range/route.test.ts` so the mock-response parity assertion derives expected coordinates from the shared adapter and still verifies buffer metadata semantics.</action>
  <verify>`npm test -- --run src/app/api/crimes/range/route.test.ts` passes</verify>
  <done>`/api/crimes/range` imports the shared adapter, no longer defines local Chicago bounds for mock normalization, and route tests still prove response parity.</done>
</task>

<task type="auto">
  <name>Remove duplicate normalization ownership from the query layer</name>
  <files>src/lib/queries.ts, src/lib/coordinate-normalization.ts</files>
  <action>Refactor `src/lib/queries.ts` so all Chicago bounds come from `src/lib/coordinate-normalization.ts`. Replace local `MIN_*` and `MAX_*` constants with `CHICAGO_BOUNDS` in hotspot fallback, clamping, and any other spatial-range setup. Replace mock record `x`/`z` calculation with `lonLatToNormalized`. For SQL-backed `queryCrimesInRange`, stop embedding local normalization bounds/formulas; instead build the projection from shared adapter exports so the query layer no longer owns a second copy of the Chicago normalization contract. Also remove any remaining duplicate Chicago bound declarations in `queryDensityBins` so one adapter stays authoritative across this file.</action>
  <verify>`npm run typecheck && rg "MIN_LON|MAX_LON|MIN_LAT|MAX_LAT|const minLon = -87.9|const maxLon = -87.5|const minLat = 41.6|const maxLat = 42.1" src/app/api/crimes/range/route.ts src/lib/queries.ts` returns no matches</verify>
  <done>`src/lib/queries.ts` imports the shared normalization module for both mock and SQL paths, and duplicate Chicago bounds ownership is removed from the range data pipeline.</done>
</task>

</tasks>

<verification>
- `src/app/api/crimes/range/route.ts` imports shared normalization helpers instead of declaring Chicago bounds locally.
- `src/lib/queries.ts` uses shared adapter exports for mock generation and SQL projection inputs.
- `npm test -- --run src/app/api/crimes/range/route.test.ts src/lib/crime-api.test.ts` passes.
- `npm run typecheck` passes.
- `rg "MIN_LON|MAX_LON|MIN_LAT|MAX_LAT|const minLon = -87.9|const maxLon = -87.5|const minLat = 41.6|const maxLat = 42.1" src/app/api/crimes/range/route.ts src/lib/queries.ts` finds no remaining duplicate Chicago bounds ownership in the affected files.
</verification>

<success_criteria>
- The failed verification truth becomes true: all relevant Phase 48 consumers use the shared coordinate normalization adapter.
- `src/app/api/crimes/range/route.ts` and `src/lib/queries.ts` stop owning separate Chicago normalization bounds/formulas.
- Existing range/stream coordinate behavior stays contract-compatible while normalization ownership is centralized.
</success_criteria>

<output>
After completion, create `.planning/phases/48-api-stabilization/48-03-SUMMARY.md`
</output>
