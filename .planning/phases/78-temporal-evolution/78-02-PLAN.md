---
phase: 78-temporal-evolution
plan: 02
type: execute
wave: 2
depends_on:
  - 78-01
files_modified:
  - src/app/stkde-3d/components/StkdeSliceStack.tsx
autonomous: false
requirements_addressed:
  - TME-02
  - TME-03
must_haves:
  truths:
    - "The active slice anchors interpolation instead of being blended away."
    - "Consecutive slices morph and crossfade smoothly during playback when interpolation is enabled."
    - "Older slices remain as ghosted layers with short-lived persistence."
    - "Trail feedback stays subdued and readable, not shadow-like or strongly persistent."
    - "Temporal rendering changes stay inside the demo 3D STKDE widget path."
  artifacts:
    - path: "src/app/stkde-3d/components/StkdeSliceStack.tsx"
      provides: "Interpolation-aware slice rendering and ghosted aging trails for the demo 3D widget"
  key_links:
    - from: "src/app/stkde-3d/components/StkdeSliceStack.tsx"
      to: "src/store/useDashboardDemoCoordinationStore.ts"
      via: "activeSliceIndex, interpolation, and trail settings come from the shared demo temporal state"
      pattern: "activeSliceIndex|inspectInterpolation|inspectTrail"
    - from: "src/app/stkde-3d/components/StkdeSliceStack.tsx"
      to: "src/lib/motion/aging.ts"
      via: "trail persistence uses the existing decay helpers instead of inventing a second decay model"
      pattern: "computeTrailIntensity|buildAgingOpacityMap"
    - from: "src/app/stkde-3d/components/StkdeSliceStack.tsx"
      to: "src/lib/motion/easing.ts"
      via: "morph and crossfade timing reuse existing easing/interpolation helpers"
      pattern: "easeInOutCubic|interpolateKdeCells"
---

<objective>
Add the visual temporal motion layer inside the demo 3D STKDE widget: interpolated slice transitions and short-lived ghosted aging trails.

Purpose: phase 78’s visual payoff is that slice changes feel continuous without losing the active slice or turning the stack into a heavy history effect.
Output: a 3D stack that reads temporal change through morphing, crossfade, and bounded trail persistence.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/STKDE_TEMPORAL_SCOPE.md
@src/app/stkde-3d/components/StkdeSliceStack.tsx
@src/lib/motion/aging.ts
@src/lib/motion/easing.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add interpolation-aware slice blending around the active slice</name>
  <files>src/app/stkde-3d/components/StkdeSliceStack.tsx</files>
  <action>
    Rework the slice stack so consecutive slices morph and crossfade during playback when interpolation is enabled, with the active slice anchoring the blend instead of disappearing into a fully averaged state. Keep the existing volumetric depth cues from phase 77 intact, and do not introduce any map, timeline, or camera-orientation changes.
  </action>
  <verify>pnpm exec eslint src/app/stkde-3d/components/StkdeSliceStack.tsx</verify>
  <done>Interpolated playback reads as a smooth transition between neighboring slices rather than a hard cut.</done>
</task>

<task type="auto">
  <name>Task 2: Layer ghosted aging trails with short-lived persistence</name>
  <files>src/app/stkde-3d/components/StkdeSliceStack.tsx</files>
  <action>
    Add the aged ghost layers behind the active slice using subdued opacity falloff and bounded persistence. Keep the trail style explicitly ghosted layers, not shadow-like or strongly persistent depth echoes, and make sure the active slice stays dominant and readable at all times.
  </action>
  <verify>pnpm exec eslint src/app/stkde-3d/components/StkdeSliceStack.tsx</verify>
  <done>Analysts can perceive recency and decay inside the 3D widget without losing the current slice.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The demo 3D STKDE widget now shows interpolated playback and ghosted aging trails inside the slice stack.</what-built>
  <how-to-verify>
    1. Open the dashboard-demo experience at `/dashboard-demo`.
    2. Enable playback and confirm the active slice advances through the ordered slice list.
    3. Enable interpolation and confirm the slice transition reads as morph + crossfade, with the active slice still anchoring the blend.
    4. Inspect adjacent slices and confirm older slices persist only as faint ghosted layers.
    5. Confirm the map, timeline, and camera behavior did not change as part of this phase.
  </how-to-verify>
  <resume-signal>Type "approved" if the temporal rendering is legible, or describe the visual issues to fix.</resume-signal>
</task>

</tasks>

<verification>
Run the lint check above, then visually verify the demo 3D widget for readable interpolation and subdued ghosted trails.
</verification>

<success_criteria>
The demo 3D STKDE widget makes temporal change feel continuous, keeps the active slice clear, and renders aging as ghosted history rather than a heavy depth shadow.
</success_criteria>

<output>
After completion, create `.planning/phases/78-temporal-evolution/78-02-SUMMARY.md`
</output>
