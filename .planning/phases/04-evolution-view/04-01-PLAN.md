---
phase: 04-evolution-view
plan: 01
type: tdd
wave: 1
depends_on: []
requirements: [EVOL-03]
files_modified:
  - src/components/dashboard-demo/lib/useDemoEvolutionSequence.ts
  - src/components/dashboard-demo/lib/useDemoEvolutionSequence.test.ts
  - src/components/dashboard-demo/DemoEvolutionPanel.tsx
  - src/components/dashboard-demo/DemoEvolutionPanel.test.tsx
autonomous: true
must_haves:
  truths:
    - "Users can see slices ordered as a temporal evolution sequence."
    - "Users can step backward and forward through slices without leaving the sequence bounds."
    - "Users can start, pause, and inspect playback speed from the evolution panel."
  artifacts:
    - path: src/components/dashboard-demo/lib/useDemoEvolutionSequence.ts
      provides: "Temporal slice sequencing helper/hook"
      contains: "orderedSliceIds"
    - path: src/components/dashboard-demo/lib/useDemoEvolutionSequence.test.ts
      provides: "Sequence ordering and stepping regression coverage"
      contains: "nextSliceId"
    - path: src/components/dashboard-demo/DemoEvolutionPanel.tsx
      provides: "Evolution view rail panel"
      contains: "play/pause"
    - path: src/components/dashboard-demo/DemoEvolutionPanel.test.tsx
      provides: "Evolution panel source-contract coverage"
      contains: "Auto-play"
  key_links:
    - from: src/components/dashboard-demo/lib/useDemoEvolutionSequence.ts
      to: src/store/useDashboardDemoSliceStore.ts
      via: "visible slices -> ordered temporal sequence"
      pattern: "selectVisibleSlices|slices"
    - from: src/components/dashboard-demo/lib/useDemoEvolutionSequence.ts
      to: src/store/useDashboardDemoTimeStore.ts
      via: "current time + playback speed -> active step"
      pattern: "currentTime|speed|isPlaying"
---

<objective>
Build the evolution sequencing model and panel shell so the demo can step through slices in time order and auto-play them without duplicating analysis state.

Purpose: The evolution view needs a deterministic sequence model before the shell can render a readable filmstrip, playback controls, or active-step feedback.
Output: A pure sequence helper with regression tests and a compact panel that can drive slice stepping and playback state.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-adjacent-slice-comparison-burst-evolution/03-01-SUMMARY.md
@.planning/phases/03-adjacent-slice-comparison-burst-evolution/03-04-SUMMARY.md
@src/store/useDashboardDemoTimeStore.ts
@src/store/useDashboardDemoSliceStore.ts
@src/components/dashboard-demo/DemoTimelinePanel.tsx
@src/components/timeline/DemoDualTimeline.tsx
</context>

<feature>
  <name>Evolution sequence model</name>
  <files>src/components/dashboard-demo/lib/useDemoEvolutionSequence.ts, src/components/dashboard-demo/lib/useDemoEvolutionSequence.test.ts, src/components/dashboard-demo/DemoEvolutionPanel.tsx, src/components/dashboard-demo/DemoEvolutionPanel.test.tsx</files>
  <behavior>
    - Slices are ordered by time and range center, with invisible slices excluded.
    - The active step is derived from the current demo time and remains stable for the same inputs.
    - Stepping forward/backward resolves to the next/previous slice in sequence and clamps at the ends.
    - Playback state reuses the demo time store rather than introducing another independent timer.
  </behavior>
  <implementation>
    Start with the failing helper test that describes ordering, stepping, and empty-state behavior. Then implement a pure sequence helper plus a hook that reads the demo slice/time stores and a compact evolution panel that renders a stepped slice filmstrip, play/pause, and step controls. Keep the panel shell isolated from the cube animation work so it can ship independently.
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: RED — write the failing evolution sequence test</name>
  <files>src/components/dashboard-demo/lib/useDemoEvolutionSequence.test.ts</files>
  <read_first>src/store/useDashboardDemoTimeStore.ts, src/store/useDashboardDemoSliceStore.ts, src/components/dashboard-demo/lib/useDemoEvolutionSequence.ts</read_first>
  <action>
    Write a failing Vitest suite that asserts the evolution sequence helper orders visible slices by time, excludes hidden slices, resolves the active slice from the current time, and exposes next/previous ids with clamped bounds. Include an empty-state expectation so the helper cannot silently return an ambiguous shape.
  </action>
  <acceptance_criteria>
    - `useDemoEvolutionSequence.test.ts` contains `orderedSliceIds`, `nextSliceId`, and `previousSliceId` expectations.
    - The test covers an empty-state case.
    - The test fails before the helper is implemented.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement the sequence helper and hook</name>
  <files>src/components/dashboard-demo/lib/useDemoEvolutionSequence.ts</files>
  <read_first>src/store/useDashboardDemoTimeStore.ts, src/store/useDashboardDemoSliceStore.ts, src/components/dashboard-demo/lib/useDemoEvolutionSequence.test.ts</read_first>
  <action>
    Implement a pure `buildDemoEvolutionSequence()` helper that sorts visible slices by temporal position, derives the active step from the current time, and returns next/previous ids plus playback labels. Wrap it in `useDemoEvolutionSequence()` so the panel can read demo slice/time state without introducing a new store.
  </action>
  <acceptance_criteria>
    - `useDemoEvolutionSequence.ts` exports `buildDemoEvolutionSequence` and `useDemoEvolutionSequence`.
    - The helper returns deterministic ordering for the same inputs.
    - The helper exposes active, next, and previous slice ids.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: REFACTOR — build the evolution panel shell</name>
  <files>src/components/dashboard-demo/DemoEvolutionPanel.tsx, src/components/dashboard-demo/DemoEvolutionPanel.test.tsx</files>
  <read_first>src/components/dashboard-demo/lib/useDemoEvolutionSequence.ts, src/components/dashboard-demo/DemoTimelinePanel.tsx, src/store/useDashboardDemoTimeStore.ts</read_first>
  <action>
    Create `DemoEvolutionPanel.tsx` as a client component that renders the ordered slice filmstrip, the current step label, and play/pause plus previous/next controls. Add a compact source-contract test so the panel keeps the expected control labels and active-step text while remaining rail-friendly.
  </action>
  <acceptance_criteria>
    - `DemoEvolutionPanel.tsx` contains play/pause and step controls.
    - `DemoEvolutionPanel.test.tsx` contains the strings `play/pause`, `next`, and `previous`.
    - The panel can render an empty-state message without a selected sequence.
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Run `./node_modules/.bin/vitest src/components/dashboard-demo/lib/useDemoEvolutionSequence.test.ts src/components/dashboard-demo/DemoEvolutionPanel.test.tsx`.
2. Inspect the helper and panel source for `orderedSliceIds`, `nextSliceId`, `previousSliceId`, and `play/pause` controls.
</verification>

<success_criteria>
- The evolution panel can present slices in order and advance through them.
- Playback controls visibly affect the active step without mutating unrelated demo state.
- The sequence logic is deterministic and easy for the next phase to reuse for animation.
</success_criteria>

<output>
After completion, create `.planning/phases/04-evolution-view/04-01-SUMMARY.md`
</output>
