---
phase: 04-evolution-view
plan: 02
type: execute
wave: 2
depends_on: [04-01]
requirements: [EVOL-04, EVOL-05]
files_modified:
  - src/components/dashboard-demo/DashboardDemoRailTabs.tsx
  - src/components/viz/EvolutionFlowOverlay.tsx
  - src/components/viz/TimeSlices.tsx
  - src/components/viz/SlicePlane.tsx
  - src/lib/evolution/evolution-flow.ts
  - src/lib/evolution/evolution-flow.test.ts
  - src/components/viz/evolution-flow.phase4.test.tsx
autonomous: true
must_haves:
  truths:
    - "Users can open the Evolution tab from the demo rail."
    - "Cube slices visibly crossfade or highlight as the active evolution step changes."
    - "Directional flow cues connect consecutive slices so the temporal pattern reads as motion."
  artifacts:
    - path: src/components/dashboard-demo/DashboardDemoRailTabs.tsx
      provides: "Evolution rail tab entry"
      contains: "Evolution"
    - path: src/lib/evolution/evolution-flow.ts
      provides: "Slice-to-slice flow path helper"
      contains: "flowSegments"
    - path: src/components/viz/EvolutionFlowOverlay.tsx
      provides: "Cube flow visualization overlay"
      contains: "arrow"
    - path: src/components/viz/TimeSlices.tsx
      provides: "Scene wiring for evolution state"
      contains: "EvolutionFlowOverlay"
    - path: src/components/viz/SlicePlane.tsx
      provides: "Transition-aware slice plane rendering"
      contains: "opacity"
    - path: src/components/viz/evolution-flow.phase4.test.tsx
      provides: "Evolution overlay contract coverage"
      contains: "flowSegments"
  key_links:
    - from: src/components/dashboard-demo/DashboardDemoRailTabs.tsx
      to: src/components/dashboard-demo/DemoEvolutionPanel.tsx
      via: "TabsContent value=\"evolution\""
      pattern: "Evolution"
    - from: src/components/viz/TimeSlices.tsx
      to: src/components/viz/EvolutionFlowOverlay.tsx
      via: "active evolution step -> flow overlay"
      pattern: "EvolutionFlowOverlay"
    - from: src/components/viz/SlicePlane.tsx
      to: src/components/dashboard-demo/lib/useDemoEvolutionSequence.ts
      via: "active step index -> transition opacity"
      pattern: "activeStep|transition"
---

<objective>
Render the evolution view in the demo rail and add cube motion visuals so slice progression reads as a stepped animation with clear flow cues.

Purpose: The phase goal is only complete when the evolution panel is reachable from the rail and the cube visibly communicates slice-to-slice change.
Output: A new Evolution rail tab, transition-aware slice rendering, a directional flow overlay, and source-checked regression coverage.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-evolution-view/04-01-PLAN.md
@src/components/dashboard-demo/DashboardDemoRailTabs.tsx
@src/components/dashboard-demo/DemoEvolutionPanel.tsx
@src/components/viz/TimeSlices.tsx
@src/components/viz/SlicePlane.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Evolution rail tab and keep the shell layout stable</name>
  <files>src/components/dashboard-demo/DashboardDemoRailTabs.tsx</files>
  <read_first>src/components/dashboard-demo/DashboardDemoRailTabs.tsx, src/components/dashboard-demo/DemoEvolutionPanel.tsx, src/components/dashboard-demo/DashboardDemoShell.tsx</read_first>
  <action>
    Add an `Evolution` tab beside the existing analysis tabs and render `DemoEvolutionPanel` there. Keep the right rail fixed, preserve the map-first viewport layout, and update the tab grid so the new entry does not crowd out the existing analysis surfaces.
  </action>
  <acceptance_criteria>
    - `DashboardDemoRailTabs.tsx` contains `TabsTrigger value="evolution"`.
    - `DashboardDemoRailTabs.tsx` renders `DemoEvolutionPanel` in `TabsContent value="evolution"`.
    - The right rail still remains fixed beside the shared viewport.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Make cube slices transition-aware during evolution playback</name>
  <files>src/components/viz/TimeSlices.tsx, src/components/viz/SlicePlane.tsx</files>
  <read_first>src/components/viz/TimeSlices.tsx, src/components/viz/SlicePlane.tsx, src/components/dashboard-demo/lib/useDemoEvolutionSequence.ts, src/store/useDashboardDemoSliceStore.ts</read_first>
  <action>
    Thread the active evolution step into `TimeSlices.tsx`, then pass transition state into `SlicePlane.tsx` so the active slice can crossfade or highlight while adjacent slices fade back. Keep the underlying drag, resize, and creation behaviors unchanged; this task is only about transition rendering and step-aware emphasis.
  </action>
  <acceptance_criteria>
    - `TimeSlices.tsx` references the active evolution step from the sequence helper/hook.
    - `SlicePlane.tsx` contains transition-aware opacity or highlight logic.
    - Existing slice interaction handlers remain intact.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Render flow overlays and lock the contract</name>
  <files>src/lib/evolution/evolution-flow.ts, src/lib/evolution/evolution-flow.test.ts, src/components/viz/EvolutionFlowOverlay.tsx, src/components/viz/evolution-flow.phase4.test.tsx</files>
  <read_first>src/components/viz/TimeSlices.tsx, src/components/viz/EvolutionFlowOverlay.tsx, src/lib/evolution/evolution-flow.ts, src/components/viz/burst-evolution.phase3.test.tsx</read_first>
  <action>
    Add a pure `evolution-flow` helper that converts ordered visible slices into directional connector segments and labels, then render those segments in `EvolutionFlowOverlay.tsx` as directional cues above the cube. Add a compact regression test that verifies the overlay contract, flow segment naming, and the no-data fallback.
  </action>
  <acceptance_criteria>
    - `evolution-flow.ts` exports `flowSegments` or equivalent directional segment output.
    - `EvolutionFlowOverlay.tsx` references the flow helper and renders directional cues.
    - `evolution-flow.phase4.test.tsx` contains a no-data fallback assertion and a flow-segment assertion.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run the evolution overlay test, then source-check the rail tab, transition-aware slice rendering, and flow helper wiring.
</verification>

<success_criteria>
- Evolution is accessible from the demo rail.
- The cube visually communicates stepping from one slice to the next.
- Flow cues connect consecutive slices and do not render when there is no data.
</success_criteria>

<output>
After completion, create `.planning/phases/04-evolution-view/04-02-SUMMARY.md`
</output>
