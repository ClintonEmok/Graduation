---
phase: 44-3d-interaction-parity
plan: "01"
type: execute
wave: 1
depends_on: [43-01-SUMMARY]
files_modified:
  - src/app/timeline-test-3d/components/TimeSlices3D.tsx
  - src/app/timeline-test-3d/components/TimelineTest3DScene.tsx
  - src/app/timeline-test-3d/components/TimelineTest3DPoints.tsx
autonomous: true
must_haves:
  truths:
    - "Users can see time slices visualized as planes in the 3D scene"
    - "Users can click in 3D to create new time slices"
    - "Users can click on existing slices to select them in 3D"
  artifacts:
    - path: "src/app/timeline-test-3d/components/TimeSlices3D.tsx"
      provides: "3D slice visualization and create/select interactions"
      min_lines: 80
  key_links:
    - from: "TimelineTest3DScene.tsx"
      to: "TimeSlices3D.tsx"
      via: "component import"
      pattern: "TimeSlices3D"
    - from: "TimeSlices3D.tsx"
      to: "useSliceStore"
      via: "store hook"
      pattern: "useSliceStore"
---

<objective>
Add 3D slice visualization and create/select interactions to timeline-test-3d, matching timeline-test behavior.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-3d-timeline-test-foundation/43-01-SUMMARY.md
@src/app/timeline-test/components/SliceToolbar.tsx
@src/components/viz/TimeSlices.tsx
@src/components/viz/SlicePlane.tsx
@src/store/useSliceStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TimeSlices3D component for slice visualization</name>
  <files>src/app/timeline-test-3d/components/TimeSlices3D.tsx</files>
  <action>
    Create a new TimeSlices3D component that:
    - Imports useSliceStore, useDataStore, useTimeStore, useAdaptiveStore (like TimeSlices.tsx)
    - Imports SlicePlane or creates a simplified version for timeline-test-3d
    - Uses the same scale logic as TimelineTest3DPoints.tsx for Y-positioning (uses adaptive warp mapping)
    - Renders slice planes for each slice in useSliceStore
    - Handles double-click to create new slices at that Y position
    - Uses percent-based range [start, end] to compute Y positions (not time-based like original)

    Reference TimelineTest3DPoints.tsx for:
    - How to compute minDisplayTime/maxDisplayTime from data
    - How to map percent to Y position in 3D space
    - How to handle adaptive vs linear time scale

    Reference TimeSlices.tsx for:
    - How to handle double-click for slice creation
    - How to integrate with useSliceStore
  </action>
  <verify>
    Run build to ensure no TypeScript errors
  </verify>
  <done>
    TimeSlices3D component exists with slice visualization and create interaction
  </done>
</task>

<task type="auto">
  <name>Task 2: Add slice selection interaction in 3D</name>
  <files>src/app/timeline-test-3d/components/TimeSlices3D.tsx</files>
  <action>
    Extend TimeSlices3D to support slice selection:
    - Add click handler on slice planes to select/deselect slices
    - Use useSliceSelectionStore to manage selection state
    - Highlight selected slices with different color/opacity
    - Sync selection state with the timeline panel (shared store)

    Reference SlicePlane.tsx for selection visual feedback patterns
  </action>
  <verify>
    Run build to ensure no TypeScript errors
  </verify>
  <done>
    Clicking slices in 3D selects them and highlights visually
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire TimeSlices3D into TimelineTest3DScene</name>
  <files>src/app/timeline-test-3d/components/TimelineTest3DScene.tsx</files>
  <action>
    Update TimelineTest3DScene to include TimeSlices3D component:
    - Import TimeSlices3D from ./TimeSlices3D
    - Add <TimeSlices3D /> inside the <Scene> component, after TimelineTest3DPoints
  </action>
  <verify>
    Run build to ensure no TypeScript errors
  </verify>
  <done>
    TimeSlices3D renders inside the 3D scene alongside crime points
  </done>
</task>

</tasks>

<verification>
- [ ] Navigate to /timeline-test-3d and verify 3D scene renders
- [ ] Create a slice using the SliceToolbar, verify it appears as a plane in 3D
- [ ] Double-click in 3D space to create a slice, verify it works
- [ ] Click on a slice in 3D to select it, verify selection state syncs with timeline
</verification>

<success_criteria>
Users can see time slices visualized as planes in the 3D scene and can create/select them through 3D interactions.
</success_criteria>

<output>
After completion, create `.planning/phases/44-3d-interaction-parity/44-01-SUMMARY.md`
</output>
