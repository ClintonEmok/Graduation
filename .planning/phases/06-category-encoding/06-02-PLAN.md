---
phase: 06-category-encoding
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/components/viz/CrimeCategoryLegend.tsx
  - src/components/viz/SimpleCrimeLegend.tsx
  - src/components/map/MapTypeLegend.tsx
  - src/components/viz/category-legend.phase6.test.tsx
autonomous: true
requirements_addressed:
  - CAT-01
  - CAT-02
must_haves:
  truths:
    - "The cube and map legends render only the crime types present in the current viewport."
    - "Legend clicks continue to toggle the shared selectedTypes filter state."
    - "The cube legend is no longer a static 6-type list."
    - "The map legend keeps its hover/click affordances while using the shared legend model."
  artifacts:
    - path: "src/components/viz/CrimeCategoryLegend.tsx"
      provides: "Shared dynamic legend UI"
    - path: "src/components/viz/SimpleCrimeLegend.tsx"
      provides: "Cube legend wrapper around the shared legend UI"
    - path: "src/components/map/MapTypeLegend.tsx"
      provides: "Map legend wrapper around the shared legend UI"
    - path: "src/components/viz/category-legend.phase6.test.tsx"
      provides: "Source-inspection coverage for dynamic legend wiring"
  key_links:
    - from: "src/components/viz/CrimeCategoryLegend.tsx"
      to: "src/lib/category-legend.ts"
      via: "legend items are derived from the helper built in plan 01"
      pattern: "buildCategoryLegendEntries"
    - from: "src/components/viz/SimpleCrimeLegend.tsx"
      to: "src/store/useFilterStore.ts"
      via: "legend clicks toggle selectedTypes from the shared filter store"
      pattern: "selectedTypes|toggleType|setTypes"
    - from: "src/components/map/MapTypeLegend.tsx"
      to: "src/store/useFilterStore.ts"
      via: "map legend interaction remains the active category filter entry point"
      pattern: "selectedTypes|toggleType|setTypes"
---

<objective>
Replace the static map and cube crime-type legends with a shared data-driven legend that still drives the existing category filter state.

Purpose: the cube and map must display the same live category set and keep category toggles in sync.
Output: dynamic interactive legends on both surfaces, backed by the shared filter store.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/MVP-FINALE-ROADMAP.md
@.planning/phases/06-category-encoding/06-CONTEXT.md
@.planning/phases/06-category-encoding/06-RESEARCH.md
@src/components/viz/SimpleCrimeLegend.tsx
@src/components/map/MapTypeLegend.tsx
@src/store/useFilterStore.ts
@src/hooks/useCrimeData.ts
@src/lib/category-legend.ts
@src/lib/category-maps.ts
@src/lib/palettes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the shared dynamic category legend UI</name>
  <files>src/components/viz/CrimeCategoryLegend.tsx, src/components/viz/SimpleCrimeLegend.tsx, src/components/map/MapTypeLegend.tsx</files>
  <read_first>
    - src/components/viz/SimpleCrimeLegend.tsx
    - src/components/map/MapTypeLegend.tsx
    - src/lib/category-legend.ts
    - src/store/useFilterStore.ts
    - src/hooks/useCrimeData.ts
  </read_first>
  <action>
    Build a shared legend component that uses the helper from plan 01 and the current viewport crime records to render only present categories. Make the cube legend and map legend thin wrappers around that shared UI so the map keeps its hover/click affordances and the cube gains the same dynamic category list.
  </action>
  <acceptance_criteria>
    - CrimeCategoryLegend.tsx references `useCrimeData` and `buildCategoryLegendEntries`.
    - SimpleCrimeLegend.tsx no longer contains a hardcoded crime-type array.
    - MapTypeLegend.tsx no longer contains a hardcoded crime-type array.
  </acceptance_criteria>
  <verify>grep -n 'buildCategoryLegendEntries\|useCrimeData\|ORDERED_TYPES\|CrimeCategoryLegend' src/components/viz/CrimeCategoryLegend.tsx src/components/viz/SimpleCrimeLegend.tsx src/components/map/MapTypeLegend.tsx</verify>
  <done>The map and cube now read from the same data-driven category legend UI.</done>
</task>

<task type="auto">
  <name>Lock the dynamic legend and category toggle contract with source inspection</name>
  <files>src/components/viz/category-legend.phase6.test.tsx</files>
  <read_first>
    - src/components/viz/CrimeCategoryLegend.tsx
    - src/components/viz/SimpleCrimeLegend.tsx
    - src/components/map/MapTypeLegend.tsx
    - src/store/useFilterStore.ts
  </read_first>
  <action>
    Add a source-inspection regression test that fails if the legends fall back to hardcoded crime-type arrays, stop deriving legend items from actual viewport data, or stop toggling the shared category filter state when clicked.
  </action>
  <acceptance_criteria>
    - category-legend.phase6.test.tsx references `useCrimeData`.
    - category-legend.phase6.test.tsx references `toggleType` or `setTypes`.
    - category-legend.phase6.test.tsx references `buildCategoryLegendEntries`.
  </acceptance_criteria>
  <verify>grep -n 'useCrimeData\|toggleType\|setTypes\|buildCategoryLegendEntries' src/components/viz/category-legend.phase6.test.tsx</verify>
  <done>The dynamic legend path is pinned so the cube and map cannot drift back to static lists.</done>
</task>

</tasks>

<verification>
Confirm the map and cube legends render from actual data and continue to toggle the shared category filter state.
</verification>

<success_criteria>
The cube and map show the live crime categories present in the viewport, and legend clicks still filter both surfaces through the shared store.
</success_criteria>

<output>
After completion, create `.planning/phases/06-category-encoding/06-02-SUMMARY.md`
</output>
