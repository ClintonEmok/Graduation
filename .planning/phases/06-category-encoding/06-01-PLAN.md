---
phase: 06-category-encoding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/category-legend.ts
  - src/lib/category-legend.test.ts
autonomous: true
requirements_addressed:
  - CAT-01
must_haves:
  truths:
    - "A shared helper derives legend entries from actual viewport crime records."
    - "The helper returns only crime types present in the current data window."
    - "Legend entries carry stable ids, labels, counts, and palette colors."
    - "The helper contract is protected by tests for empty and populated inputs."
  artifacts:
    - path: "src/lib/category-legend.ts"
      provides: "Viewport-driven crime legend data model"
    - path: "src/lib/category-legend.test.ts"
      provides: "Legend helper regression coverage"
  key_links:
    - from: "src/lib/category-legend.ts"
      to: "src/hooks/useCrimeData.ts"
      via: "legend entries are built from actual viewport crime records"
      pattern: "CrimeRecord|count|palette"
    - from: "src/lib/category-legend.ts"
      to: "src/lib/category-maps.ts"
      via: "legend labels use the canonical crime-type name resolver"
      pattern: "getCrimeTypeName|getCrimeTypeId"
    - from: "src/lib/category-legend.test.ts"
      to: "src/lib/palettes.ts"
      via: "palette colors stay in sync with the active theme palette"
      pattern: "categoryColors"
---

<objective>
Create a shared crime-category legend helper that derives the legend model from actual viewport data instead of hardcoded type arrays.

Purpose: both the map and cube legends need the same data source before they can become dynamic.
Output: a reusable legend helper and tests that lock the data model.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/MVP-FINALE-ROADMAP.md
@.planning/phases/06-category-encoding/06-CONTEXT.md
@.planning/phases/06-category-encoding/06-RESEARCH.md
@src/hooks/useCrimeData.ts
@src/types/crime.ts
@src/lib/category-maps.ts
@src/lib/palettes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the shared viewport crime-legend helper</name>
  <files>src/lib/category-legend.ts</files>
  <read_first>
    - src/hooks/useCrimeData.ts
    - src/types/crime.ts
    - src/lib/category-maps.ts
    - src/lib/palettes.ts
  </read_first>
  <action>
    Add a pure helper that accepts viewport crime records plus a palette and returns legend entries for only the crime types present in the current data window. Each entry should include the crime type id, canonical label, display count, and palette color, sorted deterministically by count then label so the map and cube legends can stay in sync.
  </action>
  <acceptance_criteria>
    - category-legend.ts exports a legend-entry type and a builder function.
    - The builder ignores crime types not present in the viewport records.
    - The builder returns a stable ordering for repeated calls with the same input.
  </acceptance_criteria>
  <verify>grep -n 'legend|count|getCrimeTypeName|categoryColors' src/lib/category-legend.ts</verify>
  <done>The category legend can now be derived from actual data instead of hardcoded lists.</done>
</task>

<task type="auto">
  <name>Lock the legend helper with unit tests</name>
  <files>src/lib/category-legend.test.ts</files>
  <read_first>
    - src/lib/category-legend.ts
    - src/lib/category-maps.ts
    - src/lib/palettes.ts
  </read_first>
  <action>
    Add focused tests that verify the helper returns no items for empty input, counts repeated crime types correctly, and preserves deterministic ordering when counts match. Include at least one assertion that a known palette color is threaded through unchanged.
  </action>
  <acceptance_criteria>
    - category-legend.test.ts covers empty input and populated input.
    - category-legend.test.ts asserts a specific count for a repeated crime type.
    - category-legend.test.ts asserts the palette color is preserved on the legend entry.
  </acceptance_criteria>
  <verify>grep -n 'empty\|count\|categoryColors\|legend' src/lib/category-legend.test.ts</verify>
  <done>The legend helper is pinned down by regression tests and ready for UI wiring.</done>
</task>

</tasks>

<verification>
Confirm the helper produces deterministic legend entries from actual viewport crime records.
</verification>

<success_criteria>
The legend model can be derived from real data, and the helper contract is protected by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/06-category-encoding/06-01-SUMMARY.md`
</output>
