---
phase: 77-volumetric-duration-depth-encoding
plan: 02
type: execute
wave: 2
depends_on:
  - 77-01
files_modified:
  - src/components/dashboard-demo/Demo3dSpatialView.tsx
  - src/app/stkde-3d/components/Stkde3DScene.tsx
  - src/app/stkde-3d/components/StkdeSliceStack.tsx
autonomous: false
requirements_addressed:
  - VOL-01
  - VOL-03
  - VOL-04
  - VOL-05
must_haves:
  truths:
    - "Longer slice durations occupy visibly more depth or thickness than shorter durations."
    - "Adjacent slices do not collapse into a single flat surface when durations vary."
    - "Shorter durations remain visible when volumes overlap or cluster densely."
    - "The active slice stays clear while Inspect/3D interactions change the focused slice."
    - "The demo widget consumes the shared normalization settings instead of inventing a second scaling model."
  artifacts:
    - path: "src/components/dashboard-demo/Demo3dSpatialView.tsx"
      provides: "Demo widget entry point that computes ordered/count slices and passes normalized duration data onward"
    - path: "src/app/stkde-3d/components/Stkde3DScene.tsx"
      provides: "3D scene wrapper that forwards volumetric slice data to the stack"
    - path: "src/app/stkde-3d/components/StkdeSliceStack.tsx"
      provides: "Depth-aware slice rendering and overlap handling"
  key_links:
    - from: "src/components/dashboard-demo/Demo3dSpatialView.tsx"
      to: "src/app/stkde-3d/components/Stkde3DScene.tsx"
      via: "ordered/count slices and activeIndex already exist and should feed the depth model"
      pattern: "countedSlices|activeIndex"
    - from: "src/app/stkde-3d/components/Stkde3DScene.tsx"
      to: "src/app/stkde-3d/components/StkdeSliceStack.tsx"
      via: "scene props must carry the duration-volume profile through to the stack renderer"
      pattern: "sliceKdes|activeIndex|volume"
    - from: "src/app/stkde-3d/components/StkdeSliceStack.tsx"
      to: "src/store/useDashboardDemoCoordinationStore.ts"
      via: "active emphasis and opacity should respect the shared inspect/3D coordination state"
      pattern: "inspectSliceOpacity|activeIndex"
---

<objective>
Render slice duration as actual 3D depth in the demo 3D STKDE widget while keeping the active slice legible.

Purpose: phase 77’s visual payoff is that temporal extent reads at a glance instead of relying on labels or flat color alone.
Output: the demo 3D widget uses depth-aware volume shaping, overlap falloff, and synchronized active-slice emphasis.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/components/dashboard-demo/Demo3dSpatialView.tsx
@src/app/stkde-3d/components/Stkde3DScene.tsx
@src/app/stkde-3d/components/StkdeSliceStack.tsx
@src/app/stkde-3d/lib/volume-encoding.ts
@src/components/viz/Trajectory.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread normalized volume data from the demo view into the 3D scene</name>
  <files>src/components/dashboard-demo/Demo3dSpatialView.tsx, src/app/stkde-3d/components/Stkde3DScene.tsx</files>
  <action>
    Compute the duration-volume profile from the ordered/count slices in the demo wrapper, read the shared volume settings from the coordination state, and pass those values through the scene props alongside the existing activeIndex synchronization. Keep the current active-slice guard logic intact and do not introduce camera or spatial-orientation changes.
  </action>
  <verify>pnpm exec eslint src/components/dashboard-demo/Demo3dSpatialView.tsx src/app/stkde-3d/components/Stkde3DScene.tsx</verify>
  <done>The 3D scene receives normalized duration-volume data from the demo path without changing the current interaction model.</done>
</task>

<task type="auto">
  <name>Task 2: Replace flat slice planes with depth-aware volume rendering</name>
  <files>src/app/stkde-3d/components/StkdeSliceStack.tsx</files>
  <action>
    Rework the slice stack so each slice renders as a depth-aware slab or layered volume instead of a flat CanvasTexture plane. Use the normalized duration profile to control thickness/extrusion, inter-slice spacing, and overlap falloff; keep the existing heatmap texture, labels, and active-ring cues, but make sure shorter slices remain visible when volumes overlap. Borrow the idea of derived thickness from the trajectory component only as a visual pattern, not as a new interaction model.
  </action>
  <verify>pnpm exec eslint src/app/stkde-3d/components/StkdeSliceStack.tsx</verify>
  <done>Longer durations read as deeper/thicker volumes, and the stack stays legible when adjacent slices crowd together.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The demo 3D STKDE widget now encodes slice duration as volume/depth with normalized scaling and active-slice emphasis.</what-built>
  <how-to-verify>
    1. Open the dashboard-demo experience at `/dashboard-demo`.
    2. Inspect at least one short-duration slice and one long-duration slice.
    3. Confirm the longer slice occupies visibly more depth/thickness than the short slice.
    4. Switch active slices in Inspect/3D and confirm the active slice stays clear instead of flattening into the stack.
    5. Change the visible time window or slice count and confirm the relative volume still reads consistently after normalization.
    6. Confirm no camera-constraint or general-orientation behavior was added as part of this phase.
  </how-to-verify>
  <resume-signal>Type "approved" if the volumetric encoding is legible, or describe the visual issues to fix.</resume-signal>
</task>

</tasks>

<verification>
Run the eslint checks above, then visually verify the dashboard-demo 3D widget for readable depth cues and stable active-slice emphasis.
</verification>

<success_criteria>
The demo 3D STKDE widget makes duration legible as depth, preserves readability under overlap, and keeps active-slice interactions synchronized.
</success_criteria>

<output>
After completion, create `.planning/phases/77-volumetric-duration-depth-encoding/77-02-SUMMARY.md`
</output>
