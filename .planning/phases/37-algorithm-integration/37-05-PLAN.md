---
phase: 37-algorithm-integration
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/useSuggestionStore.ts
  - src/app/timeslicing/components/SuggestionCard.tsx
  - src/app/timeslicing/components/SuggestionPanel.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Bulk select and accept/reject multiple suggestions"
    - "Save generation presets for reuse"
  artifacts:
    - path: "src/store/useSuggestionStore.ts"
      provides: "Selection state and preset storage"
---

<objective>
Add bulk actions and presets - enable users to work more efficiently with multiple suggestions.

Purpose: Power user features that improve workflow for advanced users.
</objective>

<context>
Current state:
- Must accept/reject one at a time
- No way to save preferred generation settings

These are efficiency improvements for power users.
</context>

<tasks>

<task type="auto">
  <name>Add bulk selection and actions</name>
  <files>src/store/useSuggestionStore.ts, src/app/timeslicing/components/SuggestionPanel.tsx</files>
  <action>
    Enable batch operations:
    1. Add selectedIds Set to store for tracking selected suggestions
    2. Add selection actions: selectOne, selectAll, deselectAll, toggleSelect
    3. Add bulk Accept Selected and Reject Selected buttons to panel header
    4. Add checkboxes to each SuggestionCard for selection
    5. Show selection count in panel header
  </action>
  <verify>Can select multiple and accept/reject all at once</verify>
  <done>Bulk operations work for efficient batch processing</done>
</task>

<task type="auto">
  <name>Add generation presets</name>
  <files>src/store/useSuggestionStore.ts, src/app/timeslicing/components/SuggestionToolbar.tsx</files>
  <action>
    Allow saving generation parameter presets:
    1. Add presets array to store: { name, warpCount, intervalCount, snapToUnit, boundaryMethod }[]
    2. Add savePreset action that stores current toolbar settings with a name
    3. Add preset selector dropdown to toolbar (presets + "Custom")
    4. Show preset name in toolbar when one is active
    5. Persist presets to localStorage
  </action>
  <verify>Can save current settings as preset and reload them</verify>
  <done>Presets enable quick access to preferred configurations</done>
</task>

</tasks>

<verification>
- [ ] Bulk select and accept/reject works
- [ ] Presets can be saved and loaded
</verification>

<success_criteria>
Bulk actions and presets implemented for power users
</success_criteria>

<output>
After completion, create `.planning/phases/37-algorithm-integration/37-05-SUMMARY.md`
</output>
