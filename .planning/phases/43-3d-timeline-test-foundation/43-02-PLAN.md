---
phase: 43-3d-timeline-test-foundation
plan: '02'
type: execute
wave: 2
depends_on: ['43-01']
files_modified:
  - src/app/timeline-test-3d/page.tsx
  - src/app/timeline-test-3d/components/TimelineTest3DScene.tsx
  - src/app/timeline-test-3d/components/TimelineTest3DPoints.tsx
  - src/app/page.tsx
autonomous: false
user_setup: []

must_haves:
  truths:
    - "3D view and timeline panel stay synchronized to the same selected range and adaptive state"
    - "Selecting a point in 3D updates timeline/details selection using canonical useDataStore index mapping"
    - "The new route is discoverable from home for QA execution"
  artifacts:
    - path: "src/app/timeline-test-3d/components/TimelineTest3DScene.tsx"
      provides: "Route-local 3D scene surface for timeline-test-3d"
      contains: "Scene + CameraControls composition without viewport fetch coupling"
    - path: "src/app/timeline-test-3d/components/TimelineTest3DPoints.tsx"
      provides: "Store-backed 3D point rendering and canonical selection index emission"
      contains: "useDataStore-driven points and useCoordinationStore setSelectedIndex"
    - path: "src/app/timeline-test-3d/page.tsx"
      provides: "Timeline + 3D composition bound to same stores/domain"
      contains: "Timeline panel and 3D scene mounted in one route layout"
  key_links:
    - from: "src/app/timeline-test-3d/components/TimelineTest3DPoints.tsx"
      to: "src/store/useCoordinationStore.ts"
      via: "point click sets canonical selectedIndex"
      pattern: "setSelectedIndex"
    - from: "src/app/timeline-test-3d/components/TimelineTest3DPoints.tsx"
      to: "src/store/useDataStore.ts"
      via: "render source and index alignment"
      pattern: "useDataStore"
    - from: "src/app/timeline-test-3d/page.tsx"
      to: "src/components/timeline/DualTimeline.tsx"
      via: "shared stores for synchronized timeline/cube rendering"
      pattern: "DualTimeline"
---

<objective>
Complete Phase 43 sync guarantees by adding a route-local 3D scene adapter that consumes the same canonical stores as timeline, then verify interaction parity for range and selection synchronization.

Purpose: Satisfy the core synchronization criterion for this phase and de-risk Phase 44 by ensuring 3D interactions already align with timeline-test state contracts.
Output: Store-backed 3D scene components integrated into `/timeline-test-3d`, plus QA entry access and human verification checkpoint.
Constraint: Keep this experience inside its own route folder (`src/app/timeline-test-3d/`), with no embedding into `/timeline-test` or `/dashboard` folders.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-3d-timeline-test-foundation/43-RESEARCH.md
@.planning/phases/43-3d-timeline-test-foundation/43-01-SUMMARY.md
@src/components/viz/Scene.tsx
@src/components/viz/SimpleCrimePoints.tsx
@src/components/timeline/DualTimeline.tsx
@src/lib/selection.ts
@src/store/useDataStore.ts
@src/store/useCoordinationStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build route-local 3D scene and canonical point adapter</name>
  <files>src/app/timeline-test-3d/components/TimelineTest3DScene.tsx, src/app/timeline-test-3d/components/TimelineTest3DPoints.tsx</files>
  <action>
    Create route-local 3D components that intentionally avoid dashboard viewport-fetch coupling:
    - `TimelineTest3DScene.tsx`: compose `Scene` and camera controls for the test route surface.
    - `TimelineTest3DPoints.tsx`: render points from `useDataStore` data (not `useCrimeData` inside component), filter by current shared range state, and emit canonical point indices.

    Required behavior for `TimelineTest3DPoints.tsx`:
    - Use deterministic index mapping aligned to `useDataStore.data` ordering.
    - On click, call `useCoordinationStore.setSelectedIndex(index, 'cube')` and open details.
    - Respect current time mode/adaptive state for y-axis placement so timeline + 3D remain semantically aligned.

    Do not modify `src/components/viz/MainScene.tsx` or `src/components/viz/SimpleCrimePoints.tsx` in this plan.
  </action>
  <verify>npx eslint src/app/timeline-test-3d/components/TimelineTest3DScene.tsx src/app/timeline-test-3d/components/TimelineTest3DPoints.tsx</verify>
  <done>3D route has isolated scene components that read canonical stores and emit correct selection indices for timeline synchronization.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate 3D scene with timeline route and expose QA entry point</name>
  <files>src/app/timeline-test-3d/page.tsx, src/app/page.tsx</files>
  <action>
    Integrate the new scene adapter into `/timeline-test-3d`:
    - Mount `TimelineTest3DScene` in the page layout alongside `DualTimeline` and existing control stack from Plan 43-01.
    - Ensure selected time range, active adaptive mode/source, and selected point state are reflected consistently between timeline and 3D.
    - Keep route orchestration copied/local; do not introduce shared abstraction extraction in this step.

    Add a home-page entry in `src/app/page.tsx` linking to `/timeline-test-3d` so QA can run the new flow without manual URL typing.
  </action>
  <verify>npx eslint src/app/timeline-test-3d/page.tsx src/app/page.tsx && npx tsc --noEmit</verify>
  <done>The timeline-test-3d route visibly includes both timeline and 3D surfaces using shared stores, and the route is reachable from the home page.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Dedicated 3D timeline-test route with shared-domain timeline + 3D synchronization and canonical point selection wiring</what-built>
  <how-to-verify>
    1) Start app: `npm run dev`.
    2) Open `http://localhost:3000`, then enter the new "timeline test 3D" path from home.
    3) Confirm timeline panel, core controls, and 3D scene all render on `/timeline-test-3d`.
    4) Change time scale mode and warp source/mode; verify timeline and 3D view both respond (no one-sided updates).
    5) Brush or change selected range in timeline; verify visible 3D points update to the same temporal context.
    6) Click multiple 3D points; verify selection/details correspond to expected timeline points (no index mismatch drift).
    7) Trigger generation/accept controls; verify no duplicate action firing after route refresh/re-entry.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe the failing behavior and step number.</resume-signal>
</task>

</tasks>

<verification>
- [ ] `npx eslint src/app/timeline-test-3d/components/TimelineTest3DScene.tsx src/app/timeline-test-3d/components/TimelineTest3DPoints.tsx src/app/timeline-test-3d/page.tsx src/app/page.tsx` passes.
- [ ] `npx tsc --noEmit` passes after integration.
- [ ] `/timeline-test-3d` shows timeline + 3D surfaces with shared runtime state.
- [ ] 3D point selection drives canonical coordination index and timeline reflects selection consistently.
- [ ] Route remains parity-copy based (no cross-surface consolidation required).
</verification>

<success_criteria>
- Phase 43 sync criterion is met: timeline and 3D surfaces stay aligned on domain/range/selection state.
- CUBE-01 and CUBE-02 are fully usable on the dedicated 3D route.
- CUBE-09 remains satisfied: implementation uses 3D-specific route logic copies and route-local adapters without mandatory shared refactor.
</success_criteria>

<output>
After completion, create `.planning/phases/43-3d-timeline-test-foundation/43-02-SUMMARY.md`
</output>
