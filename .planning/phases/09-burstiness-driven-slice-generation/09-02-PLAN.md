---
phase: 09-burstiness-driven-slice-generation
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/components/dashboard-demo/WorkflowSkeleton.tsx
  - src/components/dashboard-demo/DemoSlicePanel.tsx
  - src/components/timeline/DemoDualTimeline.tsx
  - src/app/dashboard-demo/page.shell.test.tsx
autonomous: true
must_haves:
  truths:
    - "The dashboard-demo workflow exposes an explicit user action for generating burst drafts."
    - "Burst draft state is visible in the workflow rail and timeline before apply."
    - "If no burst windows are available, the UI shows a preset-bias fallback instead of an empty draft state."
    - "The stable dashboard-demo route still does not gain a standalone burst map mode."
  artifacts:
    - path: "src/components/dashboard-demo/WorkflowSkeleton.tsx"
      provides: "Explicit burst generation trigger and fallback copy"
    - path: "src/components/dashboard-demo/DemoSlicePanel.tsx"
      provides: "Burst draft summary and review controls in the side rail"
    - path: "src/components/timeline/DemoDualTimeline.tsx"
      provides: "Burst draft preview styling in the timeline"
    - path: "src/app/dashboard-demo/page.shell.test.tsx"
      provides: "Source-inspection coverage for the burst flow and route boundary"
  key_links:
    - from: "src/components/dashboard-demo/WorkflowSkeleton.tsx"
      to: "src/store/useDashboardDemoTimeslicingModeStore.ts"
      via: "Generate burst drafts button calls the new store action"
      pattern: "useBurstWindows|generateBurstDraftBinsFromWindows"
    - from: "src/components/dashboard-demo/DemoSlicePanel.tsx"
      to: "src/components/timeline/DemoDualTimeline.tsx"
      via: "pending burst draft metadata stays consistent across rail and timeline preview"
      pattern: "pendingGeneratedBins|Burst draft|isGeneratedDraft"
    - from: "src/app/dashboard-demo/page.shell.test.tsx"
      to: "src/components/dashboard-demo/WorkflowSkeleton.tsx"
      via: "source inspection locks the new burst button and fallback copy"
      pattern: "Generate burst drafts|fallback|Burst draft"
---

<objective>
Expose burst-driven draft generation in the dashboard-demo workflow and surface those drafts clearly in the rail and timeline before apply.

Purpose: make the burst path intentional, inspectable, and visibly distinct from the existing preset-bias generation flow.
Output: workflow controls, rail copy, timeline preview styling, and source-inspection coverage.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-burstiness-driven-slice-generation/09-CONTEXT.md
@src/components/viz/BurstList.tsx
@src/components/dashboard-demo/WorkflowSkeleton.tsx
@src/components/dashboard-demo/DemoSlicePanel.tsx
@src/components/timeline/DemoDualTimeline.tsx
@src/store/useDashboardDemoTimeslicingModeStore.ts
@src/store/useDashboardDemoTimeStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a deliberate burst-generation control to the workflow skeleton</name>
  <files>src/components/dashboard-demo/WorkflowSkeleton.tsx</files>
  <read_first>
    - src/components/dashboard-demo/WorkflowSkeleton.tsx
    - src/components/viz/BurstList.tsx
    - src/store/useDashboardDemoTimeslicingModeStore.ts
    - src/components/dashboard-demo/lib/demo-burst-generation.ts
    - src/store/useDashboardDemoTimeStore.ts
    - src/store/useTimelineDataStore.ts
  </read_first>
  <action>
    Add an explicit `Generate burst drafts` action in the Build step that pulls the current burst windows from the existing timeline burst hook and passes them into the new store action. Show a compact fallback message when no burst windows overlap the active selection, and keep the button deliberately user-triggered rather than auto-running on every selection change. Preserve the existing generate/apply controls for the preset-bias path so the burst path is an addition, not a replacement.
  </action>
  <acceptance_criteria>
    - `src/components/dashboard-demo/WorkflowSkeleton.tsx` contains the string `Generate burst drafts`.
    - The file references `useBurstWindows` and `generateBurstDraftBinsFromWindows`.
    - The file contains fallback copy for the no-burst case.
  </acceptance_criteria>
  <verify>grep -n "Generate burst drafts\|useBurstWindows\|generateBurstDraftBinsFromWindows\|fallback" src/components/dashboard-demo/WorkflowSkeleton.tsx</verify>
  <done>The workflow shell can deliberately launch burst-driven draft generation from the current timeline burst windows.</done>
</task>

<task type="auto">
  <name>Task 2: Show burst draft review state in the slice rail and timeline</name>
  <files>src/components/dashboard-demo/DemoSlicePanel.tsx, src/components/timeline/DemoDualTimeline.tsx</files>
  <read_first>
    - src/components/dashboard-demo/DemoSlicePanel.tsx
    - src/components/timeline/DemoDualTimeline.tsx
    - src/store/useDashboardDemoTimeslicingModeStore.ts
    - src/store/slice-domain/types.ts
    - src/components/dashboard-demo/lib/demo-burst-generation.ts
  </read_first>
  <action>
    Update the slice rail to show the burst draft count, burst-first labeling, and a short summary of the current burst-generated draft set while keeping merge, split, delete, and apply controls intact. Update the dual timeline preview layer so pending generated bins read as burst drafts with a subtle burst cue, while leaving the map relationship unchanged and avoiding any new standalone burst map mode.
  </action>
  <acceptance_criteria>
    - `src/components/dashboard-demo/DemoSlicePanel.tsx` contains burst draft summary copy.
    - `src/components/timeline/DemoDualTimeline.tsx` contains burst-draft preview styling and `isGeneratedDraft`.
    - Both files still reference `pendingGeneratedBins`.
  </acceptance_criteria>
  <verify>grep -n "pendingGeneratedBins\|isGeneratedDraft\|Burst draft\|Burst 1\|burstClass" src/components/dashboard-demo/DemoSlicePanel.tsx src/components/timeline/DemoDualTimeline.tsx</verify>
  <done>Burst drafts are visible in the workflow rail and timeline before they are applied.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the burst workflow with source-inspection coverage</name>
  <files>src/app/dashboard-demo/page.shell.test.tsx</files>
  <read_first>
    - src/app/dashboard-demo/page.shell.test.tsx
    - src/components/dashboard-demo/WorkflowSkeleton.tsx
    - src/components/dashboard-demo/DemoSlicePanel.tsx
    - src/components/timeline/DemoDualTimeline.tsx
    - src/store/useDashboardDemoTimeslicingModeStore.ts
    - src/store/slice-domain/createSliceCoreSlice.ts
  </read_first>
  <action>
    Extend the existing dashboard-demo shell test so it source-inspects the new burst generation control, the burst draft summary copy, the timeline preview styling, and the burst-aware slice application path. Keep the existing stable-route isolation assertions intact so the demo still proves burst logic stays out of the stable `/timeslicing` route.
  </action>
  <acceptance_criteria>
    - The shell test matches `Generate burst drafts`, `generateBurstDraftBinsFromWindows`, `Burst draft`, `isGeneratedDraft`, and `isBurst: true`.
    - The shell test still asserts the stable `/timeslicing` route does not gain demo-local burst generation UI.
    - `pnpm exec vitest run src/app/dashboard-demo/page.shell.test.tsx --reporter=dot` passes.
  </acceptance_criteria>
  <verify>pnpm exec vitest run src/app/dashboard-demo/page.shell.test.tsx --reporter=dot</verify>
  <done>The burst workflow is source-locked and the stable route boundary remains intact.</done>
</task>

</tasks>

<verification>
Run the dashboard-demo shell test and source-inspection checks for the burst generation button, burst preview copy, and stable-route isolation.
</verification>

<success_criteria>
Users can deliberately generate burst drafts, inspect them before apply, and still keep the stable dashboard route free of burst-specific workflow chrome.
</success_criteria>

<output>
After completion, create `.planning/phases/09-burstiness-driven-slice-generation/09-02-SUMMARY.md`
</output>
