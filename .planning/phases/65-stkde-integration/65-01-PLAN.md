---
phase: 65-stkde-integration
plan: 01
type: execute
wave: 1
depends_on: [64-02]
files_modified:
  - src/store/useStkdeStore.ts
  - src/app/dashboard-v2/hooks/useDashboardStkde.ts
  - src/app/dashboard-v2/hooks/useDashboardStkde.test.ts
autonomous: true
requirements: [STKD-01, STKD-02, STKD-03]
requirements_addressed: [STKD-01, STKD-02, STKD-03]

must_haves:
  truths:
    - "User can run STKDE manually from dashboard-v2 and see run status/provenance"
    - "Applied-slice changes mark STKDE output stale instead of silently recomputing"
    - "STKDE run scope is explicit (Applied Slices vs Full Viewport)"
  artifacts:
    - path: "src/store/useStkdeStore.ts"
      provides: "Dashboard STKDE state contract (params, run status, results, scope, stale/provenance state)"
      contains: "scopeMode, isStale, runStatus, runMeta, runStkde, cancelRun"
    - path: "src/app/dashboard-v2/hooks/useDashboardStkde.ts"
      provides: "dashboard-v2 STKDE orchestration (request/cancel/projection/stale marking)"
      contains: "AbortController, /api/stkde/hotspots POST, worker projection, explicit manual trigger"
    - path: "src/app/dashboard-v2/hooks/useDashboardStkde.test.ts"
      provides: "Regression coverage for run/cancel/stale orchestration"
      contains: "manual-run-only behavior and stale-on-slice-change assertions"
  key_links:
    - from: "src/app/dashboard-v2/hooks/useDashboardStkde.ts"
      to: "/api/stkde/hotspots"
      via: "explicit runStkde action with bounded payload"
      pattern: "fetch\('/api/stkde/hotspots'"
    - from: "src/app/dashboard-v2/hooks/useDashboardStkde.ts"
      to: "src/workers/stkdeHotspot.worker.ts"
      via: "worker postMessage for hotspot projection/filtering"
      pattern: "new Worker\(|postMessage\("
---

<objective>
Create the reusable STKDE orchestration foundation for dashboard-v2.

Purpose: Reuse proven Phase 55 compute/worker patterns while enforcing Phase 65 decisions (manual trigger, staleness, provenance, scope clarity).

Output: A dashboard-grade STKDE store + hook that other plans can wire into map/panel/cube UI.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-stkde-integration/65-CONTEXT.md
@.planning/phases/65-stkde-integration/65-RESEARCH.md
@.planning/phases/55-stkde-exploration-route-with-chicago-heatmap-and-hotspots-panel/55-01-SUMMARY.md
@src/store/useStkdeStore.ts
@src/app/stkde/lib/StkdeRouteShell.tsx
@src/lib/stkde/contracts.ts
@src/workers/stkdeHotspot.worker.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand STKDE store to dashboard workflow contract</name>
  <files>src/store/useStkdeStore.ts</files>
  <read_first>
  - src/store/useStkdeStore.ts
  - src/lib/stkde/contracts.ts
  - .planning/phases/65-stkde-integration/65-CONTEXT.md
  </read_first>
  <action>
  Extend `useStkdeStore` beyond selection-only state to include explicit dashboard run lifecycle.

  Add concrete fields with these defaults:
  - `scopeMode: 'applied-slices' | 'full-viewport'` (default `applied-slices`)
  - `params.spatialBandwidthMeters` default `750`
  - `params.temporalBandwidthHours` default `24`
  - `params.gridCellMeters` default `500`
  - `params.topK` default `12`
  - `params.minSupport` default `5`
  - `params.timeWindowHours` default `24`
  - `runStatus: 'idle' | 'running' | 'success' | 'error' | 'cancelled'` (default `idle`)
  - `isStale` boolean (default `false`)
  - `errorMessage: string | null`
  - `response` payload bucket for `heatmap`, `hotspots`, `meta`
  - `lastRunAt` timestamp and `runMeta` object carrying `requestedComputeMode`, `effectiveComputeMode`, `truncated`, `fallbackApplied`, `clampsApplied`

  Add actions:
  - `setScopeMode(mode)`
  - `setParams(patch)`
  - `markStale(reason)`
  - `startRun()` / `finishRunSuccess(response)` / `finishRunError(message)` / `finishRunCancelled()`
  - `setHotspotSelection(selectedId, hoveredId)`
  - `resetRunArtifacts()` (keeps params/scope, clears results/errors/stale)

  Preserve existing `selectedHotspotId`/`hoveredHotspotId` API compatibility so `/stkde` route behavior does not regress.
  </action>
  <acceptance_criteria>
  - `src/store/useStkdeStore.ts` contains literal `scopeMode` union with values `applied-slices` and `full-viewport`.
  - `src/store/useStkdeStore.ts` contains literal `runStatus` union with `idle`, `running`, `success`, `error`, `cancelled`.
  - `src/store/useStkdeStore.ts` contains functions named `markStale`, `startRun`, and `finishRunSuccess`.
  - Store initialization contains numeric defaults `750`, `24`, `500`, `12`, `5`, `24` for STKDE params.
  </acceptance_criteria>
  <verify>pnpm -s tsc --noEmit</verify>
  <done>Dashboard STKDE state can represent scope, staleness, run lifecycle, and provenance without ad-hoc page state.</done>
</task>

<task type="auto">
  <name>Task 2: Add dashboard STKDE orchestration hook with manual run and cancellation</name>
  <files>src/app/dashboard-v2/hooks/useDashboardStkde.ts</files>
  <read_first>
  - src/app/stkde/lib/StkdeRouteShell.tsx
  - src/lib/stkde/contracts.ts
  - src/workers/stkdeHotspot.worker.ts
  - src/store/useStkdeStore.ts
  - src/store/useSliceDomainStore.ts
  </read_first>
  <action>
  Create `useDashboardStkde.ts` as the single orchestration hook consumed by `dashboard-v2` UI.

  Implement concrete behavior:
  1. Export `runStkde`, `cancelStkde`, `setScopeMode`, `setParams`, and derived `scopeLabel`.
  2. `runStkde` must POST to `/api/stkde/hotspots` with request payload shaped exactly like Phase 55 contract (`domain`, `filters`, `params`, `limits`, `guardrails`, `callerIntent: 'stkde'`).
  3. Use `AbortController` newest-request-wins behavior (`abortRef.current?.abort()` before a new run).
  4. Use worker projection (`new Worker(...stkdeHotspot.worker.ts...)`) for hotspot ranking/filtering after response.
  5. Do not auto-run on parameter edits; run only when `runStkde` is invoked.
  6. Observe applied slice changes from `useSliceDomainStore`; when applied slices change after a successful run, set `isStale=true` and keep previous results visible.
  7. Map stale reason to a user-facing status token (`applied-slices-updated`) stored in STKDE store.
  </action>
  <acceptance_criteria>
  - `src/app/dashboard-v2/hooks/useDashboardStkde.ts` contains `fetch('/api/stkde/hotspots'`.
  - `src/app/dashboard-v2/hooks/useDashboardStkde.ts` contains `AbortController` and abort-before-rerun logic.
  - `src/app/dashboard-v2/hooks/useDashboardStkde.ts` contains worker initialization referencing `stkdeHotspot.worker.ts`.
  - Hook source contains no auto-run `useEffect` tied directly to params edits.
  - Hook source contains logic marking stale with token `applied-slices-updated` when applied slices change.
  </acceptance_criteria>
  <verify>pnpm -s tsc --noEmit && pnpm vitest src/app/dashboard-v2/hooks/useDashboardStkde.test.ts --run</verify>
  <done>dashboard-v2 has one reusable STKDE controller with explicit run/cancel semantics and stale-state awareness.</done>
</task>

</tasks>

<verification>
1. Store and hook compile and expose explicit run/scope/stale contract.
2. STKDE orchestration remains manual-triggered and cancel-capable.
3. Applied-slice updates mark STKDE output stale while preserving prior results.
</verification>

<success_criteria>
- STKD-01/03 foundation exists for in-route overlay compute orchestration.
- STKD-02 foundation exists for hotspot metadata/provenance state.
- No route split is required to run STKDE from dashboard-v2.
</success_criteria>

<output>
After completion, create `.planning/phases/65-stkde-integration/65-01-SUMMARY.md`
</output>
