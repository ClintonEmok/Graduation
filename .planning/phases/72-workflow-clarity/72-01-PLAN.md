---
phase: 72-workflow-clarity
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [FLOW-07]
files_modified:
  - src/components/dashboard-demo/DashboardDemoRailTabs.tsx
  - src/components/dashboard-demo/DemoDetectPanel.tsx
autonomous: true
must_haves:
  truths:
    - "Detect is the first obvious workflow entry for burst scanning and slice generation"
    - "Users can tell from the Detect panel what prerequisite must exist before generation runs"
    - "Burst scan and slice generation feedback is unambiguous after the action finishes"
  artifacts:
    - path: "src/components/dashboard-demo/DashboardDemoRailTabs.tsx"
      provides: "Workflow tab order and labels"
    - path: "src/components/dashboard-demo/DemoDetectPanel.tsx"
      provides: "Primary burst scan/generation controls and guidance"
  key_links:
    - from: "src/components/dashboard-demo/DashboardDemoRailTabs.tsx"
      to: "src/components/dashboard-demo/DemoDetectPanel.tsx"
      via: "first primary tab entry"
      pattern: "value=\"detect\""
    - from: "src/components/dashboard-demo/DemoDetectPanel.tsx"
      to: "useDashboardDemoTimeslicingModeStore"
      via: "scan/generate handlers"
      pattern: "generateBurstDraftBinsFromWindows|fetchBurstBins"
---

<objective>
Make Detect read as the obvious entry point for burst scanning and slice generation.

Purpose: Phase 72 should feel like a real workflow, not a set of peer tabs. Detect must clearly start the analysis loop.
Output: Updated rail tab ordering/labels and a Detect panel whose primary copy and CTA make the burst-scan path obvious.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/FEATURE-ANALYSIS.md
@.planning/codebase/SLICES.md
@src/components/dashboard-demo/DashboardDemoRailTabs.tsx
@src/components/dashboard-demo/DemoDetectPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Put Detect first in the workflow rail</name>
  <files>src/components/dashboard-demo/DashboardDemoRailTabs.tsx</files>
  <read_first>
    - src/components/dashboard-demo/DashboardDemoRailTabs.tsx
    - src/components/dashboard-demo/DemoDetectPanel.tsx
    - .planning/ROADMAP.md
  </read_first>
  <action>
    Reorder the tab triggers so the workflow starts with Detect, not Scan. Keep the existing tab values so store state and content wiring stay intact, but change the visual ordering and labels to make the sequence obvious:
    - put `detect` first
    - keep `slices` second
    - keep `inspect` third
    - relabel the current `scan` trigger as `Overview` so it reads as supporting context, not the entry point
    - keep `configure` last

    Also update the surrounding rail copy so the header and tab strip imply a Detect → Slices → Inspect flow instead of a collection of equal surfaces.
  </action>
  <acceptance_criteria>
    - `DashboardDemoRailTabs.tsx` contains `value="detect"` before `value="scan"` in the trigger list
    - the label `Overview` appears for the former Scan surface
    - the tab strip still contains five triggers and five matching tab contents
  </acceptance_criteria>
  <verify>Run `pnpm lint src/components/dashboard-demo/DashboardDemoRailTabs.tsx` and confirm no lint errors.</verify>
  <done>The rail visually reads Detect first, with Overview demoted to supporting context.</done>
</task>

<task type="auto">
  <name>Task 2: Make the Detect panel narrate scan → generate</name>
  <files>src/components/dashboard-demo/DemoDetectPanel.tsx</files>
  <read_first>
    - src/components/dashboard-demo/DemoDetectPanel.tsx
    - src/components/dashboard-demo/lib/demo-burst-generation.ts
    - src/components/dashboard-demo/lib/useDemoBurstWindows.ts
    - src/lib/time-domain.ts
  </read_first>
  <action>
    Rewrite the panel copy so the user immediately understands this surface starts the workflow. Keep the underlying burst detection logic, but change the presentation:
    - title should name the Detect step directly
    - description should say this is where users scan the brushed range and generate candidate slices
    - the brushed-time-range prerequisite should be shown before the controls
    - the primary action text should distinguish scan from generate instead of burying both behind generic burst wording
    - success and failure toasts should continue to differentiate scan/bin fetch errors from generation errors

    Do not move the logic into another component; this task is about making the existing panel read like the first step of the workflow.
  </action>
  <acceptance_criteria>
    - the panel title or description explicitly contains the word `Detect`
    - the copy mentions scanning the brushed range before generation
    - the empty/prerequisite state tells users to select or brush a time range first
  </acceptance_criteria>
  <verify>Run `pnpm lint src/components/dashboard-demo/DemoDetectPanel.tsx` and confirm the file is lint-clean.</verify>
  <done>Users can open Detect and immediately understand how to start burst scanning and slice generation.</done>
</task>

</tasks>

<verification>
1. `pnpm lint src/components/dashboard-demo/DashboardDemoRailTabs.tsx src/components/dashboard-demo/DemoDetectPanel.tsx`
2. Open `/dashboard-demo` and confirm Detect is the first obvious action surface
3. Confirm the Detect panel copy explains the brushed-range prerequisite and the scan/generate sequence
</verification>

<success_criteria>
- Detect is the first clear workflow entry in the rail
- Detect panel copy makes the burst-scan path obvious without extra explanation
- The phase can be executed without changing unrelated dashboard behavior
</success_criteria>

<output>
After completion, create `.planning/phases/72-workflow-clarity/72-01-SUMMARY.md`
</output>
