---
phase: 42-full-auto-package-acceptance-alignment
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/full-auto-orchestrator.ts
  - src/lib/full-auto-orchestrator.test.ts
  - src/hooks/useSuggestionGenerator.ts
  - src/app/timeslicing/page.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Accepting a generated full-auto package applies both warp slices and interval slices in one action"
    - "Normal full-auto generation returns ranked sets that include both warp and interval artifacts"
    - "Ranking order, recommended badge behavior, and whyRecommended rationale remain stable for identical input"
    - "Manual rerun still regenerates ranked packages without changing trigger/status semantics"
    - "No-result and low-confidence safeguards remain active after contract alignment"
  artifacts:
    - path: "src/lib/full-auto-orchestrator.ts"
      provides: "Package-complete full-auto generation contract with unchanged ranking semantics"
      contains: "generateRankedAutoProposalSets returns AutoProposalSet entries with intervals"
    - path: "src/lib/full-auto-orchestrator.test.ts"
      provides: "Regression coverage for package completeness and ranking invariants"
      contains: "interval presence assertions + deterministic recommendation checks"
    - path: "src/app/timeslicing/page.tsx"
      provides: "Acceptance handler that applies package artifacts and treats missing intervals as legacy path"
      contains: "handleAcceptFullAutoPackage warp + interval apply flow"
    - path: "src/hooks/useSuggestionGenerator.ts"
      provides: "Generation bridge aligned to package-complete semantics"
      contains: "full-auto pipeline without warp-only assumptions"
  key_links:
    - from: "src/lib/full-auto-orchestrator.ts"
      to: "src/hooks/useSuggestionGenerator.ts"
      via: "generateRankedAutoProposalSets output consumed by setFullAutoSets"
      pattern: "generateRankedAutoProposalSets"
    - from: "src/hooks/useSuggestionGenerator.ts"
      to: "src/app/timeslicing/page.tsx"
      via: "store fullAutoProposalSets selected and accepted through accept-full-auto-package"
      pattern: "accept-full-auto-package"
    - from: "src/app/timeslicing/page.tsx"
      to: "src/store/useSliceStore.ts"
      via: "handleAcceptIntervalBoundary applies proposalSet.intervals.boundaries"
      pattern: "handleAcceptIntervalBoundary"
---

<objective>
Close the Phase 42 audit gap by aligning full-auto generation and acceptance contracts so normal generated packages are package-complete (warp + intervals) and acceptance applies exactly what users reviewed.

Purpose: Restore milestone-consistent package acceptance semantics without regressing Phase 41 ranking/recommendation behavior or Phase 40 manual rerun behavior.
Output: Updated full-auto orchestrator contract, regression tests, and acceptance/generation bridge wiring aligned to package-complete semantics.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.3-v1.3-MILESTONE-AUDIT.md
@.planning/phases/42-full-auto-package-acceptance-alignment/42-RESEARCH.md
@.planning/phases/40-fully-automated-timeslicing-orchestration/40-03-SUMMARY.md
@.planning/phases/41-full-auto-optimization-ranking/41-02-SUMMARY.md
@src/lib/full-auto-orchestrator.ts
@src/lib/full-auto-orchestrator.test.ts
@src/hooks/useSuggestionGenerator.ts
@src/app/timeslicing/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make full-auto generation package-complete at producer boundary</name>
  <files>src/lib/full-auto-orchestrator.ts</files>
  <action>
    Update `generateRankedAutoProposalSets` so normal generated sets include both `warp` and `intervals` artifacts.

    Implementation requirements:
    - Generate interval boundaries in orchestrator using existing `detectBoundaries` from `src/lib/interval-detection.ts` (do not hand-roll boundary logic in page consumer).
    - Use a deterministic strategy for this phase: one boundary result per generation run (based on current `crimes`, `context.timeRange`, and `params.snapToUnit`) attached to each ranked set.
    - Ensure every returned ranked set on normal path has `intervals.boundaries.length >= 2`.
    - Preserve current ranking/scoring behavior from Phase 41 exactly: same score dimensions, weight constants, sorting rules, `recommendedId` derivation, and `whyRecommended` generation.
    - Keep no-result and low-confidence metadata pathways unchanged.

    Avoid:
    - Acceptance-time interval synthesis in `page.tsx` as the primary fix.
    - Any score contribution changes tied to interval generation.
  </action>
  <verify>npx vitest run src/lib/full-auto-orchestrator.test.ts</verify>
  <done>Generated full-auto sets are package-complete (warp + intervals) while ranking outputs remain deterministic and Phase 41-compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Lock contract and regression invariants in orchestrator tests</name>
  <files>src/lib/full-auto-orchestrator.test.ts</files>
  <action>
    Update and extend orchestrator tests to enforce the new package contract and protect against ranking regressions.

    Required assertions:
    - Replace warp-only assertion with package-complete assertions: each generated top set has `intervals`, boundaries length >= 2, and valid ascending boundary range.
    - Keep deterministic ordering checks for same input and keep recommendation invariant (`recommendedId === sets[0].id`).
    - Assert recommended set still exposes `reasonMetadata.whyRecommended`.
    - Retain no-result and low-confidence tests so safeguard semantics remain unchanged.

    Add focused edge coverage for any legacy-safe path introduced in Task 3 (missing intervals tolerated by acceptance consumer but treated as non-standard).
  </action>
  <verify>npx vitest run src/lib/full-auto-orchestrator.test.ts</verify>
  <done>Test suite fails if generated sets ever regress to warp-only normal output or if ranking/recommendation invariants drift.</done>
</task>

<task type="auto">
  <name>Task 3: Align generation bridge and acceptance handler to package-complete semantics</name>
  <files>src/hooks/useSuggestionGenerator.ts, src/app/timeslicing/page.tsx</files>
  <action>
    Remove warp-only assumptions in the generator bridge and harden acceptance consumer behavior for legacy/debug payloads.

    In `useSuggestionGenerator.ts`:
    - Remove/update comments or logic that declare full-auto packages as warp-only.
    - Keep full-auto trigger policy unchanged (entry auto-run, debounced auto-refresh, manual rerun source path and status updates).

    In `page.tsx` (`handleAcceptFullAutoPackage`):
    - Keep atomic apply flow: clear previous slices, apply warp intervals, set adaptive mode, then apply interval boundaries from `proposalSet.intervals` when present.
    - Treat missing/invalid intervals as legacy/degraded input (do not crash; keep warp apply), and surface a lightweight warning path suitable for debugging.
    - Do not alter recommendation selection UI wiring or rerun event wiring.

    Keep scope focused to contract/wiring alignment. Do not refactor unrelated page helpers in this plan.
  </action>
  <verify>npm run lint && npm test -- src/lib/full-auto-orchestrator.test.ts</verify>
  <done>Acceptance behavior matches reviewed package artifacts for normal generation, while manual rerun and recommendation/badge behaviors remain intact.</done>
</task>

</tasks>

<verification>
- [ ] `npx vitest run src/lib/full-auto-orchestrator.test.ts` passes with package-complete contract assertions.
- [ ] `/timeslicing` auto-run still renders ranked top-3 full-auto cards and a single recommended badge.
- [ ] Recommended card still shows "Why recommended" rationale in panel/card UI.
- [ ] Accepting selected package applies visible warp slices and interval slices in one action.
- [ ] Clicking `Rerun Full-Auto` still regenerates packages and preserves status semantics (`auto`, `manual`, fresh/error transitions).
- [ ] No-result context still blocks package acceptance with safeguard messaging.
</verification>

<success_criteria>
- Full-auto producer/consumer contract is aligned: normal generated sets include intervals and acceptance applies them.
- Critical v1.3 audit flow "accept package applies warp + intervals" passes.
- Ranking, recommendation rationale/badge, and manual rerun behavior remain unchanged from Phase 40/41 expectations.
</success_criteria>

<output>
After completion, create `.planning/phases/42-full-auto-package-acceptance-alignment/42-01-SUMMARY.md`
</output>
