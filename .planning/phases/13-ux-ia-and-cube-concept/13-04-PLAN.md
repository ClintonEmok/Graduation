---
phase: 13-ux-ia-and-cube-concept
plan: 04
type: execute
wave: 2
depends_on:
  - 13-01
  - 13-02
  - 13-03
requirements: [UX-03]
files_modified:
  - src/components/dashboard-demo/lib/buildDashboardDemoSelectionStory.ts
  - src/components/dashboard-demo/DemoDualTimeline.tsx
  - src/components/dashboard-demo/DemoMapVisualization.tsx
  - src/components/viz/CubeVisualization.tsx
autonomous: true
must_haves:
  truths:
    - "User can brush or select in the timeline and see the map and cube update from the same active window."
    - "User can read one shared selection story across timeline, map, and cube."
    - "Stable dashboard/store paths stay untouched while the demo path carries the linked behavior."
  artifacts:
    - path: "src/components/dashboard-demo/lib/buildDashboardDemoSelectionStory.ts"
      provides: "shared selection story derivation"
    - path: "src/components/dashboard-demo/DemoMapVisualization.tsx"
      provides: "map consumer of the shared selection story"
    - path: "src/components/viz/CubeVisualization.tsx"
      provides: "cube consumer of the shared selection story"
  key_links:
    - from: "src/components/timeline/DemoDualTimeline.tsx"
      to: "src/components/dashboard-demo/lib/buildDashboardDemoSelectionStory.ts"
      via: "shared demo selection state"
      pattern: "buildDashboardDemoSelectionStory"
    - from: "src/components/dashboard-demo/lib/buildDashboardDemoSelectionStory.ts"
      to: "src/components/dashboard-demo/DemoMapVisualization.tsx"
      via: "map highlight payload"
      pattern: "active window"
---

<objective>
Make the timeline the source of truth for linked selection so map and cube always reflect the same active analysis story.

Purpose: users should brush or select once and see the same window highlighted everywhere.
Output: a shared demo selection story helper plus synchronized map and cube consumers.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ux-ia-and-cube-concept/13-CONTEXT.md
@.planning/phases/13-ux-ia-and-cube-concept/13-QUESTION-WORKFLOW.md
@.planning/phases/13-ux-ia-and-cube-concept/13-MASTER-DESIGN-BRIEF.md
@src/components/dashboard-demo/DemoDualTimeline.tsx
@src/components/dashboard-demo/DemoMapVisualization.tsx
@src/components/viz/CubeVisualization.tsx
@src/store/useDashboardDemoCoordinationStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive a single shared selection story</name>
  <files>src/components/dashboard-demo/lib/buildDashboardDemoSelectionStory.ts</files>
  <read_first>
    - src/components/dashboard-demo/DemoDualTimeline.tsx
    - src/components/dashboard-demo/DemoMapVisualization.tsx
    - src/components/viz/CubeVisualization.tsx
    - src/store/useDashboardDemoCoordinationStore.ts
    - .planning/phases/13-ux-ia-and-cube-concept/13-QUESTION-WORKFLOW.md
  </read_first>
  <action>Create a pure helper that turns the current demo selection, brush range, warp state, and workflow phase into one readable story object: active window label, compare state, linked highlight label, and the short explanation text shown by the views. Keep it framework-agnostic and read-only so the timeline, map, and cube can share the same derivation without duplicating copy.</action>
  <acceptance_criteria>
    - The helper exports a reusable story builder.
    - The helper outputs active window, compare state, and linked highlight labels.
    - The helper does not fetch data or mutate stores.
  </acceptance_criteria>
  <verify>rg -n "active window|compare|linked|story|selection" src/components/dashboard-demo/lib/buildDashboardDemoSelectionStory.ts</verify>
  <done>The demo has one shared selection story that the views can all read.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the map and cube to the shared story</name>
  <files>src/components/dashboard-demo/DemoMapVisualization.tsx, src/components/viz/CubeVisualization.tsx</files>
  <read_first>
    - src/components/dashboard-demo/DemoMapVisualization.tsx
    - src/components/viz/CubeVisualization.tsx
    - src/components/dashboard-demo/lib/buildDashboardDemoSelectionStory.ts
    - src/store/useDashboardDemoCoordinationStore.ts
  </read_first>
  <action>Use the shared selection story in the map and cube so both views show the same active window, linked highlight summary, and compare-state copy. Keep stable-route stores untouched and avoid introducing a second data source; the demo override path should be the only place that reads the story helper.</action>
  <acceptance_criteria>
    - DemoMapVisualization.tsx and CubeVisualization.tsx both reference the shared selection story helper.
    - Both views show the same active window or linked highlight text.
    - No stable route store is rewritten to support the demo-only story.
  </acceptance_criteria>
  <verify>rg -n "buildDashboardDemoSelectionStory|active window|linked highlight|compare" src/components/dashboard-demo/DemoMapVisualization.tsx src/components/viz/CubeVisualization.tsx</verify>
  <done>The map and cube now tell the same selection story as the timeline.</done>
</task>

<task type="auto">
  <name>Task 3: Keep the timeline authoritative for the shared story</name>
  <files>src/components/timeline/DemoDualTimeline.tsx</files>
  <read_first>
    - src/components/timeline/DemoDualTimeline.tsx
    - src/components/dashboard-demo/lib/buildDashboardDemoSelectionStory.ts
    - src/store/useDashboardDemoCoordinationStore.ts
  </read_first>
  <action>Thread the shared selection story through DemoDualTimeline so brush, selection, and compare labels remain the authoritative source for the other views. Preserve the existing selection and warp behavior, but make the visible copy obvious enough that map and cube consumers can follow the same window narrative.</action>
  <acceptance_criteria>
    - DemoDualTimeline.tsx still updates brush and selection state.
    - DemoDualTimeline.tsx references the shared selection story helper or its labels.
    - The timeline remains the source of truth for the demo selection story.
  </acceptance_criteria>
  <verify>rg -n "buildDashboardDemoSelectionStory|brush|selection|compare|active window" src/components/timeline/DemoDualTimeline.tsx</verify>
  <done>The timeline remains authoritative while the other views stay synchronized.</done>
</task>

</tasks>

<verification>
Run the selection-story source checks and confirm the timeline, map, and cube all resolve to the same active-window labels.
</verification>

<success_criteria>
- Brushing once updates all linked views.
- The same active window is readable in timeline, map, and cube.
- Demo-only selection wiring stays isolated from the stable route path.
</success_criteria>

<output>
After completion, create `.planning/phases/13-ux-ia-and-cube-concept/13-04-SUMMARY.md`
</output>
