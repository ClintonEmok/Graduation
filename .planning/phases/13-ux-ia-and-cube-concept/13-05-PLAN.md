---
phase: 13-ux-ia-and-cube-concept
plan: 05
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-02
  - 13-03
  - 13-04
requirements: [UX-01, UX-02, UX-03, CUBE-01, CUBE-02, CUBE-03]
files_modified:
  - src/app/dashboard-demo/page.shell.test.tsx
  - src/app/dashboard/page.shell.test.tsx
  - src/components/timeline/DemoTimelinePanel.phase13.test.ts
  - src/components/viz/CubeVisualization.phase13.test.ts
autonomous: true
must_haves:
  truths:
    - "The demo shell is locked by source-inspection coverage."
    - "The timeline compare controls are locked by source-inspection coverage."
    - "The cube relational language is locked by source-inspection coverage."
    - "The stable dashboard route still excludes the demo-only IA wording."
  artifacts:
    - path: "src/app/dashboard-demo/page.shell.test.tsx"
      provides: "demo shell regression coverage"
    - path: "src/components/timeline/DemoTimelinePanel.phase13.test.ts"
      provides: "timeline compare-copy regression coverage"
    - path: "src/components/viz/CubeVisualization.phase13.test.ts"
      provides: "cube relational-copy regression coverage"
  key_links:
    - from: "src/app/dashboard-demo/page.shell.test.tsx"
      to: "src/components/dashboard-demo/DashboardDemoShell.tsx"
      via: "source inspection"
      pattern: "DemoExplainPanel"
    - from: "src/components/viz/CubeVisualization.phase13.test.ts"
      to: "src/components/viz/CubeVisualization.tsx"
      via: "relational copy guard"
      pattern: "relational"
---

<objective>
Lock Phase 13’s IA, timeline, and cube contracts with source-inspection tests so later refactors cannot drift the guided workflow.

Purpose: keep the shell, compare controls, and relational cube language stable after the redesign lands.
Output: regression tests that prove the new story language and route isolation remain intact.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ux-ia-and-cube-concept/13-CONTEXT.md
@.planning/phases/13-ux-ia-and-cube-concept/13-MASTER-DESIGN-BRIEF.md
@.planning/phases/13-ux-ia-and-cube-concept/13-QUESTION-WORKFLOW.md
@src/app/dashboard-demo/page.shell.test.tsx
@src/app/dashboard/page.shell.test.tsx
@src/components/timeline/DemoTimelinePanel.tsx
@src/components/viz/CubeVisualization.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the route-shell regression coverage</name>
  <files>src/app/dashboard-demo/page.shell.test.tsx, src/app/dashboard/page.shell.test.tsx</files>
  <read_first>
    - src/app/dashboard-demo/page.shell.test.tsx
    - src/app/dashboard/page.shell.test.tsx
    - src/components/dashboard-demo/DashboardDemoShell.tsx
    - src/components/dashboard-demo/WorkflowSkeleton.tsx
    - src/components/dashboard-demo/DashboardDemoRailTabs.tsx
  </read_first>
  <action>Update the existing route-shell tests so they assert the new guided-workflow copy, the explain rail, the manual workflow stepper, and the continued isolation of the stable dashboard route. Keep the checks source-inspection based and make sure the stable route still excludes demo-only wording.</action>
  <acceptance_criteria>
    - The demo shell test checks for DemoExplainPanel and the new workflow stage names.
    - The stable dashboard test still excludes dashboard-demo wording.
    - Both tests remain source-inspection tests, not browser tests.
  </acceptance_criteria>
  <verify>pnpm vitest src/app/dashboard-demo/page.shell.test.tsx src/app/dashboard/page.shell.test.tsx</verify>
  <done>The shell regression coverage proves the new IA did not leak into the stable dashboard route.</done>
</task>

<task type="auto">
  <name>Task 2: Add a timeline compare-copy regression test</name>
  <files>src/components/timeline/DemoTimelinePanel.phase13.test.ts</files>
  <read_first>
    - src/components/timeline/DemoTimelinePanel.tsx
    - src/components/timeline/DemoDualTimeline.tsx
    - src/components/timeline/hooks/useDemoTimelineSummary.ts
  </read_first>
  <action>Create a source-inspection test that locks the compare-mode wording, the Linear/Adaptive labels, and the primary-driver framing in the timeline panel. Use exact string checks that prove the copy still reads like an analysis control surface rather than a generic slider cluster.</action>
  <acceptance_criteria>
    - The test file exists and reads DemoTimelinePanel.tsx source.
    - The assertions cover Linear, Adaptive, compare, and focused-track wording.
    - The test fails if the panel loses the Phase 13 compare framing.
  </acceptance_criteria>
  <verify>pnpm vitest src/components/timeline/DemoTimelinePanel.phase13.test.ts</verify>
  <done>The timeline compare copy is locked by an explicit regression test.</done>
</task>

<task type="auto">
  <name>Task 3: Add a cube relational-copy regression test</name>
  <files>src/components/viz/CubeVisualization.phase13.test.ts</files>
  <read_first>
    - src/components/viz/CubeVisualization.tsx
    - src/components/viz/MainScene.tsx
    - src/components/viz/TimeSlices.tsx
    - src/components/viz/SelectedWarpSliceOverlay.tsx
    - src/components/viz/SliceStats.tsx
  </read_first>
  <action>Create a source-inspection test that locks the cube’s relational-mode wording, comparison cues, and linked-selection language. The test should also ensure the cube shell no longer reads like a raw browser or debug console.</action>
  <acceptance_criteria>
    - The test file exists and reads CubeVisualization.tsx source.
    - The assertions cover relational, comparison, linked-selection, and slice-summary wording.
    - The test fails if the cube loses the synthesis-layer language.
  </acceptance_criteria>
  <verify>pnpm vitest src/components/viz/CubeVisualization.phase13.test.ts</verify>
  <done>The cube relational contract is locked by source inspection.</done>
</task>

</tasks>

<verification>
Run the shell, timeline, and cube source tests together so the Phase 13 IA contract is locked before execution.
</verification>

<success_criteria>
- The new IA is protected by regression coverage.
- The stable dashboard route remains isolated.
- Timeline and cube copy cannot silently drift back to generic visualization language.
</success_criteria>

<output>
After completion, create `.planning/phases/13-ux-ia-and-cube-concept/13-05-SUMMARY.md`
</output>
