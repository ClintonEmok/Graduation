---
phase: 13-ux-ia-and-cube-concept
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [UX-01]
files_modified:
  - src/components/dashboard-demo/DashboardDemoShell.tsx
  - src/components/dashboard-demo/WorkflowSkeleton.tsx
  - src/components/dashboard-demo/DashboardDemoRailTabs.tsx
  - src/components/dashboard-demo/DemoExplainPanel.tsx
autonomous: true
must_haves:
  truths:
    - "User can understand the demo as a guided workflow with a fixed left drawer and right analysis rail."
    - "User can follow Orient, Find, Compare, Inspect, Explain, and Apply without the workflow disappearing."
    - "User can open a dedicated explanation surface that tells them why something is highlighted."
  artifacts:
    - path: "src/components/dashboard-demo/DashboardDemoShell.tsx"
      provides: "timeline-first demo shell composition"
    - path: "src/components/dashboard-demo/WorkflowSkeleton.tsx"
      provides: "manual stepper for the workflow stages"
    - path: "src/components/dashboard-demo/DemoExplainPanel.tsx"
      provides: "dedicated rationale / explain surface"
  key_links:
    - from: "src/components/dashboard-demo/DashboardDemoShell.tsx"
      to: "src/components/dashboard-demo/WorkflowSkeleton.tsx"
      via: "left-anchored drawer"
      pattern: "WorkflowSkeleton"
    - from: "src/components/dashboard-demo/DashboardDemoRailTabs.tsx"
      to: "src/components/dashboard-demo/DemoExplainPanel.tsx"
      via: "right analysis rail tab"
      pattern: "DemoExplainPanel"
---

<objective>
Reframe the dashboard-demo shell as the guided workflow entry point so the user always sees the Orient → Apply path, the fixed workflow drawer, and a dedicated explanation surface.

Purpose: make the demo read like a question-driven analysis workflow instead of a loose set of panels.
Output: a story-led shell, a manual workflow stepper, and a right-rail explain panel wired into the existing demo shell.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ux-ia-and-cube-concept/13-CONTEXT.md
@.planning/phases/13-ux-ia-and-cube-concept/13-MASTER-DESIGN-BRIEF.md
@.planning/phases/13-ux-ia-and-cube-concept/13-IA-TREE.md
@.planning/phases/13-ux-ia-and-cube-concept/13-QUESTION-WORKFLOW.md
@src/components/dashboard-demo/DashboardDemoShell.tsx
@src/components/dashboard-demo/WorkflowSkeleton.tsx
@src/components/dashboard-demo/DashboardDemoRailTabs.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recompose the shell around the guided workflow</name>
  <files>src/components/dashboard-demo/DashboardDemoShell.tsx</files>
  <read_first>
    - src/components/dashboard-demo/DashboardDemoShell.tsx
    - .planning/phases/13-ux-ia-and-cube-concept/13-MASTER-DESIGN-BRIEF.md
    - .planning/phases/13-ux-ia-and-cube-concept/13-IA-TREE.md
  </read_first>
  <action>Keep the map/cube toggle, shared viewport, bottom timeline anchor, left drawer, and fixed right rail, but restyle the shell so the hierarchy reads as: top scope/mode controls → center timeline → right viewport toggle → bottom slices/explain. Update aria labels and any phase markers so the shell clearly presents the Phase 13 workflow rather than the older generic demo stack. Do not change data loading or store ownership; this is a layout/copy refactor only.</action>
  <acceptance_criteria>
    - DashboardDemoShell.tsx contains the map/cube toggle buttons and the right rail remains fixed.
    - DashboardDemoShell.tsx contains story-led text that references the guided workflow or analysis flow.
    - DashboardDemoShell.tsx still renders WorkflowSkeleton and DashboardDemoRailTabs.
  </acceptance_criteria>
  <verify>rg -n "WorkflowSkeleton|DashboardDemoRailTabs|Show map viewport|Show cube viewport|guided workflow|analysis flow" src/components/dashboard-demo/DashboardDemoShell.tsx</verify>
  <done>The shell reads as a Phase 13 workflow entry point, not a generic panel grid.</done>
</task>

<task type="auto">
  <name>Task 2: Add a dedicated Explain rail panel</name>
  <files>src/components/dashboard-demo/DashboardDemoRailTabs.tsx, src/components/dashboard-demo/DemoExplainPanel.tsx</files>
  <read_first>
    - src/components/dashboard-demo/DashboardDemoRailTabs.tsx
    - src/components/dashboard-demo/DemoStatsPanel.tsx
    - src/components/dashboard-demo/DemoStkdePanel.tsx
    - src/components/dashboard-demo/DemoSlicePanel.tsx
    - .planning/phases/13-ux-ia-and-cube-concept/13-QUESTION-WORKFLOW.md
  </read_first>
  <action>Create a dedicated DemoExplainPanel component that explains why a selection is highlighted, which burst or slice it belongs to, and what the next action should be. Wire it into the fixed right rail so the demo has an explicit rationale surface alongside Stats, STKDE, and Slices. Keep the panel lightweight and explanatory; do not move stats/STKDE logic into it.</action>
  <acceptance_criteria>
    - DemoExplainPanel.tsx exports a React component named DemoExplainPanel.
    - DashboardDemoRailTabs.tsx references DemoExplainPanel and an Explain label.
    - The explain panel copy includes rationale language such as why, confidence, or next action.
  </acceptance_criteria>
  <verify>rg -n "DemoExplainPanel|Explain|why|confidence|next action" src/components/dashboard-demo/DashboardDemoRailTabs.tsx src/components/dashboard-demo/DemoExplainPanel.tsx</verify>
  <done>The right rail includes a dedicated explanation surface instead of forcing users to infer rationale from Stats or STKDE.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the workflow drawer as a manual stepper</name>
  <files>src/components/dashboard-demo/WorkflowSkeleton.tsx</files>
  <read_first>
    - src/components/dashboard-demo/WorkflowSkeleton.tsx
    - .planning/phases/13-ux-ia-and-cube-concept/13-CONTEXT.md
    - .planning/phases/13-ux-ia-and-cube-concept/13-QUESTION-WORKFLOW.md
  </read_first>
  <action>Keep the drawer left-anchored and always visible, but rewrite the copy into a manual Orient → Find → Compare → Inspect → Explain → Apply stepper. Preserve open/collapse behavior and the existing stage navigation pattern, but remove auto-advance language and make each stage show a short recap before the user moves on.</action>
  <acceptance_criteria>
    - WorkflowSkeleton.tsx contains the stage names Orient, Find, Compare, Inspect, Explain, and Apply.
    - WorkflowSkeleton.tsx contains recap text for the active stage.
    - WorkflowSkeleton.tsx no longer presents the flow as automatic or hidden.
  </acceptance_criteria>
  <verify>rg -n "Orient|Find|Compare|Inspect|Explain|Apply|recap|auto-advance|manual" src/components/dashboard-demo/WorkflowSkeleton.tsx</verify>
  <done>The workflow drawer behaves like a visible manual stepper that matches the Phase 13 story.</done>
</task>

</tasks>

<verification>
Run the shell-focused source checks for the new explain surface and manual stepper, then confirm the demo shell still composes the workflow drawer and right rail.
</verification>

<success_criteria>
- The shell reads like a guided analysis workflow.
- The workflow drawer stays visible and manual.
- The right rail exposes a dedicated explain surface.
</success_criteria>

<output>
After completion, create `.planning/phases/13-ux-ia-and-cube-concept/13-01-SUMMARY.md`
</output>
