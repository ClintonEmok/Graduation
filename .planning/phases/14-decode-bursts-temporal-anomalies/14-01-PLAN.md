---
phase: 14-decode-bursts-temporal-anomalies
plan: 01
type: execute
wave: 1
depends_on: []
requirements:
  - T4
  - T8
files_modified:
  - src/store/useDashboardDemoCoordinationStore.ts
  - src/store/useDashboardDemoCoordinationStore.test.ts
autonomous: true
must_haves:
  truths:
    - "Only one burst can be active in dashboard-demo state at a time"
    - "Selecting a different burst replaces the previous burst selection"
    - "Clearing burst selection closes the pinned detail panel"
  artifacts:
    - path: src/store/useDashboardDemoCoordinationStore.ts
      provides: "Singleton burst-selection state and open/close control"
    - path: src/store/useDashboardDemoCoordinationStore.test.ts
      provides: "Regression coverage for replacement and clear semantics"
  key_links:
    - from: "toggleBurstWindow(...)"
      to: "selectedBurstWindows"
      via: "replace-or-clear semantics"
      pattern: "selectedBurstWindows: \\[window\\]"
    - from: "clearSelectedBurstWindows()"
      to: "detailsOpen"
      via: "close-on-clear behavior"
      pattern: "detailsOpen: false"
---

<objective>
Make burst selection behave like a single active inspection target so the demo timeline never accumulates multiple pinned bursts.

Purpose: The phase needs one clear active burst at a time before the timeline and explain rail can tell a coherent anomaly story.
Output: A demo coordination store that replaces burst selections instead of stacking them, plus regression coverage for the contract.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-decode-bursts-temporal-anomalies/14-CONTEXT.md
@.planning/phases/14-decode-bursts-temporal-anomalies/14-DISCUSSION-LOG.md
@src/store/useDashboardDemoCoordinationStore.ts
@src/store/useDashboardDemoCoordinationStore.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make burst selection singleton</name>
  <files>src/store/useDashboardDemoCoordinationStore.ts</files>
  <action>Change `toggleBurstWindow` so dashboard-demo burst selection is single-active only: if the same burst is toggled again, clear `selectedBurstWindows` and close `detailsOpen`; if a different burst is toggled, replace the array with that one window and open `detailsOpen`. Update `clearSelectedBurstWindows` to also close `detailsOpen`. Do not change the stable `useCoordinationStore` implementation.</action>
  <verify>Run `pnpm vitest src/store/useDashboardDemoCoordinationStore.test.ts` after the store logic is updated.</verify>
  <done>Dashboard-demo state never holds more than one active burst, and clearing burst state also clears the detail-panel flag.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the singleton burst contract</name>
  <files>src/store/useDashboardDemoCoordinationStore.test.ts</files>
  <action>Expand the store regression test to assert that the first burst is replaced by the second burst, the array length stays at 1, and clearing the selection resets both `selectedBurstWindows` and `detailsOpen`.</action>
  <verify>`pnpm vitest src/store/useDashboardDemoCoordinationStore.test.ts` exits 0 and the new assertions cover replace-on-toggle behavior.</verify>
  <done>The singleton burst-selection contract is protected by a focused store test.</done>
</task>

</tasks>

<verification>
`pnpm vitest src/store/useDashboardDemoCoordinationStore.test.ts`
</verification>

<success_criteria>
The demo store keeps exactly one active burst, toggling a new burst replaces the previous one, and clearing burst state closes the pinned details flag.
</success_criteria>

<output>
After completion, create `.planning/phases/14-decode-bursts-temporal-anomalies/14-01-SUMMARY.md`
</output>
