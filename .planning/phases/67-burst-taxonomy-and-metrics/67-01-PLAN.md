---
phase: 67-burst-taxonomy-and-metrics
plan: 01
type: execute
wave: 1
depends_on: [66-01, 66-02]
requirements_addressed: [BQ-01, BQ-04]
files_modified:
  - src/lib/binning/burst-taxonomy.ts
  - src/lib/binning/types.ts
  - src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.ts
  - src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.test.ts
autonomous: true

must_haves:
  truths:
    - "The same input window always resolves to the same burst class, confidence, and rationale"
    - "Prolonged peaks, isolated spikes, valleys, and neutral windows are all distinguishable in diagnostics output"
    - "Legacy burst-pattern behavior remains visible as a compatibility bridge instead of replacing the taxonomy"
    - "Flat, sparse, single-bin, and threshold-tie cases resolve deterministically"
  artifacts:
    - path: "src/lib/binning/burst-taxonomy.ts"
      provides: "Deterministic burst taxonomy classifier and metadata helpers"
      contains: "classifyBurstWindow, BurstTaxonomy, BURST_TAXONOMY_RULE_VERSION"
    - path: "src/lib/binning/types.ts"
      provides: "Shared burst taxonomy types on time bins"
      contains: "burstClass, burstConfidence, burstProvenance"
    - path: "src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.ts"
      provides: "Adaptive diagnostics rows annotated with burst taxonomy metadata"
      contains: "prolonged-peak, isolated-spike, valley, neutral, burst-pattern"
    - path: "src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.test.ts"
      provides: "Regression coverage for taxonomy classification and edge cases"
      contains: "threshold ties, sparse windows, single-bin high windows"
  key_links:
    - from: "src/lib/binning/burst-taxonomy.ts"
      to: "src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.ts"
      via: "shared deterministic classifier"
      pattern: "classifyBurstWindow|deriveBurstConfidence|BurstTaxonomy"
    - from: "src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.ts"
      to: "src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.test.ts"
      via: "taxonomy regression cases"
      pattern: "burst-pattern|prolonged-peak|isolated-spike|valley|neutral"
---

<objective>
Create the deterministic burst taxonomy core and wire it into adaptive diagnostics so review workflows can distinguish prolonged peaks, isolated spikes, valleys, and neutral windows without losing the existing burst-pattern compatibility signal.

Purpose: Phase 67 needs a stable classification layer before any UI surface can explain burst meaning in a trustworthy way.
Output: A reusable taxonomy helper, updated diagnostic rows, and regression tests that lock the classification contract.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/67-burst-taxonomy-and-metrics/67-CONTEXT.md
@src/lib/binning/types.ts
@src/lib/binning/engine.ts
@src/lib/confidence-scoring.ts
@src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.ts
@src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the deterministic burst taxonomy helper and shared types</name>
  <read_first>
    - src/lib/binning/types.ts
    - src/lib/binning/engine.ts
    - src/lib/confidence-scoring.ts
    - .planning/phases/67-burst-taxonomy-and-metrics/67-CONTEXT.md
  </read_first>
  <files>src/lib/binning/burst-taxonomy.ts, src/lib/binning/types.ts</files>
  <action>
Create a new `src/lib/binning/burst-taxonomy.ts` module that exports a deterministic classifier for burst windows. The helper must:

- classify each window into exactly one of `prolonged-peak`, `isolated-spike`, `valley`, or `neutral`;
- emit a numeric score, confidence, rule version, threshold source, neighborhood summary, tie-break reason, and a concise human-readable rationale;
- prefer global thresholds for base cutoffs and local neighborhood rules for ties or ambiguous edge cases;
- stay fully deterministic for the same input and avoid random or learned scoring.

Extend `src/lib/binning/types.ts` with optional burst taxonomy fields that allow bins or derived windows to carry `burstClass`, `burstRuleVersion`, `burstScore`, `burstConfidence`, `burstProvenance`, `tieBreakReason`, `thresholdSource`, and `neighborhoodSummary` without breaking existing bin consumers.

Do not change the existing burstiness strategy behavior; this helper is a semantic layer on top of the current bin/output shape.
  </action>
  <acceptance_criteria>
    - src/lib/binning/burst-taxonomy.ts exists and exports the classifier plus taxonomy types
    - src/lib/binning/types.ts contains the optional burst taxonomy fields
    - The helper implements the four target labels exactly: prolonged-peak, isolated-spike, valley, neutral
  </acceptance_criteria>
  <verify>test -f src/lib/binning/burst-taxonomy.ts && pnpm exec tsc --noEmit</verify>
  <done>The taxonomy contract exists as a reusable, deterministic core primitive.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate adaptive diagnostics with taxonomy metadata and lock regressions</name>
  <read_first>
    - src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.ts
    - src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.test.ts
    - src/lib/binning/burst-taxonomy.ts
    - src/lib/confidence-scoring.ts
  </read_first>
  <files>src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.ts, src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.test.ts</files>
  <action>
Update `buildAdaptiveBinDiagnostics()` so each diagnostic row carries the burst taxonomy metadata produced by the new helper. Preserve the current `burst-pattern` bridge by keeping that label as a compatibility diagnostic when concentration ratio and minimum-event rules are met; do not replace it with the new semantic labels.

Then extend `adaptive-bin-diagnostics.test.ts` with deterministic cases that prove:

- a prolonged peak stays a prolonged peak across repeated runs;
- a single-bin high window resolves to isolated-spike unless neighborhood evidence says otherwise;
- flat and sparse windows fall back to neutral or valley only when the local rules justify it;
- threshold ties always break the same way.

Use the existing confidence-scoring style for any supporting score normalization, but keep the taxonomy logic itself isolated in the new helper module.
  </action>
  <acceptance_criteria>
    - adaptive diagnostics rows include burst taxonomy metadata fields
    - burst-pattern still appears for the legacy concentration-rule cases
    - adaptive-bin-diagnostics.test.ts covers edge cases and deterministic repeatability
    - `pnpm vitest src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.test.ts --run` exits 0
  </acceptance_criteria>
  <verify>pnpm vitest src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.test.ts --run</verify>
  <done>Diagnostics now expose stable taxonomy labels while preserving the existing burst-pattern bridge.</done>
</task>

</tasks>

<verification>
1. Burst taxonomy helpers classify the same input the same way every time.
2. Adaptive diagnostics emit taxonomy metadata and still bridge legacy burst-pattern behavior.
3. Edge-case regression tests prove ties, sparse windows, and flat windows are deterministic.
</verification>

<success_criteria>
- Burst taxonomy is available as a reusable core primitive.
- Adaptive diagnostics expose semantic burst meaning without breaking legacy burst-window behavior.
- The classification contract is regression-tested before any UI wiring starts.
</success_criteria>

<output>
After completion, create `.planning/phases/67-burst-taxonomy-and-metrics/67-01-SUMMARY.md`
</output>
