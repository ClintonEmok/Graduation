---
phase: 05-workflow-isolation
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/app/timeslicing/page.tsx
  - src/app/timeslicing/components/SuggestionPanel.tsx
  - src/app/timeslicing/components/ComparisonView.tsx
  - src/components/timeline/DualTimeline.tsx
  - src/store/useTimeslicingModeStore.ts
  - src/store/useBinningStore.ts
  - src/app/timeslicing/page.full-auto-acceptance.test.tsx
  - src/app/timeslicing/page.binning-mode.test.ts
autonomous: true
must_haves:
  truths:
    - "Apply preview is timeline-only and editable in place."
    - "Review warnings remain visible while the user edits pending bins."
    - "Clicking Apply commits the pending bins and routes directly to /dashboard with no intermediate confirmation screen."
  artifacts:
    - path: "src/app/timeslicing/page.tsx"
      provides: "Apply action and route handoff"
    - path: "src/app/timeslicing/components/SuggestionPanel.tsx"
      provides: "Review/apply surface with visible warnings"
    - path: "src/components/timeline/DualTimeline.tsx"
      provides: "Timeline-only apply preview"
    - path: "src/store/useTimeslicingModeStore.ts"
      provides: "Pending-bin editing and apply commit state"
  key_links:
    - from: "src/app/timeslicing/components/SuggestionPanel.tsx"
      to: "src/store/useTimeslicingModeStore.ts"
      via: "pending-bin merge/split/delete actions"
      pattern: "mergePendingGeneratedBins|splitPendingGeneratedBin|deletePendingGeneratedBin"
    - from: "src/app/timeslicing/page.tsx"
      to: "src/store/useTimeslicingModeStore.ts"
      via: "applyGeneratedBins commits the pending bins"
      pattern: "applyGeneratedBins"
    - from: "src/app/timeslicing/page.tsx"
      to: "/dashboard"
      via: "router push after a successful apply"
      pattern: "push('/dashboard')"
---

<objective>
Make the apply step editable and self-contained, then hand off straight into the dashboard the moment the user clicks Apply.

Purpose: preserve the review/warning surface while removing the last confirmation step between workflow and dashboard.
Output: a timeline-only apply preview with in-place bin edits, direct commit state, and immediate dashboard routing.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-demo-stats-stkde/04-CONTEXT.md
@.planning/phases/03-demo-timeline-rewrite/03-CONTEXT.md
@.planning/phases/02-dashboard-demo-ui-ux/02-CONTEXT.md
@src/app/timeslicing/page.tsx
@src/app/timeslicing/components/SuggestionPanel.tsx
@src/app/timeslicing/components/ComparisonView.tsx
@src/components/timeline/DualTimeline.tsx
@src/store/useTimeslicingModeStore.ts
@src/store/useBinningStore.ts
@src/app/dashboard/page.tsx
@src/components/layout/DashboardLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the apply preview editable and warnings-visible</name>
  <files>src/app/timeslicing/components/SuggestionPanel.tsx, src/app/timeslicing/components/ComparisonView.tsx, src/components/timeline/DualTimeline.tsx, src/store/useBinningStore.ts</files>
  <read_first>
    - src/app/timeslicing/components/SuggestionPanel.tsx
    - src/app/timeslicing/components/ComparisonView.tsx
    - src/components/timeline/DualTimeline.tsx
    - src/store/useBinningStore.ts
    - src/store/useTimeslicingModeStore.ts
  </read_first>
  <action>
    Keep the apply-preview surface timeline-only, show review warnings prominently, and wire split/merge/delete interactions to the pending-bin stores so edits happen in place before handoff. Keep the review model tied to the current pending bins and do not introduce dashboard chrome into the preview surface.
  </action>
  <acceptance_criteria>
    - The apply-preview timeline renders pending bins instead of a dashboard summary.
    - Merge, split, and delete actions mutate the pending bins in place.
    - Review warnings remain visible while the user edits the preview.
  </acceptance_criteria>
  <verify>grep -n "warnings\|pending bins\|split\|merge\|delete" src/app/timeslicing/components/SuggestionPanel.tsx src/app/timeslicing/components/ComparisonView.tsx src/components/timeline/DualTimeline.tsx src/store/useBinningStore.ts</verify>
  <done>The apply-preview stays timeline-only and editable, with warnings kept in view.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the applied bins and route straight to the dashboard</name>
  <files>src/app/timeslicing/page.tsx, src/store/useTimeslicingModeStore.ts, src/app/timeslicing/page.full-auto-acceptance.test.tsx, src/app/timeslicing/page.binning-mode.test.ts</files>
  <read_first>
    - src/app/timeslicing/page.tsx
    - src/store/useTimeslicingModeStore.ts
    - src/app/dashboard/page.tsx
    - src/components/layout/DashboardLayout.tsx
    - src/app/timeslicing/page.full-auto-acceptance.test.tsx
    - src/app/timeslicing/page.binning-mode.test.ts
  </read_first>
  <action>
    Make Apply call `applyGeneratedBins`, set the workflow state to applied, clear the pending bins, and immediately `router.push('/dashboard')` on success. Do not add any intermediate summary or confirmation screen between apply-preview and the dashboard.
  </action>
  <acceptance_criteria>
    - `applyGeneratedBins` is invoked before navigation.
    - A successful Apply sends the user directly to `/dashboard`.
    - The acceptance tests assert that there is no intermediate confirmation screen.
  </acceptance_criteria>
  <verify>pnpm exec vitest run src/app/timeslicing/page.full-auto-acceptance.test.tsx src/app/timeslicing/page.binning-mode.test.ts --reporter=dot</verify>
  <done>The workflow now hands off to the dashboard immediately after Apply with no extra stopping point.</done>
</task>

</tasks>

<verification>
Run the existing timeslicing acceptance suites, then verify the handoff manually from apply-preview into /dashboard.
</verification>

<success_criteria>
Apply preview remains editable, warnings stay visible, and Apply lands on the dashboard immediately.
</success_criteria>

<output>
After completion, create `.planning/phases/05-workflow-isolation/05-03-SUMMARY.md`
</output>
