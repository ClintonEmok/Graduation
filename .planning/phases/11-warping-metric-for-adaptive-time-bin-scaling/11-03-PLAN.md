---
phase: 11-warping-metric-for-adaptive-time-bin-scaling
plan: 03
type: execute
wave: 3
depends_on:
  - 11-02
files_modified: []
autonomous: false
must_haves:
  truths:
    - "The showcase visually demonstrates same-granularity bin scoring without changing bin order."
    - "The widest bin expands relative to its peers, but low-scoring bins still keep visible width."
    - "Flat or invalid inputs show an explicit neutral fallback instead of collapsing the selection."
  artifacts:
    - path: "src/app/demo/non-uniform-time-slicing/showcase.tsx"
      provides: "Visual proof of comparable-bin scoring and width scaling"
    - path: "src/components/dashboard-demo/lib/demo-warp-map.ts"
      provides: "Shared authored preview path that should still behave normally"
    - path: "src/lib/binning/warp-scaling.ts"
      provides: "Single source of truth for comparable-bin warp scoring"
  key_links:
    - from: "src/app/demo/non-uniform-time-slicing/showcase.tsx"
      to: "src/lib/binning/warp-scaling.ts"
      via: "the route should reflect shared helper output directly"
      pattern: "scoreComparableWarpBins|buildComparableWarpMap"
    - from: "src/components/dashboard-demo/lib/demo-warp-map.ts"
      to: "src/lib/binning/warp-scaling.ts"
      via: "authored preview still uses the shared scoring contract"
      pattern: "scoreComparableWarpBins|buildComparableWarpMap"
---

<objective>
Visually verify the comparable-bin warp scoring behavior.

Purpose: confirm the helper preserves order, keeps a visible minimum width, and exposes a neutral fallback before the next workflow phase depends on it.
Output: a blocking human-verify checkpoint.
</objective>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Shared same-granularity warp scoring in the showcase route plus the authored preview path.</what-built>
  <how-to-verify>
    1. Open `/demo/non-uniform-time-slicing/showcase`.
    2. Switch between hourly and daily partitions and confirm the bin order stays the same while the strongest bin grows wider than its peers.
    3. Confirm the narrowest bins still retain visible width and never collapse to zero.
    4. Force a flat or neutral input scenario and confirm the UI shows an explicit neutral fallback instead of hiding the bins.
    5. Open `/dashboard-demo` and confirm the authored warp preview still loads normally with the shared helper wired in.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<verification>
Use the browser to confirm the showcase and dashboard-demo preview still behave as expected after shared warp scoring is wired in.
</verification>

<success_criteria>
The comparable-bin warp scoring contract is visually verified and safe for the next phase.
</success_criteria>

<output>
After completion, create `.planning/phases/11-warping-metric-for-adaptive-time-bin-scaling/11-03-SUMMARY.md`
</output>
