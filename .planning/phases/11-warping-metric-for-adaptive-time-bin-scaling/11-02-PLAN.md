---
phase: 11-warping-metric-for-adaptive-time-bin-scaling
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/components/dashboard-demo/lib/demo-warp-map.ts
  - src/app/demo/non-uniform-time-slicing/showcase.tsx
  - src/app/demo/non-uniform-time-slicing/showcase.test.tsx
  - src/app/dashboard-demo/page.shell.test.tsx
autonomous: true
must_haves:
  truths:
    - "The authored warp preview and the demo showcase use the same comparable-bin scoring helper."
    - "The showcase can display relative scores and warp widths without reordering the bins or collapsing low-score bins."
    - "The dashboard-demo shell still stays isolated while the shared warp helper is wired into its preview path."
  artifacts:
    - path: "src/components/dashboard-demo/lib/demo-warp-map.ts"
      provides: "Shared authored-warp preview adapter built on comparable-bin scoring"
    - path: "src/app/demo/non-uniform-time-slicing/showcase.tsx"
      provides: "Visual route that shows score, width, and neutral fallback behavior"
    - path: "src/app/demo/non-uniform-time-slicing/showcase.test.tsx"
      provides: "Source-inspection regression for the scoring helper and no-collapse contract"
    - path: "src/app/dashboard-demo/page.shell.test.tsx"
      provides: "Route-regression coverage for the dashboard-demo preview still using the shared helper"
  key_links:
    - from: "src/components/dashboard-demo/lib/demo-warp-map.ts"
      to: "src/lib/binning/warp-scaling.ts"
      via: "visible slices are adapted into comparable bins before warp-map construction"
      pattern: "scoreComparableWarpBins|buildComparableWarpMap"
    - from: "src/app/demo/non-uniform-time-slicing/showcase.tsx"
      to: "src/lib/binning/warp-scaling.ts"
      via: "partitioned bins are scored and rendered with the shared helper"
      pattern: "scoreComparableWarpBins|clampComparableWarpWeight"
    - from: "src/app/dashboard-demo/page.shell.test.tsx"
      to: "src/components/dashboard-demo/lib/demo-warp-map.ts"
      via: "source-inspection keeps the preview path tied to the shared helper"
      pattern: "buildDemoSliceAuthoredWarpMap|scoreComparableWarpBins"
---

<objective>
Wire the reusable same-granularity score engine into the authored warp preview and the demo showcase so both surfaces use the same width-scaling semantics.

Purpose: prove the helper is actually reusable and not just a standalone library.
Output: shared scoring in the demo preview path, visible score/width reporting in the showcase, and regression coverage that locks the contract.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Adapt the authored warp preview to the shared comparable-bin helper</name>
  <files>src/components/dashboard-demo/lib/demo-warp-map.ts</files>
  <action>
    Convert the visible slice list into the comparable-bin adapter shape expected by `scoreComparableWarpBins`, derive a monotonic warp map with `buildComparableWarpMap`, and keep the existing slice-authored fallback when the helper cannot produce a valid result. Preserve the current minimum-width floor and keep the output ordered exactly like the input slices so the preview never reorders the selection.
  </action>
  <verify>pnpm vitest run src/app/dashboard-demo/page.shell.test.tsx</verify>
  <done>The authored warp preview is generated from the shared helper and still respects order, minimum width, and fallback behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Show the shared warp score contract in the demo showcase</name>
  <files>src/app/demo/non-uniform-time-slicing/showcase.tsx, src/app/demo/non-uniform-time-slicing/showcase.test.tsx, src/app/dashboard-demo/page.shell.test.tsx</files>
  <action>
    Update the showcase route to feed each same-granularity partition through the shared warp-scoring helper, then render the resulting score, warp weight, and minimum-width guard in the bin rows and summary cards. Add source-inspection coverage that proves the route imports the shared helper, preserves bin order, and keeps the neutral fallback path visible instead of collapsing to nothing.
  </action>
  <verify>pnpm vitest run src/app/demo/non-uniform-time-slicing/showcase.test.tsx src/app/dashboard-demo/page.shell.test.tsx</verify>
  <done>The showcase demonstrates comparable-bin scoring with stable order, visible width scaling, and an explicit neutral fallback.</done>
</task>

</tasks>

<verification>
Run the showcase source-inspection test and the dashboard-demo shell source-inspection test after wiring the shared helper.
</verification>

<success_criteria>
The same-granularity warp engine is reusable, preserves bin order, avoids collapse, and drives both the showcase route and authored warp preview.
</success_criteria>

<output>
After completion, create `.planning/phases/11-warping-metric-for-adaptive-time-bin-scaling/11-02-SUMMARY.md`
</output>
