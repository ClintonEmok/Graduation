---
phase: 57-context-aware-timeslicing-core-temporal-spatial-data-driven-diagnostics
plan: 2
type: execute
wave: 2
depends_on: [57-01]
requirements: []
files_modified:
  - src/store/useSuggestionStore.ts
  - src/hooks/useSuggestionGenerator.ts
  - src/hooks/useContextExtractor.ts
  - src/hooks/useSmartProfiles.ts
  - src/hooks/useSuggestionGenerator.test.ts
autonomous: true

must_haves:
  truths:
    - "Suggestion runs persist the detected dynamic profile label and diagnostics metadata so accepted/history entries are auditable"
    - "When signal is weak or no-strong, metadata carries explicit warning/fallback semantics instead of pretending strong confidence"
    - "Pipeline keeps backward compatibility for existing consumers that still read `profileName`"
  artifacts:
    - path: "src/store/useSuggestionStore.ts"
      provides: "extended SuggestionContextMetadata schema for context diagnostics audit fields"
      contains: "contextDiagnostics"
    - path: "src/hooks/useSuggestionGenerator.ts"
      provides: "orchestration wiring from context extraction + static profile + dynamic diagnostics into metadata"
      contains: "profileComparison"
    - path: "src/hooks/useSuggestionGenerator.test.ts"
      provides: "regressions for metadata persistence and weak-signal semantics"
      contains: "No strong profile"
  key_links:
    - from: "src/hooks/useSuggestionGenerator.ts"
      to: "src/lib/context-diagnostics/index.ts"
      via: "compute diagnostics once per run and propagate into suggestion metadata"
      pattern: "buildContextDiagnostics|resolveDynamicProfile"
    - from: "src/hooks/useSuggestionGenerator.ts"
      to: "src/store/useSuggestionStore.ts"
      via: "persist dynamic profile + comparison metadata on suggestions and history"
      pattern: "contextMetadata"
---

<objective>
Wire the diagnostics engine into suggestion generation and metadata persistence.

Purpose: Diagnostics must be attached to real suggestion runs to support QA interpretation, history auditing, and static-vs-dynamic explainability.
Output: Suggestion pipeline/store metadata extensions with regression coverage.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-context-aware-timeslicing-core-temporal-spatial-data-driven-diagnostics/57-CONTEXT.md
@.planning/phases/57-context-aware-timeslicing-core-temporal-spatial-data-driven-diagnostics/57-RESEARCH.md
@.planning/phases/57-context-aware-timeslicing-core-temporal-spatial-data-driven-diagnostics/57-01-SUMMARY.md
@src/hooks/useSuggestionGenerator.ts
@src/store/useSuggestionStore.ts
@src/hooks/useSmartProfiles.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend suggestion metadata contract for diagnostics audit fields</name>
  <files>src/store/useSuggestionStore.ts</files>
  <action>Extend `SuggestionContextMetadata` with a nested diagnostics payload that preserves: dynamic profile label, weak/no-strong state flags, optional confidence value, static profile label, comparison outcome, and reason sentence. Keep existing `profileName` field for backward compatibility by mapping it to the dynamic profile display label. Ensure history entry persistence/reapply paths preserve new metadata shape.</action>
  <verify>`npm run typecheck` passes with no breakage across suggestion consumers.</verify>
  <done>Store contracts can persist dynamic diagnostics metadata without breaking current consumers.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate diagnostics computation into suggestion generation flow</name>
  <files>src/hooks/useSuggestionGenerator.ts, src/hooks/useContextExtractor.ts, src/hooks/useSmartProfiles.ts</files>
  <action>In `useSuggestionGenerator`, compute context diagnostics once per generation run from current context + sampled crimes, alongside existing static profile detection. Persist dynamic profile name to `contextMetadata.profileName` and attach expanded diagnostics fields under `contextMetadata.contextDiagnostics`. Keep existing generation ranking/proposal contracts unchanged (diagnostics-only phase). Ensure low-signal scenarios still emit best profile with warning or explicit `No strong profile` state per engine contract.</action>
  <verify>`npm run typecheck` passes and metadata assignment paths include dynamic profile + comparison data for both full-auto and manual paths.</verify>
  <done>Suggestion generation produces auditable diagnostics metadata while preserving existing behavior contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Add pipeline regressions for metadata persistence and fallback semantics</name>
  <files>src/hooks/useSuggestionGenerator.test.ts</files>
  <action>Add/extend tests that assert generated suggestions include: dynamic profile label, weak/no-strong semantics, static-vs-dynamic comparison reason when profiles differ, and backward-compatible `profileName`. Include cases for partial diagnostics availability so metadata explicitly marks missing temporal/spatial sections rather than omitting fields silently.</action>
  <verify>`npm test -- --run src/hooks/useSuggestionGenerator.test.ts` passes.</verify>
  <done>Pipeline metadata semantics are regression-protected for strong, weak, and partial contexts.</done>
</task>

</tasks>

<verification>
- `npm test -- --run src/hooks/useSuggestionGenerator.test.ts`
- `npm run typecheck`
</verification>

<success_criteria>
- Suggestion metadata contains dynamic diagnostics and static-vs-dynamic comparison data.
- Existing consumers remain compatible through retained `profileName` field.
- Weak/no-strong and partial diagnostics semantics are explicitly persisted and test-covered.
</success_criteria>

<output>
After completion, create `.planning/phases/57-context-aware-timeslicing-core-temporal-spatial-data-driven-diagnostics/57-02-SUMMARY.md`
</output>
