---
phase: 57-context-aware-timeslicing-core-temporal-spatial-data-driven-diagnostics
plan: 4
type: execute
wave: 1
depends_on: []
requirements: []
files_modified:
  - src/app/timeslicing-algos/lib/strategy-stats.ts
  - src/app/timeslicing-algos/lib/strategy-comparison.ts
  - src/app/timeslicing-algos/lib/strategy-comparison.test.ts
  - src/app/timeslicing-algos/lib/TimeslicingAlgosStrategyStats.tsx
  - src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx
  - src/app/timeslicing-algos/page.timeline-algos.test.ts
autonomous: false
gap_closure: true

must_haves:
  truths:
    - "`/timeslicing-algos` shows an always-visible comparison that makes the behavioral difference between `uniform-time` and `uniform-events` obvious before opening verbose diagnostics"
    - "Users can tell which strategy better preserves fixed-width time readability versus which one better surfaces concentrated bursts for the current context"
    - "Comparative burst-identification cues stay deterministic and update from the active route dataset without reintroducing dense per-bin tables by default"
  artifacts:
    - path: "src/app/timeslicing-algos/lib/strategy-comparison.ts"
      provides: "deterministic comparison model for interpretability and burst-identification deltas"
      contains: "buildStrategyComparison"
    - path: "src/app/timeslicing-algos/lib/TimeslicingAlgosStrategyStats.tsx"
      provides: "comparison-first UI with side-by-side strategy effect summaries"
      contains: "What changes when you switch"
    - path: "src/app/timeslicing-algos/page.timeline-algos.test.ts"
      provides: "route-level regression guard for strategy comparison visibility"
      contains: "strategy comparison"
  key_links:
    - from: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      to: "src/app/timeslicing-algos/lib/TimeslicingAlgosStrategyStats.tsx"
      via: "passes current-domain timestamps so comparison reflects the active algos context"
      pattern: "TimeslicingAlgosStrategyStats"
    - from: "src/app/timeslicing-algos/lib/TimeslicingAlgosStrategyStats.tsx"
      to: "src/app/timeslicing-algos/lib/strategy-comparison.ts"
      via: "renders deterministic interpretability and burst-delta summaries from computed stats"
      pattern: "buildStrategyComparison"
---

<objective>
Close the Phase 57 human-verification gap by making `/timeslicing-algos` explicitly compare strategy effects instead of only showing compact shared diagnostics.

Purpose: QA needs to see how `uniform-time` and `uniform-events` change readability and burst identification for the same dataset without opening dense per-bin diagnostics or inferring the differences from raw stats alone.
Output: Deterministic strategy-comparison helper, comparison-first `/timeslicing-algos` diagnostics UI, targeted regressions, and a human verification checkpoint.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-context-aware-timeslicing-core-temporal-spatial-data-driven-diagnostics/57-CONTEXT.md
@.planning/phases/57-context-aware-timeslicing-core-temporal-spatial-data-driven-diagnostics/57-context-aware-timeslicing-core-temporal-spatial-data-driven-diagnostics-VERIFICATION.md
@.planning/phases/57-context-aware-timeslicing-core-temporal-spatial-data-driven-diagnostics/57-03-SUMMARY.md
@.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-07-SUMMARY.md
@.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-09-SUMMARY.md
@src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx
@src/app/timeslicing-algos/lib/TimeslicingAlgosStrategyStats.tsx
@src/app/timeslicing-algos/lib/strategy-stats.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build a deterministic strategy-comparison model for readability and burst effects</name>
  <files>src/app/timeslicing-algos/lib/strategy-stats.ts, src/app/timeslicing-algos/lib/strategy-comparison.ts, src/app/timeslicing-algos/lib/strategy-comparison.test.ts</files>
  <action>Extend the existing strategy-stats surface with the minimum additional metrics needed to explain behavioral deltas, then add a pure `buildStrategyComparison` helper that turns the two strategy stat entries into human-readable QA signals. The helper must explicitly call out (1) fixed-width readability vs variable-width remapping, (2) burst emphasis/compression differences using deterministic bin-concentration cues, and (3) the most important numeric deltas a user can scan quickly (for example empty-bin change, peak-bin change, width spread/compression). Keep the helper comparison-first and compact; do not revive the old per-bin table as the default surface and do not add profile-search or section-finder tooling.</action>
  <verify>`npm test -- --run src/app/timeslicing-algos/lib/strategy-comparison.test.ts` passes with deterministic cases for burst-heavy and relatively even distributions.</verify>
  <done>The route has a test-pinned comparison DTO that explains what changes when switching between `uniform-time` and `uniform-events` for the current timestamps.</done>
</task>

<task type="auto">
  <name>Task 2: Surface comparison-first strategy effects in `/timeslicing-algos`</name>
  <files>src/app/timeslicing-algos/lib/TimeslicingAlgosStrategyStats.tsx, src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx, src/app/timeslicing-algos/page.timeline-algos.test.ts</files>
  <action>Rework the existing strategy stats section so it answers the gap directly: make the comparison visible without any extra click, keep both strategies side-by-side, and add a short "What changes when you switch" summary that explicitly contrasts interpretability and burst-identification consequences. Preserve the current compact information hierarchy by leaving provenance/detail chips behind the existing toggle, but ensure the default route view now highlights why a QA user would choose one strategy over the other in the active context. Keep the route focused on strategy comparison only; do not reintroduce `AdaptiveBinDiagnosticsPanel` or other dense diagnostics as the default block.</action>
  <verify>`npm test -- --run src/app/timeslicing-algos/page.timeline-algos.test.ts` passes and source assertions cover the comparison summary plus continued absence of the old per-bin diagnostics panel in the default route shell.</verify>
  <done>`/timeslicing-algos` exposes comparative strategy effects clearly enough that a user can see interpretability and burst-identification tradeoffs at a glance.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Explicit strategy-comparison diagnostics on `/timeslicing-algos` for `uniform-time` vs `uniform-events`</what-built>
  <how-to-verify>
    1. Open `/timeslicing-algos` with a context that includes both sparse and bursty periods.
    2. Confirm the default view now shows a comparison-first summary, not only generic compact diagnostics chips.
    3. Confirm you can immediately tell which strategy preserves fixed time comparability and which one better exposes concentrated bursts for the current context.
    4. Switch between `uniform-time` and `uniform-events` and confirm the active-state styling updates while the side-by-side explanation remains visible.
    5. Confirm verbose provenance still stays behind the existing diagnostics toggle and no dense per-bin table is shown by default.
  </how-to-verify>
  <resume-signal>Type "approved" if the comparative effect is now obvious, or describe exactly which difference is still hard to interpret.</resume-signal>
</task>

</tasks>

<verification>
- `npm test -- --run src/app/timeslicing-algos/lib/strategy-comparison.test.ts src/app/timeslicing-algos/page.timeline-algos.test.ts`
- `npm run typecheck`
- Human verification confirms comparative strategy effects are obvious in `/timeslicing-algos`
</verification>

<success_criteria>
- `/timeslicing-algos` answers "what changes if I switch strategies?" in the default view.
- The route explicitly contrasts readability and burst-identification effects for `uniform-time` and `uniform-events` using deterministic, test-covered signals.
- Compact shared diagnostics remain compact, while comparative strategy insight becomes the primary algos-route diagnostic surface.
</success_criteria>

<output>
After completion, create `.planning/phases/57-context-aware-timeslicing-core-temporal-spatial-data-driven-diagnostics/57-04-SUMMARY.md`
</output>
