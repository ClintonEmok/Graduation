---
phase: 56-variable-sampling-selection-fidelity
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/crimes/range/route.ts
  - src/app/api/crimes/range/route.test.ts
  - src/app/api/crimes/range/sampling-policy.ts
  - src/app/api/crimes/range/sampling-policy.test.ts
  - src/types/crime.ts
autonomous: true

must_haves:
  truths:
    - "`/api/crimes/range` accepts variable sampling intent (`context` vs `selection-detail`) and optional `targetPoints` without breaking legacy callers"
    - "Sampling provenance is explicit in response metadata (requested intent, requested target points, effective target limit, stride policy result)"
    - "Existing callers that only send start/end/buffer/limit keep current behavior and response fields (`sampled`, `sampleStride`, `returned`, `totalMatches`)"
  artifacts:
    - path: "src/app/api/crimes/range/sampling-policy.ts"
      provides: "Pure policy resolver for intent + targetPoints + fallback/default behavior"
      contains: "resolveSamplingPolicy"
    - path: "src/app/api/crimes/range/route.ts"
      provides: "Request parsing + policy application + provenance metadata"
      contains: "sampling"
    - path: "src/app/api/crimes/range/route.test.ts"
      provides: "Route contract regressions for backward compatibility and variable sampling semantics"
      contains: "sampling intent"
  key_links:
    - from: "src/app/api/crimes/range/route.ts"
      to: "src/app/api/crimes/range/sampling-policy.ts"
      via: "route resolves effective target/stride from policy helper"
      pattern: "resolveSamplingPolicy"
    - from: "src/app/api/crimes/range/route.ts"
      to: "src/types/crime.ts"
      via: "response meta contract includes explicit sampling provenance"
      pattern: "sampling"
---

<objective>
Define and implement the backend variable-sampling contract for `/api/crimes/range` so callers can request context-grade vs selection-detail fidelity explicitly.

Purpose: Current static limit-driven stride lowers fidelity for dense selection windows. This plan introduces intent-aware policy semantics while preserving old caller behavior.
Output: A policy-backed API contract with explicit provenance metadata and backward-compatibility regression coverage.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-09-SUMMARY.md
@src/app/api/crimes/range/route.ts
@src/app/api/crimes/range/route.test.ts
@src/types/crime.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pure variable-sampling policy resolver for range requests</name>
  <files>src/app/api/crimes/range/sampling-policy.ts, src/app/api/crimes/range/sampling-policy.test.ts</files>
  <action>Create a dedicated pure helper that resolves sampling behavior from request inputs: `limit` (legacy), `samplingIntent` (`context` | `selection-detail`), and optional `targetPoints`. Encode defaults and clamps in one place (for example: context default target equals prior 50k baseline; selection-detail default target is higher but capped). Return a deterministic policy result object with effective target points, max cap reason (if clamped), and computed stride inputs. Do not couple this helper to Next.js request APIs so it can be unit tested directly.</action>
  <verify>`npm test -- --run src/app/api/crimes/range/sampling-policy.test.ts` passes with cases for legacy-only, context intent, selection-detail intent, and invalid/clamped target points.</verify>
  <done>A deterministic policy module exists that can derive effective sampling configuration without route side effects.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate new request semantics into `/api/crimes/range` with explicit provenance metadata</name>
  <files>src/app/api/crimes/range/route.ts, src/types/crime.ts</files>
  <action>Extend query parsing in `GET` to accept `samplingIntent` and `targetPoints` while keeping all existing parameters unchanged. Use Task 1 policy output to compute effective limit/stride behavior and preserve existing metadata fields (`sampled`, `sampleStride`, `returned`, `totalMatches`, buffer info). Add explicit provenance object in `meta` (for example `sampling: { intent, requestedTargetPoints, effectiveTargetPoints, policyVersion, clamped }`). Keep default behavior exactly backward-compatible when new params are absent.</action>
  <verify>`npm run typecheck` passes and API responses include legacy fields plus explicit `meta.sampling` when present (or with deterministic defaults).</verify>
  <done>Route supports variable sampling semantics and still serves legacy callers without contract breakage.</done>
</task>

<task type="auto">
  <name>Task 3: Lock route-level compatibility and provenance regressions</name>
  <files>src/app/api/crimes/range/route.test.ts</files>
  <action>Add/extend route tests that prove: (1) legacy requests without `samplingIntent`/`targetPoints` retain old default behavior; (2) `samplingIntent=context` uses context policy defaults; (3) `samplingIntent=selection-detail` uses higher-fidelity policy defaults or target override; (4) malformed inputs return safe 400 or clamp behavior per policy; (5) provenance fields remain explicit and stable. Ensure tests assert both backward-compatible fields and new provenance metadata together.</action>
  <verify>`npm test -- --run src/app/api/crimes/range/route.test.ts` passes.</verify>
  <done>Variable sampling contract is protected by regression tests and backward compatibility is evidence-backed.</done>
</task>

</tasks>

<verification>
- `npm test -- --run src/app/api/crimes/range/sampling-policy.test.ts src/app/api/crimes/range/route.test.ts`
- `npm run typecheck`
- Manual API checks:
  1. `curl "/api/crimes/range?startEpoch=1000&endEpoch=2000"` returns 200 with legacy metadata unchanged.
  2. `curl "/api/crimes/range?startEpoch=1000&endEpoch=2000&samplingIntent=context"` returns 200 with explicit sampling provenance.
  3. `curl "/api/crimes/range?startEpoch=1000&endEpoch=2000&samplingIntent=selection-detail&targetPoints=200000"` returns 200 with higher-fidelity policy metadata and bounded target.
</verification>

<success_criteria>
- API contract supports variable sampling intent and optional target points.
- Existing clients remain compatible without request changes.
- Sampling provenance is explicit and test-covered.
- Dashboard files are untouched (scope remains backend API contract only).
</success_criteria>

<risks>
- Unclear precedence between `limit` and `targetPoints` could cause caller confusion if not documented in metadata.
- Overly permissive target points may increase route latency for dense windows.
- Contract churn in `meta` may break fragile consumers if fields are renamed.
</risks>

<rollback>
- Keep policy helper but disable new query params by ignoring them in route parsing.
- Revert to legacy `limit`-only stride while preserving non-breaking metadata schema.
- If regressions occur, roll back `meta.sampling` additions and keep tests for future reintroduction.
</rollback>

<output>
After completion, create `.planning/phases/56-variable-sampling-selection-fidelity/56-01-SUMMARY.md`
</output>
