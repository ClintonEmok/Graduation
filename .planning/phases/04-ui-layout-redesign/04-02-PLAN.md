---
phase: 04-ui-layout-redesign
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/components/map/MapVisualization.tsx
  - src/app/layout-test/page.tsx
autonomous: true
must_haves:
  truths:
    - "Map component renders within the layout"
    - "Map fills the container completely"
  artifacts:
    - path: "src/components/map/MapVisualization.tsx"
      provides: "Map panel content"
  key_links:
    - from: "src/components/map/MapVisualization.tsx"
      to: "src/components/map/MapBase.tsx"
      via: "import"
---

<objective>
Implement the Map Visualization component for the left panel.
Purpose: Provide the geospatial context view.
Output: A reusable MapVisualization component wired into the test layout.
</objective>

<context>
@src/components/map/MapBase.tsx
</context>

<tasks>

<task type="auto">
  <name>Create MapVisualization component</name>
  <files>src/components/map/MapVisualization.tsx</files>
  <action>
    Create `src/components/map/MapVisualization.tsx`.
    
    Implementation:
    - Import `MapBase` from `./MapBase`.
    - Render `MapBase` wrapped in a `div` with `className="w-full h-full relative"`.
    - This ensures it follows the "Pattern 1: Absolute Positioning" from research.
  </action>
  <verify>
    File exists and imports MapBase.
  </verify>
  <done>
    Component created.
  </done>
</task>

<task type="auto">
  <name>Update verification route</name>
  <files>src/app/layout-test/page.tsx</files>
  <action>
    Update `src/app/layout-test/page.tsx`.
    - Import `MapVisualization`.
    - Replace the red placeholder div in `mapSlot` with `<MapVisualization />`.
  </action>
  <verify>
    grep "MapVisualization" src/app/layout-test/page.tsx
  </verify>
  <done>
    Test route now renders the real map.
  </done>
</task>

</tasks>

<verification>
Check for type errors.
</verification>

<success_criteria>
- [ ] MapVisualization component created
- [ ] Test page renders MapVisualization
</success_criteria>

<output>
After completion, create .planning/phases/04-ui-layout-redesign/04-02-SUMMARY.md
</output>
