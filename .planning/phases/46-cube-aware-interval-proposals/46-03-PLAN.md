---
phase: 46-cube-aware-interval-proposals
plan: 03
type: execute
wave: 3
depends_on: [46-02]
files_modified:
  - src/app/cube-sandbox/lib/applyIntervalProposal.ts
  - src/store/useIntervalProposalStore.ts
  - src/store/useIntervalProposalStore.test.ts
  - src/app/cube-sandbox/components/IntervalProposalPanel.tsx
  - src/components/viz/CubeVisualization.tsx
autonomous: true
must_haves:
  truths:
    - User can edit interval proposal boundaries and keep proposal linkage to original spatial context.
    - Confidence and quality feedback recomputes immediately after interval boundary edits.
    - User can preview, apply, and undo interval proposal effects while keeping traceability in cube diagnostics.
  artifacts:
    - path: src/store/useIntervalProposalStore.ts
      provides: Edit-aware proposal state with live quality recomputation and invalid downgrade handling.
    - path: src/app/cube-sandbox/components/IntervalProposalPanel.tsx
      provides: Boundary edit controls plus preview/apply/undo interactions.
    - path: src/components/viz/CubeVisualization.tsx
      provides: In-cube diagnostics cues for selected/applied interval and confidence state.
  key_links:
    - from: src/app/cube-sandbox/components/IntervalProposalPanel.tsx
      to: src/store/useIntervalProposalStore.ts
      via: boundary edit actions and preview/apply/undo controls
      pattern: updateProposalRange|previewSelected|applySelected|undoLastApply
    - from: src/store/useIntervalProposalStore.ts
      to: src/app/cube-sandbox/lib/applyIntervalProposal.ts
      via: apply action maps interval payload into slice/runtime state
      pattern: applyIntervalProposal
---

<objective>
Complete editable interval proposal workflows with live constraint-aware feedback.

Purpose: Satisfy CINTV-03 by letting users modify proposal boundaries and immediately see confidence/quality impact, then preview/apply/undo changes with clear cube diagnostics.
Output: Edit-aware interval proposal store logic, apply adapter, panel controls, diagnostics updates, and tests.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-cube-aware-interval-proposals/46-01-PLAN.md
@.planning/phases/46-cube-aware-interval-proposals/46-02-PLAN.md
@src/store/useSliceStore.ts
@src/components/viz/CubeVisualization.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement editable interval proposal state with live quality recompute</name>
  <files>src/store/useIntervalProposalStore.ts, src/store/useIntervalProposalStore.test.ts</files>
  <action>Extend interval proposal store with boundary edit actions that preserve original proposal id and source linkage while storing edited range separately. Recompute confidence and quality metrics immediately after every edit using deterministic scoring rules from plan 46-01. If edits cross invalid thresholds, keep proposal editable but mark downgraded quality/confidence instead of removing it. Add tests for edit recomputation, linkage preservation, invalid downgrade handling, and deterministic recompute output.</action>
  <verify>npm run test -- src/store/useIntervalProposalStore.test.ts</verify>
  <done>Edited proposals remain traceable and produce immediate, deterministic feedback updates even when quality degrades.</done>
</task>

<task type="auto">
  <name>Task 2: Add preview-apply-undo interval actions and cube diagnostics cues</name>
  <files>src/app/cube-sandbox/lib/applyIntervalProposal.ts, src/app/cube-sandbox/components/IntervalProposalPanel.tsx, src/components/viz/CubeVisualization.tsx</files>
  <action>Create an apply adapter that maps selected interval proposals into the runtime slice workflow (create/update slice selection and preserve provenance metadata). Wire panel controls for preview, apply, and undo latest apply. Update cube diagnostics text to show selected/applied interval id or label, edited-vs-original cue, and confidence/quality status so users can verify impact inline.</action>
  <verify>npm run lint -- src/app/cube-sandbox/lib/applyIntervalProposal.ts src/app/cube-sandbox/components/IntervalProposalPanel.tsx src/components/viz/CubeVisualization.tsx</verify>
  <done>User can edit, preview, apply, and undo interval proposals with immediate diagnostics feedback in cube view.</done>
</task>

</tasks>

<verification>
Run: `npm run test -- src/store/useIntervalProposalStore.test.ts`
Run: `npm run lint -- src/app/cube-sandbox/lib/applyIntervalProposal.ts src/store/useIntervalProposalStore.ts src/app/cube-sandbox/components/IntervalProposalPanel.tsx src/components/viz/CubeVisualization.tsx`
Run: `npm run dev` and in `/cube-sandbox` edit a proposal range, confirm confidence/quality updates immediately, apply it, then undo and verify state/diagnostics rollback.
</verification>

<success_criteria>
Users can adjust interval proposal boundaries and immediately receive cube-aware confidence feedback, then complete preview/apply/undo without losing proposal traceability, satisfying CINTV-03.
</success_criteria>

<output>
After completion, create `.planning/phases/46-cube-aware-interval-proposals/46-03-SUMMARY.md`
</output>
