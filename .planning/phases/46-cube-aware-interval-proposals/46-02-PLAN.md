---
phase: 46-cube-aware-interval-proposals
plan: 02
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - src/app/cube-sandbox/components/IntervalProposalPanel.tsx
  - src/app/cube-sandbox/components/SandboxContextPanel.tsx
  - src/app/cube-sandbox/page.tsx
  - src/app/cube-sandbox/lib/resetSandboxState.ts
  - src/app/cube-sandbox/lib/resetSandboxState.test.ts
autonomous: true
must_haves:
  truths:
    - User can generate and inspect interval proposals directly in the sandbox right rail.
    - Each interval proposal card shows confidence and quality signals with linked spatial context.
    - Interval proposal state resets cleanly on hard reset and sandbox route exit.
  artifacts:
    - path: src/app/cube-sandbox/components/IntervalProposalPanel.tsx
      provides: Rail-first UI for generating, listing, selecting, and inspecting interval proposals.
    - path: src/app/cube-sandbox/components/SandboxContextPanel.tsx
      provides: Integration point for interval review within always-on diagnostics rail.
    - path: src/app/cube-sandbox/lib/resetSandboxState.ts
      provides: Reset orchestration that clears interval proposal state.
  key_links:
    - from: src/app/cube-sandbox/components/IntervalProposalPanel.tsx
      to: src/store/useIntervalProposalStore.ts
      via: generate/select/clear actions and selected proposal details
      pattern: useIntervalProposalStore
    - from: src/app/cube-sandbox/components/SandboxContextPanel.tsx
      to: src/app/cube-sandbox/components/IntervalProposalPanel.tsx
      via: panel mounted inline in diagnostics rail
      pattern: IntervalProposalPanel
---

<objective>
Expose cube-aware interval proposals in the sandbox review surface.

Purpose: Satisfy user-facing CINTV-01/CINTV-02 behavior by making generated proposals readable, reviewable, and traceable to active spatial context directly in the right rail.
Output: Interval proposal panel UI, sandbox panel integration, and lifecycle/reset safety wiring.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/45-cube-constrained-warp-proposals/45-02-SUMMARY.md
@.planning/phases/46-cube-aware-interval-proposals/46-01-PLAN.md
@src/app/cube-sandbox/components/WarpProposalPanel.tsx
@src/app/cube-sandbox/components/SandboxContextPanel.tsx
@src/app/cube-sandbox/lib/resetSandboxState.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build interval proposal panel with confidence-first rationale cards</name>
  <files>src/app/cube-sandbox/components/IntervalProposalPanel.tsx</files>
  <action>Create an interval proposal panel component following the existing rail style: generate control, clear control, ranked list, and selected details. Render plain-language rationale first, then metrics. Show linked spatial context labels, confidence band + numeric score, density concentration, hotspot coverage, and proposal range. Keep default list concise (top candidates) with expandable details rather than dense always-open cards.</action>
  <verify>npm run lint -- src/app/cube-sandbox/components/IntervalProposalPanel.tsx</verify>
  <done>User can generate and inspect confidence-scored interval proposals in one rail section without leaving `/cube-sandbox`.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate interval proposal panel into sandbox context rail</name>
  <files>src/app/cube-sandbox/components/SandboxContextPanel.tsx</files>
  <action>Mount `IntervalProposalPanel` in the existing diagnostics rail composition and add concise summary cues (proposal count, selected interval, generation timestamp) consistent with current sandbox panel language. Preserve existing controls and avoid modal or route transition dependencies.</action>
  <verify>npm run lint -- src/app/cube-sandbox/components/SandboxContextPanel.tsx</verify>
  <done>Interval proposal workflow is always visible and co-located with cube sandbox diagnostics.</done>
</task>

<task type="auto">
  <name>Task 3: Extend lifecycle cleanup and hard reset coverage for interval proposals</name>
  <files>src/app/cube-sandbox/page.tsx, src/app/cube-sandbox/lib/resetSandboxState.ts, src/app/cube-sandbox/lib/resetSandboxState.test.ts</files>
  <action>Clear interval proposal store state both on `/cube-sandbox` unmount and in `resetSandboxState` alongside existing proposal/slice resets. Extend reset regression test to seed interval proposals and assert all interval selection/generation state is cleared after reset.</action>
  <verify>npm run test -- src/app/cube-sandbox/lib/resetSandboxState.test.ts</verify>
  <done>No stale interval proposal state survives route exit or hard reset.</done>
</task>

</tasks>

<verification>
Run: `npm run lint -- src/app/cube-sandbox/components/IntervalProposalPanel.tsx src/app/cube-sandbox/components/SandboxContextPanel.tsx src/app/cube-sandbox/page.tsx src/app/cube-sandbox/lib/resetSandboxState.ts src/app/cube-sandbox/lib/resetSandboxState.test.ts`
Run: `npm run test -- src/app/cube-sandbox/lib/resetSandboxState.test.ts`
Run: `npm run dev` then in `/cube-sandbox` generate interval proposals and confirm confidence/quality cards render with linked constraint labels.
</verification>

<success_criteria>
Users can generate and inspect cube-aware interval proposals with confidence/quality indicators in the sandbox rail, and lifecycle/reset behavior remains deterministic.
</success_criteria>

<output>
After completion, create `.planning/phases/46-cube-aware-interval-proposals/46-02-SUMMARY.md`
</output>
