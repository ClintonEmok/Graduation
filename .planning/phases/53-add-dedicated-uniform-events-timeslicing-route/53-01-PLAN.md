---
phase: 53-add-dedicated-uniform-events-timeslicing-route
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/timeslicing/lib/TimeslicingRouteShell.tsx
  - src/app/timeslicing/page.tsx
  - src/app/timeslicing/page.binning-mode.test.ts
  - src/app/timeslicing-uniform-events/page.tsx
  - src/app/timeslicing-uniform-events/page.binning-mode.test.ts
autonomous: true

must_haves:
  truths:
    - "Users can open a uniform-time route and a uniform-events route as separate URLs"
    - "Each route computes adaptive maps with its intended binning mode"
    - "Users can switch between methods by route without changing shared global mode state"
  artifacts:
    - path: "src/app/timeslicing/lib/TimeslicingRouteShell.tsx"
      provides: "Shared timeslicing orchestration shell with route-injected binning mode"
      contains: "computeMaps"
    - path: "src/app/timeslicing/page.tsx"
      provides: "Uniform-time route wrapper"
      contains: "binningMode: 'uniform-time'"
    - path: "src/app/timeslicing-uniform-events/page.tsx"
      provides: "Uniform-events route wrapper"
      contains: "binningMode: 'uniform-events'"
  key_links:
    - from: "src/app/timeslicing/page.tsx"
      to: "src/app/timeslicing/lib/TimeslicingRouteShell.tsx"
      via: "wrapper props"
      pattern: "TimeslicingRouteShell"
    - from: "src/app/timeslicing-uniform-events/page.tsx"
      to: "src/app/timeslicing/lib/TimeslicingRouteShell.tsx"
      via: "wrapper props"
      pattern: "TimeslicingRouteShell"
---

<objective>
Create route-level separation for timeslicing binning modes so users can switch method by URL.

Purpose: This removes mixed-mode ambiguity in a single route and gives a stable workflow boundary for comparing uniform-time and uniform-events behavior.
Output: A shared timeslicing shell plus two route wrappers (`/timeslicing` for uniform-time, `/timeslicing-uniform-events` for uniform-events) with regression guards.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-add-dedicated-uniform-events-timeslicing-route/53-RESEARCH.md
@.planning/phases/52-uniform-events-binning-for-timeslicing/52-02-SUMMARY.md
@src/app/timeslicing/page.tsx
@src/app/timeslicing/page.binning-mode.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract a shared timeslicing route shell with explicit binning-mode injection</name>
  <files>src/app/timeslicing/lib/TimeslicingRouteShell.tsx</files>
  <action>Create a reusable client component by moving current orchestration/render logic out of `src/app/timeslicing/page.tsx` and into `TimeslicingRouteShell`. Add explicit props for route-specific config: adaptive `binningMode` (`'uniform-time' | 'uniform-events'`), page title/description text, and method-switch link metadata. Keep existing acceptance event wiring, store mirroring, and suggestion workflow behavior unchanged.</action>
  <verify>`npm run typecheck` passes after extraction.</verify>
  <done>Timeslicing runtime logic lives in one shared shell and can be configured per route without duplicating the full page implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Create dedicated route wrappers for uniform-time and uniform-events</name>
  <files>src/app/timeslicing/page.tsx, src/app/timeslicing-uniform-events/page.tsx</files>
  <action>Replace `src/app/timeslicing/page.tsx` with a thin wrapper that renders `TimeslicingRouteShell` configured for `binningMode: 'uniform-time'`. Add `src/app/timeslicing-uniform-events/page.tsx` as a sibling wrapper configured for `binningMode: 'uniform-events'`. In both wrappers, include explicit route-switch affordance text/link so users can move between methods by route in one click.</action>
  <verify>Run `npm run typecheck` and confirm both routes compile with no missing imports.</verify>
  <done>Both routes exist, are route-configured by mode, and users can switch methods by navigating between the two pages.</done>
</task>

<task type="auto">
  <name>Task 3: Add route-intent regression tests for both wrappers</name>
  <files>src/app/timeslicing/page.binning-mode.test.ts, src/app/timeslicing-uniform-events/page.binning-mode.test.ts</files>
  <action>Update the existing `/timeslicing` source-contract test to assert wrapper config remains `binningMode: 'uniform-time'`. Add a matching test for `/timeslicing-uniform-events` asserting `binningMode: 'uniform-events'`. Also assert each wrapper contains its counterpart route link path so route-switch affordance cannot regress silently.</action>
  <verify>`npm test -- --run src/app/timeslicing/page.binning-mode.test.ts src/app/timeslicing-uniform-events/page.binning-mode.test.ts` passes.</verify>
  <done>Tests fail fast if either route loses its explicit mode assignment or route-switch link intent.</done>
</task>

</tasks>

<verification>
- `/timeslicing` is uniform-time and `/timeslicing-uniform-events` is uniform-events.
- Route wrappers delegate to one shared shell instead of duplicating full orchestration code.
- Route-intent tests lock mode assignment and method-switch links.
</verification>

<success_criteria>
- Users can switch binning method by URL and each route consistently applies its configured mode.
- No duplicated full timeslicing page implementation is introduced for the dedicated route.
</success_criteria>

<output>
After completion, create `.planning/phases/53-add-dedicated-uniform-events-timeslicing-route/53-01-SUMMARY.md`
</output>
