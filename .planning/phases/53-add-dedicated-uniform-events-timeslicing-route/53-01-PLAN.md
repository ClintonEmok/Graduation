---
phase: 53-add-dedicated-uniform-events-timeslicing-route
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx
  - src/app/timeslicing-algos/page.tsx
  - src/app/timeslicing-algos/page.timeline-algos.test.ts
autonomous: true

must_haves:
  truths:
    - "Users can open `/timeslicing-algos` and use a dedicated timeslicing algorithm test surface"
    - "Users can drive timeline interactions on that route and immediately see algorithm output updates"
    - "The dedicated route excludes suggestion-review/full-auto acceptance workflow UI so algorithm behavior is isolated"
  artifacts:
    - path: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      provides: "Algorithm-focused route shell with timeline controls and compute wiring"
      contains: "computeMaps"
    - path: "src/app/timeslicing-algos/page.tsx"
      provides: "Dedicated `/timeslicing-algos` route entry"
      contains: "TimeslicingAlgosRouteShell"
    - path: "src/app/timeslicing-algos/page.timeline-algos.test.ts"
      provides: "Regression coverage for route intent and timeline algorithm interaction"
      contains: "timeslicing-algos"
  key_links:
    - from: "src/app/timeslicing-algos/page.tsx"
      to: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      via: "route shell render"
      pattern: "TimeslicingAlgosRouteShell"
    - from: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      to: "timeline interaction handlers"
      via: "timeline selection/brush callbacks -> compute/update"
      pattern: "on.*(Brush|Selection|Range)"
---

<objective>
Create a dedicated `/timeslicing-algos` route focused on core timeslicing algorithm behavior and timeline testing.

Purpose: This gives a clean route for algorithm iteration without full-auto package workflow noise, so timeline interactions and algorithm responses can be verified directly.
Output: A new route shell under `src/app/timeslicing-algos/`, a dedicated page at `/timeslicing-algos`, and regression tests covering interaction-focused intent.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-add-dedicated-uniform-events-timeslicing-route/53-RESEARCH.md
@.planning/phases/52-uniform-events-binning-for-timeslicing/52-01-SUMMARY.md
@src/app/timeslicing/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build an algorithm-focused route shell under `src/app/timeslicing-algos/`</name>
  <files>src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx</files>
  <action>Create a dedicated client shell that contains only core timeslicing algorithm controls and timeline-test interaction plumbing needed to run and observe algorithm outputs. Reuse existing compute/timeline primitives where available, but do not carry over full-auto package flows, suggestion review/history panels, or acceptance-event orchestration from broader timeslicing surfaces.</action>
  <verify>`npm run typecheck` passes with the new shell file.</verify>
  <done>The route shell supports algorithm execution + timeline interaction while excluding suggestion/full-auto orchestration UI and logic.</done>
</task>

<task type="auto">
  <name>Task 2: Add the dedicated `/timeslicing-algos` route entry page</name>
  <files>src/app/timeslicing-algos/page.tsx</files>
  <action>Create the route page under `src/app/timeslicing-algos/page.tsx` and render `TimeslicingAlgosRouteShell` as its primary surface. Keep page copy/instructions oriented to algorithm + timeline testing (not review/approval workflow steps), and ensure naming consistently uses "timeslicing algos" route intent.</action>
  <verify>`npm run typecheck` confirms route page compiles and is reachable via app-router conventions.</verify>
  <done>Visiting `/timeslicing-algos` renders the dedicated algorithm test surface with route-specific intent.</done>
</task>

<task type="auto">
  <name>Task 3: Add route-intent and interaction-surface regression tests</name>
  <files>src/app/timeslicing-algos/page.timeline-algos.test.ts</files>
  <action>Add deterministic tests that assert `/timeslicing-algos` mounts the algorithm shell, exposes timeline-interaction entry points used for testing algorithm behavior, and does not import/render suggestion history/review or acceptance orchestration components.</action>
  <verify>`npm test -- --run src/app/timeslicing-algos/page.timeline-algos.test.ts` passes.</verify>
  <done>Tests fail if the dedicated route loses interaction-focused algorithm coverage or regresses into workflow-heavy UI orchestration.</done>
</task>

</tasks>

<verification>
- `/timeslicing-algos` exists and renders an algorithm-focused timeline testing surface.
- Dedicated route shell excludes full-auto/suggestion acceptance orchestration concerns.
- Tests lock route intent and timeline interaction coverage.
</verification>

<success_criteria>
- Users can exercise core timeslicing algorithm behavior through timeline interactions on `/timeslicing-algos`.
- The dedicated route remains focused (no suggestion review/history or acceptance event orchestration creep).
</success_criteria>

<output>
After completion, create `.planning/phases/53-add-dedicated-uniform-events-timeslicing-route/53-01-SUMMARY.md`
</output>
