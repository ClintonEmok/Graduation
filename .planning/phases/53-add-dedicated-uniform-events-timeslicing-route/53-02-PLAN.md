---
phase: 53-add-dedicated-uniform-events-timeslicing-route
plan: 2
type: execute
wave: 2
depends_on: [53-01]
files_modified:
  - src/lib/adaptive/route-binning-mode.ts
  - src/lib/adaptive/route-binning-mode.test.ts
  - src/components/viz/MainScene.tsx
autonomous: true

must_haves:
  truths:
    - "Visiting `/timeslicing` drives global adaptive fetches with `uniform-time` binning mode"
    - "Visiting `/timeslicing-algos` can drive global adaptive fetches with either `uniform-time` or `uniform-events` based on algorithm route mode selection"
    - "Visiting unrelated or malformed paths safely falls back to `uniform-time` mode"
  artifacts:
    - path: "src/lib/adaptive/route-binning-mode.ts"
      provides: "Central route->AdaptiveBinningMode resolver with explicit override support"
      contains: "resolveRouteBinningMode"
    - path: "src/components/viz/MainScene.tsx"
      provides: "Global adaptive fetch mode derived from shared resolver"
      contains: "route-binning-mode"
    - path: "src/lib/adaptive/route-binning-mode.test.ts"
      provides: "Coverage for known route mappings and default fallback"
      contains: "uniform-events"
  key_links:
    - from: "src/components/viz/MainScene.tsx"
      to: "src/lib/adaptive/route-binning-mode.ts"
      via: "resolveRouteBinningMode(pathname)"
      pattern: "resolveRouteBinningMode"
    - from: "src/lib/adaptive/route-binning-mode.ts"
      to: "src/app/timeslicing-algos/page.tsx"
      via: "path contract"
      pattern: "timeslicing-algos"
---

<objective>
Harden route-mode wiring for the dedicated `timeslicing algos` route using a centralized resolver.

Purpose: This keeps timeline algorithm testing behavior deterministic on `/timeslicing-algos`, supports in-route mode switching between both algorithms, and prevents path-heuristic drift in global adaptive fetch mode selection while preserving room for future algorithm variants.
Output: A tested route-mode resolver supporting route defaults plus explicit override, `MainScene` wired to that resolver, and regression tests for route contracts.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-add-dedicated-uniform-events-timeslicing-route/53-RESEARCH.md
@src/components/viz/MainScene.tsx
@src/app/timeslicing/page.tsx
@src/app/timeslicing-algos/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a central route-to-binning resolver utility</name>
  <files>src/lib/adaptive/route-binning-mode.ts</files>
  <action>Create a focused utility that resolves pathname to `AdaptiveBinningMode` using explicit route matching plus optional explicit override input (from `/timeslicing-algos` mode controls). Route defaults should map `/timeslicing` -> `uniform-time` and `/timeslicing-algos` -> `uniform-events`, with a safe default of `uniform-time` for all other paths. If an explicit override of `uniform-time` or `uniform-events` is present, it must take precedence over the route default. Keep this as the single source of truth for mode resolution and avoid embedding assumptions that the algorithm route will permanently support only two methods.</action>
  <verify>`npm run typecheck` passes and utility exports a typed resolver function.</verify>
  <done>A single, explicit resolver exists for route defaults and `/timeslicing-algos` override-based switching between both modes.</done>
</task>

<task type="auto">
  <name>Task 2: Replace MainScene inline pathname heuristic with resolver usage</name>
  <files>src/components/viz/MainScene.tsx</files>
  <action>Remove inline `pathname?.startsWith('/timeslicing')` logic from `MainScene` and derive `activeBinningMode` via the new resolver utility, including any explicit mode override surfaced by `/timeslicing-algos` route state/query. Keep existing `/api/adaptive/global?binningMode=...` fetch shape and fallback behavior unchanged; this task is about wiring ownership, not changing adaptive fetch contracts. Do not add suggestion/full-auto acceptance orchestration concerns here; keep this change limited to algorithm route mode resolution.</action>
  <verify>`npm run typecheck` passes and `MainScene.tsx` no longer contains `startsWith('/timeslicing')`.</verify>
  <done>Global adaptive mode selection in `MainScene` is centralized, route-aware, and supports explicit mode switching on `/timeslicing-algos`.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for resolver mappings and default behavior</name>
  <files>src/lib/adaptive/route-binning-mode.test.ts</files>
  <action>Add deterministic tests covering known mappings (`/timeslicing`, `/timeslicing-algos`), explicit override precedence (`uniform-time` and `uniform-events`), and default fallback for unrelated paths. Include an assertion for nullish/empty pathname inputs to ensure the resolver remains fail-safe and defaults to `uniform-time`.</action>
  <verify>`npm test -- --run src/lib/adaptive/route-binning-mode.test.ts` passes.</verify>
  <done>Automated tests prevent route-mode mapping and override-precedence regressions while locking default-safe behavior.</done>
</task>

</tasks>

<verification>
- `MainScene` uses shared resolver utility for route mode selection.
- Resolver includes `/timeslicing` and `/timeslicing-algos` route mappings with safe default and explicit override support.
- Resolver tests cover explicit mappings, override precedence, and fallback paths.
</verification>

<success_criteria>
- Route-mode behavior remains stable when adding/changing timeslicing-related routes.
- Global adaptive fetch mode cannot silently regress due to inline pathname string drift.
</success_criteria>

<output>
After completion, create `.planning/phases/53-add-dedicated-uniform-events-timeslicing-route/53-02-SUMMARY.md`
</output>
