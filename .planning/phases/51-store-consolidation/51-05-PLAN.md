---
phase: 51-store-consolidation
plan: 05
type: execute
wave: 3
depends_on: [51-02]
files_modified:
  - src/app/timeline-test-3d/components/TimeSlices3D.tsx
autonomous: true

must_haves:
  truths:
    - "Timeline-test-3d slice interactions route through the bounded slice-domain store"
    - "3D slice editing behavior remains parity-aligned with timeline-test after rewiring"
    - "timeline-test-3d no longer depends on split slice-store module imports"
  artifacts:
    - path: "src/app/timeline-test-3d/components/TimeSlices3D.tsx"
      provides: "3D slice editing subscriptions/actions bound to `useSliceDomainStore`"
      contains: "useSliceDomainStore"
  key_links:
    - from: "src/app/timeline-test-3d/components/TimeSlices3D.tsx"
      to: "src/store/useSliceDomainStore.ts"
      via: "3D selection/create/adjust action and selector wiring"
      pattern: "useSliceDomainStore"
---

<objective>
Rewire timeline-test-3d slice interactions to bounded domain selectors/actions.

Purpose: This isolates 3D slice rewiring from timeline-test and data-store migration work to reduce context pressure.
Output: `TimeSlices3D` uses bounded domain selectors/actions and removes split-slice imports.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-store-consolidation/51-CONTEXT.md
@.planning/phases/51-store-consolidation/51-RESEARCH.md
@.planning/phases/51-store-consolidation/51-02-SUMMARY.md
@src/app/timeline-test-3d/components/TimeSlices3D.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire TimeSlices3D to bounded slice-domain selectors/actions</name>
  <files>src/app/timeline-test-3d/components/TimeSlices3D.tsx</files>
  <action>Replace split-slice store usage in `TimeSlices3D` with `useSliceDomainStore` and selector helpers while preserving existing 3D create/select/adjust behavior and parity semantics.</action>
  <verify>`npm run lint -- src/app/timeline-test-3d/components/TimeSlices3D.tsx` passes</verify>
  <done>`TimeSlices3D` compiles and uses bounded slice-domain selectors/actions only.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce timeline-test-3d split-store import gate</name>
  <files>src/app/timeline-test-3d/components/TimeSlices3D.tsx</files>
  <action>Run import-gate checks for `TimeSlices3D` and remove any remaining split-slice store imports in that file.</action>
  <verify>`rg -l "@/store/useSlice(SelectionStore|CreationStore|AdjustmentStore)" src/app/timeline-test-3d/components/TimeSlices3D.tsx | wc -l` returns `0`</verify>
  <done>`TimeSlices3D` has no direct imports of split slice stores.</done>
</task>

</tasks>

<verification>
- `TimeSlices3D` uses bounded slice-domain selectors/actions.
- `rg -l "@/store/useSlice(SelectionStore|CreationStore|AdjustmentStore)" src/app/timeline-test-3d/components/TimeSlices3D.tsx | wc -l` equals `0`.
</verification>

<success_criteria>
- Phase 51 criterion (1) is satisfied for timeline-test-3d slice interactions.
- Timeline-test-3d no longer relies on split-slice import paths.
</success_criteria>

<output>
After completion, create `.planning/phases/51-store-consolidation/51-05-SUMMARY.md`
</output>
