---
phase: 51-store-consolidation
plan: 03
type: execute
wave: 2
depends_on: [51-01]
files_modified:
  - src/lib/data/types.ts
  - src/lib/data/selectors.ts
  - src/store/useTimelineDataStore.ts
  - src/store/useDataStore.ts
  - src/lib/trajectories.ts
  - src/components/viz/ClusterManager.tsx
  - src/components/viz/TrajectoryLayer.tsx
autonomous: true

must_haves:
  truths:
    - "Timeline and visualization views keep showing the same filtered data during migration"
    - "Users see no behavior change in clustering and trajectory rendering after this extraction step"
    - "The app remains stable while the deprecated data-store path is prepared for safe removal"
  artifacts:
    - path: "src/lib/data/types.ts"
      provides: "Shared `DataPoint`, `ColumnarData`, and `FilteredPoint` contracts"
      exports: ["DataPoint", "ColumnarData", "FilteredPoint"]
    - path: "src/lib/data/selectors.ts"
      provides: "`selectFilteredData` selector extracted from deprecated store file"
      exports: ["selectFilteredData"]
    - path: "src/store/useTimelineDataStore.ts"
      provides: "Primary timeline data store replacing deprecated file ownership"
      exports: ["useTimelineDataStore"]
    - path: "src/store/useDataStore.ts"
      provides: "Temporary compatibility shim only"
      contains: "useTimelineDataStore"
  key_links:
    - from: "src/components/viz/ClusterManager.tsx"
      to: "src/lib/data/selectors.ts"
      via: "filtered-point derivation for clustering"
      pattern: "selectFilteredData"
    - from: "src/lib/trajectories.ts"
      to: "src/lib/data/types.ts"
      via: "filtered-point typing"
      pattern: "FilteredPoint"
---

<objective>
Create a non-deprecated timeline data contract/store surface and move shared selector/type ownership out of `useDataStore.ts`.

Purpose: This de-risks deletion by separating reusable data contracts from legacy store location before bulk consumer migration.
Output: `src/lib/data/*` shared contracts/selectors, `useTimelineDataStore`, and a temporary compatibility shim in `useDataStore.ts`.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-store-consolidation/51-CONTEXT.md
@.planning/phases/51-store-consolidation/51-RESEARCH.md
@.planning/phases/51-store-consolidation/51-01-SUMMARY.md
@src/store/useDataStore.ts
@src/components/viz/ClusterManager.tsx
@src/components/viz/TrajectoryLayer.tsx
@src/lib/trajectories.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared data contracts and filtered-data selector from deprecated store file</name>
  <files>src/lib/data/types.ts, src/lib/data/selectors.ts, src/lib/trajectories.ts, src/components/viz/ClusterManager.tsx, src/components/viz/TrajectoryLayer.tsx</files>
  <action>Move `DataPoint`, `ColumnarData`, `FilteredPoint`, and `selectFilteredData` ownership out of `src/store/useDataStore.ts` into `src/lib/data/types.ts` and `src/lib/data/selectors.ts` with unchanged signatures/behavior. Update immediate type/selector consumers (`trajectories`, `ClusterManager`, `TrajectoryLayer`) to import from the new modules so these contracts are no longer anchored to the deprecated store file.</action>
  <verify>`npm run typecheck` passes</verify>
  <done>Shared data contracts/selectors are defined in `src/lib/data/*`, and downstream imports compile with parity behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Introduce non-deprecated timeline data store path</name>
  <files>src/store/useTimelineDataStore.ts</files>
  <action>Create `useTimelineDataStore` as the primary store for timeline/viz data metadata and loading helpers currently held in `useDataStore` (`data`, `columns`, spatial/time bounds, mock flags, load/generate actions). Keep runtime behavior equivalent to existing store semantics and avoid introducing new async data ownership decisions in this task.</action>
  <verify>`npm run lint -- src/store/useTimelineDataStore.ts` passes</verify>
  <done>Non-deprecated timeline data store exists with contract parity for existing consumers.</done>
</task>

<task type="auto">
  <name>Task 3: Convert useDataStore.ts into a compatibility shim for staged migration</name>
  <files>src/store/useDataStore.ts</files>
  <action>Refactor `src/store/useDataStore.ts` into a temporary compatibility surface that re-exports `useTimelineDataStore` and shared `src/lib/data/*` contracts/selectors without defining original logic inline. Preserve import compatibility for pending consumers during migration, but clearly mark this file as temporary shim-to-delete in the same phase.</action>
  <verify>`npm run typecheck && npm test -- --run src/store/useSliceStore.test.ts` passes</verify>
  <done>Deprecated file is reduced to a compatibility bridge; canonical logic/types/selectors are owned by non-deprecated modules.</done>
</task>

</tasks>

<verification>
- `selectFilteredData` and `FilteredPoint` are exported from `src/lib/data/selectors.ts` / `src/lib/data/types.ts`.
- `src/store/useTimelineDataStore.ts` is the canonical implementation surface.
- `src/store/useDataStore.ts` acts as compatibility shim only, enabling safe migration in the next plan.
</verification>

<success_criteria>
- Phase 51 criterion (2) precondition is met: migration target contracts are extracted before deleting deprecated path.
- Deprecated store ownership is reduced to compatibility-only, lowering deletion blast radius.
</success_criteria>

<output>
After completion, create `.planning/phases/51-store-consolidation/51-03-SUMMARY.md`
</output>
