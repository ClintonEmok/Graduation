---
phase: 51-store-consolidation
plan: 02
type: execute
wave: 2
depends_on: [51-01]
files_modified:
  - src/store/useSliceStore.ts
  - src/store/useSliceSelectionStore.ts
  - src/store/useSliceCreationStore.ts
  - src/store/useSliceAdjustmentStore.ts
autonomous: true

must_haves:
  truths:
    - "Legacy slice-store paths remain import-compatible while no longer owning independent Zustand roots"
    - "Slice creation/selection/adjustment APIs delegate to `useSliceDomainStore` as the single state owner"
    - "Adapter conversion keeps exported signatures stable for staged consumer rewiring"
  artifacts:
    - path: "src/store/useSliceStore.ts"
      provides: "Core slice compatibility surface mapped to bounded domain selectors/actions"
      contains: "useSliceDomainStore"
    - path: "src/store/useSliceSelectionStore.ts"
      provides: "Selection compatibility surface mapped to bounded domain selectors/actions"
      contains: "selectedIds"
    - path: "src/store/useSliceCreationStore.ts"
      provides: "Creation compatibility surface mapped to bounded domain selectors/actions"
      contains: "startCreating"
    - path: "src/store/useSliceAdjustmentStore.ts"
      provides: "Adjustment compatibility surface mapped to bounded domain selectors/actions"
      contains: "startBoundaryAdjustment"
  key_links:
    - from: "src/store/useSliceStore.ts"
      to: "src/store/useSliceDomainStore.ts"
      via: "legacy core slice exports delegate to bounded domain store"
      pattern: "useSliceDomainStore"
    - from: "src/store/useSliceAdjustmentStore.ts"
      to: "src/store/useSliceDomainStore.ts"
      via: "adjustment actions/selectors forward through bounded domain store"
      pattern: "useSliceDomainStore"
---

<objective>
Convert legacy slice-store modules into compatibility adapters over the bounded slice-domain store.

Purpose: This isolates adapter conversion from broad consumer rewiring so execution stays context-safe and reduces migration risk.
Output: The four legacy slice-store files become adapter-only surfaces over `useSliceDomainStore` with no separate Zustand roots.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-store-consolidation/51-CONTEXT.md
@.planning/phases/51-store-consolidation/51-RESEARCH.md
@.planning/phases/51-store-consolidation/51-01-SUMMARY.md
@src/store/useSliceStore.ts
@src/store/useSliceSelectionStore.ts
@src/store/useSliceCreationStore.ts
@src/store/useSliceAdjustmentStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert legacy slice-store modules into bounded-domain compatibility adapters</name>
  <files>src/store/useSliceStore.ts, src/store/useSliceSelectionStore.ts, src/store/useSliceCreationStore.ts, src/store/useSliceAdjustmentStore.ts</files>
  <action>Refactor each legacy slice store file so it no longer creates an independent Zustand root. Preserve exported names and call signatures (`useSliceStore`, selection helpers, creation actions, adjustment actions) but delegate read/write behavior to `useSliceDomainStore` selectors/actions. Keep parity with existing slice behavior; this task changes ownership boundaries, not user-visible semantics.</action>
  <verify>`npm run typecheck` passes</verify>
  <done>All four legacy files remain import-compatible while delegating state ownership to `useSliceDomainStore`.</done>
</task>

<task type="auto">
  <name>Task 2: Add an explicit no-new-root guard for converted adapters</name>
  <files>src/store/useSliceStore.ts, src/store/useSliceSelectionStore.ts, src/store/useSliceCreationStore.ts, src/store/useSliceAdjustmentStore.ts</files>
  <action>Verify converted adapter files do not instantiate new stores and only route through the bounded domain store. Keep this gate strict so downstream consumer rewiring can assume a single slice-state owner is already enforced.</action>
  <verify>`rg -n "\bcreate\(" src/store/useSliceStore.ts src/store/useSliceSelectionStore.ts src/store/useSliceCreationStore.ts src/store/useSliceAdjustmentStore.ts` returns `0` matches</verify>
  <done>No legacy adapter file directly calls Zustand `create`, confirming no split store roots remain in the compatibility layer.</done>
</task>

</tasks>

<verification>
- Legacy slice-store modules do not instantiate separate Zustand roots.
- Adapter exports remain import-compatible while delegating to `useSliceDomainStore`.
</verification>

<success_criteria>
- Phase 51 criterion (1) migration groundwork is complete: one bounded slice-state owner exists behind compatibility exports.
- Active consumer rewiring can proceed in a separate plan without adapter churn.
</success_criteria>

<output>
After completion, create `.planning/phases/51-store-consolidation/51-02-SUMMARY.md`
</output>
