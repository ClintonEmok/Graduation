---
phase: 51-store-consolidation
plan: 07
type: execute
wave: 4
depends_on: [51-03, 51-04, 51-05, 51-06]
files_modified:
  - src/app/timeslicing/page.tsx
  - src/app/timeline-test/page.tsx
  - src/app/timeline-test-3d/page.tsx
  - src/app/timeline-test-3d/components/TimelineTest3DPoints.tsx
  - src/app/timeline-test-3d/components/TimeSlices3D.tsx
  - src/app/timeline-test-3d/components/WarpSlices3D.tsx
  - src/app/timeline-test-3d/3d/useCanonicalPoints.ts
  - src/components/viz/ClusterManager.tsx
  - src/components/viz/TrajectoryLayer.tsx
  - src/store/useSliceStore.ts
  - src/store/useDataStore.ts
autonomous: true

must_haves:
  truths:
    - "No source files in `src/` import deprecated `useDataStore` after migration"
    - "Route entry points and residual advanced consumers use canonical data-store contracts"
    - "`src/store/useDataStore.ts` is removed only after a zero-import gate passes"
  artifacts:
    - path: "src/app/timeslicing/page.tsx"
      provides: "Timeslicing route reads canonical data-store/selectors"
      contains: "useTimelineDataStore"
    - path: "src/components/viz/ClusterManager.tsx"
      provides: "Cluster derivation uses canonical store/selectors after migration"
      contains: "selectFilteredData"
    - path: "src/store/useDataStore.ts"
      provides: "Deprecated store path retired from source tree"
      contains: "(deleted)"
  key_links:
    - from: "src/components/viz/TrajectoryLayer.tsx"
      to: "src/lib/data/selectors.ts"
      via: "trajectory rendering uses canonical filtered-data selector"
      pattern: "selectFilteredData"
    - from: "src/app/timeline-test-3d/components/TimelineTest3DPoints.tsx"
      to: "src/store/useTimelineDataStore.ts"
      via: "3D route point rendering uses canonical timeline data store"
      pattern: "useTimelineDataStore"
---

<objective>
Finish remaining `useDataStore` rewires, enforce zero-import deletion gate, and remove the deprecated file.

Purpose: This closes the phase objective while preserving the explicit removal safeguard: delete only after import count proves zero.
Output: Remaining route/advanced consumer files are migrated; `src/store/useDataStore.ts` is deleted after passing the gate.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-store-consolidation/51-CONTEXT.md
@.planning/phases/51-store-consolidation/51-RESEARCH.md
@.planning/phases/51-store-consolidation/51-04-SUMMARY.md
@.planning/phases/51-store-consolidation/51-05-SUMMARY.md
@.planning/phases/51-store-consolidation/51-06-SUMMARY.md
@src/store/useTimelineDataStore.ts
@src/lib/data/types.ts
@src/lib/data/selectors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate remaining route and advanced consumer imports off `useDataStore`</name>
  <files>src/app/timeslicing/page.tsx, src/app/timeline-test/page.tsx, src/app/timeline-test-3d/page.tsx, src/app/timeline-test-3d/components/TimelineTest3DPoints.tsx, src/app/timeline-test-3d/components/TimeSlices3D.tsx, src/app/timeline-test-3d/components/WarpSlices3D.tsx, src/app/timeline-test-3d/3d/useCanonicalPoints.ts, src/components/viz/ClusterManager.tsx, src/components/viz/TrajectoryLayer.tsx, src/store/useSliceStore.ts</files>
  <action>Replace any remaining `@/store/useDataStore` imports in this residual set with canonical replacements (`@/store/useTimelineDataStore`, `@/lib/data/types`, `@/lib/data/selectors`) while preserving existing behavior and selector granularity.</action>
  <verify>`npm run typecheck` passes</verify>
  <done>Remaining route and advanced consumer surfaces compile with canonical imports and no `useDataStore` dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce phase-wide zero-import gate before deletion</name>
  <files>src/store/useDataStore.ts</files>
  <action>Run the phase-wide import gate and only proceed to deletion once `src/` has zero `useDataStore` imports/references.
</action>
  <verify>`test "$(rg -l "@/store/useDataStore|useDataStore" src | wc -l | tr -d ' ')" = "0"` passes</verify>
  <done>Zero-import verification passes across `src/`, proving safe deletion preconditions are met.</done>
</task>

<task type="auto">
  <name>Task 3: Delete deprecated store file and run targeted regression gate</name>
  <files>src/store/useDataStore.ts, src/app/timeslicing/page.tsx, src/app/timeline-test/page.tsx, src/app/timeline-test-3d/page.tsx, src/components/viz/ClusterManager.tsx, src/components/viz/TrajectoryLayer.tsx</files>
  <action>Delete `src/store/useDataStore.ts` after Task 2 passes, then run parity-focused checks on timeline/map/viz high-risk surfaces. Keep this a stabilization gate only; do not introduce feature changes.</action>
  <verify>`npm run typecheck && npm run lint -- src/components/timeline src/components/viz src/components/map src/hooks src/app/timeslicing/page.tsx src/app/timeline-test/page.tsx src/app/timeline-test-3d/page.tsx && npm test -- --run src/store/useSliceStore.test.ts src/store/useSliceAdjustmentStore.test.ts` passes</verify>
  <done>Deprecated store file is removed and parity-focused checks pass with no contract regressions.</done>
</task>

</tasks>

<verification>
- `rg -l "@/store/useDataStore|useDataStore" src | wc -l` equals `0` before and after deletion.
- `src/store/useDataStore.ts` is deleted.
- Typecheck, targeted lint, and targeted tests pass.
</verification>

<success_criteria>
- Phase 51 criterion (2) is fully satisfied: deprecated `useDataStore.ts` is removed after verified migration.
- Phase 51 criterion (3) is satisfied for data workflows: no duplicate data-store source remains in active surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/51-store-consolidation/51-07-SUMMARY.md`
</output>
