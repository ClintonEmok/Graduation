---
phase: 07-dashboard-demo-preset-thresholds
plan: 01
type: execute
wave: 1
depends_on:
  - 06-02
files_modified:
  - src/components/dashboard-demo/lib/demo-preset-thresholds.ts
  - src/store/useDashboardDemoTimeslicingModeStore.ts
  - src/components/dashboard-demo/DemoSlicePanel.tsx
  - src/app/dashboard-demo/page.shell.test.tsx
autonomous: true
must_haves:
  truths:
    - "User can tune threshold values separately for each context-aware generation preset inside /dashboard-demo."
    - "Switching presets restores that preset's own threshold values instead of a shared global value."
    - "Threshold edits stay demo-local and do not change the stable /timeslicing route."
    - "The demo reuses existing preset concepts rather than introducing a new algorithm family."
  artifacts:
    - path: "src/components/dashboard-demo/lib/demo-preset-thresholds.ts"
      provides: "Demo-local preset threshold defaults and labels"
    - path: "src/store/useDashboardDemoTimeslicingModeStore.ts"
      provides: "Persisted per-preset threshold state for the demo"
    - path: "src/components/dashboard-demo/DemoSlicePanel.tsx"
      provides: "Preset-scoped threshold controls in the demo rail"
    - path: "src/app/dashboard-demo/page.shell.test.tsx"
      provides: "Source-inspection coverage for demo-local controls and stable-route isolation"
  key_links:
    - from: "src/components/dashboard-demo/DemoSlicePanel.tsx"
      to: "src/store/useDashboardDemoTimeslicingModeStore.ts"
      via: "preset switch and threshold editor interactions"
      pattern: "presetThresholds|setPresetThreshold|selectedPreset"
    - from: "src/store/useDashboardDemoTimeslicingModeStore.ts"
      to: "src/components/dashboard-demo/lib/demo-preset-thresholds.ts"
      via: "seed defaults for each preset family"
      pattern: "DEFAULT_PRESET_THRESHOLDS|PRESET_THRESHOLD_GROUPS"
    - from: "src/app/dashboard-demo/page.shell.test.tsx"
      to: "src/store/useTimeslicingModeStore.ts"
      via: "source inspection proving the stable route stays unchanged"
      pattern: "useTimeslicingModeStore|TimesliceToolbar"
---

<objective>
Give `/dashboard-demo` separate, user-editable thresholds for each existing context-aware generation preset so the demo can be tuned per preset without touching the stable route.

Purpose: preserve the demo-local preset experimentation workflow while keeping the `/timeslicing` route stable and unchanged.
Output: per-preset threshold state, demo rail controls, and regression coverage that locks the route boundary.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-dashboard-demo-preset-thresholds/07-CONTEXT.md
@.planning/phases/06-demo-timeline-polish/06-CONTEXT.md
@src/store/useDashboardDemoTimeslicingModeStore.ts
@src/store/useTimeslicingModeStore.ts
@src/components/dashboard-demo/DemoSlicePanel.tsx
@src/components/timeslicing/TimesliceToolbar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add demo-local per-preset threshold state</name>
  <files>src/components/dashboard-demo/lib/demo-preset-thresholds.ts, src/store/useDashboardDemoTimeslicingModeStore.ts</files>
  <action>
    Define the demo-local preset threshold schema and defaults for the existing context-aware generation presets, then persist those values in the dashboard-demo store. Reuse the existing preset families and labels already used by the project, and keep the stable `/timeslicing` store untouched.
  </action>
  <verify>grep -n "presetThresholds\|DEFAULT_PRESET_THRESHOLDS\|PRESET_THRESHOLD_GROUPS\|weekend-heavy\|weekday-weekend\|business-hours" src/components/dashboard-demo/lib/demo-preset-thresholds.ts src/store/useDashboardDemoTimeslicingModeStore.ts</verify>
  <done>The demo store can hold and restore separate threshold values for each preset family.</done>
</task>

<task type="auto">
  <name>Task 2: Render preset-scoped controls in the demo rail</name>
  <files>src/components/dashboard-demo/DemoSlicePanel.tsx, src/app/dashboard-demo/page.shell.test.tsx</files>
  <action>
    Surface a preset-scoped threshold editor in the demo slice rail so each preset can be tuned independently. When the user switches presets, load that preset's own threshold values, keep the controls clearly demo-local, and update the source-inspection test so it locks the separate-control contract and the stable-route boundary.
  </action>
  <verify>pnpm exec vitest run src/app/dashboard-demo/page.shell.test.tsx --reporter=dot</verify>
  <done>/dashboard-demo shows separate controls per preset and `/timeslicing` remains unchanged.</done>
</task>

</tasks>

<verification>
Run the dashboard-demo shell test and confirm the preset controls remain demo-local and preset-scoped.
</verification>

<success_criteria>
Each demo preset has its own thresholds, switching presets restores the correct values, and the stable route stays untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-demo-preset-thresholds/07-01-SUMMARY.md`
</output>
