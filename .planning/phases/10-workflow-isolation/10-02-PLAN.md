---
phase: 10-workflow-isolation
plan: 02
type: execute
wave: 1
depends_on:
  - 08-01
files_modified:
  - src/app/dashboard/page.tsx
  - src/components/layout/DashboardLayout.tsx
  - src/components/stkde/DashboardStkdePanel.tsx
  - src/app/dashboard/page.shell.test.tsx
autonomous: true
must_haves:
  truths:
    - "The dashboard opens map-first in a single shared viewport."
    - "The user can swap between map and cube without losing the current selection or time window."
    - "The final dashboard shows a fixed, always-visible STKDE rail on the right and no workflow chrome."
  artifacts:
    - path: "src/app/dashboard/page.tsx"
      provides: "Post-apply dashboard route"
    - path: "src/components/layout/DashboardLayout.tsx"
      provides: "Single main viewport with manual swap control"
    - path: "src/components/stkde/DashboardStkdePanel.tsx"
      provides: "Fixed right-rail STKDE analysis panel"
  key_links:
    - from: "src/app/dashboard/page.tsx"
      to: "src/components/layout/DashboardLayout.tsx"
      via: "render the map/cube viewport and the lower control rail"
      pattern: "DashboardLayout"
    - from: "src/components/layout/DashboardLayout.tsx"
      to: "src/components/map/MapVisualization.tsx"
      via: "default the main viewport to the map"
      pattern: "MapVisualization"
    - from: "src/components/layout/DashboardLayout.tsx"
      to: "src/components/viz/CubeVisualization.tsx"
      via: "swap the same viewport to the cube while preserving store state"
      pattern: "CubeVisualization"
    - from: "src/components/stkde/DashboardStkdePanel.tsx"
      to: "src/store/useStkdeStore.ts"
      via: "hotspot selection and run state"
      pattern: "useStkdeStore"
---

<objective>
Turn the post-apply dashboard into the landing zone for Phase 2: map-first, single viewport, and a fixed STKDE investigation rail.

Purpose: make the handoff land in the simplified dashboard the user asked for instead of the older multi-panel composition.
Output: a dashboard shell that opens on the map, can swap to cube, and keeps the STKDE panel pinned to the right.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-contextual-data-enrichment/08-CONTEXT.md
@.planning/phases/07-dashboard-demo-preset-thresholds/07-CONTEXT.md
@.planning/phases/06-demo-timeline-polish/06-CONTEXT.md
@.planning/phases/05-stats-stkde-interaction/05-CONTEXT.md
@.planning/phases/04-demo-stats-stkde/04-CONTEXT.md
@.planning/phases/03-demo-timeline-rewrite/03-CONTEXT.md
@.planning/phases/02-dashboard-demo-ui-ux/02-CONTEXT.md
@src/app/dashboard/page.tsx
@src/app/dashboard-v2/page.tsx
@src/components/layout/DashboardLayout.tsx
@src/components/stkde/DashboardStkdePanel.tsx
@src/components/map/MapVisualization.tsx
@src/components/viz/CubeVisualization.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the dashboard shell around one shared viewport</name>
  <files>src/app/dashboard/page.tsx, src/components/layout/DashboardLayout.tsx</files>
  <read_first>
    - src/app/dashboard/page.tsx
    - src/components/layout/DashboardLayout.tsx
    - src/app/dashboard-v2/page.tsx
    - src/components/map/MapVisualization.tsx
    - src/components/viz/CubeVisualization.tsx
  </read_first>
  <action>
    Replace the current multi-panel dashboard composition with a single main viewport that opens map-first and can swap to the cube from viewport chrome. Keep the applied timeline rail as the lower secondary control strip, preserve shared store-driven selection and time-window state, and do not reintroduce generate/review/apply workflow chrome into the dashboard route.
  </action>
  <acceptance_criteria>
    - `src/app/dashboard/page.tsx` renders the dashboard route with the map as the default main viewport.
    - `src/components/layout/DashboardLayout.tsx` contains a single swap target for map and cube instead of two always-visible main panels.
    - The dashboard route does not render workflow step labels like `Generate`, `Review`, or `Apply`.
  </acceptance_criteria>
  <verify>grep -n "DashboardLayout\|MapVisualization\|CubeVisualization" src/components/layout/DashboardLayout.tsx && grep -n "DashboardStkdePanel" src/app/dashboard/page.tsx</verify>
  <done>The dashboard opens map-first and treats the cube as a manual swap target inside the same viewport.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the STKDE rail to the final dashboard</name>
  <files>src/components/stkde/DashboardStkdePanel.tsx, src/app/dashboard/page.shell.test.tsx</files>
  <read_first>
    - src/components/stkde/DashboardStkdePanel.tsx
    - src/app/dashboard/page.tsx
    - src/store/useStkdeStore.ts
    - src/store/useSliceDomainStore.ts
  </read_first>
  <action>
    Keep the STKDE panel fixed on the right side of the final dashboard, make hotspot selection the primary action, and remove any generate/review/apply cues from the rail. Add a route-level test that proves the rail stays visible on the dashboard and the workflow chrome stays out of the final shell.
  </action>
  <acceptance_criteria>
    - The dashboard route renders `DashboardStkdePanel` as a fixed right rail.
    - The STKDE rail still reads applied slice state from the shared stores.
    - The dashboard route test passes and asserts workflow chrome is absent.
  </acceptance_criteria>
  <verify>pnpm exec vitest run src/app/dashboard/page.shell.test.tsx --reporter=dot</verify>
  <done>The final dashboard keeps STKDE visible, separated, and limited to post-apply analysis.</done>
</task>

</tasks>

<verification>
Run the dashboard route test and then visually confirm the map-first swap flow and fixed STKDE rail.
</verification>

<success_criteria>
The dashboard opens map-first, swaps cleanly between map and cube, and keeps the STKDE rail visible without any workflow chrome.
</success_criteria>

<output>
After completion, create `.planning/phases/10-workflow-isolation/10-02-SUMMARY.md`
</output>
