---
phase: 09-workflow-isolation
plan: 01
type: execute
wave: 1
depends_on:
  - 08-01
files_modified:
  - src/app/timeslicing/layout.tsx
  - src/app/timeslicing/page.tsx
  - src/app/timeslicing/components/TimeslicingWorkflowShell.tsx
  - src/app/timeslicing/page.workflow-shell.test.tsx
autonomous: true
must_haves:
  truths:
    - "Generate, review, and apply each open as separate full-screen workflow screens."
    - "Workflow screens show wizard chrome and never expose dashboard-only panels."
    - "The route clearly indicates the current step and supports linear next/back navigation."
  artifacts:
    - path: "src/app/timeslicing/layout.tsx"
      provides: "Full-screen route shell"
    - path: "src/app/timeslicing/page.tsx"
      provides: "Route-scoped generate/review/apply content"
    - path: "src/app/timeslicing/components/TimeslicingWorkflowShell.tsx"
      provides: "Step chrome and navigation controls"
  key_links:
    - from: "src/app/timeslicing/page.tsx"
      to: "src/app/timeslicing/components/TimeslicingWorkflowShell.tsx"
      via: "render step sections inside the shell"
      pattern: "TimeslicingWorkflowShell"
    - from: "src/app/timeslicing/components/TimeslicingWorkflowShell.tsx"
      to: "src/store/useTimeslicingModeStore.ts"
      via: "derive the active workflow step from generation/apply state"
      pattern: "generationStatus|mode|lastAppliedAt"
---

<objective>
Isolate the generate/review/apply flow into its own full-screen workflow shell so the workflow stays separate from dashboard analysis.

Purpose: keep workflow chrome separate from analysis chrome and make the step boundary obvious before the handoff work begins.
Output: a route-scoped timeslicing shell with explicit step navigation and a route test that locks the isolation.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-contextual-data-enrichment/08-CONTEXT.md
@.planning/phases/07-dashboard-demo-preset-thresholds/07-CONTEXT.md
@.planning/phases/06-demo-timeline-polish/06-CONTEXT.md
@.planning/phases/05-stats-stkde-interaction/05-CONTEXT.md
@.planning/phases/03-demo-timeline-rewrite/03-CONTEXT.md
@.planning/phases/02-dashboard-demo-ui-ux/02-CONTEXT.md
@src/app/timeslicing/layout.tsx
@src/app/timeslicing/page.tsx
@src/app/dashboard-v2/page.tsx
@src/store/useTimeslicingModeStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the timeslicing workflow shell</name>
  <files>src/app/timeslicing/layout.tsx, src/app/timeslicing/components/TimeslicingWorkflowShell.tsx</files>
  <read_first>
    - src/app/timeslicing/layout.tsx
    - src/app/dashboard-v2/page.tsx
    - src/store/useTimeslicingModeStore.ts
  </read_first>
  <action>
    Replace the bare route wrapper with a compact full-screen wizard shell: top stepper, linear next/back controls, and step labels for Generate, Review, and Apply. Keep the shell route-scoped to /timeslicing, use the existing slate/black styling language, and do not import any dashboard-only surfaces such as the main dashboard header or STKDE panel.
  </action>
  <acceptance_criteria>
    - `src/app/timeslicing/layout.tsx` still contains a full-height wrapper (`min-h-screen`).
    - `src/app/timeslicing/components/TimeslicingWorkflowShell.tsx` contains the strings `Generate`, `Review`, and `Apply`.
    - The shell does not import `DashboardHeader`, `DashboardStkdePanel`, or `ContextualSlicePanel`.
  </acceptance_criteria>
  <verify>grep -n "Generate\|Review\|Apply" src/app/timeslicing/components/TimeslicingWorkflowShell.tsx && grep -n "min-h-screen" src/app/timeslicing/layout.tsx</verify>
  <done>The /timeslicing route renders as an isolated workflow shell with explicit step navigation and no dashboard chrome.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor the timeslicing page into step sections</name>
  <files>src/app/timeslicing/page.tsx, src/app/timeslicing/page.workflow-shell.test.tsx</files>
  <read_first>
    - src/app/timeslicing/page.tsx
    - src/app/timeslicing/components/TimeslicingWorkflowShell.tsx
    - src/app/timeslicing/page.full-auto-acceptance.test.tsx
    - src/app/timeslicing/page.timeline-qa.test.ts
  </read_first>
  <action>
    Reorganize the existing monolithic page into generate, review, and apply sections that render inside the new shell while preserving crime-data loading, generation effects, and the timeline QA card. Keep dashboard-only panels out of this route, and make the route test assert that the workflow shell is present while dashboard surfaces remain absent.
  </action>
  <acceptance_criteria>
    - `/timeslicing` still loads the crime-data summary and timeline QA context.
    - The route test asserts the step labels are present and dashboard-only panels are absent.
    - The page still compiles without moving any dashboard concerns into the timeslicing route.
  </acceptance_criteria>
  <verify>pnpm exec vitest run src/app/timeslicing/page.workflow-shell.test.tsx src/app/timeslicing/page.timeline-qa.test.ts src/app/timeslicing/page.full-auto-acceptance.test.tsx --reporter=dot</verify>
  <done>The timeslicing route is cleanly segmented into workflow steps and remains visually isolated from the dashboard.</done>
</task>

</tasks>

<verification>
Run the focused timeslicing route tests for the workflow shell and current acceptance cases.
</verification>

<success_criteria>
The workflow route is isolated, step-based, and test-locked without leaking dashboard chrome into /timeslicing.
</success_criteria>

<output>
After completion, create `.planning/phases/09-workflow-isolation/09-01-SUMMARY.md`
</output>
