---
phase: 08-contextual-data-enrichment
plan: 01
type: execute
wave: 1
depends_on:
  - 07-01
files_modified:
  - src/components/dashboard-demo/DemoMapVisualization.tsx
  - src/components/dashboard-demo/DemoStatsPanel.tsx
  - src/components/dashboard-demo/DemoStatsMapOverlay.tsx
  - src/components/dashboard-demo/DashboardDemoShell.tsx
autonomous: true
must_haves:
  truths:
    - "The demo gains contextual layers beyond district selection."
    - "Context remains demo-local and does not alter the stable routes."
    - "Context helps explain patterns instead of adding noise."
    - "The contextual layer is ready for workflow isolation after the demo surfaces are stabilized."
  artifacts:
    - path: "src/components/dashboard-demo/DemoMapVisualization.tsx"
      provides: "Demo-local map wrapper for contextual overlays"
    - path: "src/components/dashboard-demo/DemoStatsPanel.tsx"
      provides: "Demo-local stats entry surface"
    - path: "src/components/dashboard-demo/DashboardDemoShell.tsx"
      provides: "Shell composition for context-aware demo surfaces"
    - path: "src/components/dashboard-demo/DemoStatsMapOverlay.tsx"
      provides: "Stats overlay and district/context presentation"
  key_links:
    - from: "src/components/dashboard-demo/DemoStatsPanel.tsx"
      to: "src/components/dashboard-demo/DemoMapVisualization.tsx"
      via: "feed selection context into map-side context layers"
      pattern: "useDashboardDemoAnalysisStore"
    - from: "src/components/dashboard-demo/DemoMapVisualization.tsx"
      to: "src/components/dashboard-demo/DemoStatsMapOverlay.tsx"
      via: "render context-aware overlays inside the demo map shell"
      pattern: "DemoStatsMapOverlay"
---

<objective>
Add contextual data layers to `/dashboard-demo` so the crime analysis can be interpreted against broader conditions like socioeconomic signals, holidays, events, or traffic pressure.

Purpose: enrich the demo without changing the stable routes or diluting the core analysis flow.
Output: a demo-local contextual layer or set of layers that make patterns easier to explain.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-dashboard-demo-preset-thresholds/07-CONTEXT.md
@.planning/phases/06-demo-timeline-polish/06-CONTEXT.md
@src/components/dashboard-demo/DemoMapVisualization.tsx
@src/components/dashboard-demo/DemoStatsPanel.tsx
@src/components/dashboard-demo/DemoStatsMapOverlay.tsx
@src/components/dashboard-demo/DashboardDemoShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the contextual data model for the demo</name>
  <files>src/components/dashboard-demo/DemoStatsPanel.tsx, src/components/dashboard-demo/DemoMapVisualization.tsx</files>
  <read_first>
    - src/components/dashboard-demo/DemoStatsPanel.tsx
    - src/components/dashboard-demo/DemoMapVisualization.tsx
    - src/store/useDashboardDemoAnalysisStore.ts
  </read_first>
  <action>
    Identify the smallest useful contextual signals for the demo and shape them into demo-local state that can be read by the stats and map surfaces without changing the stable routes.
  </action>
  <acceptance_criteria>
    - The demo has a clear contextual data model.
    - The stable routes do not need to change for the demo context.
    - The model supports later UI wiring for overlays or summary chips.
  </acceptance_criteria>
  <verify>grep -n "useDashboardDemoAnalysisStore\|context" src/components/dashboard-demo/DemoStatsPanel.tsx src/components/dashboard-demo/DemoMapVisualization.tsx</verify>
  <done>The demo contextual state is defined and isolated from the stable routes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire contextual presentation into the demo shell</name>
  <files>src/components/dashboard-demo/DemoStatsMapOverlay.tsx, src/components/dashboard-demo/DashboardDemoShell.tsx</files>
  <read_first>
    - src/components/dashboard-demo/DemoStatsMapOverlay.tsx
    - src/components/dashboard-demo/DashboardDemoShell.tsx
  </read_first>
  <action>
    Add the demo-local contextual presentation surface so it can explain crime patterns without overwhelming the dashboard demo or touching the stable dashboard/stats routes.
  </action>
  <acceptance_criteria>
    - The demo surface can show context without replacing the core analysis flow.
    - The overlay remains demo-local.
    - The route test can lock the new contextual wiring.
  </acceptance_criteria>
  <verify>pnpm exec vitest run src/app/dashboard-demo/page.shell.test.tsx --reporter=dot</verify>
  <done>The demo shell can display contextual signals as a lightweight analysis aid.</done>
</task>

</tasks>

<verification>
Review the demo shell visually to confirm the contextual layer helps explain patterns without clutter.
</verification>

<success_criteria>
The demo gains useful contextual signals while staying local, readable, and analysis-first.
</success_criteria>

<output>
After completion, create `.planning/phases/08-contextual-data-enrichment/08-01-SUMMARY.md`
</output>
