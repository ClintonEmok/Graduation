---
phase: 03-demo-timeline-rewrite
plan: 01
type: execute
wave: 1
depends_on:
  - 02-05
files_modified:
  - src/components/timeline/DemoDualTimeline.tsx
  - src/components/dashboard-demo/DemoTimelinePanel.tsx
  - src/components/dashboard-demo/DashboardDemoShell.tsx
  - src/app/dashboard-demo/page.shell.test.tsx
autonomous: true
requirements:
  - DTL-01
  - DTL-02
  - DTL-03
  - DTL-04
  - DTL-05
must_haves:
  truths:
    - "The demo timeline is a route-specific temporal surface for /dashboard-demo, not a modified copy of the production TimelinePanel."
    - "Slices and overlays are presented in a dedicated collapsible companion section above the timeline instead of as inline clutter."
    - "The demo timeline stays the primary temporal control surface while playback, stepping, scaling, and resolution controls remain visible."
    - "The demo rewrite stays isolated from the production DualTimeline and TimelinePanel baselines."
    - "The demo shell and its regression test point to the new demo-only timeline composition instead of the shared timeline shell."
  artifacts:
    - path: "src/components/timeline/DemoDualTimeline.tsx"
      provides: "Demo-only timeline core wrapper that reuses the shared DualTimeline baseline"
      contains: "DemoDualTimeline"
    - path: "src/components/dashboard-demo/DemoTimelinePanel.tsx"
      provides: "Route-specific demo timeline panel with collapsible companion section"
      contains: "DemoTimelinePanel"
    - path: "src/components/dashboard-demo/DashboardDemoShell.tsx"
      provides: "Demo route shell wiring that renders the demo timeline panel"
      contains: "DemoTimelinePanel"
    - path: "src/app/dashboard-demo/page.shell.test.tsx"
      provides: "Source-inspection regression coverage for demo-shell isolation"
      contains: "DemoDualTimeline"
  key_links:
    - from: "src/components/dashboard-demo/DashboardDemoShell.tsx"
      to: "src/components/dashboard-demo/DemoTimelinePanel.tsx"
      via: "render the route-specific timeline panel in the demo shell"
      pattern: "DemoTimelinePanel"
    - from: "src/components/dashboard-demo/DemoTimelinePanel.tsx"
      to: "src/components/timeline/DemoDualTimeline.tsx"
      via: "compose the demo-only core timeline beneath the companion section"
      pattern: "DemoDualTimeline"
    - from: "src/components/dashboard-demo/DemoTimelinePanel.tsx"
      to: "src/store/useSliceStore.ts"
      via: "read slice state for the companion section without creating a new store boundary"
      pattern: "useSliceStore|useTimeslicingModeStore|useTimeStore|useTimelineDataStore"
    - from: "src/app/dashboard-demo/page.shell.test.tsx"
      to: "src/app/dashboard/page.tsx"
      via: "source-inspection regression guard for stable dashboard isolation"
      pattern: "DashboardLayout"
---

<objective>
Rebuild the `/dashboard-demo` timeline as a demo-specific temporal surface with a collapsible slice companion above the timeline.

Purpose: reduce clutter in the demo shell while preserving the control breadth users need and keeping the production timeline baseline untouched.
Output: a new demo-only timeline panel and core wrapper, plus shell wiring and regression coverage that keep `/dashboard-demo` isolated from the stable dashboard route.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@.planning/phases/03-demo-timeline-rewrite/03-CONTEXT.md
@.planning/phases/03-demo-timeline-rewrite/03-RESEARCH.md
@src/components/dashboard-demo/DashboardDemoShell.tsx
@src/components/dashboard-demo/WorkflowSkeleton.tsx
@src/components/timeline/TimelinePanel.tsx
@src/components/timeline/DualTimeline.tsx
@src/components/viz/SliceManagerUI.tsx
@src/app/dashboard-demo/page.shell.test.tsx
@src/app/dashboard/page.tsx
@src/store/useSliceStore.ts
@src/store/useTimeStore.ts
@src/store/useTimelineDataStore.ts
@src/store/useTimeslicingModeStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the demo-only timeline wrapper and companion section</name>
  <files>src/components/timeline/DemoDualTimeline.tsx, src/components/dashboard-demo/DemoTimelinePanel.tsx</files>
  <read_first>
    - src/components/timeline/DualTimeline.tsx
    - src/components/timeline/TimelinePanel.tsx
    - src/components/viz/SliceManagerUI.tsx
    - src/components/dashboard-demo/DashboardDemoShell.tsx
    - src/store/useSliceStore.ts
    - src/store/useTimeStore.ts
    - src/store/useTimelineDataStore.ts
    - src/store/useTimeslicingModeStore.ts
    - .planning/phases/03-demo-timeline-rewrite/03-CONTEXT.md
    - .planning/phases/03-demo-timeline-rewrite/03-RESEARCH.md
  </read_first>
  <action>
    Create `DemoDualTimeline` as a demo-specific wrapper around the existing shared `DualTimeline` API so the production component stays untouched and remains the comparison baseline. Then create `DemoTimelinePanel` as the route-specific panel that places a compact, collapsible slice companion above the demo timeline, keeps playback/stepping/scale/resolution controls visible, and uses the existing slice/time stores as passive inputs only. Do not introduce a new Zustand store boundary; keep the demo-specific behavior in composition and presentation.
  </action>
  <acceptance_criteria>
    - `DemoDualTimeline.tsx` exports `DemoDualTimeline` and references the shared `DualTimeline` implementation.
    - `DemoTimelinePanel.tsx` exports `DemoTimelinePanel` and contains a collapsible companion section above the timeline.
    - `DemoTimelinePanel.tsx` references `useSliceStore`, `useTimeStore`, `useTimelineDataStore`, and `useTimeslicingModeStore` without adding a new store file.
    - `git diff --name-only -- src/components/timeline/DualTimeline.tsx src/components/timeline/TimelinePanel.tsx` remains empty after the change.
  </acceptance_criteria>
  <verify>grep -n "DemoDualTimeline\|DemoTimelinePanel\|useSliceStore\|useTimeStore\|useTimelineDataStore\|useTimeslicingModeStore" src/components/timeline/DemoDualTimeline.tsx src/components/dashboard-demo/DemoTimelinePanel.tsx && git diff --name-only -- src/components/timeline/DualTimeline.tsx src/components/timeline/TimelinePanel.tsx</verify>
  <done>The demo timeline composition exists without mutating the production timeline baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire the demo shell and lock route-isolation regression tests</name>
  <files>src/components/dashboard-demo/DashboardDemoShell.tsx, src/app/dashboard-demo/page.shell.test.tsx</files>
  <read_first>
    - src/components/dashboard-demo/DashboardDemoShell.tsx
    - src/app/dashboard-demo/page.shell.test.tsx
    - src/app/dashboard/page.tsx
    - src/components/timeline/DemoTimelinePanel.tsx
    - src/components/timeline/DemoDualTimeline.tsx
    - src/components/timeline/TimelinePanel.tsx
    - src/components/stkde/DashboardStkdePanel.tsx
    - src/components/dashboard-demo/WorkflowSkeleton.tsx
  </read_first>
  <action>
    Replace the shared `TimelinePanel` import in `DashboardDemoShell` with `DemoTimelinePanel` so `/dashboard-demo` uses the new route-specific composition. Update the source-inspection regression test to assert `DemoTimelinePanel` and `DemoDualTimeline` are present in the demo shell, keep the stable `/dashboard` route pinned to `DashboardLayout`, and add negative checks so the shared timeline shell does not leak back into the demo route.
  </action>
  <acceptance_criteria>
    - `DashboardDemoShell.tsx` imports `DemoTimelinePanel` instead of `TimelinePanel`.
    - `page.shell.test.tsx` matches `DemoTimelinePanel` and `DemoDualTimeline` in the demo shell.
    - `page.shell.test.tsx` still asserts the stable dashboard route uses `DashboardLayout` and does not pick up demo-shell strings.
    - `pnpm exec vitest run src/app/dashboard-demo/page.shell.test.tsx --reporter=dot` passes.
  </acceptance_criteria>
  <verify>pnpm exec vitest run src/app/dashboard-demo/page.shell.test.tsx --reporter=dot</verify>
  <done>The demo route is wired to the new timeline surface and the regression test protects the stable routes from drift.</done>
</task>

</tasks>

<verification>
1. `grep` confirms the new demo timeline files exist and reference the expected stores and wrapper names.
2. `pnpm exec vitest run src/app/dashboard-demo/page.shell.test.tsx --reporter=dot` passes.
3. `git diff --name-only -- src/components/timeline/DualTimeline.tsx src/components/timeline/TimelinePanel.tsx` shows no changes.
</verification>

<success_criteria>
1. `/dashboard-demo` uses a route-specific demo timeline panel instead of the shared timeline shell.
2. Slices and overlays are presented as a collapsible companion layer above the timeline.
3. The production `DualTimeline` and `TimelinePanel` remain untouched and usable as the stable baseline.
4. The demo shell regression test proves the new demo composition stays isolated from the stable dashboard route.
</success_criteria>

<output>
After completion, create `.planning/phases/03-demo-timeline-rewrite/03-01-SUMMARY.md`
</output>
