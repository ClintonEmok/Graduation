---
phase: 02-3d-stkde-on-cube-planes
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/lib/stkde/heatmap-scale.ts
  - src/components/map/MapStkdeHeatmapLayer.tsx
  - src/components/viz/SlicePlane.tsx
  - src/components/viz/TimeSlices.tsx
  - src/components/viz/stkde-overlay.phase2.test.ts
autonomous: true
must_haves:
  truths:
    - "Every visible slice plane shows its own STKDE density overlay."
    - "The cube and the 2D map use the same heatmap color scale."
    - "Hidden slices do not render overlay work or phantom density surfaces."
  artifacts:
    - path: src/lib/stkde/heatmap-scale.ts
      provides: "Shared STKDE heatmap palette"
      contains: "heatmap-color"
    - path: src/components/viz/SlicePlane.tsx
      provides: "Per-slice 3D overlay host"
      contains: "stkde"
    - path: src/components/viz/TimeSlices.tsx
      provides: "Slice-to-response wiring for the cube"
      contains: "stkdeResponse"
    - path: src/components/map/MapStkdeHeatmapLayer.tsx
      provides: "Map heatmap renderer using the shared palette"
      contains: "heatmap-color"
    - path: src/components/viz/stkde-overlay.phase2.test.ts
      provides: "Overlay contract regression coverage"
      contains: "visible planes"
  key_links:
    - from: src/components/viz/TimeSlices.tsx
      to: src/components/viz/SlicePlane.tsx
      via: "sliceResults[slice.id] prop flow"
      pattern: "stkdeResponse|sliceResults"
    - from: src/lib/stkde/heatmap-scale.ts
      to: src/components/map/MapStkdeHeatmapLayer.tsx
      via: "shared gradient stops"
      pattern: "heatmap-color"
---

<objective>
Render the slice-keyed STKDE surface directly on the cube planes with the same color language as the map.

Purpose: Analysts should read density on each visible plane without switching mental models between the 2D map and the 3D cube.
Output: A shared heatmap palette, per-plane overlays in the cube, and regression coverage for the rendering contract.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/viz/TimeSlices.tsx
@src/components/viz/SlicePlane.tsx
@src/components/map/MapStkdeHeatmapLayer.tsx
@src/store/useDashboardDemoAnalysisStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract a shared STKDE heatmap palette</name>
  <files>src/lib/stkde/heatmap-scale.ts, src/components/map/MapStkdeHeatmapLayer.tsx</files>
  <action>
    Move the heatmap color stops and any related scale constants into a shared `src/lib/stkde/heatmap-scale.ts` helper so the cube and map cannot drift apart. Update `MapStkdeHeatmapLayer.tsx` to import that shared palette instead of hardcoding its gradient locally.
  </action>
  <verify>Run a source-inspection test or grep the map layer to confirm it imports the shared STKDE palette helper and no longer owns the gradient tokens.</verify>
  <done>The 2D map and the 3D cube can consume one shared heatmap language.</done>
</task>

<task type="auto">
  <name>Task 2: Mount slice overlays inside the 3D plane renderer</name>
  <files>src/components/viz/SlicePlane.tsx, src/components/viz/TimeSlices.tsx</files>
  <action>
    Thread the slice-aware STKDE response into `TimeSlices.tsx`, then pass the per-slice payload into `SlicePlane.tsx` so each visible plane can render its own overlay host. Keep the existing drag, resize, and creation behavior unchanged; the new work is only the data-to-plane wiring and the density surface layer.
  </action>
  <verify>Confirm the scene uses the slice-keyed response shape and that plane overlays are only created for visible slices.</verify>
  <done>Visible slices in the cube now have a deterministic place to draw their own STKDE density surface.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the overlay contract with regression coverage</name>
  <files>src/components/viz/stkde-overlay.phase2.test.ts</files>
  <action>
    Add a compact source- or component-level regression test that verifies the cube overlay path uses the shared palette, consumes the keyed slice result, and does not render overlays for hidden slices.
  </action>
  <verify>Run `./node_modules/.bin/vitest src/components/viz/stkde-overlay.phase2.test.ts`.</verify>
  <done>The overlay contract fails fast if the cube stops using shared colors or stops binding overlays to visible slices.</done>
</task>

</tasks>

<verification>
Run the palette source check, then the overlay test, then inspect `TimeSlices.tsx`/`SlicePlane.tsx` for the slice-keyed prop flow.
</verification>

<success_criteria>
- The cube renders one STKDE overlay per visible slice plane.
- The 2D map and cube overlays share the same heatmap palette.
- Hidden slices do not allocate or display extra heatmap layers.
</success_criteria>

<output>
After completion, create `.planning/phases/02-3d-stkde-on-cube-planes/02-02-SUMMARY.md`
</output>
