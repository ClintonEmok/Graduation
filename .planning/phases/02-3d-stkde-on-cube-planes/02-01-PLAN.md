---
phase: 02-3d-stkde-on-cube-planes
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/stkde/contracts.ts
  - src/lib/stkde/compute.ts
  - src/app/api/stkde/hotspots/route.ts
  - src/lib/stkde/slice-stkde.phase2.test.ts
autonomous: true
must_haves:
  truths:
    - "The STKDE API can accept a set of visible slices and return slice-keyed density results."
    - "Existing single-surface callers still get the current aggregate heatmap/hotspot contract."
    - "Missing or empty slice input does not break the sampled STKDE path."
  artifacts:
    - path: src/lib/stkde/contracts.ts
      provides: "Slice-aware STKDE request/response types"
      contains: "sliceResults"
    - path: src/lib/stkde/compute.ts
      provides: "Per-slice STKDE aggregation"
      exports: ["computeStkdeFromCrimes"]
    - path: src/app/api/stkde/hotspots/route.ts
      provides: "API forwarding for slice-aware STKDE requests"
      exports: ["POST"]
    - path: src/lib/stkde/slice-stkde.phase2.test.ts
      provides: "Slice-keyed STKDE regression coverage"
      contains: "sliceResults"
  key_links:
    - from: src/app/api/stkde/hotspots/route.ts
      to: src/lib/stkde/compute.ts
      via: "validated request body -> compute call"
      pattern: "computeStkdeFromCrimes|computeStkdeFromAggregates"
    - from: src/lib/stkde/contracts.ts
      to: src/lib/stkde/compute.ts
      via: "shared response schema"
      pattern: "sliceResults"
---

<objective>
Make STKDE compute per visible slice so the cube can paint one density surface per plane.

Purpose: The 3D cube needs slice-keyed density data before any plane overlay can render correctly or update safely when slices change.
Output: Updated STKDE contracts, slice-aware compute plumbing, and a failing-then-passing regression test that locks the response shape.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-store-sync-slice-planes/01-02-SUMMARY.md
@src/lib/stkde/contracts.ts
@src/lib/stkde/compute.ts
@src/app/api/stkde/hotspots/route.ts
</context>

<feature>
  <name>Slice-keyed STKDE computation</name>
  <files>src/lib/stkde/contracts.ts, src/lib/stkde/compute.ts, src/app/api/stkde/hotspots/route.ts, src/lib/stkde/slice-stkde.phase2.test.ts</files>
  <behavior>
    - Accept an optional active-slice list in the STKDE request payload and preserve the existing request shape for callers that do not send slices.
    - Return a stable keyed response per slice while keeping the current aggregate top-level heatmap/hotspot fields intact for backwards compatibility.
    - Keep empty-slice and single-slice cases deterministic so the API never regresses to an untyped or partial response.
  </behavior>
  <implementation>
    Start by writing the regression test that describes the desired slice-keyed contract, verify it fails, then extend the STKDE contract types and compute pipeline to produce keyed slice results. Finish by threading the validated request through the API route and re-running the existing STKDE route coverage to ensure aggregate callers still pass.
  </implementation>
</feature>

<verification>
1. Run `./node_modules/.bin/vitest src/lib/stkde/slice-stkde.phase2.test.ts` and confirm the new contract test fails first, then passes after implementation.
2. Run `./node_modules/.bin/vitest src/app/api/stkde/hotspots/route.test.ts` to confirm the route still serves the legacy aggregate contract.
3. Inspect the response schema for a slice-keyed field and unchanged aggregate metadata.
</verification>

<success_criteria>
- Slice-aware requests produce a keyed per-slice STKDE response.
- Legacy STKDE callers still receive the current aggregate heatmap/hotspot payload.
- The regression test protects the slice contract from drifting back to a single-surface-only shape.
</success_criteria>

<output>
After completion, create `.planning/phases/02-3d-stkde-on-cube-planes/02-01-SUMMARY.md`
</output>
