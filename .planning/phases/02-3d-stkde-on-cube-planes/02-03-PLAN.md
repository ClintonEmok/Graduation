---
phase: 02-3d-stkde-on-cube-planes
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/dashboard-demo/lib/useDemoStkde.ts
  - src/components/dashboard-demo/lib/useDemoStkde.phase2.test.ts
autonomous: true
must_haves:
  truths:
    - "Changing the active slices or time range triggers a fresh STKDE request."
    - "Only the latest in-flight request can update the demo rail state."
    - "Rapid slice edits are debounced instead of flooding the API."
  artifacts:
    - path: src/components/dashboard-demo/lib/useDemoStkde.ts
      provides: "Debounced, slice-aware STKDE fetch hook"
      contains: "AbortController"
    - path: src/components/dashboard-demo/lib/useDemoStkde.phase2.test.ts
      provides: "Latest-response regression coverage"
      contains: "out-of-order"
  key_links:
    - from: src/components/dashboard-demo/lib/useDemoStkde.ts
      to: src/store/useSliceStore.ts
      via: "slice signature dependency"
      pattern: "useSliceStore"
    - from: src/components/dashboard-demo/lib/useDemoStkde.ts
      to: /api/stkde/hotspots
      via: "debounced request body"
      pattern: "fetch\\('/api/stkde/hotspots'"
---

<objective>
Make the dashboard-demo STKDE rail recompute when slices or time changes without letting stale responses win.

Purpose: The cube is interactive, so the analysis rail must stay synchronized without spamming the server or flickering from out-of-order responses.
Output: A slice-aware, debounced fetch hook and a regression test that locks the latest-response behavior.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/dashboard-demo/lib/useDemoStkde.ts
@src/components/dashboard-demo/DemoStkdePanel.tsx
@src/store/useDashboardDemoAnalysisStore.ts
@src/store/useSliceStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the demo STKDE fetch slice-aware and debounced</name>
  <files>src/components/dashboard-demo/lib/useDemoStkde.ts</files>
  <action>
    Update the hook to derive a stable slice signature from the active cube slices and include that signature in the request dependency set alongside the current time range and district context. Debounce the fetch so rapid plane drags or range edits coalesce into one request, and keep the existing AbortController/latest-request guard so stale responses cannot overwrite newer state.
  </action>
  <verify>Manually inspect the hook effect dependencies and the debounce/abort path, then run the hook test once it exists.</verify>
  <done>Slice and time changes trigger one latest-only STKDE refresh instead of a burst of competing requests.</done>
</task>

<task type="auto">
  <name>Task 2: Lock latest-response behavior with a regression test</name>
  <files>src/components/dashboard-demo/lib/useDemoStkde.phase2.test.ts</files>
  <action>
    Add a focused test that stubs fetch, changes the slice signature/time context in quick succession, and asserts the hook keeps only the newest response while aborting or ignoring older in-flight results. Cover the stale/loading transition so the rail does not settle on outdated data.
  </action>
  <verify>Run `./node_modules/.bin/vitest src/components/dashboard-demo/lib/useDemoStkde.phase2.test.ts`.</verify>
  <done>The demo STKDE refresh contract fails fast if debounce or latest-response protection regresses.</done>
</task>

</tasks>

<verification>
Run the hook regression test, then inspect `useDemoStkde.ts` to confirm the slice signature participates in the fetch dependency set and the abort guard still wraps every request.
</verification>

<success_criteria>
- STKDE refreshes when the active slices or time range change.
- Only the newest response can update the demo rail state.
- Rapid edits are coalesced by debounce instead of triggering redundant work.
</success_criteria>

<output>
After completion, create `.planning/phases/02-3d-stkde-on-cube-planes/02-03-SUMMARY.md`
</output>
