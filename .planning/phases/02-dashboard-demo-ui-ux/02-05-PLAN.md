---
phase: 02-dashboard-demo-ui-ux
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard-demo/DashboardDemoShell.tsx
  - src/components/dashboard-demo/WorkflowSkeleton.tsx
  - src/app/dashboard-demo/page.shell.test.tsx
autonomous: true
requirements:
  - DEMO-01
  - DEMO-02
  - DEMO-03
  - DEMO-04
  - DEMO-05
  - DEMO-06
  - UXF-01
  - UXF-02
  - UXF-03
  - UXF-04
  - WFUI-01
  - WFUI-02
  - WFUI-03
  - WFUI-04
  - WFUI-05
must_haves:
  truths:
    - "The /dashboard-demo route reads as one cohesive demo shell, not two competing apps."
    - "Users can switch between map and cube from a compact icon-only control with obvious active-state feedback."
    - "The persistent shell chrome shows only applied-state status, not generation clutter or draft metadata."
    - "The desktop layout stays low-density and keeps the timeline rail, shared viewport, and fixed STKDE sidebar visually subordinate to the main analysis surface."
    - "The workflow skeleton is nested under /dashboard-demo as a left-anchored drawer, not a separate route."
    - "The workflow starts at Explore and presents a continuous Build/Review flow before handoff."
    - "Existing /dashboard and /timeslicing shells remain stable and distinct from the demo shell."
  artifacts:
    - path: "src/app/dashboard-demo/page.tsx"
      provides: "Route entry for the demo shell"
      contains: "DashboardDemoShell"
    - path: "src/components/dashboard-demo/DashboardDemoShell.tsx"
      provides: "Map-first shell chrome, viewport swap, timeline rail, and nested workflow host"
      contains: "DashboardStkdePanel"
    - path: "src/components/dashboard-demo/WorkflowSkeleton.tsx"
      provides: "Left-anchored multistep workflow drawer"
      contains: "Explore"
    - path: "src/app/dashboard-demo/page.shell.test.tsx"
      provides: "Route regression coverage for shell composition and route isolation"
      contains: "DashboardDemoShell"
  key_links:
    - from: "src/app/dashboard-demo/page.tsx"
      to: "src/components/dashboard-demo/DashboardDemoShell.tsx"
      via: "route entry"
      pattern: "DashboardDemoShell"
    - from: "src/components/dashboard-demo/DashboardDemoShell.tsx"
      to: "src/components/map/MapVisualization.tsx"
      via: "shared viewport render"
      pattern: "MapVisualization"
    - from: "src/components/dashboard-demo/DashboardDemoShell.tsx"
      to: "src/components/dashboard-demo/WorkflowSkeleton.tsx"
      via: "nested left drawer render"
      pattern: "WorkflowSkeleton"
    - from: "src/app/dashboard-demo/page.shell.test.tsx"
      to: "src/app/dashboard/page.tsx"
      via: "source-inspection regression guard"
      pattern: "DashboardLayout"
    - from: "src/app/dashboard-demo/page.shell.test.tsx"
      to: "src/app/timeslicing/page.tsx"
      via: "source-inspection regression guard"
      pattern: "TimeslicingWorkflowShell"
---

<objective>
Polish `/dashboard-demo` into a cohesive demo shell with a nested workflow scaffold and route-isolation regressions.

Purpose: validate the UX pivot before technical workflow wiring by keeping the demo low-density, map-first, and clearly separate from the stable `/dashboard` and `/timeslicing` experiences.
Output: a demo shell, regression tests, and a nested workflow drawer that can later be wired to real slice state without changing layout.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@.planning/phases/02-dashboard-demo-ui-ux/02-CONTEXT.md
@.planning/phases/02-dashboard-demo-ui-ux/02-DISCUSSION-LOG.md
@src/app/dashboard-demo/page.tsx
@src/components/dashboard-demo/DashboardDemoShell.tsx
@src/app/dashboard/page.tsx
@src/app/timeslicing/page.tsx
@src/components/map/MapVisualization.tsx
@src/components/viz/CubeVisualization.tsx
@src/components/timeline/TimelinePanel.tsx
@src/components/stkde/DashboardStkdePanel.tsx
@src/app/timeslicing/components/TimeslicingWorkflowShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework the demo shell chrome and viewport toggle</name>
  <files>src/components/dashboard-demo/DashboardDemoShell.tsx</files>
  <read_first>
    - src/components/dashboard-demo/DashboardDemoShell.tsx
    - src/app/dashboard-demo/page.tsx
    - .planning/phases/02-dashboard-demo-ui-ux/02-CONTEXT.md
    - .planning/phases/02-dashboard-demo-ui-ux/02-DISCUSSION-LOG.md
    - src/components/timeline/TimelinePanel.tsx
    - src/components/stkde/DashboardStkdePanel.tsx
  </read_first>
  <action>
    Remove the persistent route label and any explanatory viewport copy, convert the map/cube switch into a small icon-only utility toggle with explicit accessible labels, keep the swap as a subtle fade, and strip the bottom status chip down to applied-state messaging only. Preserve the map-first shared viewport, the timeline rail, and the fixed right STKDE sidebar; do not introduce extra chrome or a second dashboard shell.
  </action>
  <acceptance_criteria>
    - `DashboardDemoShell.tsx` no longer contains the visible labels `Map-first shared viewport`, `2D map`, or `3D cube`.
    - `DashboardDemoShell.tsx` no longer references `generationStatus` or `lastGeneratedMetadata` in the persistent shell chrome.
    - The viewport toggle remains present and uses icon-only controls with accessible labels.
  </acceptance_criteria>
  <verify>grep -n "Map-first shared viewport\|2D map\|3D cube\|generationStatus\|lastGeneratedMetadata" src/components/dashboard-demo/DashboardDemoShell.tsx</verify>
  <done>The demo shell reads as a low-density map-first product surface with minimal persistent chrome.</done>
</task>

<task type="auto">
  <name>Task 2: Pin route-isolation regressions in the shell test</name>
  <files>src/app/dashboard-demo/page.shell.test.tsx</files>
  <read_first>
    - src/app/dashboard-demo/page.shell.test.tsx
    - src/app/dashboard/page.tsx
    - src/app/timeslicing/page.tsx
    - src/components/dashboard-demo/DashboardDemoShell.tsx
    - src/app/timeslicing/components/TimeslicingWorkflowShell.tsx
  </read_first>
  <action>
    Expand the source-inspection test so it proves three things: `/dashboard-demo` still routes through `DashboardDemoShell`, `/dashboard` still uses the Phase 1 dashboard composition, and `/timeslicing` still uses its own workflow shell. Add negative checks so the demo chrome strings do not leak into the stable routes and the stable route imports do not regress into the demo shell.
  </action>
  <acceptance_criteria>
    - `page.shell.test.tsx` asserts `DashboardDemoShell`, `DashboardLayout`, and `TimeslicingWorkflowShell` in the right places.
    - `page.shell.test.tsx` includes negative checks for `Map-first shared viewport` leaking into stable route files.
    - The test file can be run directly with Vitest and passes once the shell work is complete.
  </acceptance_criteria>
  <verify>pnpm exec vitest run src/app/dashboard-demo/page.shell.test.tsx --reporter=dot</verify>
  <done>The demo route has regression coverage that protects the stable dashboard and timeslicing shells.</done>
</task>

<task type="auto">
  <name>Task 3: Add the nested workflow drawer and stage scaffold</name>
  <files>src/components/dashboard-demo/WorkflowSkeleton.tsx, src/components/dashboard-demo/DashboardDemoShell.tsx</files>
  <read_first>
    - src/components/dashboard-demo/DashboardDemoShell.tsx
    - src/app/timeslicing/components/TimeslicingWorkflowShell.tsx
    - .planning/phases/02-dashboard-demo-ui-ux/02-CONTEXT.md
    - .planning/phases/02-dashboard-demo-ui-ux/02-DISCUSSION-LOG.md
    - src/components/map/MapVisualization.tsx
    - src/components/stkde/DashboardStkdePanel.tsx
  </read_first>
  <action>
    Create a compact left-anchored `WorkflowSkeleton` drawer that opens by default, collapses to a slim rail, and presents three visible stages: `Explore`, `Build`, and `Review`. Make `Explore` the initial orientation step, `Build` the continuous slice-building and review surface, and `Review` the final handoff gate. Keep the stepper form-like with previous/current/next awareness, placeholder panels, and muted inactive context; integrate it into `DashboardDemoShell` so it stays visually subordinate to the fixed right STKDE rail.
  </action>
  <acceptance_criteria>
    - `WorkflowSkeleton.tsx` exports a component with the labels `Explore`, `Build`, and `Review`.
    - The drawer has a collapsed rail state and an open drawer state.
    - `DashboardDemoShell.tsx` renders `WorkflowSkeleton` inside the demo shell.
  </acceptance_criteria>
  <verify>grep -n "Explore\|Build\|Review\|WorkflowSkeleton\|drawer\|slim rail" src/components/dashboard-demo/WorkflowSkeleton.tsx src/components/dashboard-demo/DashboardDemoShell.tsx</verify>
  <done>The demo shell contains a nested left drawer that reads like a multistep form and stays secondary to the main viewport.</done>
</task>

</tasks>

<verification>
Confirm the demo still reads as a single cohesive shell, the stable `/dashboard` and `/timeslicing` routes remain isolated, and the nested workflow drawer behaves like a compact form-style scaffold.
</verification>

<success_criteria>
The phase is complete when `/dashboard-demo` feels like a polished map-first product surface with a nested workflow scaffold, and the route regressions prove the stable shell routes did not absorb the demo chrome.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-demo-ui-ux/02-05-SUMMARY.md`
</output>
