---
phase: 25-adaptive-intervals-burstiness
plan: 04
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - src/components/timeline/TimelineContainer.tsx
  - src/components/timeline/AdaptiveControls.tsx
autonomous: true
must_haves:
  truths:
    - "AdaptiveControls slider is visible in the UI"
    - "Slider interaction updates the warp factor"
  artifacts:
    - path: "src/components/timeline/AdaptiveControls.tsx"
      provides: "Warp Slider Component"
    - path: "src/components/timeline/TimelineContainer.tsx"
      provides: "Controls Overlay Container"
  key_links:
    - from: "src/components/timeline/AdaptiveControls.tsx"
      to: "src/store/useAdaptiveStore.ts"
      via: "useAdaptiveStore warpFactor"
---

<objective>
Fix visibility of the AdaptiveControls slider and ensure DensityTrack is verifiable.

Purpose: Close the gap where the warp slider is reported invisible by the user.
Output: Visible and functional Adaptive Controls UI.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-adaptive-intervals-burstiness/25-03-SUMMARY.md
@.planning/phases/25-adaptive-intervals-burstiness/25-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix AdaptiveControls Visibility</name>
  <files>src/components/timeline/TimelineContainer.tsx</files>
  <action>
    Inspect `src/components/timeline/TimelineContainer.tsx` and `src/components/timeline/AdaptiveControls.tsx`.
    Identify why the slider is hidden (check `z-index`, `position: absolute`, `overflow: hidden` on parent).
    Adjust CSS classes to ensure `AdaptiveControls` appears above the timeline canvas/SVG.
    Ensure it is not conditionally hidden incorrectly.
  </action>
  <verify>
    Inspect the rendered CSS or check z-index values.
    `grep "z-" src/components/timeline/TimelineContainer.tsx` to confirm high z-index.
  </verify>
  <done>AdaptiveControls has correct positioning and z-index to be visible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>AdaptiveControls Visibility Fix</what-built>
  <how-to-verify>
    1. Open the application.
    2. Look for the "Warp Factor" slider (likely top-left of timeline).
    3. Confirm it is visible and draggable.
    4. Confirm dragging it updates the "Warp Factor" value.
    5. Also verify the Density Heatmap (colored strip) is visible above the timeline.
  </how-to-verify>
  <resume-signal>Type "approved" if slider is visible and functional.</resume-signal>
</task>

</tasks>

<verification>
Ensure the slider is visible and controls the warp factor.
</verification>

<success_criteria>
- [ ] AdaptiveControls slider is visible.
- [ ] Warp Factor state updates on drag.
</success_criteria>

<output>
After completion, create `.planning/phases/25-adaptive-intervals-burstiness/25-04-SUMMARY.md`
</output>
