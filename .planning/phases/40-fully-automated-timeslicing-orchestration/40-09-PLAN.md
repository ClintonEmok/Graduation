---
phase: 40-fully-automated-timeslicing-orchestration
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/timeline/DualTimeline.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Warp slices from auto proposal show on timeline"
    - "Both manual and suggestion source warps render"
  artifacts:
    - path: "src/components/timeline/DualTimeline.tsx"
      provides: "Render warp slices with source 'suggestion'"
---

<objective>
Fix warp overlay to show suggestion-sourced warp slices.

Purpose: When user accepts a full-auto package, the warp slices have source: 'suggestion', but DualTimeline only renders source: 'manual'. Need to include 'suggestion' source too.
Output: DualTimeline shows all enabled warp slices regardless of source.
</objective>

<context>
@src/components/timeline/DualTimeline.tsx
</context>

<tasks>

<task type="auto">
  <name>Fix warp overlay to include suggestion source</name>
  <files>src/components/timeline/DualTimeline.tsx</files>
  <action>
    - Find the warp overlay filter (around line 868)
    - Change: slice.source === 'manual'
    - To: slice.source === 'manual' || slice.source === 'suggestion'
    - Or more simply: slice.enabled (to show all enabled warps)
  </action>
  <verify>Warp overlay shows after accepting auto proposal</verify>
  <done>Suggestion-sourced warp slices render on timeline</done>
</task>

</tasks>

<verification>
- Accept a full-auto package
- Refresh page
- Warp overlay should be visible on timeline
</verification>

<success_criteria>
- Both manual and auto-proposal warps display
- User sees warp effect after accepting package
</success_criteria>

<output>
After completion, create `.planning/phases/40-fully-automated-timeslicing-orchestration/40-09-SUMMARY.md`
</output>
