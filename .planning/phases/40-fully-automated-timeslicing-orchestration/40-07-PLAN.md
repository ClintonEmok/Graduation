---
phase: 40-fully-automated-timeslicing-orchestration
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/timeslicing/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "No orange burst slices on timeslicing page"
    - "Only user-created time slices shown"
  artifacts:
    - path: "src/app/timeslicing/page.tsx"
      provides: "Removed useAutoBurstSlices call"
---

<objective>
Disable auto burst slices on timeslicing route.

Purpose: The timeslicing page focuses on user-created time slices and warp profiles, not automatic burst detection. Burst slices should not appear.
Output: Remove useAutoBurstSlices from timeslicing page.
</objective>

<context>
@src/app/timeslicing/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Remove burst slice auto-creation from timeslicing</name>
  <files>src/app/timeslicing/page.tsx</files>
  <action>
    - Find where useAutoBurstSlices is imported or used in timeslicing/page.tsx
    - Remove the import if only used for burst slices
    - Remove the useAutoBurstSlices(burstWindows) call
    - The page should NOT auto-create burst slices
  </action>
  <verify>No orange burst slices appear on timeslicing page</verify>
  <done>Auto burst slices disabled on timeslicing route</done>
</task>

</tasks>

<verification>
- Refresh timeslicing page - no orange burst slices visible
- Only user-created time slices should appear
</verification>

<success_criteria>
- Timeslicing page shows only user slices
- Focus remains on warp profiles and suggestion workflow
</success_criteria>

<output>
After completion, create `.planning/phases/40-fully-automated-timeslicing-orchestration/40-07-SUMMARY.md`
</output>
