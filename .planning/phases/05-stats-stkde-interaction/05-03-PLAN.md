---
phase: 05-stats-stkde-interaction
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/app/dashboard-demo/page.shell.test.tsx
  - src/app/api/stkde/hotspots/route.test.ts
autonomous: true
requirements_addressed:
  - STAT-01
  - STAT-02
  - STAT-03
  - STAT-04
  - STAT-05
  - STAT-06
must_haves:
  truths:
    - "Source-inspection tests lock the demo rail to Stats-first behavior."
    - "Source-inspection tests prove the stats panel still uses district-first copy and friendly place names."
    - "Route tests prove STKDE accepts district filters in sampled and full-population paths."
    - "The verification coverage breaks if the one-way stats-to-STKDE contract regresses."
  artifacts:
    - path: "src/app/dashboard-demo/page.shell.test.tsx"
      provides: "Source-inspection coverage for the demo rail and stats panel"
    - path: "src/app/api/stkde/hotspots/route.test.ts"
      provides: "API regression coverage for district-filtered STKDE requests"
  key_links:
    - from: "src/app/dashboard-demo/page.shell.test.tsx"
      to: "src/components/dashboard-demo/DashboardDemoRailTabs.tsx|src/components/dashboard-demo/DemoStatsPanel.tsx"
      via: "inspect source for Stats-first default tab and district-first copy"
      pattern: 'defaultValue="stats"|getDistrictDisplayName|Spatial distribution'
    - from: "src/app/api/stkde/hotspots/route.test.ts"
      to: "src/app/api/stkde/hotspots/route.ts"
      via: "assert districts move through sampled and full-population request paths"
      pattern: "districts|callerIntent: 'stkde'"
---

<objective>
Lock the phase 5 interaction contract with regression tests so the stats-first rail and district-filtered STKDE path cannot drift unnoticed.

Purpose: the plan is not done until the important behaviors are pinned down in tests.
Output: updated source-inspection coverage for the demo shell and API tests that prove district filters reach STKDE.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-stats-stkde-interaction/05-01-PLAN.md
@.planning/phases/05-stats-stkde-interaction/05-02-PLAN.md
@.planning/phases/05-stats-stkde-interaction/05-CONTEXT.md
@.planning/phases/05-stats-stkde-interaction/05-RESEARCH.md
@src/app/dashboard-demo/page.shell.test.tsx
@src/app/api/stkde/hotspots/route.test.ts
@src/components/dashboard-demo/DashboardDemoRailTabs.tsx
@src/components/dashboard-demo/DemoStatsPanel.tsx
@src/components/dashboard-demo/lib/useDemoStkde.ts
@src/lib/stkde/contracts.ts
@src/lib/stkde/full-population-pipeline.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock Stats-first demo shell behavior in the source-inspection test</name>
  <files>src/app/dashboard-demo/page.shell.test.tsx</files>
  <read_first>
    - src/app/dashboard-demo/page.shell.test.tsx
    - src/components/dashboard-demo/DashboardDemoRailTabs.tsx
    - src/components/dashboard-demo/DemoStatsPanel.tsx
    - src/components/dashboard-demo/lib/useDemoStkde.ts
  </read_first>
  <action>
    Extend the demo shell inspection so it explicitly expects the Stats-first default tab, the district-first stats copy, and the plain-language spatial distribution label. Keep the existing isolation assertions for the stable /dashboard and /timeslicing shells intact.
  </action>
  <acceptance_criteria>
    - page.shell.test.tsx contains defaultValue="stats".
    - page.shell.test.tsx contains getDistrictDisplayName.
    - page.shell.test.tsx contains Spatial distribution.
  </acceptance_criteria>
  <verify>grep -n 'defaultValue="stats"\|getDistrictDisplayName\|Spatial distribution' src/app/dashboard-demo/page.shell.test.tsx</verify>
  <done>The shell regression test fails if the Stats-first demo rail or district-first stats wording regresses.</done>
</task>

<task type="auto">
  <name>Task 2: Prove district filters reach STKDE through the API tests</name>
  <files>src/app/api/stkde/hotspots/route.test.ts</files>
  <read_first>
    - src/app/api/stkde/hotspots/route.test.ts
    - src/app/api/stkde/hotspots/route.ts
    - src/lib/stkde/full-population-pipeline.ts
    - src/lib/stkde/contracts.ts
  </read_first>
  <action>
    Add assertions for a sampled request that includes districts and for a full-population request that keeps the same districts in the normalized request object. Preserve the existing guardrail and fallback tests so this file still checks compute mode behavior while also locking the district filter path.
  </action>
  <acceptance_criteria>
    - route.test.ts contains an assertion that queryCrimesInRange receives districts.
    - route.test.ts contains an assertion that buildFullPopulationStkdeInputs receives a request with districts.
    - route.test.ts still covers the existing full-population fallback cases.
  </acceptance_criteria>
  <verify>grep -n 'districts\|buildFullPopulationStkdeInputs\|queryCrimesInRange' src/app/api/stkde/hotspots/route.test.ts</verify>
  <done>The API tests break if STKDE stops honoring district filters or regresses its compute-mode behavior.</done>
</task>

</tasks>

<verification>
Run the demo shell source-inspection test and the STKDE route test together to confirm the phase 5 contract stays locked.
</verification>

<success_criteria>
The demo shell remains Stats-first, the stats panel still reads in friendly district language, and the STKDE API tests prove district filters flow through both compute paths.
</success_criteria>

<output>
After completion, create `.planning/phases/05-stats-stkde-interaction/05-03-SUMMARY.md`
</output>
