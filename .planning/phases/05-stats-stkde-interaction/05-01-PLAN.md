---
phase: 05-stats-stkde-interaction
plan: 01
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - src/components/dashboard-demo/DashboardDemoRailTabs.tsx
  - src/components/dashboard-demo/DemoStatsPanel.tsx
autonomous: true
requirements_addressed:
  - STAT-01
  - STAT-02
  - STAT-04
  - STAT-05
must_haves:
  truths:
    - "The demo rail opens on the Stats tab instead of STKDE."
    - "The stats panel reads like the entry surface for district-based analysis."
    - "Selected districts are rendered with friendly place names."
    - "The stats surface still includes the spatial distribution view."
  artifacts:
    - path: "src/components/dashboard-demo/DashboardDemoRailTabs.tsx"
      provides: "Stats-first default tab boundary for the demo rail"
    - path: "src/components/dashboard-demo/DemoStatsPanel.tsx"
      provides: "District-first stats entry surface and spatial summary"
  key_links:
    - from: "src/components/dashboard-demo/DashboardDemoRailTabs.tsx"
      to: "Tabs defaultValue=\"stats\""
      via: "make Stats the first active tab"
      pattern: "defaultValue=\"stats\""
    - from: "src/components/dashboard-demo/DemoStatsPanel.tsx"
      to: "src/lib/category-maps.ts"
      via: "district ids rendered with getDistrictDisplayName"
      pattern: "getDistrictDisplayName\\("
    - from: "src/components/dashboard-demo/DemoStatsPanel.tsx"
      to: "useDemoNeighborhoodStats"
      via: "summary metrics and district filters remain shared"
      pattern: "useDemoNeighborhoodStats"
---

<objective>
Make the demo stats tab the default entry point and reframe the panel around district-first analysis language.

Purpose: the demo should greet users with the most approachable analysis surface before they move into hotspot investigation.
Output: a stats-first demo rail and a stats panel that uses human-readable district names and keeps the spatial distribution visible.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-stats-stkde-interaction/05-CONTEXT.md
@.planning/phases/05-stats-stkde-interaction/05-RESEARCH.md
@src/components/dashboard-demo/DemoStatsPanel.tsx
@src/components/dashboard-demo/DashboardDemoRailTabs.tsx
@src/components/dashboard-demo/lib/useDemoNeighborhoodStats.ts
@src/app/stats/lib/stats-view-model.ts
@src/lib/category-maps.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make Stats the default demo tab</name>
  <files>src/components/dashboard-demo/DashboardDemoRailTabs.tsx</files>
  <read_first>
    - src/components/dashboard-demo/DashboardDemoRailTabs.tsx
    - src/app/dashboard-demo/page.shell.test.tsx
  </read_first>
  <action>
    Change the demo rail Tabs defaultValue from "stkde" to "stats". Keep the tab order as Stats, STKDE, Slices, and do not introduce any new shell state or layout changes.
  </action>
  <acceptance_criteria>
    - DashboardDemoRailTabs.tsx contains defaultValue="stats".
    - The tab labels still read Stats, STKDE, and Slices in that order.
  </acceptance_criteria>
  <verify>grep -n 'defaultValue="stats"\|TabsTrigger value="stats"\|TabsTrigger value="stkde"\|TabsTrigger value="slices"' src/components/dashboard-demo/DashboardDemoRailTabs.tsx</verify>
  <done>The demo rail opens on Stats by default.</done>
</task>

<task type="auto">
  <name>Task 2: Reframe the stats panel around district-first language</name>
  <files>src/components/dashboard-demo/DemoStatsPanel.tsx</files>
  <read_first>
    - src/components/dashboard-demo/DemoStatsPanel.tsx
    - src/components/dashboard-demo/lib/useDemoNeighborhoodStats.ts
    - src/app/stats/lib/stats-view-model.ts
    - src/lib/category-maps.ts
  </read_first>
  <action>
    Rewrite the demo stats panel copy so it reads like the entry surface for district-based analysis. Render selected districts with getDistrictDisplayName, keep the summary cards and hourly pulse intact, and add an explicit spatial distribution section label so the hotspot/map piece stays visible inside the stats experience.
  </action>
  <acceptance_criteria>
    - DemoStatsPanel.tsx contains getDistrictDisplayName.
    - DemoStatsPanel.tsx contains the string Spatial distribution.
    - DemoStatsPanel.tsx still contains Hourly pulse.
  </acceptance_criteria>
  <verify>grep -n 'getDistrictDisplayName\|Spatial distribution\|Hourly pulse' src/components/dashboard-demo/DemoStatsPanel.tsx</verify>
  <done>The stats panel uses plain-language district names and stays readable as the demo entry surface.</done>
</task>

</tasks>

<verification>
Confirm Stats is the first demo rail tab and the stats panel uses human-readable district framing without losing the spatial distribution summary.
</verification>

<success_criteria>
Users land on Stats first, can read district-based summaries in plain language, and still see the spatial distribution context inside the demo stats panel.
</success_criteria>

<output>
After completion, create `.planning/phases/05-stats-stkde-interaction/05-01-SUMMARY.md`
</output>
