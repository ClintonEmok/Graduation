---
phase: 05-stats-stkde-interaction
plan: 02
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - src/lib/stkde/contracts.ts
  - src/lib/stkde/full-population-pipeline.ts
  - src/app/api/stkde/hotspots/route.ts
  - src/components/dashboard-demo/lib/useDemoStkde.ts
autonomous: true
requirements_addressed:
  - STAT-03
  - STAT-06
must_haves:
  truths:
    - "Selecting districts in stats narrows the STKDE request."
    - "The STKDE backend honors district filters in both sampled and full-population paths."
    - "The STKDE rail keeps hotspot selection local and never writes back into stats state."
    - "The STKDE summary speaks in friendly district names instead of raw codes."
  artifacts:
    - path: "src/lib/stkde/contracts.ts"
      provides: "STKDE request contract that accepts district filters"
    - path: "src/lib/stkde/full-population-pipeline.ts"
      provides: "Full-population STKDE SQL path that filters by district"
    - path: "src/app/api/stkde/hotspots/route.ts"
      provides: "API route that forwards district filters into STKDE compute"
    - path: "src/components/dashboard-demo/lib/useDemoStkde.ts"
      provides: "Demo hook that pads districts, sends the request, and formats the summary"
  key_links:
    - from: "src/components/dashboard-demo/lib/useDemoStkde.ts"
      to: "src/lib/stkde/contracts.ts"
      via: "selected districts are padded and placed into request.filters.districts"
      pattern: "padDistrict|districts: paddedDistricts"
    - from: "src/app/api/stkde/hotspots/route.ts"
      to: "queryCrimesInRange|buildFullPopulationStkdeInputs"
      via: "district filters flow through both sampled and full-population compute paths"
      pattern: "districts"
    - from: "src/lib/stkde/full-population-pipeline.ts"
      to: "\"District\" SQL column"
      via: "districts are appended to the WHERE clause"
      pattern: '"District" IN '
---

<objective>
Carry the selected demo districts all the way into STKDE so the hotspot surface truly follows the stats selection.

Purpose: the demo interaction only works if a district click actually changes what STKDE computes.
Output: a district-aware STKDE request contract, backend filter path, and demo hook that preserves one-way stats-to-STKDE flow.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-demo-stats-stkde/04-01-SUMMARY.md
@.planning/phases/05-stats-stkde-interaction/05-CONTEXT.md
@src/components/dashboard-demo/lib/useDemoStkde.ts
@src/lib/stkde/contracts.ts
@src/lib/stkde/full-population-pipeline.ts
@src/app/api/stkde/hotspots/route.ts
@src/lib/queries/filters.ts
@src/lib/queries/types.ts
@src/lib/stats/aggregation.ts
@src/lib/category-maps.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add district filters to the STKDE contract and backend</name>
  <files>src/lib/stkde/contracts.ts, src/lib/stkde/full-population-pipeline.ts, src/app/api/stkde/hotspots/route.ts</files>
  <read_first>
    - src/lib/stkde/contracts.ts
    - src/lib/stkde/full-population-pipeline.ts
    - src/app/api/stkde/hotspots/route.ts
    - src/lib/queries/filters.ts
    - src/lib/queries/types.ts
  </read_first>
  <action>
    Add districts?: string[] to StkdeRequest.filters, keep the existing crimeTypes and bbox fields intact, and thread the new districts array through both STKDE compute paths. Sampled mode should pass districts into queryCrimesInRange; full-population mode should append the "District" filter to the SQL WHERE clause using parameter placeholders, not string interpolation.
  </action>
  <acceptance_criteria>
    - contracts.ts contains districts?: string[].
    - route.ts forwards districts into queryCrimesInRange.
    - full-population-pipeline.ts contains a "District" IN (...) clause.
  </acceptance_criteria>
  <verify>grep -n 'districts?: string\[]\|districts\b\|"District" IN' src/lib/stkde/contracts.ts src/lib/stkde/full-population-pipeline.ts src/app/api/stkde/hotspots/route.ts</verify>
  <done>The STKDE backend can now filter by district without changing the existing compute modes.</done>
</task>

<task type="auto">
  <name>Task 2: Send padded demo districts into STKDE and summarize them clearly</name>
  <files>src/components/dashboard-demo/lib/useDemoStkde.ts</files>
  <read_first>
    - src/components/dashboard-demo/lib/useDemoStkde.ts
    - src/lib/stats/aggregation.ts
    - src/lib/category-maps.ts
    - src/lib/stkde/contracts.ts
    - src/app/api/stkde/hotspots/route.ts
  </read_first>
  <action>
    Import padDistrict and getDistrictDisplayName. Convert selectedDistricts to padded district codes before building the STKDE request, include those codes in request.filters.districts only when at least one district is selected, and update summaryLabel so it appends friendly district names or "all districts". Keep hotspot click handlers mutating only the STKDE-side state (selected hotspot, spatial filter, temporal filter) and never the stats store.
  </action>
  <acceptance_criteria>
    - useDemoStkde.ts contains padDistrict and getDistrictDisplayName.
    - useDemoStkde.ts contains districts: paddedDistricts in the request body.
    - useDemoStkde.ts still does not call stats setters such as setSelectedDistricts or setTimeRange.
  </acceptance_criteria>
  <verify>grep -n 'padDistrict\|getDistrictDisplayName\|districts: paddedDistricts\|setSelectedDistricts\|setTimeRange' src/components/dashboard-demo/lib/useDemoStkde.ts</verify>
  <done>The demo hook turns the stats district selection into a real STKDE filter and keeps the interaction one-way.</done>
</task>

</tasks>

<verification>
Confirm the STKDE request contract accepts districts and the demo hook forwards padded district codes without adding any stats-side writes.
</verification>

<success_criteria>
Selecting a district in stats changes the STKDE request, and the STKDE summary speaks in plain-language district terms while hotspot clicks stay local to STKDE.
</success_criteria>

<output>
After completion, create `.planning/phases/05-stats-stkde-interaction/05-02-SUMMARY.md`
</output>
