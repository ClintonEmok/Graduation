---
phase: 03-adjacent-slice-comparison-burst-evolution
plan: 03
type: execute
wave: 2
depends_on: [03-01]
requirements_addressed: [BURST-01, BURST-02]
files_modified:
  - src/lib/stkde/burst-evolution.ts
  - src/components/viz/BurstEvolutionOverlay.tsx
  - src/components/viz/TimeSlices.tsx
  - src/components/viz/burst-evolution.phase3.test.tsx
autonomous: true
must_haves:
  truths:
    - "Burst lifecycle connectors span consecutive visible slice planes in the cube."
    - "The same burst window is visually linked across adjacent slices instead of being rendered as isolated markers."
    - "Hidden slices never produce burst connectors or extra overlay clutter."
  artifacts:
    - path: src/lib/stkde/burst-evolution.ts
      provides: "Burst lifecycle path helper"
      contains: "connectorSegments"
    - path: src/components/viz/BurstEvolutionOverlay.tsx
      provides: "Cube burst lifecycle overlay renderer"
      contains: "burstPath"
    - path: src/components/viz/TimeSlices.tsx
      provides: "Scene wiring for the burst evolution overlay"
      contains: "BurstEvolutionOverlay"
    - path: src/components/viz/burst-evolution.phase3.test.tsx
      provides: "Burst overlay regression coverage"
      contains: "connectorSegments"
  key_links:
    - from: src/store/useDashboardDemoCoordinationStore.ts
      to: src/lib/stkde/burst-evolution.ts
      via: "selectedBurstWindows"
      pattern: "selectedBurstWindows"
    - from: src/components/viz/TimeSlices.tsx
      to: src/components/viz/BurstEvolutionOverlay.tsx
      via: "scene overlay mount"
      pattern: "BurstEvolutionOverlay"
---

<objective>
Render the burst lifecycle across consecutive slice planes in the cube so burst continuity is visible, not implied.

Purpose: Phase 03 must let users see how a burst flows through adjacent slices, while preserving the existing slice interaction model from Phase 01/02.
Output: A burst evolution helper, a scene overlay for connector paths, and a regression test that locks the render contract.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/viz/TimeSlices.tsx
@src/components/viz/SlicePlane.tsx
@src/store/useDashboardDemoCoordinationStore.ts
@src/components/viz/BurstDetails.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a burst evolution helper and test</name>
  <files>src/lib/stkde/burst-evolution.ts, src/lib/stkde/burst-evolution.phase3.test.tsx</files>
  <read_first>src/lib/stkde/contracts.ts, src/components/viz/TimeSlices.tsx, src/components/timeline/DemoDualTimeline.tsx</read_first>
  <action>
    Create a pure helper that turns ordered visible slices plus the selected burst window(s) into connector segments, per-slice burst scores, and burst label metadata. Add a regression test that validates the path order, neutral fallback, and hidden-slice filtering.
  </action>
  <acceptance_criteria>
    - `burst-evolution.ts` exports a function that returns `connectorSegments`.
    - `burst-evolution.phase3.test.tsx` asserts a neutral fallback for missing input.
    - The helper output is deterministic for the same slice ordering.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Mount the burst lifecycle overlay in the cube</name>
  <files>src/components/viz/BurstEvolutionOverlay.tsx, src/components/viz/TimeSlices.tsx</files>
  <read_first>src/components/viz/TimeSlices.tsx, src/store/useDashboardDemoCoordinationStore.ts, src/lib/stkde/burst-evolution.ts</read_first>
  <action>
    Add `BurstEvolutionOverlay.tsx` and mount it inside `TimeSlices.tsx`. Pass in the visible slices, the selected burst windows, and the helper output so the overlay can draw connector paths above the existing slice planes. Do not alter drag, resize, or creation behavior.
  </action>
  <acceptance_criteria>
    - `TimeSlices.tsx` contains `BurstEvolutionOverlay`.
    - Hidden slices are filtered before burst connectors are rendered.
    - Existing `SlicePlane` interaction code remains untouched.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run the burst evolution overlay test, then inspect the scene mount in `TimeSlices.tsx`.
</verification>

<success_criteria>
- Burst continuity is visible across adjacent cube slices.
- Only visible slices contribute connector segments.
- The cube overlay remains non-invasive to slice editing.
</success_criteria>

<output>
After completion, create `.planning/phases/03-adjacent-slice-comparison-burst-evolution/03-03-SUMMARY.md`
</output>
