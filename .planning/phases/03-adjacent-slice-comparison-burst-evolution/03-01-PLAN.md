---
phase: 03-adjacent-slice-comparison-burst-evolution
plan: 01
type: tdd
wave: 1
depends_on: []
requirements_addressed: [EVOL-01, EVOL-02]
files_modified:
  - src/lib/stkde/adjacent-slice-comparison.ts
  - src/lib/stkde/adjacent-slice-comparison.phase3.test.ts
  - src/store/useDashboardDemoCoordinationStore.ts
  - src/store/useDashboardDemoCoordinationStore.test.ts
autonomous: true
must_haves:
  truths:
    - "Two adjacent slices can be selected as a stable comparison pair."
    - "Comparison output always includes count delta, density ratio, dominant type shift, and district overlap."
    - "Clearing or replacing one side of the pair never corrupts the other side."
  artifacts:
    - path: src/lib/stkde/adjacent-slice-comparison.ts
      provides: "Adjacent slice comparison summary helper"
      contains: "countDelta"
    - path: src/store/useDashboardDemoCoordinationStore.ts
      provides: "Two-slot comparison selection state"
      contains: "comparisonSliceIds"
    - path: src/lib/stkde/adjacent-slice-comparison.phase3.test.ts
      provides: "Comparison math regression coverage"
      contains: "densityRatio"
  key_links:
    - from: src/store/useDashboardDemoCoordinationStore.ts
      to: src/lib/stkde/adjacent-slice-comparison.ts
      via: "comparisonSliceIds -> comparison summary"
      pattern: "comparisonSliceIds"
    - from: src/lib/stkde/adjacent-slice-comparison.ts
      to: src/hooks/useSliceStats.ts
      via: "typeCounts and districtCounts"
      pattern: "typeCounts|districtCounts"
---

<objective>
Build the adjacent-slice comparison model and pair-selection state that the demo UI can consume.

Purpose: Phase 03 needs a deterministic comparison primitive before any comparison panel or burst evolution view can show a trustworthy diff.
Output: A pure comparison helper, stable two-slot comparison state, and a regression test that fails if the diff math or slot behavior regresses.
</objective>

<feature>
  <name>Adjacent slice comparison model</name>
  <files>src/lib/stkde/adjacent-slice-comparison.ts, src/lib/stkde/adjacent-slice-comparison.phase3.test.ts, src/store/useDashboardDemoCoordinationStore.ts, src/store/useDashboardDemoCoordinationStore.test.ts</files>
  <behavior>
    - Comparing two slice snapshots returns a deterministic summary with `countDelta`, `densityRatio`, `dominantTypeShift`, `districtOverlap`, and `hotspotDelta`.
    - Missing or identical inputs return a neutral comparison object instead of throwing or returning null.
    - The coordination store keeps exactly two comparison slots (`left` and `right`) and replaces the oldest slot when a third slice is chosen.
  </behavior>
  <implementation>
    Write the failing comparison test first, then implement `compareAdjacentSlices()` and the two-slot comparison actions on the dashboard-demo coordination store. Keep the helper pure and shape-stable so downstream UI can render it without special casing.
  </implementation>
</feature>

<verification>
1. Run `./node_modules/.bin/vitest src/lib/stkde/adjacent-slice-comparison.phase3.test.ts`.
2. Run `./node_modules/.bin/vitest src/store/useDashboardDemoCoordinationStore.test.ts`.
3. Grep the helper for `countDelta`, `densityRatio`, and `districtOverlap`.
</verification>

<success_criteria>
- Adjacent comparison output is deterministic for the same inputs.
- Comparison selection state always resolves to at most two slice ids.
- The helper can be consumed by UI code without extra transformation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-adjacent-slice-comparison-burst-evolution/03-01-SUMMARY.md`
</output>
