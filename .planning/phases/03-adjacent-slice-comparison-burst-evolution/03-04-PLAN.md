---
phase: 03-adjacent-slice-comparison-burst-evolution
plan: 04
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/timeline/lib/burst-score-series.ts
  - src/components/timeline/lib/burst-score-series.test.ts
  - src/components/timeline/BurstScoreRail.tsx
  - src/components/timeline/DualTimelineSurface.tsx
  - src/components/timeline/DemoDualTimeline.tsx
autonomous: true
must_haves:
  truths:
    - "Each visible slice can display a stable burst score in the timeline rail."
    - "The highest burst-score slice is visually emphasized without hiding lower-scoring slices."
    - "Slices without burst scores fall back to a neutral baseline instead of breaking the rail."
  artifacts:
    - path: src/components/timeline/lib/burst-score-series.ts
      provides: "Per-slice burst score series helper"
      contains: "burstScore"
    - path: src/components/timeline/BurstScoreRail.tsx
      provides: "Timeline burst score strip"
      contains: "BurstScoreRail"
    - path: src/components/timeline/DualTimelineSurface.tsx
      provides: "Rail rendering for burst score bars"
      contains: "burstScoreSeries"
    - path: src/components/timeline/burst-score-series.test.ts
      provides: "Burst score series regression coverage"
      contains: "neutral baseline"
  key_links:
    - from: src/components/timeline/DemoDualTimeline.tsx
      to: src/components/timeline/lib/burst-score-series.ts
      via: "visible slice geometries -> burst score series"
      pattern: "burstScoreSeries"
    - from: src/components/timeline/DualTimelineSurface.tsx
      to: src/components/timeline/BurstScoreRail.tsx
      via: "rail prop flow"
      pattern: "BurstScoreRail"
---

<objective>
Show per-slice burst intensity directly in the timeline rail so slice-to-slice change is readable at a glance.

Purpose: The phase goal needs a compact score strip that complements the comparison panel and cube overlay, making burst progression legible in the same rail the user already scans.
Output: A pure burst-score series helper, a rendered rail strip, and a regression test that locks the scoring contract.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/timeline/DemoDualTimeline.tsx
@src/components/timeline/DualTimelineSurface.tsx
@src/store/slice-domain/types.ts
@src/components/timeline/DemoTimelinePanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a burst-score series helper and test</name>
  <files>src/components/timeline/lib/burst-score-series.ts, src/components/timeline/lib/burst-score-series.test.ts</files>
  <action>
    Create a pure helper that accepts the visible slice geometries plus their optional burst scores and returns a stable ordered series with `{ id, label, score, normalizedScore, isActive, isBurst }`. Filter hidden slices upstream, keep the output deterministic, and fall back to a neutral `0` score when a slice has no burst score.
  </action>
  <verify>Run `./node_modules/.bin/vitest src/components/timeline/lib/burst-score-series.test.ts`.</verify>
  <done>The rail data contract is protected by a deterministic test for scored and unscored slices.</done>
</task>

<task type="auto">
  <name>Task 2: Render the burst score rail and wire it into the timeline</name>
  <files>src/components/timeline/BurstScoreRail.tsx, src/components/timeline/DualTimelineSurface.tsx, src/components/timeline/DemoDualTimeline.tsx</files>
  <action>
    Add a compact bar/line strip above the detail timeline that renders the helper output, highlights the active slice, and labels the strongest burst score. Thread the series from `DemoDualTimeline.tsx` into `DualTimelineSurface.tsx` without changing existing brush, cursor, or burst-window interactions.
  </action>
  <verify>Inspect `DemoDualTimeline.tsx` for `burstScoreSeries` flow and confirm `DualTimelineSurface.tsx` mounts `BurstScoreRail`.</verify>
  <done>The bottom rail now exposes burst intensity per slice as a readable trend instead of only discrete windows.</done>
</task>

</tasks>

<verification>
Run the burst score series test, then inspect the rail prop flow and the new score strip render path.
</verification>

<success_criteria>
- The timeline rail shows a stable burst score for each visible slice.
- The top-scoring slice is visually emphasized.
- Missing burst scores degrade to a neutral baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/03-adjacent-slice-comparison-burst-evolution/03-04-SUMMARY.md`
</output>
