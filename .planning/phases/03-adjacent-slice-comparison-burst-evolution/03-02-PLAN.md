---
phase: 03-adjacent-slice-comparison-burst-evolution
plan: 02
type: execute
wave: 2
depends_on: [03-01]
requirements_addressed: [EVOL-01, EVOL-02]
files_modified:
  - src/components/dashboard-demo/DemoComparisonPanel.tsx
  - src/components/dashboard-demo/DemoSlicePanel.tsx
  - src/components/dashboard-demo/DashboardDemoRailTabs.tsx
  - src/components/dashboard-demo/DemoComparisonPanel.test.tsx
autonomous: true
must_haves:
  truths:
    - "Users can open a side-by-side comparison panel for two chosen slices."
    - "The comparison panel shows the same diff metrics produced by the shared comparison helper."
    - "Users can assign or swap the left/right comparison slots from the slice panel without losing the active selection."
  artifacts:
    - path: src/components/dashboard-demo/DemoComparisonPanel.tsx
      provides: "Adjacent slice comparison UI"
      contains: "countDelta"
    - path: src/components/dashboard-demo/DemoSlicePanel.tsx
      provides: "Comparison slot controls on each slice card"
      contains: "comparisonSliceIds"
    - path: src/components/dashboard-demo/DashboardDemoRailTabs.tsx
      provides: "Rail tab entry for the comparison panel"
      contains: "Compare"
    - path: src/components/dashboard-demo/DemoComparisonPanel.test.tsx
      provides: "Comparison panel source contract coverage"
      contains: "densityRatio"
  key_links:
    - from: src/store/useDashboardDemoCoordinationStore.ts
      to: src/components/dashboard-demo/DemoComparisonPanel.tsx
      via: "comparisonSliceIds"
      pattern: "comparisonSliceIds"
    - from: src/lib/stkde/adjacent-slice-comparison.ts
      to: src/components/dashboard-demo/DemoComparisonPanel.tsx
      via: "summary helper"
      pattern: "compareAdjacentSlices"
---

<objective>
Render the adjacent-slice comparison surface in the dashboard-demo rail and let users pick the pair from the slice panel.

Purpose: The phase goal is user-visible comparison, so the rail needs an explicit comparison panel that reuses the shared comparison model instead of inventing a second diff path.
Output: A dedicated comparison panel, slice-level pair controls, rail tab wiring, and a regression test for the shell contract.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/store/useDashboardDemoCoordinationStore.ts
@src/store/useDashboardDemoCoordinationStore.test.ts
@src/hooks/useSliceStats.ts
@src/components/dashboard-demo/DemoSlicePanel.tsx
@src/components/dashboard-demo/DashboardDemoRailTabs.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the adjacent slice comparison panel</name>
  <files>src/components/dashboard-demo/DemoComparisonPanel.tsx</files>
  <read_first>src/components/dashboard-demo/DemoSlicePanel.tsx, src/store/useDashboardDemoCoordinationStore.ts, src/lib/stkde/adjacent-slice-comparison.ts, src/hooks/useSliceStats.ts</read_first>
  <action>
    Create `DemoComparisonPanel.tsx` as a client component that reads the left/right comparison slice ids from `useDashboardDemoCoordinationStore`, pulls slice composition with `useSliceStats(sliceId)`, and renders a side-by-side diff summary using `compareAdjacentSlices()`. Show `countDelta`, `densityRatio`, `dominantTypeShift`, `districtOverlap`, and `hotspotDelta`, plus an empty-state message when fewer than two slices are selected.
  </action>
  <acceptance_criteria>
    - `DemoComparisonPanel.tsx` contains the text `countDelta` and `densityRatio` in its rendering logic.
    - The component imports `compareAdjacentSlices` from `src/lib/stkde/adjacent-slice-comparison.ts`.
    - The component handles missing left/right ids without throwing.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add pair controls to the slice panel</name>
  <files>src/components/dashboard-demo/DemoSlicePanel.tsx</files>
  <read_first>src/components/dashboard-demo/DemoSlicePanel.tsx, src/store/useDashboardDemoCoordinationStore.ts</read_first>
  <action>
    Add explicit left/right comparison controls to each slice card in `DemoSlicePanel.tsx`. Use the coordination store actions from Plan 01 to assign the current slice to the left slot, assign it to the right slot, swap the pair, and clear the pair. Keep the existing slice edit / burst draft controls intact.
  </action>
  <acceptance_criteria>
    - `DemoSlicePanel.tsx` includes UI text for left/right comparison actions.
    - Clicking the comparison controls updates `comparisonSliceIds` in the coordination store.
    - Clearing the pair does not mutate unrelated slice metadata.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Surface the comparison rail tab and lock the contract</name>
  <files>src/components/dashboard-demo/DashboardDemoRailTabs.tsx, src/components/dashboard-demo/DemoComparisonPanel.test.tsx</files>
  <read_first>src/components/dashboard-demo/DashboardDemoRailTabs.tsx, src/components/dashboard-demo/DemoComparisonPanel.tsx, src/components/dashboard-demo/page.shell.test.tsx</read_first>
  <action>
    Add a new `Compare` rail tab in `DashboardDemoRailTabs.tsx` that renders `DemoComparisonPanel`. Create `DemoComparisonPanel.test.tsx` as a source-inspection test that verifies the rail exposes the new tab, the empty-state copy exists, and the comparison summary fields remain present.
  </action>
  <acceptance_criteria>
    - `DashboardDemoRailTabs.tsx` contains a `Compare` tab label.
    - `DemoComparisonPanel.test.tsx` references `countDelta` and `densityRatio`.
    - The comparison panel can be mounted without a selected pair.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run the comparison panel test, then source-check the rail tabs and slice panel controls.
</verification>

<success_criteria>
- Users can pick two slices and see a readable side-by-side comparison.
- The comparison panel uses the shared comparison helper and store state.
- The rail contains a dedicated Compare entry.
</success_criteria>

<output>
After completion, create `.planning/phases/03-adjacent-slice-comparison-burst-evolution/03-02-SUMMARY.md`
</output>
