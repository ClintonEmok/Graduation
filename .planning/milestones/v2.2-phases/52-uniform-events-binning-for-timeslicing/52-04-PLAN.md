---
phase: 52-uniform-events-binning-for-timeslicing
plan: 4
type: execute
wave: 3
depends_on: [52-03]
files_modified:
  - src/lib/queries/aggregations.ts
  - src/lib/queries.test.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Global adaptive cache-miss persistence executes with valid SQL column/value parity"
    - "Global adaptive cache insert builder always emits one VALUES placeholder per target column"
    - "Regression coverage fails immediately if insert SQL placeholder parity drifts"
  artifacts:
    - path: "src/lib/queries/aggregations.ts"
      provides: "Correct global adaptive cache insert SQL for 11 cache columns"
      contains: "VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)"
    - path: "src/lib/queries.test.ts"
      provides: "Deterministic parity assertion between insert columns and placeholders"
      contains: "buildGlobalAdaptiveCacheQueries"
  key_links:
    - from: "src/lib/queries.ts"
      to: "src/lib/queries/aggregations.ts"
      via: "persistQueries.insert.sql execution"
      pattern: "persistQueries\\.insert"
    - from: "src/lib/queries.test.ts"
      to: "src/lib/queries/aggregations.ts"
      via: "builder-level SQL parity regression assertion"
      pattern: "VALUES \\(\\?"
---

<objective>
Close the Phase 52 verification blocker by fixing malformed global adaptive cache insert SQL and locking parity with regression coverage.

Purpose: Cache-miss persistence must be reliable so global adaptive precompute can always return mode-aware maps without runtime SQL failures.
Output: Corrected insert SQL in aggregation builders plus regression tests that enforce target-column and placeholder parity.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-uniform-events-binning-for-timeslicing/52-uniform-events-binning-for-timeslicing-VERIFICATION.md
@.planning/phases/52-uniform-events-binning-for-timeslicing/52-03-SUMMARY.md
@src/lib/queries/aggregations.ts
@src/lib/queries.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix global adaptive cache insert placeholder parity</name>
  <files>src/lib/queries/aggregations.ts</files>
  <action>Update `buildGlobalAdaptiveCacheQueries().insert.sql` so the `VALUES` clause contains 11 placeholders for the 11 target columns (`cache_key` through `warp_json`). Keep column order unchanged to preserve existing parameter binding in `getOrCreateGlobalAdaptiveMaps`, and avoid broad query refactors beyond this blocker fix.</action>
  <verify>`npm run typecheck` passes and insert SQL string includes 11 placeholders.</verify>
  <done>Global adaptive cache insert SQL has exact column/value parity and no longer risks cache-miss persistence failure due to malformed SQL.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression guard for insert column/value parity</name>
  <files>src/lib/queries.test.ts</files>
  <action>Add a focused regression test for `buildGlobalAdaptiveCacheQueries()` that parses `insert.sql` and asserts the number of listed insert columns matches the number of `?` placeholders in `VALUES`. Keep the test deterministic and unit-level (no DuckDB runtime dependency) so it reliably catches future placeholder drift.</action>
  <verify>`npm test -- --run src/lib/queries.test.ts` passes with the new parity assertion.</verify>
  <done>Automated tests fail if global adaptive cache insert SQL ever regresses to mismatched column/value counts.</done>
</task>

</tasks>

<verification>
- Global adaptive cache insert SQL defines 11 insert columns and 11 `?` placeholders.
- Targeted query-layer test suite passes and includes the new placeholder-parity regression.
- Phase 52 verification blocker in `52-uniform-events-binning-for-timeslicing-VERIFICATION.md` is fully addressed.
</verification>

<success_criteria>
- Cache-miss path in `getOrCreateGlobalAdaptiveMaps` can execute `persistQueries.insert` without SQL placeholder mismatch errors.
- Regression coverage explicitly enforces insert column/value parity for global adaptive cache SQL assembly.
</success_criteria>

<output>
After completion, create `.planning/phases/52-uniform-events-binning-for-timeslicing/52-04-SUMMARY.md`
</output>
