---
phase: 52-uniform-events-binning-for-timeslicing
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/useAdaptiveStore.ts
  - src/workers/adaptiveTime.worker.ts
  - src/workers/adaptiveTime.worker.test.ts
  - src/store/useAdaptiveStore.test.ts
autonomous: true

must_haves:
  truths:
    - "Calling `computeMaps(timestamps, domain)` without options keeps uniform-time behavior by default"
    - "Adaptive worker returns `densityMap`, `countMap`, `burstinessMap`, and `warpMap` for both uniform-time and uniform-events modes"
    - "Uniform-events outputs remain finite and stable even when timestamps contain duplicates or zero-width bins"
  artifacts:
    - path: "src/store/useAdaptiveStore.ts"
      provides: "Mode-aware compute contract with parity-safe default"
      contains: "binningMode?: 'uniform-time' | 'uniform-events'"
    - path: "src/workers/adaptiveTime.worker.ts"
      provides: "Uniform-time and uniform-events binning implementations with stable output shape"
      contains: "countMap"
    - path: "src/workers/adaptiveTime.worker.test.ts"
      provides: "Regression coverage for mode default and finite uniform-events outputs"
      contains: "uniform-events"
  key_links:
    - from: "src/store/useAdaptiveStore.ts"
      to: "src/workers/adaptiveTime.worker.ts"
      via: "worker postMessage config payload"
      pattern: "config:.*binningMode"
---

<objective>
Introduce a backward-compatible adaptive binning contract and implement the uniform-events worker algorithm.

Purpose: This creates the foundational dual-mode behavior needed for timeslicing while preserving existing default behavior everywhere else.
Output: Mode-aware store-to-worker contract, uniform-events worker path with finite guards, and regression tests for defaults and count semantics.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-uniform-events-binning-for-timeslicing/52-RESEARCH.md
@.planning/phases/51-store-consolidation/51-12-SUMMARY.md
@src/store/useAdaptiveStore.ts
@src/workers/adaptiveTime.worker.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend adaptive store and worker request contract with binning mode defaults</name>
  <files>src/store/useAdaptiveStore.ts, src/workers/adaptiveTime.worker.ts</files>
  <action>Add `binningMode: 'uniform-time' | 'uniform-events'` to adaptive worker config and store-facing `computeMaps` options while keeping `computeMaps(timestamps, domain)` fully valid. Default missing mode to `uniform-time` in store/worker boundaries so timeline-test, timeline-test-3d, and MainScene call sites do not require immediate edits.</action>
  <verify>`npm run typecheck` passes with unchanged call sites still compiling.</verify>
  <done>Store and worker contracts accept optional mode, and legacy two-argument compute calls remain behavior-compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Implement uniform-events binning math with stable output shape and finite guards</name>
  <files>src/workers/adaptiveTime.worker.ts</files>
  <action>Implement equal-event-target boundaries from sorted timestamps for `uniform-events`, compute raw `countMap` per bin, and derive density as `count / binDuration` before normalization. Preserve existing output keys (`densityMap`, `countMap`, `burstinessMap`, `warpMap`) for both modes, enforce monotonic boundaries/epsilon widening, and guard all divide operations to avoid NaN/Infinity when bins collapse.</action>
  <verify>`npm run typecheck` passes and worker output lengths match `binCount` in both modes.</verify>
  <done>Worker produces parity-safe outputs for both modes, with `countMap` representing raw counts and density normalized from mode-appropriate inputs.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for default mode, uniform-events stability, and count semantics</name>
  <files>src/workers/adaptiveTime.worker.test.ts, src/store/useAdaptiveStore.test.ts</files>
  <action>Create focused Vitest coverage that proves: (1) omitted mode resolves to uniform-time, (2) uniform-events outputs stay finite with duplicate timestamps, (3) `countMap` tracks raw per-bin event counts instead of smoothed density. Keep tests deterministic with fixed timestamp fixtures and explicit expected lengths/value invariants.</action>
  <verify>`npm test -- --run src/workers/adaptiveTime.worker.test.ts src/store/useAdaptiveStore.test.ts` passes.</verify>
  <done>Automated tests fail if default mode drifts, finite guards regress, or countMap semantics change.</done>
</task>

</tasks>

<verification>
- Uniform-time remains the default when no mode override is provided.
- Worker emits finite `densityMap`, `countMap`, `burstinessMap`, and `warpMap` arrays for both modes.
- Regression tests lock raw-count `countMap` semantics and duplicate-timestamp safety.
</verification>

<success_criteria>
- Phase 52 criteria (2) and (3) are satisfied at the worker/store contract level.
</success_criteria>

<output>
After completion, create `.planning/phases/52-uniform-events-binning-for-timeslicing/52-01-SUMMARY.md`
</output>
