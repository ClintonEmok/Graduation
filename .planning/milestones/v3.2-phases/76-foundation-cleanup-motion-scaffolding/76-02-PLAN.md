---
phase: 76-foundation-cleanup-motion-scaffolding
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/useDashboardDemoCoordinationStore.ts
  - src/store/useDashboardDemoAdaptiveStore.ts
  - src/store/useDashboardDemoWarpStore.ts
  - src/store/useDashboardDemoAnalysisStore.ts
  - src/store/useDashboardDemoTimeslicingModeStore.ts
  - src/store/useDashboardDemoTimeslicingModeStore.test.ts
  - src/components/dashboard-demo/DemoMapVisualization.tsx
  - src/components/dashboard-demo/DemoStkdePanel.tsx
  - src/components/dashboard-demo/DemoConfigurePanel.tsx
  - src/components/dashboard-demo/DemoSlicePanel.tsx
  - src/components/dashboard-demo/DemoStatsPanel.tsx
  - src/components/dashboard-demo/DemoStatsMapOverlay.tsx
  - src/components/timeline/DemoDualTimeline.tsx
  - src/components/dashboard-demo/DemoTimelineSettingsCard.tsx
  - src/components/viz/TimeSlices.tsx
  - src/components/viz/MainScene.tsx
autonomous: true

must_haves:
  truths:
    - "All cross-view controls synchronize through a single coordination store"
    - "Active slice index, filters, and selections do not drift between views"
    - "STKDE params, burst threshold, and warp state are accessible from coordinationStore"
    - "Existing dashboard-demo functionality works identically after consolidation"
  artifacts:
    - path: "src/store/useDashboardDemoCoordinationStore.ts"
      provides: "Single source of truth for all cross-view demo state"
      min_lines: 500
    - path: "src/store/useDashboardDemoAdaptiveStore.ts"
      provides: "DELETED — burstThreshold merged into coordinationStore"
    - path: "src/store/useDashboardDemoWarpStore.ts"
      provides: "DELETED — all warp fields merged into coordinationStore"
    - path: "src/store/useDashboardDemoAnalysisStore.ts"
      provides: "DELETED — analysis fields merged into coordinationStore"
  key_links:
    - from: "src/components/dashboard-demo/DemoMapVisualization.tsx"
      to: "useDashboardDemoCoordinationStore"
      via: "zustand selector for burstThreshold, stkdeResponse"
      pattern: "useDashboardDemoCoordinationStore\\(.*burstThreshold"
    - from: "src/components/dashboard-demo/DemoConfigurePanel.tsx"
      to: "useDashboardDemoCoordinationStore"
      via: "zustand selector for warp state, analysis params"
      pattern: "useDashboardDemoCoordinationStore\\(.*timeScaleMode"
    - from: "src/components/dashboard-demo/DemoTimeslicingModeStore.ts"
      to: "useDashboardDemoCoordinationStore"
      via: "getState() for setWarpSource"
      pattern: "useDashboardDemoCoordinationStore\\.getState\\(\\).*setWarpSource"
---

<objective>
Consolidate three drift-prone dashboard-demo stores (adaptiveStore, warpStore, analysisStore) into the central useDashboardDemoCoordinationStore. This eliminates cross-store synchronization drift and reduces the demo store count from 8 to 6.

Purpose: FND-04 — Ensure filters, active selections, STKDE params, warp state, and burst threshold stay synchronized across all views through a single coordination state. Phase 77-79 features depend on this store being the single source of truth.

Output: CoordinationStore grows from 22 to ~42 fields. Three store files deleted. Eleven consumer files updated.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-foundation-cleanup-motion-scaffolding/76-RESEARCH.md
@.planning/phases/76-foundation-cleanup-motion-scaffolding/76-CONTEXT.md
@.planning/codebase/STKDE_TEMPORAL_SCOPE.md

# Source files to read before editing:
@src/store/useDashboardDemoCoordinationStore.ts
@src/store/useDashboardDemoAdaptiveStore.ts
@src/store/useDashboardDemoWarpStore.ts
@src/store/useDashboardDemoAnalysisStore.ts
@src/store/useDashboardDemoTimeslicingModeStore.ts
@src/components/dashboard-demo/DemoMapVisualization.tsx
@src/components/dashboard-demo/DemoStkdePanel.tsx
@src/components/dashboard-demo/DemoConfigurePanel.tsx
@src/components/dashboard-demo/DemoSlicePanel.tsx
@src/components/dashboard-demo/DemoStatsPanel.tsx
@src/components/dashboard-demo/DemoStatsMapOverlay.tsx
@src/components/timeline/DemoDualTimeline.tsx
@src/components/dashboard-demo/DemoTimelineSettingsCard.tsx
@src/components/viz/TimeSlices.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge 3 stores into CoordinationStore</name>
  <files>
    src/store/useDashboardDemoCoordinationStore.ts,
    src/store/useDashboardDemoAdaptiveStore.ts,
    src/store/useDashboardDemoWarpStore.ts,
    src/store/useDashboardDemoAnalysisStore.ts
  </files>
  <action>
**Step 1: Read all four store files** before making any edits. Understand the full interface of each store.

**Step 2: Extend coordinationStore interface and state**

Add the following to `DashboardDemoCoordinationState` interface in `src/store/useDashboardDemoCoordinationStore.ts`:

**From AdaptiveStore (1 field, 2 actions):**
- `burstThreshold: number` (default 0.7)
- `setBurstThreshold: (value: number) => void` (clamps to 0-1)
- `resetBurstThreshold: () => void` (resets to 0.7)

**From WarpStore (8 fields, 7 actions):**
- Import types at top: `DemoWarpScaleMode`, `DemoWarpSource` from the warp store file (copy type exports before deleting)
- `timeScaleMode: DemoWarpScaleMode` (default `'linear'`)
- `warpSource: DemoWarpSource` (default `'density'`)
- `warpFactor: number` (default 0)
- `densityMap: Float32Array | null` (default null)
- `warpMap: Float32Array | null` (default null)
- `mapDomain: [number, number]` (default `[0, 100]`)
- `isComputing: boolean` (default false)
- `setTimeScaleMode`, `setWarpSource`, `setWarpFactor`, `setPrecomputedMaps`, `setIsComputing`, `resetWarp`

**From AnalysisStore (11 fields, 11 actions):**
- Import `StkdeParams`, `StkdeSpatialFilter`, `StkdeTemporalFilter` from `@/store/useStkdeStore`
- Import `StkdeResponse` from `@/lib/stkde/contracts`
- Export constants `ALL_DEMO_DISTRICTS` (copy from analysisStore)
- Export types `DemoStkdeScopeMode`, `DemoAnalysisTimeRange`
- `selectedDistricts: string[]` (default `[]`)
- `timeRange: DemoAnalysisTimeRange`
- `stkdeScopeMode: DemoStkdeScopeMode` (default `'applied-slices'`)
- `stkdeParams: StkdeParams` (with 6 defaults from analysisStore)
- `selectedHotspotId: string | null` (default null)
- `hoveredHotspotId: string | null` (default null)
- `spatialFilter: StkdeSpatialFilter | null` (default null)
- `temporalFilter: StkdeTemporalFilter | null` (default null)
- `stkdeResponse: StkdeResponse | null` (default null)
- All corresponding setters: `setSelectedDistricts`, `toggleDistrict`, `setTimeRange`, `setStkdeScopeMode`, `setStkdeParams`, `setSelectedHotspot`, `setHoveredHotspot`, `setSpatialFilter`, `setTemporalFilter`, `setStkdeResponse`, `resetAnalysis`

**Step 3: Copy implementations**

Copy the exact setter implementations from the source stores into the coordinationStore's `create((set) => ({...}))` object. Preserve all logic exactly as-is (e.g., `clampParam` function from analysisStore, `Math.min(1, Math.max(0, value))` from warpStore, `Math.max(0, Math.min(1, value))` from adaptiveStore).

Move the `clampParam` helper function into coordinationStore.ts (it's used by `setStkdeParams`).

**Step 4: Do NOT delete old store files yet**

Keep them for reference during Task 2 import migration. Move the type exports (`DemoWarpScaleMode`, `DemoWarpSource`, `DemoStkdeScopeMode`, `DemoAnalysisTimeRange`) into coordinationStore.ts so consumers can import types from one place. Export `ALL_DEMO_DISTRICTS` from coordinationStore.ts.
</action>
  <verify>TypeScript compiles coordinationStore.ts with 0 errors. `pnpm typecheck` on just this file confirms all types resolve correctly.</verify>
  <done>CoordinationStore interface and implementation include all fields from adaptiveStore (1 field), warpStore (8 fields), and analysisStore (11 fields). Types exported from coordinationStore. Old store files still exist (not yet deleted).</done>
</task>

<task type="auto">
  <name>Task 2: Update all consumer imports and delete old stores</name>
  <files>
    src/components/dashboard-demo/DemoMapVisualization.tsx,
    src/components/dashboard-demo/DemoStkdePanel.tsx,
    src/components/dashboard-demo/DemoConfigurePanel.tsx,
    src/components/dashboard-demo/DemoSlicePanel.tsx,
    src/components/dashboard-demo/DemoStatsPanel.tsx,
    src/components/dashboard-demo/DemoStatsMapOverlay.tsx,
    src/components/timeline/DemoDualTimeline.tsx,
    src/components/dashboard-demo/DemoTimelineSettingsCard.tsx,
    src/components/viz/TimeSlices.tsx,
    src/store/useDashboardDemoTimeslicingModeStore.ts,
    src/store/useDashboardDemoTimeslicingModeStore.test.ts,
    src/store/useDashboardDemoAdaptiveStore.ts,
    src/store/useDashboardDemoWarpStore.ts,
    src/store/useDashboardDemoAnalysisStore.ts
  </files>
  <action>
**Step 1: Update imports in every consumer file**

For each file below, replace old store imports with coordinationStore imports. Use find-and-replace patterns:

| Old Import | New Import |
|-----------|-----------|
| `import { useDashboardDemoAdaptiveStore } from '@/store/useDashboardDemoAdaptiveStore'` | Remove entirely — re-exported from coordinationStore |
| `import { useDashboardDemoWarpStore } from '@/store/useDashboardDemoWarpStore'` | Remove entirely |
| `import { useDashboardDemoAnalysisStore, ALL_DEMO_DISTRICTS } from '@/store/useDashboardDemoAnalysisStore'` | `import { useDashboardDemoCoordinationStore, ALL_DEMO_DISTRICTS } from '@/store/useDashboardDemoCoordinationStore'` |

**Consumer file mapping — exact changes:**

1. **DemoMapVisualization.tsx**: Replace `useDashboardDemoAnalysisStore` + `useDashboardDemoAdaptiveStore` imports. Change `useDashboardDemoAnalysisStore((state) => state.stkdeResponse)` → `useDashboardDemoCoordinationStore((s) => s.stkdeResponse)`. Change `useDashboardDemoAnalysisStore((state) => state.selectedHotspotId)` → `useDashboardDemoCoordinationStore((s) => s.selectedHotspotId)`. Replace `useDashboardDemoAdaptiveStore` reference (if any in current code, check actual usage).

2. **DemoStkdePanel.tsx**: Replace `useDashboardDemoAnalysisStore` → `useDashboardDemoCoordinationStore` for stkdeParams, selectedHotspotId, spatialFilter, temporalFilter, stkdeResponse, selectedDistricts, stkdeScopeMode, timeRange.

3. **DemoConfigurePanel.tsx**: Replace both `useDashboardDemoWarpStore` and `useDashboardDemoAnalysisStore` imports. Map all warp selectors (timeScaleMode, warpSource, warpFactor, isComputing) to coordinationStore. Map all analysis selectors (stkdeParams, stkdeScopeMode) to coordinationStore.

4. **DemoSlicePanel.tsx**: Replace `useDashboardDemoWarpStore` import. Map `timeScaleMode`, `warpFactor`, `warpSource` to coordinationStore.

5. **DemoStatsPanel.tsx**: Replace `ALL_DEMO_DISTRICTS` import source from analysisStore to coordinationStore. If `useDashboardDemoAnalysisStore` is used (check actual file), replace with coordinationStore.

6. **DemoStatsMapOverlay.tsx**: Replace `ALL_DEMO_DISTRICTS` and `useDashboardDemoAnalysisStore` import. Map analysis selectors to coordinationStore.

7. **DemoDualTimeline.tsx**: Replace `useDashboardDemoWarpStore` import. Map `timeScaleMode`, `warpFactor` to coordinationStore.

8. **DemoTimelineSettingsCard.tsx**: Replace `useDashboardDemoWarpStore` import. Map state to coordinationStore.

9. **TimeSlices.tsx**: Replace `useDashboardDemoAnalysisStore` import. Map analysis selectors to coordinationStore.

10. **DemoTimeslicingModeStore.ts**: Replace `import { useDashboardDemoWarpStore } from './useDashboardDemoWarpStore'` with `import { useDashboardDemoCoordinationStore } from './useDashboardDemoCoordinationStore'`. Change `useDashboardDemoWarpStore.getState().setWarpSource('density')` (lines ~406, ~424) to `useDashboardDemoCoordinationStore.getState().setWarpSource('density')`.

11. **DemoTimeslicingModeStore.test.ts**: Replace warpStore import with coordinationStore import. Change `expect(useDashboardDemoWarpStore.getState().warpSource).toBe('density')` (line ~363) to `expect(useDashboardDemoCoordinationStore.getState().warpSource).toBe('density')`.

**Step 2: Delete old store files**

After confirming all imports are migrated, delete these three files:
- `src/store/useDashboardDemoAdaptiveStore.ts`
- `src/store/useDashboardDemoWarpStore.ts`
- `src/store/useDashboardDemoAnalysisStore.ts`

**Step 3: Verify no stale references**

```bash
grep -r "useDashboardDemoAdaptiveStore\|useDashboardDemoWarpStore\|useDashboardDemoAnalysisStore" src/ --include="*.ts" --include="*.tsx"
```

Output must be empty (0 matches).

**Step 4: Run full typecheck**

```bash
pnpm typecheck
```

Must pass with 0 errors. If any consumer file was missed, fix the import and re-run.
</action>
  <verify>`pnpm typecheck` passes with 0 errors. `grep -r "useDashboardDemoAdaptiveStore\|useDashboardDemoWarpStore\|useDashboardDemoAnalysisStore" src/` returns 0 matches. Old store files are deleted. All 11 consumer files import from coordinationStore.</verify>
  <done>All 3 old stores deleted. All 11 consumer files reference useDashboardDemoCoordinationStore. TypeScript compiles clean. Dashboard-demo functionality unchanged — store count reduced from 8 to 6.</done>
</task>

</tasks>

<verification>
After Task 2 completion:
1. Run `pnpm typecheck` — must pass with 0 errors
2. Run `pnpm test` — must pass all existing tests (including DemoTimeslicingModeStore test)
3. `grep` for old store imports returns 0 matches
4. CoordinationStore file has >500 lines (was 290, now ~550 with merged fields)
5. `ls src/store/useDashboardDemo{Analysis,Adaptive,Warp}Store.ts` returns "No such file"
</verification>

<success_criteria>
- [ ] TypeScript compiles with 0 errors across all consumer files
- [ ] All existing tests pass (no regressions from store consolidation)
- [ ] No old store imports anywhere in src/
- [ ] Three old store files deleted
- [ ] CoordinationStore exports all types: DemoWarpScaleMode, DemoWarpSource, DemoStkdeScopeMode, DemoAnalysisTimeRange, ALL_DEMO_DISTRICTS
- [ ] Cross-view state no longer drifts — all views read/write one store
</success_criteria>

<output>
After completion, create `.planning/phases/76-foundation-cleanup-motion-scaffolding/76-02-SUMMARY.md`
</output>
