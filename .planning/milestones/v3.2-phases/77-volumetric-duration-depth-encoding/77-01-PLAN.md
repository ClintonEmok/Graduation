---
phase: 77-volumetric-duration-depth-encoding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/useDashboardDemoCoordinationStore.ts
  - src/store/useDashboardDemoCoordinationStore.test.ts
  - src/app/stkde-3d/lib/volume-encoding.ts
  - src/app/stkde-3d/lib/volume-encoding.test.ts
autonomous: true
requirements_addressed:
  - VOL-02
  - VOL-06
must_haves:
  truths:
    - "The demo coordination state can store and reset duration-volume scale, exaggeration, and normalization settings."
    - "Normalized duration values stay comparable across different slice counts and time-window spans."
    - "A pure helper can derive duration-to-volume profiles without React or widget coupling."
    - "The helper contract stays deterministic for empty, mixed, and repeated inputs."
  artifacts:
    - path: "src/store/useDashboardDemoCoordinationStore.ts"
      provides: "Shared demo volume settings state and actions"
    - path: "src/app/stkde-3d/lib/volume-encoding.ts"
      provides: "Pure duration normalization and volume profile helpers"
    - path: "src/store/useDashboardDemoCoordinationStore.test.ts"
      provides: "Store behavior regression coverage"
    - path: "src/app/stkde-3d/lib/volume-encoding.test.ts"
      provides: "Volume helper regression coverage"
  key_links:
    - from: "src/store/useDashboardDemoCoordinationStore.ts"
      to: "src/app/stkde-3d/components/StkdeSliceStack.tsx"
      via: "shared volume settings become the widget source of truth"
      pattern: "volume|exaggeration|normalization"
    - from: "src/app/stkde-3d/lib/volume-encoding.ts"
      to: "src/components/viz/Trajectory.tsx"
      via: "borrow the derived-thickness idea without copying the trajectory interaction model"
      pattern: "gap|thickness"
    - from: "src/app/stkde-3d/lib/volume-encoding.ts"
      to: "src/components/dashboard-demo/Demo3dSpatialView.tsx"
      via: "slice duration is normalized from ordered/count slices before rendering"
      pattern: "duration|normalize"
---

<objective>
Establish the duration-volume state and normalization foundation that the demo 3D STKDE widget needs before any rendering changes land.

Purpose: phase 77 needs a stable, testable magnitude model so longer durations can be encoded as depth without drifting across windows or slice counts.
Output: a shared volume-settings state path plus pure duration-normalization helpers.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/store/useDashboardDemoCoordinationStore.ts
@src/components/viz/Trajectory.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add duration-volume state to the shared demo coordination store</name>
  <files>src/store/useDashboardDemoCoordinationStore.ts, src/store/useDashboardDemoCoordinationStore.test.ts</files>
  <action>
    Add duration-volume settings to the consolidated dashboard-demo coordination state (scale, exaggeration, and normalization mode) plus explicit setters and a reset path. Keep the defaults conservative so the new encoding does not disturb the current widget layout, and keep the store as the single source of truth rather than adding a second coordination store.
  </action>
  <verify>pnpm exec vitest run src/store/useDashboardDemoCoordinationStore.test.ts</verify>
  <done>The demo store can now persist, update, and reset the volume settings that phase 77 needs.</done>
</task>

<task type="auto">
  <name>Task 2: Build pure duration-to-volume normalization helpers</name>
  <files>src/app/stkde-3d/lib/volume-encoding.ts, src/app/stkde-3d/lib/volume-encoding.test.ts</files>
  <action>
    Create a pure helper module that derives normalized duration ratios and render-ready volume metrics from a rendered slice set and the current volume settings. The helper should account for differing slice counts and visible time-window spans, and it should expose deterministic outputs that can drive extrusion, thickness, and falloff without depending on React or scene code.
  </action>
  <verify>pnpm exec vitest run src/app/stkde-3d/lib/volume-encoding.test.ts</verify>
  <done>Duration normalization can be computed independently of the widget, with stable results across empty and populated inputs.</done>
</task>

</tasks>

<verification>
The shared volume state and normalization helpers behave deterministically and are ready to be consumed by the 3D widget.
</verification>

<success_criteria>
Phase 77 can read duration-volume settings from one place and compute normalized depth values that stay comparable across slice windows.
</success_criteria>

<output>
After completion, create `.planning/phases/77-volumetric-duration-depth-encoding/77-01-SUMMARY.md`
</output>
