---
phase: 78-temporal-evolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/useDashboardDemoCoordinationStore.ts
  - src/store/useDashboardDemoCoordinationStore.test.ts
  - src/components/dashboard-demo/Demo3dSpatialView.tsx
  - src/app/stkde-3d/components/SliceScrubber.tsx
autonomous: true
requirements_addressed:
  - TME-01
  - TME-04
must_haves:
  truths:
    - "Playback advances activeSliceIndex through the ordered slice list inside the demo 3D widget."
    - "Dragging or changing slices pauses playback instead of fighting it."
    - "Playback speed is adjustable from the compact Inspect-panel control surface."
    - "The interpolation mode is opt-in, only available during playback, and the label reads Interpolated."
    - "The temporal controls stay compact and quiet rather than becoming a large tool strip."
  artifacts:
    - path: "src/store/useDashboardDemoCoordinationStore.ts"
      provides: "Shared temporal state for playback, scrubbing, interpolation gating, and trail controls"
    - path: "src/components/dashboard-demo/Demo3dSpatialView.tsx"
      provides: "Playback orchestration that steps activeSliceIndex through the ordered slice list"
    - path: "src/app/stkde-3d/components/SliceScrubber.tsx"
      provides: "Compact Inspect-panel control surface for playback speed, interpolation, and trails"
    - path: "src/store/useDashboardDemoCoordinationStore.test.ts"
      provides: "Regression coverage for temporal-state defaults, setters, and resets"
  key_links:
    - from: "src/store/useDashboardDemoCoordinationStore.ts"
      to: "src/components/dashboard-demo/Demo3dSpatialView.tsx"
      via: "shared activeSliceIndex, inspectIsPlaying, and inspectPlaybackSpeed drive the playback loop"
      pattern: "activeSliceIndex|inspectIsPlaying|inspectPlaybackSpeed"
    - from: "src/app/stkde-3d/components/SliceScrubber.tsx"
      to: "src/store/useDashboardDemoCoordinationStore.ts"
      via: "compact controls mutate shared temporal state instead of duplicating local state"
      pattern: "setInspectIsPlaying|setInspectPlaybackSpeed|set.*Interpolation|set.*Trail"
    - from: "src/components/dashboard-demo/Demo3dSpatialView.tsx"
      to: "src/app/stkde-3d/components/Stkde3DScene.tsx"
      via: "ordered slice list and activeSliceIndex remain the only 3D widget sequencing inputs"
      pattern: "activeSliceIndex|orderedSlices"
---

<objective>
Make the demo 3D widget’s temporal controls and playback orchestration work from the shared dashboard-demo coordination store without leaking motion to map/timeline surfaces.

Purpose: phase 78 needs a quiet, compact control plane before the visual stack can read interpolation and trail state.
Output: shared temporal state, playback sequencing, and compact Inspect-panel controls.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/STKDE_TEMPORAL_SCOPE.md
@src/store/useDashboardDemoCoordinationStore.ts
@src/components/dashboard-demo/Demo3dSpatialView.tsx
@src/app/stkde-3d/components/SliceScrubber.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add temporal playback state to the shared demo coordination store</name>
  <files>src/store/useDashboardDemoCoordinationStore.ts, src/store/useDashboardDemoCoordinationStore.test.ts</files>
  <action>
    Add shared state and actions for temporal playback, interpolation gating, and trail controls in the existing dashboard-demo coordination store. Keep the store as the sole source of truth for the demo 3D widget’s temporal controls, wire explicit reset paths, and keep the defaults conservative so nothing starts noisy or over-animated. Extend the existing store test coverage to assert the new defaults, setters, and resets.
  </action>
  <verify>pnpm exec vitest run src/store/useDashboardDemoCoordinationStore.test.ts</verify>
  <done>The demo temporal controls can be stored, changed, and reset without introducing a second coordination channel.</done>
</task>

<task type="auto">
  <name>Task 2: Drive active-slice playback from the ordered demo slice list</name>
  <files>src/components/dashboard-demo/Demo3dSpatialView.tsx</files>
  <action>
    Add the playback loop that advances `activeSliceIndex` through the already-ordered slice list, pauses while scrubbing, and uses a brief pause before loop restart. Keep playback bounded to the demo 3D widget path only, and do not touch map animation, timeline animation, or camera-orientation behavior.
  </action>
  <verify>pnpm exec eslint src/components/dashboard-demo/Demo3dSpatialView.tsx</verify>
  <done>The demo 3D widget sequences slices continuously while remaining analytically legible and isolated from other views.</done>
</task>

<task type="auto">
  <name>Task 3: Rework SliceScrubber into a compact temporal control strip</name>
  <files>src/app/stkde-3d/components/SliceScrubber.tsx</files>
  <action>
    Reshape the scrubber into a compact, quiet Inspect-panel control surface that exposes playback toggle, adjustable speed slider, opt-in interpolation labeled `Interpolated`, and trail controls while preserving active-slice readout and direct slice stepping. Keep the feedback minimal: no extra progress-bar chrome or bulky tool strip.
  </action>
  <verify>pnpm exec eslint src/app/stkde-3d/components/SliceScrubber.tsx</verify>
  <done>The Inspect panel can manage temporal behavior without expanding the control surface or leaking into other routes.</done>
</task>

</tasks>

<verification>
Run the store test and lint checks above, then confirm the playback controls remain compact and the demo 3D path still receives only the ordered slice index updates.
</verification>

<success_criteria>
The demo 3D STKDE widget can play, pause, scrub, and change interpolation/speed settings from one shared control source without affecting map or timeline animation.
</success_criteria>

<output>
After completion, create `.planning/phases/78-temporal-evolution/78-01-SUMMARY.md`
</output>
