---
phase: 54-adaptive-timeslicing-algos-verbosity
plan: 3
type: execute
wave: 3
depends_on: [54-01, 54-02]
files_modified:
  - src/lib/adaptive/route-binning-mode.test.ts
  - src/app/api/adaptive/global/route.ts
  - src/app/api/adaptive/global/route.test.ts
  - src/app/timeslicing-algos/lib/mode-intent.test.ts
  - src/app/timeslicing-algos/page.timeline-algos.test.ts
autonomous: true

must_haves:
  truths:
    - "Regression tests prove `uniform-time` and `uniform-events` behavior remains backward compatible while adaptive intent is introduced in `/timeslicing-algos`"
    - "Adaptive intent and invalid modes have explicit fallback semantics that are observable in tests"
    - "Diagnostics visibility is test-locked to `/timeslicing-algos` and explicit verbose gating"
  artifacts:
    - path: "src/app/api/adaptive/global/route.test.ts"
      provides: "API-level contract coverage for binning mode parsing and fallback behavior"
      contains: "binningMode"
    - path: "src/app/api/adaptive/global/route.ts"
      provides: "Testable mode parsing helper/export surface without changing external response contract"
      contains: "resolveBinningMode"
    - path: "src/lib/adaptive/route-binning-mode.test.ts"
      provides: "Central resolver regression guard for route defaults and explicit override precedence"
      contains: "timeslicing-algos"
    - path: "src/app/timeslicing-algos/lib/mode-intent.test.ts"
      provides: "Adaptive intent -> effective mode fallback matrix coverage"
      contains: "adaptive"
    - path: "src/app/timeslicing-algos/page.timeline-algos.test.ts"
      provides: "Route-level adaptive option + diagnostics scope guard coverage"
      contains: "verbose"
  key_links:
    - from: "src/app/timeslicing-algos/lib/mode-intent.test.ts"
      to: "src/lib/adaptive/route-binning-mode.ts"
      via: "intent wrapper delegates effective mode resolution"
      pattern: "resolveRouteBinningMode"
    - from: "src/app/api/adaptive/global/route.test.ts"
      to: "src/app/api/adaptive/global/route.ts"
      via: "mode parsing + response contract assertions"
      pattern: "resolveBinningMode|GET"
---

<objective>
Lock adaptive-intent and diagnostics behavior with deterministic regression coverage across resolver, route shell intent logic, and adaptive API parsing.

Purpose: This prevents silent fallback regressions and guarantees backward compatibility for existing mode behavior while adding adaptive workflow coverage.
Output: Expanded test coverage for resolver/intents/diagnostics/API mode parsing plus any minimal route export adjustments needed for testability.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-CONTEXT.md
@.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-RESEARCH.md
@.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-01-SUMMARY.md
@.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-02-SUMMARY.md
@src/lib/adaptive/route-binning-mode.test.ts
@src/app/api/adaptive/global/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden API mode parsing contract tests for fallback transparency</name>
  <files>src/app/api/adaptive/global/route.ts, src/app/api/adaptive/global/route.test.ts</files>
  <action>Add/extend API tests covering accepted modes (`uniform-time`, `uniform-events`) plus invalid or `adaptive` input fallback behavior. Ensure tests assert the response `binningMode` echo and fallback semantics explicitly so regressions cannot hide behind status-only checks. If needed, minimally adjust `route.ts` exports/helpers to make parsing logic testable without altering public endpoint behavior or response shape.</action>
  <verify>`npm test -- --run src/app/api/adaptive/global/route.test.ts` passes.</verify>
  <done>API mode coercion behavior is deterministic, tested, and backward compatible for existing callers that omit or pass legacy modes.</done>
</task>

<task type="auto">
  <name>Task 2: Complete resolver and intent test matrix for adaptive + legacy mode compatibility</name>
  <files>src/lib/adaptive/route-binning-mode.test.ts, src/app/timeslicing-algos/lib/mode-intent.test.ts</files>
  <action>Extend central resolver and algos intent helper tests to cover route defaults, explicit override precedence, adaptive intent translation, and invalid input fallback. Keep assertions explicit about which layer owns which fallback (resolver vs route intent helper) and preserve existing expected outputs for `uniform-time` / `uniform-events`. Do not change resolver contract shape; add tests that confirm wrappers consume it correctly.</action>
  <verify>`npm test -- --run src/lib/adaptive/route-binning-mode.test.ts src/app/timeslicing-algos/lib/mode-intent.test.ts` passes.</verify>
  <done>Any drift in adaptive translation or legacy route-mode behavior causes deterministic test failures.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize route-level regression checks for adaptive control and diagnostics visibility gates</name>
  <files>src/app/timeslicing-algos/page.timeline-algos.test.ts</files>
  <action>Update route-level regression coverage so it verifies all three mode controls (`uniform-time`, `uniform-events`, `adaptive`) and diagnostics gate behavior (`verbose`-enabled on algos route only). Preserve existing assertions that `/timeslicing-algos` remains algorithm-focused and excludes suggestion/full-auto orchestration UI. Keep tests deterministic and CI-safe.</action>
  <verify>`npm test -- --run src/app/timeslicing-algos/page.timeline-algos.test.ts` passes.</verify>
  <done>Regression suite locks adaptive route controls, diagnostics scope, and no-regression boundaries for existing route intent.</done>
</task>

</tasks>

<verification>
- API mode parsing/fallback behavior is explicitly covered and visible in assertions.
- Resolver + route intent helper tests preserve backward-compatible mode behavior while validating adaptive translation.
- Route tests lock adaptive controls and diagnostics gating to `/timeslicing-algos`.
- Full targeted suite passes:
  `npm test -- --run src/lib/adaptive/route-binning-mode.test.ts src/app/timeslicing-algos/lib/mode-intent.test.ts src/app/timeslicing-algos/page.timeline-algos.test.ts src/app/api/adaptive/global/route.test.ts`
</verification>

<success_criteria>
- Adaptive-mode testing coverage exists across route intent, API parsing, and diagnostics guard behavior.
- Existing `uniform-time` and `uniform-events` behaviors remain unchanged under test.
- Silent fallback regressions are prevented by explicit assertions and route-scoped diagnostics checks.
</success_criteria>

<output>
After completion, create `.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-03-SUMMARY.md`
</output>
