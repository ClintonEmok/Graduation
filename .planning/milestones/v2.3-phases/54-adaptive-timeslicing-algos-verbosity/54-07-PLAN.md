---
phase: 54-adaptive-timeslicing-algos-verbosity
plan: 7
type: execute
wave: 5
depends_on: [54-06]
files_modified:
  - src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.ts
  - src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.test.ts
  - src/app/timeslicing-algos/lib/AdaptiveBinDiagnosticsPanel.tsx
  - src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx
  - src/app/timeslicing-algos/page.timeline-algos.test.ts
  - src/workers/adaptiveTime.worker.test.ts
autonomous: true

must_haves:
  truths:
    - "QA can inspect each current `/timeslicing-algos` bin and see its index, time boundaries, raw event count, density context, and adaptive multiplier/warp factor contribution"
    - "Per-bin diagnostics follow the currently selected strategy and interaction mode, so changing route controls updates the table without changing timeline/dashboard behavior"
    - "The multiplier shown in diagnostics matches the same normalization/weighting logic used by adaptive compute, rather than a hand-waved UI-only estimate"
    - "Regression coverage fails if per-bin diagnostics drift from worker/store contracts or leak outside the algos route"
  artifacts:
    - path: "src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.ts"
      provides: "Pure per-bin diagnostics model builder derived from current route/store maps"
      contains: "adaptiveMultiplier"
    - path: "src/app/timeslicing-algos/lib/AdaptiveBinDiagnosticsPanel.tsx"
      provides: "Compact but detailed QA table/panel for per-bin inspection"
      contains: "Bin"
    - path: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      provides: "Route-scoped passive diagnostics wiring bound to current algos state"
      contains: "AdaptiveBinDiagnosticsPanel"
    - path: "src/app/timeslicing-algos/page.timeline-algos.test.ts"
      provides: "Route regression guard for algos-only diagnostics rendering and data-flow wiring"
      contains: "adaptive bin diagnostics"
  key_links:
    - from: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      to: "src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.ts"
      via: "derive rows from selected strategy + current adaptive store outputs"
      pattern: "buildAdaptiveBinDiagnostics"
    - from: "src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.ts"
      to: "src/workers/adaptiveTime.worker.ts"
      via: "shared worker-aligned multiplier and boundary semantics verified by tests"
      pattern: "1 \+ .*5|uniform-events|uniform-time"
    - from: "src/app/timeslicing-algos/lib/AdaptiveBinDiagnosticsPanel.tsx"
      to: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      via: "read-only QA widget rendered only on algos route"
      pattern: "AdaptiveBinDiagnosticsPanel"
---

<objective>
Add deep per-bin diagnostics to `/timeslicing-algos` so adaptive behavior is inspectable at the smallest useful level without widening shared runtime contracts.

Purpose: Current Phase 54 diagnostics cover route/mode context, but QA still cannot verify why adaptive behavior changes from bin to bin. This plan exposes the actual per-bin inputs and derived multiplier/warp contribution so testers can validate adaptive correctness directly from the algos route.
Output: A worker-aligned per-bin diagnostics helper, a compact algos-only QA panel/table, and regression coverage proving the multiplier/data-flow stays accurate and non-invasive.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-CONTEXT.md
@.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-05-SUMMARY.md
@.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-06-PLAN.md
@src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx
@src/app/timeslicing-algos/lib/TimeslicingAlgosStrategyStats.tsx
@src/store/useAdaptiveStore.ts
@src/workers/adaptiveTime.worker.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build a worker-aligned per-bin diagnostics model from existing adaptive outputs</name>
  <files>src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.ts, src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.test.ts, src/workers/adaptiveTime.worker.test.ts</files>
  <action>Create a pure diagnostics helper that accepts the current `/timeslicing-algos` inputs already available at runtime: selected strategy, current domain, raw timestamps (for boundary reconstruction when needed), and adaptive store outputs (`countMap`, `densityMap`, `warpMap`). For each bin, derive: bin index, start/end boundaries, width/span, raw event count, density-per-second where meaningful, normalized density, adaptive multiplier/weight (`1 + normalizedDensity * 5` or the exact current equivalent), and at least one warp-impact value such as warped span share or cumulative warp offset. Do not widen Zustand/public API contracts or add dashboard-facing fields just for diagnostics. If exact boundary or weighting math would drift from worker behavior, extract or mirror only the minimal pure logic needed and lock it with tests against `computeAdaptiveMaps` so the diagnostics values proveably match current adaptive semantics.</action>
  <verify>`npm test -- --run src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.test.ts src/workers/adaptiveTime.worker.test.ts` passes with cases for both `uniform-time` and `uniform-events` bins.</verify>
  <done>Per-bin diagnostics rows can be generated deterministically from current route/store data, and multiplier values are test-verified against worker behavior instead of guessed in the UI.</done>
</task>

<task type="auto">
  <name>Task 2: Add an algos-only compact diagnostics panel for per-bin QA inspection</name>
  <files>src/app/timeslicing-algos/lib/AdaptiveBinDiagnosticsPanel.tsx, src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx</files>
  <action>Implement a route-local diagnostics widget/table that stays compact but supports detailed inspection. Render it only inside `/timeslicing-algos`, colocated with the existing stats surface, and keep it read-only. The panel should summarize current strategy/timescale/domain at the top, then provide a scrollable per-bin table with columns for bin index, time range/boundaries, count, density, normalized density (or equivalent), adaptive multiplier, and one derived warp-correctness field (for example warped width share or cumulative offset). Prefer passive derivation from Task 1 helper over any new store writes or worker message changes. Keep shared timeline/dashboard components untouched so diagnostics improve QA visibility without altering behavior outside the algos route.</action>
  <verify>`npm run typecheck` passes and `src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx` renders the panel from current route/store state without new writes to shared stores.</verify>
  <done>`/timeslicing-algos` exposes a detailed per-bin diagnostics surface that is practical for QA and isolated from non-algos routes.</done>
</task>

<task type="auto">
  <name>Task 3: Lock route-scoped wiring and no-regression behavior with targeted tests</name>
  <files>src/app/timeslicing-algos/page.timeline-algos.test.ts</files>
  <action>Extend the algos route regression file to assert the new per-bin diagnostics panel is wired from current strategy/timescale and adaptive maps, remains confined to `/timeslicing-algos`, and does not replace or mutate existing timeline interactions. Add assertions that the route shell still computes maps through the existing `computeMaps(..., { binningMode: selectedStrategy })` path while diagnostics are derived passively from current outputs. Preserve existing tests for route focus, strategy controls, and span-aware timeline wiring.</action>
  <verify>`npm test -- --run src/app/timeslicing-algos/page.timeline-algos.test.ts` passes.</verify>
  <done>Regression coverage protects algos-only scope, passive data flow, and core route behavior while the new diagnostics panel is present.</done>
</task>

</tasks>

<verification>
- Worker/helper alignment: `npm test -- --run src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.test.ts src/workers/adaptiveTime.worker.test.ts`
- Route regression: `npm test -- --run src/app/timeslicing-algos/page.timeline-algos.test.ts`
- Type safety: `npm run typecheck`
- Manual QA:
  1. Open `/timeslicing-algos` and switch between `uniform-time` and `uniform-events`, then between `linear` and `adaptive`.
  2. Confirm the per-bin table updates its boundaries/counts/density context with strategy changes.
  3. In adaptive mode, confirm bins with higher normalized density show larger multipliers/warp shares than low-density bins.
  4. Brush/zoom the timeline and confirm diagnostics update passively without breaking timeline interaction or changing non-algos routes.
</verification>

<success_criteria>
- QA can inspect any current `/timeslicing-algos` bin and see the exact multiplier-driving context, not just aggregate route-level diagnostics.
- Reported per-bin multipliers stay aligned with the worker/store formulas currently driving adaptive behavior.
- Diagnostics remain read-only and route-scoped, with no dashboard or shared-component behavior changes.
- Automated coverage catches drift in multiplier derivation, boundary reconstruction, or algos-only wiring.
</success_criteria>

<risks>
- Reconstructing uniform-events boundaries from current data can drift if diagnostics duplicate worker logic loosely.
- A wide per-bin table can become noisy unless the panel is scrollable and prioritizes the few values QA actually needs.
- Touching shared adaptive math for reuse can accidentally widen behavior impact if changes are not kept pure and test-locked.
</risks>

<rollback>
- Remove the new panel from `TimeslicingAlgosRouteShell.tsx` while keeping helper tests if the UI proves too noisy.
- Revert any shared math extraction and fall back to route-local derivation if worker reuse introduces unintended surface-area changes.
- Keep diagnostics helper API internal to `/timeslicing-algos` unless a later phase explicitly promotes it to shared ownership.
</rollback>

<output>
After completion, create `.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-07-SUMMARY.md`
</output>
