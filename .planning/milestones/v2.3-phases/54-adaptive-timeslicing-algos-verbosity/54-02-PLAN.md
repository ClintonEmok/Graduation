---
phase: 54-adaptive-timeslicing-algos-verbosity
plan: 2
type: execute
wave: 2
depends_on: [54-01]
files_modified:
  - src/app/timeslicing-algos/lib/adaptive-diagnostics.ts
  - src/app/timeslicing-algos/lib/AdaptiveDiagnosticsPanel.tsx
  - src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx
  - src/app/timeslicing-algos/page.timeline-algos.test.ts
autonomous: true

must_haves:
  truths:
    - "QA can turn on verbose diagnostics on `/timeslicing-algos` and inspect selected intent, effective mode, fallback status, request parameters, and cache-key context"
    - "Diagnostics remain scoped to `/timeslicing-algos` and do not leak into other routes"
    - "Diagnostics output reflects effective mode from central resolver-backed route wiring"
  artifacts:
    - path: "src/app/timeslicing-algos/lib/adaptive-diagnostics.ts"
      provides: "Route-scoped diagnostics payload builder for algos mode intent/effective mode/request context"
      contains: "fallbackApplied"
    - path: "src/app/timeslicing-algos/lib/AdaptiveDiagnosticsPanel.tsx"
      provides: "Verbose diagnostics UI panel for algos route"
      contains: "effectiveMode"
    - path: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      provides: "Gated diagnostics render/log wiring bound to `/timeslicing-algos`"
      contains: "verbose"
    - path: "src/app/timeslicing-algos/page.timeline-algos.test.ts"
      provides: "Route-level regression guard for diagnostics scope and gating"
      contains: "diagnostics"
  key_links:
    - from: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      to: "src/app/timeslicing-algos/lib/adaptive-diagnostics.ts"
      via: "build diagnostics model from selected/effective mode and request params"
      pattern: "build.*Diagnostics"
    - from: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      to: "src/app/timeslicing-algos/lib/AdaptiveDiagnosticsPanel.tsx"
      via: "verbose route-scoped render gate"
      pattern: "AdaptiveDiagnosticsPanel"
---

<objective>
Add route-scoped verbose diagnostics for `/timeslicing-algos` adaptive-mode validation workflows.

Purpose: This makes fallback and payload behavior observable during algorithm comparisons without adding global logging noise or changing non-algos route behavior.
Output: Diagnostics builder utilities, a route-local diagnostics panel, shell-level verbose gating, and regression tests for visibility scope.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-CONTEXT.md
@.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-RESEARCH.md
@.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-01-SUMMARY.md
@src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx
@src/lib/queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a route-scoped diagnostics model helper for adaptive mode observability</name>
  <files>src/app/timeslicing-algos/lib/adaptive-diagnostics.ts</files>
  <action>Create a pure helper that builds diagnostics payload data from route-local state: selected intent, effective mode, fallback reason, request params (`binCount`, `kernelWidth`, effective `binningMode`), and cache-key context string using the established `global:${binCount}:${kernelWidth}:${mode}` format. Keep this helper route-local to avoid global diagnostics coupling, and ensure it accepts data already resolved by central mode contracts instead of re-resolving mode internally.</action>
  <verify>`npm run typecheck` passes and helper exports a typed diagnostics payload builder.</verify>
  <done>Diagnostics payload generation is deterministic and reusable, with no duplicate mode-resolution logic and no global routing side effects.</done>
</task>

<task type="auto">
  <name>Task 2: Add a gated diagnostics panel and optional scoped logging in algos route shell</name>
  <files>src/app/timeslicing-algos/lib/AdaptiveDiagnosticsPanel.tsx, src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx</files>
  <action>Implement a compact diagnostics panel component and render it only when both conditions are true: pathname is `/timeslicing-algos` and verbose diagnostics is enabled (for example `verbose=1` query flag or equivalent route-local toggle). Display selected intent, effective mode, fallback status/reason, request payload values, and cache-key context. If console logging is added, gate it behind the same condition and use structured `console.debug` output only. Do not introduce diagnostics rendering/logging paths in shared or non-algos components.</action>
  <verify>`npm run typecheck` passes and diagnostics code paths are contained to `src/app/timeslicing-algos/lib/*`.</verify>
  <done>Diagnostics are available for algos QA when explicitly enabled and absent from non-algos routes by construction.</done>
</task>

<task type="auto">
  <name>Task 3: Extend route regression coverage for diagnostics gate and route scope</name>
  <files>src/app/timeslicing-algos/page.timeline-algos.test.ts</files>
  <action>Update route-level tests to assert diagnostics wiring exists only for `/timeslicing-algos` and is tied to the verbose gate. Include checks that diagnostics output includes effective-mode and fallback fields, and that unguarded/global diagnostics patterns are not introduced. Keep existing assertions for route focus and mode controls intact.</action>
  <verify>`npm test -- --run src/app/timeslicing-algos/page.timeline-algos.test.ts` passes.</verify>
  <done>Tests fail if diagnostics leak outside the route, lose key observability fields, or bypass the explicit verbose gate.</done>
</task>

</tasks>

<verification>
- Verbose diagnostics can be enabled for `/timeslicing-algos` and show mode/payload/cache-key context.
- Diagnostics are route-scoped and do not emit global noise.
- Tests protect diagnostics gate behavior and route scoping.
</verification>

<success_criteria>
- QA can confirm fallback and effective mode behavior directly in `/timeslicing-algos` without inspecting unrelated logs.
- Non-algos routes remain free of this diagnostics surface.
</success_criteria>

<output>
After completion, create `.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-02-SUMMARY.md`
</output>
