---
phase: 54-adaptive-timeslicing-algos-verbosity
plan: 5
type: execute
wave: 3
depends_on: [54-04]
files_modified:
  - src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx
  - src/app/timeslicing-algos/page.timeline-algos.test.ts
autonomous: true

must_haves:
  truths:
    - "`/timeslicing-algos` timeline domain, fetch window, and detail-range behavior follow the same interaction lifecycle as `/timeslicing`"
    - "Selection drag/brush updates timeline viewport immediately, while adaptive map recompute and data fetch stay anchored to the base timeline domain"
    - "Route status chips/labels describe real timeline domain and fetch/buffer context instead of a disconnected metadata range"
  artifacts:
    - path: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      provides: "Canonical parity wiring for domain source-of-truth, fetch window, detail override, recompute triggers, and timeline/data labels"
      contains: "detailRangeOverride"
    - path: "src/app/timeslicing-algos/page.timeline-algos.test.ts"
      provides: "Regression guardrails locking parity-critical wiring against future drift"
      contains: "timeslicing parity"
  key_links:
    - from: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      to: "src/hooks/useCrimeData.ts"
      via: "useCrimeData called with base timeline domain (not settled selection window)"
      pattern: "startEpoch: baseDomainStartSec"
    - from: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      to: "src/components/timeline/DualTimeline.tsx"
      via: "detailRangeOverride explicitly receives resolved rangeStart/rangeEnd"
      pattern: "detailRangeOverride=\{\[rangeStart, rangeEnd\]\}"
    - from: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      to: "src/store/useAdaptiveStore.ts"
      via: "computeMaps domain and trigger parity with timeslicing route"
      pattern: "computeMaps\(timestamps, \[baseDomainStartSec, baseDomainEndSec\]"
---

<objective>
Close timeline UX parity gaps so `/timeslicing-algos` behaves like `/timeslicing` for domain ownership, range updates, recompute timing, and status reporting.

Purpose: The algos route currently diverges in core timeline lifecycle (selection-driven fetch window, delayed settled range, and metadata labels), which makes interaction feel different from the source-of-truth route. Use `/timeslicing` timeline wiring as the template and keep only the algos-specific controls/stats surface.
Output: A parity-aligned algos route shell and regression tests that lock expected behavior against `/timeslicing` semantics.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-CONTEXT.md
@.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-04-SUMMARY.md
@src/app/timeslicing/page.tsx
@src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx
@src/components/timeline/DualTimeline.tsx
@src/hooks/useCrimeData.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebase algos timeline wiring on the timeslicing template</name>
  <files>src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx</files>
  <action>Use `src/app/timeslicing/page.tsx` as the implementation template for timeline domain + data fetch wiring, then strip suggestion/full-auto-specific parts. Remove selection-settling query-window ownership (`SELECTION_SETTLE_DELAY_MS`, settled selected range state, and selection-driven `queryStartSec/queryEndSec` fetch path). Fetch algos timeline data from the same base domain source used by `/timeslicing` (adaptive domain when valid, otherwise timeline store min/max defaults) with `bufferDays: 30`. Keep selection state as an interaction concern (for detail display), not a fetch-domain concern. Also remove `/api/crime/meta` dependency so timeline labels come from the actual in-use domain path.</action>
  <verify>`npm run typecheck` passes and `useCrimeData` in algos shell uses `baseDomainStartSec/baseDomainEndSec` as fetch window inputs.</verify>
  <done>Brushing/dragging no longer shifts the algos fetch domain; main timeline data loading follows the same base-domain ownership model as `/timeslicing`.</done>
</task>

<task type="auto">
  <name>Task 2: Port timeslicing timeline lifecycle semantics, then keep algos-only UI extras</name>
  <files>src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx</files>
  <action>Port the `/timeslicing` range/lifecycle behavior directly: compute `rangeStart/rangeEnd` from selected time range with viewport fallback and pass `detailRangeOverride={[rangeStart, rangeEnd]}` to `DualTimeline` unconditionally. Update adaptive recompute wiring so `computeMaps` runs off fetched crimes + base domain + selected strategy (not delayed settled selection windows). Set timeline store min/max with the same domain contract as `/timeslicing` and keep strategy selection intact. Replace current timeline chip labeling with parity-safe labels that show active timeline domain and fetched/buffered context from `useCrimeData` meta. Preserve only algos-specific additions (strategy controls + stats widget), not separate timeline lifecycle logic.</action>
  <verify>Manual code check confirms `detailRangeOverride={[rangeStart, rangeEnd]}` and `computeMaps(..., [baseDomainStartSec, baseDomainEndSec], { binningMode: selectedStrategy })` in route shell.</verify>
  <done>Detail timeline follows selection immediately while data/recompute lifecycle remains stable; displayed range chips reflect actual timeline/fetch behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add parity regression guards against future timeline drift</name>
  <files>src/app/timeslicing-algos/page.timeline-algos.test.ts</files>
  <action>Extend algos route regression tests to assert parity-critical tokens/behaviors: no settled selection delay mechanism, no `/api/crime/meta` label source, base-domain `useCrimeData` fetch inputs, explicit `detailRangeOverride={[rangeStart, rangeEnd]}`, and base-domain computeMaps call with selected strategy. Preserve existing assertions for strategy controls and exclusion of suggestion orchestration UI.</action>
  <verify>`npm test -- --run src/app/timeslicing-algos/page.timeline-algos.test.ts` passes.</verify>
  <done>Test suite fails immediately if algos timeline wiring drifts back to selection-driven fetch domains or metadata-based range labeling.</done>
</task>

</tasks>

<verification>
- Type safety: `npm run typecheck`
- Route regression: `npm test -- --run src/app/timeslicing-algos/page.timeline-algos.test.ts`
- Source route guard (unchanged): `npm test -- --run src/app/timeslicing/page.binning-mode.test.ts`
- Manual parity QA:
  1. Open `/timeslicing` and `/timeslicing-algos` in separate tabs.
  2. Drag/brush timeline selection repeatedly and confirm both routes keep stable base domain labels while detail view follows selection immediately.
  3. Toggle strategy/time-scale in algos and confirm recompute occurs without domain chip jumps tied to selection drag.
  4. Refresh both routes and confirm timeline range/data chips remain consistent with actual displayed timeline domain and buffered fetch context.
</verification>

<success_criteria>
- `/timeslicing-algos` no longer uses settled selection ranges as fetch-domain source-of-truth.
- Timeline detail override and selection lifecycle feel consistent with `/timeslicing` during drag/brush interaction.
- Adaptive recompute and chip labels represent real in-use domain/fetch context and remain strategy-aware.
- Regression tests lock the parity contract and fail on drift.
</success_criteria>

<risks>
- Removing settled-selection throttling may increase fetch/recompute frequency if future code re-couples selection to fetch windows.
- Changing label sources can invalidate existing QA expectations tied to global metadata wording.
- Store-domain changes can affect downstream consumers that implicitly relied on buffered applied ranges.
</risks>

<rollback>
- Restore previous `TimeslicingAlgosRouteShell.tsx` selection-settle/query-window logic from pre-plan commit if interaction regressions appear.
- Revert parity label changes while keeping test updates isolated to identify whether divergence is visual-only or lifecycle-level.
- Re-run targeted tests and confirm previous behavior baseline before reapplying parity changes incrementally.
</rollback>

<output>
After completion, create `.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-05-SUMMARY.md`
</output>
