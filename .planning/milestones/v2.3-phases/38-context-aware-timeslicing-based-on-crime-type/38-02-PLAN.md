---
phase: 38-context-aware-timeslicing-based-on-crime-type
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/useContextProfileStore.ts
  - src/app/timeslicing/components/ProfileManager.tsx
autonomous: true

must_haves:
  truths:
    - "User can save current filter combination as a named custom profile"
    - "User can load previously saved custom profiles"
    - "Custom profiles persist across browser sessions"
  artifacts:
    - path: "src/store/useContextProfileStore.ts"
      provides: "Profile storage with persist middleware"
      exports: ["ContextProfile", "useContextProfileStore", "addCustomProfile", "deleteProfile", "setActiveProfile"]
    - path: "src/app/timeslicing/components/ProfileManager.tsx"
      provides: "UI for saving/loading custom profiles"
      contains: "ProfileList, SaveProfileButton, LoadProfileButton"
  key_links:
    - from: "ProfileManager"
      to: "useContextProfileStore"
      via: "hook reads customProfiles, calls addCustomProfile/deleteProfile"
      pattern: "useContextProfileStore.*addCustomProfile"
    - from: "useContextProfileStore"
      to: "useContextExtractor"
      via: "stores FilterContext with profile"
      pattern: "FilterContext"
---

<objective>
Create profile management system: Zustand store for persisting custom profiles and UI component for saving/loading. This enables users to save their preferred filter configurations.
</objective>

<execution_context>
@/Users/clintonemok/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/clintonemok/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-context-aware-timeslicing-based-on-crime-type/38-01-PLAN.md
@src/store/useSuggestionStore.ts
@src/hooks/useContextExtractor.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useContextProfileStore</name>
  <files>src/store/useContextProfileStore.ts</files>
  <action>
Create src/store/useContextProfileStore.ts with:
- ContextProfile interface: id, name, context (FilterContext), createdAt, isSmart
- useContextProfileStore using Zustand with persist middleware (per RESEARCH.md)
- Storage key: 'timeslicing-context-profiles-v1'
- State: smartProfiles (computed), customProfiles (persisted), activeProfileId
- Actions:
  - addCustomProfile(name, context): creates profile with UUID, adds to customProfiles
  - deleteProfile(id): removes from customProfiles, clears active if deleted
  - setActiveProfile(id): sets active profile
  - getActiveProfile(): returns active profile or null
- partialize: only persist customProfiles and activeProfileId (smartProfiles computed)
- Per RESEARCH.md: validate for duplicate names before saving
  </action>
  <verify>File exists, TypeScript compiles, store uses persist middleware, exports all actions</verify>
  <done>Custom profiles persist to localStorage, can be added/deleted/loaded. Active profile tracked.</done>
</task>

<task type="auto">
  <name>Task 2: Create ProfileManager component</name>
  <files>src/app/timeslicing/components/ProfileManager.tsx</files>
  <action>
Create src/app/timeslicing/components/ProfileManager.tsx with:
- ProfileManager component: collapsible section in SuggestionPanel
- SaveProfileButton: saves current context with name input dialog
- ProfileList: displays saved custom profiles with:
  - Profile name
  - Crime types summary (e.g., "3 crime types" or "All crimes")
  - Load button (applies profile filters)
  - Delete button (with confirmation)
- Smart profile display: shows detected smart profile with "Active" badge
- Use existing UI patterns from SuggestionPanel (badges, buttons)
- Per CONTEXT.md: smart profiles auto-detected, custom profiles user-saved
  </action>
  <verify>Component renders in SuggestionPanel, profiles can be saved/loaded/deleted</verify>
  <done>User can save current filters as named profile, load saved profiles, delete profiles. Smart profiles shown with active state.</done>
</task>

</tasks>

<verification>
- [ ] useContextProfileStore with persist middleware
- [ ] ProfileManager renders in SuggestionPanel
- [ ] Custom profiles save to localStorage
- [ ] Profiles load and apply context
- [ ] TypeScript compiles without errors
</verification>

<success_criteria>
Profile management complete:
1. Custom profiles can be saved with names
2. Saved profiles persist across browser sessions
3. Profiles can be loaded to restore filter context
4. Smart profiles display when detected
</success_criteria>

<output>
After completion, create `.planning/phases/38-context-aware-timeslicing-based-on-crime-type/38-02-SUMMARY.md`
</output>
