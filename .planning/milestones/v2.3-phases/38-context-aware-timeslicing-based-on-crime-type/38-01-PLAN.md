---
phase: 38-context-aware-timeslicing-based-on-crime-type
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useContextExtractor.ts
  - src/hooks/useSmartProfiles.ts
  - src/app/timeslicing/components/SuggestionToolbar.tsx
autonomous: true

must_haves:
  truths:
    - "User can toggle between 'analyze visible' and 'analyze all' modes"
    - "User sees smart profile suggestions when matching filter combinations are active"
    - "Context is extracted from active filters (crime types, districts, time range)"
  artifacts:
    - path: "src/hooks/useContextExtractor.ts"
      provides: "Extract current filter context from stores"
      exports: ["FilterContext", "useContextExtractor", "getCurrentContext", "getContextSignature"]
    - path: "src/hooks/useSmartProfiles.ts"
      provides: "Smart profile detection based on context"
      exports: ["SmartProfile", "useSmartProfiles"]
    - path: "src/app/timeslicing/components/SuggestionToolbar.tsx"
      provides: "Mode toggle (visible/all) added to toolbar"
      contains: "AnalyzeVisibleAllToggle"
  key_links:
    - from: "useContextExtractor"
      to: "useFilterStore"
      via: "hook reads selectedTypes, selectedDistricts, selectedTimeRange"
      pattern: "useFilterStore.*selectedTypes"
    - from: "useContextExtractor"
      to: "useViewportStore"
      via: "hook reads crimeFilters, startDate, endDate"
      pattern: "useCrimeFilters.*useViewportStart.*useViewportEnd"
    - from: "SuggestionToolbar"
      to: "useSuggestionGenerator"
      via: "mode passed to trigger function"
      pattern: "contextMode.*trigger"
---

<objective>
Create the context extraction foundation: hooks for extracting filter context, smart profile detection, and mode toggle UI. This establishes the data layer for context-aware timeslicing.
</objective>

<execution_context>
@/Users/clintonemok/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/clintonemok/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-context-aware-timeslicing-based-on-crime-type/38-CONTEXT.md
@.planning/phases/38-context-aware-timeslicing-based-on-crime-type/38-RESEARCH.md
@src/store/useSuggestionStore.ts
@src/store/useFilterStore.ts
@src/hooks/useSuggestionGenerator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useContextExtractor hook</name>
  <files>src/hooks/useContextExtractor.ts</files>
  <action>
Create src/hooks/useContextExtractor.ts with:
- FilterContext interface: crimeTypes (string[]), districts (string[]), timeRange ({start, end}), isFullDataset (boolean)
- useContextExtractor hook using fine-grained selectors from useFilterStore and useViewportStore
- getCurrentContext(mode): returns FilterContext for 'visible' (viewport) or 'all' (selectedTimeRange)
- getContextSignature(context): returns deterministic string for context comparison
- Use existing viewportStore selectors (useCrimeFilters, useViewportStart, useViewportEnd) per RESEARCH.md patterns
  </action>
  <verify>File exists, TypeScript compiles, exports FilterContext interface and useContextExtractor hook</verify>
  <done>useContextExtractor hook extracts crime types, districts, and time range from stores. getContextSignature returns stable string.</done>
</task>

<task type="auto">
  <name>Task 2: Create useSmartProfiles hook</name>
  <files>src/hooks/useSmartProfiles.ts</files>
  <action>
Create src/hooks/useSmartProfiles.ts with:
- SmartProfile interface: id, name, description, crimeTypes[], intervals[]
- Predefined smart profiles: "Burglary Focus", "Violent Crime", "All Crimes" (per RESEARCH.md patterns)
- useSmartProfiles(context: FilterContext): returns matching SmartProfile or null
- Detection logic: 
  - Empty crimeTypes = all-crimes
  - "Burglary" only = burglary profile
  - 2+ violent crimes (Assault, Robbery, Homicide) = violent-crime profile
  - Otherwise null
- Use useMemo for memoization per React patterns
  </action>
  <verify>File exists, TypeScript compiles, exports SmartProfile interface and useSmartProfiles hook</verify>
  <done>Smart profile detection returns matching profile for burglary-only, violent-crime combinations, and all-crimes. Returns null for mixed/other contexts.</done>
</task>

<task type="auto">
  <name>Task 3: Add mode toggle to SuggestionToolbar</name>
  <files>src/app/timeslicing/components/SuggestionToolbar.tsx</files>
  <action>
Add AnalyzeVisibleAllToggle component to SuggestionToolbar.tsx:
- Toggle button group: "Analyze Visible" / "Analyze All" 
- State stored in local useState (will be used by generator in Plan 3)
- Visual: pill-style toggle, active state highlighted
- Label: "Analysis scope" with tooltip explaining visible vs all
- Placement: After generation params, before Generate button
- Per CONTEXT.md: both modes must be available
  </action>
  <verify>Toggle renders in toolbar, switches between visible/all modes, persists state during session</verify>
  <done>User can toggle between "analyze visible" and "analyze all" modes. Toggle is visible and functional in the SuggestionToolbar.</done>
</task>

</tasks>

<verification>
- [ ] useContextExtractor exports FilterContext interface
- [ ] useSmartProfiles detects burglary, violent crime, and all-crimes profiles
- [ ] Mode toggle renders in toolbar with both options
- [ ] TypeScript compiles without errors
</verification>

<success_criteria>
Context extraction foundation complete:
1. Filter context can be extracted from filter/viewport stores
2. Smart profiles are detected for common filter combinations  
3. Mode toggle allows switching between visible and all analysis modes
</success_criteria>

<output>
After completion, create `.planning/phases/38-context-aware-timeslicing-based-on-crime-type/38-01-SUMMARY.md`
</output>
