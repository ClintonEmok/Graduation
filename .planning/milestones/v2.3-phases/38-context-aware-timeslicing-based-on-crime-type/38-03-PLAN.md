---
phase: 38-context-aware-timeslicing-based-on-crime-type
plan: "03"
type: execute
wave: 2
depends_on:
  - "38-01"
  - "38-02"
files_modified:
  - src/app/timeslicing/components/ContextBadge.tsx
  - src/app/timeslicing/components/SuggestionCard.tsx
  - src/store/useSuggestionStore.ts
  - src/hooks/useSuggestionGenerator.ts
autonomous: true

must_haves:
  truths:
    - "User can see context badges on suggestions showing what filters were used"
    - "Accepted suggestions show context metadata in history"
    - "Auto-regenerate triggers when filters change (debounced)"
  artifacts:
    - path: "src/app/timeslicing/components/ContextBadge.tsx"
      provides: "Badge showing context info on suggestions"
      exports: ["ContextBadge"]
    - path: "src/app/timeslicing/components/SuggestionCard.tsx"
      provides: "Context badge integrated into suggestion cards"
      contains: "ContextBadge import and usage"
    - path: "src/store/useSuggestionStore.ts"
      provides: "History entries with contextMetadata"
      contains: "contextMetadata in HistoryEntry"
    - path: "src/hooks/useSuggestionGenerator.ts"
      provides: "Context-aware generation with mode"
      contains: "contextMode, context extraction"
  key_links:
    - from: "SuggestionCard"
      to: "ContextBadge"
      via: "import and render badge below confidence"
      pattern: "ContextBadge.*context"
    - from: "useSuggestionStore.acceptSuggestion"
      to: "useContextExtractor"
      via: "captures current context on accept"
      pattern: "contextMetadata.*acceptedAt"
    - from: "useSuggestionGenerator"
      to: "useContextExtractor"
      via: "passes contextMode to generation"
      pattern: "contextMode.*getCurrentContext"
---

<objective>
Integrate context awareness into the UI: add context badges to suggestions, extend history with context metadata, wire up auto-regenerate with mode support. This completes the context-aware timeslicing experience.
</objective>

<execution_context>
@/Users/clintonemok/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/clintonemok/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-context-aware-timeslicing-based-on-crime-type/38-01-PLAN.md
@.planning/phases/38-context-aware-timeslicing-based-on-crime-type/38-02-PLAN.md
@src/app/timeslicing/components/SuggestionPanel.tsx
@src/app/timeslicing/components/SuggestionCard.tsx
@src/store/useSuggestionStore.ts
@src/hooks/useSuggestionGenerator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ContextBadge component</name>
  <files>src/app/timeslicing/components/ContextBadge.tsx</files>
  <action>
Create src/app/timeslicing/components/ContextBadge.tsx with:
- ContextBadgeProps: crimeTypes, isFullDataset, smartProfileName
- Rendering logic:
  - If smartProfileName: show amber/smart badge with Lightbulb icon
  - Else: show gray badge with crime types summary ("Burglary" or "3 types" or "All crimes")
  - If isFullDataset: append "(full range)" text
- Per CONTEXT.md: badge design is Claude's discretion - use Badge component with outline variant
- Style: compact, positioned below confidence badge
  </action>
  <verify>Component renders with correct content based on props, uses Badge from existing UI</verify>
  <done>ContextBadge shows smart profile name (amber) or crime types summary (gray). Full dataset indicator visible.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate ContextBadge into SuggestionCard</name>
  <files>src/app/timeslicing/components/SuggestionCard.tsx</files>
  <action>
Update SuggestionCard.tsx to:
- Import ContextBadge from ./ContextBadge
- Add context prop to SuggestionCard (or get from store)
- Render ContextBadge below confidence badge
- Show smart profile badge if detected, otherwise show crime type summary
- Position: below confidence, above suggestion content
  </action>
  <verify>ContextBadge renders on each suggestion card, shows relevant context info</verify>
  <done>All suggestion cards display context badge showing filters used for generation.</done>
</task>

<task type="auto">
  <name>Task 3: Extend history with context metadata</name>
  <files>src/store/useSuggestionStore.ts</files>
  <action>
Update useSuggestionStore.ts:
- Extend HistoryEntry interface with optional contextMetadata:
  - crimeTypes: string[]
  - timeRange: { start: number; end: number }
  - isFullDataset: boolean
  - profileName?: string
- Modify acceptSuggestion action to capture current context:
  - Import useContextExtractor to get current context at accept time
  - Store contextMetadata with each accepted suggestion
- Per CONTEXT.md: "Persist context metadata on accept: YES"
  </action>
  <verify>History entries contain contextMetadata when accepted</verify>
  <done>Accepted suggestions store context metadata. History view shows what context generated each suggestion.</done>
</task>

<task type="auto">
  <name>Task 4: Wire context mode into suggestion generator</name>
  <files>src/hooks/useSuggestionGenerator.ts</files>
  <action>
Update useSuggestionGenerator.ts:
- Add contextMode state: 'visible' | 'all' (from mode toggle in Plan 1)
- Modify generateSuggestions to use context-aware date range:
  - 'visible': use viewportStart/viewportEnd
  - 'all': use selectedTimeRange or full dataset bounds
- Pass context to generation algorithms for context-aware analysis
- Integrate with auto-regenerate: use 750ms debounce (per RESEARCH.md recommendation)
- Add isGenerating guard to prevent feedback loops (per RESEARCH.md pitfalls)
- Per CONTEXT.md: both auto debounced + manual button available
  </action>
  <verify>Generator uses correct date range based on mode, auto-regenerate works with debounce</verify>
  <done>Suggestions generated with context-aware date range. Auto-regenerate on filter changes works with mode consideration.</done>
</task>

</tasks>

<verification>
- [ ] ContextBadge renders on suggestion cards
- [ ] History shows context metadata for accepted suggestions
- [ ] Mode toggle affects generation scope
- [ ] Auto-regenerate debounces on filter changes
- [ ] TypeScript compiles without errors
</verification>

<success_criteria>
Context-aware integration complete:
1. Context badges visible on all suggestions
2. History entries contain context metadata
3. Mode toggle affects analysis scope
4. Auto-regenerate triggers on filter changes
5. Full workflow functional: extract context -> generate -> display badges -> persist on accept
</success_criteria>

<output>
After completion, create `.planning/phases/38-context-aware-timeslicing-based-on-crime-type/38-03-SUMMARY.md`
</output>
