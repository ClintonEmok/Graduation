---
phase: 59-add-stats-page
plan: 3
type: execute
wave: 3
depends_on: ["59-01", "59-02"]
files_modified:
  - src/app/stats/lib/components/SpatialHotspotMap.tsx
  - src/app/stats/lib/components/NeighborhoodContext.tsx
  - src/app/stats/lib/components/StatsSectionLayout.tsx
  - src/app/stats/lib/StatsRouteShell.tsx
  - src/app/stats/page.stats.test.ts
autonomous: true

must_haves:
  truths:
    - "User can see a spatial hotspot map showing crime density by location"
    - "User can see neighbourhood context showing POI density correlated with crime"
    - "Map and charts are laid out in a responsive dashboard grid"
    - "All visualizations update when district selection or time range changes"
  artifacts:
    - path: "src/app/stats/lib/components/SpatialHotspotMap.tsx"
      provides: "MapLibre map with crime density heatmap"
      contains: "MapLibre|heatmap"
    - path: "src/app/stats/lib/components/NeighborhoodContext.tsx"
      provides: "POI context cards showing neighbourhood characteristics"
      contains: "buildNeighbourhoodSummary"
    - path: "src/app/stats/lib/components/StatsSectionLayout.tsx"
      provides: "Reusable section layout for stats dashboard"
      contains: "section"
  key_links:
    - from: "src/app/stats/lib/components/SpatialHotspotMap.tsx"
      to: "src/hooks/useCrimeData.ts"
      via: "displays crime point density"
      pattern: "useCrimeData"
    - from: "src/app/stats/lib/components/NeighborhoodContext.tsx"
      to: "src/lib/neighbourhood/index.ts"
      via: "fetches POI data for bounds"
      pattern: "buildNeighbourhoodSummary"
---

<objective>
Add spatial hotspot map and neighbourhood context to complete the stats dashboard.

Purpose: Provide geographic visualization of crime patterns and correlate with neighbourhood characteristics (POI density). Complete the stats dashboard layout.
Output: MapLibre hotspot map, neighbourhood context cards, responsive dashboard grid layout.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-add-stats-page/59-CONTEXT.md
@.planning/phases/59-add-stats-page/59-01-SUMMARY.md
@.planning/phases/59-add-stats-page/59-02-SUMMARY.md
@src/app/stats/hooks/useNeighborhoodStats.ts
@src/app/stats/lib/components/CrimeTypeBreakdown.tsx
@src/app/stats/lib/components/TemporalPatternChart.tsx
@src/lib/neighbourhood/index.ts
@src/lib/context-diagnostics/spatial.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SpatialHotspotMap component</name>
  <files>src/app/stats/lib/components/SpatialHotspotMap.tsx</files>
  <action>
Create a MapLibre map showing crime density hotspots:
1. Create `src/app/stats/lib/components/SpatialHotspotMap.tsx`:
   - Use MapLibre GL JS (same as existing MapVisualization component)
   - Center on Chicago bounds: -87.85 to -87.5 lon, 41.6 to 42.05 lat
   - Add heatmap layer using crime point data
   - Heatmap intensity based on crime count per grid cell
   - Use existing heatmap layer pattern from MapHeatmapOverlay or MapStkdeHeatmapLayer
   - Optional: Add district boundary overlay if available

2. Data integration:
   - Use useNeighborhoodStats() hook to get crime data
   - Transform CrimeRecord[] into GeoJSON for MapLibre source
   - Only render points within selected districts

3. Interactions:
   - Pan/zoom enabled
   - Hover on hotspot shows tooltip with crime count
   - Click on hotspot zooms to that area
   - Layer toggle: heatmap vs points

4. Error/loading states:
   - Loading: Map skeleton with pulsing placeholder
   - Error: "Map unavailable" message with retry button
   - No data: "Select districts to see hotspots"

5. Wire into StatsRouteShell in a "Spatial Distribution" section
  </action>
  <read_first>
src/components/map/MapStkdeHeatmapLayer.tsx
src/components/map/MapHeatmapOverlay.tsx
src/app/stkde/lib/StkdeRouteShell.tsx
src/app/stats/hooks/useNeighborhoodStats.ts
  </read_first>
  <verify>`npm run typecheck` succeeds; map renders in browser</verify>
  <done>Spatial hotspot map displays crime density over Chicago with heatmap visualization</done>
  <acceptance_criteria>
- Map renders Chicago area centered
- Heatmap layer shows crime density
- Points filtered by selected districts
- Hover tooltip shows crime count
- Pan/zoom works smoothly
- Loading skeleton shown while data loads
- Graceful error state if map fails
</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create NeighborhoodContext component</name>
  <files>src/app/stats/lib/components/NeighborhoodContext.tsx, src/app/stats/lib/components/NeighborhoodContext.module.css</files>
  <action>
Create neighbourhood context cards showing POI and environmental data:
1. Create `src/app/stats/lib/components/NeighborhoodContext.tsx`:
   - Fetch neighbourhood data using buildNeighbourhoodSummary() from lib/neighbourhood
   - Derive bounds from selected districts (union of district bounding boxes)
   - Display POI category breakdown:
     * Food/Drink venues
     * Shopping
     * Parks
     * Transit stops
     * Education
     * Healthcare
   - Show total POI count
   - Optional: correlation indicator (e.g., "High transit, high crime")

2. UI Layout:
   - Card-based layout with category icons
   - Use Lucide React icons for categories (Utensils, ShoppingBag, Trees, Bus, GraduationCap, Heart)
   - Horizontal scrolling if many categories
   - Each category: icon, count, label

3. Correlation analysis (simple):
   - Compare POI density to crime density
   - Show "Crime vs Environment" insight
   - E.g., "High restaurant density correlates with high THEFT"

4. Error/loading states:
   - Loading: skeleton cards
   - No bounds: "Select districts to see neighbourhood context"
   - API error: "Context unavailable" with retry

5. Wire into StatsRouteShell in a "Neighbourhood Context" section
  </action>
  <read_first>
src/lib/neighbourhood/index.ts
src/lib/context-diagnostics/spatial.ts
src/app/timeslicing-algos/lib/NeighbourhoodDiagnosticsPanel.tsx
  </read_first>
  <verify>`npm run typecheck` succeeds; cards render with POI data</verify>
  <done>Neighbourhood context cards show POI breakdown for selected districts</done>
  <acceptance_criteria>
- Cards show 6 POI categories with icons and counts
- Total POI count displayed
- Data fetched from buildNeighbourhoodSummary
- Loading skeleton shown while data loads
- Error state with retry button
- Empty state when no districts selected
</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Finalize Stats dashboard layout and add tests</name>
  <files>src/app/stats/lib/StatsRouteShell.tsx, src/app/stats/lib/components/StatsSectionLayout.tsx, src/app/stats/page.stats.test.ts</files>
  <action>
Finalize the stats dashboard layout and add tests:
1. Update `src/app/stats/lib/StatsRouteShell.tsx` to include all components:
   - Time range selector (top)
   - Overview cards row (below time range)
   - Two-column layout:
     * Left: Crime type breakdown chart
     * Right: Temporal pattern chart
   - Full-width spatial hotspot map
   - Neighbourhood context cards (below map)

2. Create `src/app/stats/lib/components/StatsSectionLayout.tsx`:
   - Reusable section wrapper component
   - Props: title, description, children, collapsible
   - Consistent padding and border styling

3. Add responsive layout:
   - Desktop: multi-column grid
   - Tablet: 2 columns
   - Mobile: single column, stacked

4. Create `src/app/stats/page.stats.test.ts`:
   - Test route renders without errors
   - Test district selector updates data
   - Test overview cards show correct values
   - Test chart components receive data
   - Test map renders
   - Use Vitest with MSW for API mocking

5. Performance check:
   - Verify no excessive re-renders
   - Verify lazy loading for heavy components
   - Use React.memo and useMemo where appropriate
  </action>
  <read_first>
src/app/stats/lib/StatsRouteShell.tsx
src/app/stats/lib/components/CrimeTypeBreakdown.tsx
src/app/stats/lib/components/TemporalPatternChart.tsx
src/app/stats/lib/components/SpatialHotspotMap.tsx
src/app/stats/lib/components/NeighborhoodContext.tsx
  </read_first>
  <verify>`npm test -- --run src/app/stats/page.stats.test.ts` passes; `npm run typecheck` succeeds</verify>
  <done>Complete stats dashboard with responsive layout and test coverage</done>
  <acceptance_criteria>
- All components wired into StatsRouteShell
- Responsive layout works on desktop/tablet/mobile
- Tests cover route rendering and key interactions
- No console errors in browser
- Page load time < 3 seconds
</acceptance_criteria>
</task>

</tasks>

<verification>
- `npm run typecheck`
- `npm test -- --run src/app/stats/page.stats.test.ts`
- Manual check:
  1. Navigate to /stats
  2. Select districts, verify all visualizations update
  3. Check responsive layout at different screen sizes
  4. Verify map interactions work
  5. Verify neighbourhood context loads
  6. Check browser console for errors
</verification>

<success_criteria>
- Complete stats dashboard renders with all planned components
- Spatial hotspot map shows crime density
- Neighbourhood context cards display POI data
- Responsive layout works on all screen sizes
- Test coverage for key functionality
- No console errors
</success_criteria>

<risks>
- **Map performance:** Large point sets may slow map rendering
  - Mitigation: Use clustering, limit visible points, use heatmap layer
- **POI API reliability:** External APIs may fail
  - Mitigation: Graceful error handling, retry logic, cache results
</risks>

<rollback>
- Each component is self-contained, can disable individually
- Map can be hidden behind feature flag if performance issues
- Full route can be removed without side effects
</rollback>

<output>
After completion, create `.planning/phases/59-add-stats-page/59-03-SUMMARY.md`
</output>
