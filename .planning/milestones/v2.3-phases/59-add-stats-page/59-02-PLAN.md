---
phase: 59-add-stats-page
plan: 2
type: execute
wave: 2
depends_on: ["59-01"]
files_modified:
  - src/app/stats/lib/components/CrimeTypeBreakdown.tsx
  - src/app/stats/lib/components/TemporalPatternChart.tsx
  - src/app/stats/lib/components/StatsOverviewCards.tsx
  - src/app/stats/lib/stats-view-model.ts
  - src/app/stats/page.stkde.test.ts
autonomous: true

must_haves:
  truths:
    - "User can see crime type breakdown as a bar chart with counts and percentages"
    - "User can see temporal patterns as a heatmap (hours x days) and trend line"
    - "Overview cards show key metrics: total crimes, avg/day, peak hour, most common type"
    - "Charts update when user changes district selection or time range"
  artifacts:
    - path: "src/app/stats/lib/components/CrimeTypeBreakdown.tsx"
      provides: "Horizontal bar chart showing top crime types"
      contains: "Visx Bar"
    - path: "src/app/stats/lib/components/TemporalPatternChart.tsx"
      provides: "Heatmap grid and trend line for temporal analysis"
      contains: "heatmap"
    - path: "src/app/stats/lib/components/StatsOverviewCards.tsx"
      provides: "Summary stat cards (total, avg/day, peak)"
      contains: "totalCrimes"
    - path: "src/app/stats/lib/stats-view-model.ts"
      provides: "Data transformation for chart-ready format"
      contains: "transformForChart"
  key_links:
    - from: "src/app/stats/lib/components/CrimeTypeBreakdown.tsx"
      to: "src/app/stats/hooks/useNeighborhoodStats.ts"
      via: "receives aggregated byType data"
      pattern: "useNeighborhoodStats"
    - from: "src/app/stats/lib/components/TemporalPatternChart.tsx"
      to: "src/app/stats/hooks/useNeighborhoodStats.ts"
      via: "receives aggregated byHour/byDayOfWeek data"
      pattern: "useNeighborhoodStats"
    - from: "src/app/stats/lib/components/StatsOverviewCards.tsx"
      to: "src/app/stats/lib/stats-view-model.ts"
      via: "receives computed summary metrics"
      pattern: "transformStatsSummary"
---

<objective>
Add crime type breakdown charts and temporal pattern visualizations to the stats page.

Purpose: Give users insight into crime patterns through visual analytics. Shows what types of crimes are most common and when they occur.
Output: Crime type bar chart, temporal heatmap, trend line, and overview summary cards.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-add-stats-page/59-CONTEXT.md
@.planning/phases/59-add-stats-page/59-01-SUMMARY.md
@src/app/stats/hooks/useNeighborhoodStats.ts
@src/lib/stats/aggregation.ts
@src/lib/context-diagnostics/temporal.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StatsOverviewCards component</name>
  <files>src/app/stats/lib/components/StatsOverviewCards.tsx, src/app/stats/lib/stats-view-model.ts</files>
  <action>
Create summary stat cards showing key metrics:
1. Create `src/app/stats/lib/stats-view-model.ts` with transformation functions:
   - `transformStatsSummary(crimes: CrimeRecord[], byDistrict: Map, byType: Map, byHour: number[]): StatsSummary`
   - Compute: totalCrimes, avgPerDay, peakHour (0-23 formatted as "3:00 PM"), mostCommonCrime, districtCount
   - StatsSummary interface with: totalCrimes, avgPerDay, peakHour, peakHourLabel, mostCommonCrime, mostCommonCrimeCount, districtCount, dateRange

2. Create `src/app/stats/lib/components/StatsOverviewCards.tsx`:
   - Display 4-6 stat cards in a responsive grid
   - Each card: label, value, optional trend indicator
   - Cards: Total Crimes, Avg/Day, Peak Hour, Top Crime Type, Districts Selected
   - Use dark card styling (bg-neutral-900, rounded-lg, p-4)
   - Numbers formatted with locale (e.g., "12,345")
   - Loading skeleton state while data fetches
3. Import and wire into StatsRouteShell below the time range selector
  </action>
  <read_first>
src/app/stats/lib/StatsRouteShell.tsx
src/app/stats/hooks/useNeighborhoodStats.ts
src/lib/context-diagnostics/temporal.ts
  </read_first>
  <verify>`npm run typecheck` succeeds</verify>
  <done>Overview cards display computed stats from selected data</done>
  <acceptance_criteria>
- StatsOverviewCards renders 5 cards: Total Crimes, Avg/Day, Peak Hour, Top Crime Type, Districts
- Numbers formatted with locale (1,234 not 1234)
- Loading skeleton shown while data loads
- Cards update when district selection or time range changes
- Peak hour displayed as "3:00 PM" format
</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create CrimeTypeBreakdown chart component</name>
  <files>src/app/stats/lib/components/CrimeTypeBreakdown.tsx, src/app/stats/lib/components/CrimeTypeBreakdown.module.css</files>
  <action>
Create a horizontal bar chart showing crime type distribution:
1. Create `src/app/stats/lib/components/CrimeTypeBreakdown.tsx`:
   - Use Visx Bar component (from @visx/shape) for horizontal bars
   - Show top 10 crime types by count
   - Each bar: crime type label, count, percentage of total
   - Bars sorted by count descending
   - Interactive: hover shows tooltip with full details
   - Responsive: adjust bar width/thickness based on container size
   - Use existing color palette from app theme
   - Empty state: "Select districts to see crime types"
   - Loading state: skeleton bars

2. Create CSS module for custom styling if needed

3. Wire into StatsRouteShell in a "Crime Distribution" section:
   - Section header: "Crime Type Distribution"
   - Chart container with padding
   - Legend showing top 3 crime types

4. Use data from useNeighborhoodStats().byType Map
  </action>
  <read_first>
src/app/stats/lib/StatsRouteShell.tsx
src/app/stats/hooks/useNeighborhoodStats.ts
src/lib/category-maps.ts
  </read_first>
  <verify>`npm run typecheck` succeeds; bars render correctly in browser</verify>
  <done>Crime type breakdown chart shows top 10 types with counts and percentages</done>
  <acceptance_criteria>
- Chart shows top 10 crime types sorted by count
- Each bar shows: label, count number, percentage label
- Hover tooltip displays crime type details
- Empty state shown when no data
- Loading skeleton shown while data loads
- Chart responsive to container width changes
</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create TemporalPatternChart component</name>
  <files>src/app/stats/lib/components/TemporalPatternChart.tsx</files>
  <action>
Create temporal pattern visualizations:
1. Create `src/app/stats/lib/components/TemporalPatternChart.tsx` with two sub-views:

   **Hour x Day Heatmap:**
   - 24 columns (hours 0-23) x 7 rows (days Sun-Sat)
   - Cell color intensity = crime count (use color scale)
   - Axis labels: "12am" to "11pm", "Sun" to "Sat"
   - Hover tooltip: "Tuesday 3PM: 245 crimes"
   - Use Visx heatmap pattern or custom SVG grid

   **Monthly Trend Line:**
   - Line chart showing crime count over time
   - X-axis: months within selected range
   - Y-axis: crime count
   - Use Visx LinePath component
   - Smooth curve interpolation

2. Add toggle between heatmap and trend views (use Radix ToggleGroup)

3. Wire into StatsRouteShell in a "Temporal Patterns" section:
   - Section header: "When Crimes Occur"
   - Toggle for heatmap/trend view
   - Chart container

4. Use data from useNeighborhoodStats().byHour and useCrimeData timestamps
  </action>
  <read_first>
src/app/stats/lib/StatsRouteShell.tsx
src/app/stats/hooks/useNeighborhoodStats.ts
src/lib/context-diagnostics/temporal.ts
  </read_first>
  <verify>`npm run typecheck` succeeds; charts render in browser</verify>
  <done>Temporal pattern chart shows hour-day heatmap and monthly trend with toggle</done>
  <acceptance_criteria>
- Heatmap grid renders 24x7 cells with color intensity
- Day/hour labels displayed correctly
- Hover tooltip shows exact count for cell
- Trend line shows monthly data points
- Toggle switches between heatmap and trend views
- Both views update when filter changes
- Empty state shown when no data
</acceptance_criteria>
</task>

</tasks>

<verification>
- `npm run typecheck`
- `npm test -- --run src/app/stats/**/*.test.ts` (if tests exist)
- Manual check:
  1. Navigate to /stats with districts selected
  2. Verify overview cards show computed stats
  3. Verify crime type chart shows top 10 types
  4. Verify temporal heatmap shows hour/day distribution
  5. Toggle to trend view, verify monthly line chart
  6. Change districts, verify charts update
  7. Change time range, verify charts update
</verification>

<success_criteria>
- Overview cards show accurate computed statistics
- Crime type chart displays top 10 types with percentages
- Temporal heatmap shows hour/day distribution pattern
- Monthly trend line shows crime count over time
- All charts are interactive with tooltips
- Charts update reactively when filters change
</success_criteria>

<risks>
- **Visx complexity:** Custom heatmap may be complex to implement
  - Mitigation: Use simple SVG grid if Visx heatmap unavailable
- **Performance risk:** Re-rendering charts on every filter change
  - Mitigation: Memoize chart data transformations
</risks>

<rollback>
- Revert to Plan 1 state (route exists but charts are empty)
- Charts are self-contained components, easy to remove
</rollback>

<output>
After completion, create `.planning/phases/59-add-stats-page/59-02-SUMMARY.md`
</output>
