---
phase: 66-full-integration-testing
plan: 01
type: execute
wave: 1
depends_on: []
requirements_addressed: [FLOW-01, FLOW-02, FLOW-03, FLOW-05]
files_modified:
  - src/app/dashboard-v2/page.stkde.test.ts
  - src/store/useCoordinationStore.test.ts
  - src/store/useStkdeStore.test.ts
  - src/app/dashboard-v2/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can execute generate -> review -> apply inside dashboard-v2 without broken transitions"
    - "User can refine after apply without desynchronizing timeline/map/cube state"
    - "Cross-panel synchronization status reflects real mismatch/error states"
    - "Empty, low-confidence, and error paths surface understandable feedback"
  artifacts:
    - path: "src/app/dashboard-v2/page.stkde.test.ts"
      provides: "Route-level integration assertions for dashboard-v2 workflow and STKDE wiring"
      contains: "DashboardV2Page, setWorkflowPhase, setSyncStatus, commitSelection"
    - path: "src/store/useCoordinationStore.test.ts"
      provides: "Deterministic synchronization contract checks"
      contains: "partial sync, no-match, workflow phase transitions"
    - path: "src/store/useStkdeStore.test.ts"
      provides: "STKDE stale/error/default behavior regression checks"
      contains: "markStale, run lifecycle, params clamping"
  key_links:
    - from: "src/app/dashboard-v2/page.tsx"
      to: "src/store/useCoordinationStore.ts"
      via: "setWorkflowPhase + setSyncStatus effects"
      pattern: "setWorkflowPhase\(|setSyncStatus\("
    - from: "src/app/dashboard-v2/page.tsx"
      to: "src/store/useTimeslicingModeStore.ts"
      via: "generation status / pending bins selectors"
      pattern: "generationStatus|pendingGeneratedBins|generationError"
---

<objective>
Codify blocker-journey hardening as deterministic automated checks and apply only minimal fixes required to pass those checks.

Purpose: Phase 66 is a lightweight gate, so confidence must come from reliable regression signals rather than broad feature rework.
Output: Updated integration/store tests plus targeted dashboard-v2 hardening changes that close FLOW-01/FLOW-02/FLOW-03/FLOW-05 risks.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-full-integration-testing/66-CONTEXT.md
@.planning/phases/66-full-integration-testing/66-RESEARCH.md
@.planning/phases/66-full-integration-testing/66-QA-CHECKLIST-timeslicing-binning-quality.md
@.planning/phases/65-stkde-integration/65-03-SUMMARY.md
@src/app/dashboard-v2/page.tsx
@src/app/dashboard-v2/page.stkde.test.ts
@src/store/useCoordinationStore.test.ts
@src/store/useStkdeStore.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand dashboard-v2 blocker-journey integration tests</name>
  <read_first>
    - src/app/dashboard-v2/page.stkde.test.ts
    - src/app/dashboard-v2/page.tsx
    - .planning/phases/66-full-integration-testing/66-QA-CHECKLIST-timeslicing-binning-quality.md
  </read_first>
  <files>src/app/dashboard-v2/page.stkde.test.ts</files>
  <action>
Add route-level tests that prove, at minimum:
- workflow phase moves to `review` when `pendingGeneratedBins.length > 0`, `applied` when generated slices exist, and `refine` when mode is manual with applied slices;
- sync status enters `partial` when `generationError` exists or panel no-match metadata is present;
- hotspot selection commits through `setTimeRange`, `setSpatialBounds`, and `commitSelection` once per selected hotspot id;
- mismatch copy uses the exact string: `Hotspot time window is an investigative overlay; applied slices remain the workflow source of truth.`

Do not add snapshot-only tests; assert on explicit calls/strings/branch behavior.
  </action>
  <acceptance_criteria>
    - src/app/dashboard-v2/page.stkde.test.ts contains tests that assert review/applied/refine workflow transitions
    - src/app/dashboard-v2/page.stkde.test.ts contains assertion for exact investigative overlay mismatch copy
    - `pnpm vitest src/app/dashboard-v2/page.stkde.test.ts --run` exits 0
  </acceptance_criteria>
  <verify>pnpm vitest src/app/dashboard-v2/page.stkde.test.ts --run</verify>
  <done>Dashboard blocker-journey transitions and mismatch semantics are regression-protected.</done>
</task>

<task type="auto">
  <name>Task 2: Harden synchronization + edge-state contracts with store-level tests</name>
  <read_first>
    - src/store/useCoordinationStore.test.ts
    - src/store/useStkdeStore.test.ts
    - src/store/useCoordinationStore.ts
    - src/store/useStkdeStore.ts
  </read_first>
  <files>src/store/useCoordinationStore.test.ts, src/store/useStkdeStore.test.ts</files>
  <action>
Add/adjust deterministic tests to lock:
- panel-local no-match behavior preserving global selection while setting `syncStatus.status === 'partial'`;
- `clearSelection(reason)` retaining the invalidation reason text for status-strip surfacing;
- STKDE stale signaling after success (`markStale('applied-slices-updated')`) without wiping previous result;
- invalid parameter clamping behavior for out-of-range numeric inputs.

If tests expose a real blocker in production code, apply the smallest fix in `src/app/dashboard-v2/page.tsx` required to satisfy FLOW-03/FLOW-05 without broad refactor.
  </action>
  <acceptance_criteria>
    - src/store/useCoordinationStore.test.ts contains explicit partial-sync/no-match assertions
    - src/store/useStkdeStore.test.ts contains stale-after-success and clamping assertions
    - `pnpm vitest src/store/useCoordinationStore.test.ts src/store/useStkdeStore.test.ts --run` exits 0
  </acceptance_criteria>
  <verify>pnpm vitest src/store/useCoordinationStore.test.ts src/store/useStkdeStore.test.ts --run</verify>
  <done>Cross-panel sync and edge-state invariants are enforced by deterministic store tests.</done>
</task>

<task type="auto">
  <name>Task 3: Run phase hardening safety loop and capture residual blockers</name>
  <read_first>
    - .planning/phases/66-full-integration-testing/66-VALIDATION.md
    - src/app/dashboard-v2/page.stkde.test.ts
    - src/store/useCoordinationStore.test.ts
    - src/store/useStkdeStore.test.ts
  </read_first>
  <files>.planning/phases/66-full-integration-testing/66-VALIDATION.md</files>
  <action>
Execute in order:
1. `pnpm -s tsc --noEmit`
2. `pnpm vitest src/app/dashboard-v2/page.stkde.test.ts src/store/useCoordinationStore.test.ts src/store/useStkdeStore.test.ts --run`
3. `pnpm vitest --run`

Then update `66-VALIDATION.md` Per-Task Verification Map status cells for 66-01-01, 66-01-02, and 66-01-03 to `✅ green` only if commands pass; if anything fails, record failure briefly in the same row and keep status non-green.
  </action>
  <acceptance_criteria>
    - `pnpm -s tsc --noEmit` exits 0
    - targeted vitest command exits 0
    - `pnpm vitest --run` exits 0
    - 66-VALIDATION.md rows 66-01-01..66-01-03 are updated with current status
  </acceptance_criteria>
  <verify>pnpm -s tsc --noEmit && pnpm vitest src/app/dashboard-v2/page.stkde.test.ts src/store/useCoordinationStore.test.ts src/store/useStkdeStore.test.ts --run && pnpm vitest --run</verify>
  <done>Automated hardening checks are executed and validation status is explicitly recorded.</done>
</task>

</tasks>

<verification>
1. FLOW-01/FLOW-02 transitions are asserted in route-level tests.
2. FLOW-03 synchronization/no-match contracts are asserted in store and route tests.
3. FLOW-05 edge-state semantics are asserted via partial-status and mismatch-copy checks.
4. Type-check + targeted tests + full suite are green, with status reflected in 66-VALIDATION.md.
</verification>

<success_criteria>
- Blocker journeys are encoded as deterministic tests instead of ad-hoc manual checks.
- Any code changes are minimal and directly tied to failing hardening assertions.
- Phase validation rows for Plan 66-01 show explicit green/non-green status.
</success_criteria>

<output>
After completion, create `.planning/phases/66-full-integration-testing/66-01-SUMMARY.md`
</output>
