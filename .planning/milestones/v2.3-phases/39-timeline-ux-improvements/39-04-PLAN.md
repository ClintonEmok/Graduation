---
phase: 39-timeline-ux-improvements
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/timeline/DualTimeline.tsx
  - src/components/timeline/TimelineContainer.tsx
  - src/store/useSliceStore.ts
autonomous: true

must_haves:
  truths:
    - "Loading indicator shows on timeline during data fetch"
    - "Empty state message displays when selected range has no data"
    - "Multi-slice overlap has better visual differentiation"
    - "Selected slice/time is more prominently highlighted"
  artifacts:
    - path: "src/components/timeline/DualTimeline.tsx"
      provides: "Loading and empty states"
    - path: "src/store/useSliceStore.ts"
      provides: "Selection highlight state"
  key_links:
    - from: "DualTimeline.tsx"
      to: "useViewportCrimeData"
      via: "isLoading"
---

<objective>
Add loading/empty states and polish slice selection visuals.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/components/timeline/DualTimeline.tsx
@src/components/timeline/TimelineContainer.tsx
@src/store/useSliceStore.ts
</context>

<tasks>

<task type="auto">
  <name>Add loading indicator to timeline</name>
  <files>src/components/timeline/DualTimeline.tsx</files>
  <action>
    Add a loading spinner or skeleton state to the timeline:
    - Show when useViewportCrimeData isLoading
    - Use a subtle spinner or shimmer effect
    - Position: center of timeline area
    - Don't block interaction if possible
  </action>
  <verify>Navigate to /timeline-test, verify loading indicator appears during initial data fetch</verify>
  <done>Loading indicator visible during data fetch</done>
</task>

<task type="auto">
  <name>Add empty state for no data</name>
  <files>src/components/timeline/DualTimeline.tsx</files>
  <action>
    Add empty state message when selected brush range has no crime data:
    - Show "No data in this range" message
    - Include suggestion to expand the range or adjust filters
    - Style with muted text, centered in timeline area
  </action>
  <verify>Select a brush range with no data, verify empty state message appears</verify>
  <done>Empty state message displays when range has no data</done>
</task>

<task type="auto">
  <name>Improve multi-slice overlap visualization</name>
  <files>src/store/useSliceStore.ts, src/components/timeline/DualTimeline.tsx</files>
  <action>
    Enhance visual differentiation for overlapping slices:
    - Add opacity variation for overlapping slices (reduce opacity of underlying slices)
    - Add hatching pattern or border style variation
    - Show overlap count badge when 3+ slices overlap
    - Read slices from useSliceStore and check for overlaps
  </action>
  <verify>Create 3 overlapping slices, verify overlap visualization is clearer</verify>
  <done>Overlapping slices have enhanced visual differentiation</done>
</task>

<task type="auto">
  <name>Enhance selection highlight</name>
  <files>src/components/timeline/DualTimeline.tsx</files>
  <action>
    Make selected slice/time more prominent:
    - Add stronger border/glow on selected slice
    - Add subtle pulse animation on selection
    - Ensure selected slice is always brought to front (z-index)
  </action>
  <verify>Select a slice, verify it has enhanced highlight</verify>
  <done>Selected slice has prominent visual highlight</done>
</task>

</tasks>

<verification>
- Loading state shows during fetch
- Empty state shows when no data
- Overlapping slices visually distinct
- Selection highlight enhanced
</verification>

<success_criteria>
- User understands timeline state at all times
- User can easily identify selected and overlapping elements
</success_criteria>

<output>
After completion, create .planning/phases/39-timeline-ux-improvements/39-04-SUMMARY.md
</output>
