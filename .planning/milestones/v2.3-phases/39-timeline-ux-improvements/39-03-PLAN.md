---
phase: 39-timeline-ux-improvements
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/timeline/AdaptiveControls.tsx
  - src/components/timeline/DualTimeline.tsx
autonomous: true

must_haves:
  truths:
    - "Warp factor slider shows real-time preview of time distortion"
    - "Time cursor is more visually prominent"
  artifacts:
    - path: "src/components/timeline/AdaptiveControls.tsx"
      provides: "Warp preview on slider change"
    - path: "src/components/timeline/DualTimeline.tsx"
      provides: "Enhanced time cursor rendering"
  key_links:
    - from: "AdaptiveControls"
      to: "useAdaptiveStore"
      via: "warpFactor"
    - from: "DualTimeline.tsx"
      to: "useTimeStore"
      via: "currentTime"
---

<objective>
Add warp preview feedback and enhance time cursor visibility.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/components/timeline/AdaptiveControls.tsx
@src/components/timeline/DualTimeline.tsx
</context>

<tasks>

<task type="auto">
  <name>Add warp factor real-time preview</name>
  <files>src/components/timeline/AdaptiveControls.tsx</files>
  <action>
    In AdaptiveControls, when warpFactor slider changes:
    - Debounce at 50ms for smooth preview
    - Show mini density preview or axis distortion indicator next to slider value
    - Or add a small "previewing..." indicator that appears during drag
    - The preview should show how the axis will warp without committing
  </action>
  <verify>Drag warp factor slider, verify preview updates in real-time</verify>
  <done>Warp factor shows real-time preview during slider drag</done>
</task>

<task type="auto">
  <name>Enhance time cursor visibility</name>
  <files>src/components/timeline/DualTimeline.tsx</files>
  <action>
    Improve the time cursor line visibility in DualTimeline:
    - Increase stroke width from current to 2px
    - Add subtle glow effect (filter: drop-shadow)
    - Use a more prominent color (e.g., emerald-500 instead of current)
    - Add a small circular handle at the top for easier grabbing
    - Read currentTime from useTimeStore
  </action>
  <verify>Navigate to timeline, verify time cursor line is more prominent and has glow effect</verify>
  <done>Time cursor line is visually enhanced with glow and thicker stroke</done>
</task>

</tasks>

<verification>
- Warp factor slider shows live preview
- Time cursor is more visible
</verification>

<success_criteria>
- User sees immediate feedback when adjusting warp
- User can easily spot the current time position
</success_criteria>

<output>
After completion, create .planning/phases/39-timeline-ux-improvements/39-03-SUMMARY.md
</output>
