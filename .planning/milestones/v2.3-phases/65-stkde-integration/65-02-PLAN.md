---
phase: 65-stkde-integration
plan: 02
type: execute
wave: 2
depends_on: [65-01]
files_modified:
  - src/app/dashboard-v2/page.tsx
  - src/components/stkde/DashboardStkdePanel.tsx
  - src/store/useMapLayerStore.ts
  - src/components/map/MapLayerManager.tsx
  - src/components/map/MapVisualization.tsx
  - src/components/map/MapStkdeHeatmapLayer.tsx
autonomous: true
requirements: [STKD-01, STKD-02, STKD-03, STKD-05]
requirements_addressed: [STKD-01, STKD-02, STKD-03, STKD-05]

must_haves:
  truths:
    - "User can toggle STKDE overlay on/off while staying in dashboard-v2"
    - "User can inspect hotspot rows with location, intensity, support, and time window"
    - "User can switch between standard slice exploration and STKDE-enhanced analysis"
  artifacts:
    - path: "src/components/stkde/DashboardStkdePanel.tsx"
      provides: "STKDE controls + hotspot panel for dashboard-v2"
      contains: "Run STKDE button, scope chip, hotspot list fields, stale/provenance messaging"
    - path: "src/store/useMapLayerStore.ts"
      provides: "Shared map layer visibility/opacity state"
      contains: "visibility.stkde and opacity.stkde"
    - path: "src/components/map/MapVisualization.tsx"
      provides: "Map rendering with optional STKDE heatmap layer"
      contains: "MapStkdeHeatmapLayer conditional wiring"
  key_links:
    - from: "src/components/map/MapLayerManager.tsx"
      to: "src/store/useMapLayerStore.ts"
      via: "shared visibility and opacity state"
      pattern: "useMapLayerStore"
    - from: "src/components/map/MapVisualization.tsx"
      to: "src/store/useStkdeStore.ts"
      via: "cells + selected hotspot marker render"
      pattern: "MapStkdeHeatmapLayer"
---

<objective>
Integrate STKDE controls, overlay, and hotspot panel into dashboard-v2 map workflow.

Purpose: Deliver in-route STKDE analysis as part of the same slice investigation surface users already use.

Output: dashboard-v2 panel + map overlay wiring with clear mode switching and hotspot context.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-stkde-integration/65-CONTEXT.md
@.planning/phases/65-stkde-integration/65-RESEARCH.md
@src/app/dashboard-v2/page.tsx
@src/components/map/MapVisualization.tsx
@src/components/map/MapLayerManager.tsx
@src/components/map/MapStkdeHeatmapLayer.tsx
@src/app/stkde/lib/HotspotPanel.tsx
@src/store/useStkdeStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dashboard STKDE control panel and hotspot list surface</name>
  <files>src/components/stkde/DashboardStkdePanel.tsx src/app/dashboard-v2/page.tsx</files>
  <read_first>
  - src/app/dashboard-v2/page.tsx
  - src/app/stkde/lib/HotspotPanel.tsx
  - src/app/stkde/lib/StkdeRouteShell.tsx
  - src/store/useStkdeStore.ts
  - src/app/dashboard-v2/hooks/useDashboardStkde.ts
  </read_first>
  <action>
  Create `DashboardStkdePanel.tsx` and mount it in `dashboard-v2/page.tsx` as a route-native panel (not separate page).

  Implement concrete panel content:
  - Header label: `STKDE Investigation`.
  - Scope toggle with two explicit options: `Applied Slices` and `Full Viewport`.
  - Persistent scope indicator chip showing current scope text exactly.
  - Numeric inputs for `spatialBandwidthMeters`, `temporalBandwidthHours`, `gridCellMeters`, `topK`, `minSupport`, `timeWindowHours`.
  - Primary button label `Run STKDE` and secondary button `Cancel` while running.
  - Status row showing `computing…` during run plus provenance summary (`requested=`, `effective=`, `truncated`, `fallback=`, `clamps=`) after run.
  - Stale warning copy including phrase `applied slices changed — rerun STKDE` when store marks stale.
  - Hotspot rows with mandatory fields: `Location`, `Intensity`, `Support`, `Time window`.

  In `dashboard-v2/page.tsx`, add panel toggle control named `STKDE` and render panel in the right-side stack with existing refinement/layers semantics.
  </action>
  <acceptance_criteria>
  - `src/components/stkde/DashboardStkdePanel.tsx` contains literal text `STKDE Investigation` and `Run STKDE`.
  - `src/components/stkde/DashboardStkdePanel.tsx` contains literal scope labels `Applied Slices` and `Full Viewport`.
  - `src/components/stkde/DashboardStkdePanel.tsx` hotspot row template contains `Location`, `Intensity`, `Support`, and `Time window`.
  - `src/app/dashboard-v2/page.tsx` contains a panel toggle labeled `STKDE` and renders `DashboardStkdePanel`.
  </acceptance_criteria>
  <verify>pnpm -s tsc --noEmit</verify>
  <done>Users can control STKDE runs and inspect hotspot context directly inside dashboard-v2.</done>
</task>

<task type="auto">
  <name>Task 2: Wire shared layer state and STKDE heatmap rendering into map view</name>
  <files>src/store/useMapLayerStore.ts src/components/map/MapLayerManager.tsx src/components/map/MapVisualization.tsx src/components/map/MapStkdeHeatmapLayer.tsx</files>
  <read_first>
  - src/components/map/MapLayerManager.tsx
  - src/components/map/MapVisualization.tsx
  - src/components/map/MapStkdeHeatmapLayer.tsx
  - src/store/useStkdeStore.ts
  - src/store/useFilterStore.ts
  </read_first>
  <action>
  Replace local-only map layer toggles with one shared source of truth and render STKDE overlay from dashboard store data.

  Implement concrete wiring:
  1. Create `useMapLayerStore.ts` with persisted `visibility` and `opacity` including `stkde` key (`visibility.stkde` default `false`, `opacity.stkde` default `0.6`).
  2. Update `MapLayerManager` to read/write from `useMapLayerStore` (remove component-local visibility/opacity state).
  3. Update `MapVisualization` to conditionally render `MapStkdeHeatmapLayer` when `visibility.stkde === true` and STKDE response has cells.
  4. Pass `opacity.stkde` into heatmap layer and apply it to `heatmap-opacity` instead of fixed `0.85`.
  5. Keep existing event/heatmap/cluster/trajectory rendering intact (STKDE is additive, not replacement).

  Also add mode badge in map overlay UI:
  - `Mode: Standard` when STKDE layer is hidden
  - `Mode: STKDE Enhanced` when STKDE layer is visible
  </action>
  <acceptance_criteria>
  - `src/store/useMapLayerStore.ts` exists and contains `visibility.stkde` and `opacity.stkde`.
  - `src/components/map/MapLayerManager.tsx` imports `useMapLayerStore` and does not define local `useState` for visibility/opacity.
  - `src/components/map/MapVisualization.tsx` imports and renders `MapStkdeHeatmapLayer` behind `visibility.stkde` condition.
  - `src/components/map/MapStkdeHeatmapLayer.tsx` receives and applies configurable opacity prop (no fixed `0.85`).
  - `src/components/map/MapVisualization.tsx` contains literal strings `Mode: Standard` and `Mode: STKDE Enhanced`.
  </acceptance_criteria>
  <verify>pnpm -s tsc --noEmit && pnpm vitest src/app/dashboard-v2/page.stkde.test.ts --run</verify>
  <done>STKDE overlay is toggleable and integrated with map rendering inside unified dashboard-v2 workflow.</done>
</task>

</tasks>

<verification>
1. STKDE controls and hotspots render in dashboard-v2.
2. Layer manager and map share one STKDE visibility/opacity state.
3. Users can switch between standard and STKDE-enhanced map analysis without leaving route.
</verification>

<success_criteria>
- STKD-01: KDE overlay is available during slice-based investigation.
- STKD-02: Hotspot panel provides contextual hotspot details.
- STKD-03: Heatmap is integrated inside dashboard-v2.
- STKD-05: User can switch analysis mode via STKDE layer toggle and panel controls.
</success_criteria>

<output>
After completion, create `.planning/phases/65-stkde-integration/65-02-SUMMARY.md`
</output>
