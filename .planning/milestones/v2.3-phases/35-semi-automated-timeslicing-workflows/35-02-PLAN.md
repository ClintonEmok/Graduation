---
phase: 35-semi-automated-timeslicing-workflows
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Side panel opens and displays suggestions list"
    - "Each suggestion card shows type, confidence percentage, and action buttons"
    - "Empty state displays helpful message"
  artifacts:
    - path: "src/app/timeslicing/components/SuggestionPanel.tsx"
      provides: "Side panel component for displaying suggestions"
    - path: "src/app/timeslicing/components/SuggestionCard.tsx"
      provides: "Individual suggestion card component"
    - path: "src/app/timeslicing/components/ConfidenceBadge.tsx"
      provides: "Confidence percentage display"
---

<objective>
Build suggestion panel UI components - side panel, suggestion cards, confidence display.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/35-semi-automated-timeslicing-workflows/35-CONTEXT.md
@.planning/phases/35-semi-automated-timeslicing-workflows/35-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create SuggestionPanel side panel component</name>
  <files>src/app/timeslicing/components/SuggestionPanel.tsx</files>
  <action">
Create SuggestionPanel component at src/app/timeslicing/components/SuggestionPanel.tsx:
- Fixed right-side panel (following ContextualSlicePanel pattern: fixed right-0, h-full, w-80)
- Panel header with title "Suggestions" and close button
- Body with scrollable suggestions list
- Import useSuggestionStore to get suggestions and isPanelOpen state
- Render list of SuggestionCard components
- Include empty state: "No suggestions yet. Click 'Generate' to create suggestions."
- Toggle panel open/close based on isPanelOpen state from store
- Use @/components/ui patterns for styling (border, shadow, scroll-area)
  </action>
  <verify>Panel renders with header, list area, and responds to isPanelOpen state</verify>
  <done>Side panel component renders correctly with proper styling</done>
</task>

<task type="auto">
  <name>Create ConfidenceBadge component</name>
  <files>src/app/timeslicing/components/ConfidenceBadge.tsx</files>
  <action">
Create ConfidenceBadge component:
- Display numerical percentage: "{confidence}% confidence"
- Per CONTEXT.md: NOT color coded, NOT verbal labels - just numerical
- Clean typography: percentage in font-medium, "confidence" in muted text
- Accept confidence as prop (0-100 number)
- Example: <ConfidenceBadge confidence={87} /> renders "87% confidence"
  </action>
  <verify>Component renders "87% confidence" format correctly</verify>
  <done>ConfidenceBadge displays numerical percentage as specified</done>
</task>

<task type="auto">
  <name>Create SuggestionCard component</name>
  <files>src/app/timeslicing/components/SuggestionCard.tsx</files>
  <action">
Create SuggestionCard component:
- Props: suggestion (from useSuggestionStore Suggestion type)
- Display: type (formatted: "warp-profile" → "Warp Profile"), confidence via ConfidenceBadge
- Show suggestion data details (JSON or formatted based on type)
- Include Accept/Modify/Reject button group:
  - Accept: variant="default" (primary), with Check icon
  - Modify: variant="secondary", with Pencil icon  
  - Reject: variant="ghost" or "outline" with X icon
- Handle pending/accepted/rejected states (show actions only when pending)
- On click: set active suggestion in store
- Wire up acceptSuggestion, rejectSuggestion, modifySuggestion from store
- Use @/components/ui/button for buttons
  </action>
  <verify>Card displays suggestion with all info and buttons are clickable</verify>
  <done>SuggestionCard renders with type, confidence, details, and action buttons</done>
</task>

<task type="auto">
  <name>Integrate SuggestionPanel into timeslicing page</name>
  <files>src/app/timeslicing/page.tsx</files>
  <action">
Update timeslicing page to include SuggestionPanel:
- Import SuggestionPanel component
- Add to page layout (side-by-side with timeline)
- Ensure panel is positioned correctly (right side, full height)
- Timeline takes remaining width
- Layout should be: [Timeline Area][Suggestion Panel]
  </action>
  <verify>Page renders with timeline and suggestion panel side by side</verify>
  <done>SuggestionPanel integrated into timeslicing route layout</done>
</task>

</tasks>

<verification>
- [ ] SuggestionPanel renders fixed right-side
- [ ] ConfidenceBadge shows "X% confidence" format
- [ ] SuggestionCard shows type, confidence, actions
- [ ] Accept/Modify/Reject buttons functional
- [ ] Empty state displays when no suggestions
</verification>

<success_criteria>
Side panel with working suggestion cards and confidence display
</success_criteria>

<output>
After completion, create `.planning/phases/35-semi-automated-timeslicing-workflows/35-02-SUMMARY.md`
</output>
