---
phase: 56-variable-sampling-selection-fidelity
plan: 3
type: execute
wave: 3
depends_on: [56-01, 56-02]
files_modified:
  - src/app/api/crimes/range/route.test.ts
  - src/app/timeslicing-algos/page.timeline-algos.test.ts
  - scripts/capture-refactor-baseline.mjs
  - .planning/baselines/56-variable-sampling-baseline.md
autonomous: true

must_haves:
  truths:
    - "Variable sampling behavior is regression-protected across API and `/timeslicing-algos` integration paths"
    - "Performance guardrails exist for selection-detail requests so high-fidelity intent does not silently degrade route responsiveness"
    - "Rollback path is documented and executable if selection-detail policy causes latency or payload pressure"
  artifacts:
    - path: ".planning/baselines/56-variable-sampling-baseline.md"
      provides: "Recorded baseline/guardrail expectations for variable sampling API and selection-detail route behavior"
      contains: "guardrails"
    - path: "src/app/api/crimes/range/route.test.ts"
      provides: "Regression assertions for clamped targets and policy provenance stability"
      contains: "targetPoints"
    - path: "src/app/timeslicing-algos/page.timeline-algos.test.ts"
      provides: "Regression assertions for fallback stability under capped/sampled selection responses"
      contains: "fallback"
  key_links:
    - from: "src/app/api/crimes/range/route.test.ts"
      to: "src/app/api/crimes/range/route.ts"
      via: "tests lock clamping + provenance behavior for variable sampling requests"
      pattern: "effectiveTargetPoints"
    - from: "src/app/timeslicing-algos/page.timeline-algos.test.ts"
      to: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      via: "tests lock fallback/provenance UI under high-density selection scenarios"
      pattern: "Selection fetch"
---

<objective>
Finalize variable sampling rollout with explicit regression hardening, baseline checks, and guardrailed rollback instructions.

Purpose: High-fidelity selection detail must remain safe under dense workloads; this plan adds guardrails and evidence so rollout can be trusted and quickly reverted if needed.
Output: Cross-layer regression coverage, baseline artifact for performance expectations, and executable rollback notes.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-variable-sampling-selection-fidelity/56-01-SUMMARY.md
@.planning/phases/56-variable-sampling-selection-fidelity/56-02-SUMMARY.md
@src/app/api/crimes/range/route.test.ts
@src/app/timeslicing-algos/page.timeline-algos.test.ts
@scripts/capture-refactor-baseline.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden dense-selection regression cases across API + route</name>
  <files>src/app/api/crimes/range/route.test.ts, src/app/timeslicing-algos/page.timeline-algos.test.ts</files>
  <action>Add focused regression scenarios for high-density requests: oversized `targetPoints` clamping, missing/invalid sampling intent fallback, sampled/full provenance consistency, and selection-detail fallback stability when policy caps trigger. Assert explicit provenance fields remain present and meaningful. Keep tests deterministic using mocked query totals and route responses.</action>
  <verify>`npm test -- --run src/app/api/crimes/range/route.test.ts src/app/timeslicing-algos/page.timeline-algos.test.ts` passes.</verify>
  <done>Dense selection edge cases are guarded end-to-end by deterministic tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add baseline capture/guardrail documentation for variable sampling rollout</name>
  <files>scripts/capture-refactor-baseline.mjs, .planning/baselines/56-variable-sampling-baseline.md</files>
  <action>Extend baseline capture workflow (or add a focused branch in existing script) to record key signals for this rollout: API returned count vs total matches under context and selection-detail intents, effective target points/clamp state, and route-level selection fallback frequency during scripted checks. Write a baseline markdown artifact with acceptable ranges/thresholds and commands to re-run checks. Keep this confined to planning/baselines instrumentation, not dashboard behavior.</action>
  <verify>`node scripts/capture-refactor-baseline.mjs --write` produces updated baseline output and `.planning/baselines/56-variable-sampling-baseline.md` documents thresholds + rerun commands.</verify>
  <done>Variable sampling rollout has measurable guardrails and repeatable baseline evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Document executable rollback switches and verification flow in plan summary output</name>
  <files>.planning/baselines/56-variable-sampling-baseline.md</files>
  <action>Add an explicit rollback checklist in the baseline doc: disable intent-aware selection target override, revert to legacy limit-only mode, verify provenance labels still render, and rerun targeted tests. Include exact commands and expected pass signals so rollback can be executed quickly without interpretation.</action>
  <verify>Checklist includes concrete commands for rollback + verification and is consistent with existing test script names.</verify>
  <done>Rollback path for variable sampling is documented, concrete, and test-validated.</done>
</task>

</tasks>

<verification>
- `npm test -- --run src/app/api/crimes/range/route.test.ts src/app/timeslicing-algos/page.timeline-algos.test.ts`
- `npm run typecheck`
- `node scripts/capture-refactor-baseline.mjs --write`
- Confirm `.planning/baselines/56-variable-sampling-baseline.md` includes: thresholds, rerun commands, rollback checklist.
</verification>

<success_criteria>
- Regression tests cover dense selection + provenance edge cases across API and route integration.
- Performance guardrail baseline exists for variable sampling rollout.
- Rollback steps are explicit, executable, and verified by commands.
- Dashboard code remains untouched.
</success_criteria>

<risks>
- Baseline signals may be noisy if capture scripts are not sufficiently deterministic.
- Over-constrained thresholds could create false alarms in CI/local environments.
- Missing rollback validation could lead to partial rollback states.
</risks>

<rollback>
- Revert baseline-script additions if they introduce instability and keep manual checklist only.
- Temporarily disable variable target semantics in route while preserving legacy `limit` behavior.
- Re-run targeted tests to confirm compatibility after rollback.
</rollback>

<output>
After completion, create `.planning/phases/56-variable-sampling-selection-fidelity/56-03-SUMMARY.md`
</output>
