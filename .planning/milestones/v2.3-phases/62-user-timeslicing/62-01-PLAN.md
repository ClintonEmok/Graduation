---
phase: 62-user-timeslicing
plan: 01
type: execute
wave: 1
depends_on: [61-01]
files_modified:
  - src/store/useTimeslicingModeStore.ts
  - src/components/timeslicing/TimesliceToolbar.tsx
  - src/components/timeslicing/ManualTimesliceEditor.tsx
  - src/app/timeslicing/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can toggle between auto and manual timeslicing modes"
    - "User can create slices manually via click-drag on timeline"
    - "User can adjust slice boundaries by dragging handles"
    - "User can apply preset configurations (hourly, daily, weekly)"
    - "User can save and load custom slice templates"
    - "Manual mode persists user-created slices across sessions"
  artifacts:
    - path: "src/store/useTimeslicingModeStore.ts"
      provides: "State management for manual/auto mode toggle and slice operations"
      contains: "mode, presets, templates, createSlice, updateSlice, deleteSlice"
    - path: "src/components/timeslicing/TimesliceToolbar.tsx"
      provides: "UI for mode toggle, preset selection, template management"
      contains: "ModeToggle, PresetDropdown, TemplateButtons, ClearSlices"
    - path: "src/components/timeslicing/ManualTimesliceEditor.tsx"
      provides: "Interactive timeline editor for manual slice creation"
      contains: "DragHandle, SliceBody, CreatePreview, BoundarySnap"
---

<objective>
Implement user-driven timeslicing with manual mode, presets, and templates.

Purpose: Enable users to manually control time slice creation and adjustment instead of relying solely on auto-computed slices.
Output: Complete manual timeslicing workflow with UI controls.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/store/useTimeslicingModeStore.ts
@src/components/timeline/DualTimeline.tsx
@src/store/useSliceStore.ts
@.planning/phases/61-dynamic-binning-system/61-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend timeslicing mode store with manual controls</name>
  <files>src/store/useTimeslicingModeStore.ts</files>
  <action>
Extend the timeslicing mode store:
- Add 'mode': 'auto' | 'manual' | 'hybrid' state
- Add 'presets': predefined configurations (hourly, daily, weekly, weekday/weekend, business-hours, night-only)
- Add 'templates': user-saved slice configurations with name/description
- Add actions: setMode(), applyPreset(), saveTemplate(), loadTemplate(), deleteTemplate()
- Add 'activeSliceIds': currently selected slices in manual mode
- Add 'sliceSnapInterval': snap-to-grid interval in seconds (default: 300 = 5min)
- Integrate with useSliceStore for slice CRUD operations
  </action>
  <verify>Store state transitions correctly between modes</verify>
  <done>Store provides complete manual mode state management</done>
</task>

<task type="auto">
  <name>Task 2: Build TimesliceToolbar component</name>
  <files>src/components/timeslicing/TimesliceToolbar.tsx</files>
  <action>
Create comprehensive toolbar:
- Mode toggle switch (Auto/Manual/Hybrid) with clear visual indicator
- Preset selector dropdown with preview descriptions
- Quick template buttons (save current, load saved, clear all)
- Slice count badge showing current slice count
- Undo/Redo buttons for slice operations
- Help tooltip explaining manual mode workflow
- Responsive design for mobile/tablet/desktop
  </action>
  <verify>Toolbar renders correctly and mode toggle works</verify>
  <done>Toolbar provides complete timeslicing control surface</done>
</task>

<task type="auto">
  <name>Task 3: Implement ManualTimesliceEditor</name>
  <files>src/components/timeslicing/ManualTimesliceEditor.tsx</files>
  <action>
Build interactive timeline editor:
- Draggable slice handles for start/end boundary adjustment
- Slice body drag to move entire slice in time
- Double-click on empty timeline area to create new slice
- Visual preview overlay during creation showing proposed slice
- Snap-to-grid functionality based on sliceSnapInterval
- Multi-select support (shift+click) for batch operations
- Keyboard shortcuts: Delete (remove selected), Arrow keys (nudge boundaries)
- Undo/redo stack for all manual operations
  </action>
  <verify>All drag operations update slice boundaries correctly</verify>
  <done>Editor enables complete manual slice manipulation</done>
</task>

<task type="auto">
  <name>Task 4: Wire timeslicing into /timeslicing route</name>
  <files>src/app/timeslicing/page.tsx</files>
  <action>
Integrate manual timeslicing into main route:
- Add TimesliceToolbar to the control panel area
- Enable manual mode in DualTimeline when mode === 'manual'
- Connect manual slice events to useTimeslicingModeStore
- Add preset application to timeline when preset selected
- Ensure auto-computed slices load when switching back to auto mode
- Persist manual slices to localStorage for session recovery
  </action>
  <verify>Route loads with timeslicing controls, mode toggle works</verify>
  <done>/timeslicing route supports both auto and manual timeslicing</done>
</task>

</tasks>

<verification>
1. Mode toggle switches between auto/manual/hybrid
2. Presets apply correct slice configuration to timeline
3. Manual editor allows creating slices via double-click
4. Drag handles adjust slice boundaries
5. Templates can be saved and loaded
6. Manual slices persist across page refreshes
</verification>

<success_criteria>
- User can switch to manual mode and see empty timeline ready for creation
- User can double-click to create a new slice
- User can drag slice boundaries to resize
- User can apply hourly/daily/weekly presets
- User can save custom template and reload it later
- Auto mode continues to work as before when toggled back
</success_criteria>

<output>
After completion, create `.planning/phases/62-user-timeslicing/{phase}-01-SUMMARY.md`
</output>
