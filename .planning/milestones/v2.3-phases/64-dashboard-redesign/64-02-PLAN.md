---
phase: 64-dashboard-redesign
plan: 02
type: execute
wave: 2
depends_on: [64-01]
files_modified:
  - src/app/dashboard-v2/page.tsx
  - src/components/dashboard/DashboardHeader.tsx
  - src/store/useLayoutStore.ts
autonomous: false
requirements: [SYNC-01, SYNC-02, SYNC-03, SYNC-04, SYNC-05]
requirements_addressed: [SYNC-01, SYNC-02, SYNC-03, SYNC-04, SYNC-05]

must_haves:
  truths:
    - "Applied slices are visible in dashboard-v2 timeline and map/heatmap context without route switching"
    - "dashboard-v2 includes a synchronized 3D cube that reflects the same selection context"
    - "dashboard-v2 communicates workflow state and sync state clearly at route level"
    - "Users can see strategy, granularity, and active slice context at a glance"
  artifacts:
    - path: "src/app/dashboard-v2/page.tsx"
      provides: "Unified dashboard-v2 composition with timeline, map/heatmap, and cube tied to shared coordination state"
      contains: "DashboardHeader usage, sync status surface, cube panel integration"
    - path: "src/components/dashboard/DashboardHeader.tsx"
      provides: "At-a-glance workflow summary (phase, strategy, granularity, active slice set)"
      contains: "workflow/sync badges and applied slice summary"
    - path: "src/store/useLayoutStore.ts"
      provides: "Panel visibility persistence including cube panel"
      contains: "cube visibility state and toggle actions"
  key_links:
    - from: "DashboardHeader"
      to: "useCoordinationStore + useTimeslicingModeStore + useBinningStore"
      via: "single summary bar for workflow/sync/strategy/granularity"
    - from: "dashboard-v2 page"
      to: "MapVisualization + DualTimeline + CubeVisualization"
      via: "shared route composition under one layout and one coordination store"
---

<objective>
Wire dashboard-v2 into a genuinely unified synchronized workflow surface.

Purpose: Complete Phase 64’s user-facing behavior so timeline, map/heatmap, cube, and control/status panels reflect one coherent workflow.

Output: dashboard-v2 route with synchronized cross-view composition, persistent panel controls, and clear workflow/sync/status communication.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-dashboard-redesign/64-CONTEXT.md
@.planning/phases/64-dashboard-redesign/64-01-SUMMARY.md
@src/app/dashboard-v2/page.tsx
@src/components/dashboard/DashboardHeader.tsx
@src/store/useLayoutStore.ts
@src/components/map/MapVisualization.tsx
@src/components/viz/CubeVisualization.tsx
@src/components/timeline/DualTimeline.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate route-level workflow/sync/status header for dashboard-v2</name>
  <files>src/components/dashboard/DashboardHeader.tsx src/app/dashboard-v2/page.tsx</files>
  <read_first>
  - src/components/dashboard/DashboardHeader.tsx
  - src/app/dashboard-v2/page.tsx
  - src/store/useCoordinationStore.ts
  - src/store/useTimeslicingModeStore.ts
  - src/store/useBinningStore.ts
  </read_first>
  <action>
  Replace the current ad-hoc top bar in `dashboard-v2/page.tsx` with `DashboardHeader` and make `DashboardHeader` the canonical status surface.

  Implement concrete header fields:
  1. Workflow phase badge from coordination store (`generate | review | applied | refine`).
  2. Sync status badge (`syncing | synchronized | partial`) and no-match reason text when partial.
  3. Strategy + granularity summary from binning/timeslicing stores.
  4. Active slice summary (`applied slice count`, draft count if present).

  Keep navigation links minimal and non-primary (dashboard-v2 remains the main route identity).
  </action>
  <acceptance_criteria>
  - `src/components/dashboard/DashboardHeader.tsx` contains all three summary dimensions: workflow, sync, and strategy/granularity.
  - `src/app/dashboard-v2/page.tsx` renders `DashboardHeader` and no longer duplicates a separate conflicting status model.
  - Header text includes literal tokens `generate`, `review`, `applied`, and `refine` in user-visible mapping logic.
  </acceptance_criteria>
  <verify>pnpm -s tsc --noEmit</verify>
  <done>dashboard-v2 has one authoritative workflow/status narrative at the top of the route.</done>
</task>

<task type="auto">
  <name>Task 2: Compose timeline + map/heatmap + cube under shared synchronized layout</name>
  <files>src/app/dashboard-v2/page.tsx src/store/useLayoutStore.ts</files>
  <read_first>
  - src/app/dashboard-v2/page.tsx
  - src/store/useLayoutStore.ts
  - src/components/map/MapVisualization.tsx
  - src/components/viz/CubeVisualization.tsx
  - src/components/timeline/DualTimeline.tsx
  </read_first>
  <action>
  Recompose dashboard-v2 so the center workspace exposes all synchronized analysis views in one route:

  1. Use `MapVisualization` as the map surface (includes heatmap/selection behaviors already wired to coordination store).
  2. Add a cube panel using `CubeVisualization` in the same route layout (not hidden behind another route).
  3. Keep `DualTimeline` visible as timeline context for applied/draft slices.
  4. Extend `useLayoutStore` panel visibility with `cube` toggle and persistence (`layout-storage-v3` schema-compatible update).
  5. Ensure panel toggles can hide/show timeline, refinement, layers, and cube without losing store state.
  </action>
  <acceptance_criteria>
  - `src/app/dashboard-v2/page.tsx` imports and renders both `MapVisualization` and `CubeVisualization`.
  - `src/store/useLayoutStore.ts` contains `cube` in `PanelVisibility` and toggle logic supports it.
  - `src/app/dashboard-v2/page.tsx` retains `DualTimeline` rendering in the unified route.
  - `pnpm -s tsc --noEmit` exits 0 after layout/store updates.
  </acceptance_criteria>
  <verify>pnpm -s tsc --noEmit</verify>
  <done>dashboard-v2 now provides synchronized timeline, map/heatmap, and cube surfaces in one workspace.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Unified dashboard-v2 synchronization flow across header status, timeline, map/heatmap, and cube.</what-built>
  <how-to-verify>
  1. Run `pnpm dev` and open `http://localhost:3000/dashboard-v2`.
  2. Generate draft bins, review them, and apply them.
  3. Confirm header workflow state transitions (generate → review → applied) and sync status updates are visible.
  4. Click/select in timeline and map; verify selection context is reflected without route changes and cube remains in same workspace.
  5. Toggle cube/timeline/layers panels off/on, refresh page, and confirm visibility persistence.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. dashboard-v2 route contains timeline, map/heatmap, and cube in one synchronized surface.
2. Workflow state and sync state are explicit and readable from one header/status source.
3. Panel visibility (including cube) persists across refresh.
</verification>

<success_criteria>
- SYNC-01: Applied slices are clearly represented in timeline context.
- SYNC-02: Map/heatmap context is driven by the same workflow/slice state model.
- SYNC-03: Cube is present in dashboard-v2 and participates in synchronized selection context.
- SYNC-04: Workflow stage communication is explicit and stable.
- SYNC-05: Strategy/granularity/active slice summary is understandable at a glance.
</success_criteria>

<output>
After completion, create `.planning/phases/64-dashboard-redesign/64-02-SUMMARY.md`
</output>
