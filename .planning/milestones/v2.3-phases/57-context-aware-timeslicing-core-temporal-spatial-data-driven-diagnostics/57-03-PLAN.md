---
phase: 57-context-aware-timeslicing-core-temporal-spatial-data-driven-diagnostics
plan: 3
type: execute
wave: 3
depends_on: [57-02]
requirements: []
files_modified:
  - src/app/timeslicing/components/ContextBadge.tsx
  - src/app/timeslicing/components/SuggestionCard.tsx
  - src/app/timeslicing/components/SuggestionPanel.tsx
  - src/app/timeslicing/components/SuggestionPanel.test.tsx
  - src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx
  - src/app/timeslicing-algos/page.timeline-algos.test.ts
autonomous: false

must_haves:
  truths:
    - "Users see compact temporal/spatial context diagnostics and one readable dynamic profile in suggestion UI"
    - "When static and dynamic profiles differ, UI shows a collapsed-by-default comparison section with one concise reason sentence"
    - "Confidence details are hidden by default and only shown when user explicitly toggles them"
    - "If temporal/spatial analysis is partially missing, UI clearly indicates missing sections instead of hiding failure"
  artifacts:
    - path: "src/app/timeslicing/components/SuggestionPanel.tsx"
      provides: "compact diagnostics summary + collapsed comparison + confidence toggle behavior"
      contains: "No strong profile"
    - path: "src/app/timeslicing/components/ContextBadge.tsx"
      provides: "readable dynamic profile badge and weak-signal fallback labels"
      contains: "Signal is weak"
    - path: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      provides: "route-level diagnostics chips aligned with phase-57 semantics"
      contains: "Diagnostics"
  key_links:
    - from: "src/app/timeslicing/components/SuggestionPanel.tsx"
      to: "src/store/useSuggestionStore.ts"
      via: "reads persisted contextDiagnostics metadata for summary/comparison/fallback rendering"
      pattern: "contextDiagnostics"
    - from: "src/app/timeslicing/components/SuggestionCard.tsx"
      to: "src/app/timeslicing/components/ContextBadge.tsx"
      via: "passes dynamic profile + weak-signal context to badge"
      pattern: "smartProfileName|profileName"
---

<objective>
Surface context-aware diagnostics in the route UI with compact defaults and explainable detail-on-demand.

Purpose: Phase 57 is diagnostics visibility and interpretation quality. This plan turns persisted diagnostics metadata into readable, QA-friendly UI behavior.
Output: Suggestion/timeslicing-algos diagnostics UI + regression tests + human verification checkpoint.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-context-aware-timeslicing-core-temporal-spatial-data-driven-diagnostics/57-CONTEXT.md
@.planning/phases/57-context-aware-timeslicing-core-temporal-spatial-data-driven-diagnostics/57-RESEARCH.md
@.planning/phases/57-context-aware-timeslicing-core-temporal-spatial-data-driven-diagnostics/57-02-SUMMARY.md
@src/app/timeslicing/components/SuggestionPanel.tsx
@src/app/timeslicing/components/SuggestionCard.tsx
@src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compact diagnostics rendering and fallback states in suggestion UI</name>
  <files>src/app/timeslicing/components/ContextBadge.tsx, src/app/timeslicing/components/SuggestionCard.tsx, src/app/timeslicing/components/SuggestionPanel.tsx</files>
  <action>Render dynamic diagnostics as compact, readable summaries by default: temporal summary sentence + top 1-3 spatial hotspots + single dynamic profile label. Add plain-language fallback states (`No strong profile`, `Signal is weak`) and explicit missing-section notices for partial temporal/spatial diagnostics. Keep technical IDs out of primary display. Add a collapsed-by-default comparison section showing static vs dynamic result plus one concise reason sentence when they differ.</action>
  <verify>`npm run typecheck` passes and UI code contains collapsed comparison + fallback/missing-section rendering paths wired to metadata.</verify>
  <done>Suggestion UI shows interpretable diagnostics-first output with explicit weak/partial semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Add confidence reveal toggle and route-scoped diagnostics parity checks</name>
  <files>src/app/timeslicing/components/SuggestionPanel.tsx, src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx, src/app/timeslicing/components/SuggestionPanel.test.tsx, src/app/timeslicing-algos/page.timeline-algos.test.ts</files>
  <action>Implement confidence visibility as hidden-by-default with an explicit user toggle in suggestion diagnostics. Ensure route-level diagnostics chips in `/timeslicing-algos` remain compact and consistent with phase semantics (readable labels, explicit fallback/source context, no verbose dumps by default). Add/update tests to assert default-hidden confidence, comparison collapsed default, fallback labels, and route-scoped diagnostics presence.</action>
  <verify>`npm test -- --run src/app/timeslicing/components/SuggestionPanel.test.tsx src/app/timeslicing-algos/page.timeline-algos.test.ts` passes.</verify>
  <done>Confidence/details-on-demand behavior and route diagnostics semantics are regression-guarded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 57 diagnostics UI behavior (compact summaries, weak/partial states, collapsed comparison, confidence toggle)</what-built>
  <how-to-verify>
    1. Open `/timeslicing` and generate suggestions under at least two filter contexts (one strong-signal, one weak/noisy).
    2. Confirm each suggestion shows one readable dynamic profile label and compact temporal/spatial summary (top hotspots only).
    3. Confirm comparison section is collapsed by default; expand it and verify it shows static vs dynamic plus one concise reason sentence when different.
    4. Confirm confidence details are hidden initially and appear only after toggling.
    5. Force/observe a partial diagnostic case (missing temporal or spatial section) and confirm explicit missing notice appears.
    6. Open `/timeslicing-algos` and confirm diagnostics labels remain compact/readable and consistent with fallback/source semantics.
  </how-to-verify>
  <resume-signal>Type "approved" if behavior matches, or provide exact mismatch notes.</resume-signal>
</task>

</tasks>

<verification>
- `npm test -- --run src/app/timeslicing/components/SuggestionPanel.test.tsx src/app/timeslicing-algos/page.timeline-algos.test.ts`
- `npm run typecheck`
- Human verification checkpoint approval
</verification>

<success_criteria>
- Diagnostics are interpretable-by-default (compact, readable, not verbose tables).
- Dynamic profile, weak/no-strong fallback, and partial-missing semantics are visible and explicit.
- Static-vs-dynamic comparison is collapsed by default with deterministic one-sentence reason.
- Confidence is opt-in reveal, not default noise.
</success_criteria>

<output>
After completion, create `.planning/phases/57-context-aware-timeslicing-core-temporal-spatial-data-driven-diagnostics/57-03-SUMMARY.md`
</output>
