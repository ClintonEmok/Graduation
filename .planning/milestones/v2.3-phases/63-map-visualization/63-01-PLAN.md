---
phase: 63-map-visualization
plan: 01
type: execute
wave: 1
depends_on: [62-01]
files_modified:
  - src/app/dashboard-v2/page.tsx
  - src/components/map/MapBase.tsx
  - src/components/map/MapVisualization.tsx
  - src/components/map/MapPoiLayer.tsx
  - src/components/map/MapDistrictLayer.tsx
  - src/components/map/MapLayerManager.tsx
  - src/components/timeline/DualTimeline.tsx
  - src/store/useLayoutStore.ts
autonomous: true

must_haves:
  truths:
    - "dashboard-v2 becomes the user-facing home for v3.0 after Phase 62 foundation work"
    - "Users can review applied slices and manually refine them from dashboard-v2"
    - "Map displays OpenStreetMap tiles as base layer"
    - "POI markers render with category-specific icons and colors"
    - "District boundaries render as GeoJSON polygons"
    - "Layer Manager allows toggling each layer visibility"
    - "Map, timeline, and refinement controls coexist inside dashboard-v2 without splitting the workflow"
  artifacts:
    - path: "src/app/dashboard-v2/page.tsx"
      provides: "Unified Phase 63 route surface for refinement and investigation"
      contains: "workflow shell, map/timeline layout, refinement panels, layer controls"
    - path: "src/components/map/MapBase.tsx"
      provides: "MapLibre base with OSM tile layer"
      contains: "OSMTileLayer, BaseMapConfig"
    - path: "src/components/map/MapPoiLayer.tsx"
      provides: "Points of interest markers and popups"
      contains: "POIMarker, POIPopup, category filtering"
    - path: "src/components/map/MapDistrictLayer.tsx"
      provides: "Chicago district boundary polygons"
      contains: "DistrictPolygon, DistrictPopup, selection highlight"
    - path: "src/components/map/MapLayerManager.tsx"
      provides: "Layer toggle and opacity controls"
      contains: "LayerPanel, LayerToggle, OpacitySlider"
---

<objective>
Turn `dashboard-v2` into the first true unified v3.0 route by combining manual slice refinement, map investigation layers, and timeline context in one surface.

Purpose: Move beyond the Phase 62 `/timeslicing` foundation so users can refine generated slices and investigate them spatially without jumping between separate routes.
Output: A working `dashboard-v2` surface where generated/applied slices can be inspected, refined, and explored with map overlays.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/dashboard-v2/page.tsx
@src/app/timeslicing/page.tsx
@src/components/binning/BinningControls.tsx
@src/app/timeslicing/components/SuggestionToolbar.tsx
@src/components/timeline/DualTimeline.tsx
@src/components/map/MapBase.tsx
@src/components/map/MapVisualization.tsx
@src/lib/poi-data.ts
@.planning/phases/62-user-driven-timeslicing-manual-mode/62-01-SUMMARY.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build dashboard-v2 as the unified Phase 63 shell</name>
  <files>src/app/dashboard-v2/page.tsx src/store/useLayoutStore.ts</files>
  <action>
Create the primary Phase 63 route surface:
- Make `src/app/dashboard-v2/page.tsx` the main home for v3.0 user workflow
- Bring forward the useful Phase 62 workflow pieces needed for review/apply visibility
- Create a layout that can show timeline, map, and refinement controls together
- Add enough layout state to support panel visibility and responsive organization
- Keep the route focused on one coherent workflow instead of route hopping
  </action>
  <verify>`/dashboard-v2` renders as a coherent investigation/refinement workspace</verify>
  <done>dashboard-v2 becomes the Phase 63 working surface</done>
</task>

<task type="auto">
  <name>Task 2: Add manual refinement UX on top of applied/generated slices</name>
  <files>src/app/dashboard-v2/page.tsx src/components/timeline/DualTimeline.tsx</files>
  <action>
Add Phase 63 refinement behavior:
- Show the currently applied slices from Phase 62 clearly inside dashboard-v2
- Allow basic manual refinement actions after generation, such as adjusting or selecting slices for follow-up editing
- Preserve the visual distinction between generated draft context and applied slice state where still relevant
- Make burst-heavy periods easier to inspect in the refinement view
- Ensure the refinement experience feels like an extension of generate -> review -> apply, not a separate tool
  </action>
  <verify>Users can inspect and begin manually refining slices from dashboard-v2</verify>
  <done>dashboard-v2 supports post-generation refinement workflow</done>
</task>

<task type="auto">
  <name>Task 3: Configure map investigation layers for dashboard-v2</name>
  <files>src/components/map/MapBase.tsx src/components/map/MapPoiLayer.tsx src/components/map/MapDistrictLayer.tsx</files>
  <action>
Add spatial context layers needed for investigation:
- Configure OpenStreetMap tiles as the map base layer
- Integrate POI markers with category-specific display and useful hover/click details
- Render district boundaries with hover/select states and readable styling
- Keep the layers performant enough for interactive investigation
  </action>
  <verify>OSM, POI, and district layers render correctly on the map</verify>
  <done>dashboard-v2 map has the main geographic context layers</done>
</task>

<task type="auto">
  <name>Task 4: Add layer management and connect map/timeline workflow inside dashboard-v2</name>
  <files>src/app/dashboard-v2/page.tsx src/components/map/MapVisualization.tsx src/components/map/MapLayerManager.tsx</files>
  <action>
Connect the route into one investigation surface:
- Wire POI, district, and OSM layers into the main map visualization
- Add a Layer Manager with toggles and opacity control for the main overlays
- Place the Layer Manager inside dashboard-v2 near the refinement workflow
- Ensure map and timeline context can be interpreted together while Phase 64 synchronization work remains ahead
- Persist layer visibility preferences where practical
  </action>
  <verify>All layers render inside dashboard-v2 and Layer Manager controls work</verify>
  <done>dashboard-v2 supports unified map investigation controls</done>
</task>

</tasks>

<verification>
1. `/dashboard-v2` loads as the main Phase 63 surface
2. Applied slice context is visible inside dashboard-v2
3. Basic post-generation refinement is possible from dashboard-v2
4. OSM tiles load with attribution
5. POI markers and district boundaries render correctly
6. Layer Manager toggles and opacity controls work
7. The route supports map + timeline investigation without obvious workflow fragmentation
</verification>

<success_criteria>
- dashboard-v2 is clearly the active user-facing route for v3.0 work
- Users can continue from generated/applied slices into refinement inside dashboard-v2
- Map layers provide useful geographic context for investigation
- Layer controls are available without leaving dashboard-v2
- Phase 63 sets up Phase 64 synchronization work cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/63-map-visualization/{phase}-01-SUMMARY.md`
</output>
