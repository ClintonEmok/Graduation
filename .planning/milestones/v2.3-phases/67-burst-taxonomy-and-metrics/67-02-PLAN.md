---
phase: 67-burst-taxonomy-and-metrics
plan: 02
type: execute
wave: 2
depends_on: [67-01]
requirements_addressed: [BQ-02, BQ-03, BQ-04]
files_modified:
  - src/components/binning/BinningControls.tsx
  - src/components/timeline/DualTimeline.tsx
  - src/components/viz/BurstList.tsx
  - src/components/viz/BurstDetails.tsx
  - src/app/timeslicing-algos/page.timeline-algos.test.ts
autonomous: true

must_haves:
  truths:
    - "Users can see burst class and confidence in the review and timeline surfaces without opening devtools"
    - "The same label vocabulary is used in the burst list, burst details, and timeline indicators"
    - "Selecting a burst still focuses the existing slice and does not break the review/apply flow"
    - "Review copy clearly separates class, confidence, and rationale from raw counts"
  artifacts:
    - path: "src/components/binning/BinningControls.tsx"
      provides: "Generation controls with taxonomy legend/chips"
      contains: "burstClass, burstConfidence, burstProvenance"
    - path: "src/components/timeline/DualTimeline.tsx"
      provides: "Timeline indicators and burst propagation using taxonomy-aware windows"
      contains: "prolonged-peak, isolated-spike, valley, neutral"
    - path: "src/components/viz/BurstList.tsx"
      provides: "Burst window list with visible taxonomy labels"
      contains: "Class:, Confidence:, Reason:"
    - path: "src/components/viz/BurstDetails.tsx"
      provides: "Selected burst detail panel with class/confidence/rationale"
      contains: "Why this label, burstConfidence, burstProvenance"
    - path: "src/app/timeslicing-algos/page.timeline-algos.test.ts"
      provides: "Route-level source regression coverage for taxonomy wiring"
      contains: "burstClass, burstConfidence, burstProvenance"
  key_links:
    - from: "src/lib/binning/burst-taxonomy.ts"
      to: "src/components/viz/BurstList.tsx"
      via: "shared taxonomy classifier for displayed burst windows"
      pattern: "classifyBurstWindow|burstClass|burstConfidence"
    - from: "src/components/viz/BurstList.tsx"
      to: "src/components/viz/BurstDetails.tsx"
      via: "shared label vocabulary and selected-window reasoning"
      pattern: "Why this label|burstProvenance|tieBreakReason"
    - from: "src/components/timeline/DualTimeline.tsx"
      to: "src/store/useSliceStore.ts"
      via: "existing useAutoBurstSlices flow remains intact"
      pattern: "useAutoBurstSlices|BurstWindow"
---

<objective>
Surface the burst taxonomy in the actual review workflow so users can read class, confidence, and rationale in the list, detail, and timeline views without losing the existing burst selection behavior.

Purpose: The taxonomy is only useful if it is visible where analysts compare bursts and decide what to inspect next.
Output: Taxonomy-aware review UI plus regression coverage that locks the shared vocabulary and wiring.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/67-burst-taxonomy-and-metrics/67-CONTEXT.md
@src/components/binning/BinningControls.tsx
@src/components/timeline/DualTimeline.tsx
@src/components/viz/BurstList.tsx
@src/components/viz/BurstDetails.tsx
@src/store/useSliceStore.ts
@src/app/timeslicing-algos/page.timeline-algos.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread taxonomy labels into burst list and timeline indicators</name>
  <read_first>
    - src/components/binning/BinningControls.tsx
    - src/components/timeline/DualTimeline.tsx
    - src/components/viz/BurstList.tsx
    - src/lib/binning/burst-taxonomy.ts
  </read_first>
  <files>src/components/binning/BinningControls.tsx, src/components/timeline/DualTimeline.tsx, src/components/viz/BurstList.tsx</files>
  <action>
Update the generation/review surfaces so burst windows can display their taxonomy class alongside the existing peak/count/duration information. In `BurstList.tsx`, add visible class and confidence text for each window and keep the click behavior exactly as-is so it still focuses the matching slice and updates the review panel.

In `DualTimeline.tsx`, carry the same taxonomy vocabulary into the burst indicators that influence the timeline, keeping `useAutoBurstSlices()` behavior intact and not reintroducing any alternate burst workflow. In `BinningControls.tsx`, surface a small taxonomy legend or chip summary near the generation controls so analysts can tell how burst labels are being interpreted.

Do not change the selected slice semantics or remove the current density/burstiness controls.
  </action>
  <acceptance_criteria>
    - BurstList renders class/confidence text for burst windows
    - DualTimeline uses the same taxonomy vocabulary for burst indicators
    - BinningControls shows a visible taxonomy legend/chip summary
    - Existing burst selection and focus behavior still exists in the file text
  </acceptance_criteria>
  <verify>pnpm vitest src/app/timeslicing-algos/page.timeline-algos.test.ts --run</verify>
  <done>The burst list and timeline can explain what each window means without changing the underlying burst workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Show burst class, confidence, and rationale in the detail panel and lock the route contract</name>
  <read_first>
    - src/components/viz/BurstDetails.tsx
    - src/app/timeslicing-algos/page.timeline-algos.test.ts
    - src/lib/binning/burst-taxonomy.ts
    - src/app/timeslicing-algos/lib/adaptive-bin-diagnostics.ts
  </read_first>
  <files>src/components/viz/BurstDetails.tsx, src/app/timeslicing-algos/page.timeline-algos.test.ts</files>
  <action>
Refactor `BurstDetails.tsx` so the selected-window panel explicitly shows the taxonomy class, confidence, rule version/provenance, and the short rationale that explains why the label was chosen. Preserve the existing event-count, gap, and top-crime-type summaries; the new taxonomy fields should augment, not replace, the current composition readout.

Then extend `page.timeline-algos.test.ts` to assert the route source still contains the burst taxonomy vocabulary (`prolonged-peak`, `isolated-spike`, `valley`, `neutral`) and the new `burstClass`, `burstConfidence`, and `burstProvenance` wiring. Keep the test source-based and deterministic, matching the existing route-intent checks.
  </action>
  <acceptance_criteria>
    - BurstDetails contains visible class/confidence/rationale copy
    - page.timeline-algos.test.ts checks the burst taxonomy vocabulary and wiring strings
    - `pnpm vitest src/app/timeslicing-algos/page.timeline-algos.test.ts --run` exits 0
  </acceptance_criteria>
  <verify>pnpm vitest src/app/timeslicing-algos/page.timeline-algos.test.ts --run</verify>
  <done>The review panel now explains burst meaning in plain terms and the route regression keeps the contract stable.</done>
</task>

</tasks>

<verification>
1. Burst list, details, and timeline all use the same taxonomy vocabulary.
2. Review surfaces still focus and select the existing burst slices.
3. Route-intent tests fail if the taxonomy wiring strings disappear.
</verification>

<success_criteria>
- Burst class and confidence are visible in the review experience.
- The taxonomy vocabulary is consistent across list, details, and timeline surfaces.
- Existing burst selection/apply behavior remains intact while taxonomy is added.
</success_criteria>

<output>
After completion, create `.planning/phases/67-burst-taxonomy-and-metrics/67-02-SUMMARY.md`
</output>
