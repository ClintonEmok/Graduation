---
phase: 46-guardrails-and-baselines
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/timeline/lib/interaction-guards.ts
  - src/components/timeline/lib/interaction-guards.test.ts
  - src/components/timeline/DualTimeline.tsx
autonomous: true

must_haves:
  truths:
    - "Brush/zoom synchronization math for timeline interactions is covered by deterministic regression tests"
    - "Point-selection projection used by DualTimeline is validated for normal and empty-data paths"
    - "DualTimeline keeps existing behavior while critical interaction math is moved behind testable helpers"
  artifacts:
    - path: "src/components/timeline/lib/interaction-guards.ts"
      provides: "Pure interaction helper functions extracted from DualTimeline"
      min_lines: 80
    - path: "src/components/timeline/lib/interaction-guards.test.ts"
      provides: "Regression tests for brush/zoom/selection helpers"
      min_lines: 120
    - path: "src/components/timeline/DualTimeline.tsx"
      provides: "DualTimeline wired to tested interaction helpers"
      contains: "interaction-guards"
  key_links:
    - from: "src/components/timeline/DualTimeline.tsx"
      to: "src/components/timeline/lib/interaction-guards.ts"
      via: "imports and uses helper functions"
      pattern: "from './lib/interaction-guards'"
    - from: "src/components/timeline/lib/interaction-guards.test.ts"
      to: "brush/zoom + selection invariants"
      via: "input/output assertions"
      pattern: "expect"
---

<objective>
Harden DualTimeline interaction behavior with testable guard functions before decomposition.

Purpose: Later extraction work in Phase 49 needs protected behavior around brush/zoom sync and selection mapping so refactors do not silently break timeline interactions.
Output: Extracted pure interaction guard helpers, DualTimeline wiring updates, and focused regression tests.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REFACTORING-PLAN.md
@src/components/timeline/DualTimeline.tsx
@src/lib/selection.ts
@src/lib/stores/viewportStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract deterministic interaction guard helpers from DualTimeline</name>
  <files>src/components/timeline/lib/interaction-guards.ts, src/components/timeline/DualTimeline.tsx</files>
  <action>Move non-visual, deterministic interaction math from `DualTimeline.tsx` into `src/components/timeline/lib/interaction-guards.ts`. Include helpers for range normalization/clamping, brush selection conversion, zoom-domain translation, and selection x-position fallback handling. Keep function signatures small and typed; do not change UI structure or event contracts. Update `DualTimeline.tsx` to call these helpers so behavior remains equivalent while logic becomes testable.</action>
  <verify>Run `npm run lint -- src/components/timeline/lib/interaction-guards.ts src/components/timeline/DualTimeline.tsx` and confirm no lint errors are introduced.</verify>
  <done>DualTimeline uses extracted pure helpers for critical interaction calculations without behavior changes.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression tests for brush/zoom sync and point selection mapping</name>
  <files>src/components/timeline/lib/interaction-guards.test.ts</files>
  <action>Implement focused tests for helper invariants that correspond to fragile timeline interactions: (1) brush range normalization and domain clamp behavior, (2) zoom/brush transform mapping round-trips, (3) selection x-position resolution when selected index/time is invalid or out of domain. Use explicit numeric fixtures so tests are deterministic and easy to compare after refactors.</action>
  <verify>Run `npx vitest run src/components/timeline/lib/interaction-guards.test.ts` and confirm all interaction guard tests pass.</verify>
  <done>Regression coverage exists for brush/zoom sync and selection fallback behavior required by REFACTOR-02.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/components/timeline/lib/interaction-guards.test.ts` passes.
2. `DualTimeline.tsx` compiles and imports the new guard helpers.
3. Interaction helper tests are deterministic and cover invalid-input edge cases.
</verification>

<success_criteria>
- REFACTOR-01: Key timeline interaction paths have baseline behavioral tests.
- REFACTOR-02: Brush/zoom/selection sync logic has explicit guardrails before decomposition starts.
</success_criteria>

<output>
After completion, create `.planning/phases/46-guardrails-and-baselines/46-03-SUMMARY.md`
</output>
