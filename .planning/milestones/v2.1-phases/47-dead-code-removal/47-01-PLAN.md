---
phase: 47-dead-code-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Legacy useSuggestionTrigger.ts is removed from src/hooks"
    - "No runtime imports reference removed file"
    - "Build and regression suites pass after deletion"
  artifacts:
    - path: "src/hooks/useSuggestionTrigger.ts"
      state: deleted
      reason: "Replaced by useSuggestionGenerator.ts - no longer imported"
  key_links: []
---

<objective>
Remove obsolete useSuggestionTrigger.ts hook with zero behavior change.

Purpose: Clean up legacy mock code that's been replaced by useSuggestionGenerator.ts
Output: Deleted file, verified build/test pass
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REFACTORING-PLAN.md
@.planning/phases/47-dead-code-removal/47-CONTEXT.md
@src/hooks/useSuggestionTrigger.ts
</context>

<tasks>

<task type="auto">
  <name>Delete legacy useSuggestionTrigger.ts</name>
  <files>src/hooks/useSuggestionTrigger.ts</files>
  <action>
    Delete the file src/hooks/useSuggestionTrigger.ts (137 lines).
    This is the legacy mock hook that was replaced by useSuggestionGenerator.ts.
    Confirm no runtime imports exist (grep verified 0 results for "from.*useSuggestionTrigger" in src/).
  </action>
  <verify>File no longer exists in src/hooks/</verify>
  <done>src/hooks/useSuggestionTrigger.ts deleted</done>
</task>

<task type="auto">
  <name>Verify build passes after deletion</name>
  <files>[]</files>
  <action>
    Run the build to confirm no import errors:
    - npm run build
    or if not configured:
    - npx tsc --noEmit
    
    This verifies the deletion didn't break any remaining imports.
  </action>
  <verify>Build completes without errors related to useSuggestionTrigger</verify>
  <done>Build passes without useSuggestionTrigger errors</done>
</task>

<task type="auto">
  <name>Run regression test suite</name>
  <files>[]</files>
  <action>
    Run the test suite to confirm no regressions:
    - npm test -- --run
    
    Focus on tests that cover the suggestion workflow to ensure behavior is preserved.
  </action>
  <verify>Tests pass - no failures related to suggestion workflow</verify>
  <done>Regression suite passes</done>
</task>

</tasks>

<verification>
- [ ] src/hooks/useSuggestionTrigger.ts no longer exists
- [ ] No build errors
- [ ] All tests pass
</verification>

<success_criteria>
- Legacy useSuggestionTrigger.ts is removed
- Build passes without errors
- Test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/47-dead-code-removal/47-01-SUMMARY.md`
</output>
