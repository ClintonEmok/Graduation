---
phase: 49-dualtimeline-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/timeline/DualTimeline.tsx
  - src/components/timeline/hooks/useScaleTransforms.ts
  - src/components/timeline/hooks/useScaleTransforms.test.ts
  - src/components/timeline/hooks/useDensityStripDerivation.ts
  - src/components/timeline/hooks/useDensityStripDerivation.test.ts
autonomous: true

must_haves:
  truths:
    - "Adaptive/linear timeline transforms are computed through a dedicated hook without changing display behavior"
    - "Density-strip derivation is computed through a dedicated hook and preserves existing fallback rules"
    - "DualTimeline consumes extracted transform and density hooks while keeping timeline interactions unchanged"
  artifacts:
    - path: "src/components/timeline/hooks/useScaleTransforms.ts"
      provides: "Typed transform-domain hook for adaptive and linear scale conversion"
      exports: ["useScaleTransforms"]
    - path: "src/components/timeline/hooks/useDensityStripDerivation.ts"
      provides: "Typed density derivation hook with detail-window recompute vs density-map fallback"
      exports: ["useDensityStripDerivation"]
    - path: "src/components/timeline/hooks/useScaleTransforms.test.ts"
      provides: "Deterministic transform behavior coverage"
      contains: "toDisplaySeconds"
    - path: "src/components/timeline/hooks/useDensityStripDerivation.test.ts"
      provides: "Deterministic density derivation parity coverage"
      contains: "DETAIL_DENSITY_RECOMPUTE_MAX_DAYS"
    - path: "src/components/timeline/DualTimeline.tsx"
      provides: "Timeline orchestration wired to extracted hooks"
      contains: "useScaleTransforms"
  key_links:
    - from: "src/components/timeline/DualTimeline.tsx"
      to: "src/components/timeline/hooks/useScaleTransforms.ts"
      via: "hook import and returned scale transform values"
      pattern: "useScaleTransforms"
    - from: "src/components/timeline/DualTimeline.tsx"
      to: "src/components/timeline/hooks/useDensityStripDerivation.ts"
      via: "hook import and derived density strip values"
      pattern: "useDensityStripDerivation"
---

<objective>
Extract transform and density derivation domains from `DualTimeline.tsx` into focused hooks while preserving existing behavior.

Purpose: This creates the low-risk foundation for later brush/zoom and selection extraction by isolating pure derivation logic first.
Output: New `useScaleTransforms` and `useDensityStripDerivation` hooks with deterministic tests, plus DualTimeline rewiring to consume them.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-dualtimeline-decomposition/49-CONTEXT.md
@.planning/phases/49-dualtimeline-decomposition/49-RESEARCH.md
@.planning/phases/46-guardrails-and-baselines/46-03-SUMMARY.md
@src/components/timeline/DualTimeline.tsx
@src/components/timeline/lib/interaction-guards.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract adaptive/linear transform logic into useScaleTransforms hook</name>
  <files>src/components/timeline/hooks/useScaleTransforms.ts, src/components/timeline/hooks/useScaleTransforms.test.ts</files>
  <action>Create `useScaleTransforms` under `src/components/timeline/hooks/` to encapsulate transform-domain logic currently embedded in `DualTimeline.tsx`, including adaptive and linear conversion helpers used for overview/detail interaction scales. Keep existing contracts and value semantics unchanged (same domain/range clamping and inversion behavior). Export only typed inputs/outputs needed by the timeline orchestrator. Add deterministic unit tests for core transform behavior with fixed fixtures, including warp-map and non-warp-map paths.</action>
  <verify>`npm test -- --run src/components/timeline/hooks/useScaleTransforms.test.ts` passes</verify>
  <done>`useScaleTransforms` exists with typed return values and test coverage proving parity for adaptive vs linear transform behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Extract density-strip derivation into useDensityStripDerivation hook</name>
  <files>src/components/timeline/hooks/useDensityStripDerivation.ts, src/components/timeline/hooks/useDensityStripDerivation.test.ts</files>
  <action>Create `useDensityStripDerivation` to own detail density derivation logic currently in `DualTimeline.tsx`, including the parity-critical fallback rule: recompute from detail points only when the detail window is within threshold, otherwise slice from existing `densityMap`. Keep constants/behavior unchanged (`ADAPTIVE_BIN_COUNT`, `ADAPTIVE_KERNEL_WIDTH`, `DETAIL_DENSITY_RECOMPUTE_MAX_DAYS`). Add deterministic unit tests covering both recompute and fallback branches with explicit fixture windows.</action>
  <verify>`npm test -- --run src/components/timeline/hooks/useDensityStripDerivation.test.ts` passes</verify>
  <done>Density derivation behavior is isolated in one hook with tests proving parity for recompute-vs-fallback decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire DualTimeline orchestration to consume transform and density hooks</name>
  <files>src/components/timeline/DualTimeline.tsx</files>
  <action>Update `DualTimeline.tsx` to import and use `useScaleTransforms` and `useDensityStripDerivation` for previously inlined logic while preserving render shape, prop contracts, and interaction flow. Do not change brush/zoom or point-selection behavior in this task; this plan is structural extraction only. Keep hook invocation order lint-safe and avoid introducing new stores or dependencies.</action>
  <verify>`npm run lint -- src/components/timeline/DualTimeline.tsx src/components/timeline/hooks/useScaleTransforms.ts src/components/timeline/hooks/useDensityStripDerivation.ts && npm run typecheck` passes</verify>
  <done>`DualTimeline.tsx` delegates transform and density derivation to extracted hooks and remains behavior-compatible.</done>
</task>

</tasks>

<verification>
- `useScaleTransforms` and `useDensityStripDerivation` exist as dedicated hook modules with deterministic tests.
- `DualTimeline.tsx` imports both hooks and no longer owns their full inlined implementations.
- `npm test -- --run src/components/timeline/hooks/useScaleTransforms.test.ts src/components/timeline/hooks/useDensityStripDerivation.test.ts` passes.
- `npm run typecheck` passes.
</verification>

<success_criteria>
- Phase 49 criterion (1) is partially satisfied for transform and density domains via extracted hooks.
- DualTimeline is measurably more orchestration-oriented, with derivation logic moved out.
- Extraction introduces no behavior drift in tested transform and density contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/49-dualtimeline-decomposition/49-01-SUMMARY.md`
</output>
