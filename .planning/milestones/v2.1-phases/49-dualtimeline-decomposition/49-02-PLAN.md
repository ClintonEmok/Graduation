---
phase: 49-dualtimeline-decomposition
plan: 02
type: execute
wave: 2
depends_on: [49-01]
files_modified:
  - src/components/timeline/DualTimeline.tsx
  - src/components/timeline/hooks/useBrushZoomSync.ts
  - src/components/timeline/hooks/useBrushZoomSync.test.ts
autonomous: true

must_haves:
  truths:
    - "Brush and zoom stay synchronized without feedback-loop jitter after extraction"
    - "Range updates from brush/zoom continue to write time, coordination, and viewport stores together"
    - "DualTimeline delegates D3 brush/zoom side effects to a dedicated hook"
  artifacts:
    - path: "src/components/timeline/hooks/useBrushZoomSync.ts"
      provides: "D3 brush/zoom synchronization hook with parity-safe side-effect boundaries"
      exports: ["useBrushZoomSync"]
    - path: "src/components/timeline/hooks/useBrushZoomSync.test.ts"
      provides: "Deterministic and jsdom-backed regression checks for brush/zoom wiring"
      contains: "isSyncingRef"
    - path: "src/components/timeline/DualTimeline.tsx"
      provides: "Timeline orchestration that consumes brush/zoom synchronization hook"
      contains: "useBrushZoomSync"
  key_links:
    - from: "src/components/timeline/hooks/useBrushZoomSync.ts"
      to: "src/components/timeline/lib/interaction-guards.ts"
      via: "reuse existing guard helpers for brush/zoom conversions"
      pattern: "interaction-guards"
    - from: "src/components/timeline/hooks/useBrushZoomSync.ts"
      to: "src/lib/stores/viewportStore.ts"
      via: "range update flow includes setViewport"
      pattern: "setViewport"
---

<objective>
Extract brush/zoom synchronization into a focused hook while preserving existing D3 interaction behavior and store-update contracts.

Purpose: Brush/zoom is the highest-risk interaction domain; isolating it with focused tests protects parity before final point-selection extraction.
Output: `useBrushZoomSync` hook, targeted regression coverage (including minimal jsdom effect wiring), and DualTimeline orchestration rewiring.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-dualtimeline-decomposition/49-CONTEXT.md
@.planning/phases/49-dualtimeline-decomposition/49-RESEARCH.md
@.planning/phases/46-guardrails-and-baselines/46-03-SUMMARY.md
@src/components/timeline/DualTimeline.tsx
@src/components/timeline/lib/interaction-guards.ts
@src/lib/stores/viewportStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract D3 brush/zoom synchronization side effects into useBrushZoomSync</name>
  <files>src/components/timeline/hooks/useBrushZoomSync.ts</files>
  <action>Create `useBrushZoomSync` to own brush/zoom behavior registration, event handling, and cleanup currently in `DualTimeline.tsx`. Preserve parity-critical contracts: keep `isSyncingRef` guard semantics to prevent brush/zoom feedback loops, reuse `interaction-guards` for conversion math, and keep range update flow equivalent to existing behavior (`setTimeRange`, `setRange`, `setBrushRange`, and `setViewport`). Avoid changing interaction thresholds or introducing new data sources.</action>
  <verify>`npm run typecheck` passes after extraction scaffolding</verify>
  <done>`useBrushZoomSync` encapsulates D3 side effects and maintains existing synchronization contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused regression tests for brush/zoom sync parity</name>
  <files>src/components/timeline/hooks/useBrushZoomSync.test.ts</files>
  <action>Add deterministic tests for hook-level behavior boundaries and at least one minimal jsdom-scoped test for D3 effect wiring. Cover parity-critical cases: guard against recursive sync, preserve brush-to-zoom transform mapping, and ensure range updates include viewport synchronization. Keep DOM coverage narrow; prefer deterministic helper-level assertions where possible.</action>
  <verify>`npm test -- --run src/components/timeline/hooks/useBrushZoomSync.test.ts` passes</verify>
  <done>Brush/zoom extraction is protected by deterministic coverage plus one jsdom wiring check.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire DualTimeline to consume useBrushZoomSync</name>
  <files>src/components/timeline/DualTimeline.tsx</files>
  <action>Replace in-component brush/zoom effect wiring in `DualTimeline.tsx` with `useBrushZoomSync` while preserving refs, callback boundaries, and existing render output. Keep previously extracted transform/density hook usage intact and avoid changes to point-selection logic in this plan.</action>
  <verify>`npm run lint -- src/components/timeline/DualTimeline.tsx src/components/timeline/hooks/useBrushZoomSync.ts && npm test -- --run src/components/timeline/lib/interaction-guards.test.ts src/components/timeline/hooks/useBrushZoomSync.test.ts` passes</verify>
  <done>`DualTimeline.tsx` delegates brush/zoom synchronization to the new hook and existing interaction-guard regressions still pass.</done>
</task>

</tasks>

<verification>
- Brush/zoom D3 side effects are centralized in `useBrushZoomSync`.
- `isSyncingRef` guard semantics are preserved and tested.
- Store range updates still include viewport synchronization.
- `npm run typecheck` and targeted brush/zoom regression tests pass.
</verification>

<success_criteria>
- Phase 49 criterion (1) is satisfied for brush/zoom synchronization extraction.
- Timeline brush/zoom behavior remains stable with no feedback-loop regressions.
- DualTimeline further shifts from domain logic toward orchestration.
</success_criteria>

<output>
After completion, create `.planning/phases/49-dualtimeline-decomposition/49-02-SUMMARY.md`
</output>
