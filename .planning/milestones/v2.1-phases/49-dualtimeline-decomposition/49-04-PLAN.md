---
phase: 49-dualtimeline-decomposition
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/timeline/DualTimeline.tsx
  - src/components/timeline/hooks/useBrushZoomSync.ts
  - src/components/timeline/hooks/useBrushZoomSync.test.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Brush-triggered range changes execute the unified range update contract through `applyRangeToStores`"
    - "Zoom-triggered range changes execute the same unified range update contract through `applyRangeToStores`"
    - "Viewport synchronization (`setViewport`) remains in the same update contract as `setTimeRange`, `setRange`, and `setBrushRange`"
  artifacts:
    - path: "src/components/timeline/hooks/useBrushZoomSync.test.ts"
      provides: "Deterministic regression coverage for brush/zoom -> applyRangeToStores contract flow"
      contains: "applyRangeToStores"
    - path: "src/components/timeline/hooks/useBrushZoomSync.ts"
      provides: "Testable brush/zoom handler boundaries that preserve callback contract"
      exports: ["useBrushZoomSync"]
    - path: "src/components/timeline/DualTimeline.tsx"
      provides: "Parity-critical unified range update flow used by brush/zoom callbacks"
      contains: "setTimeRange"
  key_links:
    - from: "src/components/timeline/hooks/useBrushZoomSync.ts"
      to: "src/components/timeline/DualTimeline.tsx"
      via: "brush/zoom event paths call injected applyRangeToStores callback"
      pattern: "applyRangeToStores\(startSec, endSec\)"
    - from: "src/components/timeline/DualTimeline.tsx"
      to: "src/lib/stores/viewportStore.ts"
      via: "unified range update flow writes viewport in same contract"
      pattern: "setViewport"
---

<objective>
Close the Phase 49 verification gap by adding deterministic regression coverage for brush/zoom range-update parity.

Purpose: Verification found missing proof that brush/zoom paths still execute the full multi-store update contract together through `applyRangeToStores`.
Output: Focused tests that exercise brush and zoom callback paths and assert unified store-write + viewport-sync parity.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-dualtimeline-decomposition/49-dualtimeline-decomposition-VERIFICATION.md
@.planning/phases/49-dualtimeline-decomposition/49-02-SUMMARY.md
@.planning/phases/49-dualtimeline-decomposition/49-03-SUMMARY.md
@src/components/timeline/hooks/useBrushZoomSync.ts
@src/components/timeline/hooks/useBrushZoomSync.test.ts
@src/components/timeline/DualTimeline.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make brush/zoom callback boundaries deterministically testable without changing runtime behavior</name>
  <files>src/components/timeline/hooks/useBrushZoomSync.ts, src/components/timeline/DualTimeline.tsx</files>
  <action>Introduce or expose minimal pure helper boundaries needed to deterministically invoke brush and zoom event callback logic in tests (without jsdom worker dependence), while preserving the existing runtime path and callback ownership in `DualTimeline`. Keep `applyRangeToStores` as the single parity-safe store update contract. Do not alter interaction math sources (`interaction-guards`) or change production brush/zoom behavior; this task only improves testability of existing behavior.</action>
  <verify>`npm run typecheck` passes after helper-boundary extraction</verify>
  <done>Brush and zoom callback paths can be invoked in deterministic tests and still route through the same `applyRangeToStores` contract owned by `DualTimeline`.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic regression tests for unified multi-store + viewport update flow parity</name>
  <files>src/components/timeline/hooks/useBrushZoomSync.test.ts</files>
  <action>Expand `useBrushZoomSync.test.ts` beyond `withSyncGuard` to cover both brush-triggered and zoom-triggered update flows. Use deterministic fixtures and spies to assert each event path triggers the unified range update contract via `applyRangeToStores`, and that the resulting contract includes the full store-write set together (`setTimeRange`, `setRange`, `setBrushRange`, `setViewport`). Include assertions that viewport synchronization remains in the same contract boundary for both event paths. Keep tests deterministic and node-safe; do not reintroduce brittle jsdom-only worker dependencies.</action>
  <verify>`npm test -- --run src/components/timeline/hooks/useBrushZoomSync.test.ts && npm run typecheck` passes</verify>
  <done>Regression coverage proves brush and zoom paths preserve full range-update parity, including viewport synchronization in the same update contract.</done>
</task>

</tasks>

<verification>
- Brush and zoom event paths are deterministically exercised in automated tests.
- Tests prove `applyRangeToStores` remains the shared contract boundary for both paths.
- The contract coverage explicitly includes `setTimeRange`, `setRange`, `setBrushRange`, and `setViewport` together.
- `npm test -- --run src/components/timeline/hooks/useBrushZoomSync.test.ts` and `npm run typecheck` pass.
</verification>

<success_criteria>
- The failed verification truth for regression parity is closed with deterministic automated evidence.
- Brush/zoom-triggered updates are proven to preserve the same unified multi-store + viewport synchronization contract.
- Phase 49 can be re-verified without the prior gap on range update flow parity.
</success_criteria>

<output>
After completion, create `.planning/phases/49-dualtimeline-decomposition/49-04-SUMMARY.md`
</output>
