---
phase: 51-store-consolidation
plan: 09
type: execute
wave: 3
depends_on: [51-03]
files_modified:
  - src/components/viz/BurstDetails.tsx
  - src/components/viz/BurstList.tsx
  - src/components/viz/HeatmapOverlay.tsx
  - src/components/viz/PointInspector.tsx
  - src/components/viz/SliceManagerUI.tsx
autonomous: true

must_haves:
  truths:
    - "Users see the same burst details, overlays, and inspector outputs as before"
    - "Inspector interactions and detail panes behave consistently during this migration batch"
    - "No user-facing regressions appear in supporting visualization tools"
  artifacts:
    - path: "src/components/viz/PointInspector.tsx"
      provides: "Inspector path using canonical data contracts"
      contains: "DataPoint"
    - path: "src/components/viz/BurstList.tsx"
      provides: "Burst list subscriptions from canonical data-store path"
      contains: "useTimelineDataStore"
    - path: "src/components/viz/HeatmapOverlay.tsx"
      provides: "Overlay data reads without deprecated store imports"
      contains: "useTimelineDataStore"
  key_links:
    - from: "src/components/viz/PointInspector.tsx"
      to: "src/lib/data/types.ts"
      via: "canonical typed point metadata access"
      pattern: "DataPoint"
    - from: "src/components/viz/BurstDetails.tsx"
      to: "src/store/useTimelineDataStore.ts"
      via: "burst details data subscriptions"
      pattern: "useTimelineDataStore"
---

<objective>
Migrate supporting viz overlays and inspector surfaces off deprecated `useDataStore` imports.

Purpose: This keeps the second viz migration batch focused on overlays/details while preserving render parity.
Output: Supporting viz files use canonical data-store and extracted data modules.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-store-consolidation/51-CONTEXT.md
@.planning/phases/51-store-consolidation/51-RESEARCH.md
@.planning/phases/51-store-consolidation/51-03-SUMMARY.md
@src/store/useTimelineDataStore.ts
@src/lib/data/types.ts
@src/lib/data/selectors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire supporting viz overlays and inspector files to canonical imports</name>
  <files>src/components/viz/BurstDetails.tsx, src/components/viz/BurstList.tsx, src/components/viz/HeatmapOverlay.tsx, src/components/viz/PointInspector.tsx, src/components/viz/SliceManagerUI.tsx</files>
  <action>Replace deprecated `@/store/useDataStore` imports in the listed supporting viz files with canonical store/selectors/types. Preserve overlay visibility logic, burst details behavior, and inspector parity.</action>
  <verify>`npm run lint -- src/components/viz/BurstDetails.tsx src/components/viz/BurstList.tsx src/components/viz/HeatmapOverlay.tsx src/components/viz/PointInspector.tsx src/components/viz/SliceManagerUI.tsx` passes</verify>
  <done>Supporting viz batch compiles and no longer imports deprecated `useDataStore`.</done>
</task>

<task type="auto">
  <name>Task 2: Verify zero deprecated imports in supporting viz batch</name>
  <files>src/components/viz/BurstDetails.tsx, src/components/viz/BurstList.tsx, src/components/viz/HeatmapOverlay.tsx, src/components/viz/PointInspector.tsx, src/components/viz/SliceManagerUI.tsx</files>
  <action>Run a targeted import gate over the supporting viz batch and remove all residual deprecated store imports.</action>
  <verify>`rg -l "@/store/useDataStore" src/components/viz/BurstDetails.tsx src/components/viz/BurstList.tsx src/components/viz/HeatmapOverlay.tsx src/components/viz/PointInspector.tsx src/components/viz/SliceManagerUI.tsx | wc -l` returns `0`</verify>
  <done>Supporting viz batch is fully migrated off `useDataStore`.</done>
</task>

</tasks>

<verification>
- Supporting viz batch files no longer import `@/store/useDataStore`.
- Lint passes for the files in this batch.
</verification>

<success_criteria>
- Viz-heavy migration is split into coherent, lower-breadth batches.
- Final deletion preparation is narrowed to residual route/advanced consumers.
</success_criteria>

<output>
After completion, create `.planning/phases/51-store-consolidation/51-09-SUMMARY.md`
</output>
