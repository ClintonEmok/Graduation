---
phase: 51-store-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/51-store-consolidation/51-store-dependency-audit.md
  - src/store/slice-domain/types.ts
  - src/store/slice-domain/createSliceCoreSlice.ts
  - src/store/slice-domain/createSliceSelectionSlice.ts
  - src/store/slice-domain/createSliceCreationSlice.ts
  - src/store/slice-domain/createSliceAdjustmentSlice.ts
  - src/store/slice-domain/selectors.ts
  - src/store/useSliceDomainStore.ts
autonomous: true

must_haves:
  truths:
    - "Slice creation, selection, adjustment, and authored-slice state are owned by one bounded Zustand domain store"
    - "Persisted slice state is limited to authored `slices`, while drag/hover/selection interaction state stays transient"
    - "A concrete store-dependency audit exists for this phase and captures coupled groups plus migration targets"
  artifacts:
    - path: ".planning/phases/51-store-consolidation/51-store-dependency-audit.md"
      provides: "Store dependency inventory and coupling evidence used to drive migration order"
      contains: "useSliceStore"
    - path: "src/store/useSliceDomainStore.ts"
      provides: "Bounded slice-domain store composed from internal slices"
      exports: ["useSliceDomainStore"]
    - path: "src/store/slice-domain/createSliceCoreSlice.ts"
      provides: "Authored-slice lifecycle actions and persisted slice state"
      contains: "addSlice"
    - path: "src/store/slice-domain/selectors.ts"
      provides: "Fine-grained selector helpers for parity-safe subscriptions"
      contains: "select"
  key_links:
    - from: "src/store/useSliceDomainStore.ts"
      to: "src/store/slice-domain/createSliceCoreSlice.ts"
      via: "slice composition in bounded store factory"
      pattern: "createSliceCoreSlice"
    - from: "src/store/useSliceDomainStore.ts"
      to: "src/store/slice-domain/createSliceAdjustmentSlice.ts"
      via: "single-store composition for transient interaction state"
      pattern: "createSliceAdjustmentSlice"
---

<objective>
Establish the bounded slice-domain store foundation and a concrete store dependency audit before consumer migration.

Purpose: This creates the domain boundary that Phase 51 requires, so later migration steps can move callers without risking dual-store drift.
Output: A documented `src/store/` coupling audit plus a new `useSliceDomainStore` composed from internal slice modules with selector helpers.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-store-consolidation/51-CONTEXT.md
@.planning/phases/51-store-consolidation/51-RESEARCH.md
@.planning/phases/50-query-layer-decomposition/50-03-SUMMARY.md
@src/store/useSliceStore.ts
@src/store/useSliceSelectionStore.ts
@src/store/useSliceCreationStore.ts
@src/store/useSliceAdjustmentStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce the phase store-coupling audit artifact</name>
  <files>.planning/phases/51-store-consolidation/51-store-dependency-audit.md</files>
  <action>Audit `src/store/` and active workflow imports to document tightly coupled groups, current cross-store writes, and migration order constraints for Phase 51. Include concrete evidence snippets for: (a) slice domain coupling (`useSliceStore` + creation/selection/adjustment stores), (b) deprecated data-path coupling (`useDataStore` import map), and (c) out-of-scope stores left unchanged. Do not add speculative architecture outside the locked phase scope.</action>
  <verify>`rg -n "useSlice(Store|SelectionStore|CreationStore|AdjustmentStore)|useDataStore" src --glob "*.{ts,tsx}"` returns evidence referenced in the audit file</verify>
  <done>Audit artifact exists with actionable coupling groups and migration sequencing that matches Phase 51 context and research.</done>
</task>

<task type="auto">
  <name>Task 2: Create bounded slice-domain store modules with explicit persisted/transient boundaries</name>
  <files>src/store/slice-domain/types.ts, src/store/slice-domain/createSliceCoreSlice.ts, src/store/slice-domain/createSliceSelectionSlice.ts, src/store/slice-domain/createSliceCreationSlice.ts, src/store/slice-domain/createSliceAdjustmentSlice.ts, src/store/useSliceDomainStore.ts</files>
  <action>Introduce the `src/store/slice-domain/` module set and compose them in `useSliceDomainStore.ts` using Zustand slice-composition. Keep action semantics parity with existing stores (add/update/merge/select/drag/snap behavior), and apply `persist` only at the combined store boundary with `partialize` limited to authored `slices`. Keep drag/hover/tooltip/selection preview state non-persisted. Avoid changing user-visible slice behavior in this foundation plan.</action>
  <verify>`npm run typecheck` passes</verify>
  <done>`useSliceDomainStore` composes all four slice concerns with persisted `slices` only and compiles without contract drift.</done>
</task>

<task type="auto">
  <name>Task 3: Add selector helpers for parity-safe consumer migration</name>
  <files>src/store/slice-domain/selectors.ts, src/store/useSliceDomainStore.ts</files>
  <action>Add focused selector helpers used by current timeline/map/viz workflows so migration can preserve fine-grained subscriptions instead of selecting large store objects. Cover selectors for slice lists, active-slice metadata, selection IDs/count, creation preview state, and adjustment drag/snap state. Keep selector naming explicit and importable from one place.</action>
  <verify>`npm run lint -- src/store/slice-domain/selectors.ts src/store/useSliceDomainStore.ts` passes</verify>
  <done>Selector helpers exist for each migrated concern, enabling downstream consumer rewires without introducing broad subscriptions.</done>
</task>

</tasks>

<verification>
- `src/store/useSliceDomainStore.ts` composes four internal slice-domain modules into one bounded store.
- `persist` is applied once at the bounded store and partialized to authored `slices` only.
- `.planning/phases/51-store-consolidation/51-store-dependency-audit.md` documents coupled groups and migration order evidence.
</verification>

<success_criteria>
- Phase 51 criterion (1) has a concrete implementation base: one coherent slice-domain store exists.
- Migration can proceed with evidence-backed coupling analysis instead of ad hoc file-by-file edits.
</success_criteria>

<output>
After completion, create `.planning/phases/51-store-consolidation/51-01-SUMMARY.md`
</output>
