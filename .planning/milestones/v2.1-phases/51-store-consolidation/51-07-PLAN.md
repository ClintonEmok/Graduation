---
phase: 51-store-consolidation
plan: 07
type: execute
wave: 3
depends_on: [51-03]
files_modified:
  - src/hooks/useAdaptiveScale.ts
  - src/hooks/useDebouncedDensity.ts
  - src/hooks/useSelectionSync.ts
  - src/hooks/useSliceStats.ts
  - src/lib/selection.ts
autonomous: true

must_haves:
  truths:
    - "User-visible selection, density, and slice-stat behavior stays unchanged after hook rewires"
    - "High-frequency interactions continue to feel smooth with no new lag or flicker"
    - "No regression appears in selection synchronization and derived-stat displays"
  artifacts:
    - path: "src/hooks/useSelectionSync.ts"
      provides: "Selection synchronization using canonical selector contracts"
      contains: "selectFilteredData"
    - path: "src/hooks/useAdaptiveScale.ts"
      provides: "Adaptive-scale logic reading canonical timeline data store"
      contains: "useTimelineDataStore"
    - path: "src/lib/selection.ts"
      provides: "Selection utility path migrated to canonical data contracts"
      contains: "useTimelineDataStore"
  key_links:
    - from: "src/hooks/useSelectionSync.ts"
      to: "src/lib/data/selectors.ts"
      via: "filtered-point derivation path"
      pattern: "selectFilteredData"
    - from: "src/hooks/useSliceStats.ts"
      to: "src/store/useTimelineDataStore.ts"
      via: "slice statistics data reads"
      pattern: "useTimelineDataStore"
---

<objective>
Migrate core hook/lib consumers off deprecated `useDataStore` imports.

Purpose: This isolates hook/lib rewires from component rewires so parity-sensitive data logic changes stay context-safe.
Output: Hook and selection utility files consume canonical `useTimelineDataStore` and shared data modules.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-store-consolidation/51-CONTEXT.md
@.planning/phases/51-store-consolidation/51-RESEARCH.md
@.planning/phases/51-store-consolidation/51-03-SUMMARY.md
@src/store/useTimelineDataStore.ts
@src/lib/data/types.ts
@src/lib/data/selectors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire hook/lib data consumers to canonical store and selectors</name>
  <files>src/hooks/useAdaptiveScale.ts, src/hooks/useDebouncedDensity.ts, src/hooks/useSelectionSync.ts, src/hooks/useSliceStats.ts, src/lib/selection.ts</files>
  <action>Replace `@/store/useDataStore` imports with canonical replacements (`@/store/useTimelineDataStore`, `@/lib/data/types`, `@/lib/data/selectors`) and preserve existing behavior in debounced density, selection sync, adaptive scaling, and slice statistics paths.</action>
  <verify>`npm run typecheck` passes</verify>
  <done>All listed hook/lib files compile and no longer import `useDataStore`.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce import gate for migrated hook/lib batch</name>
  <files>src/hooks/useAdaptiveScale.ts, src/hooks/useDebouncedDensity.ts, src/hooks/useSelectionSync.ts, src/hooks/useSliceStats.ts, src/lib/selection.ts</files>
  <action>Run targeted import checks and remove any residual deprecated store imports from this batch.</action>
  <verify>`rg -l "@/store/useDataStore" src/hooks/useAdaptiveScale.ts src/hooks/useDebouncedDensity.ts src/hooks/useSelectionSync.ts src/hooks/useSliceStats.ts src/lib/selection.ts | wc -l` returns `0`</verify>
  <done>Hook/lib batch is fully off deprecated store imports.</done>
</task>

</tasks>

<verification>
- Hook/lib batch has zero `@/store/useDataStore` imports.
- Typecheck remains green for canonical rewires.
</verification>

<success_criteria>
- Parity-sensitive hook/lib migration is complete in a focused, reduced-breadth plan.
- Final deletion work no longer depends on these hook/lib files.
</success_criteria>

<output>
After completion, create `.planning/phases/51-store-consolidation/51-07-SUMMARY.md`
</output>
