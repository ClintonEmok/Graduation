---
phase: 51-store-consolidation
plan: 12
type: execute
wave: 5
depends_on: [51-03, 51-04, 51-05, 51-06, 51-07, 51-08, 51-09, 51-10, 51-11]
files_modified:
  - src/store/useDataStore.ts
autonomous: true

must_haves:
  truths:
    - "Users experience no regression after the deprecated store file is fully removed"
    - "Timeline, map, and visualization workflows continue to behave the same post-deletion"
    - "The deprecated data-store path is fully retired without breaking active app flows"
  artifacts:
    - path: "src/store/useDataStore.ts"
      provides: "Deprecated store path removed from source tree"
      contains: "(deleted)"
  key_links:
    - from: "src/"
      to: "src/store/useDataStore.ts"
      via: "phase-wide import/reference gate"
      pattern: "@/store/useDataStore|useDataStore"
---

<objective>
Isolate the zero-import gate and delete `src/store/useDataStore.ts` as a dedicated final plan.

Purpose: This enforces the locked deletion safeguard as a focused terminal step, separate from broad rewires.
Output: Verified zero-import state across `src/`, deleted deprecated file, and post-delete regression gates passing.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-store-consolidation/51-CONTEXT.md
@.planning/phases/51-store-consolidation/51-RESEARCH.md
@.planning/phases/51-store-consolidation/51-10-SUMMARY.md
@.planning/phases/51-store-consolidation/51-11-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run phase-wide zero-import gate for deprecated store path</name>
  <files>src/store/useDataStore.ts</files>
  <action>Execute a strict phase-wide gate and confirm `src/` has zero references/imports of `@/store/useDataStore` or `useDataStore` before deletion. If non-zero, stop and clear residual imports first; do not delete early.</action>
  <verify>`test "$(rg -l "@/store/useDataStore|useDataStore" src | wc -l | tr -d ' ')" = "0"` passes</verify>
  <done>Zero-import gate passes across the entire `src/` tree.</done>
</task>

<task type="auto">
  <name>Task 2: Delete deprecated store file after gate pass</name>
  <files>src/store/useDataStore.ts</files>
  <action>Delete `src/store/useDataStore.ts` only after Task 1 succeeds. Keep this task deletion-only; do not mix broad rewires here.</action>
  <verify>`test ! -f src/store/useDataStore.ts` passes</verify>
  <done>Deprecated store file is removed from the repository.</done>
</task>

<task type="auto">
  <name>Task 3: Run post-delete parity-focused regression gates</name>
  <files>src/store/useDataStore.ts</files>
  <action>Run parity-focused checks for timeline/map/viz/store-critical paths after deletion. Treat failures as migration regressions to be fixed before closing the phase.</action>
  <verify>`npm run typecheck && npm run lint -- src/components/timeline src/components/viz src/components/map src/hooks src/app/timeslicing/page.tsx src/app/timeline-test/page.tsx src/app/timeline-test-3d/page.tsx && npm test -- --run src/store/useSliceStore.test.ts src/store/useSliceAdjustmentStore.test.ts` passes</verify>
  <done>Post-delete typecheck, targeted lint, and targeted tests all pass.</done>
</task>

</tasks>

<verification>
- `rg -l "@/store/useDataStore|useDataStore" src | wc -l` equals `0` before deletion.
- `src/store/useDataStore.ts` is deleted.
- Typecheck, targeted lint, and targeted tests pass post-deletion.
</verification>

<success_criteria>
- Phase 51 criterion (2) is satisfied with explicit verified deletion sequencing.
- Phase 51 criterion (3) is satisfied for data-store ownership in active workflows.
</success_criteria>

<output>
After completion, create `.planning/phases/51-store-consolidation/51-12-SUMMARY.md`
</output>
