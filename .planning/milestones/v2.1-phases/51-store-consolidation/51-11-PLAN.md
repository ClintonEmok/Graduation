---
phase: 51-store-consolidation
plan: 11
type: execute
wave: 3
depends_on: [51-02, 51-03]
files_modified:
  - src/components/viz/ClusterManager.tsx
  - src/components/viz/TrajectoryLayer.tsx
  - src/store/useSliceStore.ts
autonomous: true

must_haves:
  truths:
    - "Users see unchanged cluster and trajectory visuals after advanced rewiring"
    - "Clustering and trajectory interactions remain stable with no parity drift"
    - "No user-visible regression appears in advanced visualization paths"
  artifacts:
    - path: "src/components/viz/ClusterManager.tsx"
      provides: "Cluster derivation sourced from canonical filtered-data selector"
      contains: "selectFilteredData"
    - path: "src/components/viz/TrajectoryLayer.tsx"
      provides: "Trajectory rendering using canonical data contracts"
      contains: "FilteredPoint"
    - path: "src/store/useSliceStore.ts"
      provides: "Slice compatibility adapter without `useDataStore` dependency"
      contains: "useSliceDomainStore"
  key_links:
    - from: "src/components/viz/ClusterManager.tsx"
      to: "src/lib/data/selectors.ts"
      via: "cluster filtered-point derivation"
      pattern: "selectFilteredData"
    - from: "src/components/viz/TrajectoryLayer.tsx"
      to: "src/store/useTimelineDataStore.ts"
      via: "trajectory data source migration"
      pattern: "useTimelineDataStore"
---

<objective>
Migrate advanced viz and legacy adapter residuals off deprecated `useDataStore`.

Purpose: This isolates the remaining advanced consumer rewires from route rewires and from final deletion gating.
Output: Cluster/trajectory and `useSliceStore` residual imports move to canonical data paths.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-store-consolidation/51-CONTEXT.md
@.planning/phases/51-store-consolidation/51-RESEARCH.md
@.planning/phases/51-store-consolidation/51-03-SUMMARY.md
@src/store/useTimelineDataStore.ts
@src/lib/data/types.ts
@src/lib/data/selectors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire advanced viz consumers and legacy adapter residual imports</name>
  <files>src/components/viz/ClusterManager.tsx, src/components/viz/TrajectoryLayer.tsx, src/store/useSliceStore.ts</files>
  <action>Replace any remaining `@/store/useDataStore` imports in this advanced batch with canonical replacements (`@/store/useTimelineDataStore`, `@/lib/data/types`, `@/lib/data/selectors`) and preserve clustering/trajectory behavior.</action>
  <verify>`npm run typecheck` passes</verify>
  <done>Advanced viz and adapter residual files compile with canonical imports and no deprecated store dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Verify zero deprecated imports in advanced residual batch</name>
  <files>src/components/viz/ClusterManager.tsx, src/components/viz/TrajectoryLayer.tsx, src/store/useSliceStore.ts</files>
  <action>Run targeted import gates for this advanced residual set and remove any stragglers.</action>
  <verify>`rg -l "@/store/useDataStore" src/components/viz/ClusterManager.tsx src/components/viz/TrajectoryLayer.tsx src/store/useSliceStore.ts | wc -l` returns `0`</verify>
  <done>Advanced residual batch is fully off deprecated data-store imports.</done>
</task>

</tasks>

<verification>
- Advanced residual files no longer import `@/store/useDataStore`.
- Typecheck remains green after rewiring.
</verification>

<success_criteria>
- Remaining advanced consumers are migrated in a minimal, coherent batch.
- Final deletion plan can remain strictly gate/delete focused.
</success_criteria>

<output>
After completion, create `.planning/phases/51-store-consolidation/51-11-SUMMARY.md`
</output>
