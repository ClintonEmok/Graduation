---
phase: 40-fully-automated-timeslicing-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/autoProposalSet.ts
  - src/lib/full-auto-orchestrator.ts
  - src/hooks/useSuggestionGenerator.ts
autonomous: true

must_haves:
  truths:
    - "User can trigger full-auto generation and receive complete proposal sets containing both warp and interval outputs"
    - "Proposal sets are ranked and include an explicit recommended option"
    - "Each proposal set exposes score breakdown for review transparency"
  artifacts:
    - path: "src/types/autoProposalSet.ts"
      provides: "Canonical type for full-auto proposal set and score breakdown"
    - path: "src/lib/full-auto-orchestrator.ts"
      provides: "Generation and ranking of complete warp+interval proposal sets"
    - path: "src/hooks/useSuggestionGenerator.ts"
      provides: "Hook integration that produces ranked full-auto results"
  key_links:
    - from: "src/hooks/useSuggestionGenerator.ts"
      to: "src/lib/full-auto-orchestrator.ts"
      via: "full-auto generation path"
    - from: "src/lib/full-auto-orchestrator.ts"
      to: "src/lib/warp-generation.ts + src/lib/interval-detection.ts"
      via: "compose complete sets from existing algorithms"
---

<objective>
Create the full-auto orchestration core so generation returns ranked complete proposal sets instead of isolated suggestion items.

Purpose: Establish the data and orchestration foundation needed for package-level review/accept UX.
Output: New proposal-set types, orchestrator module, and hook integration that returns ranked full-auto results.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-fully-automated-timeslicing-orchestration/40-CONTEXT.md
@src/hooks/useSuggestionGenerator.ts
@src/lib/warp-generation.ts
@src/lib/interval-detection.ts
</context>

<tasks>

<task type="auto">
  <name>Create full-auto proposal set domain types</name>
  <files>src/types/autoProposalSet.ts</files>
  <action>
    Create a canonical type module for full-auto output packages. Include:
    - Proposal set identity and rank fields
    - Complete payload: selected warp profile + interval boundaries
    - Confidence and score breakdown fields (coverage, relevance, overlap, continuity, contextFit, total)
    - Recommendation markers (`isRecommended`, low-confidence/no-result reason metadata)
    Keep this independent from UI-only shape so store and hook can share it safely.
  </action>
  <verify>Run `pnpm exec tsc --noEmit` and confirm type module compiles without introducing circular imports</verify>
  <done>Typed full-auto proposal set contract exists and can be imported by orchestration and UI layers</done>
</task>

<task type="auto">
  <name>Implement full-auto generation and ranking orchestrator</name>
  <files>src/lib/full-auto-orchestrator.ts</files>
  <action>
    Add orchestration logic that composes complete sets from existing algorithms. Requirements:
    - Generate warp candidates using existing warp-generation utilities
    - Generate compatible interval candidates and pair them into complete sets
    - Score each set using transparent weighted criteria and produce top 3 ranked sets
    - Mark rank #1 as recommended
    - Emit low-confidence/no-result metadata when data is insufficient
    Keep deterministic ordering and avoid random output to support stable UX.
  </action>
  <verify>Run `pnpm test -- full-auto-orchestrator` (or add/execute targeted test command used in repo) and verify ranking + recommendation behavior</verify>
  <done>Orchestrator returns ranked complete proposal sets with score breakdown and recommendation metadata</done>
</task>

<task type="auto">
  <name>Integrate full-auto orchestrator into suggestion generator hook</name>
  <files>src/hooks/useSuggestionGenerator.ts</files>
  <action>
    Extend the generator hook to call the orchestrator path and expose ranked full-auto set results alongside existing item-level suggestions.
    - Add a full-auto trigger path without removing existing manual controls
    - Persist generated full-auto sets for panel consumption
    - Preserve current error handling and in-flight guard behavior
    - Keep backward compatibility so existing review flow does not break
  </action>
  <verify>Run `pnpm exec eslint src/hooks/useSuggestionGenerator.ts` and validate full-auto trigger produces ranked sets in /timeslicing UI</verify>
  <done>Generator hook outputs ranked full-auto sets and keeps existing suggestion flow functional</done>
</task>

</tasks>

<verification>
- Trigger full-auto generation in /timeslicing and confirm top 3 ranked complete sets are produced
- Confirm first set is marked recommended and includes score breakdown
- Confirm low-confidence/no-result metadata is populated when data is sparse
</verification>

<success_criteria>
- Full-auto generation returns complete ranked packages (warp + intervals), not only separate items
- Ranking output is deterministic and transparent
- Foundation is ready for package-level review/accept UI in downstream plans
</success_criteria>

<output>
After completion, create `.planning/phases/40-fully-automated-timeslicing-orchestration/40-01-SUMMARY.md`
</output>
