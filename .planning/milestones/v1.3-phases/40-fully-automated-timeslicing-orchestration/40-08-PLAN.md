---
phase: 40-fully-automated-timeslicing-orchestration
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/timeslicing/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "No orange burst slices on timeslicing page (even after refresh)"
    - "Existing burst slices are cleared on page load"
  artifacts:
    - path: "src/app/timeslicing/page.tsx"
      provides: "Clear existing burst slices on mount"
---

<objective>
Clear existing burst slices from persistence on timeslicing route.

Purpose: The slices are persisted in localStorage. Even though we're not creating NEW burst slices, OLD ones still show. Need to clear them on page load.
Output: Call clearBurstSlices or remove existing isBurst slices on mount.
</objective>

<context>
@src/app/timeslicing/page.tsx
@src/store/useSliceStore.ts
</context>

<tasks>

<task type="auto">
  <name>Clear existing burst slices on timeslicing mount</name>
  <files>src/app/timeslicing/page.tsx</files>
  <action>
    - Import clearSlices or add a method to remove burst slices from useSliceStore
    - On page mount (useEffect), clear any existing burst slices
    - Either: clear ALL slices, OR filter out only isBurst slices
    - The simplest approach: clear all slices on timeslicing route mount
  </action>
  <verify>Refresh timeslicing page - no orange slices, no slices at all initially</verify>
  <done>Burst slices cleared on timeslicing page load</done>
</task>

</tasks>

<verification>
- Clear browser localStorage for the slices store OR
- Refresh page and verify no slices appear
</verification>

<success_criteria>
- Timeslicing page starts with no slices
- User can create fresh time slices
</success_criteria>

<output>
After completion, create `.planning/phases/40-fully-automated-timeslicing-orchestration/40-08-SUMMARY.md`
</output>
