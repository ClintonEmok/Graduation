---
phase: 40-fully-automated-timeslicing-orchestration
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/timeslicing/components/AutoProposalSetCard.tsx
  - src/app/timeslicing/components/SuggestionPanel.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Warp intervals show as date ranges (e.g., 'Jan 2001 - Jun 2003')"
    - "Weights shown as percentage (e.g., '75% warp')"
    - "Uses actual time range from viewport/selection"
  artifacts:
    - path: "src/app/timeslicing/components/AutoProposalSetCard.tsx"
      receives: "startEpoch and endEpoch props"
    - path: "src/app/timeslicing/components/SuggestionPanel.tsx"
      passes: "startDate and endDate to AutoProposalSetCard"
---

<objective>
Convert warp interval percentages to actual date ranges.

Purpose: Users understand time ranges better as dates than percentages.
Output: Intervals display as "Jan 2001 - Jun 2003" instead of "0% → 25%".
</objective>

<context>
@src/app/timeslicing/components/AutoProposalSetCard.tsx
@src/app/timeslicing/components/SuggestionPanel.tsx
@src/lib/time-domain.ts
</context>

<tasks>

<task type="auto">
  <name>Add epoch props to AutoProposalSetCard</name>
  <files>src/app/timeslicing/components/AutoProposalSetCard.tsx</files>
  <action>
    - Add props: startEpoch?: number, endEpoch?: number
    - Import normalizedToEpochSeconds from @/lib/time-domain
    - Add formatEpochDate helper: converts epoch to "MMM YYYY" format
    - In expanded warp intervals section:
      - Convert startPercent to epoch using normalizedToEpochSeconds
      - Convert endPercent to epoch using normalizedToEpochSeconds
      - Display as "MMM YYYY - MMM YYYY" (e.g., "Jan 2001 - Jun 2003")
      - Show strength as percentage (e.g., "75% warp")
  </action>
  <verify>Card shows date ranges, not percentages</verify>
  <done>AutoProposalSetCard accepts epoch props and displays dates</done>
</task>

<task type="auto">
  <name>Pass epoch values from SuggestionPanel</name>
  <files>src/app/timeslicing/components/SuggestionPanel.tsx</files>
  <action>
    - Find where AutoProposalSetCard is rendered (around line 456)
    - Pass startEpoch={startDate} endEpoch={endDate} to each AutoProposalSetCard
    - Use startDate/endDate from useViewportStart/useViewportEnd or selectionRange
  </action>
  <verify>SuggestionPanel passes epoch values to cards</verify>
  <done>Cards receive actual time range for conversion</done>
</task>

</tasks>

<verification>
- Intervals display as date ranges ("Jan 2001 - Jun 2003")
- Strength shows as percentage ("75% warp")
- Fallback to percentages if epoch props not provided
</verification>

<success_criteria>
- User sees meaningful date ranges in warp intervals
- Clear understanding of which time periods are affected
</success_criteria>

<output>
After completion, create `.planning/phases/40-fully-automated-timeslicing-orchestration/40-06-SUMMARY.md`
</output>
