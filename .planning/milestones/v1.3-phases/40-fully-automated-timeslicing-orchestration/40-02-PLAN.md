---
phase: 40-fully-automated-timeslicing-orchestration
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - src/store/useSuggestionStore.ts
  - src/app/timeslicing/components/AutoProposalSetCard.tsx
  - src/app/timeslicing/components/SuggestionPanel.tsx
autonomous: true

must_haves:
  truths:
    - "User can review top 3 ranked full-auto proposal sets in one place"
    - "User can identify which set is recommended and why"
    - "User can choose a set for quick package acceptance"
  artifacts:
    - path: "src/store/useSuggestionStore.ts"
      provides: "Store state and actions for ranked full-auto proposal sets"
    - path: "src/app/timeslicing/components/AutoProposalSetCard.tsx"
      provides: "UI card for package-level review and selection"
    - path: "src/app/timeslicing/components/SuggestionPanel.tsx"
      provides: "Panel integration for full-auto ranked set review"
  key_links:
    - from: "SuggestionPanel.tsx"
      to: "useSuggestionStore"
      via: "full-auto set list and selected set"
    - from: "AutoProposalSetCard.tsx"
      to: "proposal set score breakdown"
      via: "expanded explanation section"
---

<objective>
Add package-level review UI and store wiring so users can evaluate ranked full-auto sets instead of managing only item-level suggestions.

Purpose: Make full-auto outcomes reviewable and trustworthy before acceptance.
Output: Store support + new proposal-set card + panel integration for ranked full-auto review.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-fully-automated-timeslicing-orchestration/40-CONTEXT.md
@.planning/phases/40-fully-automated-timeslicing-orchestration/40-01-SUMMARY.md
@src/store/useSuggestionStore.ts
@src/app/timeslicing/components/SuggestionPanel.tsx
@src/app/timeslicing/components/SuggestionCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Add full-auto set state and actions to suggestion store</name>
  <files>src/store/useSuggestionStore.ts</files>
  <action>
    Extend the suggestion store with package-level full-auto state:
    - Ranked proposal set collection
    - Selected set id and recommended set id
    - Actions to set results, select a set, clear stale sets, and expose low-confidence/no-result flags
    Preserve existing suggestion-item APIs so current review flows continue to work.
  </action>
  <verify>Run `pnpm exec tsc --noEmit` and confirm existing consumers compile without store API breakage</verify>
  <done>Store can persist and serve ranked full-auto proposal sets while preserving prior behavior</done>
</task>

<task type="auto">
  <name>Create package-level proposal card component</name>
  <files>src/app/timeslicing/components/AutoProposalSetCard.tsx</files>
  <action>
    Build a reusable card component for full-auto sets with:
    - Rank badge and recommended marker
    - Total score display
    - Expand/collapse section for score breakdown dimensions
    - Low-confidence warning treatment when applicable
    - Primary action affordance to select/accept this package
    Keep visual language aligned with existing suggestion cards.
  </action>
  <verify>Run `pnpm exec eslint src/app/timeslicing/components/AutoProposalSetCard.tsx` and confirm card renders expected fields in panel</verify>
  <done>Proposal-set card renders ranked package info and score explanation clearly</done>
</task>

<task type="auto">
  <name>Integrate ranked package review into SuggestionPanel</name>
  <files>src/app/timeslicing/components/SuggestionPanel.tsx</files>
  <action>
    Add a full-auto review section/tab in SuggestionPanel:
    - Render top 3 ranked package cards
    - Preselect recommended set
    - Provide quick package-select action and keep existing per-item views available
    - Show no-result/low-confidence guidance blocks from store state
    Keep panel interaction patterns (history/compare/processed sections) consistent and non-breaking.
  </action>
  <verify>Run app and verify panel shows ranked full-auto package section with recommendation and expandable scoring details</verify>
  <done>User can review and select full-auto ranked packages in the panel without losing existing suggestion workflows</done>
</task>

</tasks>

<verification>
- Full-auto section appears in SuggestionPanel with top 3 ranked package cards
- Recommended package is clearly identified and preselected
- Low-confidence/no-result guidance is visible when returned by generation
</verification>

<success_criteria>
- Ranked package review is first-class in panel UX
- Users can understand recommendation rationale before accepting
- Existing suggestion-item UX remains functional
</success_criteria>

<output>
After completion, create `.planning/phases/40-fully-automated-timeslicing-orchestration/40-02-SUMMARY.md`
</output>
