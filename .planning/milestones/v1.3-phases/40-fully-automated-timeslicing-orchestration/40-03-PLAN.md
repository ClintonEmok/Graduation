---
phase: 40-fully-automated-timeslicing-orchestration
plan: 03
type: execute
wave: 3
depends_on: ["40-01", "40-02"]
files_modified:
  - src/hooks/useSuggestionGenerator.ts
  - src/app/timeslicing/components/SuggestionToolbar.tsx
  - src/app/timeslicing/page.tsx
autonomous: true

must_haves:
  truths:
    - "Full-auto generation runs automatically on entry and context changes, while keeping a manual rerun option"
    - "Users can accept a complete package in one action that applies warp and intervals together"
    - "Low-confidence/no-result states provide clear guidance before acceptance"
  artifacts:
    - path: "src/hooks/useSuggestionGenerator.ts"
      provides: "Auto-run policy with debounce and stale-result safety"
    - path: "src/app/timeslicing/components/SuggestionToolbar.tsx"
      provides: "Manual rerun control and full-auto state visibility"
    - path: "src/app/timeslicing/page.tsx"
      provides: "Atomic package acceptance wiring"
  key_links:
    - from: "useSuggestionGenerator.ts"
      to: "useSuggestionStore"
      via: "set full-auto package results + stale state"
    - from: "page.tsx"
      to: "useWarpSliceStore + useSliceStore"
      via: "single action applies selected package"
---

<objective>
Complete the full-auto orchestration loop with automatic triggering, atomic package acceptance, and robust low-confidence/no-result behavior.

Purpose: Make the phase outcome genuinely full-auto while preserving user trust and control.
Output: Auto-run policies, one-click package apply, and clear safeguard UX.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-fully-automated-timeslicing-orchestration/40-CONTEXT.md
@.planning/phases/40-fully-automated-timeslicing-orchestration/40-01-SUMMARY.md
@.planning/phases/40-fully-automated-timeslicing-orchestration/40-02-SUMMARY.md
@src/hooks/useSuggestionGenerator.ts
@src/app/timeslicing/components/SuggestionToolbar.tsx
@src/app/timeslicing/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Implement hybrid auto-run orchestration behavior</name>
  <files>src/hooks/useSuggestionGenerator.ts</files>
  <action>
    Implement trigger policy from context decisions:
    - Auto-run full-auto generation once on page entry
    - Auto-regenerate on meaningful context/filter signature changes with debounce + in-flight guard
    - Keep manual rerun available and first-class
    - Do not silently mutate accepted state; mark newly generated results as fresh/stale for user review
    Preserve existing guardrails against feedback loops.
  </action>
  <verify>Run /timeslicing and confirm initial auto-run + debounced auto-refresh + manual rerun all work without infinite loops</verify>
  <done>Generation behavior matches hybrid policy and keeps user-controlled acceptance semantics</done>
</task>

<task type="auto">
  <name>Add one-click atomic package acceptance wiring</name>
  <files>src/app/timeslicing/page.tsx</files>
  <action>
    Add a package-level accept path that applies selected full-auto set atomically:
    - Clear/replace previous active warp set according to existing single-active-warp behavior
    - Apply interval boundaries as timeline slices in same interaction flow
    - Mark accepted package in store/history metadata
    Ensure partial apply states cannot occur on a successful accept action.
  </action>
  <verify>Accept a ranked package and verify both warp + intervals update together in timeline and panel state</verify>
  <done>User can accept a complete full-auto package in one action with consistent resulting state</done>
</task>

<task type="auto">
  <name>Finalize toolbar and no-result/low-confidence safeguards</name>
  <files>src/app/timeslicing/components/SuggestionToolbar.tsx</files>
  <action>
    Update toolbar and user feedback to support full-auto state:
    - Expose rerun controls and status text for auto-generated results
    - Surface low-confidence warning and no-result guidance copy
    - Disable package accept pathways when no-result state is active
    Keep messaging concise and actionable (adjust filters/context, expand date range, rerun).
  </action>
  <verify>Simulate sparse-data and no-result scenarios; confirm warning/guidance appears and unsafe accepts are blocked</verify>
  <done>Users receive clear safeguards in weak-data cases while retaining rerun controls</done>
</task>

</tasks>

<verification>
- Full-auto generation runs on entry and context changes with stable debounce behavior
- Manual rerun remains available and functional
- Package acceptance applies warp + intervals atomically
- Low-confidence/no-result states are visible and enforce safe interaction rules
</verification>

<success_criteria>
- Phase 40 delivers a true full-auto orchestration loop with user review
- Users can trust recommendations via transparent scores and safeguards
- Acceptance flow is package-oriented and consistent with existing timeline state rules
</success_criteria>

<output>
After completion, create `.planning/phases/40-fully-automated-timeslicing-orchestration/40-03-SUMMARY.md`
</output>
