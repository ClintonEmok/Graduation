---
phase: 40-fully-automated-timeslicing-orchestration
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/timeslicing/components/AutoProposalSetCard.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Each warp interval shows start-end range with weight percentage"
    - "User can see what time periods are being warped"
    - "Weights are displayed as percentage (e.g., 75%)"
  artifacts:
    - path: "src/app/timeslicing/components/AutoProposalSetCard.tsx"
      provides: "Expanded card shows warp interval details"
---

<objective>
Display warp interval details in full-auto package cards.

Purpose: Users need to see WHAT time periods are being warped and by HOW MUCH to make informed decisions.
Output: Expanded card shows each interval's start-end range and weight percentage.
</objective>

<context>
@src/types/autoProposalSet.ts
@src/app/timeslicing/components/AutoProposalSetCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Add warp interval display to card</name>
  <files>src/app/timeslicing/components/AutoProposalSetCard.tsx</files>
  <action>
    In the expanded view, add a warp intervals section that displays:
    - Each interval as a row with: start %, end %, weight %
    - Format percentages with 1 decimal place (e.g., "0% → 25%" or "25.0% → 50.0%")
    - Show strength as percentage (e.g., "strength: 75%")
    - If intervals are date-based, convert using the time range (pass context if needed)
    
    The data is in proposalSet.warp.intervals[]:
    - startPercent: number (0-100)
    - endPercent: number (0-100)  
    - strength: number (0-1, display as percentage)
  </action>
  <verify>Expand card shows each interval with start-end range and weight</verify>
  <done>Card displays warp interval details in expanded section</done>
</task>

</tasks>

<verification>
- Card expanded view shows list of intervals
- Each interval shows start%, end%, and strength%
- Format is readable (e.g., "0% → 25% (75%)" or "0-25%: 75% warp")
</verification>

<success_criteria>
- User can see exact warp intervals being suggested
- Weight/strength clearly displayed
- Informed selection decision possible
</success_criteria>

<output>
After completion, create `.planning/phases/40-fully-automated-timeslicing-orchestration/40-05-SUMMARY.md`
</output>
