---
phase: 40-fully-automated-timeslicing-orchestration
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/timeslicing/components/SuggestionPanel.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Test button generates extreme warp package"
    - "Extreme warp is clearly visible on timeline"
    - "Can see the warping effect apply instantly"
  artifacts:
    - path: "src/app/timeslicing/components/SuggestionPanel.tsx"
      provides: "Add debug test button"
---

<objective>
Add test button to generate extreme warp package.

Purpose: Visually confirm that warping actually works by applying a dramatically exaggerated warp to the timeline.
Output: Debug button that creates a test package with 5x-10x warp multiplier on first 20% and last 10% of timeline.
</objective>

<context>
@src/app/timeslicing/components/SuggestionPanel.tsx
@src/types/autoProposalSet.ts
</context>

<tasks>

<task type="auto">
  <name>Add debug test extreme warp button</name>
  <files>src/app/timeslicing/components/SuggestionPanel.tsx</files>
  <action>
    - Add a "Test Extreme Warp" debug button (only show in dev/test mode, or always)
    - When clicked, create a test AutoProposalSet with:
      - Warp intervals: 
        - 0-20%: 10x strength (extremely stretched)
        - 20-80%: 0.1x strength (extremely compressed)
        - 80-100%: 8x strength (extremely stretched)
      - Name: "Test Extreme Warp (DEBUG)"
      - Confidence: 100%
    - Add to panel in a visible location
    - Clicking should immediately show the package and user can accept it
  </action>
  <verify>Button appears, clicking generates test package with extreme values</verify>
  <done>Test extreme warp button functional</done>
</task>

</tasks>

<verification>
- Click "Test Extreme Warp" button
- Package appears showing 10x, 0.1x, 8x warp intervals
- Accept it
- Timeline warps dramatically with compressed middle section
</verification>

<success_criteria>
- Extreme warp visibly compresses/stretches timeline sections
- Clear visual proof that warping works
</success_criteria>

<output>
After completion, create `.planning/phases/40-fully-automated-timeslicing-orchestration/40-11-SUMMARY.md`
</output>
