---
phase: 45-3d-suggestion-and-acceptance-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/app/timeline-test-3d/page.tsx]
autonomous: true

must_haves:
  truths:
    - "SuggestionPanel is imported and rendered in timeline-test-3d route"
    - "Generate/review/compare/history flows work in 3D test"
    - "Ranked package rationale (whyRecommended) visible in 3D test"
    - "Score breakdown visible in 3D test review flow"
    - "Accepting package applies artifacts consistently"
  artifacts:
    - path: "src/app/timeline-test-3d/page.tsx"
      provides: "SuggestionPanel rendered in 3D test interface"
      min_lines: 390
  key_links:
    - from: "SuggestionPanel"
      to: "useSuggestionStore"
      via: "fullAutoProposalSets, selectedFullAutoSetId, fullAutoNoResultReason, addToHistory"
      pattern: "store hooks already wired"
    - from: "SuggestionPanel accept buttons"
      to: "handleAcceptFullAutoPackage"
      via: "accept-full-auto-package custom event"
      pattern: "window\\.dispatchEvent.*CustomEvent"
---

<objective>
Add SuggestionPanel component to the 3D test route to enable full suggestion workflow parity.

Purpose: Users can generate, review, compare, and accept suggestions in the 3D test experience with the same functionality available in timeslicing.
Output: SuggestionPanel rendered in timeline-test-3d/page.tsx, suggestion workflows functional.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/timeline-test-3d/page.tsx
@src/app/timeslicing/page.tsx (for SuggestionPanel import reference)
@src/app/timeslicing/components/SuggestionPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Add SuggestionPanel import and render to timeline-test-3d</name>
  <files>src/app/timeline-test-3d/page.tsx, src/app/timeline-test-3d/components/SuggestionPanel.tsx, src/app/timeline-test-3d/components/SuggestionCard.tsx, src/app/timeline-test-3d/components/AutoProposalSetCard.tsx, src/app/timeline-test-3d/components/ComparisonView.tsx, src/app/timeline-test-3d/components/ProfileManager.tsx, src/app/timeline-test-3d/components/ContextBadge.tsx, src/app/timeline-test-3d/components/ConfidenceBadge.tsx</files>
  <action>
1. Create the components directory: src/app/timeline-test-3d/components/ (if not exists)

2. Copy all suggestion-related component files from src/app/timeslicing/components/ to src/app/timeline-test-3d/components/:
   - SuggestionPanel.tsx
   - SuggestionCard.tsx
   - AutoProposalSetCard.tsx
   - ComparisonView.tsx
   - ProfileManager.tsx
   - ContextBadge.tsx
   - ConfidenceBadge.tsx

3. In SuggestionPanel.tsx, update internal imports to use relative paths within the new location:
   ```typescript
   import { SuggestionCard } from './SuggestionCard';
   import { ComparisonView } from './ComparisonView';
   import { ProfileManager } from './ProfileManager';
   import { AutoProposalSetCard } from './AutoProposalSetCard';
   import { ContextBadge } from './ContextBadge';
   import { ConfidenceBadge } from './ConfidenceBadge';
   ```

4. Add import for SuggestionPanel in timeline-test-3d/page.tsx:
   ```typescript
   import { SuggestionPanel } from './components/SuggestionPanel';
   ```

5. Render SuggestionPanel in the UI - add it below SuggestionToolbar in the controls section (around line 351). The SuggestionPanel should appear after SuggestionToolbar for proper flow (generate -> review).
  </action>
  <verify>
- SuggestionPanel renders without console errors
- Generate suggestions button triggers fullAutoProposalSets to populate
- Ranked packages display with whyRecommended rationale
- Score breakdown is visible
- Accept button dispatches accept-full-auto-package event
  </verify>
  <done>
SuggestionPanel component is imported and rendered in timeline-test-3d route. 
Users can: generate suggestions, review ranked packages with rationale/scores, compare history, and accept packages.
</done>
</task>

</tasks>

<verification>
1. Build succeeds: npm run build (or next build) completes without errors
2. Page loads: /timeline-test-3d renders with SuggestionPanel visible
3. Generate works: Clicking generate suggestions populates the panel
4. Review works: Ranked packages show whyRecommended and score breakdown
5. Accept works: Accepting a package applies artifacts and preserves safeguards
</verification>

<success_criteria>
- CUBE-06: Suggestion generation and review (manual and auto) are usable in 3D test
- CUBE-07: Ranked package rationale (whyRecommended) and score breakdown are visible in 3D test review flow
- CUBE-08: Accepting reviewed packages in 3D applies artifacts consistently and preserves safeguards/rerun behavior
</success_criteria>

<output>
After completion, create `.planning/phases/45-3d-suggestion-and-acceptance-parity/45-01-SUMMARY.md`
</output>
