---
phase: 44-3d-interaction-parity
plan: "02"
type: execute
wave: 2
depends_on: [44-01-SUMMARY]
files_modified:
  - src/app/timeline-test-3d/components/TimeSlices3D.tsx
  - src/app/timeline-test-3d/components/WarpSlices3D.tsx
  - src/app/timeline-test-3d/components/TimelineTest3DScene.tsx
autonomous: true
must_haves:
  truths:
    - "Users can drag slice boundaries to adjust slice ranges in 3D"
    - "Users can see warp slices visualized in 3D with appropriate styling"
    - "Selected slices show active state visual feedback in 3D"
    - "Warp preview/overlap cues are visible when hovering or adjusting"
  artifacts:
    - path: "src/app/timeline-test-3d/components/WarpSlices3D.tsx"
      provides: "3D warp slice visualization"
      min_lines: 60
  key_links:
    - from: "TimelineTest3DScene.tsx"
      to: "WarpSlices3D.tsx"
      via: "component import"
      pattern: "WarpSlices3D"
    - from: "WarpSlices3D.tsx"
      to: "useWarpSliceStore"
      via: "store hook"
      pattern: "useWarpSliceStore"
---

<objective>
Add slice editing, warp slice visualization, and interaction feedback to timeline-test-3d.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-3d-interaction-parity/44-01-SUMMARY.md
@src/app/timeline-test/components/WarpSliceEditor.tsx
@src/store/useWarpSliceStore.ts
@src/components/viz/SlicePlane.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add boundary drag editing to TimeSlices3D</name>
  <files>src/app/timeline-test-3d/components/TimeSlices3D.tsx</files>
  <action>
    Extend TimeSlices3D to support boundary dragging for range slices:
    - Add drag handles at slice boundaries (top and bottom edges)
    - Use useSliceAdjustmentStore for snap settings (adaptive/fixed mode, snap presets)
    - On drag, update slice range percent values via useSliceStore.updateSlice
    - Add visual feedback during drag (highlight, cursor change)

    Reference SlicePlane.tsx for boundary handle patterns
    Reference useSliceAdjustmentStore for snap configuration
  </action>
  <verify>
    Run build to ensure no TypeScript errors
  </verify>
  <done>
    Users can drag slice boundaries in 3D to adjust ranges
  </done>
</task>

<task type="auto">
  <name>Task 2: Create WarpSlices3D component for warp visualization</name>
  <files>src/app/timeline-test-3d/components/WarpSlices3D.tsx</files>
  <action>
    Create a new WarpSlices3D component that:
    - Imports useWarpSliceStore to get warp slices
    - Imports useAdaptiveStore for warpMap and mapDomain
    - Visualizes each warp slice as a semi-transparent plane in 3D
    - Uses different styling than regular slices (e.g., cyan/amber color, lower opacity)
    - Shows slice label on hover via Html from @react-three/drei

    Use the same Y-positioning logic as TimelineTest3DPoints.tsx for consistency
  </action>
  <verify>
    Run build to ensure no TypeScript errors
  </verify>
  <done>
    WarpSlices3D component exists and renders warp slices in 3D
  </done>
</task>

<task type="auto">
  <name>Task 3: Add interaction feedback (hover, active state)</name>
  <files>src/app/timeline-test-3d/components/TimeSlices3D.tsx
src/app/timeline-test-3d/components/WarpSlices3D.tsx</files>
  <action>
    Add visual feedback for interactions:
    - Hover: brighten color, show label with slice name
    - Active slice: different border/outline color
    - Selected slices: highlight border (blue or per SlicePlane.tsx pattern)
    - When adjusting boundaries: show preview of new position

    Reference SlicePlane.tsx for hover and active state patterns
  </action>
  <verify>
    Run build to ensure no TypeScript errors
  </verify>
  <done>
    All interaction states (hover, selected, active) have appropriate visual feedback
  </done>
</task>

<task type="auto">
  <name>Task 4: Wire WarpSlices3D into TimelineTest3DScene</name>
  <files>src/app/timeline-test-3d/components/TimelineTest3DScene.tsx</files>
  <action>
    Update TimelineTest3DScene to include WarpSlices3D:
    - Import WarpSlices3D from ./WarpSlices3D
    - Add <WarpSlices3D /> inside the <Scene> component, after TimeSlices3D
  </action>
  <verify>
    Run build to ensure no TypeScript errors
  </verify>
  <done>
    WarpSlices3D renders inside the 3D scene alongside regular slices
  </done>
</task>

</tasks>

<verification>
- [ ] Navigate to /timeline-test-3d and verify warp slices appear in 3D when created via WarpSliceEditor
- [ ] Drag slice boundaries in 3D and verify the slice range updates
- [ ] Hover over slices to verify hover feedback (label, color change)
- [ ] Select slices and verify active/selected state is visible
</verification>

<success_criteria>
Users can adjust slice boundaries in 3D, see warp slices visualized, and interaction feedback is visible for all states.
</success_criteria>

<output>
After completion, create `.planning/phases/44-3d-interaction-parity/44-02-SUMMARY.md`
</output>
