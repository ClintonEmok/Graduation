---
phase: 06-demo-timeline-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
requirements_addressed:
  - TPL-01
  - TPL-02
  - TPL-03
  - TPL-04
  - TPL-05
files_modified:
  - src/components/dashboard-demo/DemoTimelinePanel.tsx
  - src/components/timeline/DemoDualTimeline.tsx
  - src/app/dashboard-demo/page.shell.test.tsx
autonomous: true
must_haves:
  truths:
    - "The demo timeline stays calm and readable even when warp is enabled."
    - "Demo slices in `/dashboard-demo` still shape warping through the slice-authored warp map."
    - "Warp is explained with a quiet, bands-first cue instead of connector-heavy chrome."
    - "Stable `/dashboard` and `/timeslicing` remain isolated from demo warp wiring."
    - "Warp controls remain available in the demo timeline without dominating the card."
  artifacts:
    - path: "src/components/dashboard-demo/DemoTimelinePanel.tsx"
      provides: "Compact demo timeline legend and warp control row"
    - path: "src/components/timeline/DemoDualTimeline.tsx"
      provides: "Demo-local slice-authored warp map wiring into the dual timeline"
    - path: "src/app/dashboard-demo/page.shell.test.tsx"
      provides: "Source-inspection coverage for demo warp behavior and stable-route isolation"
  key_links:
    - from: "src/components/timeline/DemoDualTimeline.tsx"
      to: "src/components/dashboard-demo/lib/demo-warp-map.ts"
      via: "buildDemoSliceAuthoredWarpMap turns demo slices into the adaptive warp map"
      pattern: "buildDemoSliceAuthoredWarpMap"
    - from: "src/components/timeline/DemoDualTimeline.tsx"
      to: "src/components/timeline/DualTimeline.tsx"
      via: "adaptiveWarpMapOverride plus warpOverlayBandsOverride keep the visible warp cue quiet"
      pattern: "adaptiveWarpMapOverride|warpOverlayBandsOverride"
    - from: "src/app/dashboard-demo/page.shell.test.tsx"
      to: "src/app/dashboard/page.tsx and src/app/timeslicing/page.tsx"
      via: "source-inspection guards keep the stable routes free of demo warp references"
      pattern: "DashboardDemoShell|DemoTimelinePanel|DemoDualTimeline|useDashboardDemoWarpStore|buildDemoSliceAuthoredWarpMap"
---

<objective>
Add the warp follow-up to Phase 6 without reopening the route architecture: keep `/dashboard-demo` demo-local, keep the proven slice-authored warp-map approach, and simplify the visible warp language so the timeline stays calm.

Purpose: respond to the first warp pass looking too busy while preserving the working demo-local warp plumbing.
Output: a quieter demo timeline surface, a bands-first warp cue, and regression coverage that keeps stable routes isolated.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-demo-timeline-polish/06-CONTEXT.md
@.planning/phases/06-demo-timeline-polish/06-DISCUSSION-LOG.md
@.planning/phases/06-demo-timeline-polish/06-01-SUMMARY.md
@.planning/phases/06-demo-timeline-polish/06-UAT.md
@src/app/timeline-test/page.tsx
@src/components/dashboard-demo/lib/demo-warp-map.ts
@src/components/timeline/DemoDualTimeline.tsx
@src/components/dashboard-demo/DemoTimelinePanel.tsx
@src/app/dashboard-demo/page.shell.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify the demo-local warp surface</name>
  <files>src/components/dashboard-demo/DemoTimelinePanel.tsx, src/components/timeline/DemoDualTimeline.tsx</files>
  <read_first>
    - src/components/dashboard-demo/DemoTimelinePanel.tsx
    - src/components/timeline/DemoDualTimeline.tsx
    - src/components/dashboard-demo/lib/demo-warp-map.ts
    - src/app/timeline-test/page.tsx
    - src/components/timeline/DualTimeline.tsx
  </read_first>
  <action>
    In `DemoTimelinePanel.tsx`, collapse the helper copy into a single short legend line (keep the existing "Focused track above • raw baseline below" intent), keep the temporal-resolution slider and the play/step/speed/time-scale controls in one compact row, and avoid any extra warp explanation copy that makes the card feel taller.

    In `DemoDualTimeline.tsx`, keep the demo-local store overrides, keep `disableAutoBurstSlices={true}` by default, continue deriving the adaptive warp map from `buildDemoSliceAuthoredWarpMap(slices, warpDomain, Math.max(96, slices.length * 8 || 0))`, and add a demo-local `warpOverlayBandsOverride` derived from `useDashboardDemoSliceStore` so the visible cue is subtle bands instead of connector-heavy chrome. Do not enable `showWarpConnectors` on the demo path, and do not add any stable-route imports or cross-route wiring.
  </action>
  <acceptance_criteria>
    - `DemoTimelinePanel.tsx` still renders `DemoDualTimeline` and the temporal-resolution slider.
    - `DemoTimelinePanel.tsx` contains a compact legend line with `Focused track above` and `raw baseline below`.
    - `DemoDualTimeline.tsx` still contains `buildDemoSliceAuthoredWarpMap` and `adaptiveWarpMapOverride`.
    - `DemoDualTimeline.tsx` contains `warpOverlayBandsOverride` for the demo-local slice bands.
    - `DemoDualTimeline.tsx` does not set `showWarpConnectors={true}` or `warpConnectorStyle="curved"` in the demo wrapper.
  </acceptance_criteria>
  <verify>grep -nE "Focused track above|raw baseline below|buildDemoSliceAuthoredWarpMap|warpOverlayBandsOverride" src/components/dashboard-demo/DemoTimelinePanel.tsx src/components/timeline/DemoDualTimeline.tsx</verify>
  <done>The demo timeline keeps the same warp plumbing but reads as a calmer two-track surface with a quiet warp cue.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the demo warp contract and stable-route isolation</name>
  <files>src/app/dashboard-demo/page.shell.test.tsx</files>
  <read_first>
    - src/app/dashboard-demo/page.shell.test.tsx
    - src/app/dashboard/page.tsx
    - src/app/timeslicing/page.tsx
    - src/components/dashboard-demo/DemoTimelinePanel.tsx
    - src/components/timeline/DemoDualTimeline.tsx
  </read_first>
  <action>
    Update the source-inspection regression test so it explicitly locks the quieter warp follow-up: assert that the demo timeline panel still uses the compact legend and warp controls, assert that the demo dual timeline uses `buildDemoSliceAuthoredWarpMap` plus `warpOverlayBandsOverride`, and assert that the stable `/dashboard` and `/timeslicing` pages do not reference `DashboardDemoShell`, `DemoTimelinePanel`, `DemoDualTimeline`, `useDashboardDemoWarpStore`, or `buildDemoSliceAuthoredWarpMap`.

    Keep the existing shell isolation checks in place; the goal is to prove the demo warp surface is still demo-local while the stable routes remain untouched.
  </action>
  <acceptance_criteria>
    - `page.shell.test.tsx` contains assertions for `buildDemoSliceAuthoredWarpMap` and `warpOverlayBandsOverride`.
    - `page.shell.test.tsx` still asserts stable-route isolation for `/dashboard` and `/timeslicing`.
    - `page.shell.test.tsx` does not assert any connector-heavy demo wording as the primary warp explanation.
  </acceptance_criteria>
  <verify>pnpm exec vitest run src/app/dashboard-demo/page.shell.test.tsx --reporter=dot</verify>
  <done>The shell test locks the quieter warp surface and protects the stable routes from demo warp leakage.</done>
</task>

</tasks>

<verification>
Run the dashboard-demo shell test, then inspect the updated sources to confirm the warp follow-up is quiet, demo-local, and bands-first.
</verification>

<success_criteria>
The Phase 6 demo timeline remains calm and readable, demo slices still shape warping through the proven slice-authored map, and `/dashboard` plus `/timeslicing` stay isolated from the demo warp surface.
</success_criteria>

<output>
After completion, create `.planning/phases/06-demo-timeline-polish/06-02-SUMMARY.md`
</output>
