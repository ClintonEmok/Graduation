---
phase: 07-dashboard-demo-preset-thresholds
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard-demo/lib/demo-preset-thresholds.ts
  - src/store/useDashboardDemoTimeslicingModeStore.ts
  - src/components/dashboard-demo/DemoSlicePanel.tsx
  - src/app/dashboard-demo/page.shell.test.tsx
autonomous: true
requirements:
  - PTH-01
  - PTH-02
  - PTH-03
  - PTH-04
  - PTH-05
must_haves:
  truths:
    - "User can tune Bias separately for each existing generation preset inside /dashboard-demo."
    - "Switching presets restores that preset's own Bias value instead of a shared global value."
    - "Bias edits stay demo-local and do not change the stable /timeslicing route."
    - "The demo reuses existing preset concepts rather than introducing a new algorithm family."
  artifacts:
    - path: "src/components/dashboard-demo/lib/demo-preset-thresholds.ts"
      provides: "Demo-local Bias defaults, labels, and reset copy"
    - path: "src/store/useDashboardDemoTimeslicingModeStore.ts"
      provides: "Persisted per-preset Bias state for the demo"
    - path: "src/components/dashboard-demo/DemoSlicePanel.tsx"
      provides: "Preset-scoped Bias controls in the demo rail"
    - path: "src/app/dashboard-demo/page.shell.test.tsx"
      provides: "Source-inspection coverage for demo-local controls and stable-route isolation"
  key_links:
    - from: "src/components/dashboard-demo/DemoSlicePanel.tsx"
      to: "src/store/useDashboardDemoTimeslicingModeStore.ts"
      via: "preset switch and Bias editor interactions"
      pattern: "presetBiases|setPresetBias|resetPresetBias|resetAllPresetBiases"
    - from: "src/store/useDashboardDemoTimeslicingModeStore.ts"
      to: "src/components/dashboard-demo/lib/demo-preset-thresholds.ts"
      via: "seed defaults and reset targets for each preset family"
      pattern: "DEFAULT_PRESET_BIASES|PRESET_BIAS_RANGE|PRESET_BIAS_HELPERS"
    - from: "src/app/dashboard-demo/page.shell.test.tsx"
      to: "src/store/useTimeslicingModeStore.ts"
      via: "source inspection proving the stable route stays unchanged"
      pattern: "useTimeslicingModeStore|TimesliceToolbar|Bias|presetBiases"
---

<objective>
Give `/dashboard-demo` separate, user-editable Bias controls for each existing generation preset so the demo can be tuned per preset without touching the stable route.

Purpose: preserve the demo-local preset experimentation workflow while keeping the `/timeslicing` route stable and unchanged.
Output: per-preset Bias state, demo rail controls, and regression coverage that locks the route boundary.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-dashboard-demo-preset-thresholds/07-CONTEXT.md
@.planning/phases/06-demo-timeline-polish/06-CONTEXT.md
@.planning/phases/06-demo-timeline-polish/06-02-SUMMARY.md
@src/store/useDashboardDemoTimeslicingModeStore.ts
@src/store/useTimeslicingModeStore.ts
@src/components/dashboard-demo/DemoSlicePanel.tsx
@src/components/dashboard-demo/DemoStkdePanel.tsx
@src/components/timeslicing/TimesliceToolbar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add demo-local per-preset Bias state</name>
  <files>src/components/dashboard-demo/lib/demo-preset-thresholds.ts, src/store/useDashboardDemoTimeslicingModeStore.ts</files>
  <read_first>
    - src/store/useDashboardDemoTimeslicingModeStore.ts
    - src/store/useTimeslicingModeStore.ts
    - src/components/dashboard-demo/DemoStkdePanel.tsx
    - .planning/phases/07-dashboard-demo-preset-thresholds/07-CONTEXT.md
  </read_first>
  <action>
    Define the demo-local preset Bias schema and defaults for the existing preset families: hourly, daily, weekly, monthly, weekday-weekend, morning-afternoon-evening-night, business-hours, and custom. Use a 0-100 slider scale with coarse 5-point steps, friendlier preset labels, compact helper copy, and preset-specific reset values. Add persisted store state keyed by preset, plus setters and resetters that only touch the dashboard-demo store and leave `/timeslicing` untouched.
  </action>
  <acceptance_criteria>
    - `grep -n "DEFAULT_PRESET_BIASES\|PRESET_BIAS_RANGE\|presetBiases\|setPresetBias\|resetPresetBias\|resetAllPresetBiases" src/components/dashboard-demo/lib/demo-preset-thresholds.ts src/store/useDashboardDemoTimeslicingModeStore.ts` returns matches.
    - `src/store/useDashboardDemoTimeslicingModeStore.ts` still does not import `useTimeslicingModeStore`.
  </acceptance_criteria>
  <done>The demo store can persist one Bias value per preset and can restore a preset or all presets back to their recommended starting values.</done>
</task>

<task type="auto">
  <name>Task 2: Render preset-scoped Bias controls in the demo rail</name>
  <files>src/components/dashboard-demo/DemoSlicePanel.tsx, src/app/dashboard-demo/page.shell.test.tsx</files>
  <read_first>
    - src/components/dashboard-demo/DemoSlicePanel.tsx
    - src/app/dashboard-demo/page.shell.test.tsx
    - src/components/dashboard-demo/lib/demo-preset-thresholds.ts
    - src/store/useDashboardDemoTimeslicingModeStore.ts
    - src/store/useTimeslicingModeStore.ts
    - src/components/timeslicing/TimesliceToolbar.tsx
  </read_first>
  <action>
    Add a preset-selection and Bias-editing block to the demo slice rail. Show the active preset badge, a compact summary line, a slider for the active preset's Bias value, and footer reset actions for the current preset and all presets with a confirmation prompt. Use the helper copy from demo-preset-thresholds.ts for the preset names and summary text, and keep all writes on useDashboardDemoTimeslicingModeStore only. Extend the shell test so it source-inspects the demo panel for Bias/Active/reset copy and source-inspects src/store/useTimeslicingModeStore.ts and src/components/timeslicing/TimesliceToolbar.tsx to prove the stable route did not gain demo-local Bias controls.
  </action>
  <acceptance_criteria>
    - `pnpm exec vitest run src/app/dashboard-demo/page.shell.test.tsx --reporter=dot` exits 0.
    - `grep -n "Bias\|Active\|Reset preset\|Reset all\|presetBiases" src/components/dashboard-demo/DemoSlicePanel.tsx src/app/dashboard-demo/page.shell.test.tsx` returns matches.
    - `src/app/dashboard-demo/page.shell.test.tsx` asserts that `src/store/useTimeslicingModeStore.ts` and `src/components/timeslicing/TimesliceToolbar.tsx` do not contain demo-local Bias controls.
  </acceptance_criteria>
  <done>/dashboard-demo exposes per-preset Bias controls, switching presets restores the matching values, and the stable /timeslicing route remains free of demo-local threshold UI.</done>
</task>

</tasks>

<verification>
Run the dashboard-demo shell test and confirm the preset controls remain demo-local, preset-scoped, and isolated from the stable route.
</verification>

<success_criteria>
Each demo preset has its own Bias, switching presets restores the correct values, and the stable route stays untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-demo-preset-thresholds/07-01-SUMMARY.md`
</output>
