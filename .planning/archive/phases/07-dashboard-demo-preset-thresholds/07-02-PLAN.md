---
phase: 07-dashboard-demo-preset-thresholds
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/store/useDashboardDemoTimeslicingModeStore.ts
  - src/components/dashboard-demo/DemoSlicePanel.tsx
  - src/components/dashboard-demo/lib/demo-preset-thresholds.ts
  - src/app/dashboard-demo/page.shell.test.tsx
autonomous: true
requirements:
  - PTH-01
  - PTH-02
  - PTH-03
  - PTH-04
  - PTH-05
must_haves:
  truths:
    - "User can generate demo slices from the selected preset using that preset's own Bias threshold."
    - "Generation behavior in `/dashboard-demo` uses the per-preset Bias values from the demo-local store."
    - "Preset switching preserves each preset's own Bias value and influences generation immediately."
    - "`/timeslicing` remains unchanged; generation integration is demo-local only."
  artifacts:
    - path: "src/store/useDashboardDemoTimeslicingModeStore.ts"
      provides: "Demo-local generation actions that consume per-preset Bias values"
    - path: "src/components/dashboard-demo/DemoSlicePanel.tsx"
      provides: "Demo generation trigger wired to active preset + Bias"
    - path: "src/components/dashboard-demo/lib/demo-preset-thresholds.ts"
      provides: "Preset metadata/defaults reused by generation logic"
    - path: "src/app/dashboard-demo/page.shell.test.tsx"
      provides: "Regression coverage for demo-only generation wiring and stable-route isolation"
  key_links:
    - from: "src/components/dashboard-demo/DemoSlicePanel.tsx"
      to: "src/store/useDashboardDemoTimeslicingModeStore.ts"
      via: "trigger generation using active preset + presetBiases[preset]"
      pattern: "presetBiases|preset|generate"
    - from: "src/store/useDashboardDemoTimeslicingModeStore.ts"
      to: "src/components/dashboard-demo/lib/demo-preset-thresholds.ts"
      via: "apply recommended defaults and bias range constraints during generation"
      pattern: "DEFAULT_PRESET_BIASES|clampPresetBias"
    - from: "src/app/dashboard-demo/page.shell.test.tsx"
      to: "src/store/useTimeslicingModeStore.ts"
      via: "assert no demo-local preset-generation hooks leaked into stable route"
      pattern: "presetBiases|Bias|generate"
---

<objective>
Wire slice generation in `/dashboard-demo` to the new per-preset Bias controls so users can generate slices from context-aware presets using their own threshold values.

Purpose: complete the first practical loop from preset tuning → generation result inside the demo surface.
Output: demo-local generation actions and UI wiring that use active preset + per-preset Bias while preserving stable-route isolation.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-dashboard-demo-preset-thresholds/07-CONTEXT.md
@.planning/phases/07-dashboard-demo-preset-thresholds/07-DISCUSSION.md
@.planning/phases/07-dashboard-demo-preset-thresholds/07-01-SUMMARY.md
@src/store/useDashboardDemoTimeslicingModeStore.ts
@src/components/dashboard-demo/DemoSlicePanel.tsx
@src/components/dashboard-demo/lib/demo-preset-thresholds.ts
@src/components/binning/BinningControls.tsx
@src/store/useTimeslicingModeStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add demo-local generation actions tied to preset Bias</name>
  <files>src/store/useDashboardDemoTimeslicingModeStore.ts, src/components/dashboard-demo/lib/demo-preset-thresholds.ts</files>
  <read_first>
    - src/store/useDashboardDemoTimeslicingModeStore.ts
    - src/components/dashboard-demo/lib/demo-preset-thresholds.ts
    - src/store/useTimeslicingModeStore.ts
    - src/components/binning/BinningControls.tsx
  </read_first>
  <action>
    Introduce demo-local generation helper/actions that derive generation behavior from the active preset and its Bias value. Keep the implementation simple for iteration 1: map preset families to coarse generation profiles and apply per-preset Bias as the primary threshold signal. Ensure values are clamped to demo Bias constraints and state remains isolated to dashboard-demo store paths.
  </action>
  <acceptance_criteria>
    - Demo store exposes generation action(s) that read `preset` + `presetBiases[preset]`.
    - Preset Bias changes can affect generation behavior without changing other presets.
    - Stable `useTimeslicingModeStore` remains untouched by demo-local generation actions.
  </acceptance_criteria>
  <verify>grep -n "presetBiases\|preset\|generate" src/store/useDashboardDemoTimeslicingModeStore.ts</verify>
  <done>Demo-local generation logic is explicitly parameterized by active preset Bias.</done>
</task>

<task type="auto">
  <name>Task 2: Wire generation trigger in demo slice rail and lock regressions</name>
  <files>src/components/dashboard-demo/DemoSlicePanel.tsx, src/app/dashboard-demo/page.shell.test.tsx</files>
  <read_first>
    - src/components/dashboard-demo/DemoSlicePanel.tsx
    - src/app/dashboard-demo/page.shell.test.tsx
    - src/store/useDashboardDemoTimeslicingModeStore.ts
  </read_first>
  <action>
    Add a clear generation trigger in the demo slice rail that runs the new demo-local generation action using the active preset and its Bias. Keep UX compact and consistent with existing rail tone. Extend shell/source regression assertions to lock that generation is wired in dashboard-demo and that `/timeslicing` does not gain demo-specific preset threshold generation controls.
  </action>
  <acceptance_criteria>
    - `/dashboard-demo` has a generation trigger tied to active preset Bias.
    - Source-inspection test covers demo generation tokens and stable-route absence checks.
    - Existing dashboard-demo shell tests continue passing.
  </acceptance_criteria>
  <verify>pnpm exec vitest run src/app/dashboard-demo/page.shell.test.tsx --reporter=dot</verify>
  <done>Users can tune preset Bias and generate slices from that tuned preset directly in dashboard-demo.</done>
</task>

</tasks>

<verification>
Confirm preset switching + per-preset Bias tuning impacts generation behavior in `/dashboard-demo`, and confirm `/timeslicing` remains unchanged.
</verification>

<success_criteria>
Dashboard-demo supports the first complete preset-threshold generation loop: tune Bias per preset, then generate slices from those tuned values.
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-demo-preset-thresholds/07-02-SUMMARY.md`
</output>
