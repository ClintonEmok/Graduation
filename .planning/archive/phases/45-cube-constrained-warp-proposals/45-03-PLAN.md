---
phase: 45-cube-constrained-warp-proposals
plan: 03
type: execute
wave: 2
depends_on: [45-01, 45-02]
files_modified:
  - src/app/cube-sandbox/lib/applyWarpProposal.ts
  - src/store/useAdaptiveStore.ts
  - src/store/useWarpProposalStore.ts
  - src/components/viz/CubeVisualization.tsx
autonomous: true
must_haves:
  truths:
    - User can apply a selected warp proposal in one action.
    - Applying a proposal immediately updates adaptive mapping used by cube visuals.
    - Applied proposal state is visible in sandbox controls for clear workflow continuity.
  artifacts:
    - path: src/app/cube-sandbox/lib/applyWarpProposal.ts
      provides: Deterministic mapper from proposal payload to adaptive store updates.
    - path: src/store/useAdaptiveStore.ts
      provides: Runtime adaptive state updates for proposal-applied warp values.
    - path: src/components/viz/CubeVisualization.tsx
      provides: Immediate visual feedback in cube axes after proposal apply.
  key_links:
    - from: src/app/cube-sandbox/lib/applyWarpProposal.ts
      to: src/store/useAdaptiveStore.ts
      via: setWarpFactor/setWarpSource/setPrecomputedMaps
      pattern: setWarp
    - from: src/store/useWarpProposalStore.ts
      to: src/app/cube-sandbox/lib/applyWarpProposal.ts
      via: apply selected proposal action
      pattern: applyProposal
---

<objective>
Close the loop from proposal review to live cube adaptation.

Purpose: Satisfy CWARP-03 by making proposal apply actions immediately mutate adaptive mapping and reflect in cube interaction without route reload.
Output: Proposal apply orchestration and live visualization wiring updates.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/45-cube-constrained-warp-proposals/45-01-PLAN.md
@.planning/phases/45-cube-constrained-warp-proposals/45-02-PLAN.md
@src/store/useAdaptiveStore.ts
@src/components/viz/CubeVisualization.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement proposal apply adapter into adaptive state</name>
  <files>src/app/cube-sandbox/lib/applyWarpProposal.ts, src/store/useWarpProposalStore.ts</files>
  <action>Add a single apply path that translates selected proposal payload into adaptive store updates (source tag, factor, and warp map/domain where available). Persist applied proposal id in proposal store so users can see current active proposal and avoid accidental ambiguity.</action>
  <verify>npm run lint -- src/app/cube-sandbox/lib/applyWarpProposal.ts src/store/useWarpProposalStore.ts</verify>
  <done>Apply action consistently updates adaptive runtime state and records proposal provenance.</done>
</task>

<task type="auto">
  <name>Task 2: Ensure cube visuals react immediately after proposal apply</name>
  <files>src/store/useAdaptiveStore.ts, src/components/viz/CubeVisualization.tsx</files>
  <action>Wire any missing subscriptions/selectors so cube rendering responds immediately when warp proposal apply updates adaptive state. Add lightweight applied-proposal cue in cube diagnostics/status if needed to make feedback explicit during interaction.</action>
  <verify>npm run lint -- src/store/useAdaptiveStore.ts src/components/viz/CubeVisualization.tsx</verify>
  <done>User sees immediate adaptive deformation change in cube view after applying a proposal.</done>
</task>

</tasks>

<verification>
Run: `npm run lint -- src/app/cube-sandbox/lib/applyWarpProposal.ts src/store/useWarpProposalStore.ts src/store/useAdaptiveStore.ts src/components/viz/CubeVisualization.tsx`
Run: `npm run dev` and in `/cube-sandbox` select a proposal, apply it, and confirm cube adaptive mapping updates instantly.
</verification>

<success_criteria>
Applying a selected cube-constrained warp proposal updates adaptive cube mapping immediately and preserves applied-state visibility, satisfying CWARP-03.
</success_criteria>

<output>
After completion, create `.planning/phases/45-cube-constrained-warp-proposals/45-03-SUMMARY.md`
</output>
