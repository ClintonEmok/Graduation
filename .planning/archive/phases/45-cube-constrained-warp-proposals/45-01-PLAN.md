---
phase: 45-cube-constrained-warp-proposals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/cube-sandbox/lib/warpProposalEngine.ts
  - src/app/cube-sandbox/lib/warpProposalEngine.test.ts
  - src/store/useWarpProposalStore.ts
autonomous: true
must_haves:
  truths:
    - System can generate multiple warp proposals scoped to currently enabled cube spatial constraints.
    - Proposal payload includes rationale metrics that explain why each proposal was suggested.
    - Proposal generation is deterministic for identical input state to support repeatable review.
  artifacts:
    - path: src/app/cube-sandbox/lib/warpProposalEngine.ts
      provides: Pure constraint-aware proposal generation and ranking logic.
    - path: src/store/useWarpProposalStore.ts
      provides: Sandbox state for proposal list, generation metadata, and selection.
    - path: src/app/cube-sandbox/lib/warpProposalEngine.test.ts
      provides: Regression coverage for scoring/rationale output and deterministic proposal ordering.
  key_links:
    - from: src/app/cube-sandbox/lib/warpProposalEngine.ts
      to: src/store/useCubeSpatialConstraintsStore.ts
      via: enabled constraint inputs
      pattern: enabled|bounds
    - from: src/store/useWarpProposalStore.ts
      to: src/app/cube-sandbox/lib/warpProposalEngine.ts
      via: proposal generation orchestration
      pattern: generateWarpProposals
---

<objective>
Build the proposal-generation foundation for cube-constrained warp workflows.

Purpose: Satisfy CWARP-01 and CWARP-02 at the data layer by generating ranked warp proposals tied to active constraints, each with inspectable rationale indicators.
Output: A pure proposal engine, proposal store, and tests validating deterministic outputs.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/44-cube-spatial-context-setup/44-03-SUMMARY.md
@src/store/useCubeSpatialConstraintsStore.ts
@src/store/useAdaptiveStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement pure cube-constrained warp proposal generator</name>
  <files>src/app/cube-sandbox/lib/warpProposalEngine.ts</files>
  <action>Create a pure function that accepts enabled constraints plus current temporal context and returns ranked warp proposals. Each proposal should include: id, label, suggested warp factor/range payload, and rationale fields (at minimum density concentration and hotspot coverage signals). Keep generation deterministic and side-effect free so the UI and tests can rely on stable outputs.</action>
  <verify>npm run lint -- src/app/cube-sandbox/lib/warpProposalEngine.ts</verify>
  <done>Proposal generation returns reproducible, constraint-scoped results with rationale metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Add sandbox proposal state store</name>
  <files>src/store/useWarpProposalStore.ts</files>
  <action>Introduce a dedicated Zustand store for warp proposals with actions to generate, clear, select, and mark an applied proposal. Track generation timestamp and source constraint ids for traceability. Keep this store route-safe and independent from global app navigation state.</action>
  <verify>npm run lint -- src/store/useWarpProposalStore.ts</verify>
  <done>Cube sandbox has first-class proposal state that can drive review and apply workflows.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic scoring and rationale tests</name>
  <files>src/app/cube-sandbox/lib/warpProposalEngine.test.ts</files>
  <action>Add tests for: enabled-only constraint selection, stable proposal order for identical input, rationale field population, and graceful empty-result behavior when no constraints are active. Keep tests at function level for fast feedback.</action>
  <verify>npm run test -- src/app/cube-sandbox/lib/warpProposalEngine.test.ts</verify>
  <done>Regression tests guard CWARP proposal quality before UI integration.</done>
</task>

</tasks>

<verification>
Run: `npm run test -- src/app/cube-sandbox/lib/warpProposalEngine.test.ts`
Run: `npm run lint -- src/app/cube-sandbox/lib/warpProposalEngine.ts src/store/useWarpProposalStore.ts`
</verification>

<success_criteria>
Constraint-aware warp proposals can be generated with inspectable rationale signals and stable ordering, satisfying CWARP-01 and the data foundation of CWARP-02.
</success_criteria>

<output>
After completion, create `.planning/phases/45-cube-constrained-warp-proposals/45-01-SUMMARY.md`
</output>
