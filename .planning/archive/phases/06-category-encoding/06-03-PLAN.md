---
phase: 06-category-encoding
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/lib/category-shapes.ts
  - src/lib/category-shapes.test.ts
  - src/components/viz/SimpleCrimePoints.tsx
  - src/components/viz/category-shapes.phase6.test.tsx
autonomous: true
requirements_addressed:
  - CAT-03
must_haves:
  truths:
    - "The cube uses three stable marker shapes to distinguish broad crime categories."
    - "The broad-category mapping is centralized in a helper instead of being duplicated in the renderer."
    - "Category encoding preserves the current filtering, hover, and selection behavior."
    - "Regression tests fail if the cube loses the three-shape encoding contract."
  artifacts:
    - path: "src/lib/category-shapes.ts"
      provides: "Broad-category to shape mapping helper"
    - path: "src/lib/category-shapes.test.ts"
      provides: "Shape helper regression coverage"
    - path: "src/components/viz/SimpleCrimePoints.tsx"
      provides: "Cube point renderer upgraded to category-shaped markers"
    - path: "src/components/viz/category-shapes.phase6.test.tsx"
      provides: "Source-inspection coverage for shape encoding"
  key_links:
    - from: "src/lib/category-shapes.ts"
      to: "src/lib/category-legend.ts"
      via: "broad-category groups stay aligned with the legend registry"
      pattern: "sphere|cube|cone"
    - from: "src/components/viz/SimpleCrimePoints.tsx"
      to: "src/lib/category-shapes.ts"
      via: "renderer buckets crime records into shape groups before drawing"
      pattern: "shape|category|broad"
    - from: "src/components/viz/SimpleCrimePoints.tsx"
      to: "src/store/useFilterStore.ts"
      via: "existing selectedTypes / selectedDistricts / spatial filters remain intact"
      pattern: "selectedTypes|selectedDistricts|selectedSpatialBounds"
---

<objective>
Introduce cube-only category shape encoding so broad crime groups read as distinct marker geometries instead of color alone.

Purpose: the cube should communicate category differences through shape, not just hue.
Output: a shared broad-category shape helper, a shape-encoded cube renderer, and regression coverage.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/MVP-FINALE-ROADMAP.md
@.planning/phases/06-category-encoding/06-CONTEXT.md
@.planning/phases/06-category-encoding/06-RESEARCH.md
@src/components/viz/SimpleCrimePoints.tsx
@src/store/useFilterStore.ts
@src/lib/category-maps.ts
@src/lib/palettes.ts

</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a broad-category shape mapping helper</name>
  <files>src/lib/category-shapes.ts, src/lib/category-shapes.test.ts</files>
  <read_first>
    - src/lib/category-maps.ts
    - src/lib/palettes.ts
    - src/hooks/useCrimeData.ts
  </read_first>
  <action>
    Create a helper that maps crime type ids or crime type names into three stable broad categories and associated marker shapes (sphere, cube, cone). Include a helper that groups crime records into those shape buckets so the renderer can stay deterministic and testable.
  </action>
  <acceptance_criteria>
    - category-shapes.ts exports a stable shape mapping helper.
    - category-shapes.ts includes three broad shapes: sphere, cube, and cone.
    - category-shapes.test.ts proves the same crime type always maps to the same shape bucket.
  </acceptance_criteria>
  <verify>grep -n 'sphere\|cube\|cone\|shape\|bucket' src/lib/category-shapes.ts src/lib/category-shapes.test.ts</verify>
  <done>The cube shape taxonomy is now centralized and test-locked.</done>
</task>

<task type="auto">
  <name>Refactor the cube crime renderer to use category-shaped markers</name>
  <files>src/components/viz/SimpleCrimePoints.tsx</files>
  <read_first>
    - src/components/viz/SimpleCrimePoints.tsx
    - src/lib/category-shapes.ts
    - src/lib/category-maps.ts
    - src/store/useFilterStore.ts
  </read_first>
  <action>
    Split the filtered cube crime records into the three broad shape buckets and render each bucket with its own instanced geometry (sphere, box, cone) instead of a single point cloud. Preserve the existing filter, hover, and selection behavior so the category shape layer stays interactive and aligned with the current cube controls.
  </action>
  <acceptance_criteria>
    - SimpleCrimePoints.tsx references `sphereGeometry`, `boxGeometry`, and `coneGeometry`.
    - SimpleCrimePoints.tsx still references `selectedTypes`, `selectedDistricts`, and `selectedSpatialBounds`.
    - The renderer keeps the existing selection and hover flows intact.
  </acceptance_criteria>
  <verify>grep -n 'sphereGeometry\|boxGeometry\|coneGeometry\|selectedTypes\|selectedDistricts\|selectedSpatialBounds' src/components/viz/SimpleCrimePoints.tsx</verify>
  <done>The cube now communicates category differences through shape as well as color.</done>
</task>

<task type="auto">
  <name>Lock the category-shape encoding contract with source inspection</name>
  <files>src/components/viz/category-shapes.phase6.test.tsx</files>
  <read_first>
    - src/components/viz/SimpleCrimePoints.tsx
    - src/lib/category-shapes.ts
    - src/store/useFilterStore.ts
  </read_first>
  <action>
    Add a source-inspection regression test that fails if the cube stops using all three shape geometries, if the broad-category helper disappears, or if the renderer stops honoring the existing filter state while drawing the shape buckets.
  </action>
  <acceptance_criteria>
    - category-shapes.phase6.test.tsx references `sphereGeometry`, `boxGeometry`, and `coneGeometry`.
    - category-shapes.phase6.test.tsx references the broad-category helper exported from category-shapes.ts.
    - category-shapes.phase6.test.tsx references `selectedTypes` or `selectedSpatialBounds`.
  </acceptance_criteria>
  <verify>grep -n 'sphereGeometry\|boxGeometry\|coneGeometry\|selectedTypes\|selectedSpatialBounds\|category-shapes' src/components/viz/category-shapes.phase6.test.tsx</verify>
  <done>The shape-encoding contract is pinned and cannot silently regress.</done>
</task>

</tasks>

<verification>
Confirm the cube renderer now uses three category shapes and preserves the existing filter / selection flow.
</verification>

<success_criteria>
The cube distinguishes broad crime categories by shape, and the helper/test contract keeps the encoding stable.
</success_criteria>

<output>
After completion, create `.planning/phases/06-category-encoding/06-03-SUMMARY.md`
</output>
