---
phase: 10-workflow-isolation
plan: 04
type: execute
wave: 3
depends_on:
  - 08-01
  - 06-01
  - 05-03
files_modified: []
autonomous: false
must_haves:
  truths:
    - "Generate, review, and apply stay isolated until Apply is clicked."
    - "Apply lands directly on the dashboard with no extra confirmation screen."
    - "The dashboard opens map-first and keeps the STKDE rail visible."
  artifacts:
    - path: "src/app/timeslicing/page.tsx"
      provides: "Generate/review/apply handoff entry point"
    - path: "src/app/dashboard/page.tsx"
      provides: "Post-apply landing route"
    - path: "src/components/layout/DashboardLayout.tsx"
      provides: "Map-first shared viewport"
  key_links:
    - from: "src/app/timeslicing/page.tsx"
      to: "/dashboard"
      via: "Apply button routing"
      pattern: "push('/dashboard')"
    - from: "src/app/dashboard/page.tsx"
      to: "src/components/layout/DashboardLayout.tsx"
      via: "main viewport composition"
      pattern: "DashboardLayout"
---

<objective>
Visually verify the completed Phase 10 flow end to end.

Purpose: confirm the workflow isolation, dashboard landing behavior, and fixed STKDE rail before the phase is marked done.
Output: a final human verification gate with exact steps and expected outcomes.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-contextual-data-enrichment/08-CONTEXT.md
@.planning/phases/07-dashboard-demo-preset-thresholds/07-CONTEXT.md
@.planning/phases/06-demo-timeline-polish/06-CONTEXT.md
@.planning/phases/05-stats-stkde-interaction/05-CONTEXT.md
@.planning/phases/04-demo-stats-stkde/04-CONTEXT.md
@.planning/phases/03-demo-timeline-rewrite/03-CONTEXT.md
@.planning/phases/02-dashboard-demo-ui-ux/02-CONTEXT.md
@src/app/timeslicing/page.tsx
@src/app/dashboard/page.tsx
@src/components/layout/DashboardLayout.tsx
@src/components/stkde/DashboardStkdePanel.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 10 end state: isolated generate/review/apply workflow, editable apply preview, direct dashboard handoff, and map-first post-apply dashboard.</what-built>
  <how-to-verify>
    1. Open /timeslicing and confirm the workflow is isolated from the dashboard.
    2. Generate a draft result, edit the apply preview, and confirm warnings remain visible.
    3. Click Apply and confirm the app lands directly on /dashboard.
    4. Confirm /dashboard opens map-first, allows map/cube swapping, and keeps the STKDE rail fixed on the right.
  </how-to-verify>
  <resume-signal>Type "approved" once the end-to-end flow matches the expected behavior.</resume-signal>
</task>

</tasks>

<verification>
Use the application UI to confirm the full workflow handoff and post-apply dashboard behavior.
</verification>

<success_criteria>
The end-to-end workflow is visually verified and matches the isolated workflow plus simplified dashboard requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/10-workflow-isolation/10-04-SUMMARY.md`
</output>
