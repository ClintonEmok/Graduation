---
phase: 05-clustering
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/components/viz/MainScene.tsx
  - src/components/viz/CubeVisualization.tsx
  - src/components/viz/ClusterHighlights.tsx
  - src/components/viz/ClusterLabels.tsx
autonomous: true
requirements_addressed:
  - CLUS-02
must_haves:
  truths:
    - "The cube scene renders clustering overlays when the clustering feature is enabled."
    - "Cluster hulls read as 3D volumes and the selected cluster stands out visually."
    - "Cluster labels show count, dominant crime type, and time span."
    - "The existing map/timeline shells are unaffected by the clustering render wiring."
  artifacts:
    - path: "src/components/viz/MainScene.tsx"
      provides: "Cube scene composition with cluster renderers mounted"
    - path: "src/components/viz/CubeVisualization.tsx"
      provides: "Cube shell overlay that exposes the active cluster context"
    - path: "src/components/viz/ClusterHighlights.tsx"
      provides: "3D cluster volume rendering"
    - path: "src/components/viz/ClusterLabels.tsx"
      provides: "Cluster label overlay with summary details"
  key_links:
    - from: "src/components/viz/MainScene.tsx"
      to: "src/store/useClusterStore.ts"
      via: "mount cluster renderers only when clustering is enabled"
      pattern: "ClusterHighlights|ClusterLabels|ClusterManager"
    - from: "src/components/viz/ClusterLabels.tsx"
      to: "src/store/useClusterStore.ts"
      via: "labels render the richer metadata produced by plan 01"
      pattern: "dominantType|timeRange|count"
    - from: "src/components/viz/ClusterHighlights.tsx"
      to: "src/store/useClusterStore.ts"
      via: "selected cluster state changes the visual emphasis"
      pattern: "selectedClusterId"
---

<objective>
Render the global clustering result directly inside the cube so dense regions become visible as explicit 3D volumes with readable labels and selection emphasis.

Purpose: the model from plan 01 is only useful if the cube can actually show it.
Output: cluster volume overlays, label overlays, and cube shell context that present the global clustering result clearly.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/MVP-FINALE-ROADMAP.md
@.planning/phases/05-clustering/05-CONTEXT.md
@.planning/phases/05-clustering/05-RESEARCH.md
@src/components/viz/MainScene.tsx
@src/components/viz/CubeVisualization.tsx
@src/components/viz/ClusterHighlights.tsx
@src/components/viz/ClusterLabels.tsx
@src/components/viz/Scene.tsx
@src/store/useClusterStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount the clustering overlays in the cube scene</name>
  <files>src/components/viz/MainScene.tsx, src/components/viz/CubeVisualization.tsx</files>
  <read_first>
    - src/components/viz/MainScene.tsx
    - src/components/viz/CubeVisualization.tsx
    - src/store/useClusterStore.ts
    - src/lib/feature-flags.ts
  </read_first>
  <action>
    Add the clustering render path to the cube scene: wire the existing clustering feature flag into the main scene, mount the cluster manager, and ensure the cube shell can surface the active cluster context without changing the existing map/timeline layout.
  </action>
  <acceptance_criteria>
    - MainScene.tsx references the clustering render components or cluster manager.
    - CubeVisualization.tsx still renders the same cube shell but now shows cluster context when a cluster is active.
    - The clustering render path remains behind the existing feature flag.
  </acceptance_criteria>
  <verify>grep -n 'ClusterManager\|ClusterHighlights\|ClusterLabels\|clustering' src/components/viz/MainScene.tsx src/components/viz/CubeVisualization.tsx</verify>
  <done>The cube scene knows how to display the clustering layer without altering the rest of the shell.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade cluster volumes and labels to show readable cluster summaries</name>
  <files>src/components/viz/ClusterHighlights.tsx, src/components/viz/ClusterLabels.tsx</files>
  <read_first>
    - src/components/viz/ClusterHighlights.tsx
    - src/components/viz/ClusterLabels.tsx
    - src/store/useClusterStore.ts
    - src/lib/clustering/cluster-analysis.ts
  </read_first>
  <action>
    Rework the existing cluster overlays so the 3D hulls remain subtle but clearly readable, and the labels surface the richer summary fields from plan 01: member count, dominant type, and time span. Keep the interaction affordances lightweight and readable in the cube.
  </action>
  <acceptance_criteria>
    - ClusterLabels.tsx contains both `dominantType` and a human-readable time span reference.
    - ClusterHighlights.tsx still renders a 3D volume for each cluster.
    - The selected cluster is visually distinct from the rest.
  </acceptance_criteria>
  <verify>grep -n 'dominantType\|timeRange\|selectedClusterId\|hoveredClusterId' src/components/viz/ClusterHighlights.tsx src/components/viz/ClusterLabels.tsx</verify>
  <done>The global cluster overlays are legible enough to read as actual analysis output, not just debug boxes.</done>
</task>

</tasks>

<verification>
Confirm the cube scene mounts clustering overlays and the cluster labels expose the richer summary data.
</verification>

<success_criteria>
The cube can show global cluster hulls, the selected cluster reads as highlighted, and labels expose the summary information needed for analysis.
</success_criteria>

<output>
After completion, create `.planning/phases/05-clustering/05-02-SUMMARY.md`
</output>
