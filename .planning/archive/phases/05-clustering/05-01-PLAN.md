---
phase: 05-clustering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/clustering/cluster-analysis.ts
  - src/lib/clustering/cluster-analysis.test.ts
  - src/store/useClusterStore.ts
  - src/components/viz/ClusterManager.tsx
autonomous: true
requirements_addressed:
  - CLUS-01
must_haves:
  truths:
    - "DBSCAN runs through a shared helper instead of being embedded in render code."
    - "The cluster metadata shape includes count, dominant type, spatial bounds, and time span."
    - "The cluster store can accept both global cluster results and per-slice cluster buckets."
    - "The helper contract is locked by a focused unit test on clustered and non-clustered inputs."
  artifacts:
    - path: "src/lib/clustering/cluster-analysis.ts"
      provides: "Pure DBSCAN helper and shared cluster metadata types"
    - path: "src/lib/clustering/cluster-analysis.test.ts"
      provides: "Helper regression coverage for cluster grouping and empty inputs"
    - path: "src/store/useClusterStore.ts"
      provides: "Cluster store shape for global and per-slice cluster buckets"
    - path: "src/components/viz/ClusterManager.tsx"
      provides: "Render-adjacent clustering orchestrator that calls the shared helper"
  key_links:
    - from: "src/components/viz/ClusterManager.tsx"
      to: "src/lib/clustering/cluster-analysis.ts"
      via: "build cluster buckets from filtered points instead of running DBSCAN inline"
      pattern: "buildClusterAnalysis|DBSCAN"
    - from: "src/store/useClusterStore.ts"
      to: "cluster-analysis.ts"
      via: "store receives helper-produced global and per-slice results"
      pattern: "sliceClustersById|selectedClusterId"
    - from: "src/lib/clustering/cluster-analysis.ts"
      to: "src/lib/data/types.ts"
      via: "cluster members use filtered point indexes and point metadata"
      pattern: "originalIndex|typeCounts|timeRange"
---

<objective>
Extract the DBSCAN clustering logic into a shared, testable helper and expand the cluster store so later plans can render and interact with both global and per-slice cluster results.

Purpose: clustering has to become reusable before the cube can render it in multiple places.
Output: a shared cluster-analysis helper, richer cluster store state, and regression coverage for the core cluster contract.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/MVP-FINALE-ROADMAP.md
@.planning/phases/05-clustering/05-CONTEXT.md
@.planning/phases/05-clustering/05-RESEARCH.md
@src/components/viz/ClusterManager.tsx
@src/store/useClusterStore.ts
@src/lib/data/selectors.ts
@src/lib/data/types.ts
@src/types/crime.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a shared DBSCAN cluster-analysis helper</name>
  <files>src/lib/clustering/cluster-analysis.ts, src/lib/clustering/cluster-analysis.test.ts</files>
  <read_first>
    - src/components/viz/ClusterManager.tsx
    - src/lib/data/selectors.ts
    - src/lib/data/types.ts
    - src/types/crime.ts
  </read_first>
  <action>
    Add a pure helper module that accepts filtered cube points plus clustering sensitivity and returns reusable cluster objects with id, scope, count, dominant type, typeCounts, spatial bounds, time range, and member indexes. Include a second helper for grouping per-slice results by slice id so later plans can render slice overlays without re-running the analysis shape.
  </action>
  <acceptance_criteria>
    - cluster-analysis.ts imports `DBSCAN` from `density-clustering`.
    - cluster-analysis.ts exports a reusable cluster type that includes `typeCounts` and `timeRange`.
    - cluster-analysis.test.ts proves clustered input returns at least one cluster and empty input returns no clusters.
  </acceptance_criteria>
  <verify>grep -n 'DBSCAN\|typeCounts\|timeRange\|sliceClustersById' src/lib/clustering/cluster-analysis.ts src/lib/clustering/cluster-analysis.test.ts</verify>
  <done>The clustering contract is reusable outside render code and is pinned by a helper test.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor ClusterManager to use the shared helper and richer store state</name>
  <files>src/store/useClusterStore.ts, src/components/viz/ClusterManager.tsx</files>
  <read_first>
    - src/store/useClusterStore.ts
    - src/components/viz/ClusterManager.tsx
    - src/lib/clustering/cluster-analysis.ts
    - src/lib/data/selectors.ts
  </read_first>
  <action>
    Expand the cluster store to hold the richer cluster metadata and per-slice buckets, then refactor ClusterManager so it computes filtered cube points once, calls the shared helper, and writes the helper output into the store. Preserve the existing debounce, sensitivity, and enabled checks; remove the inline DBSCAN implementation from the component.
  </action>
  <acceptance_criteria>
    - useClusterStore.ts contains a per-slice cluster field such as `sliceClustersById`.
    - ClusterManager.tsx no longer contains an inline `new DBSCAN()` call.
    - ClusterManager.tsx still filters by selected types, districts, and time range before clustering.
  </acceptance_criteria>
  <verify>grep -n 'new DBSCAN\|sliceClustersById\|selectedTypes\|selectedDistricts\|selectedTimeRange' src/store/useClusterStore.ts src/components/viz/ClusterManager.tsx</verify>
  <done>The clustering orchestrator is data-driven, store-backed, and ready for render consumers.</done>
</task>

</tasks>

<verification>
Confirm the clustering logic is extracted into a shared helper and the store can hold both global and per-slice cluster outputs.
</verification>

<success_criteria>
DBSCAN is reusable outside the render component, the cluster store can represent both global and per-slice clusters, and the helper contract is protected by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/05-clustering/05-01-SUMMARY.md`
</output>
