---
phase: 05-clustering
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/components/viz/TimeSlices.tsx
  - src/components/viz/SlicePlane.tsx
  - src/components/viz/SliceClusterOverlay.tsx
  - src/components/viz/slice-cluster-overlay.phase5.test.tsx
autonomous: true
requirements_addressed:
  - CLUS-03
must_haves:
  truths:
    - "Each visible slice can render its own cluster hulls."
    - "Per-slice cluster overlays stay aligned to the adaptive slice scale."
    - "Hidden slices never render cluster artifacts."
    - "Per-slice cluster buckets are keyed by slice id in the cluster store."
  artifacts:
    - path: "src/components/viz/TimeSlices.tsx"
      provides: "Slice loop that can mount per-slice cluster overlays"
    - path: "src/components/viz/SlicePlane.tsx"
      provides: "Slice-plane contract for overlay positioning"
    - path: "src/components/viz/SliceClusterOverlay.tsx"
      provides: "Per-slice cluster hull renderer"
    - path: "src/components/viz/slice-cluster-overlay.phase5.test.tsx"
      provides: "Source-inspection regression coverage for slice clustering"
  key_links:
    - from: "src/components/viz/TimeSlices.tsx"
      to: "src/store/useClusterStore.ts"
      via: "render per-slice cluster overlays from sliceClustersById"
      pattern: "sliceClustersById|TimeSlices"
    - from: "src/components/viz/SliceClusterOverlay.tsx"
      to: "src/components/viz/SlicePlane.tsx"
      via: "overlay positions follow the same y/time scale as the plane"
      pattern: "timeToY|yToTime|sliceId"
    - from: "src/components/viz/slice-cluster-overlay.phase5.test.tsx"
      to: "src/components/viz/TimeSlices.tsx"
      via: "source inspection locks the hidden-slice and per-slice overlay contract"
      pattern: "sliceClustersById|visible|hidden|cluster overlay"
---

<objective>
Render clustering on each slice plane so dense regions can be inspected in the same temporal context as the cube volume.

Purpose: global clusters alone are not enough; analysts need to see how density changes slice by slice.
Output: per-slice cluster overlays, slice-scale alignment, and a regression test that locks the overlay contract.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/MVP-FINALE-ROADMAP.md
@.planning/phases/05-clustering/05-CONTEXT.md
@.planning/phases/05-clustering/05-RESEARCH.md
@src/components/viz/TimeSlices.tsx
@src/components/viz/SlicePlane.tsx
@src/store/useClusterStore.ts
@src/lib/clustering/cluster-analysis.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a per-slice cluster overlay renderer</name>
  <files>src/components/viz/SliceClusterOverlay.tsx, src/components/viz/TimeSlices.tsx</files>
  <read_first>
    - src/components/viz/TimeSlices.tsx
    - src/components/viz/SlicePlane.tsx
    - src/store/useClusterStore.ts
    - src/lib/clustering/cluster-analysis.ts
  </read_first>
  <action>
    Create a slice-aware cluster overlay component that reads sliceClustersById from the cluster store and renders 2D hulls or outlines in the same slice coordinate system used by TimeSlices. Wire it into TimeSlices so each visible slice can show its own clustering result without disturbing the existing burst/evolution overlays.
  </action>
  <acceptance_criteria>
    - SliceClusterOverlay.tsx exists and references `sliceClustersById`.
    - TimeSlices.tsx mounts the per-slice cluster overlay alongside the existing overlays.
    - The overlay uses the same adaptive time scale as the slice plane.
  </acceptance_criteria>
  <verify>grep -n 'SliceClusterOverlay\|sliceClustersById\|timeToY\|yToTime' src/components/viz/TimeSlices.tsx src/components/viz/SliceClusterOverlay.tsx</verify>
  <done>Each visible slice has a dedicated cluster overlay renderer ready for the cube scene.</done>
</task>

<task type="auto">
  <name>Task 2: Lock hidden-slice gating and per-slice overlay behavior</name>
  <files>src/components/viz/SlicePlane.tsx, src/components/viz/slice-cluster-overlay.phase5.test.tsx</files>
  <read_first>
    - src/components/viz/SlicePlane.tsx
    - src/components/viz/SliceClusterOverlay.tsx
    - src/components/viz/TimeSlices.tsx
    - src/lib/clustering/cluster-analysis.ts
  </read_first>
  <action>
    Make sure the per-slice clustering layer respects slice visibility and the current adaptive scale. Add a source-inspection regression test that fails if hidden slices start rendering cluster overlays or if the overlay contract stops referencing the slice ids produced by the cluster store.
  </action>
  <acceptance_criteria>
    - SlicePlane.tsx keeps the overlay aligned to the plane's y/time transform.
    - slice-cluster-overlay.phase5.test.tsx references hidden-slice gating.
    - slice-cluster-overlay.phase5.test.tsx references `sliceClustersById`.
  </acceptance_criteria>
  <verify>grep -n 'sliceClustersById\|hidden\|visible\|timeToY\|yToTime' src/components/viz/SlicePlane.tsx src/components/viz/slice-cluster-overlay.phase5.test.tsx</verify>
  <done>The per-slice clustering layer is tied to the slice contract and will fail loudly if hidden slices leak overlays.</done>
</task>

</tasks>

<verification>
Confirm that visible slices render cluster overlays and hidden slices do not.
</verification>

<success_criteria>
Each slice can show its own cluster hulls in the correct temporal position, and hidden slices stay free of cluster artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/05-clustering/05-03-SUMMARY.md`
</output>
