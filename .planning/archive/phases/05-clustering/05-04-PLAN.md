---
phase: 05-clustering
plan: 04
type: execute
wave: 3
depends_on:
  - 05-02
  - 05-03
files_modified:
  - src/store/useClusterStore.ts
  - src/components/viz/ClusterLabels.tsx
  - src/components/viz/ClusterHighlights.tsx
  - src/components/viz/CubeVisualization.tsx
  - src/components/viz/cluster-interaction.phase5.test.tsx
autonomous: true
requirements_addressed:
  - CLUS-04
must_haves:
  truths:
    - "Hovering a cluster reveals summary state in the cube."
    - "Clicking a cluster narrows the cube through the existing spatial-bound filter path."
    - "Clearing the cluster selection restores the unfiltered cube state."
    - "Regression tests break if cluster interaction copy or filter wiring drifts."
  artifacts:
    - path: "src/store/useClusterStore.ts"
      provides: "Hover/select state for cluster interaction"
    - path: "src/components/viz/ClusterLabels.tsx"
      provides: "Hover and click entry point for cluster interaction"
    - path: "src/components/viz/ClusterHighlights.tsx"
      provides: "Selected/hovered cluster emphasis"
    - path: "src/components/viz/CubeVisualization.tsx"
      provides: "Selected cluster summary in the cube shell"
    - path: "src/components/viz/cluster-interaction.phase5.test.tsx"
      provides: "Source-inspection regression coverage for hover/select/filter wiring"
  key_links:
    - from: "src/components/viz/ClusterLabels.tsx"
      to: "src/store/useFilterStore.ts"
      via: "clicking a cluster writes the cluster bounds into the existing spatial filter path"
      pattern: "setSpatialBounds|clearSpatialBounds|selectedSpatialBounds"
    - from: "src/components/viz/ClusterHighlights.tsx"
      to: "src/store/useClusterStore.ts"
      via: "hovered and selected cluster ids change opacity and outline emphasis"
      pattern: "hoveredClusterId|selectedClusterId"
    - from: "src/components/viz/CubeVisualization.tsx"
      to: "src/store/useClusterStore.ts"
      via: "the cube summary panel reflects the active cluster state"
      pattern: "selectedClusterId|Cluster"
---

<objective>
Turn cluster visuals into a usable interaction surface by exposing hover, selection, and spatial filtering behavior.

Purpose: dense regions should not just be visible; they should be selectable and actionable.
Output: hover/select/filter cluster interactions plus regression coverage that protects the cube interaction contract.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/MVP-FINALE-ROADMAP.md
@.planning/phases/05-clustering/05-CONTEXT.md
@.planning/phases/05-clustering/05-RESEARCH.md
@src/store/useClusterStore.ts
@src/components/viz/ClusterLabels.tsx
@src/components/viz/ClusterHighlights.tsx
@src/components/viz/CubeVisualization.tsx
@src/store/useFilterStore.ts
@src/components/viz/SimpleCrimePoints.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire hover, select, and filter actions into the cluster store and labels</name>
  <files>src/store/useClusterStore.ts, src/components/viz/ClusterLabels.tsx, src/components/viz/ClusterHighlights.tsx, src/components/viz/CubeVisualization.tsx</files>
  <read_first>
    - src/store/useClusterStore.ts
    - src/components/viz/ClusterLabels.tsx
    - src/components/viz/ClusterHighlights.tsx
    - src/components/viz/CubeVisualization.tsx
    - src/store/useFilterStore.ts
    - src/components/viz/SimpleCrimePoints.tsx
  </read_first>
  <action>
    Add hovered-cluster state and wire the cluster labels so hover updates the active cluster summary while click writes the selected cluster's spatial bounds into the existing filter store. Keep the cube summary panel in sync with the active cluster and make the highlighted hull clearly distinguish hover versus selection.
  </action>
  <acceptance_criteria>
    - useClusterStore.ts contains hovered-cluster and selected-cluster state/actions.
    - ClusterLabels.tsx references `setSpatialBounds` or `clearSpatialBounds`.
    - SimpleCrimePoints.tsx already honors `selectedSpatialBounds`, so cluster clicks filter the point cloud through that existing path.
  </acceptance_criteria>
  <verify>grep -n 'hoveredClusterId\|selectedClusterId\|setSpatialBounds\|clearSpatialBounds\|selectedSpatialBounds' src/store/useClusterStore.ts src/components/viz/ClusterLabels.tsx src/components/viz/ClusterHighlights.tsx src/components/viz/CubeVisualization.tsx src/components/viz/SimpleCrimePoints.tsx</verify>
  <done>Cluster hover and click behavior drive the existing spatial filter path and are visible in the cube shell.</done>
</task>

<task type="auto">
  <name>Add source-inspection regression coverage for cluster interaction and filtering</name>
  <files>src/components/viz/cluster-interaction.phase5.test.tsx</files>
  <read_first>
    - src/components/viz/ClusterLabels.tsx
    - src/components/viz/ClusterHighlights.tsx
    - src/components/viz/CubeVisualization.tsx
    - src/components/viz/SimpleCrimePoints.tsx
    - src/store/useClusterStore.ts
  </read_first>
  <action>
    Create a source-inspection test that locks the hover/select/filter contract: it should fail if cluster labels stop showing the summary fields, if the highlight layer stops distinguishing hovered versus selected clusters, or if the cube shell stops surfacing the active cluster context. Also lock the fact that point filtering still runs through the existing spatial-bounds filter path.
  </action>
  <acceptance_criteria>
    - cluster-interaction.phase5.test.tsx references `hoveredClusterId` and `selectedClusterId`.
    - cluster-interaction.phase5.test.tsx references `setSpatialBounds` or `selectedSpatialBounds`.
    - cluster-interaction.phase5.test.tsx references the summary copy that includes count and time span.
  </acceptance_criteria>
  <verify>grep -n 'hoveredClusterId\|selectedClusterId\|setSpatialBounds\|selectedSpatialBounds\|timeRange\|dominantType' src/components/viz/cluster-interaction.phase5.test.tsx</verify>
  <done>The cluster interaction contract is now pinned by tests and will fail if hover/select/filter wiring drifts.</done>
</task>

</tasks>

<verification>
Run the cluster interaction source-inspection test and confirm it locks hover, selection, and spatial-bound filtering.
</verification>

<success_criteria>
Clusters can be hovered, selected, and used to filter the cube through the existing spatial-bound path, with regression coverage protecting the behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/05-clustering/05-04-SUMMARY.md`
</output>
