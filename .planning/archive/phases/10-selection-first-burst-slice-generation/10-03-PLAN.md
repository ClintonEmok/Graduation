---
phase: 10-non-uniform-time-slicing
plan: 03
type: execute
wave: 3
depends_on:
  - 10-02
files_modified: []
autonomous: false
must_haves:
  truths:
    - "The workflow uses the brushed selection as the canonical input for non-uniform slicing."
    - "Hourly/daily granularity changes the base partition without dropping coverage."
    - "Editable draft slices keep their burst metadata and warp weights through merge, split, delete, and apply."
  artifacts:
    - path: "src/components/dashboard-demo/WorkflowSkeleton.tsx"
      provides: "Brush-first generation controls and granularity copy"
    - path: "src/components/dashboard-demo/DemoSlicePanel.tsx"
      provides: "Editable non-uniform draft review surface"
    - path: "src/store/useDashboardDemoTimeslicingModeStore.ts"
      provides: "Granularity-driven draft state and metadata persistence"
  key_links:
    - from: "src/components/dashboard-demo/WorkflowSkeleton.tsx"
      to: "src/store/useDashboardDemoCoordinationStore.ts"
      via: "brushRange remains the generation source of truth"
      pattern: "brushRange"
    - from: "src/store/useDashboardDemoTimeslicingModeStore.ts"
      to: "src/store/slice-domain/createSliceCoreSlice.ts"
      via: "burst metadata and warp weights survive pending-draft edit operations"
      pattern: "mergePendingGeneratedBins|splitPendingGeneratedBin|deletePendingGeneratedBin|warpWeight|burstScore"
---

<objective>
Visually verify the non-uniform time slicing workflow end to end.

Purpose: confirm the brushed-selection source of truth, granularity choice, neutral-partition behavior, and editable non-uniform draft slices before the workflow isolation phase starts.
Output: a blocking human-verify checkpoint.
</objective>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Granularity-driven non-uniform draft generation, editable pending-draft metadata preservation, and neutral-partition guidance.</what-built>
  <how-to-verify>
    1. Open /dashboard-demo and brush a time range that contains both sparse and dense periods.
    2. Generate hourly slices and confirm the workflow uses the brushed selection, not a density threshold or preset-bias fallback.
    3. Switch to daily granularity and confirm the partition changes while full coverage stays intact.
    4. Merge, split, and delete a pending draft and confirm burst labels, warp weights, and metadata remain intact before apply.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<verification>
Use the application UI to confirm the non-uniform time slicing workflow, neutral partition behavior, and editable draft behavior.
</verification>

<success_criteria>
The non-uniform time slicing workflow is visually verified and the brushed selection remains the source of truth for draft generation.
</success_criteria>

<output>
After completion, create `.planning/phases/10-selection-first-burst-slice-generation/10-03-SUMMARY.md`
</output>
