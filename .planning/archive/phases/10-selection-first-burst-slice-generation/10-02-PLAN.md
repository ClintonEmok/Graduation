---
phase: 10-non-uniform-time-slicing
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/components/dashboard-demo/lib/demo-burst-generation.ts
  - src/store/useDashboardDemoTimeslicingModeStore.ts
  - src/store/useDashboardDemoTimeslicingModeStore.test.ts
  - src/components/dashboard-demo/WorkflowSkeleton.tsx
  - src/components/dashboard-demo/DemoSlicePanel.tsx
  - src/app/dashboard-demo/page.shell.test.tsx
autonomous: true
must_haves:
  truths:
    - "The brushed selection is the canonical input for selection-first slice generation."
    - "Daily is the default granularity, and the brushed selection is the only selection source."
    - "The chosen granularity controls the base bin size, and burstiness only changes expansion weight."
    - "All crime types are included by default, but crime-type filters remain relevant inputs."
    - "Generated slices keep full coverage and preserve burst metadata through merge, split, delete, and apply."
    - "When no bin is bursty, the workflow shows a muted neutral-partition state instead of an overlap failure."
  artifacts:
    - path: "src/store/useDashboardDemoTimeslicingModeStore.ts"
      provides: "Selection-first draft generation, neutral-partition guidance, and editable-draft persistence"
    - path: "src/components/dashboard-demo/WorkflowSkeleton.tsx"
      provides: "User-triggered generation controls, daily default granularity, and optional crime-type filters"
    - path: "src/components/dashboard-demo/DemoSlicePanel.tsx"
      provides: "Editable pending non-uniform slice review surface with muted neutral state"
    - path: "src/app/dashboard-demo/page.shell.test.tsx"
      provides: "Source-inspection coverage for the non-uniform slicing workflow"
    - path: "src/store/useDashboardDemoTimeslicingModeStore.test.ts"
      provides: "Regression coverage for draft persistence, neutral fallback, and apply behavior"
  key_links:
    - from: "src/components/dashboard-demo/WorkflowSkeleton.tsx"
      to: "src/store/useDashboardDemoCoordinationStore.ts"
      via: "brushRange is the generator's canonical selection source for selection-first generation"
      pattern: "brushRange"
    - from: "src/store/useDashboardDemoTimeslicingModeStore.ts"
      to: "src/store/slice-domain/createSliceCoreSlice.ts"
      via: "editable draft metadata survives merge/split/delete and apply"
      pattern: "mergePendingGeneratedBins|splitPendingGeneratedBin|deletePendingGeneratedBin|warpWeight|burstScore"
    - from: "src/components/dashboard-demo/DemoSlicePanel.tsx"
      to: "src/store/useDashboardDemoTimeslicingModeStore.ts"
      via: "granularity selector and draft review reflect the store partition state"
      pattern: "generationError|pendingGeneratedBins|granularity|editable before apply"
---

<objective>
Wire the selection-first non-uniform time slicing workflow through the demo store and review surfaces so the manually brushed selection drives granularity-aware partitions, editable drafts keep their burst identity and warp weights, and the UI shows `B` plus a muted neutral partition when nothing stands out.

Purpose: make burst expansion deterministic and inspectable while keeping the review-before-apply loop intact.
Output: a brush-sourced demo store, neutral-partition UX, and source-locked regression coverage.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Make the demo store generate selection-first drafts from the brushed selection</name>
  <files>src/components/dashboard-demo/lib/demo-burst-generation.ts, src/store/useDashboardDemoTimeslicingModeStore.ts, src/store/useDashboardDemoTimeslicingModeStore.test.ts</files>
  <action>
    Update the generation path to consume the canonical brushed selection, default the granularity to daily, partition it by the selected granularity, and score the resulting bins with the selection-first helper instead of the density-threshold path. Remove the preset-bias fallback from burst generation, keep the muted neutral-partition state when nothing stands out, and preserve burst metadata plus warp weights through edit/apply mutations so merge, split, delete, and apply do not strip burst identity from pending drafts.
  </action>
  <verify>pnpm vitest run src/components/dashboard-demo/lib/demo-burst-generation.test.ts src/store/useDashboardDemoTimeslicingModeStore.test.ts</verify>
  <done>The burst store can generate editable non-uniform drafts from the brushed selection and keeps burst metadata and warp weights intact through pending-draft edits.</done>
</task>

<task type="auto">
  <name>Task 2: Surface selection-first controls and neutral-partition guidance in the demo workflow</name>
  <files>src/components/dashboard-demo/WorkflowSkeleton.tsx, src/components/dashboard-demo/DemoSlicePanel.tsx, src/app/dashboard-demo/page.shell.test.tsx</files>
  <action>
    Read the brushed selection as the generator's canonical input in the workflow controls, surface the hourly/daily granularity choice with daily as the default, keep the generate action user-triggered rather than auto-refreshing, and leave crime-type filters available without making them mandatory. Update the slice rail copy so editable selection-first drafts are clearly marked before apply and the visible payload stays limited to `B`, state, and muted neutral output. Extend the shell test to lock the brush-first input, muted neutral-partition copy, default-all crime-type scope, and absence of any density/preset fallback language in the burst workflow.
  </action>
  <verify>pnpm vitest run src/app/dashboard-demo/page.shell.test.tsx</verify>
  <done>The demo workflow explains non-uniform slicing choices, keeps review/editing centered on the brushed selection, and avoids a false empty state.</done>
</task>

</tasks>

<verification>
Run the helper/store tests and the dashboard-demo shell source-inspection test.
</verification>

<success_criteria>
Non-uniform time slicing starts from the brushed selection, keeps full coverage, and shows a neutral-partition state instead of silently falling back.
</success_criteria>

<output>
After completion, create `.planning/phases/10-selection-first-burst-slice-generation/10-02-SUMMARY.md`
</output>
