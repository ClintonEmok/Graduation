---
phase: 43-3d-sandbox-route-foundation
plan: 02
type: execute
wave: 2
depends_on: [43-01]
files_modified:
  - src/app/cube-sandbox/page.tsx
  - src/app/cube-sandbox/components/SandboxContextPanel.tsx
  - src/app/cube-sandbox/lib/resetSandboxState.ts
  - src/app/cube-sandbox/lib/resetSandboxState.test.ts
  - src/store/useAdaptiveStore.ts
autonomous: true
must_haves:
  truths:
    - Sandbox route initializes with thesis-ready cube defaults and uniform timescaling mode.
    - User can always inspect active sandbox context (dataset state, filter summary, spatial bounds, warp mode) from a compact right-side panel.
    - User can hard reset sandbox state in-session without reloading the application.
  artifacts:
    - path: src/app/cube-sandbox/lib/resetSandboxState.ts
      provides: Single reset orchestrator for sandbox stores and mode defaults.
    - path: src/app/cube-sandbox/components/SandboxContextPanel.tsx
      provides: Compact always-visible context/debug panel for active sandbox state.
    - path: src/app/cube-sandbox/page.tsx
      provides: Runtime wiring for startup defaults, reset action, and context panel mounting.
    - path: src/app/cube-sandbox/lib/resetSandboxState.test.ts
      provides: Regression coverage for sandbox reset behavior.
  key_links:
    - from: src/app/cube-sandbox/page.tsx
      to: src/app/cube-sandbox/lib/resetSandboxState.ts
      via: reset button callback
      pattern: resetSandboxState
    - from: src/app/cube-sandbox/components/SandboxContextPanel.tsx
      to: src/store/useFilterStore.ts
      via: filter and spatial-bounds summary readout
      pattern: selectedTimeRange|selectedSpatialBounds
    - from: src/app/cube-sandbox/page.tsx
      to: src/store/useAdaptiveStore.ts
      via: force uniform startup + post-reset mode consistency
      pattern: setTimeScaleMode\('linear'\)|setWarpFactor\(0\)
---

<objective>
Implement sandbox startup defaults, compact active-context visibility, and rapid hard-reset behavior.

Purpose: Satisfy ROUTE-02/03/04 by ensuring the new route is immediately usable for repeated cube experiments with deterministic state recovery.
Output: Reset utility + tests, context panel component, and page-level wiring for startup/reset flows.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-3d-sandbox-route-foundation/43-CONTEXT.md
@.planning/phases/43-3d-sandbox-route-foundation/43-01-SUMMARY.md
@src/app/timeline-test/page.tsx
@src/store/useFilterStore.ts
@src/store/useAdaptiveStore.ts
@src/store/useSliceStore.ts
@src/store/useWarpSliceStore.ts
@src/store/useTimeStore.ts
@src/store/useDataStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic sandbox bootstrap and reset orchestrator</name>
  <files>src/app/cube-sandbox/lib/resetSandboxState.ts, src/app/cube-sandbox/page.tsx, src/store/useAdaptiveStore.ts</files>
  <action>Create `resetSandboxState` as the single route-level reset entrypoint and wire it into sandbox page startup and reset button flow. The reset must: clear active filters/spatial bounds, clear slice/warp proposal state, restore uniform mode (`timeScaleMode='linear'`, `warpFactor=0`, density warp source default), and keep dataset ready by reusing thesis data load path (`useDataStore.loadRealData`) without forcing full app reload. Add a small reset helper in `useAdaptiveStore` only if needed to avoid brittle direct `setState` calls. Do not introduce browser confirm dialogs for reset.</action>
  <verify>npm run lint -- src/app/cube-sandbox/lib/resetSandboxState.ts src/app/cube-sandbox/page.tsx src/store/useAdaptiveStore.ts</verify>
  <done>Sandbox startup and manual reset both return the workspace to a deterministic baseline within the same session.</done>
</task>

<task type="auto">
  <name>Task 2: Build compact always-visible sandbox context panel</name>
  <files>src/app/cube-sandbox/components/SandboxContextPanel.tsx, src/app/cube-sandbox/page.tsx</files>
  <action>Implement a compact right-side context panel and mount it in the sandbox shell. Panel must always expose: dataset source/readiness, active filter count + key filter flags, spatial bounds status, current time-scale mode, and warp source/factor summary. Keep it lightweight (debug-oriented, no export/share actions), aligned with Phase 43 context constraints, and visible without extra expansion steps.</action>
  <verify>npm run lint -- src/app/cube-sandbox/components/SandboxContextPanel.tsx src/app/cube-sandbox/page.tsx</verify>
  <done>User can inspect required sandbox context at a glance while interacting with cube view.</done>
</task>

<task type="auto">
  <name>Task 3: Add reset orchestration regression test coverage</name>
  <files>src/app/cube-sandbox/lib/resetSandboxState.test.ts, src/app/cube-sandbox/lib/resetSandboxState.ts</files>
  <action>Create focused unit tests proving `resetSandboxState` restores core defaults and clears mutable workflow state in one call. Cover at minimum: non-default adaptive mode -> linear, non-zero warpFactor -> 0, populated filters/spatial bounds -> cleared, populated slice/warp stores -> emptied. Keep test scope at store orchestration level (no React render harness needed).</action>
  <verify>npm run test -- src/app/cube-sandbox/lib/resetSandboxState.test.ts</verify>
  <done>Automated tests guard against reset regressions that would block rapid iterative experimentation.</done>
</task>

</tasks>

<verification>
Run: `npm run test -- src/app/cube-sandbox/lib/resetSandboxState.test.ts`
Run: `npm run lint -- src/app/cube-sandbox/page.tsx src/app/cube-sandbox/components/SandboxContextPanel.tsx src/app/cube-sandbox/lib/resetSandboxState.ts src/store/useAdaptiveStore.ts`
Run: `npm run dev` and in `/cube-sandbox` mutate filters/slices/mode then trigger reset and confirm state returns to defaults without full reload.
</verification>

<success_criteria>
ROUTE-02/ROUTE-03/ROUTE-04 are satisfied in the sandbox route: thesis-ready defaults load, context is continuously visible, and hard reset reliably restores baseline in-session.
</success_criteria>

<output>
After completion, create `.planning/phases/43-3d-sandbox-route-foundation/43-02-SUMMARY.md`
</output>
