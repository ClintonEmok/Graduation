---
phase: 43-3d-sandbox-route-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/cube-sandbox/page.tsx
  - src/app/cube-sandbox/components/SandboxShell.tsx
  - src/app/page.tsx
autonomous: true
must_haves:
  truths:
    - User can open a dedicated 3D sandbox route that is separate from the existing production/dashboard workflow.
    - Entering the sandbox route drops the user directly into cube experimentation (no intermediate landing flow).
    - The sandbox route remains isolated from timeline-test and dashboard layouts.
  artifacts:
    - path: src/app/cube-sandbox/page.tsx
      provides: Route entry for cube-first sandbox workflow.
    - path: src/app/cube-sandbox/components/SandboxShell.tsx
      provides: Route-local shell that hosts cube canvas and right-side utility rail.
    - path: src/app/page.tsx
      provides: Discoverable navigation link into the new sandbox route.
  key_links:
    - from: src/app/page.tsx
      to: /cube-sandbox
      via: primary navigation CTA
      pattern: href="/cube-sandbox"
    - from: src/app/cube-sandbox/page.tsx
      to: src/components/viz/CubeVisualization.tsx
      via: cube-first page composition
      pattern: CubeVisualization
---

<objective>
Create the dedicated cube sandbox route scaffold and make it reachable from the app entry point.

Purpose: Satisfy ROUTE-01 by establishing a clearly isolated workspace for v2.0 experimentation, using the existing timeslicing test route as structural reference for isolated route behavior.
Output: New `/cube-sandbox` route, route-local shell component, and home-page navigation entry.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-3d-sandbox-route-foundation/43-CONTEXT.md
@src/app/timeline-test/page.tsx
@src/app/dashboard/page.tsx
@src/components/viz/CubeVisualization.tsx
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold isolated `/cube-sandbox` route with cube-first shell</name>
  <files>src/app/cube-sandbox/page.tsx, src/app/cube-sandbox/components/SandboxShell.tsx</files>
  <action>Create the new route at `src/app/cube-sandbox/page.tsx` and a route-local `SandboxShell` component that renders a cube-first workspace. Use `/timeline-test` as the isolation reference pattern (single-purpose page and route-owned UI), but do not copy timeline-specific controls. The shell must mount `CubeVisualization` directly and reserve a right-side panel region for sandbox context/debug content in later plans. Keep the route independent from `DashboardLayout`, `TopBar`, and production navigation chrome so production workflow coupling is avoided.</action>
  <verify>npm run lint -- src/app/cube-sandbox/page.tsx src/app/cube-sandbox/components/SandboxShell.tsx</verify>
  <done>`/cube-sandbox` renders a dedicated cube-focused page with no dependency on dashboard/timeline layout composition.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit app entry access to the sandbox route</name>
  <files>src/app/page.tsx</files>
  <action>Add a clear navigation action from the home page to `/cube-sandbox` while keeping the existing `/timeline-test` access intact. Labeling should communicate v2.0 sandbox purpose (cube-first experimentation) and avoid wording that implies production-readiness. Keep visual style aligned with existing homepage design language.</action>
  <verify>npm run lint -- src/app/page.tsx</verify>
  <done>User can reach `/cube-sandbox` from the app entry page without typing URLs manually.</done>
</task>

</tasks>

<verification>
Run: `npm run lint -- src/app/cube-sandbox/page.tsx src/app/cube-sandbox/components/SandboxShell.tsx src/app/page.tsx`
Run: `npm run dev` and open `/cube-sandbox` to confirm isolated cube route loads.
</verification>

<success_criteria>
ROUTE-01 is materially satisfied: an isolated, discoverable cube sandbox route exists and opens directly into cube experimentation.
</success_criteria>

<output>
After completion, create `.planning/phases/43-3d-sandbox-route-foundation/43-01-SUMMARY.md`
</output>
