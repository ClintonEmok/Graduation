---
phase: 09-burstiness-driven-slice-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard-demo/lib/demo-burst-generation.ts
  - src/store/useDashboardDemoTimeslicingModeStore.ts
  - src/store/slice-domain/createSliceCoreSlice.ts
autonomous: true
must_haves:
  truths:
    - "Burst windows can become draft slices without recomputing burst detection inside the demo store."
    - "If burst windows are missing for the active selection, the demo falls back to the existing preset-bias path instead of producing an empty draft set."
    - "Applied burst drafts become named burst slices with burst metadata preserved in the slice domain."
  artifacts:
    - path: "src/components/dashboard-demo/lib/demo-burst-generation.ts"
      provides: "Pure burst-window to draft-bin conversion helpers"
    - path: "src/store/useDashboardDemoTimeslicingModeStore.ts"
      provides: "Burst draft generation action and fallback routing"
    - path: "src/store/slice-domain/createSliceCoreSlice.ts"
      provides: "Burst-aware applied slice conversion and naming"
  key_links:
    - from: "src/components/dashboard-demo/lib/demo-burst-generation.ts"
      to: "src/store/useDashboardDemoTimeslicingModeStore.ts"
      via: "burst-window conversion output feeds the deliberate generate action"
      pattern: "buildBurstDraftBinsFromWindows|generateBurstDraftBinsFromWindows"
    - from: "src/store/useDashboardDemoTimeslicingModeStore.ts"
      to: "src/store/slice-domain/createSliceCoreSlice.ts"
      via: "applied burst draft bins carry burst metadata into slice creation"
      pattern: "burstClass|isBurst: true|Burst \\${index + 1}"
---

<objective>
Turn existing burst windows into reviewable draft slices inside the dashboard-demo store, while preserving a safe fallback to the current preset-bias generator.

Purpose: make burst-driven generation deterministic and reviewable before the workflow shell starts exposing it to users.
Output: a burst-draft helper, a burst-aware demo generation action, and burst-preserving slice application.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-burstiness-driven-slice-generation/09-CONTEXT.md
@src/components/viz/BurstList.tsx
@src/lib/binning/types.ts
@src/store/useDashboardDemoTimeslicingModeStore.ts
@src/store/slice-domain/createSliceCoreSlice.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a pure burst-draft conversion helper</name>
  <files>src/components/dashboard-demo/lib/demo-burst-generation.ts</files>
  <read_first>
    - src/components/viz/BurstList.tsx
    - src/lib/binning/types.ts
    - .planning/phases/09-burstiness-driven-slice-generation/09-CONTEXT.md
  </read_first>
  <action>
    Create a pure helper module that accepts the existing burst-window shape from the timeline path and converts overlapping windows into draft TimeBin entries for the dashboard demo. Each draft bin should preserve the burst taxonomy fields already available on TimeBin, carry burst-first metadata for later display, and expose a clear fallback result when no windows overlap the active selection. Keep the helper free of store access and do not recompute burst detection here.
  </action>
  <acceptance_criteria>
    - `src/components/dashboard-demo/lib/demo-burst-generation.ts` exports `buildBurstDraftBinsFromWindows`.
    - The helper file references `burstClass`, `burstConfidence`, `burstScore`, and `burstProvenance`.
    - The helper file contains no store imports.
  </acceptance_criteria>
  <verify>grep -n "buildBurstDraftBinsFromWindows\|burstClass\|burstConfidence\|burstScore\|burstProvenance" src/components/dashboard-demo/lib/demo-burst-generation.ts</verify>
  <done>Burst windows can be translated into draft bins without any demo-store side effects.</done>
</task>

<task type="auto">
  <name>Task 2: Route burst generation through the dashboard-demo store</name>
  <files>src/store/useDashboardDemoTimeslicingModeStore.ts</files>
  <read_first>
    - src/store/useDashboardDemoTimeslicingModeStore.ts
    - src/components/dashboard-demo/lib/demo-burst-generation.ts
    - src/components/viz/BurstList.tsx
    - src/store/useDashboardDemoTimeStore.ts
    - src/store/useTimelineDataStore.ts
  </read_first>
  <action>
    Add a deliberate burst-generation action that accepts burst windows from the timeline path, filters them to the active generation time window, and stores the resulting burst draft bins with burst-aware metadata. Record the generation source in metadata so the UI can distinguish burst-driven drafts from the existing preset-bias fallback. If the helper returns no burst drafts, call the existing preset-bias generator instead of leaving the draft list empty. Keep the store isolated from burst detection logic and do not touch the stable `/timeslicing` store.
  </action>
  <acceptance_criteria>
    - `src/store/useDashboardDemoTimeslicingModeStore.ts` exports `generateBurstDraftBinsFromWindows`.
    - The store metadata includes a burst/preset generation source field.
    - The store still exports `generateBinsFromActivePresetBias` for fallback use.
  </acceptance_criteria>
  <verify>grep -n "generateBurstDraftBinsFromWindows\|generationSource\|generateBinsFromActivePresetBias" src/store/useDashboardDemoTimeslicingModeStore.ts</verify>
  <done>The demo store can build burst drafts or fall back cleanly without recomputing burst detection.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve burst metadata when draft bins are applied</name>
  <files>src/store/slice-domain/createSliceCoreSlice.ts</files>
  <read_first>
    - src/store/slice-domain/createSliceCoreSlice.ts
    - src/lib/binning/types.ts
    - src/store/slice-domain/types.ts
    - src/components/dashboard-demo/lib/demo-burst-generation.ts
  </read_first>
  <action>
    Update `replaceSlicesFromBins` so any applied draft bin carrying burst taxonomy metadata becomes a burst slice in the slice domain. Burst-derived applied slices should be named `Burst N`, keep `warpEnabled: true`, set `isBurst: true`, and copy the burst taxonomy fields through to the final slice object. Keep non-burst bins on the existing generic generated-slice path.
  </action>
  <acceptance_criteria>
    - `src/store/slice-domain/createSliceCoreSlice.ts` contains `isBurst: true` in the applied-bin path.
    - Burst-derived applied slices are named with `Burst ` instead of `Slice `.
    - Non-burst generated bins still use the generic generated-slice path.
  </acceptance_criteria>
  <verify>grep -n "isBurst: true\|Burst \$\{index \+ 1\}\|Slice \$\{index \+ 1\}" src/store/slice-domain/createSliceCoreSlice.ts</verify>
  <done>Applied burst drafts survive as burst-marked slices instead of anonymous generic ranges.</done>
</task>

</tasks>

<verification>
Run focused source-inspection checks on the helper, demo store, and slice-domain application path.
</verification>

<success_criteria>
Burst windows can be turned into draft slices, burst fallback is safe, and applied burst drafts keep burst identity in the slice domain.
</success_criteria>

<output>
After completion, create `.planning/phases/09-burstiness-driven-slice-generation/09-01-SUMMARY.md`
</output>
