---
phase: 09-burstiness-driven-slice-generation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/useDashboardDemoTimeslicingModeStore.ts
  - src/store/useDashboardDemoTimeslicingModeStore.test.ts
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Applying generated burst drafts replaces the current slice set in the slice domain."
    - "The pending draft queue is cleared only after the slice-domain replacement happens."
    - "Apply remains a no-op when there are no pending generated bins."
  artifacts:
    - path: "src/store/useDashboardDemoTimeslicingModeStore.ts"
      provides: "Apply path that forwards pending drafts into the slice domain"
    - path: "src/store/useDashboardDemoTimeslicingModeStore.test.ts"
      provides: "Regression test for replace-on-apply behavior"
  key_links:
    - from: "src/store/useDashboardDemoTimeslicingModeStore.ts"
      to: "src/store/useSliceDomainStore.ts"
      via: "applyGeneratedBins forwards pendingGeneratedBins into replaceSlicesFromBins"
      pattern: "replaceSlicesFromBins|applyGeneratedBins|pendingGeneratedBins"
    - from: "src/store/useDashboardDemoTimeslicingModeStore.ts"
      to: "src/store/useTimeslicingModeStore.ts"
      via: "mirror the existing stable apply pattern"
      pattern: "useSliceDomainStore.getState().replaceSlicesFromBins"
---

<objective>
Wire the demo apply path so burst drafts actually replace the active slice set.

Purpose: close the draft-to-slice handoff gap without changing generation or preview behavior.
Output: a store-level apply handoff and a regression test proving the replacement happens.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-burstiness-driven-slice-generation/09-VERIFICATION.md
@.planning/phases/09-burstiness-driven-slice-generation/09-CONTEXT.md
@.planning/phases/09-burstiness-driven-slice-generation/09-01-SUMMARY.md
@src/store/useDashboardDemoTimeslicingModeStore.ts
@src/store/useTimeslicingModeStore.ts
@src/store/useSliceDomainStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire demo apply through the slice domain</name>
  <files>src/store/useDashboardDemoTimeslicingModeStore.ts</files>
  <action>
    Update `applyGeneratedBins` so it reads the current `pendingGeneratedBins`, forwards them into `useSliceDomainStore.getState().replaceSlicesFromBins(pendingGeneratedBins, domain)`, and only then clears the pending queue while stamping `lastAppliedAt` and `generationStatus`. Keep the empty-queue no-op behavior intact and do not alter the burst generation preview path.
  </action>
  <verify>grep -n "replaceSlicesFromBins\|applyGeneratedBins\|pendingGeneratedBins" src/store/useDashboardDemoTimeslicingModeStore.ts</verify>
  <done>The demo apply action now truly replaces the active slice set from the pending burst drafts.</done>
</task>

<task type="auto">
  <name>Task 2: Add a replace-on-apply regression test for the demo store</name>
  <files>src/store/useDashboardDemoTimeslicingModeStore.test.ts</files>
  <action>
    Add a focused Vitest file that seeds pending draft bins, applies them into a domain, and asserts the slice domain receives the replaced slices while the pending queue is cleared afterward. Include a no-pending case so the store still returns a no-op when there is nothing to apply.
  </action>
  <verify>pnpm exec vitest run src/store/useDashboardDemoTimeslicingModeStore.test.ts --reporter=dot</verify>
  <done>The replace-on-apply handoff is locked by a direct store test.</done>
</task>

</tasks>

<verification>
Run the demo store test and source-inspection checks for the slice-domain replacement call.
</verification>

<success_criteria>
Applying burst drafts replaces the slice set, clears the pending queue afterward, and still no-ops safely when nothing is pending.
</success_criteria>

<output>
After completion, create `.planning/phases/09-burstiness-driven-slice-generation/09-04-SUMMARY.md`
</output>
