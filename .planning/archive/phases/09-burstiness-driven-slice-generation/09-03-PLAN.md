---
phase: 09-burstiness-driven-slice-generation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard-demo/WorkflowSkeleton.tsx
  - src/components/dashboard-demo/DemoSlicePanel.tsx
  - src/components/timeline/DemoDualTimeline.tsx
  - src/app/dashboard-demo/page.shell.test.tsx
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Users can edit pending burst drafts before apply from the dashboard-demo review surface."
    - "Merge, split, and delete controls act on pendingGeneratedBins rather than the applied slice list."
    - "The workflow shell and timeline preview clearly signal that the current draft set is still editable."
  artifacts:
    - path: "src/components/dashboard-demo/DemoSlicePanel.tsx"
      provides: "Pending-draft review section with merge/split/delete controls"
    - path: "src/components/dashboard-demo/WorkflowSkeleton.tsx"
      provides: "Pre-apply review copy and pending-draft status cues"
    - path: "src/components/timeline/DemoDualTimeline.tsx"
      provides: "Editable draft preview labeling in the timeline"
    - path: "src/app/dashboard-demo/page.shell.test.tsx"
      provides: "Source-inspection lock for pending-draft edit wiring"
  key_links:
    - from: "src/components/dashboard-demo/DemoSlicePanel.tsx"
      to: "src/store/useDashboardDemoTimeslicingModeStore.ts"
      via: "pending draft control handlers target pendingGeneratedBins"
      pattern: "pendingGeneratedBins|mergePendingGeneratedBins|splitPendingGeneratedBin|deletePendingGeneratedBin"
    - from: "src/components/timeline/DemoDualTimeline.tsx"
      to: "src/components/dashboard-demo/DemoSlicePanel.tsx"
      via: "timeline preview and rail share the same editable draft state"
      pattern: "isGeneratedDraft|pendingGeneratedBins|Burst draft"
---

<objective>
Expose editable pending burst drafts in the dashboard-demo workflow before apply.

Purpose: keep review-before-apply real instead of read-only.
Output: pending draft editor surface, updated workflow/timeline cues, and regression coverage.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-burstiness-driven-slice-generation/09-VERIFICATION.md
@.planning/phases/09-burstiness-driven-slice-generation/09-CONTEXT.md
@.planning/phases/09-burstiness-driven-slice-generation/09-02-SUMMARY.md
@src/components/dashboard-demo/DemoSlicePanel.tsx
@src/components/dashboard-demo/WorkflowSkeleton.tsx
@src/components/timeline/DemoDualTimeline.tsx
@src/app/dashboard-demo/page.shell.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a pending-draft editor surface in the slice rail</name>
  <files>src/components/dashboard-demo/DemoSlicePanel.tsx</files>
  <action>
    Add a dedicated pending-drafts review section that renders `pendingGeneratedBins` separately from the applied `slices` list. Reuse the store's `mergePendingGeneratedBins`, `splitPendingGeneratedBin`, and `deletePendingGeneratedBin` actions against the pending queue, and keep the existing applied-slice editor intact. Make the copy clearly state that these bins are still editable before apply.
  </action>
  <verify>grep -n "pendingGeneratedBins\|mergePendingGeneratedBins\|splitPendingGeneratedBin\|deletePendingGeneratedBin" src/components/dashboard-demo/DemoSlicePanel.tsx</verify>
  <done>Pending burst drafts have their own edit controls and can be modified without applying them.</done>
</task>

<task type="auto">
  <name>Task 2: Thread editable-draft cues through the shell and timeline</name>
  <files>src/components/dashboard-demo/WorkflowSkeleton.tsx, src/components/timeline/DemoDualTimeline.tsx</files>
  <action>
    Update the workflow shell copy and the dual timeline preview labels/badges so pending burst drafts read as an editable review state before apply. Keep the generation and apply behavior unchanged; this task only makes the pre-apply state visible and explicit in the shell and timeline preview.
  </action>
  <verify>grep -n "pendingGeneratedBins\|editable\|Burst draft\|review" src/components/dashboard-demo/WorkflowSkeleton.tsx src/components/timeline/DemoDualTimeline.tsx</verify>
  <done>The demo workflow and timeline no longer present pending burst drafts as passive previews.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the pending-draft review contract</name>
  <files>src/app/dashboard-demo/page.shell.test.tsx</files>
  <action>
    Extend the existing dashboard-demo shell test with source-inspection assertions for the pending-draft edit controls and their `pendingGeneratedBins` wiring. Keep the stable-route isolation checks from the burst workflow test intact.
  </action>
  <verify>pnpm exec vitest run src/app/dashboard-demo/page.shell.test.tsx --reporter=dot</verify>
  <done>The pending-draft edit surface is source-locked so it cannot regress silently.</done>
</task>

</tasks>

<verification>
Run the dashboard-demo shell test and focused source-inspection checks for the pending-draft editor wiring.
</verification>

<success_criteria>
Users can review and edit pending burst drafts before apply, and the shell/timeline clearly show that the draft set is still mutable.
</success_criteria>

<output>
After completion, create `.planning/phases/09-burstiness-driven-slice-generation/09-03-SUMMARY.md`
</output>
