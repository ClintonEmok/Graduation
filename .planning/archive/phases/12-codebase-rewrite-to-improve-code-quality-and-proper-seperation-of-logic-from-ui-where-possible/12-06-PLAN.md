---
phase: 12-codebase-rewrite-to-improve-code-quality-and-proper-seperation-of-logic-from-ui-where-possible
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useSuggestionGenerator.ts
  - src/lib/bounds.test.ts
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "useSuggestionGenerator imports and calls the shared debounce and bounds helpers"
    - "useSuggestionGenerator no longer defines local useDebounce or deriveBoundsFromCrimes copies"
    - "Bounds padding behavior stays the same and is covered by a focused regression test"
  artifacts:
    - path: "src/hooks/useSuggestionGenerator.ts"
      provides: "Suggestion generation wired to shared helpers"
    - path: "src/lib/bounds.ts"
      provides: "Canonical deriveBoundsFromCrimes helper"
    - path: "src/lib/bounds.test.ts"
      provides: "Padding semantics regression coverage"
  key_links:
    - from: "src/hooks/useSuggestionGenerator.ts"
      to: "src/hooks/useDebounce.ts"
      via: "import and call"
      pattern: "useDebounce\\("
    - from: "src/hooks/useSuggestionGenerator.ts"
      to: "src/lib/bounds.ts"
      via: "import and call"
      pattern: "deriveBoundsFromCrimes\\("
    - from: "src/lib/bounds.test.ts"
      to: "src/lib/bounds.ts"
      via: "direct import"
      pattern: "from '@/lib/bounds'"
---

<objective>
Close the utility-wiring gap by deleting local helper duplicates and making the suggestion generator depend only on shared utility modules.

Purpose: prove extracted helpers are actually reusable, not dead code.
Output: a thinner suggestion generator plus a regression test that locks bounds semantics.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-codebase-rewrite-to-improve-code-quality-and-proper-seperation-of-logic-from-ui-where-possible/12-02-SUMMARY.md
@.planning/phases/12-codebase-rewrite-to-improve-code-quality-and-proper-seperation-of-logic-from-ui-where-possible/12-VERIFICATION.md
@src/hooks/useSuggestionGenerator.ts
@src/hooks/useDebounce.ts
@src/lib/bounds.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire shared helpers into useSuggestionGenerator</name>
  <files>src/hooks/useSuggestionGenerator.ts</files>
  <read_first>src/hooks/useSuggestionGenerator.ts, src/hooks/useDebounce.ts, src/lib/bounds.ts</read_first>
  <action>Replace the local debounce and geographic-bounds helper implementations with imports from the shared hook/module. Keep the current 750ms debounce delay, preserve the 10% padding with 0.01 fallback in bounds calculation, and remove the dead local helper definitions so the hook only owns suggestion-generation orchestration.</action>
  <acceptance_criteria>grep -n "function useDebounce" src/hooks/useSuggestionGenerator.ts returns no matches; grep -n "function deriveBoundsFromCrimes" src/hooks/useSuggestionGenerator.ts returns no matches; grep -n "useDebounce" src/hooks/useSuggestionGenerator.ts shows an import from '@/hooks/useDebounce'; grep -n "deriveBoundsFromCrimes" src/hooks/useSuggestionGenerator.ts shows an import from '@/lib/bounds'.</acceptance_criteria>
  <verify>grep checks above show only shared imports and no local helper bodies.</verify>
  <done>useSuggestionGenerator depends on shared helper modules only, with no local duplicate debounce/bounds code left behind.</done>
</task>

<task type="auto">
  <name>Task 2: Lock bounds padding semantics with a regression test</name>
  <files>src/lib/bounds.test.ts</files>
  <read_first>src/lib/bounds.ts</read_first>
  <action>Create a focused Vitest file that exercises deriveBoundsFromCrimes directly. Cover two cases: no finite lat/lon values returns undefined, and a valid crime set pads min/max by 10% with a 0.01 minimum padding floor. Keep the assertions explicit so the helper can be reused safely elsewhere.</action>
  <acceptance_criteria>src/lib/bounds.test.ts imports deriveBoundsFromCrimes from '@/lib/bounds'; the test file contains explicit expectations for undefined output on empty/invalid input and padded min/max values on valid input.</acceptance_criteria>
  <verify>pnpm vitest src/lib/bounds.test.ts src/hooks/useSuggestionGenerator.test.ts passes.</verify>
  <done>The extracted bounds helper is behavior-locked by a dedicated regression test.</done>
</task>

</tasks>

<verification>
Run the new bounds test, then grep the suggestion generator for dead helper bodies and confirm the shared imports are present.
</verification>

<success_criteria>
The suggestion generator uses shared debounce and bounds helpers, no local duplicate helper bodies remain, and the bounds helper has direct regression coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/12-codebase-rewrite-to-improve-code-quality-and-proper-seperation-of-logic-from-ui-where-possible/12-06-SUMMARY.md`
</output>
