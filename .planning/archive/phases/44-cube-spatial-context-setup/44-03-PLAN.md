---
phase: 44-cube-spatial-context-setup
plan: 03
type: execute
wave: 2
depends_on: [44-01]
files_modified:
  - src/components/viz/SpatialConstraintOverlay.tsx
  - src/components/viz/MainScene.tsx
  - src/components/viz/CubeVisualization.tsx
  - src/components/viz/spatialConstraintGeometry.ts
  - src/components/viz/spatialConstraintGeometry.test.ts
autonomous: true
must_haves:
  truths:
    - Enabled constraints are visible in cube view as explicit spatial indicators.
    - Active constraint is visually distinguishable from non-active constraints during cube interaction.
    - Constraint indicators update immediately when constraint definitions or toggles change.
  artifacts:
    - path: src/components/viz/SpatialConstraintOverlay.tsx
      provides: Renderable cube overlay primitives for enabled spatial constraints.
    - path: src/components/viz/CubeVisualization.tsx
      provides: In-cube status cues tying sandbox constraints to user interactions.
    - path: src/components/viz/spatialConstraintGeometry.ts
      provides: Deterministic geometry conversion helper from constraint bounds to overlay primitives.
  key_links:
    - from: src/components/viz/SpatialConstraintOverlay.tsx
      to: src/store/useCubeSpatialConstraintsStore.ts
      via: selector subscription for enabled and active constraints
      pattern: enabled|activeConstraintId
    - from: src/components/viz/MainScene.tsx
      to: src/components/viz/SpatialConstraintOverlay.tsx
      via: scene composition
      pattern: SpatialConstraintOverlay
    - from: src/components/viz/spatialConstraintGeometry.test.ts
      to: src/components/viz/spatialConstraintGeometry.ts
      via: bounds-to-overlay assertions
      pattern: toOverlayBox
---

<objective>
Surface spatial constraints directly inside cube interactions as first-class context cues.

Purpose: Satisfy CSPAT-03 by ensuring constraints are not just stored in panels but are visible and actionable in the primary cube interaction surface.
Output: Scene overlay rendering, active-state visual differentiation, and helper test coverage for geometry mapping.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/44-cube-spatial-context-setup/44-01-PLAN.md
@src/components/viz/MainScene.tsx
@src/components/viz/CubeVisualization.tsx
@src/components/viz/Scene.tsx
@src/store/useCubeSpatialConstraintsStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement constraint geometry helper and tests for cube overlays</name>
  <files>src/components/viz/spatialConstraintGeometry.ts, src/components/viz/spatialConstraintGeometry.test.ts</files>
  <action>Create a pure helper that converts stored constraint bounds into render-ready cube overlay primitives (center/size and label anchor metadata). Add tests for normal ranges, swapped/min-max correction handling, and degenerate bounds guardrails so overlay rendering remains stable with user-edited values.</action>
  <verify>npm run test -- src/components/viz/spatialConstraintGeometry.test.ts</verify>
  <done>Geometry mapping is deterministic and protected by tests before integrating into live scene rendering.</done>
</task>

<task type="auto">
  <name>Task 2: Render enabled constraints in cube scene with active highlight cues</name>
  <files>src/components/viz/SpatialConstraintOverlay.tsx, src/components/viz/MainScene.tsx, src/components/viz/CubeVisualization.tsx</files>
  <action>Add an overlay layer to the cube scene that renders all enabled constraints with visible outlines and an emphasized style for the active constraint. Also show concise in-cube status chips (e.g., enabled count + active label) so users can confirm context while interacting. Ensure updates react immediately to store changes without route reloads.</action>
  <verify>npm run lint -- src/components/viz/SpatialConstraintOverlay.tsx src/components/viz/MainScene.tsx src/components/viz/CubeVisualization.tsx</verify>
  <done>Constraint definitions are visible as first-class cube interaction indicators, with clear active-state differentiation and live updates.</done>
</task>

</tasks>

<verification>
Run: `npm run test -- src/components/viz/spatialConstraintGeometry.test.ts`
Run: `npm run lint -- src/components/viz/SpatialConstraintOverlay.tsx src/components/viz/MainScene.tsx src/components/viz/CubeVisualization.tsx`
Run: `npm run dev` and in `/cube-sandbox` toggle constraints on/off and switch active constraint; confirm overlay visibility and active styling update instantly.
</verification>

<success_criteria>
Cube view displays enabled spatial constraints with clear active emphasis and real-time response to constraint state changes, satisfying CSPAT-03.
</success_criteria>

<output>
After completion, create `.planning/phases/44-cube-spatial-context-setup/44-03-SUMMARY.md`
</output>
