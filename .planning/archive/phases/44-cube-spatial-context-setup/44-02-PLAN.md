---
phase: 44-cube-spatial-context-setup
plan: 02
type: execute
wave: 2
depends_on: [44-01]
files_modified:
  - src/app/cube-sandbox/components/SpatialConstraintManager.tsx
  - src/app/cube-sandbox/components/SandboxContextPanel.tsx
  - src/app/cube-sandbox/page.tsx
autonomous: true
must_haves:
  truths:
    - User can create one or more named spatial constraints directly in the cube sandbox route.
    - User can toggle constraints on/off without losing their definitions.
    - User can see which constraints are active from always-on sandbox context controls.
  artifacts:
    - path: src/app/cube-sandbox/components/SpatialConstraintManager.tsx
      provides: Constraint authoring and management UI for add/edit/toggle/select flows.
    - path: src/app/cube-sandbox/components/SandboxContextPanel.tsx
      provides: Always-visible summary of active/available constraints in sandbox diagnostics rail.
    - path: src/app/cube-sandbox/page.tsx
      provides: Route wiring for constraint manager controls inside sandbox shell.
  key_links:
    - from: src/app/cube-sandbox/components/SpatialConstraintManager.tsx
      to: src/store/useCubeSpatialConstraintsStore.ts
      via: CRUD/toggle/activate actions
      pattern: addConstraint|updateConstraint|toggleConstraintEnabled|setActiveConstraint
    - from: src/app/cube-sandbox/components/SandboxContextPanel.tsx
      to: src/app/cube-sandbox/components/SpatialConstraintManager.tsx
      via: manager mounting in context diagnostics
      pattern: SpatialConstraintManager
---

<objective>
Deliver sandbox controls for defining and managing multiple cube spatial constraints.

Purpose: Satisfy CSPAT-01/CSPAT-02 from the user workflow perspective by making constraint creation and toggling directly accessible in the cube sandbox rail.
Output: Constraint manager component, context panel integration, and route wiring for daily sandbox use.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-3d-sandbox-route-foundation/43-02-SUMMARY.md
@.planning/phases/44-cube-spatial-context-setup/44-01-PLAN.md
@src/app/cube-sandbox/components/SandboxContextPanel.tsx
@src/app/cube-sandbox/components/SandboxShell.tsx
@src/store/useCubeSpatialConstraintsStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build spatial constraint manager UI for multi-region workflows</name>
  <files>src/app/cube-sandbox/components/SpatialConstraintManager.tsx</files>
  <action>Implement a compact manager that supports adding more than one constraint region, editing labels/geometry values, toggling enabled state, selecting one active constraint, and deleting obsolete constraints. Keep interaction model optimized for sandbox speed (inline controls, minimal modal friction). For geometry input, use explicit bounds fields aligned to the store model; do not hardcode a single fixed region.</action>
  <verify>npm run lint -- src/app/cube-sandbox/components/SpatialConstraintManager.tsx</verify>
  <done>User can create and maintain multiple named constraint regions from one always-available panel.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate manager into sandbox context rail with active-state cues</name>
  <files>src/app/cube-sandbox/components/SandboxContextPanel.tsx, src/app/cube-sandbox/page.tsx</files>
  <action>Mount the new manager in the existing sandbox context panel and add concise readouts: total constraints, enabled count, and current active constraint label. Preserve existing reset controls and diagnostics from Phase 43 while extending the panel for CSPAT workflows. Keep the panel legible at desktop thesis resolution without introducing collapsible-by-default sections.</action>
  <verify>npm run lint -- src/app/cube-sandbox/components/SandboxContextPanel.tsx src/app/cube-sandbox/page.tsx</verify>
  <done>Cube sandbox route exposes first-class controls and status cues for spatial constraints without leaving the main experiment view.</done>
</task>

</tasks>

<verification>
Run: `npm run lint -- src/app/cube-sandbox/components/SpatialConstraintManager.tsx src/app/cube-sandbox/components/SandboxContextPanel.tsx src/app/cube-sandbox/page.tsx`
Run: `npm run dev` and in `/cube-sandbox` create 2+ constraints, toggle one off/on, switch active selection, and confirm definitions remain in the manager list.
</verification>

<success_criteria>
Users can define and manage multiple spatial constraints in the sandbox rail, and toggle behavior preserves constraint definitions as required by CSPAT-01/02.
</success_criteria>

<output>
After completion, create `.planning/phases/44-cube-spatial-context-setup/44-02-SUMMARY.md`
</output>
