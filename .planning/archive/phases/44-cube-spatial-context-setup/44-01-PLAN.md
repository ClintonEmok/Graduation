---
phase: 44-cube-spatial-context-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/useCubeSpatialConstraintsStore.ts
  - src/store/useCubeSpatialConstraintsStore.test.ts
  - src/app/cube-sandbox/lib/resetSandboxState.ts
  - src/app/cube-sandbox/lib/resetSandboxState.test.ts
autonomous: true
must_haves:
  truths:
    - Users can keep more than one cube spatial constraint definition in the sandbox session.
    - A stored constraint can be activated or deactivated without deleting its definition.
    - Sandbox reset preserves saved constraint definitions so users do not lose setup work mid-session.
  artifacts:
    - path: src/store/useCubeSpatialConstraintsStore.ts
      provides: Constraint domain model and CRUD/toggle/active actions for multi-constraint workflows.
    - path: src/store/useCubeSpatialConstraintsStore.test.ts
      provides: Regression tests for multi-constraint lifecycle and toggle persistence semantics.
    - path: src/app/cube-sandbox/lib/resetSandboxState.ts
      provides: Reset behavior that clears transient workflow state without erasing constraint definitions.
  key_links:
    - from: src/app/cube-sandbox/lib/resetSandboxState.ts
      to: src/store/useCubeSpatialConstraintsStore.ts
      via: reset entrypoint orchestration
      pattern: useCubeSpatialConstraintsStore
    - from: src/store/useCubeSpatialConstraintsStore.test.ts
      to: src/store/useCubeSpatialConstraintsStore.ts
      via: direct action assertions for create/toggle/activate semantics
      pattern: createConstraint|toggleConstraintEnabled|setActiveConstraint
---

<objective>
Create the spatial-constraint state foundation for cube sandbox workflows.

Purpose: Satisfy CSPAT-01/CSPAT-02/CSPAT-04 baseline behavior before UI wiring by establishing a durable multi-constraint model with deterministic reset semantics.
Output: New Zustand store + tests and reset orchestration updates that preserve definitions across sandbox interactions.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-3d-sandbox-route-foundation/43-01-SUMMARY.md
@.planning/phases/43-3d-sandbox-route-foundation/43-02-SUMMARY.md
@src/store/useFilterStore.ts
@src/store/useSliceStore.ts
@src/app/cube-sandbox/lib/resetSandboxState.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement cube spatial constraints store with multi-region model</name>
  <files>src/store/useCubeSpatialConstraintsStore.ts</files>
  <action>Create a dedicated Zustand store for cube spatial constraints with explicit types: constraint id, label, geometry payload (support at least axis-aligned cube bounds as the initial shape), enabled flag, created/updated timestamps, and optional visual token (color/chip). Implement actions to add, update, remove, enable/disable, and set one active constraint while preserving all definitions. Keep APIs deterministic and side-effect free so later proposal engines can consume the store directly (do not couple this store to fetch calls or route components).</action>
  <verify>npm run lint -- src/store/useCubeSpatialConstraintsStore.ts</verify>
  <done>Store can represent and mutate multiple named spatial constraint regions with independent enabled state and active selection.</done>
</task>

<task type="auto">
  <name>Task 2: Align sandbox reset behavior with constraint persistence expectations</name>
  <files>src/app/cube-sandbox/lib/resetSandboxState.ts, src/app/cube-sandbox/lib/resetSandboxState.test.ts</files>
  <action>Update reset orchestration so hard reset clears transient sandbox analysis state (filters/slices/warp choices) but does not erase saved spatial constraint definitions created in-session. If needed, clear only the active pointer while retaining definitions and enabled flags. Extend existing reset tests to assert that constraints remain available after reset and can be reactivated immediately.</action>
  <verify>npm run test -- src/app/cube-sandbox/lib/resetSandboxState.test.ts</verify>
  <done>Hard reset no longer destroys spatial constraint configuration work completed during the current sandbox session.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused unit tests for constraint lifecycle and toggles</name>
  <files>src/store/useCubeSpatialConstraintsStore.test.ts</files>
  <action>Add store-level tests covering: create multiple constraints, toggle enabled state without deletion, switch active constraint, remove one constraint while keeping others intact, and preserve definitions through reset entrypoints where applicable. Keep tests at store/orchestrator level (no React render harness) for fast regression checks.</action>
  <verify>npm run test -- src/store/useCubeSpatialConstraintsStore.test.ts</verify>
  <done>Automated tests protect CSPAT-01/02/04 core behaviors from regression before UI integration.</done>
</task>

</tasks>

<verification>
Run: `npm run test -- src/store/useCubeSpatialConstraintsStore.test.ts src/app/cube-sandbox/lib/resetSandboxState.test.ts`
Run: `npm run lint -- src/store/useCubeSpatialConstraintsStore.ts src/app/cube-sandbox/lib/resetSandboxState.ts`
</verification>

<success_criteria>
Spatial constraints have a stable multi-region state model with enable/disable semantics and are retained across sandbox reset interactions.
</success_criteria>

<output>
After completion, create `.planning/phases/44-cube-spatial-context-setup/44-01-SUMMARY.md`
</output>
