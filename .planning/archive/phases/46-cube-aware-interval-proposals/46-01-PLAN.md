---
phase: 46-cube-aware-interval-proposals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/cube-sandbox/lib/intervalProposalEngine.ts
  - src/store/useIntervalProposalStore.ts
  - src/app/cube-sandbox/lib/intervalProposalEngine.test.ts
autonomous: true
must_haves:
  truths:
    - System can generate interval proposals from temporal burst signals within enabled cube spatial constraints.
    - Generated interval proposals are deterministic for identical input context.
    - Proposal records include confidence and quality fields needed for downstream review UI.
  artifacts:
    - path: src/app/cube-sandbox/lib/intervalProposalEngine.ts
      provides: Deterministic interval scoring and ranking grouped by spatial constraint context.
    - path: src/store/useIntervalProposalStore.ts
      provides: Route-local interval proposal state with generate/select/clear actions.
    - path: src/app/cube-sandbox/lib/intervalProposalEngine.test.ts
      provides: Regression coverage for deterministic generation and confidence/quality output.
  key_links:
    - from: src/store/useIntervalProposalStore.ts
      to: src/app/cube-sandbox/lib/intervalProposalEngine.ts
      via: generate action delegates scoring/ranking to pure engine
      pattern: generateIntervalProposals
    - from: src/app/cube-sandbox/lib/intervalProposalEngine.ts
      to: src/store/useCubeSpatialConstraintsStore.ts
      via: enabled constraint inputs mapped into grouped proposal outputs
      pattern: constraintId|constraintLabel
---

<objective>
Build the interval proposal generation foundation for cube-aware slicing.

Purpose: Satisfy CINTV-01/CINTV-02 at the data layer by producing deterministic, spatial-context-linked interval candidates with confidence and quality metadata.
Output: Pure interval proposal engine, sandbox interval proposal store, and deterministic regression tests.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/45-cube-constrained-warp-proposals/45-01-SUMMARY.md
@.planning/phases/45-cube-constrained-warp-proposals/45-03-SUMMARY.md
@src/app/cube-sandbox/lib/warpProposalEngine.ts
@src/store/useWarpProposalStore.ts
@src/components/viz/BurstList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement deterministic cube-aware interval proposal engine</name>
  <files>src/app/cube-sandbox/lib/intervalProposalEngine.ts</files>
  <action>Create a pure generator that accepts enabled cube constraints plus temporal burst windows and returns a ranked proposal list grouped by spatial context. Include overlap suppression and deterministic tie-breaks so identical inputs return stable ordering. Each proposal must carry: interval range, linked constraint id/label, plain-language rationale summary, confidence band + numeric score, and quality metrics (at minimum density concentration and hotspot coverage).</action>
  <verify>npm run lint -- src/app/cube-sandbox/lib/intervalProposalEngine.ts</verify>
  <done>Engine returns deterministic, context-linked interval proposals with complete rationale/confidence fields needed by review UI.</done>
</task>

<task type="auto">
  <name>Task 2: Add interval proposal store orchestration for generate/select/clear</name>
  <files>src/store/useIntervalProposalStore.ts</files>
  <action>Create a dedicated zustand store mirroring existing sandbox proposal patterns: hold proposals, selection state, generation metadata (generatedAt + source constraint ids), and actions for generate/select/clear. Preserve stable selection when regenerating if prior selected id still exists. Keep store route-local and independent from global layout concerns.</action>
  <verify>npm run lint -- src/store/useIntervalProposalStore.ts</verify>
  <done>Sandbox has a deterministic state container for interval proposal generation and review selection.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for determinism and confidence payload shape</name>
  <files>src/app/cube-sandbox/lib/intervalProposalEngine.test.ts</files>
  <action>Add focused Vitest coverage for enabled-only constraint handling, deterministic ordering across repeated runs, overlap suppression behavior, and required confidence/quality/rationale fields. Include an empty-input case returning an empty list.</action>
  <verify>npm run test -- src/app/cube-sandbox/lib/intervalProposalEngine.test.ts</verify>
  <done>Test suite proves interval proposal generation is deterministic and emits required confidence + quality metadata.</done>
</task>

</tasks>

<verification>
Run: `npm run lint -- src/app/cube-sandbox/lib/intervalProposalEngine.ts src/store/useIntervalProposalStore.ts src/app/cube-sandbox/lib/intervalProposalEngine.test.ts`
Run: `npm run test -- src/app/cube-sandbox/lib/intervalProposalEngine.test.ts`
</verification>

<success_criteria>
Cube-aware interval proposal generation exists as a deterministic foundation with confidence/quality outputs, satisfying the non-UI core of CINTV-01 and CINTV-02.
</success_criteria>

<output>
After completion, create `.planning/phases/46-cube-aware-interval-proposals/46-01-SUMMARY.md`
</output>
