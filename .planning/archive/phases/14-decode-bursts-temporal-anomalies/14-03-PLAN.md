---
phase: 14-decode-bursts-temporal-anomalies
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
requirements:
  - T4
  - T8
files_modified:
  - src/components/dashboard-demo/DemoExplainPanel.tsx
  - src/app/dashboard-demo/page.shell.test.tsx
autonomous: true
must_haves:
  truths:
    - "The explain rail leads with detection reason for the active burst"
    - "The panel shows true duration plus a relative duration cue"
    - "Neutral windows are still explainable in the rail even when the track stays quiet"
  artifacts:
    - path: src/components/dashboard-demo/DemoExplainPanel.tsx
      provides: "Pinned burst-detail panel with taxonomy-first copy"
    - path: src/app/dashboard-demo/page.shell.test.tsx
      provides: "Source-inspection coverage for the burst-detail contract"
  key_links:
    - from: "DemoExplainPanel.tsx"
      to: "selectedBurstWindows[0]"
      via: "active burst lookup"
      pattern: "selectedBurstWindows\[0\]"
    - from: "DemoExplainPanel.tsx"
      to: "classifyBurstWindow(...)"
      via: "taxonomy derivation"
      pattern: "classifyBurstWindow"
---

<objective>
Turn the pinned explain rail into the single authoritative place for burst reasoning, duration recovery, and neutral-window wording.

Purpose: The phase needs the track to stay minimal while the sidebar explains why a burst matters and how long it really lasts.
Output: A burst-detail panel that shows reason-first copy, duration cues, and plain-language neutral fallback with regression coverage.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-decode-bursts-temporal-anomalies/14-CONTEXT.md
@.planning/phases/14-decode-bursts-temporal-anomalies/14-DISCUSSION-LOG.md
@src/components/dashboard-demo/DemoExplainPanel.tsx
@src/components/dashboard-demo/DashboardDemoRailTabs.tsx
@src/components/viz/BurstList.tsx
@src/lib/binning/burst-taxonomy.ts
@src/app/dashboard-demo/page.shell.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the explain panel as the burst detail surface</name>
  <files>src/components/dashboard-demo/DemoExplainPanel.tsx</files>
  <action>Derive the active burst from `selectedBurstWindows[0]`, match it against `useBurstWindows()`, classify it with `classifyBurstWindow(...)`, and render the panel in this order: detection reason first, taxonomy-first subtitle with a plain-language fallback (`no clear burst` / `balanced window`), true duration, relative duration cue against the current brush span, and the rationale block. Keep rationale text in the panel only and do not add any inline timeline labels.</action>
  <verify>Run the updated dashboard-demo shell test with `pnpm vitest src/app/dashboard-demo/page.shell.test.tsx`.</verify>
  <done>The explain rail tells the full burst story without depending on inline timeline annotations.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the reason-first and neutral-copy contract</name>
  <files>src/app/dashboard-demo/page.shell.test.tsx</files>
  <action>Extend the source-inspection test so it asserts the explain panel source contains the reason-first burst detail copy, true-duration and relative-duration wording, plain-language neutral phrasing, and the absence of neutral burst cues on the track.</action>
  <verify>`pnpm vitest src/app/dashboard-demo/page.shell.test.tsx` exits 0.</verify>
  <done>The pinned explain rail has a regression test that protects the burst-detail and neutral-copy contract.</done>
</task>

</tasks>

<verification>
`pnpm vitest src/app/dashboard-demo/page.shell.test.tsx`
</verification>

<success_criteria>
The explain rail is the authoritative burst detail surface, and neutral windows remain explainable without adding clutter to the timeline track.
</success_criteria>

<output>
After completion, create `.planning/phases/14-decode-bursts-temporal-anomalies/14-03-SUMMARY.md`
</output>
