---
phase: 14-decode-bursts-temporal-anomalies
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
requirements:
  - T6
  - T7
  - VIEW-05
  - VIEW-06
files_modified:
  - src/components/timeline/DualTimelineSurface.tsx
  - src/components/timeline/DualTimeline.tsx
  - src/components/timeline/DemoDualTimeline.tsx
  - src/components/timeline/DemoDualTimeline.test.tsx
autonomous: true
must_haves:
  truths:
    - "Non-neutral bursts appear as minimal timeline marks instead of text chips"
    - "Neutral bursts do not render on the track"
    - "Clicking a burst keeps the brush stationary and selects exactly one active burst"
  artifacts:
    - path: src/components/timeline/DualTimelineSurface.tsx
      provides: "Minimal burst lane with optional selection hit targets"
    - path: src/components/timeline/DualTimeline.tsx
      provides: "Shared taxonomy annotation and surface wiring"
    - path: src/components/timeline/DemoDualTimeline.tsx
      provides: "Demo burst click wiring into dashboard-demo state"
    - path: src/components/timeline/DemoDualTimeline.test.tsx
      provides: "Source-inspection regression for burst lane contract"
  key_links:
    - from: "DemoDualTimeline.tsx"
      to: "useDashboardDemoCoordinationStore.toggleBurstWindow"
      via: "burst click handler"
      pattern: "toggleBurstWindow"
    - from: "DualTimelineSurface.tsx"
      to: "classifyBurstWindow(...)"
      via: "taxonomy-annotated burst marks"
      pattern: "classifyBurstWindow"
---

<objective>
Make the timeline surface the quiet burst decoder: minimal markers, no neutral clutter, and a clickable active burst that does not disturb the brush.

Purpose: The phase needs the timeline itself to reveal burst order and pacing without turning into a verbose annotation track.
Output: Shared timeline components that render compact burst marks and a demo-only click path into the singleton burst state.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-decode-bursts-temporal-anomalies/14-CONTEXT.md
@.planning/phases/14-decode-bursts-temporal-anomalies/14-DISCUSSION-LOG.md
@src/components/timeline/DualTimelineSurface.tsx
@src/components/timeline/DualTimeline.tsx
@src/components/timeline/DemoDualTimeline.tsx
@src/lib/binning/burst-taxonomy.ts
@src/components/viz/BurstList.tsx
@src/store/useDashboardDemoCoordinationStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace burst chips with minimal marks</name>
  <files>src/components/timeline/DualTimelineSurface.tsx</files>
  <action>Refactor the burst section so it renders only non-neutral burst markers on the timeline track. Add optional props for an active burst id and a burst-click callback, use compact glyph/band styling instead of taxonomy chips, and suppress neutral entries entirely. Keep reasoning text out of the track; if hover labels are needed, make them title-only or panel-only.</action>
  <verify>Run `pnpm vitest src/components/timeline/DemoDualTimeline.test.tsx` after the surface contract is updated.</verify>
  <done>The shared timeline surface shows only quiet non-neutral burst marks and has no inline neutral or rationale chips left in the track.</done>
</task>

<task type="auto">
  <name>Task 2: Wire burst clicks through the demo timeline</name>
  <files>src/components/timeline/DualTimeline.tsx
src/components/timeline/DemoDualTimeline.tsx</files>
  <action>Build a taxonomy-annotated burst array in both timeline containers with `classifyBurstWindow(...)`, pass it into the updated surface, and wire the demo path so clicking a burst calls the dashboard-demo singleton burst-selection action without moving the brush window. Keep the stable `DualTimeline` path interaction-free but on the same rendering contract.</action>
  <verify>Run the new source-inspection regression test file with `pnpm vitest src/components/timeline/DemoDualTimeline.test.tsx`.</verify>
  <done>The demo timeline can select one active burst from the track while preserving the current brushed range.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the burst-lane source contract</name>
  <files>src/components/timeline/DemoDualTimeline.test.tsx</files>
  <action>Add a source-inspection test that reads `DualTimelineSurface.tsx`, `DualTimeline.tsx`, and `DemoDualTimeline.tsx` and asserts the new burst-lane contract: minimal non-neutral marks, optional click wiring, and no neutral summary chips or inline rationale blocks on the track.</action>
  <verify>`pnpm vitest src/components/timeline/DemoDualTimeline.test.tsx` exits 0.</verify>
  <done>The burst-lane contract is locked by a focused regression test.</done>
</task>

</tasks>

<verification>
`pnpm vitest src/components/timeline/DemoDualTimeline.test.tsx`
</verification>

<success_criteria>
The timeline shows compact burst marks only for non-neutral windows, and demo clicks select a single active burst without shifting the brush.
</success_criteria>

<output>
After completion, create `.planning/phases/14-decode-bursts-temporal-anomalies/14-02-SUMMARY.md`
</output>
