---
phase: 72-workflow-clarity
plan: 02
type: execute
wave: 1
depends_on: []
requirements: [FLOW-08]
files_modified:
  - src/components/dashboard-demo/DashboardDemoShell.tsx
  - src/components/dashboard-demo/DemoSlicePanel.tsx
  - src/components/dashboard-demo/DemoInspectPanel.tsx
autonomous: true
must_haves:
  truths:
    - "After generation, the workflow routes users to Slices to review and apply before they inspect results"
    - "The Slice panel clearly separates pending drafts from already applied slices"
    - "Inspect copy makes it clear that inspection happens after slices are applied"
  artifacts:
    - path: "src/components/dashboard-demo/DashboardDemoShell.tsx"
      provides: "Workflow handoff logic between generation, Slices, and Inspect"
    - path: "src/components/dashboard-demo/DemoSlicePanel.tsx"
      provides: "Review/apply surface for pending and manual slices"
    - path: "src/components/dashboard-demo/DemoInspectPanel.tsx"
      provides: "Post-apply inspection guidance and empty state copy"
  key_links:
    - from: "src/components/dashboard-demo/DashboardDemoShell.tsx"
      to: "src/components/dashboard-demo/DemoSlicePanel.tsx"
      via: "post-generation routing"
      pattern: "setActiveRailTab\('slices'\)"
    - from: "src/components/dashboard-demo/DemoSlicePanel.tsx"
      to: "src/components/dashboard-demo/DemoInspectPanel.tsx"
      via: "apply action handoff"
      pattern: "Apply|Inspect"
    - from: "src/components/dashboard-demo/DemoInspectPanel.tsx"
      to: "src/components/dashboard-demo/DemoSlicePanel.tsx"
      via: "empty-state guidance"
      pattern: "Generate and apply slices|Review and apply slices"
---

<objective>
Make Slices the obvious review/apply surface and make Inspect feel like the post-apply analysis step.

Purpose: The workflow should not jump straight from generation to inspection. Users need a clear place to review, merge, split, and apply slices first.
Output: Shell handoff logic that lands users in Slices, a slice panel that foregrounds review/apply, and Inspect copy that points back to the correct prior step.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/SLICES.md
@.planning/codebase/SLICE_MANAGER_UI.md
@src/components/dashboard-demo/DashboardDemoShell.tsx
@src/components/dashboard-demo/DemoSlicePanel.tsx
@src/components/dashboard-demo/DemoInspectPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route the showcase flow into Slices instead of skipping to Inspect</name>
  <files>src/components/dashboard-demo/DashboardDemoShell.tsx</files>
  <read_first>
    - src/components/dashboard-demo/DashboardDemoShell.tsx
    - src/components/dashboard-demo/DemoSlicePanel.tsx
    - src/components/dashboard-demo/DemoInspectPanel.tsx
    - src/store/useDashboardDemoTimeslicingModeStore.ts
  </read_first>
  <action>
    Change the shell handoff so generated drafts land in Slices for review instead of jumping directly to Inspect. Specifically:
    - keep the showcase helper generating burst drafts, but do not auto-apply them inside the showcase callback
    - switch the active rail tab to `slices` when drafts are ready
    - keep the 3D viewport inactive until the user applies a slice from Slices
    - preserve the existing auto-switch-to-Inspect behavior only after an actual apply event (`lastAppliedAt` / applied slice count), not after draft generation alone

    The result should be that Detect starts the work, Slices owns review/apply, and Inspect only becomes the next step after application.
  </action>
  <acceptance_criteria>
    - the shell contains `setActiveRailTab('slices')` in the post-generation path
    - the showcase flow no longer calls an automatic apply helper immediately after draft generation
    - the existing `inspect` auto-switch remains tied to applied slices rather than draft creation
  </acceptance_criteria>
  <verify>Run `pnpm lint src/components/dashboard-demo/DashboardDemoShell.tsx` and confirm no lint errors.</verify>
  <done>Generated drafts land in Slices first, and Inspect is reserved for after slices are applied.</done>
</task>

<task type="auto">
  <name>Task 2: Reframe the Slice panel as a review/apply surface</name>
  <files>src/components/dashboard-demo/DemoSlicePanel.tsx</files>
  <read_first>
    - src/components/dashboard-demo/DemoSlicePanel.tsx
    - src/store/useDashboardDemoTimeslicingModeStore.ts
    - src/store/useSliceDomainStore.ts
    - src/components/dashboard-demo/lib/demo-burst-generation.ts
  </read_first>
  <action>
    Rework the panel copy and section ordering so Slices is clearly the place to review and apply work from Detect.

    Keep the underlying actions intact, but make the structure explicit:
    - rename the card heading to `Review & apply slices`
    - put pending generated bins before applied slices
    - make `Apply` the primary button for pending drafts
    - keep `Merge`, `Split`, `Delete`, and `Clear all` as secondary or destructive actions
    - add short helper copy that says pending slices come here after Detect

    Do not remove the existing editing capabilities; this is a clarity pass, not a feature cut.
  </action>
  <acceptance_criteria>
    - the panel title communicates review/apply, not just companion/reference language
    - pending drafts are rendered before applied slices
    - the Apply action remains present and visually primary
  </acceptance_criteria>
  <verify>Run `pnpm lint src/components/dashboard-demo/DemoSlicePanel.tsx` and confirm the file is lint-clean.</verify>
  <done>The Slice panel reads as the workflow step for reviewing and applying drafts.</done>
</task>

<task type="auto">
  <name>Task 3: Make Inspect explicitly post-apply</name>
  <files>src/components/dashboard-demo/DemoInspectPanel.tsx</files>
  <read_first>
    - src/components/dashboard-demo/DemoInspectPanel.tsx
    - src/components/dashboard-demo/DemoSlicePanel.tsx
    - src/app/stkde-3d/components/SliceInspector.tsx
    - src/app/stkde-3d/components/SliceScrubber.tsx
  </read_first>
  <action>
    Update the header, description, and empty state so Inspect is clearly the step after slices have already been applied. Keep the playback, focus, and scrubber controls exactly as they are; only the framing copy should change.

    Make the empty state point users back to Slices for review/apply, and make the normal-state description explain that this panel is for inspecting already applied slices.
  </action>
  <acceptance_criteria>
    - the empty state tells users to review and apply slices in Slices first
    - the visible header/description says Inspect is for applied slices
    - playback and scrubber controls still exist unchanged
  </acceptance_criteria>
  <verify>Run `pnpm lint src/components/dashboard-demo/DemoInspectPanel.tsx` and confirm the file is lint-clean.</verify>
  <done>Inspect reads as the follow-on step after slice application, not a competing workflow surface.</done>
</task>

</tasks>

<verification>
1. `pnpm lint src/components/dashboard-demo/DashboardDemoShell.tsx src/components/dashboard-demo/DemoSlicePanel.tsx src/components/dashboard-demo/DemoInspectPanel.tsx`
2. Open `/dashboard-demo` and confirm that generated drafts route to Slices before Inspect
3. Confirm the Slice panel and Inspect panel copy describe the correct workflow order
</verification>

<success_criteria>
- Slices is the obvious review/apply surface for pending and manual slices
- Inspect is clearly the post-apply analysis surface
- The dashboard workflow reads as Detect → Slices → Inspect without extra explanation
</success_criteria>

<output>
After completion, create `.planning/phases/72-workflow-clarity/72-02-SUMMARY.md`
</output>
