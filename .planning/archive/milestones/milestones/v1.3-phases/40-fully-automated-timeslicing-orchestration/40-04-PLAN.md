---
phase: 40-fully-automated-timeslicing-orchestration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/full-auto-orchestrator.ts
  - src/types/autoProposalSet.ts
  - src/app/timeslicing/components/AutoProposalSetCard.tsx
  - src/app/timeslicing/components/SuggestionPanel.tsx
  - src/app/timeslicing/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Full-auto packages show only warp profiles (no boundaries)"
    - "Accepting a package creates only warp slices"
    - "Package scoring is simplified to warp-only"
  artifacts:
    - path: "src/lib/full-auto-orchestrator.ts"
      provides: "Simplified to generate warp-only packages"
    - path: "src/types/autoProposalSet.ts"
      provides: "Removed boundaries from package type"
    - path: "src/app/timeslicing/page.tsx"
      provides: "Accept creates warp slices only"
---

<objective>
Simplify full-auto to generate warp-only packages (no boundaries).

Purpose: Boundaries add complexity without clear value. Removing them simplifies user choice and reduces confusion.
Output: Full-auto packages show 3 warp profiles, accepting creates only warp slices.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@src/lib/full-auto-orchestrator.ts
@src/types/autoProposalSet.ts
@src/app/timeslicing/components/AutoProposalSetCard.tsx
@src/app/timeslicing/components/SuggestionPanel.tsx
@src/app/timeslicing/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Simplify orchestrator to warp-only packages</name>
  <files>src/lib/full-auto-orchestrator.ts</files>
  <action>
    Modify generateRankedAutoProposalSets to create warp-only packages:
    - Remove buildIntervalCandidates() call and interval detection
    - Each package now contains just ONE warp profile (not combined with boundaries)
    - Score based on warp quality alone (remove boundary-related scoring)
    - Package ID format changes from "aggressive:peak" to just "aggressive"
    - Return 3 packages: aggressive, balanced, conservative warp profiles
  </action>
  <verify>Review generated packages contain only warp profiles, no boundaries</verify>
  <done>Orchestrator generates 3 warp-only packages</done>
</task>

<task type="auto">
  <name>Update types to remove boundaries</name>
  <files>src/types/autoProposalSet.ts</files>
  <action>
    Simplify AutoProposalSet type:
    - Remove intervals/boundaries field
    - Package is now just a warp profile + score
    - Update AutoProposalScoreBreakdown to warp-only dimensions
  </action>
  <verify>Types compile without errors</verify>
  <done>Package type reflects warp-only structure</done>
</task>

<task type="auto">
  <name>Update package card UI</name>
  <files>src/app/timeslicing/components/AutoProposalSetCard.tsx</files>
  <action>
    Simplify card display:
    - Remove boundaries/intervals section
    - Show just: warp emphasis, confidence, score breakdown (warp-only)
    - Update expand/collapse to show warp details only
  </action>
  <verify>Card shows warp info only, no boundary references</verify>
  <done>Package cards display warp-only information</done>
</task>

<task type="auto">
  <name>Simplify accept flow</name>
  <files>src/app/timeslicing/page.tsx</files>
  <action>
    Simplify handleAcceptFullAutoPackage:
    - Remove handleAcceptIntervalBoundary call
    - Only create warp slices from selected package
    - Package already contains the full warp profile to apply
  </action>
  <verify>Accepting package creates only warp slices</verify>
  <done>Package acceptance creates warp slices only</done>
</task>

</tasks>

<verification>
- Full-auto packages show 3 warp profiles (aggressive, balanced, conservative)
- No boundaries section in UI
- Accepting creates only warp slices
- Scoring reflects warp quality only
</verification>

<success_criteria>
- Full-auto simplified to warp-only
- User picks from 3 warp profiles
- Clear, simple choice
</success_criteria>

<output>
After completion, create `.planning/phases/40-fully-automated-timeslicing-orchestration/40-04-SUMMARY.md`
</output>
