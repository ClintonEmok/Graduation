---
phase: 39-timeline-ux-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/timeline/DualTimeline.tsx
  - src/components/timeline/DensityHeatStrip.tsx
  - src/store/useWarpSliceStore.ts
autonomous: true

must_haves:
  truths:
    - "User can see visual overlay indicating which time periods are affected by user-defined warp slices"
    - "User can see a clear badge/indicator showing current time scale mode (Linear vs Adaptive)"
    - "Density heat strip shows legend explaining color scale"
  artifacts:
    - path: "src/components/timeline/DualTimeline.tsx"
      provides: "Warp overlay rendering, mode indicator badge"
    - path: "src/components/timeline/DensityHeatStrip.tsx"
      provides: "Density legend component"
  key_links:
    - from: "DualTimeline.tsx"
      to: "useWarpSliceStore"
      via: "userWarpSlices"
---

<objective>
Add visual overlays for warp slices and mode indicators to timeline.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/components/timeline/DualTimeline.tsx
@src/components/timeline/DensityHeatStrip.tsx
@src/store/useWarpSliceStore.ts
</context>

<tasks>

<task type="auto">
  <name>Add warp slice overlay to timeline</name>
  <files>src/components/timeline/DualTimeline.tsx</files>
  <action>
    In DualTimeline.tsx, import useWarpSliceStore and render semi-transparent overlay bands on the overview timeline for each user-defined warp slice. The overlay should:
    - Show on both overview and detail timelines
    - Use a subtle purple/indigo tint (e.g., rgba(139, 92, 246, 0.15))
    - Have dashed border to distinguish from data-driven warp
    - Only show for slices where isUserWarp === true in useWarpSliceStore
    - Update reactively when warp slices change
  </action>
  <verify>Run dev server, navigate to /timeline-test, toggle adaptive mode, verify overlay bands appear on warp slice time ranges</verify>
  <done>User-defined warp slices show visual overlay bands on timeline</done>
</task>

<task type="auto">
  <name>Add mode indicator badge</name>
  <files>src/components/timeline/DualTimeline.tsx</files>
  <action>
    Add a small pill-shaped badge above the timeline showing "Linear" or "Adaptive" mode. Style with:
    - Background: adaptive = indigo, linear = gray
    - Text: white, small font
    - Position: top-right of timeline container
    - Read timeScaleMode from useTimeStore
  </action>
  <verify>Toggle time scale mode in controls, verify badge updates from "Linear" to "Adaptive"</verify>
  <done>Mode indicator badge visible and updates with time scale mode</done>
</task>

<task type="auto">
  <name>Add density legend to heat strip</name>
  <files>src/components/timeline/DensityHeatStrip.tsx</files>
  <action>
    Add a small legend component below or beside the density heat strip showing:
    - Gradient bar from blue (low) to red (high)
    - Labels: "Low" on left, "High" on right
    - Use same color constants as DENSITY_COLOR_LOW/HIGH from DualTimeline
    - Make it compact (e.g., 80px wide, 8px tall gradient)
  </action>
  <verify>Navigate to timeline, verify legend appears below density heat strip with color gradient</verify>
  <done>Density legend shows color scale explanation</done>
</task>

</tasks>

<verification>
- Warp slice overlays render on timeline for user-defined warp slices
- Mode badge shows current time scale state
- Density legend visible with color gradient
</verification>

<success_criteria>
- User can identify which time periods are affected by user warp
- User can identify current time scale mode at a glance
- User understands density color scale
</success_criteria>

<output>
After completion, create .planning/phases/39-timeline-ux-improvements/39-01-SUMMARY.md
</output>
