---
phase: 58-enrich-the-with-neighbourhood-data-poi-events-that-happened-on-the-day-anything-useful
plan: 03
type: execute
wave: 3
depends_on:
  - "58-02"
files_modified:
  - src/app/timeslicing-algos/lib/NeighbourhoodDiagnosticsPanel.tsx
  - src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx
autonomous: true

must_haves:
  truths:
    - "Neighbourhood diagnostics panel is visible in /timeslicing-algos"
    - "Panel shows compact summary by default with expandable details"
    - "Panel handles missing/unavailable neighbourhood gracefully"
    - "Panel styling matches existing diagnostics panels"
  artifacts:
    - path: "src/app/timeslicing-algos/lib/NeighbourhoodDiagnosticsPanel.tsx"
      provides: "Neighbourhood diagnostics UI component"
      min_lines: 50
    - path: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      provides: "Route shell extended to include neighbourhood panel"
  key_links:
    - from: "src/app/timeslicing-algos/lib/NeighbourhoodDiagnosticsPanel.tsx"
      to: "src/store/useSuggestionStore.ts"
      via: "useSuggestionStore hook"
      pattern: "useSuggestionStore\\("
    - from: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      to: "src/app/timeslicing-algos/lib/NeighbourhoodDiagnosticsPanel.tsx"
      via: "import and render"
      pattern: "NeighbourhoodDiagnosticsPanel"
---

<objective>
Add the neighbourhood diagnostics panel to `/timeslicing-algos` following the Phase 57 compact summary + expandable details pattern.

Purpose: Surfaces neighbourhood context in the diagnostics UI alongside temporal/spatial diagnostics.
Output: NeighbourhoodDiagnosticsPanel component integrated into route shell
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/timeslicing-algos/lib/AdaptiveBinDiagnosticsPanel.tsx
@src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx
@src/app/timeslicing-algos/lib/TimeslicingAlgosStrategyStats.tsx
@src/store/useSuggestionStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NeighbourhoodDiagnosticsPanel component</name>
  <files>src/app/timeslicing-algos/lib/NeighbourhoodDiagnosticsPanel.tsx</files>
  <action>
    Create src/app/timeslicing-algos/lib/NeighbourhoodDiagnosticsPanel.tsx:

    1. Import React, useState from 'react'
    2. Import useSuggestionStore from '@/store/useSuggestionStore'
    3. Import Lucide icons: MapPin, ChevronDown, ChevronUp, AlertCircle

    4. Define interface for neighbourhood data from store:
       ```typescript
       interface NeighbourhoodSection {
         status: 'available' | 'missing';
         notice?: string;
       }
       
       interface NeighbourhoodDiagnostics {
         neighbourhoodSummary?: string;
         sections: {
           neighbourhood: NeighbourhoodSection;
         };
       }
       ```

    5. Create component with:
       - Get active suggestion from useSuggestionStore
       - Extract neighbourhood diagnostics from activeSuggestion?.contextMetadata?.contextDiagnostics
       - State for expanded details (default collapsed)
       - Toggle function for expanded state

    6. Render logic:
       - Container: rounded-md border border-slate-700/70 bg-slate-900/60 p-3
       - Header row with:
         - MapPin icon (lucide)
         - "Neighbourhood Context" label
         - Expand/collapse chevron button
       - Compact summary (shown when collapsed):
         - For available: Show neighbourhoodSummary text
         - For missing: Show notice text in muted color
       - Expanded details (when toggled):
         - Parse and display POI category breakdown if available
         - Show notice if unavailable
       - Loading/empty state:
         - When no active suggestion: Show "Generate suggestions to see neighbourhood context"
         - When neighbourhood not in diagnostics: Show "Neighbourhood data not available"

    7. Style with Tailwind classes matching existing panels:
       - Text sizes: text-[11px] for labels, text-xs for content
       - Colors: text-slate-400 for labels, text-slate-300 for content, text-slate-500 for muted
       - Spacing: space-y-2 for vertical gaps

    Follow the exact styling pattern from AdaptiveBinDiagnosticsPanel.tsx for consistency.
  </action>
  <verify>`npx tsc --noEmit src/app/timeslicing-algos/lib/NeighbourhoodDiagnosticsPanel.tsx` passes</verify>
  <done>NeighbourhoodDiagnosticsPanel component exists with compact/expandable UI pattern</done>
</task>

<task type="auto">
  <name>Task 2: Integrate neighbourhood panel into route shell</name>
  <files>src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx</files>
  <action>
    Modify src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx:

    1. Add import for NeighbourhoodDiagnosticsPanel:
       ```typescript
       import { NeighbourhoodDiagnosticsPanel } from './NeighbourhoodDiagnosticsPanel';
       ```

    2. Find the location where diagnostics panels are rendered in the route shell

    3. Add NeighbourhoodDiagnosticsPanel alongside existing panels:
       - Place it after AdaptiveBinDiagnosticsPanel or in the diagnostics section
       - Keep the visual hierarchy: temporal, spatial, neighbourhood (related concepts grouped)

    4. Ensure proper spacing between panels (matching existing layout)

    The exact placement depends on the existing component structure. Common patterns:
    - If there's a diagnostics container div, add NeighbourhoodDiagnosticsPanel as a sibling
    - If there's a grid layout, add to the appropriate column

    Don't change existing styling - just add the new panel component.
  </action>
  <verify>`npx tsc --noEmit src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx` passes</verify>
  <done>TimeslicingAlgosRouteShell renders NeighbourhoodDiagnosticsPanel in the diagnostics section</done>
</task>

<task type="auto">
  <name>Task 3: Verify integration with human checkpoint</name>
  <files>src/app/timeslicing-algos/lib/NeighbourhoodDiagnosticsPanel.tsx</files>
  <action>
    This task is a verification checkpoint to ensure the neighbourhood panel integration works correctly in the running application.

    After implementing the above tasks:
    1. Start the dev server if not running
    2. Navigate to /timeslicing-algos
    3. Generate some suggestions (auto or manual)
    4. Verify:
       - Neighbourhood Context panel appears in the diagnostics section
       - Panel shows "MapPin" icon and "Neighbourhood Context" label
       - For available data: Shows POI summary (e.g., "X restaurants, Y parks, Z shops...")
       - For missing data: Shows appropriate notice
       - Panel can be expanded/collapsed
       - No console errors related to neighbourhood data
  </action>
  <verify>Human verifies in browser at /timeslicing-algos</verify>
  <done>Neighbourhood diagnostics panel visible with correct content in /timeslicing-algos route</done>
</task>

</tasks>

<verification>
- Component renders without TypeScript errors
- Panel appears in /timeslicing-algos route after suggestions are generated
- Compact/expandable toggle works
- Missing state displays gracefully
</verification>

<success_criteria>
- NeighbourhoodDiagnosticsPanel.tsx exists with proper styling
- Panel integrated into TimeslicingAlgosRouteShell
- Panel displays neighbourhood data when available
- Panel handles missing/unavailable gracefully
- Human verification confirms visual integration
</success_criteria>

<output>
After completion, create `.planning/phases/58-enrich-the-with-neighbourhood-data-poi-events-that-happened-on-the-day-anything-useful/58-03-SUMMARY.md`
</output>
