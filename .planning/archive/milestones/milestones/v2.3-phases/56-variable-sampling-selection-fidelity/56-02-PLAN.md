---
phase: 56-variable-sampling-selection-fidelity
plan: 2
type: execute
wave: 2
depends_on: [56-01]
files_modified:
  - src/hooks/useCrimeData.ts
  - src/hooks/useCrimeData.test.ts
  - src/app/timeslicing-algos/lib/selection-detail-dataset.ts
  - src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx
  - src/app/timeslicing-algos/page.timeline-algos.test.ts
autonomous: true

must_haves:
  truths:
    - "`/timeslicing-algos` can request high-fidelity selection-detail data from `/api/crimes/range` via explicit sampling intent instead of only static limit-driven stride"
    - "Selection provenance chips remain explicit (selection population, stride/sampled status, render downsampling)"
    - "Route behavior falls back safely when selection-detail requests fail or are capped, without breaking timeline interaction"
  artifacts:
    - path: "src/hooks/useCrimeData.ts"
      provides: "Hook-level support for forwarding sampling intent/target points to range API"
      contains: "samplingIntent"
    - path: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      provides: "Selection-detail fetch wiring using new API semantics"
      contains: "targetPoints"
    - path: "src/app/timeslicing-algos/page.timeline-algos.test.ts"
      provides: "Regression coverage for selection-detail route integration and explicit provenance labels"
      contains: "Selection detail"
  key_links:
    - from: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      to: "src/hooks/useCrimeData.ts"
      via: "selection request passes samplingIntent=selection-detail and optional targetPoints"
      pattern: "samplingIntent:\s*'selection-detail'"
    - from: "src/hooks/useCrimeData.ts"
      to: "/api/crimes/range"
      via: "query params include variable-sampling request semantics"
      pattern: "targetPoints"
---

<objective>
Integrate the new variable-sampling API semantics into `/timeslicing-algos` selection-detail requests while preserving explicit provenance and safe fallback behavior.

Purpose: Backend policy support is only useful if high-fidelity route calls actually send intent/target semantics and preserve clear QA-visible provenance.
Output: Hook + route integration with regression tests proving selection-detail fidelity requests and explicit labels.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-09-SUMMARY.md
@.planning/phases/56-variable-sampling-selection-fidelity/56-01-SUMMARY.md
@src/hooks/useCrimeData.ts
@src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx
@src/app/timeslicing-algos/lib/selection-detail-dataset.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend `useCrimeData` options to forward sampling intent semantics</name>
  <files>src/hooks/useCrimeData.ts, src/hooks/useCrimeData.test.ts</files>
  <action>Add optional hook options for variable sampling (`samplingIntent`, `targetPoints`) and include them in range request query params only when present. Preserve current query key behavior by including new fields so cache entries remain deterministic. Keep defaults unchanged for all existing callers. Add tests confirming URL construction forwards the new semantics while legacy requests remain identical.</action>
  <verify>`npm test -- --run src/hooks/useCrimeData.test.ts` passes with both legacy and intent-aware request cases.</verify>
  <done>Hook can express variable sampling requests safely without breaking existing call sites.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire selection-detail fetch in `/timeslicing-algos` to use intent-aware API contract</name>
  <files>src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx, src/app/timeslicing-algos/lib/selection-detail-dataset.ts</files>
  <action>Update the selection-detail `useCrimeData` call to pass `samplingIntent: 'selection-detail'` and an explicit `targetPoints` tuned for high-fidelity selections (bounded by backend policy). Keep context fetch path unchanged (`bufferDays: 30`, context semantics). Update selection dataset normalization only as needed to read and surface any new provenance metadata fields from API `meta.sampling` without removing existing chips (`returned/total`, sampled/stride, render downsampling, fallback reason). Do not modify dashboard route code.</action>
  <verify>`npm run typecheck` passes and code assertions show selection fetch includes `samplingIntent: 'selection-detail'` while context fetch remains unchanged.</verify>
  <done>`/timeslicing-algos` selection-detail path requests high-fidelity policy explicitly and keeps provenance labeling clear.</done>
</task>

<task type="auto">
  <name>Task 3: Add route regressions for intent wiring, provenance visibility, and fallback safety</name>
  <files>src/app/timeslicing-algos/page.timeline-algos.test.ts</files>
  <action>Extend route tests to assert: (1) dual-fetch remains (context + selection detail); (2) selection fetch forwards intent-aware semantics; (3) provenance chips remain explicit for population/stride/render downsampling; and (4) when selection detail fails, fallback messaging remains visible and timeline rendering still functions. Keep tests route-scoped to `/timeslicing-algos` and avoid dashboard expectations.</action>
  <verify>`npm test -- --run src/app/timeslicing-algos/page.timeline-algos.test.ts` passes.</verify>
  <done>Selection-detail integration is regression-protected and provenance clarity remains guaranteed.</done>
</task>

</tasks>

<verification>
- `npm test -- --run src/hooks/useCrimeData.test.ts src/app/timeslicing-algos/page.timeline-algos.test.ts`
- `npm run typecheck`
- Manual QA:
  1. Open `/timeslicing-algos` and create a dense selection range.
  2. Confirm selection fetch uses intent-aware semantics and selection provenance chips still show explicit population/stride/downsampling.
  3. Simulate selection request failure and confirm context fallback label is visible while timeline remains usable.
</verification>

<success_criteria>
- `/timeslicing-algos` selection detail requests use `samplingIntent=selection-detail` (and target points where configured).
- Existing context fetch behavior remains stable.
- Provenance fields/chips stay explicit and regression-tested.
- Dashboard implementation remains out of scope and unmodified.
</success_criteria>

<risks>
- Query-key expansion could trigger unexpected refetch volume if not deterministic.
- Selection target tuning may still under/over-shoot fidelity goals before perf baselines are checked.
- UI label drift risk if provenance normalization is split between helpers.
</risks>

<rollback>
- Revert route call to legacy high-limit-only selection request and keep backend contract available for later retry.
- Disable forwarding of `targetPoints` while retaining `samplingIntent` if performance pressure appears.
- If needed, pin diagnostics/detail sourcing back to context while preserving fallback labels.
</rollback>

<output>
After completion, create `.planning/phases/56-variable-sampling-selection-fidelity/56-02-SUMMARY.md`
</output>
