---
phase: 59-add-stats-page
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/stats/page.tsx
  - src/app/stats/lib/StatsRouteShell.tsx
  - src/app/stats/lib/stats-query-state.ts
  - src/app/stats/lib/stats-view-model.ts
  - src/app/stats/hooks/useNeighborhoodStats.ts
  - src/app/stats/lib/components/NeighborhoodSelector.tsx
  - src/store/useStatsStore.ts
  - src/lib/stats/aggregation.ts
  - src/lib/stats/aggregation.test.ts
autonomous: true

must_haves:
  truths:
    - "User can navigate to /stats and see the stats dashboard"
    - "User can select one or more districts to filter crime data"
    - "Stats page loads independently from dashboard without shared state"
    - "Time range selector works and updates data fetch"
    - "Initial load completes in under 3 seconds"
  artifacts:
    - path: "src/app/stats/page.tsx"
      provides: "Dedicated stats route entrypoint"
      contains: "StatsRouteShell"
    - path: "src/app/stats/lib/StatsRouteShell.tsx"
      provides: "Main stats page shell with layout"
      contains: "NeighborhoodSelector"
    - path: "src/app/stats/lib/components/NeighborhoodSelector.tsx"
      provides: "District multi-select with crime count badges"
      contains: "district"
    - path: "src/store/useStatsStore.ts"
      provides: "Route-local state for stats page"
      contains: "selectedDistricts"
    - path: "src/lib/stats/aggregation.ts"
      provides: "Crime data aggregation helpers"
      contains: "aggregateByDistrict|aggregateByType"
  key_links:
    - from: "src/app/stats/lib/StatsRouteShell.tsx"
      to: "src/app/stats/hooks/useNeighborhoodStats.ts"
      via: "hook fetches filtered crime data"
      pattern: "useNeighborhoodStats"
    - from: "src/app/stats/lib/StatsRouteShell.tsx"
      to: "src/store/useStatsStore.ts"
      via: "store selector for district filter"
      pattern: "useStatsStore"
    - from: "src/app/stats/hooks/useNeighborhoodStats.ts"
      to: "src/hooks/useCrimeData.ts"
      via: "uses existing crime data hook"
      pattern: "useCrimeData"
---

<objective>
Create the stats route foundation with neighborhood selection, data aggregation layer, and base layout. This establishes the standalone `/stats` route structure.

Purpose: Enable isolated stats exploration without coupling to dashboard state. Provides the shell and data plumbing for subsequent visualization plans.
Output: `/stats` route shell, district selector, aggregation helpers, and route-local state management.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-add-stats-page/59-CONTEXT.md
@.planning/phases/59-add-stats-page/59-RESEARCH.md
@src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx
@src/app/stkde/lib/StkdeRouteShell.tsx
@src/hooks/useCrimeData.ts
@src/types/crime.ts
@src/lib/category-maps.ts
</context>

<architecture>
- **Route isolation:** New `src/app/stats/*` route with no imports from suggestion/full-auto workflows
- **State ownership:** `useStatsStore` owns route-local selected districts and time range
- **Data flow:** `useNeighborhoodStats` hook → `useCrimeData` → aggregation helpers → view model → components
- **No dashboard coupling:** Stats page has its own time range selector independent of main dashboard
</architecture>

<tasks>

<task type="auto">
  <name>Task 1: Create stats route shell and page entry</name>
  <files>src/app/stats/page.tsx, src/app/stats/lib/StatsRouteShell.tsx</files>
  <action>
Create a standalone `/stats` route following the shell pattern from `TimeslicingAlgosRouteShell.tsx`:
1. Create `src/app/stats/page.tsx` as the route entry that renders `StatsRouteShell`
2. Create `src/app/stats/lib/StatsRouteShell.tsx` as the main shell component with:
   - `"use client"` directive
   - Time range selector (startEpoch/endEpoch) independent of dashboard
   - NeighborhoodSelector component placeholder
   - Stats overview cards placeholder
   - Layout using flexbox with dark theme (bg-black text-white)
3. Use Next.js hooks: usePathname, useRouter, useSearchParams for URL state
4. Do NOT import from suggestion/full-auto workflows
5. Do NOT share state with useFilterStore or useTimelineDataStore (use separate useStatsStore)
  </action>
  <read_first>
src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx
src/app/dashboard/page.tsx
src/app/stkde/lib/StkdeRouteShell.tsx
  </read_first>
  <verify>`npm run typecheck` succeeds</verify>
  <done>/stats route exists and renders a shell with time range selector and placeholder layout</done>
  <acceptance_criteria>
- Route at /stats renders without errors
- TypeScript compiles without errors
- Shell includes time range selector (two date inputs or epoch inputs)
- Dark theme applied (bg-black text-white)
</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create route-local stats state and aggregation helpers</name>
  <files>src/store/useStatsStore.ts, src/lib/stats/aggregation.ts, src/lib/stats/aggregation.test.ts</files>
  <action>
Create route-local state management and data aggregation utilities:
1. Create `src/store/useStatsStore.ts` with Zustand store containing:
   - `selectedDistricts: string[]` - array of selected district codes
   - `timeRange: { startEpoch: number; endEpoch: number }`
   - Actions: `setSelectedDistricts`, `setTimeRange`, `clearFilters`
   - Use `create()` pattern matching existing stores like useStkdeStore

2. Create `src/lib/stats/aggregation.ts` with pure aggregation functions:
   - `aggregateByDistrict(crimes: CrimeRecord[]): Map<string, number>` - count crimes per district
   - `aggregateByType(crimes: CrimeRecord[]): Map<string, number>` - count crimes per type
   - `aggregateByHour(crimes: CrimeRecord[]): number[]` - count per hour (0-23)
   - `aggregateByDayOfWeek(crimes: CrimeRecord[]): number[]` - count per day (0-6)
   - `aggregateByMonth(crimes: CrimeRecord[]): number[]` - count per month (0-11)
   - All functions must be pure (no side effects) and deterministic
   - Handle missing/null district and type fields gracefully

3. Create `src/lib/stats/aggregation.test.ts` with tests covering:
   - Empty array returns empty map/array of zeros
   - Single district/type aggregation
   - Multiple districts/types aggregation
   - Missing fields handled as 'Unknown'
   - Deterministic ordering (same input = same output)
  </action>
  <read_first>
src/store/useStkdeStore.ts
src/types/crime.ts
src/lib/context-diagnostics/temporal.ts
src/lib/category-maps.ts
  </read_first>
  <verify>`npm test -- --run src/lib/stats/aggregation.test.ts` passes</verify>
  <done>Stats state management and pure aggregation helpers exist and are tested</done>
  <acceptance_criteria>
- useStatsStore exports: selectedDistricts, timeRange, setSelectedDistricts, setTimeRange, clearFilters
- aggregateByDistrict({THEFT: 100, BATTERY: 50}) for crimes in districts ['001', '001', '002']
- aggregateByType returns Map with sorted entries by count descending
- aggregateByHour returns array[24] with counts per hour
- aggregateByDayOfWeek returns array[7] with counts per day (0=Sunday)
- All tests pass
</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create useNeighborhoodStats hook and NeighborhoodSelector component</name>
  <files>src/app/stats/hooks/useNeighborhoodStats.ts, src/app/stats/lib/components/NeighborhoodSelector.tsx, src/lib/stats/aggregation.ts</files>
  <action>
Create the data fetching hook and neighborhood selector UI:
1. Create `src/app/stats/hooks/useNeighborhoodStats.ts`:
   - Subscribe to useStatsStore for selectedDistricts and timeRange
   - Call useCrimeData with district filter parameter
   - Apply aggregation functions from aggregation.ts
   - Return typed stats: { byDistrict, byType, byHour, byDayOfWeek, total, isLoading, error }
   - Use `samplingIntent='context'` for larger datasets (up to 100k points)

2. Create `src/app/stats/lib/components/NeighborhoodSelector.tsx`:
   - Multi-select UI for districts 1-25
   - Use Radix Select or Checkbox components
   - Show crime count badge next to each district
   - "All Districts" toggle that selects/deselects all
   - Selected districts shown as removable chips
   - Compact layout suitable for sidebar or top bar

3. Update StatsRouteShell to include NeighborhoodSelector and wire up the hook
4. Display summary stats: total crimes, selected district count, time range
  </action>
  <read_first>
src/hooks/useCrimeData.ts
src/app/stkde/lib/HotspotPanel.tsx
src/lib/category-maps.ts
  </read_first>
  <verify>`npm run typecheck` succeeds; manual check confirms selector renders districts 1-25</verify>
  <done>Neighborhood selector works and data hook returns aggregated stats</done>
  <acceptance_criteria>
- useNeighborhoodStats hook returns: { byDistrict, byType, byHour, byDayOfWeek, total, isLoading, error }
- NeighborhoodSelector shows all 25 districts with checkboxes
- "All Districts" toggle works correctly
- Selected districts shown as removable chips
- Loading state shown while data fetches
- Error state shown if fetch fails
</acceptance_criteria>
</task>

</tasks>

<verification>
- `npm run typecheck`
- `npm test -- --run src/lib/stats/aggregation.test.ts`
- Manual route check:
  1. Navigate to /stats, confirm page loads
  2. Verify district selector shows all 25 districts
  3. Select "All Districts", confirm data loads
  4. Select specific districts, confirm filter updates
  5. Change time range, confirm data reloads
</verification>

<success_criteria>
- `/stats` route renders with functional layout
- District selector works with multi-select and badges
- Stats aggregation hook returns typed data
- No imports from dashboard or suggestion workflows
- TypeScript compiles without errors
</success_criteria>

<risks>
- **Performance risk:** Large datasets may slow initial load
  - Mitigation: Use sampling for preview, show loading skeleton
- **State sync risk:** Stats store might conflict with dashboard
  - Mitigation: Use completely isolated store, no shared state
</risks>

<rollback>
- Delete entire `src/app/stats/` directory to remove stats route
- Stats page is isolated, removal has no side effects
</rollback>

<output>
After completion, create `.planning/phases/59-add-stats-page/59-01-SUMMARY.md`
</output>
