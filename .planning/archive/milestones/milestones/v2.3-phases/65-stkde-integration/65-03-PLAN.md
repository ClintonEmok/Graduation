---
phase: 65-stkde-integration
plan: 03
type: execute
wave: 3
depends_on: [65-02]
files_modified:
  - src/app/dashboard-v2/page.tsx
  - src/components/viz/CubeVisualization.tsx
  - src/store/useStkdeStore.test.ts
  - src/app/dashboard-v2/page.stkde.test.ts
  - src/components/viz/CubeVisualization.stkde.test.ts
autonomous: true
requirements: [STKD-01, STKD-02, STKD-04, STKD-05]
requirements_addressed: [STKD-01, STKD-02, STKD-04, STKD-05]

must_haves:
  truths:
    - "Hotspot click commits global investigation focus while hover remains preview-only"
    - "Cube surface exposes STKDE-informed context for the selected hotspot"
    - "STKDE integration remains regression-tested in dashboard-v2"
  artifacts:
    - path: "src/app/dashboard-v2/page.tsx"
      provides: "Hotspot commit wiring into global filter/coordination state"
      contains: "setTimeRange/setSpatialBounds on hotspot click and preview-only hover behavior"
    - path: "src/components/viz/CubeVisualization.tsx"
      provides: "Cube STKDE context card"
      contains: "hotspot id/intensity/support/time window/provenance indicator"
    - path: "src/app/dashboard-v2/page.stkde.test.ts"
      provides: "dashboard STKDE wiring regression coverage"
      contains: "mode toggle + hotspot commit + stale warning assertions"
  key_links:
    - from: "src/app/dashboard-v2/page.tsx"
      to: "src/store/useFilterStore.ts"
      via: "hotspot click commits time/spatial filters"
      pattern: "setTimeRange|setSpatialBounds"
    - from: "src/components/viz/CubeVisualization.tsx"
      to: "src/store/useStkdeStore.ts"
      via: "selected hotspot run context read"
      pattern: "useStkdeStore"
---

<objective>
Complete cross-view STKDE focus behavior and make STKDE context visible in cube analysis.

Purpose: Satisfy Phase 65’s cross-view requirement by making hotspot outcomes actionable across map/timeline/cube, not only in map panel.

Output: Committed hotspot-to-focus wiring, cube STKDE context card, and regression tests.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-stkde-integration/65-CONTEXT.md
@.planning/phases/65-stkde-integration/65-RESEARCH.md
@src/app/dashboard-v2/page.tsx
@src/components/viz/CubeVisualization.tsx
@src/store/useFilterStore.ts
@src/store/useCoordinationStore.ts
@src/store/useStkdeStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit hotspot click into global focus while keeping hover preview-only</name>
  <files>src/app/dashboard-v2/page.tsx</files>
  <read_first>
  - src/app/dashboard-v2/page.tsx
  - src/store/useStkdeStore.ts
  - src/store/useFilterStore.ts
  - src/store/useCoordinationStore.ts
  - .planning/phases/65-stkde-integration/65-CONTEXT.md
  </read_first>
  <action>
  Implement explicit hotspot interaction semantics in dashboard-v2:

  1. On hotspot hover:
     - update STKDE hovered hotspot state only.
     - do not call global filter setters and do not call coordination commit actions.

  2. On hotspot click:
     - set STKDE selected hotspot id.
     - set global time range using `useFilterStore.setTimeRange([peakStartEpochSec, peakEndEpochSec])`.
     - set global spatial bounds using hotspot centroid/radius converted into min/max lat/lon + projected x/z bounds.
     - call coordination commit path with source `map` so status transitions remain consistent with last-interaction-wins semantics.

  3. If hotspot time window does not align with active applied slices:
     - preserve applied slices.
     - set partial sync status reason containing literal phrase `investigative overlay`.

  4. Keep existing timeline/map/cube interactions allowed to override selection afterward (no sticky lock).
  </action>
  <acceptance_criteria>
  - `src/app/dashboard-v2/page.tsx` contains hotspot click wiring that calls `setTimeRange(` and `setSpatialBounds(`.
  - `src/app/dashboard-v2/page.tsx` keeps hover path separate and does not call `setTimeRange` from hover handlers.
  - `src/app/dashboard-v2/page.tsx` contains mismatch reason copy with `investigative overlay`.
  - `src/app/dashboard-v2/page.tsx` uses coordination commit flow (`commitSelection` or equivalent) on click.
  </acceptance_criteria>
  <verify>pnpm -s tsc --noEmit</verify>
  <done>Hotspot click produces committed cross-view focus; hover remains non-destructive preview.</done>
</task>

<task type="auto">
  <name>Task 2: Surface STKDE context in cube and add regression tests</name>
  <files>src/components/viz/CubeVisualization.tsx src/store/useStkdeStore.test.ts src/app/dashboard-v2/page.stkde.test.ts src/components/viz/CubeVisualization.stkde.test.ts</files>
  <read_first>
  - src/components/viz/CubeVisualization.tsx
  - src/app/stkde/lib/HotspotPanel.tsx
  - src/store/useStkdeStore.ts
  - src/app/stkde/page.stkde.test.ts
  - src/workers/stkdeHotspot.worker.test.ts
  </read_first>
  <action>
  Add cube-visible STKDE context and lock it with tests.

  Implementation in `CubeVisualization.tsx`:
  - Render a compact context card when STKDE run data exists.
  - Card title: `STKDE Context`.
  - Show selected hotspot id/title, intensity, support, and time window.
  - Show provenance line with `requested=` and `effective=` compute mode values plus truncation/fallback tags when present.
  - When no selected hotspot but STKDE data exists, show `No hotspot selected` hint instead of hiding the card.

  Add tests:
  - `src/store/useStkdeStore.test.ts`: validate defaults, run lifecycle transitions, stale marker behavior.
  - `src/app/dashboard-v2/page.stkde.test.ts`: assert page source includes STKDE panel mount, mode switch labels, stale warning phrase, hotspot click commit wiring (`setTimeRange`, `setSpatialBounds`).
  - `src/components/viz/CubeVisualization.stkde.test.ts`: assert cube source includes `STKDE Context`, `requested=`, `effective=`, and `No hotspot selected`.
  </action>
  <acceptance_criteria>
  - `src/components/viz/CubeVisualization.tsx` contains literal heading `STKDE Context`.
  - `src/components/viz/CubeVisualization.tsx` contains literal tokens `requested=` and `effective=` in rendered provenance text.
  - `src/store/useStkdeStore.test.ts` exists and contains a test for stale-state transition after prior success.
  - `src/app/dashboard-v2/page.stkde.test.ts` exists and asserts `setTimeRange` and `setSpatialBounds` hotspot commit wiring.
  - `src/components/viz/CubeVisualization.stkde.test.ts` exists and asserts `No hotspot selected` fallback text.
  </acceptance_criteria>
  <verify>pnpm -s tsc --noEmit && pnpm vitest src/store/useStkdeStore.test.ts src/app/dashboard-v2/page.stkde.test.ts src/components/viz/CubeVisualization.stkde.test.ts --run</verify>
  <done>Cube view shows STKDE-informed analysis context and Phase 65 wiring is protected by regression tests.</done>
</task>

</tasks>

<verification>
1. Hotspot click updates global focus filters and sync status; hover remains preview-only.
2. Cube renders STKDE context card when STKDE data exists.
3. Targeted tests cover store transitions and dashboard/cube STKDE wiring.
</verification>

<success_criteria>
- STKD-04: STKDE results are visible in cube view via context/provenance surface.
- STKD-05: User can continue standard interactions after STKDE focus commit (no hard lock).
- STKD-01/STKD-02 remain coherent through committed hotspot behavior and tested wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/65-stkde-integration/65-03-SUMMARY.md`
</output>
