---
phase: 64-dashboard-redesign
plan: 01
type: execute
wave: 1
depends_on: [63-01]
files_modified:
  - src/store/useCoordinationStore.ts
  - src/store/useCoordinationStore.test.ts
autonomous: true
requirements: [SYNC-04, SYNC-05]
requirements_addressed: [SYNC-04, SYNC-05]

must_haves:
  truths:
    - "Workflow state (generate, review, applied, refine) is represented in shared state and can be rendered consistently in any panel"
    - "Selection precedence follows last-interaction-wins without destructive auto-clears"
    - "When a panel cannot resolve current selection, the selection is preserved and a panel-local no-match reason is available"
  artifacts:
    - path: "src/store/useCoordinationStore.ts"
      provides: "Cross-view synchronization contract for selection authority, workflow phase, and reconciliation status"
      contains: "workflow phase enum, selection source metadata, reconciliation/no-match fields, preview-vs-commit actions"
    - path: "src/store/useCoordinationStore.test.ts"
      provides: "Behavioral verification for coordination invariants"
      contains: "last interaction wins, no-match preservation, reconcile-on-mode-change behavior"
  key_links:
    - from: "DualTimeline / MapVisualization / CubeVisualization"
      to: "useCoordinationStore"
      via: "setSelectedIndex/preview + commit reconciliation actions"
    - from: "dashboard-v2 status UI"
      to: "useCoordinationStore"
      via: "workflowPhase + sync status selectors"
---

<objective>
Define and lock the shared synchronization contract that all dashboard-v2 panels will consume.

Purpose: Phase 64 requires one source of truth for cross-view selection and workflow state so timeline, map, cube, and status UI cannot drift.

Output: A tested coordination store contract covering workflow phase, selection precedence, and non-destructive reconciliation/no-match behavior.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-dashboard-redesign/64-CONTEXT.md
@.planning/phases/63-map-visualization/63-01-SUMMARY.md
@src/store/useCoordinationStore.ts
@src/components/timeline/DualTimeline.tsx
@src/components/map/MapVisualization.tsx
@src/components/viz/CubeVisualization.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand coordination store with workflow-phase and reconciliation contracts</name>
  <files>src/store/useCoordinationStore.ts</files>
  <read_first>
  - src/store/useCoordinationStore.ts
  - .planning/phases/64-dashboard-redesign/64-CONTEXT.md
  - src/components/timeline/DualTimeline.tsx
  </read_first>
  <action>
  Extend `useCoordinationStore` to represent Phase 64 synchronization rules explicitly:

  1. Add workflow state model for `generate | review | applied | refine` and expose `setWorkflowPhase(phase)`.
  2. Add synchronization status model with concrete values `syncing | synchronized | partial` and optional `reason` field.
  3. Track selection provenance and precedence fields (`selectedSource`, `lastInteractionAt`, `lastInteractionSource`) and enforce last-interaction-wins via a single action (for example `commitSelection(index, source)`).
  4. Add non-destructive reconciliation action used on filter/workflow changes (for example `reconcileSelection({ isValid, reason, panel })`) that keeps global selection when invalid for one panel and sets panel-local no-match status instead of clearing globally.
  5. Add explicit clear path (`clearSelection(reason)`) for true invalidation when selection is globally incompatible.
  </action>
  <acceptance_criteria>
  - `src/store/useCoordinationStore.ts` contains `workflowPhase` with the four states: `generate`, `review`, `applied`, `refine`.
  - `src/store/useCoordinationStore.ts` contains sync status tokens `syncing`, `synchronized`, `partial`.
  - `src/store/useCoordinationStore.ts` contains a reconciliation action with an explicit `reason` argument.
  - `src/store/useCoordinationStore.ts` contains fields `lastInteractionAt` and `lastInteractionSource`.
  </acceptance_criteria>
  <verify>pnpm -s tsc --noEmit</verify>
  <done>Coordination store can represent workflow phase and synchronization outcomes without implicit UI-only state.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic unit tests for cross-view synchronization invariants</name>
  <files>src/store/useCoordinationStore.test.ts</files>
  <read_first>
  - src/store/useCoordinationStore.ts
  - src/store/useTimeslicingModeStore.test.ts
  </read_first>
  <action>
  Create `src/store/useCoordinationStore.test.ts` using existing Vitest style in store tests.

  Cover these concrete behaviors:
  1. Last interaction wins: committing selection from `map` after `timeline` updates both selected index and source.
  2. Panel-local no-match: reconcile with `isValid=false` for one panel preserves global selection and marks partial/no-match reason.
  3. Global invalidation: explicit `clearSelection(reason)` removes selected index/source and stores reason for status strip rendering.
  4. Workflow phase transitions: `setWorkflowPhase` accepts and persists each required phase token.
  </action>
  <acceptance_criteria>
  - `src/store/useCoordinationStore.test.ts` exists and contains at least one test per behavior above.
  - `pnpm vitest src/store/useCoordinationStore.test.ts --run` exits with code 0.
  - Test names include the phrases `last interaction wins` and `panel-local no-match`.
  </acceptance_criteria>
  <verify>pnpm vitest src/store/useCoordinationStore.test.ts --run</verify>
  <done>Cross-view synchronization contracts are executable and regression-protected.</done>
</task>

</tasks>

<verification>
1. Coordination store has explicit workflow/sync tokens (no inferred string literals sprinkled in UI).
2. Tests prove precedence and no-match behavior independently of rendering.
3. Typecheck remains clean after state contract expansion.
</verification>

<success_criteria>
- SYNC-04 groundwork exists as shared state (workflow states + reconciliation signals).
- SYNC-05 groundwork exists as readable summary inputs (strategy/granularity/sync state can be surfaced consistently).
- Coordination behavior is deterministic and covered by unit tests.
</success_criteria>

<output>
After completion, create `.planning/phases/64-dashboard-redesign/64-01-SUMMARY.md`
</output>
