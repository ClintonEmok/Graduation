---
phase: 68-dashboard-v2-flow-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [FLOW-CONS-01, FLOW-CONS-02, FLOW-CONS-03, FLOW-CONS-04]
requirements_addressed: [FLOW-CONS-01, FLOW-CONS-02, FLOW-CONS-03, FLOW-CONS-04]
files_modified:
  - src/app/dashboard-v2/page.tsx
  - src/components/binning/BinningControls.tsx
  - src/components/dashboard/DashboardHeader.tsx
  - src/store/useLayoutStore.ts
  - src/app/dashboard-v2/page.flow-consolidation.test.tsx
  - src/components/dashboard/DashboardHeader.flow-consolidation.test.tsx
autonomous: true

must_haves:
  truths:
    - "Core investigation stays inside dashboard-v2 with no route hopping during generate -> review -> apply -> refine -> analyze."
    - "Exactly one primary CTA is visible for the active workflow step, with Generate Draft Slices as the entry CTA."
    - "Advanced analysis stays hidden until after apply/refine, including STKDE controls."
    - "The header is informational only and never competes with the workflow rail."
    - "Scenario coverage for long-window burst scan, narrow-window drilldown, manual refinement, and hotspot-led investigation is documented and testable."
  artifacts:
    - path: src/app/dashboard-v2/page.tsx
      provides: guided workflow shell, step rail, and analysis gating
      contains: workflow rail, generate -> review -> apply -> refine -> analyze
    - path: src/components/binning/BinningControls.tsx
      provides: generate-step intent inputs and the dominant generation CTA
      contains: Generate Draft Slices
    - path: src/components/dashboard/DashboardHeader.tsx
      provides: status-only workflow/sync summary
      contains: workflow, sync status, strategy / granularity, slice summary
    - path: src/store/useLayoutStore.ts
      provides: persisted panel disclosure defaults
      contains: refinement: false, layers: false, stkde: false
    - path: src/app/dashboard-v2/page.flow-consolidation.test.tsx
      provides: route-contract regression coverage
      contains: dashboard-v2, Generate Draft Slices, panels.stkde
    - path: src/components/dashboard/DashboardHeader.flow-consolidation.test.tsx
      provides: header contract regression coverage
      contains: next/link absent, href="/dashboard-v2" absent
  key_links:
    - from: src/app/dashboard-v2/page.tsx
      to: src/components/binning/BinningControls.tsx
      via: generate-step render
      pattern: BinningControls
    - from: src/app/dashboard-v2/page.tsx
      to: src/components/stkde/DashboardStkdePanel.tsx
      via: workflow-phase gate after apply/refine
      pattern: panels\.stkde
    - from: src/components/dashboard/DashboardHeader.tsx
      to: navigation routes
      via: removed route links
      pattern: next/link|href="/dashboard-v2"|href="/timeslicing"|href="/stkde"
    - from: src/store/useLayoutStore.ts
      to: dashboard-v2 shell
      via: hidden-by-default advanced panel state
      pattern: layout-storage-v3
---

<objective>
Consolidate `dashboard-v2` into one guided workflow with progressive disclosure and one primary CTA per step.

Purpose: Phase 68 should make the investigation flow obvious, reduce competing controls, and keep advanced analysis contextual rather than route-based.
Output: A streamlined `dashboard-v2` shell, status-only header, hidden-by-default advanced panels, and regression tests that lock the flow contract.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/68-dashboard-v2-flow-consolidation/68-CONTEXT.md
@.planning/phases/68-dashboard-v2-flow-consolidation/68-FLOWS.md
@src/app/dashboard-v2/page.tsx
@src/components/binning/BinningControls.tsx
@src/components/dashboard/DashboardHeader.tsx
@src/store/useLayoutStore.ts
@src/app/timeslicing/components/SuggestionToolbar.tsx
@src/components/stkde/DashboardStkdePanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild dashboard-v2 around the single workflow rail</name>
  <files>src/app/dashboard-v2/page.tsx, src/components/binning/BinningControls.tsx, src/store/useLayoutStore.ts</files>
  <read_first>
    - `src/app/dashboard-v2/page.tsx`
    - `src/components/binning/BinningControls.tsx`
    - `src/store/useLayoutStore.ts`
    - `src/app/timeslicing/components/SuggestionToolbar.tsx`
    - `src/components/stkde/DashboardStkdePanel.tsx`
    - `.planning/phases/68-dashboard-v2-flow-consolidation/68-FLOWS.md`
    - `.planning/phases/68-dashboard-v2-flow-consolidation/68-CONTEXT.md`
  </read_first>
  <action>
    Recompose the route shell so the top workflow rail is the primary guide. Surface the existing `BinningControls` as the generate step with `Generate Draft Slices` as the dominant CTA, keep `SuggestionToolbar` as the only review/apply surface, and remove any duplicate page-level apply/clear controls from `page.tsx`.

    Keep `DashboardStkdePanel` fully gated behind `workflowPhase === 'applied' || workflowPhase === 'refine'`, and set the persisted advanced-panel defaults in `useLayoutStore.ts` so `refinement`, `layers`, and `stkde` start hidden while `timeline` and `map` remain visible. Do not introduce a second navigation surface; the workflow rail is the only primary guidance.
  </action>
  <acceptance_criteria>
    - `src/components/binning/BinningControls.tsx` contains the exact CTA text `Generate Draft Slices`.
    - `src/store/useLayoutStore.ts` initializes `refinement: false`, `layers: false`, and `stkde: false`.
    - `src/app/dashboard-v2/page.tsx` renders `BinningControls` and does not show a competing equal-weight toggle strip as the primary control cluster.
    - `src/app/dashboard-v2/page.tsx` does not render `DashboardStkdePanel` while the workflow is in generate/review.
    - The duplicate page-level draft apply/clear row is removed so the review/apply CTA lives in one place only.
  </acceptance_criteria>
  <verify>grep -n "Generate Draft Slices\|DashboardStkdePanel\|refinement: false\|layers: false\|stkde: false" src/app/dashboard-v2/page.tsx src/components/binning/BinningControls.tsx src/store/useLayoutStore.ts && pnpm exec tsc --noEmit</verify>
  <done>dashboard-v2 has a single visible path from generation to analysis and no longer exposes advanced analysis too early.</done>
</task>

<task type="auto">
  <name>Task 2: Strip dashboard header navigation and keep it informational</name>
  <files>src/components/dashboard/DashboardHeader.tsx</files>
  <read_first>
    - `src/components/dashboard/DashboardHeader.tsx`
    - `src/app/dashboard-v2/page.tsx`
    - `src/store/useCoordinationStore.ts`
    - `src/store/useBinningStore.ts`
    - `src/store/useTimeslicingModeStore.ts`
    - `src/store/useSliceDomainStore.ts`
    - `.planning/phases/68-dashboard-v2-flow-consolidation/68-FLOWS.md`
  </read_first>
  <action>
    Remove route hopping from the header entirely. Keep only workflow, sync status, strategy/granularity, and slice summary content; preserve badge semantics, but do not render `next/link` or any `href` to `/dashboard-v2`, `/timeslicing`, or `/stkde`. Make the copy read as context, not navigation.
  </action>
  <acceptance_criteria>
    - `src/components/dashboard/DashboardHeader.tsx` no longer imports `next/link`.
    - No `href="/dashboard-v2"`, `href="/timeslicing"`, or `href="/stkde"` remains in the header.
    - The header still renders workflow/sync badges and slice summary text.
    - The header copy no longer implies route hopping or alternate surfaces.
  </acceptance_criteria>
  <verify>grep -n "next/link\|href=\"/dashboard-v2\"\|href=\"/timeslicing\"\|href=\"/stkde\"" src/components/dashboard/DashboardHeader.tsx && pnpm exec tsc --noEmit</verify>
  <done>The header is purely informational and no longer presents competing navigation inside dashboard-v2.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the phase 68 flow contract with regression tests</name>
  <files>src/app/dashboard-v2/page.flow-consolidation.test.tsx, src/components/dashboard/DashboardHeader.flow-consolidation.test.tsx</files>
  <read_first>
    - `src/app/dashboard-v2/page.tsx`
    - `src/components/dashboard/DashboardHeader.tsx`
    - `src/components/binning/BinningControls.tsx`
    - `src/store/useLayoutStore.ts`
    - `.planning/phases/68-dashboard-v2-flow-consolidation/68-FLOWS.md`
  </read_first>
  <action>
    Add focused Vitest coverage that fails if the phase 68 contract regresses. Cover the generate CTA label, hidden advanced-panel defaults, STKDE remaining absent before apply/refine, the absence of header route links, and the canonical flow/scenario assumptions from `68-FLOWS.md`. Keep the assertions string/state based so they are cheap to maintain.
  </action>
  <acceptance_criteria>
    - A dedicated dashboard-v2 flow-contract test file exists.
    - A dedicated header contract test file exists.
    - The tests assert `Generate Draft Slices`, hidden advanced-panel defaults, and removed header links.
    - The tests pass with a targeted Vitest run.
  </acceptance_criteria>
  <verify>pnpm vitest --run src/app/dashboard-v2/page.flow-consolidation.test.tsx src/components/dashboard/DashboardHeader.flow-consolidation.test.tsx</verify>
  <done>Phase 68 has repeatable tests that protect the one-flow dashboard contract from accidental reintroduction of clutter or route hopping.</done>
</task>

</tasks>

<verification>
1. `dashboard-v2` presents one primary workflow with no competing route links in the header.
2. `Generate Draft Slices` is the visible generation CTA and advanced analysis stays hidden until apply/refine.
3. Phase-specific regression tests pass and lock the flow contract.
</verification>

<success_criteria>
1. The page reads as a single guided investigation flow rather than six equal panels.
2. STKDE is not visible before apply/refine.
3. The header remains informational only.
4. Regression tests make the phase 68 contract explicit and repeatable.
</success_criteria>

<output>
After completion, create `.planning/phases/XX-name/{phase}-{plan}-SUMMARY.md`
</output>
