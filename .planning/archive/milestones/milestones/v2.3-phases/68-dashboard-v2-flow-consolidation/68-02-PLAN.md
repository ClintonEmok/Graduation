---
phase: 68-dashboard-v2-flow-consolidation
plan: 02
type: execute
wave: 2
depends_on: [68-01]
requirements: [FLOW-CONS-01, FLOW-CONS-02, FLOW-CONS-03, FLOW-CONS-04]
requirements_addressed: [FLOW-CONS-01, FLOW-CONS-02, FLOW-CONS-03, FLOW-CONS-04]
files_modified:
  - src/app/dashboard-v2/page.tsx
  - src/app/timeslicing/components/SuggestionToolbar.tsx
  - src/app/dashboard-v2/page.flow-consolidation.test.tsx
  - src/components/dashboard/DashboardHeader.flow-consolidation.test.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Exactly one primary CTA is visible for the active workflow step, with Generate Draft Slices as the entry CTA."
    - "Scenario coverage for long-window burst scan, narrow-window drilldown, manual refinement, and hotspot-led investigation is documented and testable."
    - "Phase requirement IDs FLOW-CONS-01..04 are accounted for in project requirement traceability."
  artifacts:
    - path: src/app/dashboard-v2/page.tsx
      provides: step-gated action surfaces that avoid concurrent primary CTAs
      contains: workflowPhase
    - path: src/app/timeslicing/components/SuggestionToolbar.tsx
      provides: review/apply controls that are only visible in review/apply stages
      contains: Apply generated bins
    - path: src/app/dashboard-v2/page.flow-consolidation.test.tsx
      provides: scenario-contract regression checks for S1-S4
      contains: S1|S2|S3|S4
    - path: .planning/REQUIREMENTS.md
      provides: FLOW-CONS requirement definitions and traceability rows
      contains: FLOW-CONS-01
  key_links:
    - from: src/app/dashboard-v2/page.tsx
      to: src/app/timeslicing/components/SuggestionToolbar.tsx
      via: workflow-phase conditional render
      pattern: workflowPhase
    - from: .planning/phases/68-dashboard-v2-flow-consolidation/68-FLOWS.md
      to: src/app/dashboard-v2/page.flow-consolidation.test.tsx
      via: scenario IDs asserted in tests
      pattern: S1|S2|S3|S4
    - from: .planning/REQUIREMENTS.md
      to: .planning/ROADMAP.md
      via: requirement definition + phase traceability
      pattern: FLOW-CONS-01|FLOW-CONS-02|FLOW-CONS-03|FLOW-CONS-04
---

<objective>
Close Phase 68 verification gaps by enforcing single-primary-CTA gating, adding explicit scenario-contract test coverage, and restoring requirement traceability for FLOW-CONS-01..04.

Purpose: The implementation is close but still violates core flow contract guarantees; this plan resolves the remaining blockers so Phase 68 can pass goal verification.
Output: One-primary-CTA-per-step behavior in dashboard-v2, S1-S4 assertions in regression tests, and REQUIREMENTS.md entries/traceability for FLOW-CONS IDs.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/68-dashboard-v2-flow-consolidation/68-01-SUMMARY.md
@.planning/phases/68-dashboard-v2-flow-consolidation/68-dashboard-v2-flow-consolidation-VERIFICATION.md
@.planning/phases/68-dashboard-v2-flow-consolidation/68-FLOWS.md
@src/app/dashboard-v2/page.tsx
@src/app/timeslicing/components/SuggestionToolbar.tsx
@src/app/dashboard-v2/page.flow-consolidation.test.tsx
@src/components/dashboard/DashboardHeader.flow-consolidation.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce one-primary-CTA-per-step in dashboard-v2 workflow</name>
  <files>src/app/dashboard-v2/page.tsx, src/app/timeslicing/components/SuggestionToolbar.tsx</files>
  <action>
    Gate action surfaces by `workflowPhase` so generate and review/apply primaries are never visible concurrently. Keep `Generate Draft Slices` as the primary action in generate, then show review/apply controls only in review/apply phases. Preserve existing behavior for refine/analyze while preventing overlapping primary actions.
  </action>
  <acceptance_criteria>
    - Generate phase shows `Generate Draft Slices` and does not show `Apply generated bins`.
    - Review/apply phase can show `Apply generated bins` while `Generate Draft Slices` is not concurrently rendered as a primary CTA.
    - Exactly one primary CTA is rendered for each active workflow step.
    - No route hopping is introduced; all gating remains inside `dashboard-v2` workflow state.
  </acceptance_criteria>
  <verify>pnpm vitest --run src/app/dashboard-v2/page.flow-consolidation.test.tsx && pnpm exec tsc --noEmit</verify>
  <done>Only one primary CTA is visible per active workflow step.</done>
</task>

<task type="auto">
  <name>Task 2: Add scenario-contract regression assertions for S1-S4 flows</name>
  <files>src/app/dashboard-v2/page.flow-consolidation.test.tsx, src/components/dashboard/DashboardHeader.flow-consolidation.test.tsx</files>
  <action>
    Add explicit S1-S4 contract assertions aligned with `68-FLOWS.md`: long-window burst scan, narrow-window drilldown, manual refinement, and hotspot-led investigation. Keep assertions source/state based and cheap to maintain, but explicit enough that regressions fail when scenario affordances disappear.
  </action>
  <acceptance_criteria>
    - Tests include explicit scenario markers or assertions for S1, S2, S3, and S4.
    - Scenario checks validate both flow assumptions and UI contract protections.
    - Targeted Vitest run passes.
  </acceptance_criteria>
  <verify>pnpm vitest --run src/app/dashboard-v2/page.flow-consolidation.test.tsx src/components/dashboard/DashboardHeader.flow-consolidation.test.tsx</verify>
  <done>S1-S4 scenario coverage is documented in code and protected by regression tests.</done>
</task>

<task type="auto">
  <name>Task 3: Restore FLOW-CONS requirement definitions and traceability</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Add FLOW-CONS-01 through FLOW-CONS-04 requirement entries to `REQUIREMENTS.md` with concise definitions matching Phase 68 scope. Update the traceability section/table so each FLOW-CONS ID maps to Phase 68 and is no longer metadata-only.
  </action>
  <acceptance_criteria>
    - `REQUIREMENTS.md` defines FLOW-CONS-01..04.
    - Each FLOW-CONS ID has both a definition entry and a traceability row linked to Phase 68.
    - IDs are discoverable by plain text search in requirement definitions and traceability.
  </acceptance_criteria>
  <verify>rg "FLOW-CONS-0[1-4]" .planning/REQUIREMENTS.md && rg "(FLOW-CONS-0[1-4].*68|Phase 68.*FLOW-CONS-0[1-4])" .planning/REQUIREMENTS.md</verify>
  <done>FLOW-CONS IDs are formally defined and traceable in project requirements.</done>
</task>

</tasks>

<verification>
1. Dashboard-v2 enforces one primary CTA per active step.
2. Regression tests assert S1-S4 scenario contract coverage.
3. FLOW-CONS-01..04 are present in REQUIREMENTS.md with traceability to Phase 68.
</verification>

<success_criteria>
1. Phase 68 verification blockers are fully resolved.
2. Goal-level flow contract is test-locked and requirement-traceable.
3. Re-running verifier for Phase 68 can achieve passed status.
</success_criteria>

<output>
After completion, create `.planning/phases/68-dashboard-v2-flow-consolidation/68-02-SUMMARY.md`
</output>
