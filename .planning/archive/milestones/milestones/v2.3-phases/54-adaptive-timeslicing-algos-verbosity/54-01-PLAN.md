---
phase: 54-adaptive-timeslicing-algos-verbosity
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/timeslicing-algos/lib/algorithm-options.ts
  - src/app/timeslicing-algos/lib/mode-intent.ts
  - src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx
  - src/app/timeslicing-algos/lib/mode-intent.test.ts
autonomous: true

must_haves:
  truths:
    - "Users can select `adaptive` from `/timeslicing-algos` without breaking existing `uniform-time` and `uniform-events` selection behavior"
    - "Adaptive selection is translated into an explicit effective binning mode through central resolver contracts before compute calls"
    - "Mode intent is visible in route state/query and does not require duplicating route-mode resolution logic"
  artifacts:
    - path: "src/app/timeslicing-algos/lib/algorithm-options.ts"
      provides: "Mode options registry includes an active `adaptive` route intent option"
      contains: "adaptive"
    - path: "src/app/timeslicing-algos/lib/mode-intent.ts"
      provides: "Single route-intent parser/resolver wrapper that delegates effective mode resolution to `resolveRouteBinningMode`"
      contains: "resolveRouteBinningMode"
    - path: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      provides: "Route shell wiring from selected mode intent to effective compute mode"
      contains: "computeMaps"
    - path: "src/app/timeslicing-algos/lib/mode-intent.test.ts"
      provides: "Deterministic intent parsing/resolution regression coverage"
      contains: "adaptive"
  key_links:
    - from: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      to: "src/app/timeslicing-algos/lib/mode-intent.ts"
      via: "selected intent -> effective mode resolution"
      pattern: "resolve.*ModeIntent"
    - from: "src/app/timeslicing-algos/lib/mode-intent.ts"
      to: "src/lib/adaptive/route-binning-mode.ts"
      via: "delegated route default + override precedence"
      pattern: "resolveRouteBinningMode"
---

<objective>
Add adaptive mode intent support to `/timeslicing-algos` while preserving existing binning-mode compatibility.

Purpose: This introduces a third algorithm-testing intent (`adaptive`) at the route level without widening core backend/store binning contracts, and keeps mode resolution centralized.
Output: Updated algos mode options, a route-intent resolver helper, shell wiring that passes effective modes to compute calls, and helper-level regression tests.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-CONTEXT.md
@.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-RESEARCH.md
@.planning/phases/53-add-dedicated-uniform-events-timeslicing-route/53-02-SUMMARY.md
@src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx
@src/lib/adaptive/route-binning-mode.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an algos route mode-intent contract with adaptive intent resolution</name>
  <files>src/app/timeslicing-algos/lib/mode-intent.ts</files>
  <action>Create a dedicated helper that owns `/timeslicing-algos` mode-intent parsing and effective mode resolution. Define a route-intent type that includes `adaptive` plus existing `AdaptiveBinningMode` values, normalize URL/query input into this intent, and derive effective mode by delegating to `resolveRouteBinningMode(pathname, explicitOverride)` where `adaptive` maps to resolver default behavior (pass null override). Do not add duplicate pathname heuristics or duplicate resolver logic in this file.</action>
  <verify>`npm run typecheck` passes and helper exports typed parse/resolve functions.</verify>
  <done>The route has one canonical intent-to-effective-mode helper, and adaptive intent resolves through existing central resolver contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Wire adaptive intent into algos options and route shell compute flow</name>
  <files>src/app/timeslicing-algos/lib/algorithm-options.ts, src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx</files>
  <action>Add an active `adaptive` option to the algos mode selector contract and update the route shell to read/write selected intent in query state while computing with the helper-derived effective mode. Keep `computeMaps(..., { binningMode })` on the existing `AdaptiveBinningMode` contract and preserve existing `uniform-time` / `uniform-events` UX and behavior. Avoid direct mode-resolution if/else blocks in the shell; call the helper instead.</action>
  <verify>`npm run typecheck` passes and `TimeslicingAlgosRouteShell.tsx` computes using a helper-derived effective mode value.</verify>
  <done>`/timeslicing-algos` accepts `mode=adaptive`, existing modes remain backward compatible, and compute requests continue using only valid backend/store binning modes.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic tests for intent parsing and effective mode fallback</name>
  <files>src/app/timeslicing-algos/lib/mode-intent.test.ts</files>
  <action>Add unit tests for the new helper covering: valid explicit intents (`uniform-time`, `uniform-events`, `adaptive`), invalid/unknown intent fallback, adaptive intent resolution to route default effective mode on `/timeslicing-algos`, and no regressions for existing mode intent behavior. Include at least one non-algos pathname case to prove fallback remains safe.</action>
  <verify>`npm test -- --run src/app/timeslicing-algos/lib/mode-intent.test.ts` passes.</verify>
  <done>Tests fail if adaptive intent parsing/resolution regresses or if existing uniform mode handling changes unexpectedly.</done>
</task>

</tasks>

<verification>
- `/timeslicing-algos` mode model includes `adaptive` intent and still supports `uniform-time` and `uniform-events`.
- Effective binning mode is always derived through central resolver contracts.
- Helper-level tests lock intent parsing and fallback behavior.
</verification>

<success_criteria>
- Adaptive intent is selectable and persisted in route query state.
- Existing two-mode behavior remains unchanged for current users.
- No duplicated route-mode resolution logic is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-01-SUMMARY.md`
</output>
