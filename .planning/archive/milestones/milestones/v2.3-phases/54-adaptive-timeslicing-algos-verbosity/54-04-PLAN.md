---
phase: 54-adaptive-timeslicing-algos-verbosity
plan: 4
type: execute
wave: 2
depends_on: [54-01]
files_modified:
  - src/app/timeslicing-algos/lib/mode-selection.ts
  - src/app/timeslicing-algos/lib/TimeslicingAlgosInteractionControls.tsx
  - src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx
  - src/app/timeslicing-algos/lib/algorithm-options.ts
  - src/app/timeslicing-algos/lib/mode-selection.test.ts
  - src/app/timeslicing-algos/page.timeline-algos.test.ts
autonomous: true

must_haves:
  truths:
    - "`/timeslicing-algos` interaction flow matches timeline-test semantics for time-scale toggling (linear vs adaptive), including warp-factor activation behavior"
    - "Users can explicitly select uniform binning strategy (`uniform-time` or `uniform-events`) regardless of timeline interaction mode"
    - "Route UI presents clear, separate controls for strategy selection and interaction mode, with deterministic query-state restoration"
  artifacts:
    - path: "src/app/timeslicing-algos/lib/mode-selection.ts"
      provides: "Single parser/resolver for route query state (`strategy`, `timescale`) with backward-compatible fallback from legacy `mode`"
      contains: "resolveTimeslicingAlgosSelection"
    - path: "src/app/timeslicing-algos/lib/TimeslicingAlgosInteractionControls.tsx"
      provides: "Dedicated UI controls for binning strategy and interaction mode"
      contains: "Binning strategy"
    - path: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      provides: "Route wiring that passes selected strategy to computeMaps and selected interaction mode to useTimeStore"
      contains: "computeMaps"
    - path: "src/app/timeslicing-algos/lib/mode-selection.test.ts"
      provides: "Unit matrix for selection parsing, fallback, and legacy query compatibility"
      contains: "strategy"
    - path: "src/app/timeslicing-algos/page.timeline-algos.test.ts"
      provides: "Route-level regression guard for explicit controls and parity-focused wiring"
      contains: "timescale"
  key_links:
    - from: "src/app/timeslicing-algos/lib/TimeslicingAlgosInteractionControls.tsx"
      to: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      via: "onStrategyChange/onTimeScaleChange callbacks"
      pattern: "onStrategyChange|onTimeScaleChange"
    - from: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      to: "src/store/useAdaptiveStore.ts"
      via: "computeMaps(..., { binningMode: selectedStrategy })"
      pattern: "binningMode"
    - from: "src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx"
      to: "src/store/useTimeStore.ts"
      via: "setTimeScaleMode(selectedTimeScale)"
      pattern: "setTimeScaleMode"
---

<objective>
Bring `/timeslicing-algos` to timeline-test interaction parity for mode handling while preserving explicit uniform binning strategy selection.

Purpose: The route currently conflates intent/mode state and does not provide a clear separation between interaction mode and binning strategy. This plan makes user intent explicit, keeps uniform binning support first-class, and aligns interaction behavior with timeline-test controls.
Output: A route-local selection contract, clear control UI, shell wiring updates, and regression tests that lock strategy/mode behavior.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-CONTEXT.md
@.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-RESEARCH.md
@.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-01-SUMMARY.md
@src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx
@src/app/timeline-test/components/SliceToolbar.tsx
@src/lib/adaptive/route-binning-mode.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a route-local strategy/time-scale selection contract with legacy compatibility</name>
  <files>src/app/timeslicing-algos/lib/mode-selection.ts, src/app/timeslicing-algos/lib/algorithm-options.ts</files>
  <action>Create a focused helper that parses query params into two explicit values: `strategy` (`uniform-time` | `uniform-events`) and `timescale` (`linear` | `adaptive`). Preserve backward compatibility by mapping legacy `mode` values (`uniform-time`, `uniform-events`, `adaptive`) into the new two-field model so old links do not break. Keep fallback defaults deterministic for `/timeslicing-algos` (`uniform-events` strategy, `linear` timescale) and avoid spreading parse/fallback logic into the route shell.</action>
  <verify>`npm run typecheck` passes and helper exports typed parse/resolve + query serialization utilities.</verify>
  <done>Route mode ownership is centralized in one helper and supports both legacy and new query formats without ambiguity.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire algos shell to timeline-test interaction semantics with explicit strategy controls</name>
  <files>src/app/timeslicing-algos/lib/TimeslicingAlgosInteractionControls.tsx, src/app/timeslicing-algos/lib/TimeslicingAlgosRouteShell.tsx</files>
  <action>Introduce a dedicated control component that clearly separates "Binning strategy" and "Timeline interaction mode" options. In the route shell, consume Task 1 helper output, persist both values to query state, set `useTimeStore.setTimeScaleMode` from selected timescale, and preserve timeline-test warp activation behavior (`adaptive` mode forces non-zero warp factor when currently zero). Always pass selected strategy to `computeMaps(..., { binningMode })`, so uniform-time and uniform-events remain selectable under either interaction mode. Do not reintroduce single `mode`-only UI coupling.</action>
  <verify>`npm run typecheck` passes and shell compute call uses strategy selection instead of inferred adaptive intent.</verify>
  <done>Users can independently switch interaction mode and binning strategy, and timeline interactions in adaptive/linear mode follow timeline-test semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Lock parity and strategy behavior with unit + route regression tests</name>
  <files>src/app/timeslicing-algos/lib/mode-selection.test.ts, src/app/timeslicing-algos/page.timeline-algos.test.ts</files>
  <action>Add unit tests for helper parsing/serialization matrix (new query params, legacy `mode` fallback, invalid values, deterministic defaults). Expand route regression tests to assert presence of separate strategy/mode controls, explicit uniform strategy options, and parity-critical wiring tokens (`setTimeScaleMode`, warp-factor guard, `computeMaps` binning strategy argument). Keep existing "no suggestion workflow orchestration" assertions intact.</action>
  <verify>`npm test -- --run src/app/timeslicing-algos/lib/mode-selection.test.ts src/app/timeslicing-algos/page.timeline-algos.test.ts` passes.</verify>
  <done>Regression suite fails on any drift that collapses strategy+mode separation or breaks timeline-test interaction semantics.</done>
</task>

</tasks>

<verification>
- Unit: `npm test -- --run src/app/timeslicing-algos/lib/mode-selection.test.ts`
- Route integration regression: `npm test -- --run src/app/timeslicing-algos/page.timeline-algos.test.ts`
- Interaction contract sanity: `npm test -- --run src/lib/adaptive/route-binning-mode.test.ts`
- Type safety gate: `npm run typecheck`
- Manual QA (e2e-style): open `/timeslicing-algos`, switch `Binning strategy` between `uniform-time` and `uniform-events`, toggle `Timeline interaction mode` between `linear` and `adaptive`, confirm timeline remains interactive and URL state restores selections on refresh.
</verification>

<success_criteria>
- `/timeslicing-algos` exposes clear separate controls for strategy and interaction mode.
- Uniform binning strategies remain first-class and selectable in all interaction contexts.
- Adaptive/linear interaction toggles behave like timeline-test (including warp factor activation guard).
- Regression tests explicitly protect behavior parity and strategy/mode decoupling.
</success_criteria>

<risks>
- Query param migration risk: old `mode=` links could lose intent if compatibility mapping is incomplete.
- UX drift risk: route-local control styling/wording may diverge from timeline-test semantics if duplicated carelessly.
- Store coupling risk: incorrect update order between query state, `timeScaleMode`, and `computeMaps` can cause transient mismatches.
</risks>

<rollback>
- Keep legacy `mode` parser path in `mode-selection.ts` behind deterministic fallback; if regression is found, temporarily prioritize legacy `mode` over new params.
- If decoupled controls cause instability, revert to previous single-mode UI by restoring `TimeslicingAlgosRouteShell.tsx` mode-intent wiring while retaining helper tests for future retry.
- Re-run targeted tests and restore previous 54-01 behavior baseline before re-attempting control split.
</rollback>

<output>
After completion, create `.planning/phases/54-adaptive-timeslicing-algos-verbosity/54-04-SUMMARY.md`
</output>
