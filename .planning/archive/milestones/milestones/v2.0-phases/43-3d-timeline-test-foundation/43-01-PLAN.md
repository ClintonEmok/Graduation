---
phase: 43-3d-timeline-test-foundation
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/timeline-test-3d/page.tsx
  - src/app/timeline-test-3d/lib/route-orchestration.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "A dedicated /timeline-test-3d route loads without using /timeline-test URL reuse"
    - "The 3D route mirrors fetched crimes into useDataStore and computes adaptive maps from the same domain"
    - "Core controls (time scale mode, warp source/mode, generation triggers) are visible and mutate shared stores"
    - "Acceptance/generation bridge events are wired in route scope with cleanup to avoid duplicate handlers"
  artifacts:
    - path: "src/app/timeline-test-3d/page.tsx"
      provides: "Route-level parity runtime with timeline panel and control wiring"
      contains: "DualTimeline + useCrimeData -> useDataStore/useAdaptiveStore mirroring"
    - path: "src/app/timeline-test-3d/lib/route-orchestration.ts"
      provides: "3D-route-local copied orchestration helpers for parity-critical flows"
      contains: "buildSliceAuthoredWarpMap and percent/domain remap helpers"
  key_links:
    - from: "src/app/timeline-test-3d/page.tsx"
      to: "src/hooks/useCrimeData.ts"
      via: "domain-based query for route data"
      pattern: "useCrimeData\\("
    - from: "src/app/timeline-test-3d/page.tsx"
      to: "src/store/useDataStore.ts"
      via: "mirrors fetched points into canonical store"
      pattern: "useDataStore\\.setState"
    - from: "src/app/timeline-test-3d/page.tsx"
      to: "src/store/useSuggestionStore.ts"
      via: "generation/acceptance event bridge"
      pattern: "accept-full-auto-package"
---

<objective>
Stand up the dedicated 3D timeline-test route with the same runtime context contracts as timeline-test/timeslicing, while explicitly keeping parity-critical orchestration as route-local copies.

Purpose: Deliver CUBE-01/CUBE-02 foundation quickly with low regression risk by reusing store contracts and copying route orchestration instead of refactoring shared abstractions.
Output: A new `/timeline-test-3d` route plus route-local orchestration helpers that load data/domain consistently and expose core parity controls.
Constraint: This must live in its own route folder (`src/app/timeline-test-3d/`) and must not be implemented inside existing route folders.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-3d-timeline-test-foundation/43-RESEARCH.md
@.planning/phases/42-full-auto-package-acceptance-alignment/42-01-SUMMARY.md
@src/app/timeslicing/page.tsx
@src/app/timeline-test/page.tsx
@src/components/timeline/DualTimeline.tsx
@src/store/useAdaptiveStore.ts
@src/store/useDataStore.ts
@src/store/useSuggestionStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dedicated 3D route shell and copied orchestration helpers</name>
  <files>src/app/timeline-test-3d/page.tsx, src/app/timeline-test-3d/lib/route-orchestration.ts</files>
  <action>
    Create `src/app/timeline-test-3d/page.tsx` as a new App Router page (do not alias or redirect `/timeline-test`) and add `src/app/timeline-test-3d/lib/route-orchestration.ts` for parity-critical helper copies.
    Route structure is mandatory: keep all new route-specific files under `src/app/timeline-test-3d/**`.

    In `route-orchestration.ts`, copy and adapt the route-level helpers used by timeline-test/timeslicing:
    - `buildSliceAuthoredWarpMap` for slice-authored adaptive override map generation.
    - selection/domain percent remap helper used for suggestion hover/accept mapping.

    Keep helper scope route-local by design (CUBE-09); avoid extracting new shared cross-route abstractions in this phase.
  </action>
  <verify>npx eslint src/app/timeline-test-3d/page.tsx src/app/timeline-test-3d/lib/route-orchestration.ts</verify>
  <done>The new route file exists, compiles, and imports route-local copied orchestration helpers instead of introducing shared refactor work.</done>
</task>

<task type="auto">
  <name>Task 2: Implement single-domain data pipeline and timeline parity wiring</name>
  <files>src/app/timeline-test-3d/page.tsx</files>
  <action>
    In `src/app/timeline-test-3d/page.tsx`, implement route orchestration based on `src/app/timeslicing/page.tsx` parity patterns:
    - Resolve effective domain from adaptive map domain with safe fallback to data-store timestamps/default epoch range.
    - Fetch crimes via `useCrimeData` once per effective domain.
    - Mirror fetched records into `useDataStore` with min/max timestamps and spatial bounds.
    - Trigger `useAdaptiveStore.getState().computeMaps` from the exact same timestamp array and domain used for mirroring.
    - Render `DualTimeline` with adaptive override support for slice-authored warp source.

    Do not use `useViewportCrimeData` or any second fetch path in this route.
  </action>
  <verify>npx eslint src/app/timeline-test-3d/page.tsx && npx tsc --noEmit</verify>
  <done>Timeline panel and adaptive maps are fed from one canonical route domain/data pipeline, preventing 3D-vs-timeline domain drift.</done>
</task>

<task type="auto">
  <name>Task 3: Wire core controls and generation/acceptance event bridge</name>
  <files>src/app/timeline-test-3d/page.tsx</files>
  <action>
    Add core parity controls to the 3D route and bind them to existing stores/contracts:
    - Render timeline-test controls (`SliceToolbar`, `WarpSliceEditor`) and timeslicing generation controls (`SuggestionToolbar`) within the route layout.
    - Wire time scale mode and warp source/mode changes to shared stores (`useTimeStore`, `useAdaptiveStore`, `useWarpSliceStore`).
    - Implement event listeners for `accept-time-scale`, `accept-interval-boundary`, and `accept-full-auto-package` following `timeslicing/page.tsx` semantics and `useEffect` cleanup patterns.

    Keep behavior parity-focused: copy route-level handler logic as needed, but do not start 2D/3D consolidation in this phase.
  </action>
  <verify>npx eslint src/app/timeline-test-3d/page.tsx && npx tsc --noEmit</verify>
  <done>Core controls are present and connected, and generation/acceptance events update shared stores without duplicate listener leaks.</done>
</task>

</tasks>

<verification>
- [ ] `npx eslint src/app/timeline-test-3d/page.tsx src/app/timeline-test-3d/lib/route-orchestration.ts` passes.
- [ ] `npx tsc --noEmit` passes after adding the route.
- [ ] Navigating to `/timeline-test-3d` mounts successfully and renders timeline panel + control section.
- [ ] Domain fetch path in route uses one `useCrimeData` pipeline, then mirrors to `useDataStore` and computes adaptive maps from the same domain.
- [ ] Event listeners for acceptance/generation actions are attached once and cleaned up on unmount/remount.
</verification>

<success_criteria>
- CUBE-01 foundation is met: dedicated 3D route exists with timeline-test-equivalent store/data domain plumbing.
- CUBE-02 foundation is met: core controls and generation triggers are visible and wired to shared runtime stores.
- CUBE-09 policy is honored: parity-critical route logic is copied into route scope without introducing mandatory cross-surface refactor.
</success_criteria>

<output>
After completion, create `.planning/phases/43-3d-timeline-test-foundation/43-01-SUMMARY.md`
</output>
