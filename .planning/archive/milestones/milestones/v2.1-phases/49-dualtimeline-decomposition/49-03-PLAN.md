---
phase: 49-dualtimeline-decomposition
plan: 03
type: execute
wave: 3
depends_on: [49-02]
files_modified:
  - src/components/timeline/DualTimeline.tsx
  - src/components/timeline/hooks/usePointSelection.ts
  - src/components/timeline/hooks/usePointSelection.test.ts
autonomous: true

must_haves:
  truths:
    - "Point hover/select interactions continue to match existing nearest-point behavior"
    - "Selection threshold semantics remain unchanged (`max(rangeSpan * 0.01, 60)`)"
    - "DualTimeline is orchestration-focused and composes dedicated hooks for transforms, density, brush/zoom sync, and point selection"
  artifacts:
    - path: "src/components/timeline/hooks/usePointSelection.ts"
      provides: "Pointer interaction hook for hover/select/clear flows"
      exports: ["usePointSelection"]
    - path: "src/components/timeline/hooks/usePointSelection.test.ts"
      provides: "Deterministic selection threshold and nearest-point parity coverage"
      contains: "max(rangeSpan * 0.01, 60)"
    - path: "src/components/timeline/DualTimeline.tsx"
      provides: "Orchestration-focused timeline component composing extracted hooks"
      contains: "usePointSelection"
  key_links:
    - from: "src/components/timeline/hooks/usePointSelection.ts"
      to: "src/lib/selection.ts"
      via: "reuse nearest-point utilities instead of re-implementing selection math"
      pattern: "selection"
    - from: "src/components/timeline/DualTimeline.tsx"
      to: "src/components/timeline/hooks"
      via: "composes four dedicated timeline hooks"
      pattern: "useScaleTransforms|useDensityStripDerivation|useBrushZoomSync|usePointSelection"
---

<objective>
Extract point-selection interactions and complete DualTimeline decomposition into an orchestration-focused component.

Purpose: This finalizes Phase 49 by isolating the remaining interaction domain and ensuring all required hooks are in place with parity-preserving tests.
Output: `usePointSelection` hook, deterministic selection regression tests, and a streamlined DualTimeline orchestrator that composes all extracted hooks.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-dualtimeline-decomposition/49-CONTEXT.md
@.planning/phases/49-dualtimeline-decomposition/49-RESEARCH.md
@.planning/phases/46-guardrails-and-baselines/46-03-SUMMARY.md
@src/components/timeline/DualTimeline.tsx
@src/lib/selection.ts
@src/components/timeline/lib/interaction-guards.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract pointer hover/select logic into usePointSelection</name>
  <files>src/components/timeline/hooks/usePointSelection.ts</files>
  <action>Create `usePointSelection` to own pointer event logic currently inside `DualTimeline.tsx` for hover, selection, and clearing behavior. Preserve existing nearest-point semantics and parity-critical threshold rule exactly: `max(rangeSpan * 0.01, 60)`. Reuse existing selection utilities/contracts where applicable; do not change selection source labels or store update semantics.</action>
  <verify>`npm run typecheck` passes after hook extraction scaffolding</verify>
  <done>`usePointSelection` encapsulates pointer selection behavior with unchanged threshold and nearest-point contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic regression coverage for point-selection parity</name>
  <files>src/components/timeline/hooks/usePointSelection.test.ts</files>
  <action>Add deterministic tests for selection edge cases using fixed fixtures: nearest point inside threshold selects, outside threshold clears, invalid coordinates are safely ignored, and empty data behaves safely. Assert the exact threshold semantics (`max(rangeSpan * 0.01, 60)`) and maintain parity with current selection behavior.</action>
  <verify>`npm test -- --run src/components/timeline/hooks/usePointSelection.test.ts` passes</verify>
  <done>Point-selection contracts are covered by deterministic tests that guard against threshold and edge-case drift.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize DualTimeline as hook-composition orchestrator</name>
  <files>src/components/timeline/DualTimeline.tsx</files>
  <action>Complete DualTimeline decomposition by wiring `usePointSelection` alongside previously extracted hooks (`useScaleTransforms`, `useDensityStripDerivation`, `useBrushZoomSync`) and removing now-duplicated in-component logic. Keep UI rendering, store interactions, and event behavior unchanged. Ensure file responsibility is orchestration-focused (state selection, hook composition, rendering), not domain math ownership.</action>
  <verify>`npm run lint -- src/components/timeline/DualTimeline.tsx src/components/timeline/hooks/usePointSelection.ts && npm test -- --run src/components/timeline/hooks/useScaleTransforms.test.ts src/components/timeline/hooks/useDensityStripDerivation.test.ts src/components/timeline/hooks/useBrushZoomSync.test.ts src/components/timeline/hooks/usePointSelection.test.ts src/components/timeline/lib/interaction-guards.test.ts && npm run typecheck` passes</verify>
  <done>DualTimeline composes all four dedicated hooks and preserves existing interactions with full targeted regression coverage passing.</done>
</task>

</tasks>

<verification>
- `DualTimeline.tsx` composes all four dedicated hook modules required by Phase 49.
- Point-selection threshold and nearest-point behavior remain unchanged.
- Extracted hook test suites and interaction-guard regressions pass together.
- Lint and typecheck pass after final orchestration cleanup.
</verification>

<success_criteria>
- Phase 49 criteria (1), (2), and (3) are fully satisfied.
- DualTimeline responsibility is orchestration-focused, with domain logic extracted into dedicated hooks.
- Existing timeline interactions are preserved under deterministic regression coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/49-dualtimeline-decomposition/49-03-SUMMARY.md`
</output>
