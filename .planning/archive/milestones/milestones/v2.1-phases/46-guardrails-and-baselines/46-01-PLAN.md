---
phase: 46-guardrails-and-baselines
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/capture-refactor-baseline.mjs
  - .planning/baselines/46-guardrails-baseline.json
  - .planning/baselines/46-guardrails-baseline.md
  - .github/pull_request_template.md
autonomous: true

must_haves:
  truths:
    - "Baseline metrics for timeline refactor hot paths can be re-collected with one command"
    - "Baseline output includes file-size and timing measurements that can be compared after each refactor phase"
    - "Refactor PRs include an explicit checklist gate for behavior parity, test updates, and log cleanup"
  artifacts:
    - path: "scripts/capture-refactor-baseline.mjs"
      provides: "Repeatable baseline capture command for size/timing metrics"
      min_lines: 60
    - path: ".planning/baselines/46-guardrails-baseline.json"
      provides: "Machine-readable baseline snapshot for comparisons"
      contains: "capturedAt"
    - path: ".github/pull_request_template.md"
      provides: "Refactor checklist gate for subsequent phases"
      contains: "Refactor Guardrails"
  key_links:
    - from: "scripts/capture-refactor-baseline.mjs"
      to: ".planning/baselines/46-guardrails-baseline.json"
      via: "writes snapshot payload"
      pattern: "writeFile"
    - from: ".github/pull_request_template.md"
      to: "Phase 47-51 PR workflows"
      via: "required checklist section"
      pattern: "behavior unchanged"
---

<objective>
Create repeatable baseline artifacts and refactor quality gates before code decomposition starts.

Purpose: The next phases (47-51) need objective before/after checkpoints so regressions are caught early and behavior parity is enforced on every refactor PR.
Output: A baseline capture script, persisted baseline snapshot/docs, and a PR template checklist used as a refactor gate.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REFACTORING-PLAN.md
@src/components/timeline/DualTimeline.tsx
@src/hooks/useCrimeData.ts
@src/lib/queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add baseline capture command and persisted snapshot</name>
  <files>scripts/capture-refactor-baseline.mjs, .planning/baselines/46-guardrails-baseline.json, .planning/baselines/46-guardrails-baseline.md</files>
  <action>Implement a Node script that collects baseline metrics for refactor-critical paths: at minimum (a) line counts for `DualTimeline.tsx`, `useCrimeData.ts`, and `queries.ts`, and (b) deterministic timing runs for key pure functions used in timeline interaction flow (selection lookup and timeline-domain helpers). The script must support a write mode (`--write`) that updates `.planning/baselines/46-guardrails-baseline.json`, and a read/print mode for quick comparison. Store metric definitions and collection steps in `.planning/baselines/46-guardrails-baseline.md` so later phases can rerun exactly the same capture process. Do not introduce new dependencies.</action>
  <verify>Run `node scripts/capture-refactor-baseline.mjs --write` and confirm the command exits 0 and updates `.planning/baselines/46-guardrails-baseline.json` with timestamped metrics.</verify>
  <done>A reproducible baseline exists in code + JSON artifact, with numeric measurements for file size and hot-path timings that can be compared after refactors.</done>
</task>

<task type="auto">
  <name>Task 2: Add refactor guardrail checklist to PR template</name>
  <files>.github/pull_request_template.md</files>
  <action>Create a repository PR template (or update if already present) with a dedicated `Refactor Guardrails` checklist section requiring: behavior unchanged confirmation, baseline comparison result, regression tests added/updated, and removal of transient debug logs introduced during refactor. Keep the template concise and focused on gating Phases 47-51 changes.</action>
  <verify>Open `.github/pull_request_template.md` and confirm all guardrail checklist items exist with markdown checkboxes.</verify>
  <done>Subsequent refactor PRs have an explicit, reusable quality gate aligned with REFACTOR-02.</done>
</task>

</tasks>

<verification>
1. `node scripts/capture-refactor-baseline.mjs --write` completes and writes a snapshot.
2. Baseline doc explains exactly how to recapture and compare metrics.
3. `.github/pull_request_template.md` includes `Refactor Guardrails` checklist items.
</verification>

<success_criteria>
- REFACTOR-01: Baseline metrics are captured and persisted in comparable format.
- REFACTOR-02: Refactor quality gates are codified and usable for every follow-on phase PR.
</success_criteria>

<output>
After completion, create `.planning/phases/46-guardrails-and-baselines/46-01-SUMMARY.md`
</output>
