---
phase: 50-query-layer-decomposition
plan: 02
type: execute
wave: 2
depends_on: [50-01]
files_modified:
  - src/lib/queries.ts
  - src/lib/queries/types.ts
  - src/lib/queries/sanitization.ts
  - src/lib/queries/filters.ts
  - src/lib/queries/builders.ts
  - src/lib/queries.test.ts
  - src/app/api/crimes/range/route.test.ts
autonomous: true

must_haves:
  truths:
    - "Hot-path range and count queries bind user-supplied values as parameters instead of interpolating them into SQL"
    - "Combined optional filters keep placeholder order aligned with parameter arrays"
    - "Range API metadata behavior (`sampleStride`, `sampled`, buffer fields) remains contract-compatible"
  artifacts:
    - path: "src/lib/queries/builders.ts"
      provides: "Parameterized builders for `queryCrimesInRange` and `queryCrimeCount`"
      exports: ["buildCrimesInRangeQuery", "buildCrimeCountQuery"]
    - path: "src/lib/queries/filters.ts"
      provides: "IN-list and range filter fragment composition with ordered params"
      contains: "IN ("
    - path: "src/lib/queries.ts"
      provides: "Public hot-path query functions executing SQL with parameter arrays"
      contains: "db.all"
    - path: "src/lib/queries.test.ts"
      provides: "Regression coverage for placeholder/param ordering and sanitization"
      contains: "queryCrimesInRange"
    - path: "src/app/api/crimes/range/route.test.ts"
      provides: "Contract checks guarding metadata parity for sampled/buffer semantics"
      contains: "sampleStride"
  key_links:
    - from: "src/lib/queries.ts"
      to: "src/lib/queries/builders.ts"
      via: "hot-path exports call parameterized builder outputs"
      pattern: "buildCrimesInRangeQuery|buildCrimeCountQuery"
    - from: "src/lib/queries/builders.ts"
      to: "src/lib/queries/filters.ts"
      via: "builder composition of filter fragments and params"
      pattern: "buildCrimeRangeFilters"
    - from: "src/app/api/crimes/range/route.ts"
      to: "src/lib/queries.ts"
      via: "route metadata computed from query outputs without contract drift"
      pattern: "queryCrimesInRange|queryCrimeCount"
---

<objective>
Migrate high-traffic range/count query paths to parameterized SQL builders while preserving API behavior.

Purpose: Hot paths carry the highest safety and regression risk; binding values first reduces injection surface and protects correctness before deeper query cleanup.
Output: Parameterized builder-backed implementations for `queryCrimesInRange` and `queryCrimeCount`, with regression coverage for placeholder ordering and range route metadata parity.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-query-layer-decomposition/50-CONTEXT.md
@.planning/phases/50-query-layer-decomposition/50-RESEARCH.md
@.planning/phases/50-query-layer-decomposition/50-01-SUMMARY.md
@src/lib/queries.ts
@src/lib/queries/builders.ts
@src/app/api/crimes/range/route.ts
@src/app/api/crimes/range/route.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement parameterized hot-path query builders with ordered fragments</name>
  <files>src/lib/queries/filters.ts, src/lib/queries/builders.ts, src/lib/queries/sanitization.ts, src/lib/queries/types.ts</files>
  <action>Implement `queryCrimesInRange` and `queryCrimeCount` SQL builders that return `{ sql, params }` using composed fragments for required predicates and optional `crimeTypes`/`districts` filters. Generate placeholder lists for dynamic IN clauses and bind every value (`startEpoch`, `endEpoch`, filter values, and safe-clamped scalar controls where bindable). Keep structural SQL parts (`tableName`, CTE layout) behind allow-list/clamp sanitization to avoid non-bindable injection vectors.</action>
  <verify>`npm run typecheck` passes</verify>
  <done>Builders for both hot paths return deterministic SQL + params with placeholder ordering preserved across optional filter combinations.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire hot-path exports to execute parameterized SQL without API signature changes</name>
  <files>src/lib/queries.ts</files>
  <action>Update `queryCrimesInRange` and `queryCrimeCount` in the compatibility facade to call new builders and execute using parameter arrays (with one execution helper if needed to normalize DuckDB call shape). Remove ad-hoc interpolation and quote-escaping for these hot paths. Preserve existing exported signatures, mock-mode behavior, and response record shaping so downstream API contracts stay stable.</action>
  <verify>`npm test -- --run src/lib/queries.test.ts` passes</verify>
  <done>Hot-path public query functions execute bound-parameter SQL while remaining signature-compatible and behavior-compatible for API consumers.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for placeholder ordering and range metadata parity</name>
  <files>src/lib/queries.test.ts, src/app/api/crimes/range/route.test.ts</files>
  <action>Extend tests to assert (a) SQL builders use placeholders for bound values, (b) params arrays align with placeholder order when `crimeTypes` and `districts` are combined, and (c) `/api/crimes/range` metadata behavior (`sampleStride`, `sampled`, and buffer-related fields) stays unchanged for representative request scenarios. Focus on parity contracts rather than implementation details.</action>
  <verify>`npm test -- --run src/lib/queries.test.ts src/app/api/crimes/range/route.test.ts` passes</verify>
  <done>Tests fail on placeholder drift or metadata behavior drift, and pass with the parameterized hot-path implementation.</done>
</task>

</tasks>

<verification>
- Grep of hot-path SQL assembly in `src/lib/queries.ts` no longer shows direct interpolation of `startEpoch`, `endEpoch`, `crimeTypes`, or `districts` into SQL strings.
- `queryCrimesInRange` and `queryCrimeCount` execute via builder-supplied `{ sql, params }` outputs.
- `npm test -- --run src/lib/queries.test.ts src/app/api/crimes/range/route.test.ts` passes.
</verification>

<success_criteria>
- Phase 50 criterion (2) is satisfied for high-traffic paths: parameterized construction replaces ad-hoc interpolation.
- Placeholder/param order drift risk is explicitly covered by tests.
- Range-route consumer-visible metadata behavior remains backward-compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/50-query-layer-decomposition/50-02-SUMMARY.md`
</output>
