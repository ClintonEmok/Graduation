---
phase: 50-query-layer-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/queries.ts
  - src/lib/queries/index.ts
  - src/lib/queries/types.ts
  - src/lib/queries/sanitization.ts
  - src/lib/queries/filters.ts
  - src/lib/queries/aggregations.ts
  - src/lib/queries/builders.ts
autonomous: true

must_haves:
  truths:
    - "Query responsibilities are split into dedicated filter, sanitization, aggregation, and builder modules"
    - "The legacy `src/lib/queries.ts` public API stays import-compatible while internals move behind module boundaries"
    - "Non-bindable SQL values have one explicit sanitization boundary before they reach SQL builders"
  artifacts:
    - path: "src/lib/queries/types.ts"
      provides: "Shared typed fragment contracts for SQL + params composition"
      exports: ["QueryFragment"]
    - path: "src/lib/queries/sanitization.ts"
      provides: "Allow-list and clamp helpers for non-bindable SQL pieces"
      exports: ["sanitizeTableName", "clampPositiveInt"]
    - path: "src/lib/queries/filters.ts"
      provides: "Composable predicate and IN-list fragment builders"
      exports: ["buildCrimeRangeFilters"]
    - path: "src/lib/queries/index.ts"
      provides: "Internal query module re-exports"
      contains: "export *"
    - path: "src/lib/queries.ts"
      provides: "Compatibility facade preserving existing exported query signatures"
      contains: "queryCrimesInRange"
  key_links:
    - from: "src/lib/queries.ts"
      to: "src/lib/queries/index.ts"
      via: "facade delegation for internal query implementation"
      pattern: "from './queries'"
    - from: "src/lib/queries/builders.ts"
      to: "src/lib/queries/sanitization.ts"
      via: "sanitization helpers applied before structural SQL fragments"
      pattern: "sanitize"
---

<objective>
Create the query module decomposition foundation and keep `src/lib/queries.ts` as a compatibility facade.

Purpose: This establishes clean boundaries before hot-path parameterization so refactor risk is contained and behavior parity is easier to verify.
Output: New `src/lib/queries/` module structure (`types`, `sanitization`, `filters`, `aggregations`, `builders`, `index`) with `src/lib/queries.ts` delegating through the new internal boundary.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-query-layer-decomposition/50-CONTEXT.md
@.planning/phases/50-query-layer-decomposition/50-RESEARCH.md
@.planning/phases/48-api-stabilization/48-03-SUMMARY.md
@src/lib/queries.ts
@src/lib/queries.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared query fragment and sanitization boundary modules</name>
  <files>src/lib/queries/types.ts, src/lib/queries/sanitization.ts, src/lib/queries/index.ts</files>
  <action>Create shared query-layer contracts in `types.ts` (typed fragment shape `{ sql, params }` and supporting query option/filter types used internally). Implement `sanitization.ts` with centralized allow-list and clamp helpers for non-bindable SQL parts (table names, limits, stride/resolution bounds). Re-export from `index.ts` to define the internal module surface. Do not change route contracts or exported signatures yet.</action>
  <verify>`npm run typecheck` passes</verify>
  <done>Typed fragment contracts and central sanitization helpers exist and are importable from `src/lib/queries/index.ts`.</done>
</task>

<task type="auto">
  <name>Task 2: Extract filter and aggregation scaffolds into dedicated modules</name>
  <files>src/lib/queries/filters.ts, src/lib/queries/aggregations.ts, src/lib/queries/builders.ts</files>
  <action>Move composable WHERE-fragment logic and aggregation-oriented SQL assembly helpers from `queries.ts` into `filters.ts` and `aggregations.ts`, and create `builders.ts` as the orchestration point that composes fragments + params in order. Keep behavior equivalent to current logic and avoid introducing new SQL semantics in this extraction step; this plan is about decomposition boundaries and ordered param composition primitives.</action>
  <verify>`npm run lint -- src/lib/queries/filters.ts src/lib/queries/aggregations.ts src/lib/queries/builders.ts` passes</verify>
  <done>Dedicated filter/aggregation/builder modules exist and own SQL composition helpers previously embedded in `queries.ts`.</done>
</task>

<task type="auto">
  <name>Task 3: Convert src/lib/queries.ts into a compatibility facade</name>
  <files>src/lib/queries.ts</files>
  <action>Update `src/lib/queries.ts` to preserve existing exported API (`queryCrimesInRange`, `queryCrimeCount`, `queryDensityBins`, `getOrCreateGlobalAdaptiveMaps`, plus exported types) while delegating internals to `src/lib/queries/*` modules. Keep runtime behavior unchanged in this step and retain current API consumer expectations.</action>
  <verify>`npm test -- --run src/lib/queries.test.ts` passes</verify>
  <done>`src/lib/queries.ts` remains import-compatible for callers while internal logic is routed through modular query files.</done>
</task>

</tasks>

<verification>
- `src/lib/queries/` contains `types.ts`, `sanitization.ts`, `filters.ts`, `aggregations.ts`, `builders.ts`, and `index.ts`.
- `src/lib/queries.ts` still exports the same public query functions used by API routes.
- `npm run typecheck` and `npm test -- --run src/lib/queries.test.ts` pass after extraction.
</verification>

<success_criteria>
- Phase 50 criterion (1) is satisfied structurally: query concerns are split into dedicated modules.
- Backward compatibility boundary is preserved through the facade, enabling safe follow-up parameterization.
</success_criteria>

<output>
After completion, create `.planning/phases/50-query-layer-decomposition/50-01-SUMMARY.md`
</output>
