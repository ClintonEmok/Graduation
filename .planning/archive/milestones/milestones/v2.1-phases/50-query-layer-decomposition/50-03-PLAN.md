---
phase: 50-query-layer-decomposition
plan: 03
type: execute
wave: 3
depends_on: [50-02]
files_modified:
  - src/lib/queries.ts
  - src/lib/queries/sanitization.ts
  - src/lib/queries/aggregations.ts
  - src/lib/queries/builders.ts
  - src/lib/queries/index.ts
  - src/lib/queries.test.ts
autonomous: true

must_haves:
  truths:
    - "Adaptive-cache and density query assembly live in dedicated modules instead of the monolithic facade"
    - "Non-bindable SQL structure in aggregation/density paths is constrained by allow-lists and numeric clamps"
    - "Public query exports used by API routes remain backward-compatible after full decomposition"
  artifacts:
    - path: "src/lib/queries/aggregations.ts"
      provides: "Global adaptive cache and density-bin SQL builders"
      exports: ["buildGlobalAdaptiveCacheQueries", "buildDensityBinsQuery"]
    - path: "src/lib/queries/sanitization.ts"
      provides: "Shared clamp/allow-list helpers for cache keys, table names, and resolution-like scalars"
      contains: "allow"
    - path: "src/lib/queries.ts"
      provides: "Thin facade delegating all major query paths to modular builders"
      contains: "queryDensityBins"
    - path: "src/lib/queries.test.ts"
      provides: "Regression checks for aggregation SQL safety boundaries and compatibility exports"
      contains: "queryDensityBins"
  key_links:
    - from: "src/lib/queries.ts"
      to: "src/lib/queries/aggregations.ts"
      via: "facade delegation for density and adaptive-cache query paths"
      pattern: "buildDensityBinsQuery|buildGlobalAdaptiveCacheQueries"
    - from: "src/lib/queries/aggregations.ts"
      to: "src/lib/queries/sanitization.ts"
      via: "resolution and structural SQL token sanitization"
      pattern: "clamp|sanitize"
---

<objective>
Complete query-layer decomposition by moving remaining aggregation/cache logic behind modular builders and safety boundaries.

Purpose: This finishes the refactor by eliminating monolithic query internals while preserving consumer-facing behavior and improving auditability of SQL safety controls.
Output: Aggregation/density/cache logic in dedicated modules, thin compatibility facade, and regression tests guarding compatibility + sanitization boundaries.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-query-layer-decomposition/50-CONTEXT.md
@.planning/phases/50-query-layer-decomposition/50-RESEARCH.md
@.planning/phases/50-query-layer-decomposition/50-02-SUMMARY.md
@src/lib/queries.ts
@src/lib/queries/aggregations.ts
@src/lib/queries/builders.ts
@src/lib/queries.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract density-bin and adaptive-cache SQL assembly into aggregations module</name>
  <files>src/lib/queries/aggregations.ts, src/lib/queries/builders.ts, src/lib/queries/index.ts</files>
  <action>Move remaining SQL construction for `queryDensityBins` and `getOrCreateGlobalAdaptiveMaps` into `aggregations.ts` and expose builders through `builders.ts`/`index.ts`. Keep existing query semantics and result-shaping expectations intact, including domain calculations, smoothing inputs, and cache table usage. Avoid API-level behavior changes; this task is decomposition plus safer construction boundaries.</action>
  <verify>`npm run typecheck` passes</verify>
  <done>Aggregation and adaptive-cache SQL assembly no longer lives directly in the facade file and is exported through modular builders.</done>
</task>

<task type="auto">
  <name>Task 2: Harden non-bindable SQL boundaries in aggregation paths</name>
  <files>src/lib/queries/sanitization.ts, src/lib/queries/aggregations.ts, src/lib/queries.ts</files>
  <action>Apply centralized sanitization helpers for all non-bindable aggregation SQL parts (table names, cache-key-sensitive statements, and resolution/bin-count/kernel-width style scalars where direct binding is not supported). Use explicit allow-lists and strict clamps to prevent structural SQL injection vectors and extreme-value drift. Keep currently expected defaulting behavior and compatibility with existing route consumers.</action>
  <verify>`npm run lint -- src/lib/queries/sanitization.ts src/lib/queries/aggregations.ts src/lib/queries.ts && npm run typecheck` passes</verify>
  <done>Aggregation paths enforce one auditable sanitization boundary for structural SQL pieces while preserving current defaults and signatures.</done>
</task>

<task type="auto">
  <name>Task 3: Add compatibility and safety regression tests for fully decomposed query layer</name>
  <files>src/lib/queries.test.ts</files>
  <action>Expand `src/lib/queries.test.ts` to verify facade export compatibility, parameterized or sanitized SQL construction in decomposed aggregation paths, and parity-critical behavior from previous phases (normalization contract and stable query outputs). Include assertions that would fail if sanitization boundaries are bypassed or if decomposition changes callable behavior expected by API routes.</action>
  <verify>`npm test -- --run src/lib/queries.test.ts` passes</verify>
  <done>Regression tests provide evidence that full decomposition is backward-compatible and safety boundaries remain enforced.</done>
</task>

</tasks>

<verification>
- `src/lib/queries.ts` is a thin compatibility facade rather than a monolithic SQL assembly file.
- Aggregation and adaptive-cache query assembly is owned by dedicated modules under `src/lib/queries/`.
- Sanitization boundaries for non-bindable SQL parts are centralized and covered by tests.
- `npm test -- --run src/lib/queries.test.ts` passes.
</verification>

<success_criteria>
- Phase 50 criteria (1) and (3) are satisfied end-to-end: decomposition is complete and public behavior remains compatible.
- Structural SQL safety is easier to audit due to explicit module boundaries and centralized sanitization.
</success_criteria>

<output>
After completion, create `.planning/phases/50-query-layer-decomposition/50-03-SUMMARY.md`
</output>
