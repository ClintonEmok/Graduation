---
phase: 52-uniform-events-binning-for-timeslicing
plan: 2
type: execute
wave: 2
depends_on: [52-01]
files_modified:
  - src/app/timeslicing/page.tsx
  - src/app/timeslicing/page.binning-mode.test.ts
autonomous: true

must_haves:
  truths:
    - "Timeslicing explicitly requests uniform-events adaptive binning when computing maps"
    - "Non-timeslicing flows keep existing uniform-time behavior because they do not pass a mode override"
    - "Timeslicing map recompute wiring stays stable after mode parameter adoption"
  artifacts:
    - path: "src/app/timeslicing/page.tsx"
      provides: "Explicit mode wiring for timeslicing adaptive map requests"
      contains: "binningMode: 'uniform-events'"
    - path: "src/app/timeslicing/page.binning-mode.test.ts"
      provides: "Regression contract for timeslicing mode intent"
      contains: "uniform-events"
  key_links:
    - from: "src/app/timeslicing/page.tsx"
      to: "src/store/useAdaptiveStore.ts"
      via: "computeMaps third-argument options"
      pattern: 'computeMaps\(.*binningMode: ''uniform-events'''
---

<objective>
Wire the new adaptive binning mode into timeslicing without changing other route behavior.

Purpose: This delivers the user-visible phase goal by enabling event-balanced bins only where requested.
Output: Timeslicing compute call requests `uniform-events` and regression coverage protects that route-specific contract.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-uniform-events-binning-for-timeslicing/52-RESEARCH.md
@.planning/phases/51-store-consolidation/51-10-SUMMARY.md
@src/app/timeslicing/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Request uniform-events mode from timeslicing adaptive recompute</name>
  <files>src/app/timeslicing/page.tsx</files>
  <action>Update the timeslicing `computeMaps` call to pass the new third-argument options object with `binningMode: 'uniform-events'`. Keep all other route call sites unchanged so they continue inheriting the uniform-time default from the store contract.</action>
  <verify>`npm run typecheck` passes and `rg "computeMaps\(" src/app/timeslicing/page.tsx` shows a mode override at the timeslicing call site.</verify>
  <done>Timeslicing explicitly opts into uniform-events while existing callers remain defaulted.</done>
</task>

<task type="auto">
  <name>Task 2: Add route-level regression coverage for explicit timeslicing mode intent</name>
  <files>src/app/timeslicing/page.binning-mode.test.ts</files>
  <action>Add a focused test that asserts timeslicing adaptive compute intent stays `uniform-events` (for example via a small extracted helper or route-local contract function) so future refactors cannot silently drop the mode override. Keep the test pure and deterministic; do not require full page rendering unless already idiomatic.</action>
  <verify>`npm test -- --run src/app/timeslicing/page.binning-mode.test.ts` passes.</verify>
  <done>Automated coverage fails if timeslicing stops requesting uniform-events bins.</done>
</task>

</tasks>

<verification>
- Timeslicing adaptive compute calls include `binningMode: 'uniform-events'`.
- Route-level regression coverage protects the explicit mode request contract.
- Typecheck remains green after route wiring update.
</verification>

<success_criteria>
- Phase 52 criterion (1) is satisfied for `/timeslicing`.
- Phase 52 criterion (3) remains satisfied because other routes keep default mode behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/52-uniform-events-binning-for-timeslicing/52-02-SUMMARY.md`
</output>
