---
phase: 52-uniform-events-binning-for-timeslicing
plan: 3
type: execute
wave: 2
depends_on: [52-01]
files_modified:
  - src/components/viz/MainScene.tsx
  - src/app/api/adaptive/global/route.ts
  - src/lib/queries/types.ts
  - src/lib/queries/aggregations.ts
  - src/lib/queries.ts
  - src/lib/queries.test.ts
autonomous: true

must_haves:
  truths:
    - "Global adaptive precompute can return count and density outputs with the same field shape used by viewport worker maps"
    - "Global adaptive cache keys remain collision-safe across binning modes"
    - "When no mode is specified, global adaptive behavior remains backward-compatible with uniform-time"
  artifacts:
    - path: "src/lib/queries.ts"
      provides: "Mode-aware global adaptive map computation and cache retrieval"
      contains: "global:${safeBinCount}:${safeKernelWidth}:${safeBinningMode}"
    - path: "src/app/api/adaptive/global/route.ts"
      provides: "Mode-aware API contract with default-safe parsing"
      contains: "binningMode"
    - path: "src/components/viz/MainScene.tsx"
      provides: "Global-map hydration that preserves countMap + densityMap parity"
      contains: "setPrecomputedMaps"
  key_links:
    - from: "src/components/viz/MainScene.tsx"
      to: "/api/adaptive/global"
      via: "fetch query parameter"
      pattern: 'adaptive/global\?'
    - from: "src/app/api/adaptive/global/route.ts"
      to: "src/lib/queries.ts"
      via: "getOrCreateGlobalAdaptiveMaps args"
      pattern: 'getOrCreateGlobalAdaptiveMaps\(.*binningMode'
---

<objective>
Make global adaptive precompute/cache mode-aware with parity-safe count and density outputs.

Purpose: This closes scope-consistency risk when adaptive maps are sourced globally and prevents cache collisions across binning modes.
Output: Mode-aware global API/query pipeline returning stable map fields (`densityMap`, `countMap`, `burstinessMap`, `warpMap`) with uniform-time default compatibility.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-uniform-events-binning-for-timeslicing/52-RESEARCH.md
@src/components/viz/MainScene.tsx
@src/app/api/adaptive/global/route.ts
@src/lib/queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend global adaptive query/cache contracts for binning mode and count parity</name>
  <files>src/lib/queries/types.ts, src/lib/queries/aggregations.ts, src/lib/queries.ts</files>
  <action>Update global adaptive types/builders to include `binningMode` with default `uniform-time`, add `countMap` persistence/return fields, and include mode in cache keys to avoid cross-mode reuse collisions. Preserve existing return shape compatibility by keeping all prior fields and only adding parity-safe fields.</action>
  <verify>`npm run typecheck` passes and cache key composition includes mode.</verify>
  <done>Global adaptive query layer computes and caches mode-specific outputs with stable density/count/burst/warp fields.</done>
</task>

<task type="auto">
  <name>Task 2: Thread binning mode through global adaptive API and scene hydration</name>
  <files>src/app/api/adaptive/global/route.ts, src/components/viz/MainScene.tsx</files>
  <action>Parse optional `binningMode` in `/api/adaptive/global` and pass it through to query-layer computation with default `uniform-time`. Update global map fetch/hydration in `MainScene` to request the active mode when available and hydrate `countMap` through `setPrecomputedMaps` so global and viewport contracts remain aligned.</action>
  <verify>`npm run typecheck` passes and global adaptive API responses include `countMap` while preserving existing fields.</verify>
  <done>Global adaptive fetch path is mode-aware, default-safe, and contract-parity aligned with worker/store outputs.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for mode-aware cache/query contract stability</name>
  <files>src/lib/queries.test.ts</files>
  <action>Extend query-layer tests to assert mode-sensitive cache keys/params, stable output contract expectations, and backward-compatible default behavior when mode is omitted. Keep tests at builder/facade level (no DuckDB integration requirement) to stay deterministic.</action>
  <verify>`npm test -- --run src/lib/queries.test.ts` passes.</verify>
  <done>Automated tests catch regressions in mode cache isolation, default fallback, and count/density output parity.</done>
</task>

</tasks>

<verification>
- Global adaptive cache/query keys include mode to prevent collisions.
- `/api/adaptive/global` supports optional mode with `uniform-time` fallback.
- Global map hydration preserves `countMap` + `densityMap` parity and query tests pass.
</verification>

<success_criteria>
- Phase 52 criterion (2) is satisfied for global adaptive precompute pathways.
- Scope-consistency pitfall (global vs viewport mode drift) is closed for this phase.
</success_criteria>

<output>
After completion, create `.planning/phases/52-uniform-events-binning-for-timeslicing/52-03-SUMMARY.md`
</output>
