---
phase: quick
type: execute
quick_id: 260323-47t
autonomous: true

must_haves:
  truths:
    - "User can access all 8M+ crime datapoints from the API"
    - "API accepts higher limit without errors"
    - "Stats page can load full dataset"
---

<objective>
Enable access to the full 8M crime datapoints by removing or increasing the API limit.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Increase API limit for crime range endpoint</name>
  <files>src/app/api/crimes/range/route.ts, src/hooks/useCrimeData.ts</files>
  <action>
1. Check current API limit in `/api/crimes/range/route.ts`
2. Increase or remove the limit parameter to allow 8M+ records
3. Update useCrimeData hook default limit if needed
4. Consider memory implications - the data goes to the frontend so large datasets may need sampling
</action>
  <read_first>
src/app/api/crimes/range/route.ts
src/hooks/useCrimeData.ts
</read_first>
  <verify>`npm run typecheck` succeeds</verify>
  <done>API accepts limit parameter up to 8M+</done>
  <acceptance_criteria>
- API accepts limit parameter
- Stats page can request full dataset
</acceptance_criteria>
</task>

</tasks>
